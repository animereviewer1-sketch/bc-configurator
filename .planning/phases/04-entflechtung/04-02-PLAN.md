---
phase: 04-entflechtung
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/bridge-registry.test.js
  - bridge.js
  - items.js
  - index.html
  - tests/helpers/loadScript.js
  - tests/bridge-protocol.test.js
  - tests/injected-code-origin.test.js
  - tests/load-script.test.js
  - tests/load-order-guard.test.js
  - bot-ui.js
  - docs/LOAD-ORDER.md
autonomous: true
requirements: [SPLIT-02, SPLIT-03]
tags: [bridge, postmessage, handler-registry, extraction, origin-pinning, vm-sandbox, red-green]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SPLIT-02: `bridge.js` besitzt `APP`, `_pingInterval`/`_connected`/`_bcOrigin`, `startPingRetry`, `_lastMsgTs`/`_heartbeatCheck` (+ `setInterval`), `_LAST_MEMBER_KEY`/`_playerChecked`/`_playerAbgelehnt`, `_bridgeSenderOk`, `manualReconnect`, `bcSend` (inkl. Pre-Handshake-Guard und EXEC-Log-Hook, jetzt `typeof _execLogAppend === 'function'`-geguardet) und den EINZIGEN `window.addEventListener('message', …)` des Tools — wörtlich verschoben (Diff-Gates auf Ping-Retry-Block und `_bridgeSenderOk`/`manualReconnect`-Block, Grep-Gates auf jede Sicherheitszeile; Orchestrator-Entscheidung 2, T-4-01, T-4-08)"
    - "SPLIT-02: Registry `onBridgeMessage(type, handler)` / `offBridgeMessage(type, handler)` / `_bridgeDispatch(ev)` in bridge.js; der Listener-Shell (app-Check → `_bridgeSenderOk` → `_playerAbgelehnt` → Origin einmal lernen → `_lastMsgTs` → Bot-Rückschreib-Hook) bleibt wörtlich und ruft statt des `switch` `_bridgeDispatch(ev)`; ein neuer Typ (z. B. `GAME_SCAN_DATA` für Phase 5) registriert sich ohne jede Änderung an items.js (Erfolgskriterium 1)"
    - "SPLIT-03: items.js registriert alle 35 bisherigen `case`-Körper unverändert als `onBridgeMessage('<TYP>', function(ev) { … })` (trailing `break;` entfällt, mittige `break;` werden `return;`); der `debugOsOutfit`-Einmal-Listener ist in die Registry gefaltet (`onBridgeMessage('OUTFIT_DEBUG_RESULT', handler)` + `offBridgeMessage` im Handler, Korrelation über `reqId` bleibt); der Bootstrap-PING der Start-IIFE läuft über `bcSend({ type: 'PING' }, true)`; statisches Gate: items.js enthält 0× `addEventListener('message'`, 0× `window.opener.postMessage(`, 0× `switch (ev.data.type)`, 0× `indexedDB.open(` (Erfolgskriterium 2)"
    - "SPLIT-03: `TOOL_ORIGIN` bleibt in items.js (Phase-3-Entscheidung „einzige Tool-seitige Origin-Definition in items.js“; `tests/injected-code-origin.test.js` bleibt bis auf die Datei-Listen unverändert); EXEC-Log-Block und `_pushCurseDBToBC` bleiben in items.js"
    - "Sicherheit unverändert: Origin-Pinning (Trust-on-first-use), Source-Check, Pre-Handshake-Abweisung, genau EIN direkter Wildcard-Send (Bootstrap-PING in `startPingRetry`, bridge.js) und genau EIN `_bcOrigin || '*'`-Fallback (bcSend) — alle 21 Fälle in tests/bridge-protocol.test.js bleiben grün, nur die zwei Datei-Ort-Aussagen (statischer Audit, Debug-Listener-Zählung) folgen dem Code"
    - "SPLIT-04 (Fortführung): Guard-Tabelle in items.js um `['onBridgeMessage', 'bridge.js']`, `['bcSend', 'bridge.js']` erweitert; bot-ui.js erhält denselben Guard (persistence.js, bridge.js, items.js); index.html lädt `persistence.js → bridge.js → items.js` im `_cbv`-Block, Kommentar und docs/LOAD-ORDER.md nachgezogen; Loader `CORE_SCRIPTS = ['persistence.js', 'bridge.js', 'items.js']`"
  artifacts:
    - path: "bridge.js"
      provides: "Kopfkommentar; `const APP`; Ping-Retry-Block; Heartbeat; Spieler-Check-Zustand; `_bridgeSenderOk`; `manualReconnect`; `bcSend`; Registry (`_bridgeHandlers`, `onBridgeMessage`, `offBridgeMessage`, `_bridgeDispatch`); message-Listener; Dual-Export-Schwanz"
      contains: "function onBridgeMessage(type, handler)"
    - path: "items.js"
      provides: "35 `onBridgeMessage('…', function(ev) {` Registrierungen an der Stelle des alten Listeners; `debugOsOutfit` über Registry; Start-IIFE mit `bcSend({ type: 'PING' }, true)`; Guard-Tabelle mit drei Zeilen; keine eigene Bridge-Logik"
      contains: "onBridgeMessage('PONG', function(ev) {"
    - path: "tests/bridge-registry.test.js"
      provides: "SPLIT-02/03: Registrierung + Dispatch, Mehrfach-Handler in Reihenfolge, `offBridgeMessage`, Selbst-Entfernung während Dispatch, unbekannter Typ, Sicherheitsshell vor der Registry (fremde Source/Origin, falsche app, `_playerAbgelehnt`), neuer Typ ohne items.js, alle 35 Typen aus items.js registriert, Dual-Export, EXEC ohne `_execLogAppend`, statisches SPLIT-03-Gate"
      contains: "GAME_SCAN_DATA"
    - path: "index.html"
      provides: "`document.write('<scr'+'ipt src=\"bridge.js?_='+_cbv+'\"><\\/scr'+'ipt>');` zwischen persistence.js- und items.js-Zeile; Kommentar `LADEREIHENFOLGE` nennt bridge.js"
      contains: "bridge.js?_="
    - path: "tests/helpers/loadScript.js"
      provides: "`CORE_SCRIPTS = ['persistence.js', 'bridge.js', 'items.js']`"
      contains: "'bridge.js'"
    - path: "bot-ui.js"
      provides: "Ladereihenfolge-Guard als erste Anweisung (persistence.js, bridge.js, items.js)"
      contains: "loadOrderFatal"
    - path: "docs/LOAD-ORDER.md"
      provides: "Zeile für bridge.js (stellt `bcSend`, `onBridgeMessage`, `offBridgeMessage`, `_bridgeSenderOk`, `manualReconnect` bereit; braucht nichts zur Parse-Zeit, zur Laufzeit `showStatus`/`stopRoomScan`/`_execLogAppend` aus items.js — alle geguardet oder erst nach Handshake erreichbar); Guard-Hinweis für bot-ui.js; Beispiel „neuen Nachrichtentyp registrieren“"
      contains: "bridge.js"
  key_links:
    - from: "bridge.js message-Listener"
      to: "items.js Handler-Körper"
      via: "`_bridgeDispatch(ev)` → `_bridgeHandlers.get(ev.data.type)` → jeder registrierte Handler mit `ev`; Sicherheitsshell davor unverändert"
      pattern: "_bridgeDispatch\\(ev\\)"
    - from: "items.js Handler-Registrierungen"
      to: "bridge.js `onBridgeMessage`"
      via: "35 Top-Level-Aufrufe zur Parse-Zeit von items.js — bridge.js muss davor geladen sein (Guard-Zeile `['onBridgeMessage', 'bridge.js']`)"
      pattern: "^onBridgeMessage\\('"
    - from: "bridge.js `bcSend`"
      to: "items.js `_execLogAppend` (STAB-08)"
      via: "`if (msg.type === 'EXEC' && typeof _execLogAppend === 'function') _execLogAppend(msg);` — einzige inhaltliche Zeilenänderung in bcSend"
      pattern: "typeof _execLogAppend === 'function'"
    - from: "items.js `debugOsOutfit`"
      to: "bridge.js Registry"
      via: "`onBridgeMessage('OUTFIT_DEBUG_RESULT', handler)`; Handler prüft nur `reqId` und ruft `offBridgeMessage(...)`; app/Absender prüft die Shell"
      pattern: "offBridgeMessage\\('OUTFIT_DEBUG_RESULT', handler\\)"
    - from: "index.html `_cbv`-Block"
      to: "bridge.js"
      via: "`document.write` NACH persistence.js und VOR items.js"
      pattern: "bridge\\.js\\?_="
  prohibitions:
    - requirement_id: SPLIT-02
      category: security
      verification: automated
      statement: "Origin-Pinning bleibt zeichengenau: `if (!window.opener || ev.source !== window.opener) return false;`, `if (_bcOrigin && ev.origin !== _bcOrigin) return false;`, `if (!_bcOrigin && ev.origin && ev.origin !== 'null') _bcOrigin = ev.origin;`, `if (!_bcOrigin && msg.type !== 'PING') {` je genau 1× in bridge.js, 0× in items.js; kein neuer Wildcard-Send (bridge.js genau 1× Bootstrap-PING, items.js 0×)"
    - requirement_id: SPLIT-03
      category: safety
      verification: automated
      statement: "Kein Handler-Körper wird inhaltlich geändert (nur `break;`→`return;`/Entfall); Anzahl `bcSend({ type: 'EXEC'`-Aufrufstellen in items.js vor/nach identisch; loader.js, bot-engine.js, bot-data.js, money.js, rank.js, shop.js, inventar.js, outfit-import.js, bc-autobackup.js unberührt"
    - requirement_id: SPLIT-03
      category: safety
      verification: automated
      statement: "`TOOL_ORIGIN` bleibt in items.js (`grep -c 'const TOOL_ORIGIN = window.location.origin;' items.js` = 1, bridge.js = 0); `location.origin` kommt in items.js+bridge.js+persistence.js+bot-engine.js+bot-ui.js genau 1× vor"
    - requirement_id: SPLIT-02
      category: safety
      verification: judgment
      statement: "Kein try/catch um Handler-Aufrufe (verbatim-Semantik: ein werfender Handler bricht wie bisher den Listener ab — dokumentiert im bridge.js-Kopf); keine Korrelations-ID-Logik in bridge.js; `debugOsOutfit`-Semantik bleibt reqId-korreliert und selbst-entfernend"
    - requirement_id: SPLIT-03
      category: safety
      verification: judgment
      statement: "`git add` nur mit explizit genannten Pfaden; RED-Test und GREEN-Implementierung in getrennten Commits; `vitest run` ohne Watch; `.planning/milestone.lock` nie stagen"
---

<objective>
Zweiter Entflechtungsschritt: Das postMessage-Protokoll zieht wörtlich aus items.js nach `bridge.js` — Verbindungs-Zustand, Ping-Retry, Heartbeat, Spieler-Check-Zustand, `_bridgeSenderOk`, `manualReconnect`, `bcSend` und der einzige `message`-Listener — und bekommt eine Handler-Registry `onBridgeMessage(type, handler)` / `offBridgeMessage(type, handler)`. Die 35 Handler-Körper bleiben in items.js und werden dort registriert (Orchestrator-Entscheidung 2, geringstes Risiko); der `debugOsOutfit`-Einmal-Listener wird in die Registry gefaltet (`offBridgeMessage` im Handler); der Bootstrap-PING der Start-IIFE läuft über `bcSend`. Danach enthält items.js keine eigene IDB- oder postMessage-Logik mehr (statisches Gate). Abschließend wird der Ladereihenfolge-Guard aus Plan 04-01 um bridge.js erweitert (items.js, bot-ui.js), index.html/docs nachgezogen.

Purpose: Erfolgskriterien 1 und 2 der Phase — neue Nachrichtentypen (Phase 5 `GAME_SCAN_DATA`) brauchen keine items.js-Änderung; die einzige Vertrauensgrenze des Tools (Origin-/Source-Prüfung, Phase 3) liegt in einer 250-Zeilen-Datei statt in einem 11.800-Zeilen-Monolithen und bleibt zeichengenau erhalten (T-4-08).
Output: `bridge.js` (neu), `items.js` (Bridge-Blöcke raus, 35 Registrierungen rein, Guard-Tabelle +2), `tests/bridge-registry.test.js` (neu), angepasste Datei-Ort-Aussagen in `tests/bridge-protocol.test.js`, `tests/injected-code-origin.test.js`, `tests/load-script.test.js`, `tests/load-order-guard.test.js`, `tests/helpers/loadScript.js` (`CORE_SCRIPTS`), `index.html` (+1 Write-Zeile, Kommentar aktualisiert), `bot-ui.js` (Guard), `docs/LOAD-ORDER.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-entflechtung/04-RESEARCH.md
@.planning/phases/04-entflechtung/04-VALIDATION.md
@.planning/phases/04-entflechtung/04-01-SUMMARY.md
@.planning/phases/03-bridge-haertung/03-01-SUMMARY.md
@.planning/phases/03-bridge-haertung/03-03-SUMMARY.md

Planer-Probe (2026-09-14, HEAD `9b41340`; Zeilennummern gelten VOR Plan 04-01 — nach 04-01 liegen sie ~80 Zeilen tiefer, deshalb ausschließlich per Anker/grep arbeiten) — verbindliche Befunde:
1. Bridge-Region in items.js: Block A = 4 Zeilen (`// ══…`, `//  POSTMESSAGE KOMMUNIKATION MIT BC`, `// ══…`, `const APP = 'BCKonfigurator';`); direkt danach 4 Kommentarzeilen + `const TOOL_ORIGIN = window.location.origin;` — BLEIBEN in items.js; Block B = von `// ── Ping-Retry ───` bis einschließlich `let _playerAbgelehnt = false;` (51 Zeilen: `_pingInterval`, `_connected`, `_bcOrigin`, `startPingRetry` mit dem Bootstrap-PING an `'*'`, Heartbeat-Kommentar, `_lastMsgTs`, `_heartbeatCheck`, `setInterval(_heartbeatCheck, 5000);`, Spieler-Check-Kommentar, `_LAST_MEMBER_KEY`, `_playerChecked`, Kommentar, `_playerAbgelehnt`); dann EXEC-Log-Block (`// ── EXEC-Log (STAB-08)` bis zum `try { … _loadExecLog() … } catch (e) {}`) — BLEIBT; Block C = von `// Absender-Doppelprüfung (STAB-06, Tool-Seite)` über `function _bridgeSenderOk(ev)`, `function manualReconnect()` bis zum schließenden `}` von `function bcSend(msg, silent)`; dann `/* Den lokalen Curse-Bestand …` + `function _pushCurseDBToBC()` — BLEIBT; Block D = `window.addEventListener('message', function(ev) {` … `});` (Shell + `switch (ev.data.type) {` + 35 `case` + `}` + `});`), unmittelbar gefolgt von `function loadCacheFromBC()`.
2. Die 35 `case`-Typen in Reihenfolge: PONG, CACHE_DATA, POS_DATA, PLAYER_DATA, BOT_EV_STATUS, BOT_LOG, BOT_MONEY, BOT_SHOP, BOT_RANG, BOT_SET_ZONE, BOT_VAR, BOT_PROBE, BOT_KEYBERICHT, BOT_INVENTAR, BOT_MAPKEY, RANG_INIT, MONEY_INIT_NEW, MONEY_QUERY, BOT_ROOM_EVER, EXEC_OK, EXEC_ERR, CURSE_DATA, LSCG_CACHE_DATA, CRAFT_CACHE_DATA, WEAR_CURSE_OK, WEAR_CURSE_ERR, CT_CHAT_MSG, CHAR_APPEARANCE_DATA, DEFAULT_OUTFIT_DATA, SCREENSHOT_DATA, CANVAS_PREVIEW_DATA, OUTFIT_SCAN_DATA, LSCG_OUTFITS_DATA, MBS_WHEEL_DATA, LOCKS_DATA (RESEARCH nennt „33“ — verifiziert sind 35). Jeder `case` endet mit `break;`, kein Fallthrough. Mittige Abbrüche: PLAYER_DATA (ein tief eingerücktes `break;` im `confirm`-Zweig), CHAR_APPEARANCE_DATA (`if (!_cb) break;`, `… break; }`), DEFAULT_OUTFIT_DATA (`if (ev.data.reqId !== _pendingDefaultOutfitCapture) break;`, `… break; }`); CACHE_DATA nutzt bereits `return;`. Kein `break` innerhalb einer Schleife im gesamten Switch (die `for`-Schleife in CRAFT_CACHE_DATA hat keines) — jedes `break;` ist ein Switch-Abbruch → `return;` in einer Funktion ist äquivalent; ein vergessenes Top-Level-`break` meldet `node --check` als SyntaxError.
3. `debugOsOutfit` (`window.debugOsOutfit = function(mk, vIdx)`): `const handler = function(ev) { if (!ev.data || ev.data.app !== 'BCKonfigurator' || ev.data.type !== 'OUTFIT_DEBUG_RESULT' || ev.data.reqId !== reqId || !_bridgeSenderOk(ev)) return; window.removeEventListener('message', handler); … }; window.addEventListener('message', handler);` — zweiter Listener, wird gefaltet (Entscheidung 2: „prefer folding“; die Shell prüft app/Absender bereits; neu gilt zusätzlich `_playerAbgelehnt` und `_lastMsgTs` wird gesetzt — für einen Konsolen-Debugbefehl unbedenklich, im SUMMARY als bewusste Abweichung festhalten).
4. Start-IIFE (`// ── Auto-load + initial PING ──`): `if (window.opener && !window.opener.closed) { console.log('[BCK-Popup] Sende ersten PING...'); window.opener.postMessage({ app: APP, type: 'PING' }, '*'); }` → wird `bcSend({ type: 'PING' }, true);` (bcSend lässt PING vor dem Handshake an `'*'` durch, loggt PING bei `silent` nicht) — einzige postMessage-Zeile, die in items.js sonst verbliebe. `APP` wird danach in items.js nirgends mehr referenziert (`const APP` zieht nach bridge.js; klassische Skripte teilen den globalen lexikalischen Scope, `let`/`const` inklusive — daher funktionieren `_connected = true`, `_playerAbgelehnt = true`, `_playerChecked = …` in den items.js-Handlern weiterhin gegen die bridge.js-Deklarationen, ebenso in der vm-Sandbox, wo alle Dateien per `runInContext` in EINEM Kontext laufen).
5. Bestehende Tests mit Datei-Ort-Aussagen, die dem Code folgen müssen (GREEN-Commit): (a) `tests/bridge-protocol.test.js` „auch der temporäre Debug-Listener von debugOsOutfit prüft den Absender“ zählt `ctx._listeners.get('message').length` (+1/−1) → künftig Registry-Zählung `evalIn(ctx, "(_bridgeHandlers.get('OUTFIT_DEBUG_RESULT') || []).length")`; „statischer Audit: genau zwei direkte Wildcard-Sends …“ liest items.js → künftig bridge.js genau 1 (PING) + items.js 0, Fallback `_bcOrigin || '*'` bridge.js 1 / items.js 0. (b) `tests/injected-code-origin.test.js`: `it('items.js: genau 2 Wildcard-Zeilen …')` (Regex `WILD = /,\s*['"]\*['"]\)/`) → bridge.js genau 1, items.js 0; `it.each` um `['items.js', 0]`, `['persistence.js', 0]`; „kein GitHub-Pages-Origin-String“-Schleife und `combined` in „Tool-Origin ist genau einmal definiert“ um bridge.js/persistence.js erweitern (Zählung bleibt 1 — `TOOL_ORIGIN` bleibt in items.js). (c) `tests/load-script.test.js` „items.js registriert seinen message-Handler in der Registry“ → bridge.js registriert genau EINEN (`toBe(1)`). Alle übrigen Bridge-Fälle (Origin lernen, fremde Source/Origin, PONG-Dispatch, BOT_LOG/POS_DATA-Durchreichung, EXEC_OK/ERR, unbekannter Typ, bcSend-Pfade, Heartbeat, Reconnect) bleiben unverändert grün — das ist der Regressionsbeweis für T-4-08.
6. `bcSend`-Aufrufstellen: items.js 72× `bcSend(`, bot-ui.js 8, bot-engine.js 5, inventar.js 3, outfit-import.js 3, bot-data.js 2 — keine davon ändert sich. `_connected` wird in bot-ui.js/bot-data.js gelesen (typeof-geguardet oder erst nach Klick) — unverändert.
7. Loader nach 04-01: `CORE_SCRIPTS = ['persistence.js', 'items.js']`, `expandLoadOrder` fügt fehlende Vorläufer vor dem ersten `items.js` ein. Nach diesem Plan: `['persistence.js', 'bridge.js', 'items.js']` — alle 16 `loadScript(['items.js'…])`-Stellen bekommen bridge.js automatisch. Sandbox-`_listeners` sammelt `addEventListener('message')` aus bridge.js genauso wie zuvor aus items.js; `dispatchMessage` funktioniert unverändert.
8. Guard nach 04-01: items.js beginnt mit dem Guard-IIFE und der Tabelle `[['idbGet', 'persistence.js']]`; index.html enthält den Kommentar `<!-- LADEREIHENFOLGE (nicht umstellen): … persistence.js → items.js → … -->` und die persistence.js-Write-Zeile vor items.js; `docs/LOAD-ORDER.md` existiert. bot-ui.js beginnt mit `function renderBotTab() {` (kein Guard).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry-Test schreiben und RED bestätigen — `tests/bridge-registry.test.js` (SPLIT-02, SPLIT-03, Wave 0)</name>
  <precondition>Plan 04-01 abgeschlossen (`npm test` = 16 Dateien grün, `CORE_SCRIPTS` exportiert, `docs/LOAD-ORDER.md` vorhanden); `git status --short` außer `.planning/milestone.lock` leer</precondition>
  <files>tests/bridge-registry.test.js</files>
  <read_first>tests/bridge-protocol.test.js (gesamt — `boot`/`mkOpener`/`msg`/`send`/`handshake`, `evalIn` für `let`-Globals, statischer Audit); tests/helpers/loadScript.js (`dispatchMessage`, `loadScript`-Expansion); items.js Bridge-Region per `grep -n "POSTMESSAGE KOMMUNIKATION\|^function bcSend\|^window.addEventListener('message'\|^function loadCacheFromBC\|window.debugOsOutfit" items.js` (Shell und Switch lesen, Befund 1-3); .planning/phases/04-entflechtung/04-RESEARCH.md „Pattern 2: Handler-registry bridge“</read_first>
  <action>
    `tests/bridge-registry.test.js` (neu, ESM): Imports `describe, it, expect, vi` aus vitest, `fs`/`path`, `loadScript, evalIn, dispatchMessage, makeElementStub, REPO_ROOT` aus `./helpers/loadScript.js`. Kopfkommentar (Deutsch): SPLIT-02/03 — bridge.js besitzt Listener + Registry; items.js registriert nur noch; neue Typen ohne items.js-Änderung. Konstanten wie in bridge-protocol.test.js (`BC`, `EVIL`, `APP = 'BCKonfigurator'`), Helfer `mkOpener()`, `msg(type, extra)`, `send(ctx, data, origin, source)`, `boot(files, extra)` → `loadScript(files, { opener, setTimeout: () => 0, clearTimeout: () => {}, logPush: vi.fn(), _handlePosData: vi.fn(), ...extra })` mit `document.getElementById`-Override auf `{ connStatus, statusMsg }`-Stubs; `handshake(ctx, opener)` sendet PONG von `BC`/opener und leert den Mock; `count(ctx, type)` → `evalIn(ctx, "(_bridgeHandlers.get('" + type + "') || []).length")`; `src(file)`; `TYPES` = die 35 Typen aus Befund 2 in dieser Reihenfolge.

    `describe('Registry-Vertrag: onBridgeMessage / offBridgeMessage / Dispatch (SPLIT-02)')` — alle mit `boot(['items.js'])` (expandiert automatisch, sobald bridge.js in CORE_SCRIPTS steht) und `handshake`:
    1. `it('onBridgeMessage registriert; Dispatch ruft den Handler mit dem Event')`: `fn = vi.fn(); ctx.onBridgeMessage('TEST_A', fn); send(ctx, msg('TEST_A', { x: 1 }), BC, opener)` → `fn` einmal aufgerufen; `fn.mock.calls[0][0].data.x === 1`; `.origin === BC`; `count(ctx, 'TEST_A') === 1`.
    2. `it('mehrere Handler desselben Typs laufen in Registrierungsreihenfolge')`: `order = []`, zwei Handler pushen `'a'`/`'b'` → nach Send `order` = `['a', 'b']`.
    3. `it('offBridgeMessage entfernt genau den übergebenen Handler und meldet Erfolg')`: zwei Handler; `ctx.offBridgeMessage('TEST_C', fnA)` → `true`; nochmal → `false`; `ctx.offBridgeMessage('NIE', fnA)` → `false`; Send → nur `fnB` läuft; `count === 1`.
    4. `it('ein Handler darf sich während des Dispatch selbst entfernen, ohne die anderen zu überspringen')`: `fnA` ruft `ctx.offBridgeMessage('TEST_D', fnA)`; `fnB` danach registriert; Send → beide je 1×; `count === 1`.
    5. `it('unbekannter Typ: kein Throw, kein Handler, _lastMsgTs aktualisiert (Shell-Semantik unverändert)')`: `evalIn(ctx, '_lastMsgTs = 0')`; `expect(() => send(ctx, msg('NOPE'), BC, opener)).not.toThrow()`; `evalIn(ctx, '_lastMsgTs') > 0`; `count(ctx, 'NOPE') === 0`.

    `describe('Sicherheitsshell liegt VOR der Registry (STAB-04/06 bleiben, T-4-08)')`:
    6. `it('fremde Source: Handler wird nicht gerufen')`: nach Handshake `send(ctx, msg('TEST_S'), BC, {})` → `fn` nicht gerufen.
    7. `it('fremder Origin nach Handshake: Handler wird nicht gerufen, _bcOrigin bleibt')`: `send(..., EVIL, opener)` → nicht gerufen; `evalIn(ctx, '_bcOrigin') === BC`.
    8. `it('falsche app: Handler wird nicht gerufen')`: `send(ctx, { app: 'x', type: 'TEST_S' }, BC, opener)` → nicht gerufen.
    9. `it('_playerAbgelehnt: Handler wird nicht gerufen')`: `evalIn(ctx, '_playerAbgelehnt = true')` → Send → nicht gerufen; `evalIn(ctx, '_playerAbgelehnt = false')` → Send → gerufen.

    `describe('Neuer Nachrichtentyp ohne items.js (Erfolgskriterium 1)')`:
    10. `it('bridge.js allein: GAME_SCAN_DATA registrieren und empfangen')`: `{ ctx, opener } = boot(['persistence.js', 'bridge.js'])` (kein items.js!); `send(ctx, msg('PONG'), BC, opener)` (Handshake — kein PONG-Handler registriert, darf nicht werfen); `evalIn(ctx, '_bcOrigin') === BC`; `fn = vi.fn(); ctx.onBridgeMessage('GAME_SCAN_DATA', fn); send(ctx, msg('GAME_SCAN_DATA', { chunk: 1 }), BC, opener)` → `fn.mock.calls[0][0].data.chunk === 1`; `typeof ctx.bcSend === 'function'`; `ctx.bcSend({ type: 'GET_SCAN' }, true) === true` und `opener.postMessage.mock.calls.at(-1)` = `[{ app: APP, type: 'GET_SCAN' }, BC]`.
    11. `it('bcSend loggt EXEC nur, wenn _execLogAppend existiert (bridge.js allein wirft nicht)')`: gleiche Sandbox ohne items.js; nach Handshake `expect(() => ctx.bcSend({ type: 'EXEC', code: '1' }, true)).not.toThrow()` → `true`; `opener.postMessage.mock.calls.at(-1)[0].type === 'EXEC'`.
    12. `it('Dual-Export: module.exports liefert die Bridge-API')`: `const module = { exports: {} }; loadScript(['bridge.js'], { module, setTimeout: () => 0, clearTimeout: () => {} })`; `Object.keys(module.exports).sort()` enthält `'APP'`, `'_bridgeSenderOk'`, `'bcSend'`, `'offBridgeMessage'`, `'onBridgeMessage'`, `'manualReconnect'`, `'startPingRetry'`, `'_heartbeatCheck'` (exakt diese acht: `toEqual([...].sort())`).

    `describe('items.js registriert alle bisherigen Typen (SPLIT-03)')`:
    13. `it.each(TYPES)('%s ist registriert', (type) => …)`: `count(ctx, type) >= 1` (Sandbox einmal in `beforeAll` booten).
    14. `it('genau 35 Typen aus dem alten Switch plus OUTFIT_DEBUG_RESULT (dynamisch)')`: `TYPES.length === 35`; `count(ctx, 'OUTFIT_DEBUG_RESULT') === 0` vor `debugOsOutfit`, `=== 1` nach `evalIn(ctx, "LSCG_DB['1'] = { versions: [{ code: 'abc' }] }"); ctx.debugOsOutfit('1', 0)`; nach `send(ctx, msg('OUTFIT_DEBUG_RESULT', { reqId, total: 0, missing: [], naked: [], missingNaked: [], assetFamily: 'F' }), BC, opener)` (reqId aus `opener.postMessage.mock.calls` per `/reqId:"(dbg_\d+)"/` wie in bridge-protocol.test.js) wieder `=== 0`.

    `describe('Statisches Gate: items.js ohne eigene Bridge-/IDB-Logik (SPLIT-03)')`:
    15. `it('items.js: 0× addEventListener(\'message\'), 0× window.opener.postMessage(, 0× switch (ev.data.type), 0× indexedDB.open(, 0× function bcSend')`: jeweils `count(src('items.js'), needle) === 0` für `"addEventListener('message'"`, `'window.opener.postMessage('`, `'switch (ev.data.type)'`, `'indexedDB.open('`, `'function bcSend('`, `'function _bridgeSenderOk('`, `'function startPingRetry('`, `'function _heartbeatCheck('`, `'function manualReconnect('`, `"const APP = 'BCKonfigurator';"`.
    16. `it('bridge.js: je genau einmal Listener, Registry, bcSend, Absenderprüfung, Sicherheitszeilen')`: `count(src('bridge.js'), needle) === 1` für `"window.addEventListener('message'"`, `'function onBridgeMessage(type, handler)'`, `'function offBridgeMessage(type, handler)'`, `'function _bridgeDispatch(ev)'`, `'function bcSend(msg, silent)'`, `'function _bridgeSenderOk(ev)'`, `'if (!window.opener || ev.source !== window.opener) return false;'`, `'if (_bcOrigin && ev.origin !== _bcOrigin) return false;'`, `"if (!_bcOrigin && ev.origin && ev.origin !== 'null') _bcOrigin = ev.origin;"`, `"if (!_bcOrigin && msg.type !== 'PING') {"`, `"window.opener.postMessage({ app: APP, ...msg }, _bcOrigin || '*');"`, `"if (msg.type === 'EXEC' && typeof _execLogAppend === 'function') _execLogAppend(msg);"`, `'if (_playerAbgelehnt) return;'`, `'_bridgeDispatch(ev);'`, `'setInterval(_heartbeatCheck, 5000);'`; und `count(src('bridge.js'), 'switch (ev.data.type)') === 0`; `count(src('bridge.js'), 'const TOOL_ORIGIN') === 0`.
    17. `it('items.js: 35 Top-Level-Registrierungen in der alten Reihenfolge + Debug-Fold + PING über bcSend')`: `lines = src('items.js').split('\n')`; für jeden Typ genau eine Zeile `onBridgeMessage('<TYP>', function(ev) {` am Zeilenanfang; die Zeilenindizes sind streng aufsteigend in TYPES-Reihenfolge; `count(src('items.js'), "onBridgeMessage('OUTFIT_DEBUG_RESULT', handler)") === 1`; `count(src('items.js'), "offBridgeMessage('OUTFIT_DEBUG_RESULT', handler)") === 1`; `count(src('items.js'), "bcSend({ type: 'PING' }, true)") === 1`; `count(src('items.js'), 'const TOOL_ORIGIN = window.location.origin;') === 1`.

    RED bestätigen: `npx vitest run tests/bridge-registry.test.js` MUSS rot sein — erwartet `ctx.onBridgeMessage is not a function`, `ENOENT … bridge.js`, `_bridgeHandlers is not defined`, statische Zählungen rot. Grün → STOPP.

    Commit: `git add tests/bridge-registry.test.js`; Message `test(04-02): add failing bridge registry tests – onBridgeMessage/offBridgeMessage, security shell before dispatch, new type without items.js, static SPLIT-03 gate (SPLIT-02, SPLIT-03)`. `git status --short items.js index.html tests/helpers/loadScript.js` leer.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/bridge-registry.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "onBridgeMessage|bridge\.js|_bridgeHandlers" "$OUT" && grep -Eq "Tests +[0-9]+ failed|Test Files +1 failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Vitest endete mit Exit 0 (Registry existiert bereits), oder die Ausgabe nennt weder `onBridgeMessage` noch `bridge.js`/`_bridgeHandlers` noch eine `failed`-Zeile</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "onBridgeMessage" tests/bridge-registry.test.js` >= 10; `grep -c "offBridgeMessage" tests/bridge-registry.test.js` >= 5; `grep -c "GAME_SCAN_DATA" tests/bridge-registry.test.js` >= 2; `grep -c "_bridgeHandlers" tests/bridge-registry.test.js` >= 1
    - `grep -c "'LOCKS_DATA'" tests/bridge-registry.test.js` >= 1 und `grep -c "'PONG'" tests/bridge-registry.test.js` >= 1 (TYPES-Liste vollständig: `grep -o "'[A-Z_]*'" tests/bridge-registry.test.js | sort -u | wc -l` >= 35)
    - `grep -Fc "typeof _execLogAppend === 'function'" tests/bridge-registry.test.js` = 1; `grep -Fc "switch (ev.data.type)" tests/bridge-registry.test.js` >= 1; `grep -Fc "bcSend({ type: 'PING' }, true)" tests/bridge-registry.test.js` = 1
    - `test "$(grep -v '^\s*//' tests/bridge-registry.test.js | grep -c '\.only(')" = 0`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/bridge-registry.test.js`; `git status --short items.js index.html` leer
  </acceptance_criteria>
  <done>17 Fälle in fünf describe-Blöcken; Lauf nachweislich rot, weil bridge.js und die Registry fehlen; roter Test committet; Produktionsdateien unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `bridge.js` wörtlich extrahieren + Registry, items.js registriert 35 Handler, Debug-Listener falten, Start-PING über bcSend, index.html/Loader nachziehen — GREEN (SPLIT-02, SPLIT-03)</name>
  <precondition>Task 1 committet; `git status --short` außer `.planning/milestone.lock` leer; `PRE=$(git rev-parse HEAD)` notiert (Basis für die Diff-Gates); `grep -c "^    case '" items.js` innerhalb des message-Listeners = 35 (per awk zwischen `^window.addEventListener('message'` und `^function loadCacheFromBC`)</precondition>
  <reversibility rating="reversible">Reiner Dateischnitt plus Registry-Indirektion; kein Protokoll-, Schema- oder Datenformatwechsel; `git revert` stellt den Switch wieder her.</reversibility>
  <files>bridge.js, items.js, index.html, tests/helpers/loadScript.js, tests/bridge-protocol.test.js, tests/injected-code-origin.test.js, tests/load-script.test.js</files>
  <read_first>items.js Bridge-Region vollständig (per Anker aus Befund 1: von `//  POSTMESSAGE KOMMUNIKATION MIT BC` bis `function loadCacheFromBC()`), `window.debugOsOutfit` (per grep) und die Start-IIFE (`// ── Auto-load + initial PING`); tests/bridge-registry.test.js (Task 1, Fälle 15-17 definieren die exakten Zeilen); tests/bridge-protocol.test.js Fälle „Debug-Listener“ und „statischer Audit“; tests/injected-code-origin.test.js Block „Statischer Quell-Audit“ und „Tool-Origin ist genau einmal definiert“; tests/load-script.test.js „items.js registriert seinen message-Handler“; tests/helpers/loadScript.js `CORE_SCRIPTS`; index.html `_cbv`-Block (`grep -n 'persistence.js?_=' index.html`)</read_first>
  <behavior>
    - Test 1-5: Registrierung, Reihenfolge, `offBridgeMessage`-Rückgabe, Selbst-Entfernung (Dispatch iteriert über eine Kopie), unbekannter Typ
    - Test 6-9: Shell (Source, Origin, app, `_playerAbgelehnt`) sitzt vor `_bridgeDispatch`
    - Test 10-12: bridge.js allein registriert/empfängt `GAME_SCAN_DATA`, `bcSend` funktioniert ohne `_execLogAppend`, Dual-Export liefert genau acht Einträge
    - Test 13-14: alle 35 Typen registriert; `OUTFIT_DEBUG_RESULT` dynamisch 0→1→0
    - Test 15-17: statische Gates (items.js ohne Bridge-Logik; bridge.js mit allen Sicherheitszeilen genau 1×; 35 Registrierungen in alter Reihenfolge)
    - Alle 21 Fälle in tests/bridge-protocol.test.js, 11 in exec-log, 15 in injected-code-origin bleiben grün (zwei Datei-Ort-Aussagen folgen dem Code); volle Suite 17 Dateien grün
  </behavior>
  <action>
    1. **bridge.js anlegen** (Write-Tool). Reihenfolge und Inhalt:
       a. Kopfkommentar (Deutsch, `// ══…`-Stil): „bridge.js — postMessage-Protokoll zum Spiel-Tab (SPLIT-02). Wörtlich aus items.js verschoben (Stand Plan 04-02): APP, Ping-Retry, Heartbeat, Spieler-Check-Zustand, `_bridgeSenderOk`, `manualReconnect`, `bcSend`, message-Listener. Neu: Handler-Registry `onBridgeMessage`/`offBridgeMessage`. Lädt NACH persistence.js und VOR items.js (docs/LOAD-ORDER.md). Einzige Vertrauensgrenze des Tools: Origin-/Source-Prüfung (Phase 3) liegt hier. Registrieren können nur Skripte dieser Seite (same-origin, klassischer Script-Scope) — T-4-03. Ein werfender Handler bricht wie bisher den Listener ab (kein try/catch, verbatim-Semantik). Zur Laufzeit erwartete items.js-Globals: `showStatus`, `stopRoomScan`, `_execLogAppend` (geguardet), `_botRueckschreibStart` (geguardet).“
       b. `const APP = 'BCKonfigurator';` (aus Block A).
       c. Block B **byte-identisch** (per awk aus items.js extrahieren: von `^// ── Ping-Retry` bis `^let _playerAbgelehnt = false;` inklusive — nicht abtippen).
       d. Block C byte-identisch (von `^// Absender-Doppelprüfung` bis zum schließenden `}` von `bcSend`), mit GENAU EINER Zeilenänderung in `bcSend`: `if (msg.type === 'EXEC') _execLogAppend(msg); // STAB-08: …` → `if (msg.type === 'EXEC' && typeof _execLogAppend === 'function') _execLogAppend(msg); // STAB-08: einziger Sendepfad = einziger Log-Hakenpunkt (Hook lebt in items.js)`.
       e. Registry-Block mit Sektionskommentar `// ── Handler-Registry (SPLIT-02) ──…`: `const _bridgeHandlers = new Map();   // type → Array<function(ev)>`; `function onBridgeMessage(type, handler)`: bei `typeof handler !== 'function'` `throw new TypeError('onBridgeMessage: handler muss eine Funktion sein (' + type + ')')`; Liste anlegen falls fehlend; `push`; `return handler`. `function offBridgeMessage(type, handler)`: Liste holen; `indexOf`; bei Treffer `splice(idx, 1)` und `return true`, sonst `return false`. `function _bridgeDispatch(ev)`: `const list = _bridgeHandlers.get(ev.data.type); if (!list || !list.length) return 0; for (const fn of list.slice()) fn(ev); return list.length;` (Kopie per `slice()` → Selbst-Entfernung sicher).
       f. Listener: Block D-Shell byte-identisch von `window.addEventListener('message', function(ev) {` bis einschließlich der Zeile `typeof _botRueckschreibStart === 'function') _botRueckschreibStart();`, dann statt `switch (ev.data.type) { … }` genau `_bridgeDispatch(ev);` und `});`.
       g. Export-Schwanz: `if (typeof module !== 'undefined' && module.exports) { module.exports = { APP, onBridgeMessage, offBridgeMessage, bcSend, _bridgeSenderOk, manualReconnect, startPingRetry, _heartbeatCheck }; }`.
    2. **items.js umbauen** (Edit-Tool, exakte Anker):
       a. Block A (4 Zeilen inkl. `const APP`) ersetzen durch den Sektionskommentar `// ── Bridge-Konsumenten: APP, bcSend, onBridgeMessage, _connected/_bcOrigin usw. liefert bridge.js (SPLIT-02/03) ──`. Die 4 TOOL_ORIGIN-Kommentarzeilen + `const TOOL_ORIGIN = window.location.origin;` bleiben unverändert stehen.
       b. Block B (`// ── Ping-Retry` … `let _playerAbgelehnt = false;`) komplett löschen. EXEC-Log-Block bleibt.
       c. Block C (`// Absender-Doppelprüfung` … Ende `bcSend`) komplett löschen. `_pushCurseDBToBC` (mit Kommentar) bleibt.
       d. Block D: die Shell (von `window.addEventListener('message', function(ev) {` bis `switch (ev.data.type) {`) und das Ende (`  }` + `});`) löschen; an ihrer Stelle Sektionskommentar `// ── Bridge-Handler: Nachrichtentypen aus dem Spiel, registriert bei bridge.js (SPLIT-02/03). Körper unverändert aus dem früheren switch; break → return. ──` und die 35 Registrierungen in der alten Reihenfolge, je exakt `onBridgeMessage('<TYP>', function(ev) {` (Zeilenanfang, Spalte 0) … Körper … `});`. Körper wörtlich übernehmen; `case 'X': {`-Blockklammern entfallen (Funktionsrumpf ist der Block); trailing `break;` entfällt; mittige `break;` (PLAYER_DATA, CHAR_APPEARANCE_DATA ×2, DEFAULT_OUTFIT_DATA ×2) werden `return;`; vorhandene `return;` bleiben. Einrückung um zwei Spalten reduzieren ist erlaubt, Inhalt nicht.
       e. `debugOsOutfit`: Guard-Zeile des Handlers → `if (!ev.data || ev.data.reqId !== reqId) return;` mit Kommentar `// app/Typ/Absender prüft die Bridge-Shell (bridge.js); hier nur die reqId-Korrelation`; `window.removeEventListener('message', handler);` → `offBridgeMessage('OUTFIT_DEBUG_RESULT', handler);`; `window.addEventListener('message', handler);` → `onBridgeMessage('OUTFIT_DEBUG_RESULT', handler);`. Sonst nichts.
       f. Start-IIFE: `window.opener.postMessage({ app: APP, type: 'PING' }, '*');` → `bcSend({ type: 'PING' }, true);` und den `STAB-04-Ausnahme`-Kommentar darüber anpassen: `// Bootstrap-PING über bcSend: vor dem Handshake lässt bcSend nur PING durch (an '*', STAB-04-Ausnahme in bridge.js)`. Der `if (window.opener && !window.opener.closed)`-Guard und das `console.log` bleiben.
       g. Guard-Tabelle am Dateianfang: `[['idbGet', 'persistence.js']]` → `[['idbGet', 'persistence.js'], ['bcSend', 'bridge.js'], ['onBridgeMessage', 'bridge.js']]` und den Guard-Kommentar um „→ bridge.js“ ergänzen (die Registrierungen laufen zur Parse-Zeit — ohne bridge.js wäre der Fehler ein tiefer `ReferenceError`).
       Gates: `grep -c "window.opener.postMessage(" items.js` = 0; `grep -c "addEventListener('message'" items.js` = 0; `grep -c "^onBridgeMessage('" items.js` = 35; `node --check items.js` (fängt vergessene Top-Level-`break`).
    3. **index.html** (Edit-Tool): zwischen persistence.js- und items.js-Write `document.write('<scr'+'ipt src="bridge.js?_='+_cbv+'"><\/scr'+'ipt>');` einfügen; im `LADEREIHENFOLGE`-Kommentar `persistence.js → items.js` → `persistence.js → bridge.js → items.js`. Sonst nichts.
    4. **tests/helpers/loadScript.js**: `CORE_SCRIPTS = ['persistence.js', 'bridge.js', 'items.js']`; Kopfkommentar-Klammer „(und ab Plan 04-02 bridge.js)“ → „und bridge.js“.
    5. **Bestehende Tests nachziehen (nur Datei-Ort-Aussagen, Befund 5)**: (a) tests/bridge-protocol.test.js: Debug-Listener-Fall → `const count = () => evalIn(ctx, "(_bridgeHandlers.get('OUTFIT_DEBUG_RESULT') || []).length");` mit `before = count()`, nach `debugOsOutfit` `before + 1`, nach EVIL-Send `before + 1`, nach BC-Send `before`; statischer Audit → `bridge.js`-Zeilen: `wildcardSends` Länge 1 mit `type: 'PING'`, `fallbackLines` Länge 1; zusätzlich items.js: beide Zählungen 0; Kopfkommentar der Datei um „(Listener/Shell in bridge.js, Handler-Körper in items.js)“ ergänzen. (b) tests/injected-code-origin.test.js: `it.each` um `['items.js', 0]` und `['persistence.js', 0]`; den items.js-2-Wildcard-Fall ersetzen durch `it('bridge.js: genau 1 Wildcard-Zeile – Bootstrap-PING in startPingRetry über window.opener')` (Länge 1, enthält `type: 'PING'` und `window.opener.postMessage(`); Schleife „kein GitHub-Pages-Origin-String“ um `'bridge.js', 'persistence.js'` erweitern; in „Tool-Origin ist genau einmal definiert“ `combined` um `src('bridge.js') + src('persistence.js')` erweitern (Erwartung bleibt 1) und `expect((src('bridge.js').match(/const TOOL_ORIGIN/g) || []).length).toBe(0)`. (c) tests/load-script.test.js: Fall umbenennen in `'bridge.js registriert genau einen message-Handler in der Registry'` und `expect(handlers.length).toBe(1)`.
    6. Verbatim-Gates ausführen (Ausgabe fürs SUMMARY): `diff <(git show $PRE:items.js | awk '/^\/\/ ── Ping-Retry/{f=1} f{print} /^let _playerAbgelehnt = false;/{if(f) exit}') <(awk '/^\/\/ ── Ping-Retry/{f=1} f{print} /^let _playerAbgelehnt = false;/{if(f) exit}' bridge.js)` → leer; `diff <(git show $PRE:items.js | awk '/^\/\/ Absender-Doppelpr/{f=1} /^function bcSend\(msg, silent\)/{exit} f{print}') <(awk '/^\/\/ Absender-Doppelpr/{f=1} /^function bcSend\(msg, silent\)/{exit} f{print}' bridge.js)` → leer. Aufrufstellen-Invarianz: `git show $PRE:items.js | grep -c "bcSend({ *type: *'EXEC'"` = `grep -c "bcSend({ *type: *'EXEC'" items.js`.
    7. `node --check bridge.js items.js` → Exit 0. GREEN: `npx vitest run tests/bridge-registry.test.js` → 17 passed (Fall 13 zählt als 35 Einzelfälle → Vitest meldet 51 passed); `npx vitest run tests/bridge-protocol.test.js tests/injected-code-origin.test.js tests/load-script.test.js tests/exec-log.test.js` grün; `npm test` → 17 Dateien grün.
    8. Commit: `git add bridge.js items.js index.html tests/helpers/loadScript.js tests/bridge-protocol.test.js tests/injected-code-origin.test.js tests/load-script.test.js`; `git diff --cached --name-only | sort` = genau diese sieben Pfade; Message `refactor(04-02): extract postMessage bridge verbatim into bridge.js with onBridgeMessage registry; items.js registers its 35 handlers (SPLIT-02, SPLIT-03)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/bridge-registry.test.js tests/bridge-protocol.test.js tests/injected-code-origin.test.js tests/load-script.test.js tests/exec-log.test.js && node --check bridge.js && node --check items.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` in der `Tests`-Summary — etwa weil ein Top-Level-`break` stehen blieb (SyntaxError in `node --check`), die Shell die Registry NACH `_bridgeDispatch` prüft (Fälle 6-9), `list.slice()` fehlt (Fall 4), `_execLogAppend` ungeguardet ist (Fall 11), ein Typ nicht registriert ist (Fall 13), der Bootstrap-PING nicht mehr als erster `postMessage`-Aufruf kommt (bridge-protocol „Bootstrap-PING beim Laden“), oder die Datei-Ort-Aussagen in den drei bestehenden Tests nicht nachgezogen wurden</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && PRE=$(git rev-parse HEAD~1) && diff <(git show $PRE:items.js | awk '/^\/\/ ── Ping-Retry/{f=1} f{print} /^let _playerAbgelehnt = false;/{if(f) exit}') <(awk '/^\/\/ ── Ping-Retry/{f=1} f{print} /^let _playerAbgelehnt = false;/{if(f) exit}' bridge.js) && diff <(git show $PRE:items.js | awk '/^\/\/ Absender-Doppelpr/{f=1} /^function bcSend\(msg, silent\)/{exit} f{print}') <(awk '/^\/\/ Absender-Doppelpr/{f=1} /^function bcSend\(msg, silent\)/{exit} f{print}' bridge.js) && test "$(git show $PRE:items.js | grep -c "bcSend({ *type: *'EXEC'")" = "$(grep -c "bcSend({ *type: *'EXEC'" items.js)" && test "$(grep -c "^onBridgeMessage('" items.js)" = 35 && test "$(grep -c "addEventListener('message'" items.js)" = 0 && test "$(grep -c 'window.opener.postMessage(' items.js)" = 0 && test "$(grep -c 'switch (ev.data.type)' items.js)" = 0 && test "$(grep -Fc "const TOOL_ORIGIN = window.location.origin;" items.js)" = 1 && test "$(grep -c 'bridge.js?_=' index.html)" = 1 && test "$(grep -n 'persistence.js?_=' index.html | cut -d: -f1)" -lt "$(grep -n 'bridge.js?_=' index.html | cut -d: -f1)" && test "$(grep -n 'bridge.js?_=' index.html | cut -d: -f1)" -lt "$(grep -n 'items.js?_=' index.html | cut -d: -f1)" && test "$(grep -c "persistence.js → bridge.js → items.js" index.html)" = 1 && test "$(grep -c "export const CORE_SCRIPTS = \['persistence.js', 'bridge.js', 'items.js'\]" tests/helpers/loadScript.js)" = 1 && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "bridge.js index.html items.js tests/bridge-protocol.test.js tests/helpers/loadScript.js tests/injected-code-origin.test.js tests/load-script.test.js " && echo BRIDGE-OK</automated>
    <fails_when>`BRIDGE-OK` fehlt — ein verschobener Block weicht vom Original ab, eine EXEC-Aufrufstelle wurde verändert, es sind nicht genau 35 Top-Level-Registrierungen, in items.js verbleibt Listener/postMessage/switch, `TOOL_ORIGIN` ist umgezogen, index.html lädt bridge.js an falscher Stelle oder der Kommentar wurde nicht aktualisiert, `CORE_SCRIPTS` fehlt bridge.js, oder der Commit enthält andere Pfade als die sieben genannten (Annahme: Task 2 ist genau ein Commit nach Task 1)</fails_when>
  </verify>
  <acceptance_criteria>
    - Beide Verbatim-Diffs (Ping-Retry-Block, `_bridgeSenderOk`/`manualReconnect`-Block) leer; bcSend-Zeilen per Fall 16 genau 1× in bridge.js
    - `grep -c "^onBridgeMessage('" items.js` = 35; `grep -c "onBridgeMessage('" items.js` = 36 (35 + Debug-Fold); `grep -c "offBridgeMessage('" items.js` = 1; `grep -c "onBridgeMessage('" bridge.js` = 0 (bridge.js registriert nichts selbst)
    - `grep -c "addEventListener('message'" items.js bridge.js` → `items.js:0`, `bridge.js:1`; `grep -c "window.opener.postMessage(" items.js bridge.js` → `0`, `2`; `grep -c "indexedDB.open(" items.js` = 0; `grep -Fc "const APP = 'BCKonfigurator';" items.js bridge.js` → `0`, `1`
    - `grep -Fc "bcSend({ type: 'PING' }, true)" items.js` = 1 (Start-IIFE); `grep -c "_bridgeSenderOk(ev)" items.js` = 0; `grep -c "_bridgeSenderOk(ev)" bridge.js` = 2 (Definition + Shell)
    - `grep -Fc "[['idbGet', 'persistence.js'], ['bcSend', 'bridge.js'], ['onBridgeMessage', 'bridge.js']]" items.js` = 1
    - `git show $PRE:items.js | grep -c "bcSend({ *type: *'EXEC'"` = `grep -c "bcSend({ *type: *'EXEC'" items.js`; `git diff $PRE HEAD --stat -- loader.js bot-engine.js bot-data.js money.js rank.js shop.js inventar.js outfit-import.js bc-autobackup.js persistence.js` leer
    - index.html: Reihenfolge persistence.js < bridge.js < items.js (Zeilennummern), `git diff $PRE HEAD -- index.html | grep -c '^-[^-]'` = 1 (nur die Kommentarzeile) und `git diff $PRE HEAD -- index.html | grep -c '^+[^+]'` = 2
    - `node --check bridge.js items.js` → Exit 0; `npx vitest run tests/bridge-registry.test.js` → alle passed; `npm test` → `Test Files  17 passed`, `2 expected fail` unverändert
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` = die sieben Pfade; `git status --short` zeigt nur `?? .planning/milestone.lock`
  </acceptance_criteria>
  <done>bridge.js besitzt Protokoll + Registry (wörtliche Blöcke, eine geguardete Hook-Zeile); items.js registriert 35 Handler + Debug-Fold, sendet den Start-PING über bcSend und enthält keine Bridge-/IDB-Logik mehr; Loader und index.html laden persistence.js → bridge.js → items.js; 17 Testdateien grün; ein Commit mit genau sieben Pfaden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard-Erweiterung um bridge.js (items.js-Fehlertext, bot-ui.js-Guard), docs/LOAD-ORDER.md — RED → GREEN in zwei Commits (SPLIT-04-Fortführung, SPLIT-03)</name>
  <precondition>Task 2 committet (`npm test` = 17 Dateien grün); `git status --short items.js bot-ui.js docs/LOAD-ORDER.md tests/load-order-guard.test.js` leer</precondition>
  <reversibility rating="reversible">Guard-Zeilen, Doku; `git revert` genügt.</reversibility>
  <files>tests/load-order-guard.test.js, bot-ui.js, docs/LOAD-ORDER.md, items.js</files>
  <read_first>tests/load-order-guard.test.js (Plan 04-01 Task 3 — bestehende fünf Fälle, `created`-Muster); items.js Zeilen 1-25 (Guard nach Task 2 mit drei Tabellenzeilen); bot-ui.js Zeilen 1-15; docs/LOAD-ORDER.md (gesamt); tests/injected-code-origin.test.js `beforeAll` (lädt `['items.js', 'bot-data.js', 'bot-ui.js', 'bot-engine.js']` — muss mit bot-ui-Guard weiter laufen)</read_first>
  <behavior>
    - Test F: `makeSandbox()` + `loadInto(sb, 'persistence.js')` + `loadInto(sb, 'items.js')` (ohne bridge.js) wirft `/bridge\.js/`; Box `#loadOrderFatal` nennt `bridge.js`, nicht `persistence.js`
    - Test G: `makeSandbox()` + persistence.js + bridge.js, dann `bot-ui.js` OHNE items.js wirft `/items\.js/`; nur persistence.js, dann bot-ui.js wirft `/bridge\.js/` (Meldung nennt beide fehlenden Dateien `bridge.js, items.js`); Box-Text beginnt mit `FATAL:` und endet auf `(siehe docs/LOAD-ORDER.md)`
    - Test H: `loadScript(['items.js', 'bot-data.js', 'bot-ui.js'])` wirft nicht; `typeof ctx.renderBotTab === 'function'`
    - Test I: index.html-Reihenfolge persistence < bridge < items (Zeilen im `_cbv`-Block), Kommentar nennt `bridge.js`; docs/LOAD-ORDER.md nennt `bridge.js`, `onBridgeMessage`, `bot-ui.js`-Guard
  </behavior>
  <action>
    **RED (Commit 1):** `tests/load-order-guard.test.js` um einen describe-Block `'Plan 04-02: bridge.js im Guard, bot-ui.js-Guard'` mit den Fällen F-I aus `<behavior>` erweitern (Helfer `loadRaw(files)` → `makeSandbox()` + `created`-Override + `loadInto` je Datei; gibt `{ sb, created }` zurück; Throw per `expect(() => …).toThrow(regex)`). Bestehende fünf Fälle unverändert. RED bestätigen: F ist heute rot (items.js wirft `onBridgeMessage is not defined`? Nein — nach Task 2 nennt der Guard bereits bridge.js → F kann grün sein; G ist rot: bot-ui.js hat keinen Guard, wirft erst spät oder gar nicht; I-Doku-Teil rot). Mindestens G und der Doku-Teil von I müssen rot sein — sonst STOPP. Commit: `git add tests/load-order-guard.test.js`; Message `test(04-02): extend load-order guard tests – bridge.js in guard, bot-ui.js guard, docs (SPLIT-04)`.

    **GREEN (Commit 2):**
    1. bot-ui.js — als ERSTE Anweisung vor `function renderBotTab() {` denselben Guard wie in items.js (Kommentar + IIFE), Tabelle `[['idbGet', 'persistence.js'], ['bcSend', 'bridge.js'], ['onBridgeMessage', 'bridge.js'], ['showStatus', 'items.js']]`, Meldung `'FATAL: ' + missing.join(', ') + ' wurde nicht vor bot-ui.js geladen – Ladereihenfolge in index.html prüfen (siehe docs/LOAD-ORDER.md)'`, Box-Id `loadOrderFatal`, gleicher `cssText`, `console.error`, `throw`. Sonst nichts in bot-ui.js.
    2. items.js — nur falls Test F rot war: Guard-Tabelle/Meldung prüfen (nach Task 2 bereits drei Zeilen); andernfalls keine Änderung an items.js in diesem Task.
    3. docs/LOAD-ORDER.md — Tabelle um die Zeile bridge.js (zwischen persistence.js und items.js: stellt `APP`, `bcSend`, `onBridgeMessage`, `offBridgeMessage`, `_bridgeSenderOk`, `manualReconnect`, `startPingRetry`, `_heartbeatCheck`, `_connected`/`_bcOrigin` bereit; braucht zur Parse-Zeit nichts, zur Laufzeit `showStatus`/`stopRoomScan`/`_execLogAppend`/`_botRueckschreibStart` aus items.js — geguardet oder erst nach Handshake erreichbar) ergänzen; Guard-Abschnitt: items.js prüft persistence.js + bridge.js, bot-ui.js prüft persistence.js + bridge.js + items.js; neuer Abschnitt „Neuen Nachrichtentyp registrieren“: in einem beliebigen Modul nach bridge.js `onBridgeMessage('MEIN_TYP', function (ev) { … })` aufrufen — keine Änderung an items.js nötig, Shell prüft app/Absender/Origin vorab; `offBridgeMessage` für Einmal-Handler; Hinweis: keine try/catch-Isolation zwischen Handlern. Kommentar-Zeile in index.html ist bereits aktuell (Task 2).
    4. `node --check bot-ui.js items.js` → Exit 0. GREEN: `npx vitest run tests/load-order-guard.test.js tests/injected-code-origin.test.js` grün; `npm test` → 17 Dateien grün.
    5. Commit: `git add bot-ui.js docs/LOAD-ORDER.md` (plus `items.js` nur, wenn unter 2. geändert); Message `feat(04-02): load-order guard in bot-ui.js, document bridge.js position and handler registration (SPLIT-04)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/load-order-guard.test.js tests/injected-code-origin.test.js && node --check bot-ui.js && node --check items.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` — etwa weil der bot-ui.js-Guard nach einer Funktionsnutzung steht, `showStatus` nicht in der Tabelle ist (Fall G wirft nicht `/items\.js/`), der Guard bot-ui.js in der Vollkette bricht (injected-code-origin `beforeAll`), oder die Doku bridge.js/`onBridgeMessage` nicht nennt</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(grep -c 'loadOrderFatal' bot-ui.js)" = 1 && test "$(grep -n 'loadOrderFatal' bot-ui.js | cut -d: -f1)" -lt 20 && test "$(grep -Fc "['showStatus', 'items.js']" bot-ui.js)" = 1 && test "$(grep -c 'bridge.js' docs/LOAD-ORDER.md)" -ge 3 && test "$(grep -c 'onBridgeMessage' docs/LOAD-ORDER.md)" -ge 2 && test "$(grep -c 'bot-ui.js' docs/LOAD-ORDER.md)" -ge 2 && test "$(git show --name-only --format= HEAD | grep -c 'bot-ui.js')" = 1 && test "$(git show --name-only --format= HEAD | grep -c 'docs/LOAD-ORDER.md')" = 1 && test "$(git show --name-only --format= HEAD | grep -vc 'bot-ui.js\|docs/LOAD-ORDER.md\|items.js')" = 0 && echo GUARD2-OK</automated>
    <fails_when>`GUARD2-OK` fehlt — bot-ui.js hat keinen oder einen zu tief liegenden Guard, die Tabelle prüft `showStatus` nicht, die Doku ist unvollständig, oder der GREEN-Commit enthält andere Pfade als bot-ui.js, docs/LOAD-ORDER.md (und ggf. items.js)</fails_when>
    <human-check>End-of-Phase (nicht blockierend, wird in Plan 04-04 gesammelt): Tool auf GitHub Pages hart neu laden, Bookmarklet im BC-Tab neu ausführen → `Verbunden`; Cache laden, Raum-Scan läuft (PLAYER_DATA), ein Bot-Deploy (EXEC_OK + EXEC-Log-Eintrag), ein Outfit-Screenshot (SCREENSHOT_DATA), `debugOsOutfit('<mk>', 0)` in der Konsole liefert die Analyse; 🔄 Verbinden nach BC-Reload lernt den Origin neu.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "loadOrderFatal" bot-ui.js` = 1 (in den ersten 20 Zeilen); `grep -Fc "wurde nicht vor bot-ui.js geladen" bot-ui.js` = 1; `head -20 bot-ui.js | grep -c "bcSend("` = 0 (Guard prüft nur Typen)
    - `grep -Fc "[['idbGet', 'persistence.js'], ['bcSend', 'bridge.js'], ['onBridgeMessage', 'bridge.js']]" items.js` = 1
    - docs/LOAD-ORDER.md: enthält `bridge.js` (>= 3×), `onBridgeMessage` (>= 2×), `offBridgeMessage`, `bot-ui.js` (>= 2×), `GAME_SCAN_DATA` oder `MEIN_TYP` als Beispiel
    - `npx vitest run tests/load-order-guard.test.js` → 9 passed; `npm test` → `Test Files  17 passed`
    - Zwei Commits dieses Tasks: `test(04-02): …` mit genau `tests/load-order-guard.test.js`; `feat(04-02): …` mit `bot-ui.js`, `docs/LOAD-ORDER.md` (+ ggf. `items.js`); `git status --short` zeigt nur `?? .planning/milestone.lock`
  </acceptance_criteria>
  <done>Fehlendes bridge.js wird von items.js und bot-ui.js sichtbar gemeldet; docs/LOAD-ORDER.md beschreibt bridge.js, beide Guards und die Registrierung neuer Typen; 17 Testdateien grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spiel-Tab (opener) → bridge.js message-Listener | Einzige Vertrauensgrenze des Tools; Origin-/Source-Prüfung muss zeichengenau erhalten bleiben |
| bridge.js Registry ← same-origin-Skripte | Jedes klassische Skript der Seite kann Handler registrieren; keine fremde Origin hat Zugriff auf diese Funktion |
| bridge.js `bcSend` → Spiel-Tab | Pre-Handshake-Abweisung (nur PING an `'*'`), gelernter Origin danach |
| items.js Handler-Körper ← bridge.js Dispatch | Körper mutieren items.js-Globals; ein werfender Handler bricht den Listener wie bisher ab |
| index.html `document.write`-Kette | bridge.js muss nach persistence.js und vor items.js liegen (Registrierungen zur Parse-Zeit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-08 | Spoofing | Extraktion schwächt Origin-Pinning (`'*'`-Wildcard, fehlender `ev.source`-Check, Origin-Neulernen) | critical | mitigate | Byte-identische Diff-Gates auf Ping-Retry- und `_bridgeSenderOk`/`manualReconnect`-Block; Grep-Gates auf jede Sicherheitszeile (Fall 16); alle 21 Bridge-Protokoll-Fälle bleiben grün; Wildcard-Audit bridge.js 1 / items.js 0 |
| T-4-03 | Tampering | Registry erlaubt jedem Skript, Handler zu registrieren | low | accept | Nur same-origin-Skripte der Seite können `onBridgeMessage` aufrufen (kein Netzwerk-/Cross-Origin-Zugriff); Shell prüft app/Absender/Origin VOR dem Dispatch (Fälle 6-9); im bridge.js-Kopf dokumentiert |
| T-4-01 | Tampering | Handler-Körper werden beim Umzug still verändert | high | mitigate | Wörtliche Übernahme, nur `break`→`return`; EXEC-Aufrufstellen-Zählung vor/nach identisch; Dispatch-Tests (PONG, BOT_LOG, POS_DATA, EXEC_OK/ERR) und 35-Typen-Registrierungstest; `node --check` fängt Top-Level-`break` |
| T-4-02 | Denial of Service | bridge.js fehlt → Registrierungen werfen tief in items.js | high | mitigate | Guard-Tabelle um bridge.js erweitert (items.js, bot-ui.js); Tests F/G; index.html-Reihenfolge per Test fixiert |
| T-4-11 | Repudiation | EXEC-Log-Hook geht beim Umzug verloren | medium | mitigate | Hook-Zeile mit `typeof`-Guard genau 1× in bridge.js (Fall 16); exec-log.test.js (11 Fälle) bleibt grün |
| T-4-07 | Tampering | Git-Staging fremder Dateien | medium | mitigate | Preconditions, `git add` nur genannte Pfade, Commit-Pfad-Gates |
| T-4-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase |
</threat_model>

<verification>
- `npx vitest run tests/bridge-registry.test.js tests/bridge-protocol.test.js tests/load-order-guard.test.js` grün; `npm test` → 17 Testdateien grün (2 expected fail unverändert).
- Verbatim-Diffs leer; `node --check bridge.js items.js bot-ui.js` grün; EXEC-Aufrufstellen-Zählung unverändert.
- Vier Commits dieses Plans: test → refactor → test → feat.
- Phase-Gate (VALIDATION.md 4-02-01/4-02-02): statisches Gate `grep -c "indexedDB.open\|addEventListener('message'" items.js` = 0 erfüllt.
</verification>

<success_criteria>
- SPLIT-02: bridge.js besitzt Protokoll + Registry; `GAME_SCAN_DATA` lässt sich ohne items.js registrieren und empfangen (Fall 10).
- SPLIT-03: items.js enthält keine `indexedDB.open`/`addEventListener('message'`/`window.opener.postMessage`-Zeile mehr; alle Handler-Körper laufen unverändert (Dispatch-Tests grün).
- Sicherheitsverhalten aus Phase 3 unverändert (21/21 grün, Wildcard-Audit).
</success_criteria>

<output>
Create `.planning/phases/04-entflechtung/04-02-SUMMARY.md` when done — mit RED-/GREEN-Ausgaben, beiden leeren Verbatim-Diffs, der EXEC-Aufrufstellen-Zählung vorher/nachher, der bewussten Abweichung beim Debug-Listener-Fold (Befund 3) und der Commit-Liste.
</output>
