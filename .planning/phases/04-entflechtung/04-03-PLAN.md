---
phase: 04-entflechtung
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - tests/screenshot-export.test.js
  - items.js
  - index.html
autonomous: true
requirements: [SPLIT-07]
tags: [screenshots, export, json, tweaks-panel, red-green]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SPLIT-07: `exportScreenshotsOnly()` (items.js) erzeugt per bestehendem `_jsonParts`/`Blob`/`URL.createObjectURL`-Pfad (Vorbild `exportAllData`) einen JSON-Download `BC_Screenshots_<YYYY-MM-DD>.json` mit `_meta` (`exportedAt`, `version: 1`, `tool`, `counts`) und den drei Maps unter GENAU den Feldnamen von `exportAllData` (`profileScreenshots`, `lscgScreenshots`, `mbsWheelShots`), sodass `importAllData()` (⬆️ Restore) die Datei unverändert einlesen kann (Orchestrator-Entscheidung 6)"
    - "Der Export liest nur; er ruft vorher `bcSpeichernJetzt()` (ausstehende Sammelspeicher-Schreibvorgänge) und verändert keine Map und keinen IDB-Schlüssel; leere Sammlungen → `showStatus('⚠️ Keine Screenshots zum Exportieren', 'info')` ohne Download; Fehler → `showStatus('❌ Screenshot-Export fehlgeschlagen: …', 'error')`"
    - "Button „📷 Screenshots exportieren“ in einer neuen Tweaks-Panel-Sektion `🖼️ Screenshot-Speicher` (index.html NUR Einfügungen, direkt nach `📜 EXEC-Log`, vor `📦 Item-Katalog`), inklusive Status-Div `#screenshotStoreInfo`, das Plan 04-04 mit dem Migrationsstatus füllt — der Export landet damit VOR der Migration im Repo (Belt-and-Braces zur ohnehin additiven Migration)"
    - "RED vor GREEN: `tests/screenshot-export.test.js` ist ohne `exportScreenshotsOnly` nachweislich rot; GREEN-Commit enthält nur items.js und index.html"
  artifacts:
    - path: "items.js"
      provides: "`function exportScreenshotsOnly()` direkt nach `exportAllData()`"
      contains: "function exportScreenshotsOnly()"
    - path: "index.html"
      provides: "Sektion `🖼️ Screenshot-Speicher` mit Button `onclick=\"exportScreenshotsOnly()\"` und `<div id=\"screenshotStoreInfo\">`"
      contains: "id=\"screenshotStoreInfo\""
    - path: "tests/screenshot-export.test.js"
      provides: "SPLIT-07: Payload-Form/-Inhalt, Feldnamen kompatibel zu importAllData, Dateiname, Flush ausstehender Writes, Leer-Fall, Fehlerpfad, Nicht-Mutation, statisches index.html-Gate"
      contains: "BC_Screenshots_"
  key_links:
    - from: "index.html Button `onclick=\"exportScreenshotsOnly()\"`"
      to: "items.js `exportScreenshotsOnly`"
      via: "Funktionsdeklaration ist global; items.js wird per `document.write` nach dem Panel-Markup geladen"
      pattern: "exportScreenshotsOnly\\(\\)"
    - from: "items.js `exportScreenshotsOnly`"
      to: "items.js `_jsonParts` / `bcSpeichernJetzt`"
      via: "`bcSpeichernJetzt()` zuerst; `const parts = _jsonParts(payload); const blob = new Blob(parts, { type: 'application/json' })`"
      pattern: "_jsonParts\\(payload\\)"
    - from: "Export-Datei"
      to: "items.js `importAllData` (`d.profileScreenshots`, `d.lscgScreenshots`, `d.mbsWheelShots`)"
      via: "identische Feldnamen — der Screenshot-Export ist per ⬆️ Restore wieder einspielbar"
      pattern: "mbsWheelShots"
  prohibitions:
    - requirement_id: SPLIT-07
      category: safety
      verification: automated
      statement: "Der Export mutiert nichts: `PROFILE_SCREENSHOTS`/`LSCG_SCREENSHOTS`/`_mbsWheelShots` sind vor und nach dem Aufruf JSON-identisch; kein `idbSet`/`delete` im Funktionskörper (statisch: der Körper von `exportScreenshotsOnly` enthält weder `idbSet(` noch `delete ` noch `= {}`)"
    - requirement_id: SPLIT-07
      category: safety
      verification: automated
      statement: "index.html nur Einfügungen gegenüber dem Plan-04-02-Ende (`git diff --numstat $BASE HEAD -- index.html` Deletions = 0); keine Änderung an `<script>`-Tags"
    - requirement_id: SPLIT-07
      category: safety
      verification: judgment
      statement: "Kein neuer Export-Serialisierer, kein zweiter Download-Pfad — ausschließlich `_jsonParts` + `Blob` wie `exportAllData` (RESEARCH „JSON export before migration“); `git add` nur genannte Pfade; RED/GREEN getrennt"
---

<objective>
SPLIT-07: Bevor Plan 04-04 die Screenshot-Migration einführt, bekommt der Nutzer einen Ein-Klick-JSON-Export aller drei Screenshot-Sammlungen (Profil, Outfit-Scan/LSCG, MBS-Wheel). `exportScreenshotsOnly()` reuse-t den bestehenden, größenfesten `_jsonParts`/`Blob`-Pfad aus `exportAllData()` und schreibt die Maps unter denselben Feldnamen, sodass die Datei über den vorhandenen ⬆️-Restore-Pfad (`importAllData`) wieder einspielbar ist. Der Button sitzt in einer neuen Tweaks-Panel-Sektion `🖼️ Screenshot-Speicher`, die auch das Status-Div für Plan 04-04 mitbringt.

Purpose: Erfolgskriterium 5, erster Halbsatz — „Vor der Migration kann der Nutzer einen JSON-Export aller Screenshot-Daten auslösen“. Kernwert-Absicherung (Datenschutz-Regel) zusätzlich zur additiven Migration.
Output: `tests/screenshot-export.test.js` (neu), `items.js` (eine Funktion), `index.html` (eine Sektion, nur Einfügungen).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-entflechtung/04-RESEARCH.md
@.planning/phases/04-entflechtung/04-02-SUMMARY.md
@.planning/phases/03-bridge-haertung/03-03-SUMMARY.md

Planer-Probe (2026-09-14, HEAD `9b41340`; Zeilen nach 04-01/04-02 verschoben — per grep ankern) — verbindliche Befunde:
1. `exportAllData()` (per `grep -n "^function exportAllData" items.js`): `bcSpeichernJetzt()` → Zählungen → Leer-Check mit `showStatus('⚠️ Keine Daten zum Exportieren', 'info')` → `payload` mit `_meta { exportedAt, version: 3, tool: 'BC Konfigurator', counts }` und u. a. `lscgScreenshots: LSCG_SCREENSHOTS`, `profileScreenshots: PROFILE_SCREENSHOTS`, `mbsWheelShots: _mbsWheelShots` → `const parts = _jsonParts(payload); const blob = new Blob(parts, { type: 'application/json' });` → `a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = 'BC_Backup_' + new Date().toISOString().slice(0, 10) + '.json'; document.body.appendChild(a); a.click(); document.body.removeChild(a); setTimeout(() => URL.revokeObjectURL(a.href), 60000);` → `showStatus('✅ Backup: …(' + mb + ' MB)', 'success')`; `catch(err)` → `showStatus('❌ Export fehlgeschlagen: ' + err.message, 'error')`.
2. `importAllData()` liest `d.lscgScreenshots` (`Object.assign(LSCG_SCREENSHOTS, …); _saveLscgScreenshots()`), `d.profileScreenshots`, `d.mbsWheelShots` — unabhängig von `_meta.version`. Gleiche Feldnamen ⇒ Screenshot-Export ist restore-fähig.
3. `_jsonParts(value, parts)` (per grep) liefert ein Array von JSON-Teilstrings; `parts.join('')` ist gültiges JSON.
4. Sandbox (tests/helpers/loadScript.js) hat KEIN `Blob` und KEIN `URL`; items.js referenziert beide nur innerhalb von Funktionen → per `extraGlobals` stubbar: `Blob`-Klasse mit `parts`/`type`/`size`, `URL = { createObjectURL: vi.fn(() => 'blob:test'), revokeObjectURL: vi.fn() }`. `document.createElement` ist überschreibbar (Muster tests/load-order-guard.test.js `created`). `setTimeout: () => 0` neutralisiert Revoke-Timer und `_sammelSpeicher.plane`; `bcSpeichernJetzt()` führt ausstehende Writes synchron aus (`_sammelSpeicher.offen()` → 0). `_sammelSpeicher` ist Top-Level-`const` → `evalIn(ctx, '_sammelSpeicher.offen()')`.
5. Tweaks-Panel (index.html, per `grep -n 'id="execLogInfo"' index.html`): Sektion `📜 EXEC-Log` endet mit `<div id="execLogInfo" …>–</div>` + `</div>`; danach folgt `<div>` / `<div class="tweaks-section-title">📦 Item-Katalog</div>`. Markup-Muster: `<div><div class="tweaks-section-title">…</div><div style="font-size:.6875rem;color:var(--text3);margin-bottom:7px">…</div><div class="tweaks-btn-group"><button class="tweaks-btn" onclick="…" title="…">…</button></div><div id="…" style="font-size:.6875rem;color:var(--text3);margin-top:5px;line-height:1.5">–</div></div>`. index.html enthält 0× `screenshotStoreInfo`, 0× `exportScreenshotsOnly`.
6. Basis für index.html-Diff-Gate: `BASE=$(git log --format=%H -n1 --grep='^feat(04-02)')` (GREEN-Commit von Plan 04-02 Task 3).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export-Test schreiben und RED bestätigen — `tests/screenshot-export.test.js` (SPLIT-07, Wave 0)</name>
  <precondition>Plan 04-02 abgeschlossen (`npm test` = 17 Dateien grün); `git status --short` außer `.planning/milestone.lock` leer</precondition>
  <files>tests/screenshot-export.test.js</files>
  <read_first>items.js `exportAllData` und `importAllData` (per grep, Befund 1-2) und `_jsonParts`; tests/exec-log.test.js (`boot`-Muster mit `setTimeout: () => 0`, `document.getElementById`-Override); tests/load-order-guard.test.js (`created`-Override für `document.createElement`); tests/helpers/loadScript.js (`evalIn`, `makeElementStub`)</read_first>
  <action>
    `tests/screenshot-export.test.js` (neu, ESM): Imports `describe, it, expect, vi` aus vitest, `fs`/`path`, `loadScript, evalIn, makeElementStub, REPO_ROOT` aus `./helpers/loadScript.js`. Kopfkommentar (Deutsch): SPLIT-07 — Screenshot-Export vor der Migration; reuse des `exportAllData`-Pfads; Feldnamen restore-kompatibel.

    Helfer `boot()`: `class FakeBlob { constructor(parts, opts) { this.parts = parts; this.type = opts && opts.type; this.size = parts.reduce((n, p) => n + String(p).length, 0); } text() { return this.parts.join(''); } }`; `urls = { createObjectURL: vi.fn(() => 'blob:test'), revokeObjectURL: vi.fn() }`; `created = []`; `ctx = loadScript(['items.js'], { Blob: FakeBlob, URL: urls, setTimeout: () => 0, clearTimeout: () => {} })`; `ctx.document.createElement = (tag) => { const el = makeElementStub(); el.tagName = tag; el.click = vi.fn(); created.push(el); return el; }`; `ctx.showStatus = vi.fn()`; `seed(ctx)` → `evalIn(ctx, "PROFILE_SCREENSHOTS = { Anna: 'data:a', 'Bob#1': 'data:b' }; LSCG_SCREENSHOTS = { '123|FP1': 'data:l1', '456': 'data:l2' }; _mbsWheelShots = { 'G:A|G:B': 'data:w' };")`; `lastBlob(urls)` → `urls.createObjectURL.mock.calls.at(-1)[0]`; `payloadOf(urls)` → `JSON.parse(lastBlob(urls).text())`; `anchor(created)` → `created.find(e => e.tagName === 'a')`; `src(file)`; `count(hay, needle)`.

    `describe('exportScreenshotsOnly(): Payload und Download (SPLIT-07)')`:
    1. `it('exportiert alle drei Maps unter den Feldnamen von exportAllData')`: `boot(); seed(ctx); ctx.exportScreenshotsOnly();` → `urls.createObjectURL` 1× mit Blob `type === 'application/json'`; `p = payloadOf(urls)`; `Object.keys(p).sort()` = `['_meta', 'lscgScreenshots', 'mbsWheelShots', 'profileScreenshots']`; `p.profileScreenshots` `toEqual({ Anna: 'data:a', 'Bob#1': 'data:b' })`; `p.lscgScreenshots['123|FP1'] === 'data:l1'`; `p.mbsWheelShots['G:A|G:B'] === 'data:w'`.
    2. `it('_meta: exportedAt (ISO), version 1, tool nennt Screenshot, counts je Map')`: `p._meta.version === 1`; `p._meta.tool` matcht `/Screenshot/`; `Date.parse(p._meta.exportedAt)` ist eine Zahl; `p._meta.counts` `toEqual({ profileScreenshots: 2, lscgScreenshots: 2, mbsWheelShots: 1 })`.
    3. `it('Download-Anker: Dateiname BC_Screenshots_YYYY-MM-DD.json, click 1×, href aus createObjectURL')`: `a = anchor(created)`; `a.download` matcht `/^BC_Screenshots_\d{4}-\d{2}-\d{2}\.json$/`; `a.href === 'blob:test'`; `a.click` 1×; `ctx.showStatus` zuletzt mit `type 'success'` und Text enthält `'2 Profil'`, `'2 Outfit-Scan'`, `'1 Wheel'` (Reihenfolge egal: drei `toContain`).
    4. `it('ausstehende Sammelspeicher-Writes werden vor dem Export ausgeführt')`: nach `seed` `evalIn(ctx, "PROFILE_SCREENSHOTS.Neu = 'data:n'"); ctx._saveProfileScreenshots();` → `evalIn(ctx, '_sammelSpeicher.offen()') === 1`; `ctx.exportScreenshotsOnly()` → `=== 0`; `payloadOf(urls).profileScreenshots.Neu === 'data:n'`.
    5. `it('mutiert nichts: Maps vor/nach identisch')`: `before = evalIn(ctx, 'JSON.stringify([PROFILE_SCREENSHOTS, LSCG_SCREENSHOTS, _mbsWheelShots])')`; Export; `after` gleich `before`.
    6. `it('leer: Hinweis per showStatus(info), kein Download')`: `boot()` ohne seed (Maps leer, IDB-Loads in der Sandbox liefern nichts, ggf. `evalIn` explizit auf `{}` setzen) → `ctx.exportScreenshotsOnly()`; `urls.createObjectURL` nicht gerufen; `anchor(created)` undefined; `ctx.showStatus` mit `('⚠️ Keine Screenshots zum Exportieren', 'info')`.
    7. `it('Fehlerpfad: createObjectURL wirft → showStatus(error), kein Throw')`: `urls.createObjectURL = vi.fn(() => { throw new Error('boom'); })`; seed; `expect(() => ctx.exportScreenshotsOnly()).not.toThrow()`; `showStatus` mit `type 'error'` und Text enthält `'Screenshot-Export fehlgeschlagen'` und `'boom'`.
    8. `it('Payload ist restore-kompatibel: importAllData liest genau diese Feldnamen (statisch)')`: `body = src('items.js').slice(src('items.js').indexOf('function importAllData'))`; `body` enthält `d.profileScreenshots`, `d.lscgScreenshots`, `d.mbsWheelShots`; `exp = src('items.js').slice(idx('function exportScreenshotsOnly'), idx('function exportScreenshotsOnly') + 3000)`; `exp` enthält `profileScreenshots:`, `lscgScreenshots:`, `mbsWheelShots:`, `_jsonParts(payload)`, `bcSpeichernJetzt()`; `exp` enthält NICHT `idbSet(`, NICHT `delete `, NICHT `= {}` (Nicht-Mutation, statisch).

    `describe('Tweaks-Panel: Sektion 🖼️ Screenshot-Speicher (index.html)')`:
    9. `it('Button und Status-Div genau einmal, zwischen EXEC-Log und Item-Katalog')`: `html = src('index.html')`; `count(html, 'onclick="exportScreenshotsOnly()"') === 1`; `count(html, '📷 Screenshots exportieren') === 1`; `count(html, 'id="screenshotStoreInfo"') === 1`; `count(html, '🖼️ Screenshot-Speicher') === 1`; `html.indexOf('id="execLogInfo"') < html.indexOf('id="screenshotStoreInfo"')` und `html.indexOf('id="screenshotStoreInfo"') < html.indexOf('📦 Item-Katalog')`.

    RED bestätigen: `npx vitest run tests/screenshot-export.test.js` MUSS rot sein (`ctx.exportScreenshotsOnly is not a function`, Zählungen 0). Grün → STOPP. Commit: `git add tests/screenshot-export.test.js`; Message `test(04-03): add failing screenshot export tests – payload, restore-compatible field names, flush, empty/error paths, panel section (SPLIT-07)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/screenshot-export.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -q "exportScreenshotsOnly" "$OUT" && grep -Eq "Tests +[0-9]+ failed|Test Files +1 failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Vitest endete mit Exit 0 (Funktion existiert bereits) oder die Ausgabe nennt weder `exportScreenshotsOnly` noch eine `failed`-Zeile</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "exportScreenshotsOnly" tests/screenshot-export.test.js` >= 8; `grep -c "BC_Screenshots_" tests/screenshot-export.test.js` >= 1; `grep -c "mbsWheelShots" tests/screenshot-export.test.js` >= 4; `grep -c "screenshotStoreInfo" tests/screenshot-export.test.js` >= 2
    - `grep -Fc "'⚠️ Keine Screenshots zum Exportieren'" tests/screenshot-export.test.js` = 1; `grep -c "Screenshot-Export fehlgeschlagen" tests/screenshot-export.test.js` >= 1; `grep -c "_sammelSpeicher.offen()" tests/screenshot-export.test.js` >= 2
    - `test "$(grep -v '^\s*//' tests/screenshot-export.test.js | grep -c '\.only(')" = 0`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/screenshot-export.test.js`; `git status --short items.js index.html` leer
  </acceptance_criteria>
  <done>Neun Fälle in zwei describe-Blöcken; Lauf nachweislich rot; roter Test committet; items.js/index.html unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `exportScreenshotsOnly()` in items.js und Sektion `🖼️ Screenshot-Speicher` in index.html — GREEN, committen (SPLIT-07)</name>
  <precondition>Task 1 committet; `git status --short items.js index.html` leer; `BASE=$(git log --format=%H -n1 --grep='^feat(04-02)')` nicht leer</precondition>
  <reversibility rating="reversible">Eine lesende Funktion und ein Panel-Block; `git revert` genügt, keine Daten berührt.</reversibility>
  <files>items.js, index.html</files>
  <read_first>items.js `exportAllData` (gesamt, Vorbild) und die Zeile danach (Einfügeort); index.html `📜 EXEC-Log`-Sektion bis `📦 Item-Katalog` (Befund 5); tests/screenshot-export.test.js (Task 1, Fälle 1-9)</read_first>
  <behavior>
    - Test 1-3: Payload-Form, `_meta`, Anker/Dateiname/Status
    - Test 4-5: Flush vor Export; keine Mutation
    - Test 6-7: Leer-Hinweis ohne Download; Fehlerpfad ohne Throw
    - Test 8-9: statische Restore-Kompatibilität und Panel-Markup
    - Volle Suite: 18 Dateien grün
  </behavior>
  <action>
    1. items.js — direkt NACH dem schließenden `}` von `function exportAllData()` einfügen (Kommentar Deutsch, Bezeichner Englisch):
       Kommentar: `/* Nur die drei Screenshot-Sammlungen sichern (SPLIT-07) – gedacht als Sicherung VOR der Screenshot-Migration (Plan 04-04) und für Nutzer, denen das Komplett-Backup zu groß ist. Gleiche Feldnamen wie exportAllData(), damit importAllData() die Datei unverändert einlesen kann. Liest nur; mutiert weder Maps noch IDB. */`
       `function exportScreenshotsOnly()`: `try { bcSpeichernJetzt();` → `const profileCount = Object.keys(PROFILE_SCREENSHOTS).length; const lscgCount = Object.keys(LSCG_SCREENSHOTS).length; const wheelCount = Object.keys(_mbsWheelShots).length;` → `if (!profileCount && !lscgCount && !wheelCount) { showStatus('⚠️ Keine Screenshots zum Exportieren', 'info'); return; }` → `const payload = { _meta: { exportedAt: new Date().toISOString(), version: 1, tool: 'BC Konfigurator – Screenshot-Export', counts: { profileScreenshots: profileCount, lscgScreenshots: lscgCount, mbsWheelShots: wheelCount } }, profileScreenshots: PROFILE_SCREENSHOTS, lscgScreenshots: LSCG_SCREENSHOTS, mbsWheelShots: _mbsWheelShots };` → `const parts = _jsonParts(payload); const blob = new Blob(parts, { type: 'application/json' });` → Anker wie in `exportAllData` (`a.download = 'BC_Screenshots_' + new Date().toISOString().slice(0, 10) + '.json'`, `appendChild`/`click`/`removeChild`, `setTimeout(() => URL.revokeObjectURL(a.href), 60000)`) → `const mb = (blob.size / 1048576).toFixed(1);` → `showStatus('✅ Screenshot-Export: ' + profileCount + ' Profil-Bilder, ' + lscgCount + ' Outfit-Scan-Bilder, ' + wheelCount + ' Wheel-Bilder (' + mb + ' MB)', 'success');` `} catch (err) { showStatus('❌ Screenshot-Export fehlgeschlagen: ' + (err && err.message ? err.message : err), 'error'); console.error('[exportScreenshotsOnly]', err); }`. Kein `idbSet`, kein `delete`, keine Zuweisung an die Maps.
    2. index.html — per Edit-Tool mit exaktem old_string unmittelbar nach dem schließenden `</div>` der Sektion `📜 EXEC-Log` (Befund 5) einfügen: `<div>` / `<div class="tweaks-section-title">🖼️ Screenshot-Speicher</div>` / `<div style="font-size:.6875rem;color:var(--text3);margin-bottom:7px">Alle Profil-, Outfit-Scan- und Wheel-Bilder als JSON sichern – empfohlen vor jedem Tool-Update. Die Datei lässt sich über ⬆️ Restore wieder einspielen.</div>` / `<div class="tweaks-btn-group">` / `<button class="tweaks-btn" onclick="exportScreenshotsOnly()" title="Nur die Screenshot-Sammlungen als JSON exportieren (restore-fähig)">📷 Screenshots exportieren</button>` / `</div>` / `<div id="screenshotStoreInfo" style="font-size:.6875rem;color:var(--text3);margin-top:5px;line-height:1.5">–</div>` / `</div>` — Einrückung wie die Nachbarsektionen; nichts löschen.
    3. `node --check items.js` → Exit 0. GREEN: `npx vitest run tests/screenshot-export.test.js` → 9 passed; `npm test` → 18 Dateien grün.
    4. Commit: `git add items.js index.html`; Message `feat(04-03): one-click JSON export of all screenshot maps (restore-compatible) with Tweaks-panel section (SPLIT-07)`.
    5. Fürs SUMMARY: `git diff --numstat $BASE HEAD -- index.html` (erwartet `N 0`), RED-/GREEN-Ausgaben.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/screenshot-export.test.js && node --check items.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` — etwa weil Feldnamen abweichen (Fall 1/8), `bcSpeichernJetzt()` fehlt (Fall 4), der Leer-Fall trotzdem einen Anker erzeugt (Fall 6), der Fehlerpfad wirft (Fall 7) oder das Markup an falscher Stelle/doppelt sitzt (Fall 9)</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && BASE=$(git log --format=%H -n1 --grep='^feat(04-02)') && test "$(git diff --numstat $BASE HEAD -- index.html | awk '{print $2}')" = "0" && test "$(grep -Fc 'onclick="exportScreenshotsOnly()"' index.html)" = 1 && test "$(grep -Fc 'id="screenshotStoreInfo"' index.html)" = 1 && test "$(grep -c '^function exportScreenshotsOnly()' items.js)" = 1 && awk '/^function exportScreenshotsOnly\(\)/{f=1} f&&/^}/{exit} f' items.js | grep -c 'idbSet(\|delete \|= {}' | grep -qx 0 && awk '/^function exportScreenshotsOnly\(\)/{f=1} f&&/^}/{exit} f' items.js | grep -q '_jsonParts(payload)' && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "index.html items.js " && echo EXPORT-OK</automated>
    <fails_when>`EXPORT-OK` fehlt — index.html hat Löschungen, Button/Div fehlen oder doppeln, der Funktionskörper mutiert (idbSet/delete/`= {}`) oder nutzt nicht `_jsonParts`, oder der Commit enthält andere Pfade als items.js und index.html</fails_when>
    <human-check>End-of-Phase (nicht blockierend, in Plan 04-04 gesammelt): ⚙️-Panel → `🖼️ Screenshot-Speicher` → „📷 Screenshots exportieren“ lädt `BC_Screenshots_<Datum>.json` herunter; Statuszeile nennt die drei Zählungen; die Datei lässt sich über ⬆️ Restore ohne Fehler wieder einlesen.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "^function exportScreenshotsOnly()" items.js` = 1, unmittelbar nach `exportAllData` (Zeilennummer von `exportScreenshotsOnly` − Zeilennummer des schließenden `}` von `exportAllData` ≤ 4)
    - Funktionskörper enthält `bcSpeichernJetzt()`, `_jsonParts(payload)`, `'BC_Screenshots_'`, `'⚠️ Keine Screenshots zum Exportieren'`, `'❌ Screenshot-Export fehlgeschlagen: '`; enthält NICHT `idbSet(`, `delete `, `= {}`
    - `grep -Fc 'onclick="exportScreenshotsOnly()"' index.html` = 1; `grep -Fc '🖼️ Screenshot-Speicher' index.html` = 1; `grep -Fc 'id="screenshotStoreInfo"' index.html` = 1; `git diff --numstat $BASE HEAD -- index.html | awk '{print $2}'` = `0`
    - `node --check items.js` → Exit 0; `npx vitest run tests/screenshot-export.test.js` → `9 passed`; `npm test` → `Test Files  18 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` = `index.html`, `items.js`; `git status --short` zeigt nur `?? .planning/milestone.lock`
  </acceptance_criteria>
  <done>Ein-Klick-Export aller Screenshot-Maps als restore-kompatibles JSON; Sektion im Tweaks-Panel mit Status-Div für Plan 04-04; 18 Testdateien grün; GREEN-Commit mit genau zwei Pfaden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RAM-Maps → JSON-Datei auf der Festplatte des Nutzers | Lesender Export; Inhalt = Bilddaten des einzigen Nutzers |
| index.html Markup → `onclick="exportScreenshotsOnly()"` | Statisches Markup, keine Nutzereingabe |
| Export-Datei → `importAllData()` | Feldnamen-Kompatibilität ist die Rücklade-Garantie |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-09 | Tampering | Export mutiert oder löscht Daten (Integrität, Kernwert) | high | mitigate | Funktionskörper ohne `idbSet`/`delete`/`= {}` (statischer Fall 8 + awk-Gate); Nicht-Mutations-Test (Fall 5); nur `bcSpeichernJetzt()` (führt bereits geplante Writes aus, erzeugt keine neuen) |
| T-4-12 | Information Disclosure | Bilddaten liegen als Klartext-JSON auf der Festplatte | low | accept | Einzelnutzer, lokales Werkzeug, identisch zum bestehenden Komplett-Backup; Nutzer löst den Export bewusst aus |
| T-4-13 | Denial of Service | Sehr große Sammlungen sprengen `JSON.stringify` (V8-String-Limit) | medium | mitigate | Reuse von `_jsonParts` (stückweise) + `Blob` — derselbe Pfad, den `exportAllData` dafür bereits nutzt |
| T-4-14 | Tampering | index.html-Edit zerstört Nachbarsektionen | medium | mitigate | Edit mit exaktem Anker; numstat-Deletions = 0; Fall 9 fixiert Reihenfolge und Einmaligkeit |
| T-4-07 | Tampering | Git-Staging fremder Dateien | medium | mitigate | `git add items.js index.html`; Commit-Pfad-Gate |
| T-4-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase |
</threat_model>

<verification>
- `npx vitest run tests/screenshot-export.test.js` → 9 passed; `npm test` → 18 Testdateien grün.
- index.html gegenüber `$BASE` nur Insertions; zwei Commits (test → feat).
- Phase-Gate (VALIDATION.md 4-03-01): ✅.
</verification>

<success_criteria>
- SPLIT-07: Ein Klick im Tweaks-Panel erzeugt ein restore-kompatibles JSON aller drei Screenshot-Maps; leere Sammlung und Fehler werden sichtbar gemeldet; nichts wird mutiert.
</success_criteria>

## Artifacts this phase produces

Phasenweite Liste (Besitzer-Plan in Klammern; dieser Plan: 04-03):

**Neue Dateien**
- `persistence.js` (04-01, erweitert in 04-04) — IDB-Helfer `_idbOpen`/`idbGet`/`idbSet`, Migrations-IIFE, `_debounce`; ab 04-04 Object-Store `screenshots`, `idbScreenshotBatch`/`idbScreenshotPut`/`idbScreenshotDelete`/`idbScreenshotGetAll`/`idbScreenshotKeys`, `_migrateScreenshotsToStore`, `_screenshotStoreReady`, `SCREENSHOT_LEGACY_KEYS`, `SCREENSHOT_MIGRATION_KEY`
- `bridge.js` (04-02) — `APP`, `bcSend`, `_bridgeSenderOk`, `manualReconnect`, `startPingRetry`, `_heartbeatCheck`, Registry `onBridgeMessage`/`offBridgeMessage`/`_bridgeDispatch`, der einzige `message`-Listener des Tools
- `docs/LOAD-ORDER.md` (04-01, ergänzt in 04-02) — dokumentierte Ladereihenfolge, Guard-Verhalten, Anleitung für neue Module und Nachrichtentypen
- Tests: `tests/persistence-module.test.js`, `tests/load-order-guard.test.js` (04-01); `tests/bridge-registry.test.js` (04-02); `tests/screenshot-export.test.js` (04-03); `tests/screenshot-migration.test.js`, `tests/screenshot-store.test.js` (04-04)

**Neue Symbole / Strukturen**
- Ladereihenfolge-Guard `#loadOrderFatal` in items.js (04-01, erweitert 04-02) und bot-ui.js (04-02)
- `CORE_SCRIPTS`/`expandLoadOrder` in `tests/helpers/loadScript.js` (04-01/04-02), `IDBKeyRange` in der Sandbox (04-04)
- `exportScreenshotsOnly()` + Tweaks-Sektion `🖼️ Screenshot-Speicher` mit Button „📷 Screenshots exportieren“ und `#screenshotStoreInfo` (04-03)
- IndexedDB `BCKonfigurator` Version 2 mit Object-Store `screenshots` (id `<kind>|<key>`, kinds `profile`/`lscg`/`wheel`), kv-Marker `BC_SCREENSHOT_MIGRATION_v1`, `_screenshotShadow`/`_screenshotFlush`/`_renderScreenshotStoreInfo` in items.js (04-04)

**Geänderte Dateien**: items.js (alle Pläne), index.html (04-01/02/03: nur Einfügungen bzw. eine Kommentarzeile), bot-ui.js (04-02), bestehende Tests mit Datei-Ort-Aussagen (04-02, 04-04)

<output>
Create `.planning/phases/04-entflechtung/04-03-SUMMARY.md` when done — mit RED-/GREEN-Ausgaben, `git diff --numstat $BASE HEAD -- index.html` und der Commit-Liste.
</output>
