---
phase: 04-entflechtung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/persistence-module.test.js
  - persistence.js
  - items.js
  - tests/helpers/loadScript.js
  - tests/load-order-guard.test.js
  - index.html
  - docs/LOAD-ORDER.md
autonomous: true
requirements: [SPLIT-01, SPLIT-04]
tags: [persistence, indexeddb, extraction, dual-export, load-order, vm-sandbox, red-green]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SPLIT-01: `persistence.js` enthält `_IDB_NAME`/`_IDB_VERSION`/`_IDB_STORE`/`_IDB_DB`, `_idbOpen`, `idbGet`, `_idbFehlerGemeldet`, `idbSet`, die generische localStorage→IDB-Migrations-IIFE und `_debounce` — als wörtlich verschobener Block (items.js Zeilen 5-96 bei HEAD 9b41340, byte-identisch per `diff`-Gate), und items.js enthält diese Definitionen nicht mehr (Orchestrator-Entscheidung 1, T-4-01)"
    - "SPLIT-01: `persistence.js` läuft unverändert als klassisches `<script>` (Funktionsdeklarationen bleiben implizite Globals, kein `window.x = x`, kein `type=\"module\"`, kein `export`/`import`) UND ist in Vitest importierbar: `createRequire(import.meta.url)('../persistence.js')` liefert `{ idbGet, idbSet, _idbOpen, _debounce }` über den geguardeten `module.exports`-Schwanz; in der vm-Sandbox ohne `module` bleibt der Schwanz wirkungslos"
    - "SPLIT-01: Die 36 Ad-hoc-`localStorage.*`-Stellen in items.js (Favoriten, `BC_CACHE_v12`-Spiegel, `BC_RoomEver_v1`, Themes, …) bleiben unangetastet (RESEARCH-Scope-Guardrail); nur die eine generische Migrations-IIFE zieht um"
    - "SPLIT-04: Fehlt `persistence.js` vor items.js, bricht items.js in der ERSTEN Anweisung mit einer sichtbaren roten Box (`#loadOrderFatal`, deutscher Text „FATAL: persistence.js … nicht vor items.js geladen – Ladereihenfolge in index.html prüfen (siehe docs/LOAD-ORDER.md)“) ab und wirft — kein stiller `ReferenceError` tief in einer Tab-Funktion (T-4-02)"
    - "SPLIT-04: Die Ladereihenfolge ist zweifach dokumentiert: als HTML-Kommentar `LADEREIHENFOLGE` direkt über dem `document.write`-Block in index.html und in `docs/LOAD-ORDER.md`; index.html wird nur um Einfügungen erweitert (`git diff --numstat 9b41340 HEAD -- index.html` Deletions = 0)"
    - "Alle bestehenden Tests bleiben nach jedem Task grün: `tests/helpers/loadScript.js` expandiert `loadScript(['items.js', …])` automatisch zu `['persistence.js', 'items.js', …]` (`CORE_SCRIPTS`/`expandLoadOrder`), sodass keine der 16 bestehenden `loadScript(['items.js'…])`-Aufrufstellen angefasst werden muss; `loadInto` bleibt roh (für den Guard-Test)"
  artifacts:
    - path: "persistence.js"
      provides: "Wörtlich verschobene IDB-Helfer + Migrations-IIFE + `_debounce`; Kopfkommentar (Zweck, Dual-Export, Ladereihenfolge); Export-Schwanz `if (typeof module !== 'undefined' && module.exports) { module.exports = { idbGet, idbSet, _idbOpen, _debounce }; }`"
      contains: "const _IDB_NAME    = 'BCKonfigurator';"
    - path: "items.js"
      provides: "Beginnt mit dem Ladereihenfolge-Guard (IIFE, Tabelle `[['idbGet', 'persistence.js']]`), danach die bisherige Zeile 97 (`const _debouncedSaveCurseDB …`); keine `indexedDB.open`, keine `function idbGet/idbSet/_idbOpen/_debounce` mehr"
      contains: "loadOrderFatal"
    - path: "tests/helpers/loadScript.js"
      provides: "`export const CORE_SCRIPTS = ['persistence.js', 'items.js']`, `export function expandLoadOrder(files)`; `loadScript` lädt `expandLoadOrder(files)`; Kopfkommentar ergänzt"
      contains: "export function expandLoadOrder(files)"
    - path: "tests/persistence-module.test.js"
      provides: "SPLIT-01: CJS-Require-Import + Round-Trip gegen fake-indexeddb, Sandbox-Klassik-Pfad (kein `module` → kein Export, Globals vorhanden), Sandbox mit `module`-Objekt → Export gefüllt, `expandLoadOrder`-Vertrag, statisches Extraktions-Gate (0× in items.js, 1× in persistence.js)"
      contains: "createRequire"
    - path: "tests/load-order-guard.test.js"
      provides: "SPLIT-04: items.js ohne persistence.js wirft und rendert `#loadOrderFatal` mit deutschem Text; mit persistence.js kein Throw; index.html: Kommentar + `persistence.js`-Write VOR `items.js`-Write; docs/LOAD-ORDER.md existiert und nennt alle Module"
      contains: "loadOrderFatal"
    - path: "index.html"
      provides: "HTML-Kommentar `LADEREIHENFOLGE` + `document.write('<scr'+'ipt src=\"persistence.js?_='+_cbv+'\"><\\/scr'+'ipt>');` unmittelbar vor der items.js-Zeile im selben `<script>`-Block"
      contains: "persistence.js?_="
    - path: "docs/LOAD-ORDER.md"
      provides: "Deutsche Doku: Reihenfolge-Tabelle (bc-icons → persistence.js → items.js → money.js … bc-autobackup.js), Begründung (document.write-Kette, ein globaler Scope, kein Bundler), Guard-Verhalten (`#loadOrderFatal`), Anleitung „neues Modul hinzufügen“, Test-Seite (`CORE_SCRIPTS`/`expandLoadOrder`)"
      contains: "persistence.js"
  key_links:
    - from: "index.html `<script>`-Block mit `var _cbv = Date.now();`"
      to: "persistence.js, dann items.js"
      via: "Zwei `document.write`-Zeilen im SELBEN Block; klassische Skripte teilen den globalen Scope, `document.write` während des Parsens führt das Skript sofort aus (RESEARCH Pitfall 1)"
      pattern: "persistence\\.js\\?_="
    - from: "items.js Zeile 1 (Guard-IIFE)"
      to: "persistence.js `idbGet` (Funktionsdeklaration → `window.idbGet`)"
      via: "`typeof window[name] !== 'function'` je Tabellenzeile; fehlt etwas → Box + `throw new Error(msg)`"
      pattern: "loadOrderFatal"
    - from: "tests/helpers/loadScript.js `loadScript(files)`"
      to: "`expandLoadOrder(files)` → `loadInto(sandbox, file)` je Datei"
      via: "Fehlende `CORE_SCRIPTS`-Vorläufer werden vor dem ersten `items.js` eingefügt; ohne `items.js` in `files` keine Änderung"
      pattern: "expandLoadOrder\\(files\\)"
    - from: "tests/persistence-module.test.js"
      to: "persistence.js `module.exports`"
      via: "`createRequire(import.meta.url)` aus `node:module` (Repo-Root ist `type: commonjs`, tests/ ist `type: module`)"
      pattern: "module\\.exports"
  prohibitions:
    - requirement_id: SPLIT-01
      category: safety
      verification: automated
      statement: "Kein Verhaltensunterschied: der Codeblock items.js 5-96 (HEAD 9b41340) erscheint byte-identisch in persistence.js (`diff` leer); keine Änderung an idbGet/idbSet-Aufrufstellen in money.js/rank.js/shop.js/inventar.js/bot-data.js/bot-ui.js/outfit-import.js/bc-autobackup.js; loader.js unberührt"
    - requirement_id: SPLIT-01
      category: safety
      verification: automated
      statement: "Kein Build-Schritt, keine neue Dependency, kein `type=\"module\"`, kein `export`/`import`-Statement in persistence.js; keine Doppeldefinition (`grep -c '^async function idbGet' items.js persistence.js` = `0` / `1`)"
    - requirement_id: SPLIT-01
      category: safety
      verification: judgment
      statement: "Die 36 Ad-hoc-`localStorage.getItem/setItem`-Stellen in items.js werden NICHT nach persistence.js verschoben (RESEARCH Anti-Pattern „Sweeping“)"
    - requirement_id: SPLIT-04
      category: safety
      verification: automated
      statement: "index.html nur Einfügungen (`git diff --numstat 9b41340 HEAD -- index.html` Deletions = 0); die `document.write`-Reihenfolge der neun Feature-Module (Block ~Zeile 3523) bleibt unverändert"
    - requirement_id: SPLIT-04
      category: safety
      verification: judgment
      statement: "`git add` nur mit explizit genannten Pfaden; RED-Test und GREEN-Implementierung in getrennten Commits; keine Watch-Flags (`vitest run`); `.planning/milestone.lock` nie stagen"
---

<objective>
Erster Entflechtungsschritt: Die IndexedDB-Helfer (`_idbOpen`, `idbGet`, `idbSet`), die generische localStorage→IDB-Migrations-IIFE und `_debounce` ziehen wörtlich (Zeilen 5-96 von items.js) in die neue Datei `persistence.js` um — klassisches `<script>` mit geguardetem `module.exports`-Schwanz (Dual-Export, RESEARCH Pattern 1, Orchestrator-Entscheidung 1). Gleichzeitig bekommt items.js den Ladereihenfolge-Guard (sichtbare rote Box statt stillem Ausfall, SPLIT-04), index.html die neue `document.write`-Zeile plus Reihenfolge-Kommentar, und `docs/LOAD-ORDER.md` dokumentiert die Kette. Der Test-Loader expandiert `loadScript(['items.js'])` automatisch um `persistence.js`, damit alle 149+2 bestehenden Tests ohne Änderung ihrer Aufrufstellen grün bleiben.

Purpose: Erfolgskriterien 1 und 3 der Phase — Persistenz ist ein eigenständiges, direkt testbares Modul; ein fehlendes Modul fällt beim Start laut auf. Plan 04-02 baut darauf auf (bridge.js wird als zweites Modul in dieselbe Kette und denselben Guard eingehängt), Plan 04-04 erweitert `_idbOpen` in persistence.js um den `screenshots`-Store.
Output: `persistence.js` (neu), `items.js` (Zeilen 1-96 entfernt, Guard am Anfang), `tests/helpers/loadScript.js` (`CORE_SCRIPTS`/`expandLoadOrder`), `tests/persistence-module.test.js` (neu), `tests/load-order-guard.test.js` (neu), `index.html` (2 Einfügungen), `docs/LOAD-ORDER.md` (neu).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-entflechtung/04-RESEARCH.md
@.planning/phases/04-entflechtung/04-VALIDATION.md
@.planning/phases/03-bridge-haertung/03-01-SUMMARY.md

Planer-Probe (2026-09-14, HEAD `9b41340`) — verbindliche Befunde:
1. items.js Zeilen 1-96 sind der komplette Persistenz-Block: 1-4 Kopfkommentar, 5-8 Konstanten (`_IDB_NAME`, `_IDB_VERSION = 1`, `_IDB_STORE = 'kv'`, `let _IDB_DB = null`), 10-21 `_idbOpen`, 23-33 `idbGet`, 35-40 Kommentar + 41 `let _idbFehlerGemeldet = 0;`, 42-65 `idbSet` (Quota-Pfad, `typeof showStatus === 'function'`-Guard), 67-87 Migrations-IIFE (`'BC_Money_v1','BC_Rank_v1','BC_Shop_v1','BC_Bots_v2','BC_BotGroups_v1'`, …), 89-96 `_debounce`. Zeile 97 (`const _debouncedSaveCurseDB = _debounce(function() { _saveCurseDB(); }, 1200);`) und folgende bleiben in items.js (referenzieren items.js-Funktionen). `indexedDB.` kommt in items.js ausschließlich in Zeile 13 vor; `_IDB_DB`/`_IDB_STORE` nur in Zeilen 5-28.
2. Cross-File-Aufrufer von `idbGet`/`idbSet` (RESEARCH-Tabelle, verifiziert): bot-data.js 10, inventar.js 4, outfit-import.js 4, shop.js 2, rank.js 2, money.js 2, bc-autobackup.js 2, bot-ui.js 1 — alle laden per `document.write` NACH items.js (index.html Block ~3523-3532), daher genügt EINE Einfügestelle vor items.js.
3. index.html ~3088-3092: `<script>` / `// Cache-Buster: …` / `var _cbv = Date.now();` / `document.write('<scr'+'ipt src="items.js?_='+_cbv+'"><\/scr'+'ipt>');` / `</script>`. Feature-Kette ~3523-3532: money.js, rank.js, shop.js, inventar.js, bot-data.js, bot-ui.js, bot-engine.js, outfit-import.js, bc-autobackup.js. Vorab im `<head>` (Zeilen 12-13): `bc-icons.js`, `bc-icons-ergaenzung.js` als plain `<script src>`; lz-string per CDN Zeile 11. `<body>` beginnt Zeile 2355 → beim Ausführen von items.js existiert `document.body`.
4. tests/helpers/loadScript.js: `loadScript(files, extraGlobals)` loopt `loadInto(sandbox, file)`; `makeSandbox` liefert `document.createElement()` → frisches `makeElementStub()`, `document.body` = Stub mit `appendChild`; `sandbox.window = sandbox`; Funktionsdeklarationen der geladenen Dateien werden Sandbox-Properties (`ctx.idbGet`), Top-Level-`let`/`const` nicht (→ `evalIn`). 16 Aufrufstellen `loadScript(['items.js'…])` in 12 Testdateien; `tests/load-script.test.js` erwartet: `loadScript(['bot-data.js'])` wirft `/idbGet is not defined/`, `loadScript(['items.js','items.js'])` wirft SyntaxError, `loadScript([])` liefert leere Sandbox — alle drei bleiben mit der Expansion wahr (Expansion greift nur, wenn `'items.js'` in `files` steht, und fügt nur FEHLENDE Vorläufer vor dem ersten `items.js` ein).
5. Node v24.12.0: `typeof localStorage === 'undefined'`, bare Zugriff wirft ReferenceError. Die Migrations-IIFE fängt das je Schlüssel (`try/catch` → 9× `console.warn`). Der CJS-Require-Test setzt deshalb VOR dem `require` ein `globalThis.localStorage`-Stub (`getItem: () => null`, `setItem() {}`, `removeItem() {}`), damit die Ausgabe sauber bleibt. `globalThis.indexedDB` kommt aus `tests/setup/fake-indexeddb.js` (setupFiles) und ist im required Modul dieselbe Instanz wie in den Sandboxen derselben Testdatei.
6. Repo-Root `package.json` `type: commonjs` (Phase-1-Entscheidung, `node --check` auf Produktionsdateien), `tests/package.json` `type: module` → in Tests `import { createRequire } from 'node:module'; const require = createRequire(import.meta.url); require('../persistence.js')` lädt die Datei als CJS und liefert `module.exports`.
7. `git status --short` zeigt bei HEAD `9b41340` nur `?? .planning/milestone.lock`; `npm test` = 14 Dateien, 149 passed + 2 expected fail (Baseline dieser Phase).
8. `docs/` existiert bereits (enthält `screenshots/`); `docs/LOAD-ORDER.md` ist neu.
</context>

<tasks>

<!-- Comment-Text-Discipline-Allowlist (#429): die folgenden Literale beschreiben Testinhalte bzw. Gates für ANDERE Dateien und müssen im Plan wörtlich stehen -->
<!-- planner-discipline-allow: indexedDB.open( -->
<!-- planner-discipline-allow: window.idbGet -->
<!-- planner-discipline-allow: 'BC_Money_v1','BC_Rank_v1','BC_Shop_v1','BC_Bots_v2','BC_BotGroups_v1' -->
<!-- planner-discipline-allow: if (typeof module !== 'undefined' && module.exports) -->
<!-- planner-discipline-allow: ^\s*(export|import)\s -->
<!-- planner-discipline-allow: loadOrderFatal -->
<!-- planner-discipline-allow: [['idbGet', 'persistence.js']] -->
<!-- planner-discipline-allow: idbGet( -->

<task type="auto">
  <name>Task 1: Persistenz-Modul-Test schreiben und RED bestätigen — `tests/persistence-module.test.js` (SPLIT-01, Wave 0)</name>
  <precondition>HEAD ist `9b41340` oder ein Nachfolger ohne Phase-4-Änderungen; `git status --short` außer `.planning/milestone.lock` leer; `npm test` = 14 Dateien grün (149 passed + 2 expected fail)</precondition>
  <files>tests/persistence-module.test.js</files>
  <read_first>tests/idb-helpers.test.js (Round-Trip-Muster, `beforeAll`, Monkeypatch-Restauration); tests/load-script.test.js (Edge-Cases der Sandbox, `loadScript([])`, Doppelladen); tests/helpers/loadScript.js (gesamte Datei — `makeSandbox`, `loadInto`, `loadScript`, `evalIn`); items.js Zeilen 1-100 (der zu verschiebende Block, für die statischen Marker); .planning/phases/04-entflechtung/04-RESEARCH.md „Pattern 1: Dual-export global script“ und „Pitfall 2“</read_first>
  <action>
    `tests/persistence-module.test.js` (neu, ESM) anlegen. Imports: `describe, it, expect, beforeAll` aus vitest; `fs` aus `node:fs`, `path` aus `node:path`; `createRequire` aus `node:module`; `loadScript, loadInto, makeSandbox, evalIn, expandLoadOrder, CORE_SCRIPTS, REPO_ROOT` aus `./helpers/loadScript.js`. Kopfkommentar (Deutsch): SPLIT-01 — persistence.js ist klassisches Skript UND per Dual-Export importierbar; Bezug auf Orchestrator-Entscheidung 1 und RESEARCH Pattern 1.

    Helfer `src(file)` → `fs.readFileSync(path.join(REPO_ROOT, file), 'utf8')`; `count(hay, needle)` → `hay.split(needle).length - 1`.

    `describe('persistence.js per CJS-Require (Dual-Export, Pattern B)')`:
    1. `beforeAll`: falls `typeof globalThis.localStorage === 'undefined'`, Stub setzen (Befund 5); dann `mod = createRequire(import.meta.url)('../persistence.js')`.
    2. `it('exportiert idbGet, idbSet, _idbOpen, _debounce als Funktionen')`: alle vier `typeof === 'function'`; `Object.keys(mod).sort()` enthält genau diese vier Namen (`toEqual(['_debounce', '_idbOpen', 'idbGet', 'idbSet'])`).
    3. `it('idbSet → idbGet Round-Trip über das required Modul gegen fake-indexeddb')`: `await mod.idbSet('PM_rt', { a: 1 })` → `true`; `await mod.idbGet('PM_rt')` → `toEqual({ a: 1 })`; `await mod.idbGet('PM_missing')` → `null`.
    4. `it('_debounce ruft fn nach Ablauf genau einmal mit den letzten Argumenten')`: `const calls = []; const d = mod._debounce((...a) => calls.push(a), 5); d(1); d(2);` → nach `await new Promise(r => setTimeout(r, 30))` ist `calls` `[[2]]`.

    `describe('persistence.js als klassisches Skript in der vm-Sandbox')`:
    5. `it('ohne module-Global: Funktionen sind Globals, kein Export-Versuch')`: `ctx = loadScript(['persistence.js'])`; `typeof ctx.idbGet/idbSet/_idbOpen/_debounce === 'function'`; `evalIn(ctx, 'typeof module')` → `'undefined'`; `evalIn(ctx, '_IDB_STORE')` → `'kv'`; `evalIn(ctx, '_IDB_NAME')` → `'BCKonfigurator'`.
    6. `it('mit module-Global füllt der Export-Schwanz module.exports')`: `const module = { exports: {} }; loadScript(['persistence.js'], { module })`; `typeof module.exports.idbGet === 'function'`; `module.exports.idbSet === ctx.idbSet` ist NICHT verlangt (andere Sandbox) — stattdessen `Object.keys(module.exports).sort()` = `['_debounce', '_idbOpen', 'idbGet', 'idbSet']`.
    7. `it('Round-Trip über die Sandbox (wie tests/idb-helpers.test.js, aber ohne items.js)')`: `ctx = loadScript(['persistence.js'])`; `await ctx.idbSet('PM_sb', [1, 2])` → true; `JSON.stringify(await ctx.idbGet('PM_sb'))` → `'[1,2]'`.

    `describe('loadScript expandiert die Kern-Reihenfolge (CORE_SCRIPTS)')`:
    8. `it('CORE_SCRIPTS beginnt mit persistence.js und endet mit items.js')`: `CORE_SCRIPTS[0] === 'persistence.js'`, `CORE_SCRIPTS.at(-1) === 'items.js'`.
    9. `it('expandLoadOrder fügt fehlende Vorläufer vor dem ersten items.js ein und lässt alles andere unangetastet')`: `expandLoadOrder(['items.js'])` → `[...CORE_SCRIPTS]`; `expandLoadOrder(['items.js', 'bot-data.js'])` → `[...CORE_SCRIPTS, 'bot-data.js']`; `expandLoadOrder(['persistence.js', 'items.js'])` → `['persistence.js', 'items.js']` (kein Duplikat); `expandLoadOrder(['bot-data.js'])` → `['bot-data.js']`; `expandLoadOrder([])` → `[]`; das Eingabe-Array wird nicht mutiert (`const inp = ['items.js']; expandLoadOrder(inp); expect(inp).toEqual(['items.js'])`).
    10. `it('loadScript([\"items.js\"]) liefert eine Sandbox mit idbGet aus persistence.js')`: `ctx = loadScript(['items.js'])`; `typeof ctx.idbGet === 'function'`; `evalIn(ctx, '_IDB_VERSION')` ist eine Zahl ≥ 1.
    11. `it('loadInto bleibt roh: items.js allein ohne persistence.js scheitert')`: `expect(() => loadInto(makeSandbox(), 'items.js')).toThrow()` (Fehlerart hier egal — Task 3 präzisiert die Guard-Meldung in tests/load-order-guard.test.js).

    `describe('statisches Extraktions-Gate (Pitfall 2: keine Doppeldefinition)')`:
    12. `it('Definitionen liegen genau einmal in persistence.js und nicht mehr in items.js')`: für jeden Marker in `['function _idbOpen()', 'async function idbGet(key)', 'async function idbSet(key, value)', 'function _debounce(fn, delay)', 'indexedDB.open(', "const _IDB_NAME    = 'BCKonfigurator';", "'BC_Money_v1','BC_Rank_v1','BC_Shop_v1','BC_Bots_v2','BC_BotGroups_v1'", 'let _idbFehlerGemeldet = 0;']`: `count(src('persistence.js'), marker) === 1` und `count(src('items.js'), marker) === 0`.
    13. `it('persistence.js ist klassisch: kein type=module, kein export/import-Statement, geguardeter module.exports-Schwanz')`: `src('persistence.js')` matcht NICHT `/^\s*(export|import)\s/m`; enthält genau 1× `if (typeof module !== 'undefined' && module.exports)`; enthält 0× `window.idbGet =` (Globals bleiben implizit, Entscheidung 1).
    14. `it('items.js referenziert kein Top-Level-window/localStorage-Sweep: die 36 Ad-hoc-localStorage-Stellen bleiben')`: `count(src('items.js'), 'localStorage.')` ≥ 30 (Guardrail: nichts wurde „mitgezogen“); `count(src('persistence.js'), 'localStorage.')` ≤ 3 (nur getItem/removeItem der Migrations-IIFE).

    RED bestätigen: `npx vitest run tests/persistence-module.test.js` MUSS rot sein — erwartet `ENOENT … persistence.js` (require/readFileSync/loadInto) und `expandLoadOrder is not a function` / `CORE_SCRIPTS` undefined. Ist die Datei unerwartet grün: STOPP und eskalieren.

    Commit: `git add tests/persistence-module.test.js`; Message `test(04-01): add failing persistence module tests – dual export, sandbox load-order expansion, static extraction gate (SPLIT-01)`. `git status --short items.js index.html tests/helpers/loadScript.js` muss leer sein.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/persistence-module.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "persistence\.js|expandLoadOrder|CORE_SCRIPTS" "$OUT" && grep -Eq "Tests +[0-9]+ failed|Test Files +1 failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Vitest endete mit Exit 0 (persistence.js oder expandLoadOrder existieren bereits), oder die Ausgabe nennt weder `persistence.js` noch `expandLoadOrder`/`CORE_SCRIPTS` noch eine `failed`-Zeile</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "createRequire" tests/persistence-module.test.js` >= 2 (Import + Aufruf); `grep -c "expandLoadOrder" tests/persistence-module.test.js` >= 5; `grep -c "CORE_SCRIPTS" tests/persistence-module.test.js` >= 3
    - `grep -Fc "if (typeof module !== 'undefined' && module.exports)" tests/persistence-module.test.js` = 1; `grep -c "indexedDB.open(" tests/persistence-module.test.js` >= 1; `grep -c "BC_BotGroups_v1" tests/persistence-module.test.js` >= 1
    - `grep -c "loadInto(makeSandbox(), 'items.js')" tests/persistence-module.test.js` = 1; `grep -c "globalThis.localStorage" tests/persistence-module.test.js` >= 1
    - `test "$(grep -v '^\s*//' tests/persistence-module.test.js | grep -c '\.only(')" = 0`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/persistence-module.test.js`; `git status --short items.js index.html tests/helpers/loadScript.js` leer
  </acceptance_criteria>
  <done>14 Fälle in vier describe-Blöcken; Lauf nachweislich rot, weil persistence.js und die Loader-Expansion fehlen; roter Test committet; Produktionsdateien und Loader unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `persistence.js` wörtlich extrahieren, items.js kürzen, Loader-Expansion — GREEN, chirurgisch committen (SPLIT-01)</name>
  <precondition>Task 1 committet; `git status --short` außer `.planning/milestone.lock` leer; `sed -n '5p;96p' items.js` liefert `const _IDB_NAME    = 'BCKonfigurator';` und `}` (Blockgrenzen unverändert seit 9b41340)</precondition>
  <reversibility rating="reversible">Reiner Dateischnitt ohne Schema- oder Protokolländerung; `git revert` des GREEN-Commits stellt items.js 1:1 wieder her, IDB-Inhalte sind unberührt.</reversibility>
  <files>persistence.js, items.js, tests/helpers/loadScript.js</files>
  <read_first>items.js Zeilen 1-110 (Block + erste bleibende Zeilen); tests/helpers/loadScript.js (gesamte Datei); tests/persistence-module.test.js (Task 1, insbesondere Fälle 9, 12, 13); .planning/phases/04-entflechtung/04-RESEARCH.md „Verified persistence surface“, „Pattern 1“, „Pitfall 2“; .planning/research/STACK.md Abschnitt „Pattern B“ (Begründung gegen `type=\"module\"`)</read_first>
  <behavior>
    - Test 1-4: `require('../persistence.js')` liefert genau `{ _debounce, _idbOpen, idbGet, idbSet }`; Round-Trip und `_debounce` funktionieren über das required Modul
    - Test 5-7: In der Sandbox ohne `module` sind die vier Funktionen Globals, `typeof module` ist `'undefined'`; mit `module`-Objekt wird `module.exports` gefüllt
    - Test 8-11: `CORE_SCRIPTS = ['persistence.js', 'items.js']`; `expandLoadOrder` fügt nur fehlende Vorläufer vor dem ersten `items.js` ein, mutiert nichts; `loadInto` bleibt roh
    - Test 12-14: Jeder Marker genau 1× in persistence.js, 0× in items.js; persistence.js ohne `export`/`import`/`window.x =`; localStorage-Sweep-Guardrail
    - Volle Suite: 15 Dateien grün (149 + 14 neue passed, 2 expected fail unverändert)
  </behavior>
  <action>
    1. **persistence.js anlegen** (Write-Tool, neue Datei, Repo-Root). Aufbau:
       - Kopfkommentar (Deutsch, Blockkommentar-Stil wie items.js Zeilen 1-4 mit `// ══…`): „Persistenz-Modul (SPLIT-01) — IndexedDB-Helfer, einmalige localStorage→IDB-Migration und `_debounce`. Wörtlich aus items.js (Zeilen 5-96, Stand 9b41340) verschoben, ohne Verhaltensänderung. Muss VOR bridge.js und items.js geladen werden (siehe docs/LOAD-ORDER.md). Klassisches Skript: Funktionsdeklarationen sind implizite Globals; der Schwanz exportiert zusätzlich für Vitest/CJS (Dual-Export, RESEARCH Pattern 1). Kein `type=\"module\"` — bricht unter `file://`.“
       - Danach der Block items.js Zeilen 5-96 **byte-identisch** (per `sed -n '5,96p' items.js` extrahieren und einfügen — nicht abtippen). Enthält: Konstanten, `_idbOpen`, `idbGet`, Kommentar + `_idbFehlerGemeldet`, `idbSet`, Migrations-IIFE, `// ── Utility: Debounce …` + `_debounce`.
       - Export-Schwanz (mit Kommentar „Dual-Export für Vitest (CJS-Require); im Browser ist `module` undefined“): `if (typeof module !== 'undefined' && module.exports) { module.exports = { idbGet, idbSet, _idbOpen, _debounce }; }` — genau diese vier, keine `window.x = x`-Zeilen (Entscheidung 1).
    2. **items.js kürzen**: Zeilen 1-96 löschen (Kopfkommentar 1-4 zieht sinngemäß mit; er beschreibt ja die verschobenen Helfer). Die Datei beginnt danach mit einem neuen kurzen Sektionskommentar `// ── Gedrosselte Speicher-/Render-Aufrufe (nutzen _debounce aus persistence.js) ──` gefolgt von der bisherigen Zeile 97 `const _debouncedSaveCurseDB = _debounce(function() { _saveCurseDB(); }, 1200);`. Nichts anderes anfassen; insbesondere keine der `localStorage.*`-Stellen. Gate: `grep -c "indexedDB.open(" items.js` = 0; `grep -c "^async function idbGet" items.js` = 0.
    3. **tests/helpers/loadScript.js erweitern** (Edit-Tool, kleine Einfügungen):
       - Kopfkommentar um einen Absatz ergänzen: „Phase 4 (SPLIT-01): `loadScript` expandiert `files` um fehlende Kern-Vorläufer (`CORE_SCRIPTS`) vor dem ersten `items.js`, weil persistence.js (und ab Plan 04-02 bridge.js) im Browser VOR items.js laufen. `loadInto` bleibt roh — der Ladereihenfolge-Guard-Test lädt items.js absichtlich ohne Vorläufer.“
       - Nach `export const SANDBOX_ORIGIN …`: `export const CORE_SCRIPTS = ['persistence.js', 'items.js'];` und `export function expandLoadOrder(files)`: `idx = files.indexOf('items.js')`; ist `idx === -1` → `files.slice()`; sonst `missing = CORE_SCRIPTS.filter(f => f !== 'items.js' && !files.includes(f))`; Rückgabe `[...files.slice(0, idx), ...missing, ...files.slice(idx)]`.
       - In `loadScript`: `for (const file of expandLoadOrder(files))` statt `for (const file of files)`; der `Array.isArray`-TypeError-Check bleibt davor.
    4. `node --check persistence.js items.js` → Exit 0. GREEN: `npx vitest run tests/persistence-module.test.js` → 14 passed; `npm test` → 15 Dateien grün, 163 passed + 2 expected fail (149 + 14).
    5. Verbatim-Gate ausführen und Ausgabe fürs SUMMARY festhalten: `diff <(git show 9b41340:items.js | sed -n '5,96p') <(awk '/^const _IDB_NAME/{f=1} f' persistence.js | head -92)` → leer (Exit 0).
    6. Commit: `git add persistence.js items.js tests/helpers/loadScript.js`; `git diff --cached --name-only | sort` = `items.js`, `persistence.js`, `tests/helpers/loadScript.js`; Message `refactor(04-01): extract IDB helpers, localStorage→IDB migration and _debounce verbatim into persistence.js with dual export (SPLIT-01)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/persistence-module.test.js && node --check persistence.js && node --check items.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` in der `Tests`-Summary — etwa weil der Block nicht byte-identisch ist (Marker-Zählung), `module.exports` unguarded ist (Sandbox-Test 5 wirft), `expandLoadOrder` das Eingabe-Array mutiert oder Duplikate erzeugt (Doppellade-Test in load-script.test.js kippt), oder eine der 16 bestehenden `loadScript(['items.js'…])`-Stellen `idbGet` nicht mehr findet</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && diff <(git show 9b41340:items.js | sed -n '5,96p') <(awk '/^const _IDB_NAME/{f=1} f' persistence.js | head -92) && test "$(grep -c 'indexedDB.open(' items.js)" = 0 && test "$(grep -c '^async function idbGet' items.js)" = 0 && test "$(grep -c '^async function idbGet' persistence.js)" = 1 && test "$(grep -c '^function _debounce' items.js)" = 0 && test "$(grep -Fc "if (typeof module !== 'undefined' && module.exports)" persistence.js)" = 1 && test "$(grep -c 'window.idbGet' persistence.js)" = 0 && test "$(grep -c 'export function expandLoadOrder' tests/helpers/loadScript.js)" = 1 && test "$(grep -c 'localStorage\.' items.js)" -ge 30 && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "items.js persistence.js tests/helpers/loadScript.js " && echo PERSIST-OK</automated>
    <fails_when>`PERSIST-OK` fehlt — der verschobene Block weicht vom Original ab (diff nicht leer), Definitionen sind doppelt oder fehlen, der Export ist nicht geguardet, `window.idbGet =` wurde entgegen Entscheidung 1 eingefügt, localStorage-Stellen wurden mitverschoben, oder der Commit enthält andere Pfade als die drei genannten</fails_when>
  </verify>
  <acceptance_criteria>
    - `diff <(git show 9b41340:items.js | sed -n '5,96p') <(awk '/^const _IDB_NAME/{f=1} f' persistence.js | head -92)` → leer
    - `grep -c "indexedDB.open(" items.js` = 0; `grep -c "indexedDB.open(" persistence.js` = 1; `grep -c "^function _idbOpen" items.js persistence.js` → `items.js:0`, `persistence.js:1`; gleiches für `^async function idbGet`, `^async function idbSet`, `^function _debounce`
    - `grep -Fc "'BC_Money_v1','BC_Rank_v1','BC_Shop_v1','BC_Bots_v2','BC_BotGroups_v1'" items.js` = 0 und in persistence.js = 1
    - `grep -Fc "if (typeof module !== 'undefined' && module.exports)" persistence.js` = 1; `grep -Ec "^\s*(export|import)\s" persistence.js` = 0; `grep -c "window\.idbGet\|window\.idbSet" persistence.js` = 0
    - `head -1 items.js` ist eine Kommentarzeile (Sektionskommentar), `grep -n "^const _debouncedSaveCurseDB" items.js` liegt in den ersten 3 Zeilen
    - `grep -c "export const CORE_SCRIPTS = \['persistence.js', 'items.js'\]" tests/helpers/loadScript.js` = 1; `grep -c "expandLoadOrder(files)" tests/helpers/loadScript.js` >= 2 (Definition + Aufruf in loadScript)
    - `node --check persistence.js items.js` → Exit 0; `npx vitest run tests/persistence-module.test.js` → `14 passed`; `npm test` → `Test Files  15 passed`, `2 expected fail` unverändert
    - `git diff 9b41340 HEAD --stat -- money.js rank.js shop.js inventar.js bot-data.js bot-ui.js bot-engine.js outfit-import.js bc-autobackup.js loader.js index.html` → leer
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` = `items.js`, `persistence.js`, `tests/helpers/loadScript.js`; `git status --short` zeigt nur `?? .planning/milestone.lock`
  </acceptance_criteria>
  <done>persistence.js enthält den byte-identischen Block plus Kopfkommentar und geguardeten Export; items.js beginnt bei der alten Zeile 97; Loader expandiert die Kernreihenfolge; 15 Testdateien grün; ein Commit mit genau drei Pfaden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ladereihenfolge-Guard in items.js, `document.write` + Kommentar in index.html, `docs/LOAD-ORDER.md` — RED → GREEN in zwei Commits (SPLIT-04)</name>
  <precondition>Task 2 committet (`npm test` = 15 Dateien grün); `git status --short items.js index.html` leer</precondition>
  <reversibility rating="reversible">Guard, Kommentar, eine `document.write`-Zeile und eine Doku-Datei; `git revert` stellt alles her, keine Datenberührung.</reversibility>
  <files>tests/load-order-guard.test.js, items.js, index.html, docs/LOAD-ORDER.md</files>
  <read_first>items.js Zeilen 1-5 (nach Task 2: Sektionskommentar + `_debouncedSaveCurseDB`); index.html Zeilen 3086-3094 (`<script>`-Block mit `_cbv`, per `grep -n '_cbv = Date.now()' index.html`) und 3522-3533 (Feature-Kette, per `grep -n 'money.js?_=' index.html`); tests/helpers/loadScript.js (`makeSandbox`, `loadInto`, `makeElementStub`); tests/storage-estimate.test.js (Muster: `document.getElementById`-Override für ein inspizierbares Element); .planning/phases/04-entflechtung/04-RESEARCH.md „Pitfall 1“ und „Code Examples → index.html script tag change“</read_first>
  <behavior>
    - Test A: `loadInto(makeSandbox(), 'items.js')` (ohne persistence.js) wirft `/persistence\.js/`; das per `document.createElement` erzeugte Element hat `id === 'loadOrderFatal'` und `textContent` beginnt mit `FATAL:` und enthält `persistence.js` und `docs/LOAD-ORDER.md`
    - Test B: `loadScript(['items.js'])` (expandiert) wirft nicht; `typeof ctx.idbGet === 'function'`
    - Test C: index.html enthält genau 1× `persistence.js?_=` und dessen Index liegt VOR dem Index von `items.js?_=`; beide im selben `<script>`-Block wie `var _cbv = Date.now();`; ein HTML-Kommentar mit `LADEREIHENFOLGE` steht vor dem Block
    - Test D: `docs/LOAD-ORDER.md` existiert, nennt `persistence.js`, `items.js`, alle neun Feature-Module, `loadOrderFatal` und `CORE_SCRIPTS`
    - Test E: der Guard steht in den ersten 20 Zeilen von items.js und nutzt keine Funktion aus persistence.js (statisch: die Zeilen vor `_debouncedSaveCurseDB` enthalten `loadOrderFatal` und `typeof window[`)
  </behavior>
  <action>
    **RED (Commit 1):** `tests/load-order-guard.test.js` (neu, ESM) mit Imports `describe, it, expect` aus vitest, `fs`/`path`, und `loadScript, loadInto, makeSandbox, makeElementStub, REPO_ROOT` aus `./helpers/loadScript.js`. Kopfkommentar (Deutsch): SPLIT-04 — fehlendes Modul → sichtbare Meldung statt stillem ReferenceError; Reihenfolge doppelt dokumentiert.
    - `it('items.js ohne persistence.js: sichtbare FATAL-Box + Throw, kein tiefer ReferenceError')`: `const sb = makeSandbox(); const created = []; sb.document.createElement = (tag) => { const el = makeElementStub(); el.tagName = tag; created.push(el); return el; };` `expect(() => loadInto(sb, 'items.js')).toThrow(/persistence\.js/)`; `const box = created.find(e => e.id === 'loadOrderFatal')`; `expect(box).toBeDefined()`; `expect(box.textContent).toMatch(/^FATAL: /)`; `toContain('persistence.js')`; `toContain('items.js')`; `toContain('docs/LOAD-ORDER.md')`; `expect(sb.idbGet).toBeUndefined()`.
    - `it('mit persistence.js davor läuft items.js an')`: `const ctx = loadScript(['items.js']); expect(typeof ctx.idbGet).toBe('function'); expect(typeof ctx.showStatus).toBe('function');`
    - `it('Guard ist die erste Anweisung und selbstgenügsam')`: `lines = src('items.js').split('\n')`; Index der Zeile mit `loadOrderFatal` < Index der Zeile mit `const _debouncedSaveCurseDB`; erste 20 Zeilen enthalten `typeof window[`; in diesen Zeilen kommt `idbGet(` NICHT als Aufruf vor (Regex `/idbGet\(/` matcht nicht — der Guard prüft nur den Typ).
    - `it('index.html: Kommentar + persistence.js-Write vor items.js-Write im _cbv-Block')`: `html = src('index.html')`; `count(html, 'persistence.js?_=') === 1`; `html.indexOf('persistence.js?_=') < html.indexOf('items.js?_=')`; `html.indexOf('LADEREIHENFOLGE') !== -1` und `< html.indexOf('var _cbv = Date.now();')`; der `<script>`-Block: `blockStart = html.lastIndexOf('<script>', html.indexOf('var _cbv = Date.now();'))`, `blockEnd = html.indexOf('</script>', blockStart)` → beide `?_=`-Indizes liegen zwischen `blockStart` und `blockEnd`; Feature-Kette unverändert: `for (const m of ['money.js','rank.js','shop.js','inventar.js','bot-data.js','bot-ui.js','bot-engine.js','outfit-import.js','bc-autobackup.js']) count(html, m + '?_=') === 1` und die Reihenfolge ihrer Indizes ist streng aufsteigend.
    - `it('docs/LOAD-ORDER.md dokumentiert Kette, Guard und Test-Expansion')`: `fs.existsSync(path.join(REPO_ROOT, 'docs/LOAD-ORDER.md'))`; Inhalt enthält `persistence.js`, `items.js`, alle neun Feature-Module, `bc-icons.js`, `loadOrderFatal`, `CORE_SCRIPTS`, `document.write`.
    RED bestätigen: `npx vitest run tests/load-order-guard.test.js` rot (Throw-Message ist heute `_debounce is not defined` statt `/persistence\.js/`, keine Box, kein Kommentar, keine Doku). Commit: `git add tests/load-order-guard.test.js`; Message `test(04-01): add failing load-order guard tests – fatal box, index.html order, docs (SPLIT-04)`.

    **GREEN (Commit 2):**
    1. items.js — ganz oben, VOR dem Sektionskommentar aus Task 2, den Guard einfügen (Kommentar + IIFE, Bezeichner Englisch, Texte Deutsch):
       Kommentar: `// ── Ladereihenfolge-Guard (SPLIT-04): persistence.js muss VOR items.js geladen sein (docs/LOAD-ORDER.md). Fehlt ein Modul, bricht items.js hier sichtbar ab statt später still in einer Tab-Funktion. Bewusst ohne Abhängigkeit zu den geprüften Modulen.`
       IIFE: `const required = [['idbGet', 'persistence.js']];` (Plan 04-02 ergänzt bridge.js-Zeilen); `const missing = required.filter(function (e) { return typeof window[e[0]] !== 'function'; }).map(function (e) { return e[1]; }).filter(function (f, i, a) { return a.indexOf(f) === i; });` `if (!missing.length) return;` `const msg = 'FATAL: ' + missing.join(', ') + ' wurde nicht vor items.js geladen – Ladereihenfolge in index.html prüfen (siehe docs/LOAD-ORDER.md)';` `try { const box = document.createElement('div'); box.id = 'loadOrderFatal'; box.textContent = msg; box.style.cssText = 'position:fixed;top:0;left:0;right:0;z-index:99999;padding:16px 20px;background:#3b0a0a;color:#ffb4b4;font:14px/1.5 monospace;border-bottom:2px solid #f66;white-space:pre-wrap'; (document.body || document.documentElement).appendChild(box); } catch (e) {}` `console.error('[BCK-Popup] ' + msg);` `throw new Error(msg);`
       Kein `idbGet(`-Aufruf, kein `showStatus` (existiert an dieser Stelle noch nicht).
    2. index.html — per Edit-Tool mit exaktem old_string: VOR `<script>` des `_cbv`-Blocks eine Kommentarzeile einfügen: `<!-- LADEREIHENFOLGE (nicht umstellen): bc-icons.js, bc-icons-ergaenzung.js (head) → persistence.js → items.js → money.js → rank.js → shop.js → inventar.js → bot-data.js → bot-ui.js → bot-engine.js → outfit-import.js → bc-autobackup.js. Details und Guard: docs/LOAD-ORDER.md -->`; und zwischen `var _cbv = Date.now();` und der items.js-Write-Zeile: `document.write('<scr'+'ipt src="persistence.js?_='+_cbv+'"><\/scr'+'ipt>');` — zwei reine Einfügungen, Feature-Kette unberührt.
    3. `docs/LOAD-ORDER.md` (neu, Deutsch, Überschrift „Ladereihenfolge der Script-Tags“): Abschnitte (a) Warum: kein Bundler, klassische Skripte teilen EINEN globalen Scope, `document.write` während des Parsens führt sofort aus, Reihenfolge = physische Reihenfolge; (b) Tabelle `Reihenfolge | Datei | Stellt bereit | Braucht` mit bc-icons.js/bc-icons-ergaenzung.js (head, `<script src>`), lz-string (CDN), persistence.js (`idbGet`, `idbSet`, `_idbOpen`, `_debounce`, Migrations-IIFE — braucht nichts), items.js (Koordinator, Tabs, `showStatus` … — braucht persistence.js), money.js, rank.js, shop.js, inventar.js, bot-data.js, bot-ui.js, bot-engine.js, outfit-import.js, bc-autobackup.js (brauchen `idbGet`/`idbSet` aus persistence.js und Globals aus items.js); (c) Guard: items.js prüft in der ersten Anweisung per `typeof window[name]`, rendert `#loadOrderFatal` (rote Box, Text „FATAL: … wurde nicht vor items.js geladen …“) und wirft — Plan 04-02 ergänzt bridge.js; (d) Neues Modul hinzufügen: `document.write`-Zeile im richtigen Block, Guard-Tabelle erweitern, `CORE_SCRIPTS` in `tests/helpers/loadScript.js` ergänzen, diese Datei nachziehen; (e) Tests: `loadScript(['items.js'])` expandiert per `expandLoadOrder`/`CORE_SCRIPTS`; `loadInto` lädt roh (so testet `tests/load-order-guard.test.js` den Fehlerfall). Kein Code-Block mit Implementierung nötig — Beschreibung reicht.
    4. `node --check items.js` → Exit 0. GREEN: `npx vitest run tests/load-order-guard.test.js` → 5 passed; `npm test` → 16 Dateien grün (168 passed + 2 expected fail).
    5. Commit: `git add items.js index.html docs/LOAD-ORDER.md`; `git diff --cached --name-only | sort` = `docs/LOAD-ORDER.md`, `index.html`, `items.js`; Message `feat(04-01): fail loudly when persistence.js is missing before items.js; document script load order in index.html and docs/LOAD-ORDER.md (SPLIT-04)`.
    6. Für das SUMMARY: `git diff --numstat 9b41340 HEAD -- index.html` (erwartet `2 0`), RED-/GREEN-Ausgaben, Verbatim-Diff aus Task 2.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/load-order-guard.test.js && node --check items.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` in der `Tests`-Summary — etwa weil der Guard NACH einer `_debounce`-Nutzung steht (Throw-Message `_debounce is not defined` statt `persistence.js`), die Box nicht per `document.createElement` erzeugt wird, die persistence.js-Write-Zeile im falschen Block liegt, oder die Doku ein Modul nicht nennt</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(git diff --numstat 9b41340 HEAD -- index.html | awk '{print $1" "$2}')" = "2 0" && test "$(grep -c 'persistence.js?_=' index.html)" = 1 && test "$(grep -c 'LADEREIHENFOLGE' index.html)" = 1 && test "$(grep -c 'loadOrderFatal' items.js)" = 1 && test "$(grep -n 'loadOrderFatal' items.js | cut -d: -f1)" -lt 20 && test "$(grep -c 'loadOrderFatal' docs/LOAD-ORDER.md)" -ge 1 && test "$(grep -c 'CORE_SCRIPTS' docs/LOAD-ORDER.md)" -ge 1 && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "docs/LOAD-ORDER.md index.html items.js " && echo GUARD-OK</automated>
    <fails_when>`GUARD-OK` fehlt — index.html hat Löschungen oder mehr/weniger als zwei Einfügungen, die Write-Zeile oder der Kommentar fehlen/doppeln, der Guard liegt nicht in den ersten 20 Zeilen, die Doku nennt Guard oder `CORE_SCRIPTS` nicht, oder der GREEN-Commit enthält andere Pfade</fails_when>
    <human-check>End-of-Phase (nicht blockierend, wird in Plan 04-04 gesammelt): Tool auf GitHub Pages hart neu laden (Strg+F5) — kein `FATAL`-Banner, alle Tabs bedienbar; anschließend testweise in den DevTools `persistence.js` blockieren und neu laden → rotes Banner „FATAL: persistence.js wurde nicht vor items.js geladen …“ erscheint oben.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "loadOrderFatal" items.js` = 1 und die Zeile liegt vor `const _debouncedSaveCurseDB`; `grep -Fc "[['idbGet', 'persistence.js']]" items.js` = 1; `grep -c "typeof window\[" items.js` >= 1; in den ersten 20 Zeilen von items.js kein `idbGet(`-Aufruf (`head -20 items.js | grep -c "idbGet("` = 0)
    - `grep -Fc "FATAL: " items.js` = 1; `grep -Fc "docs/LOAD-ORDER.md" items.js` = 1
    - `git diff --numstat 9b41340 HEAD -- index.html` = `2 0`; `grep -n "persistence.js?_=" index.html` < `grep -n "items.js?_=" index.html` (Zeilennummern); beide Zeilennummern zwischen den Zeilen von `var _cbv = Date.now();` und dem nächsten `</script>`
    - `grep -oE "(money|rank|shop|inventar|bot-data|bot-ui|bot-engine|outfit-import|bc-autobackup)\.js\?_=" index.html | wc -l` liefert 9 (Feature-Kette unverändert)
    - `docs/LOAD-ORDER.md` existiert; `grep -c "persistence.js" docs/LOAD-ORDER.md` >= 3; enthält `bc-autobackup.js`, `bot-engine.js`, `loadOrderFatal`, `CORE_SCRIPTS`, `expandLoadOrder`
    - `node --check items.js` → Exit 0; `npx vitest run tests/load-order-guard.test.js` → `5 passed`; `npm test` → `Test Files  16 passed`
    - Zwei Commits dieses Tasks: erst `test(04-01): …` mit genau `tests/load-order-guard.test.js`, dann `feat(04-01): …` mit genau `docs/LOAD-ORDER.md`, `index.html`, `items.js`; `git status --short` zeigt nur `?? .planning/milestone.lock`
  </acceptance_criteria>
  <done>Fehlendes persistence.js führt zu roter Box + Throw in der ersten Anweisung von items.js; index.html lädt persistence.js vor items.js mit Reihenfolge-Kommentar (nur Einfügungen); docs/LOAD-ORDER.md beschreibt Kette, Guard und Test-Expansion; 16 Testdateien grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| items.js → persistence.js (globaler Scope) | Extraktion darf Semantik von `idbGet`/`idbSet` (Quota-Pfad, `showStatus`-Guard) nicht verändern |
| index.html `document.write`-Kette | Physische Reihenfolge ist die einzige Abhängigkeitsauflösung; falsche Position = Totalausfall |
| Vitest → persistence.js (CJS-Require) | Test-Import darf keinen Browser-Pfad verändern (`module` im Browser undefined) |
| Git-Arbeitsbaum → Repository | Drei bzw. drei Pfade pro Commit; keine fremden Dateien |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Tampering | Extraktion ändert Verhalten still (Integrität) | high | mitigate | Byte-identischer Block (`diff`-Gate gegen `9b41340:items.js` 5-96); Marker-Zählung 0/1; alle 149 bestehenden Tests unverändert grün; kein `window.x = x`, kein Sweep der localStorage-Stellen |
| T-4-02 | Denial of Service | Fehlendes Modul → stiller Ausfall (Verfügbarkeit) | high | mitigate | Guard als ERSTE Anweisung in items.js (`#loadOrderFatal`, Throw, `console.error`); Test A/E; Kommentar in index.html + docs/LOAD-ORDER.md |
| T-4-07 | Tampering | Git-Staging fremder Dateien / falsche Reihenfolge in index.html | medium | mitigate | Preconditions `git status --short … leer`; `git add` nur genannte Pfade; numstat-Gate `2 0` für index.html; Feature-Kette per Test C fixiert |
| T-4-10 | Information Disclosure | Dual-Export macht IDB-Helfer per CJS erreichbar | low | accept | Nur im Test-Prozess relevant; im Browser ist `module` undefined, der Schwanz ist wirkungslos; keine neuen Daten erreichbar |
| T-4-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase (RESEARCH Package Legitimacy Audit: keine neuen Pakete) |
</threat_model>

<verification>
- `npx vitest run tests/persistence-module.test.js tests/load-order-guard.test.js` → 19 passed; `npm test` → 16 Testdateien grün (168 passed + 2 expected fail).
- Verbatim-Diff leer; `node --check persistence.js items.js` grün; index.html gegenüber `9b41340` nur Insertions (`2 0`).
- Vier Commits dieses Plans: test → refactor → test → feat.
- Phase-Gate (VALIDATION.md 4-01-01/4-01-02): beide Zeilen auf ✅ setzen.
</verification>

<success_criteria>
- SPLIT-01: persistence.js läuft als klassisches Skript (`loadScript(['persistence.js'])`, Round-Trip) und ist per `createRequire` importierbar; items.js enthält keine IDB-Primitiven mehr.
- SPLIT-04: items.js ohne persistence.js → `#loadOrderFatal` + Throw; Reihenfolge in index.html-Kommentar und docs/LOAD-ORDER.md dokumentiert.
- Alle bestehenden Tests unverändert grün; keine Änderung an loader.js oder den neun Feature-Modulen.
</success_criteria>

## Artifacts this phase produces

Phasenweite Liste (Besitzer-Plan in Klammern; dieser Plan: 04-01):

**Neue Dateien**
- `persistence.js` (04-01, erweitert in 04-04) — IDB-Helfer `_idbOpen`/`idbGet`/`idbSet`, Migrations-IIFE, `_debounce`; ab 04-04 Object-Store `screenshots`, `idbScreenshotBatch`/`idbScreenshotPut`/`idbScreenshotDelete`/`idbScreenshotGetAll`/`idbScreenshotKeys`, `_migrateScreenshotsToStore`, `_screenshotStoreReady`, `SCREENSHOT_LEGACY_KEYS`, `SCREENSHOT_MIGRATION_KEY`
- `bridge.js` (04-02) — `APP`, `bcSend`, `_bridgeSenderOk`, `manualReconnect`, `startPingRetry`, `_heartbeatCheck`, Registry `onBridgeMessage`/`offBridgeMessage`/`_bridgeDispatch`, der einzige `message`-Listener des Tools
- `docs/LOAD-ORDER.md` (04-01, ergänzt in 04-02) — dokumentierte Ladereihenfolge, Guard-Verhalten, Anleitung für neue Module und Nachrichtentypen
- Tests: `tests/persistence-module.test.js`, `tests/load-order-guard.test.js` (04-01); `tests/bridge-registry.test.js` (04-02); `tests/screenshot-export.test.js` (04-03); `tests/screenshot-migration.test.js`, `tests/screenshot-store.test.js` (04-04)

**Neue Symbole / Strukturen**
- Ladereihenfolge-Guard `#loadOrderFatal` in items.js (04-01, erweitert 04-02) und bot-ui.js (04-02)
- `CORE_SCRIPTS`/`expandLoadOrder` in `tests/helpers/loadScript.js` (04-01/04-02), `IDBKeyRange` in der Sandbox (04-04)
- `exportScreenshotsOnly()` + Tweaks-Sektion `🖼️ Screenshot-Speicher` mit Button „📷 Screenshots exportieren“ und `#screenshotStoreInfo` (04-03)
- IndexedDB `BCKonfigurator` Version 2 mit Object-Store `screenshots` (id `<kind>|<key>`, kinds `profile`/`lscg`/`wheel`), kv-Marker `BC_SCREENSHOT_MIGRATION_v1`, `_screenshotShadow`/`_screenshotFlush`/`_renderScreenshotStoreInfo` in items.js (04-04)

**Geänderte Dateien**: items.js (alle Pläne), index.html (04-01/02/03: nur Einfügungen bzw. eine Kommentarzeile), bot-ui.js (04-02), bestehende Tests mit Datei-Ort-Aussagen (04-02, 04-04)

<output>
Create `.planning/phases/04-entflechtung/04-01-SUMMARY.md` when done — mit RED-/GREEN-Ausgaben, dem leeren Verbatim-Diff, `git diff --numstat 9b41340 HEAD -- index.html` und der Liste der vier Commits.
</output>
