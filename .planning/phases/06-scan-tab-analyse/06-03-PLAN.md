---
phase: 06-scan-tab-analyse
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - tests/scan-tab.test.js
  - tests/analyze-snapshot.test.js
  - scan-tab.js
  - items.js
  - index.html
  - docs/LOAD-ORDER.md
  - tools/analyze-snapshot.js
  - package.json
autonomous: true
requirements: [SCAN-10]
tags: [scan-tab, ui, badges, search, filter, paging, debounce, xss, load-order, analyze-tool, red-green, human-check]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SCAN-10: Im neuen Tab „🔎 Scan“ (Bots-Gruppe, `switchTab('scan')` → `renderScanTab()`) sieht der Nutzer die Snapshot-Liste (neuester vorausgewählt; je Snapshot Datum ISO UTC, BC-Version, Mod-Anzahl, KB, Buttons „⬇ Exportieren“ und „🗑 Löschen“ auf `exportGameSnapshot(id)`/`deleteGameSnapshot(id)` mit `escJsAttr`), darunter die Fundliste des gewählten Snapshots: Kategorie-Filter (`all` / `globals` / `assets` / `groups` / `hooks` / `mods` / `patching` / `probes`), debounced Suche (150 ms über `_debounce` aus persistence.js, Name + Detail, case-insensitive), Zählzeile „<gezeigt> von <gefiltert> Einträgen (<genutzt> genutzt · <neu> neu)“, gepagte Darstellung (max. 300 Zeilen, Button „mehr laden (<rest> weitere)“), jede Zeile mit Badge „bereits genutzt“ / „neu“ / „unbekannt“ (Orchestrator-Entscheidung 4)"
    - "Badge-Regel (Baseline-Mengen aus `BASELINE_MANIFEST`, Plan 06-01): Zeilen der Kategorien globals/groups/patching/hooks → „bereits genutzt“ genau dann, wenn `name` in der Menge aller Manifest-`identifiers`-Namen (über alle kinds) liegt; assets → GRUPPEN-Badge nach `group` (RESEARCH Open Question 1, in der UI als gruppenweise Präzision ausgewiesen); mods → Modname (lowercase) in `modProbes`; probes → Probe-Key in `modProbes`; ohne geladenes Manifest → „unbekannt“ plus Hinweis in `#scanBaselineInfo` (nie fälschlich „genutzt“). Ein Name, der im Snapshot sowohl Funktion als auch Gruppe ist, erscheint je Kategorie einmal mit eigenem Badge; Snapshots anderer BC-Version werden nur nach Namen verglichen, die Version steht in der Snapshot-Zeile (Specless-Annahmen)"
    - "Sicherheit (T-6-05/T-6-06): jeder Mod-/Asset-/Hook-/Funktionsname aus dem Snapshot geht durch `escHtml` bevor er in `innerHTML` landet (Test mit `<img src=x onerror=alert(1)>`-Modname); ids in Inline-Handlern durch `escJsAttr`; 18.7k Globals frieren das Tool nicht ein (Slice auf 300 + Debounce, Tests mit 1000 synthetischen Gettern); scan-tab.js hat keine eigene Bridge-/IDB-Logik, keinen Schreibpfad und ruft `idbSnapshotDelete` weiterhin genau einmal (Audit aus 06-02 bleibt grün)"
    - "Verdrahtung (Orchestrator-Entscheidung 4): items.js erhält GENAU die drei recherchierten Änderungen — `'scan'` in `TAB_GROUPS.bots`, `'scan'` in der Sichtbarkeits-Liste von `switchTab`, die geguardete Render-Zeile `if (tab === 'scan') { if (typeof renderScanTab === 'function') renderScanTab(); }` (Diff gegen `49e4365`: 3 hinzugefügte, 2 gelöschte Zeilen); index.html nur Einfügungen (Tab-Button `🔎 Scan` nach `tab-variablen-btn`, Pane `#tab-scan` nach `#tab-variablen`, ein `<style>`-Block, Write-Zeilen `baseline-manifest.js` und `scan-tab.js` direkt nach `game-scan.js` im ersten `_cbv`-Block) plus die eine geänderte Kommentarzeile `LADEREIHENFOLGE`; `docs/LOAD-ORDER.md` mit Zeilen 8/9 und renummerierten Folgezeilen; `CORE_SCRIPTS` bleibt `['persistence.js', 'bridge.js', 'items.js', 'game-scan.js']` (scan-tab.js ist Blatt-Modul wie money.js — Tests laden es explizit); ohne Snapshot zeigt der Tab den Hinweis auf ⚙️ Tweaks → 🔎 Spiel scannen plus einen Shortcut-Button, der das bestehende `triggerGameScan()` aufruft (der Tweaks-Trigger bleibt, RESEARCH Open Question 2)"
    - "`tools/analyze-snapshot.js` (dev-only, `npm run analyze -- <export.json> [--limit N]`) liest einen Export (`{_meta, snapshot}` oder rohen Datensatz) plus `baseline-manifest.json`, nutzt DIESELBEN Funktionen `_scanFlatten`/`_scanBaselineSets`/`_scanBadge`/`_scanCountBadges` aus scan-tab.js (Node-Dual-Export) und druckt eine Markdown-Tabelle „Kategorie | gesamt | genutzt | neu | unbekannt“ plus die „neu“-Namen je Kategorie (limitiert) — die Zahlen in GAME-INVENTORY.md (Plan 06-04) sind damit identisch mit dem, was der Tab zeigt"
    - "End-of-Phase-Human-Checks (nicht blockierend, `<human-check>` in Task 2): Tab auf echten Daten bedienbar (Suche „ChatRoom“ verengt, Filter „Mods“ zeigt die 28 Mods, `ChatRoomSendChat` = genutzt), 🗑 fragt und respektiert Abbruch, ⬇ lädt eine JSON mit `snapshot.inventory` herunter"
  artifacts:
    - path: "scan-tab.js"
      provides: "Rendering-Teil: `SCAN_PAGE_SIZE`, `SCAN_CATEGORIES`, `_scanState`, `_scanFlatten`, `_scanBaselineSets`, `_scanBadge`, `_scanBadgeLabel`, `_scanFilter`, `_scanCountBadges`, `_scanManifest`, `renderScanTab`, `_scanApplySelection`, `_scanRenderSnapshots`, `_scanRenderBaselineInfo`, `_scanRender`, `scanSelectSnapshot`, `scanOnSearch`, `scanOnFilter`, `scanLoadMore`; erweiterter Dual-Export"
      contains: "async function renderScanTab()"
    - path: "items.js"
      provides: "Drei Zeilen: TAB_GROUPS.bots + Sichtbarkeits-Liste + geguardeter Render-Hook"
      contains: "if (tab === 'scan')"
    - path: "index.html"
      provides: "Tab-Button `#tab-scan-btn`, Pane `#tab-scan` (ids scanSnapshotList, scanBaselineInfo, scanSearch, scanCategory, scanCount, scanList), Styles `.scan-row`/`.scan-badge-*`/`.scan-snap`, Write-Zeilen baseline-manifest.js + scan-tab.js, Kommentar-Ergänzung"
      contains: "id=\"tab-scan\""
    - path: "docs/LOAD-ORDER.md"
      provides: "Zeilen 8 (baseline-manifest.js) und 9 (scan-tab.js), Renummerierung, Guard-Absatz, präzisierter Schritt 4 (CORE_SCRIPTS nur für Kern-Skripte), Test-Ladebeispiel"
      contains: "scan-tab.js"
    - path: "tools/analyze-snapshot.js"
      provides: "CJS-Modul + CLI: `loadExport(file)`, `analyzeSnapshot(exported, manifest)`, `renderMarkdown(result, { limit })`, `require.main`-Guard"
      contains: "function analyzeSnapshot"
    - path: "tests/scan-tab.test.js"
      provides: "Guard/Node-Export, reine Funktionen (Flatten/Badges/Filter/Counts), DOM-Rendering in der Sandbox (leer, Vorauswahl, Badges mit echtem Manifest, XSS, filter, Paging, Debounce, ohne Manifest, Snapshot-Wechsel), statische Gates (items.js/index.html/docs/scan-tab.js)"
      contains: "renderScanTab"
    - path: "tests/analyze-snapshot.test.js"
      provides: "Zähl-Konsistenz mit scan-tab.js, Markdown-Form, Export-/Rohform, CLI-Lauf, statische Gates"
      contains: "analyzeSnapshot"
    - path: "package.json"
      provides: "Script `analyze` = `node tools/analyze-snapshot.js`"
      contains: "\"analyze\": \"node tools/analyze-snapshot.js\""
  key_links:
    - from: "items.js `switchTab('scan')`"
      to: "scan-tab.js `renderScanTab()`"
      via: "typeof-geguardeter Aufruf (einzige Laufzeitkopplung items.js → scan-tab.js)"
      pattern: "if \\(tab === 'scan'\\)\\s*\\{ if \\(typeof renderScanTab === 'function'\\) renderScanTab\\(\\); \\}"
    - from: "scan-tab.js `_scanBaselineSets`"
      to: "baseline-manifest.js `BASELINE_MANIFEST`"
      via: "`typeof BASELINE_MANIFEST !== 'undefined'` in `_scanManifest()`; Mengen aus `identifiers[].name` und `modProbes`"
      pattern: "typeof BASELINE_MANIFEST"
    - from: "scan-tab.js `renderScanTab`"
      to: "persistence.js `idbSnapshotGetAll()`"
      via: "neuester = letzter Eintrag (aufsteigend nach ts sortiert)"
      pattern: "idbSnapshotGetAll\\(\\)"
    - from: "tools/analyze-snapshot.js"
      to: "scan-tab.js Dual-Export (`_scanFlatten`, `_scanBaselineSets`, `_scanBadge`, `_scanCountBadges`)"
      via: "`require(path.join(__dirname, '..', 'scan-tab.js'))` — Guard entfällt ohne `window`"
      pattern: "require\\(path\\.join\\(__dirname, '\\.\\.', 'scan-tab\\.js'\\)\\)"
    - from: "index.html `_cbv`-Block"
      to: "Reihenfolge persistence.js → bridge.js → items.js → game-scan.js → baseline-manifest.js → scan-tab.js"
      via: "document.write-Zeilen; LADEREIHENFOLGE-Kommentar"
      pattern: "scan-tab\\.js\\?_="
  prohibitions:
    - requirement_id: SCAN-10
      category: security
      verification: automated
      statement: "Kein unescaptes innerHTML: jede Zeilen-/Snapshot-Darstellung geht durch `escHtml` (Name, Detail, Gruppe, Version) bzw. `escJsAttr` (ids in onclick); XSS-Test mit `<img src=x onerror=alert(1)>`-Modname; keine `innerHTML +=`-Akkumulation"
    - requirement_id: SCAN-10
      category: safety
      verification: automated
      statement: "items.js-Diff gegen `49e4365` exakt 3 hinzugefügte / 2 gelöschte Zeilen; index.html-Diff hat außer der LADEREIHENFOLGE-Kommentarzeile 0 gelöschte Zeilen; `CORE_SCRIPTS` unverändert; index.html enthält weder `idbSnapshotDelete` noch `build-baseline` noch `analyze-snapshot`; game-scan.js, bridge.js, loader.js, persistence.js unverändert in diesem Plan"
    - requirement_id: SCAN-10
      category: constraint
      verification: automated
      statement: "Kein Build-Schritt: baseline-manifest.js und scan-tab.js werden als klassische Skripte per document.write geladen; tools/analyze-snapshot.js ist dev-only (nur fs/path, CJS)"
---

<objective>
Der Scan-Tab (SCAN-10): scan-tab.js bekommt das Rendering (Snapshot-Liste mit Export-/Lösch-Buttons je Snapshot, Kategorie-Filter, debounced Suche, gepagte Fundliste mit Badges „bereits genutzt“/„neu“ aus den Baseline-Mengen, Hinweis + Shortcut ohne Snapshot), items.js die drei recherchierten Verdrahtungszeilen, index.html den Tab-Button, das Pane, die Styles und die zwei Write-Zeilen, docs/LOAD-ORDER.md die neuen Ladepositionen. Dazu das dev-only Skript `tools/analyze-snapshot.js`, das mit den Badge-Funktionen aus scan-tab.js dieselben Zahlen liefert, die der Tab zeigt — die Grundlage für GAME-INVENTORY.md in Plan 06-04. RED → GREEN in der bestehenden Sandbox (Fresh-Stub-Pitfall: `document.getElementById` vor dem Rendern überschreiben), End-of-Phase-Human-Checks als `<human-check>`.

Purpose: Erfolgskriterium 2 der Phase („Im Scan-Tab kann der Nutzer die Fundliste durchsuchen und nach Kategorie filtern; jeder Eintrag trägt das Badge …“) und die UI-Erreichbarkeit von SCAN-11 (🗑) und dem Export (⬇).
Output: `tests/scan-tab.test.js`, `tests/analyze-snapshot.test.js`, `tools/analyze-snapshot.js` (neu); `scan-tab.js`, `items.js`, `index.html`, `docs/LOAD-ORDER.md`, `package.json` (geändert).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scan-tab-analyse/06-RESEARCH.md
@.planning/phases/06-scan-tab-analyse/06-VALIDATION.md
@.planning/phases/06-scan-tab-analyse/06-01-SUMMARY.md
@.planning/phases/06-scan-tab-analyse/06-02-SUMMARY.md
@.planning/phases/05-gamecode-inventar/05-02-SUMMARY.md
@scan-tab.js
@docs/LOAD-ORDER.md
@tests/helpers/loadScript.js
@tests/storage-estimate.test.js

Planer-Probe (2026-09-19, HEAD `49e4365`; per grep ankern) — verbindliche Befunde:
1. **items.js-Einfügepunkte (exakt):** Zeile 4225 `  bots:  ['bot','shop','rank','money','itemdefs','inventar','log','spieler','variablen'],` → `'scan'` am Ende anhängen; Zeile 4253 `  ['items','outfit','curse','bot','log','money','events','rank','shop','outfit-import','outfit-scan','lscg-wheel','locks','spieler','variablen','itemdefs','inventar'].forEach(t => {` → `,'scan'` nach `'inventar'` (RESEARCH Pitfall 1: beide Listen sind unabhängig); nach Zeile 4266 `  if (tab === 'inventar')      { if (typeof renderInventarTab === 'function') renderInventarTab(); }` die neue Zeile `  if (tab === 'scan')          { if (typeof renderScanTab === 'function') renderScanTab(); }`. Sonst nichts — `escHtml` (Zeile 6504), `escJsAttr` (6515), `showStatus` (6259), `_jsonParts` (6891) sind vorhanden.
2. **index.html-Einfügepunkte (exakt):** Tab-Buttons Zeilen 2540-2556, letzter Bot-Button Zeile 2555 `<button class="tab-btn" id="tab-variablen-btn" onclick="switchTab('variablen')">🔢 Variablen</button>` → neue Zeile danach; LADEREIHENFOLGE-Kommentar Zeile 3106 enthält `→ items.js → game-scan.js → money.js →` → auf `→ game-scan.js → baseline-manifest.js → scan-tab.js → money.js →` ändern (einzige erlaubte Zeilenänderung; Test-Slices 200/260/320 Zeichen ab `LADEREIHENFOLGE` müssen `bridge.js`, `game-scan.js` und `scan-tab.js` enthalten — der Kommentar ist eine Zeile); erster `_cbv`-Block Zeilen 3107-3114, letzte Write-Zeile 3113 `document.write('<scr'+'ipt src="game-scan.js?_='+_cbv+'"><\/scr'+'ipt>');` → zwei Write-Zeilen danach (`baseline-manifest.js`, dann `scan-tab.js`); Panes: `<div id="tab-variablen" class="tab-pane">` Zeile 3949 endet vor dem `<style>`-Block „Lock / Timer Dashboard“ (Zeile ~3964) → `#tab-scan` samt eigenem `<style>` dazwischen einfügen. Stil-Analog: `#tab-inventar` (Zeilen 3893-3947: `padding:16px 18px`, `.pane-title`, Aktualisieren-Button, Hinweistext `font-size:.6875rem;color:var(--text3)`).
3. **Bestehende Gates, die halten müssen:** tests/load-order-guard.test.js prüft `persistence.js?_=` genau 1×, Reihenfolge der Feature-Kette `money.js … bc-autobackup.js` je genau 1× (Einfügungen davor stören nicht), LADEREIHENFOLGE vor `var _cbv`, `docs/LOAD-ORDER.md` nennt alle Module + `CORE_SCRIPTS` + `document.write` + `loadOrderFatal`; tests/game-scan-bridge.test.js prüft `game-scan.js?_=` 1×, vor `</script>` des ersten Blocks, LADEREIHENFOLGE-Slice 260 Zeichen enthält `game-scan.js`, `docs/LOAD-ORDER.md` enthält `'game-scan.js'`, `332fc2d`, `GAME_SCAN_DATA`, `CORE_SCRIPTS` `toEqual(['persistence.js', 'bridge.js', 'items.js', 'game-scan.js'])`; tests/snapshot-delete.test.js (06-02) verlangt `idbSnapshotDelete(` genau 1× in scan-tab.js und 0× in index.html; tests/screenshot-export.test.js prüft Reihenfolge Tweaks-Sektionen (unberührt); tests/delete-confirmation.test.js scannt items.js auf Property-Deletes der vier Stores (drei Zeilen ohne Delete stören nicht).
4. **Sandbox-Fakten:** `document.getElementById()` liefert bei JEDEM Aufruf einen frischen Stub (RESEARCH Pitfall 2) — Tests überschreiben `ctx.document.getElementById = id => captured[id] || makeElementStub()` VOR dem Rendern (Muster tests/storage-estimate.test.js `captureInfo`); Stub-Felder `innerHTML`, `textContent`, `value`. `_debounce` (persistence.js) nutzt die Sandbox-Globals `setTimeout`/`clearTimeout` — für den Debounce-Test per `extraGlobals` mit `vi.fn`-Timer-Sammlern injizieren; für alle anderen Tests `setTimeout: () => 0` (dann feuert der Debounce nie — deshalb rufen Filter-Tests `_scanRender()` direkt). Top-Level-`let`/`const` (`_scanState`, `SCAN_PAGE_SIZE`) per `evalIn`. fake-indexeddb ist pro Datei prozessweit: der „ohne Snapshot“-Test muss als erster DOM-Test laufen (vitest führt Fälle einer Datei sequenziell aus), Vorauswahl-Tests seeden mit `ts` weit in der Zukunft (z. B. `9e12 + n`), damit „neuester“ deterministisch ist; ids `'st_' + Date.now() + '_' + n`.
5. **Echtes Manifest für Integrationstests (06-01):** `ChatRoomSendChat` (function), `ChatRoomData` (unknown), `ChatRoomMessage` (unknown), `ChatRoomRegisterMessageHandler` (unknown), `ServerSend` (function), `ItemNeck`/`ItemMouth` (assetGroup) sind Namen der Menge; `CharacterNickname`, `DrawImage`, `ItemUnknownGrp`, `BCX handler` sind es nicht; `modProbes` = bcx/lscg/mbs/themed/wce. Reine-Funktions-Tests nutzen stattdessen ein synthetisches Manifest, damit sie nicht an der Quelldrift hängen.
6. **Snapshot-Vertrag (05-02):** `inventory.globals.{getters:[Name], functions:[{name, arity}], values:[{name, type}]}`, `inventory.assets.{items:[{Name, Group, …}], groups:[{Name, assetCount?, …}]}`, `inventory.modSdk.patching:[{name, hookedByMods, patchedByMods}]`, `inventory.mods:[{name, fullName, version, repository}]`, `inventory.probes:{wce, bcx, mbs, lscg, themed, sweep:[Name]}` (Probe-Objekte mit `present`, optional `version`, `functions`/`api`/`screenFunctions.sample`/`sample`), `inventory.chatHooks:{ChatRoomRegisterMessageHandler:{exists, arity, kind}, registry:{introspectable, source?, count?, handlers?:[{Description, Priority}]}, hookedChatFunctions:[{name, hookedByMods}]}`. Der Tool-Datensatz: `{id, ts, gameVersion, modCount, mods:[{name, version}], sizeBytes, inventory}`.
7. **Node-Ladbarkeit von scan-tab.js (06-02):** Guard springt ohne `window` raus; `require('../scan-tab.js')` liefert den Dual-Export. Rendering-Funktionen dürfen `document`, `escHtml`, `_debounce`, `BASELINE_MANIFEST` nur INNERHALB von Funktionen berühren; ein Init-Hook (falls nötig) in `try/catch`. Die Debounce-Instanz wird lazy erzeugt (`_scanRenderDebounced` beim ersten `scanOnSearch()`), nicht auf Modulebene.
8. **Baseline nach Plan 06-02:** `npm test` → 26 Dateien grün.
</context>

<tasks>

<!-- Comment-Text-Discipline-Allowlist (#429): Gate-Literale der negativen Greps dieses Plans -->
<!-- planner-discipline-allow: idbSnapshotDelete -->
<!-- planner-discipline-allow: build-baseline -->
<!-- planner-discipline-allow: analyze-snapshot -->
<!-- planner-discipline-allow: postMessage( -->
<!-- planner-discipline-allow: addEventListener('message' -->
<!-- planner-discipline-allow: location.origin -->
<!-- planner-discipline-allow: toLocaleString -->
<!-- planner-discipline-allow: idbSnapshotPut( -->
<!-- planner-discipline-allow: idbSet( -->
<!-- planner-discipline-allow: setInterval( -->
<!-- planner-discipline-allow: innerHTML += -->
<!-- planner-discipline-allow: .delete( -->
<!-- planner-discipline-allow: .clear( -->
<!-- planner-discipline-allow: indexedDB.open( -->
<!-- planner-discipline-allow: <img src=x -->

<task type="auto">
  <name>Task 1: Scan-Tab- und Analyse-Skript-Tests schreiben und RED bestätigen — `tests/scan-tab.test.js`, `tests/analyze-snapshot.test.js` (SCAN-10, T-6-05/T-6-06)</name>
  <precondition>Pläne 06-01 und 06-02 gelandet: `test -f baseline-manifest.js` und `grep -c 'async function deleteGameSnapshot(id)' scan-tab.js` = 1 und `grep -c 'async function idbSnapshotDelete(id)' persistence.js` = 1; `npm test` grün (26 Dateien)</precondition>
  <files>tests/scan-tab.test.js, tests/analyze-snapshot.test.js</files>
  <read_first>tests/storage-estimate.test.js (`captureInfo`-Muster), tests/snapshot-delete.test.js und tests/scan-tab-export.test.js (06-02: `boot`, `rec`, Op-Spy, Node-require), tests/game-scan-bridge.test.js Zeilen 317-376 (statische Gates), tests/load-order-guard.test.js Zeilen 65-95 (index.html-Block-Gates), tests/helpers/loadScript.js; scan-tab.js (06-02, Dual-Export); Task 2 und 3 dieses Plans (Bezeichner, ids, Texte, Klassen sind Vertrag)</read_first>
  <action>
    **A) `tests/scan-tab.test.js` (neu, ESM):** Imports `describe, it, expect, vi, afterEach` (vitest); `fs`, `path`; `createRequire` (node:module); `loadScript, loadInto, makeSandbox, makeElementStub, evalIn, settle, CORE_SCRIPTS, expandLoadOrder, REPO_ROOT` aus `./helpers/loadScript.js`. Kopfkommentar (Deutsch): SCAN-10 — Rendering gegen die Sandbox mit überschriebenem `getElementById` (RESEARCH Pitfall 2), Badges gegen das echte Manifest nur für stabile Namen, reine Funktionen gegen ein synthetisches Manifest; fake-indexeddb pro Datei geteilt → „ohne Snapshot“ zuerst, Vorauswahl mit Zukunfts-ts. Helfer: `src`, `count`; `IDS = ['scanSnapshotList', 'scanBaselineInfo', 'scanSearch', 'scanCategory', 'scanCount', 'scanList']`; `async function boot({ manifest = true, timers = null } = {})` → `captured = Object.fromEntries(IDS.map(id => [id, makeElementStub()]))`; `extra = timers ? { setTimeout: timers.setTimeout, clearTimeout: timers.clearTimeout } : { setTimeout: () => 0, clearTimeout: () => {} }`; `files = manifest ? ['items.js', 'baseline-manifest.js', 'scan-tab.js'] : ['items.js', 'scan-tab.js']`; `ctx = loadScript(files, extra)`; `ctx.document.getElementById = id => captured[id] || makeElementStub()`; `ctx.showStatus = vi.fn()`; `await settle(40)`; `return { ctx, captured }`; `let seq = 0; function rec(extra)` → `{ id: 'st_' + Date.now() + '_' + (++seq), ts: 9e12 + seq, gameVersion: 'R131', modCount: 2, mods: [{ name: 'BCX', version: '1' }, { name: 'X', version: '0' }], sizeBytes: 2048, inventory: inv(), ...extra }`; `function inv(extra = {})` → `{ schema: 1, gameVersion: 'R131', globals: { total: 4, getters: ['PlayerG1'], functions: [{ name: 'ChatRoomSendChat', arity: 1 }, { name: 'CharacterNickname', arity: 1 }], values: [{ name: 'ChatRoomData', type: 'object' }], byPrefix: {}, inventory: { groups: [], other: { count: 0, names: [] } } }, assets: { count: 3, groupCount: 2, groups: [{ Name: 'ItemNeck', assetCount: 1 }, { Name: 'ItemUnknownGrp', assetCount: 1 }], items: [{ Name: 'Collar', Group: 'ItemNeck' }, { Name: 'Ballgag', Group: 'ItemMouth' }, { Name: 'Weird', Group: 'ItemUnknownGrp' }] }, modSdk: { available: true, version: '1.2.0', modCount: 2, patchingCount: 2, patching: [{ name: 'ServerSend', hookedByMods: ['BCX'], patchedByMods: [] }, { name: 'DrawImage', hookedByMods: ['LSCG', 'MBS'], patchedByMods: [] }] }, mods: [{ name: 'BCX', fullName: 'Bondage Club Extended', version: '1', repository: 'r' }, { name: '<img src=x onerror=alert(1)>', fullName: 'Böse', version: '0', repository: 'r' }], probes: { wce: { present: true, version: '6', functions: ['fbcVersion'] }, bcx: { present: false, loaded: null, version: null, api: [] }, mbs: { present: true, version: '2', apiVersion: 1, api: ['getActive'] }, lscg: { present: false, loaded: null, api: [], screenFunctions: { count: 0, sample: [] } }, themed: { present: false, loaded: null, screenFunctionCount: 0, sample: [] }, sweep: ['FUSAM'] }, chatHooks: { ChatRoomRegisterMessageHandler: { exists: true, arity: 1, kind: 'function' }, registry: { introspectable: true, source: 'ChatRoomMessageHandlers', count: 1, handlers: [{ Description: 'BCX handler', Priority: 100 }] }, hookedChatFunctions: [{ name: 'ChatRoomMessage', hookedByMods: ['BCX'] }] }, errors: [], ...extra }`; `SYN_MANIFEST = { schema: 1, identifiers: [{ name: 'ChatRoomSendChat', kind: 'function', files: [] }, { name: 'ItemNeck', kind: 'assetGroup', files: [] }, { name: 'ServerSend', kind: 'function', files: [] }], chatHooks: ['ChatRoomRegisterMessageHandler'], modProbes: ['wce'] }`; `rowsOf(html)` = `html.split('class="scan-row"').slice(1)`; `rowFor(html, name)` = das Segment aus `rowsOf`, das `'>' + name + '<'` enthält (Namen ohne Sonderzeichen); `badgeOf(segment)` = `/scan-badge-(genutzt|neu|unbekannt)/.exec(segment)[1]`.

    **describe „Ladereihenfolge-Guard + Node-Export (scan-tab.js)“**
    1. „in Node (ohne window) exportiert scan-tab.js die reinen Funktionen“: `m = createRequire(import.meta.url)('../scan-tab.js')`; für `['_scanFlatten', '_scanBaselineSets', '_scanBadge', '_scanFilter', '_scanCountBadges', 'deleteGameSnapshot', 'exportGameSnapshot']` `typeof === 'function'`; `m.SCAN_PAGE_SIZE === 300`; `m.SCAN_CATEGORIES` `toEqual(['globals', 'assets', 'groups', 'hooks', 'mods', 'patching', 'probes'])`.
    2. „mit allen Vorläufern: renderScanTab, scanOnSearch, scanOnFilter, scanLoadMore, scanSelectSnapshot sind Funktionen; CORE_SCRIPTS unverändert; expandLoadOrder(['items.js', 'scan-tab.js']) hängt scan-tab.js hinter game-scan.js“: `{ ctx } = await boot()`; `typeof ctx.renderScanTab === 'function'` usw.; `CORE_SCRIPTS` `toEqual(['persistence.js', 'bridge.js', 'items.js', 'game-scan.js'])`; `expandLoadOrder(['items.js', 'scan-tab.js'])` `toEqual([...CORE_SCRIPTS, 'scan-tab.js'])`.

    **describe „Reine Funktionen: Flatten, Badges, Filter, Zähler“**
    3. „_scanFlatten: eine Zeile je Fund, sieben Kategorien, assets tragen group, probes tragen probe“: `rows = m._scanFlatten(inv())`; `rows.length === 22` (Globals 4 + Assets 3 + Gruppen 2 + Hooks 3 [api 1, handler 1, hooked 1] + Mods 2 + Patching 2 + Probes 6 [5 Probes + 1 Sweep] — die Probe-API-Namen (`fbcVersion`, `getActive`) zählen als eigene Zeilen: +2 → 24; die Zahl im Test aus der Vertragsliste in Task 2 exakt ableiten und als Konstante `EXPECTED_ROWS` benennen); `new Set(rows.map(r => r.category))` `toEqual(new Set(m.SCAN_CATEGORIES))`; jede Zeile: `typeof r.category/kind/name === 'string'`, `r.name.length > 0`; `rows.filter(r => r.category === 'assets').every(r => typeof r.group === 'string')`; `rows.find(r => r.name === 'Collar').group === 'ItemNeck'`; `rows.filter(r => r.category === 'probes').every(r => typeof r.probe === 'string')`; `rows.find(r => r.name === 'wce.fbcVersion')` existiert mit `probe === 'wce'`; Zeile für `ChatRoomSendChat` hat `kind === 'function'` und `detail` enthält `'1'` (Arität); Zeile `DrawImage` hat `detail` mit `'LSCG'`; `_scanFlatten({})` und `_scanFlatten(null)` liefern `[]` ohne Throw.
    4. „_scanBaselineSets + _scanBadge: Namensmenge über alle kinds, assets nach Gruppe, mods/probes nach modProbes, null → unbekannt“: `sets = m._scanBaselineSets(SYN_MANIFEST)`; `sets.identifiers.has('ChatRoomSendChat') && sets.identifiers.has('ItemNeck')`; `sets.modProbes.has('wce')`; `sets.counts` `toEqual({ identifiers: 3, function: 2, assetGroup: 1, unknown: 0 })`; `rows = m._scanFlatten(inv())`; `b = name => m._scanBadge(rows.find(r => r.name === name), sets)`: `b('ChatRoomSendChat') === 'genutzt'`, `b('CharacterNickname') === 'neu'`, `b('Collar') === 'genutzt'` (Gruppe ItemNeck), `b('Ballgag') === 'neu'` (ItemMouth nicht im synthetischen Manifest), `b('ItemNeck') === 'genutzt'`, `b('ItemUnknownGrp') === 'neu'`, `b('ServerSend') === 'genutzt'`, `b('DrawImage') === 'neu'`, Mod-Zeile `BCX` → `'neu'` (nicht in modProbes des synthetischen Manifests), Probe-Zeile `wce` → `'genutzt'`, `bcx` → `'neu'`, `wce.fbcVersion` → `'genutzt'`; `m._scanBadge(rows[0], null) === 'unbekannt'`; `m._scanBaselineSets(null) === null`; `m._scanBaselineSets({})` liefert leere Mengen ohne Throw.
    5. „_scanFilter: Suche case-insensitive über Name + Detail, Kategorie, Kombination; leere Suche = alles“: `rows = m._scanFlatten(inv())`; `m._scanFilter(rows, '', 'all').length === rows.length`; `m._scanFilter(rows, 'chatroom', 'all').map(r => r.name).sort()` `toEqual(['ChatRoomData', 'ChatRoomMessage', 'ChatRoomRegisterMessageHandler', 'ChatRoomSendChat'])` (Hook-Registry-Zeile `ChatRoomMessageHandlers` steckt im Detail der api-Zeile? — NEIN: Detail-Treffer nur, wenn Task 2 das Detail so definiert; Test nach Vertrag in Task 2 anpassen und das Ergebnis als sortierte Liste festschreiben); `m._scanFilter(rows, '', 'mods').length === 2`; `m._scanFilter(rows, 'bcx', 'mods').length === 1`; `m._scanFilter(rows, 'lscg', 'patching').length === 1` (Detail-Treffer `hookedByMods`).
    6. „_scanCountBadges: gesamt/genutzt/neu/unbekannt je Kategorie und all“: `c = m._scanCountBadges(rows, sets)`; `c.all.total === rows.length`; `c.globals` `toEqual({ total: 4, genutzt: 1, neu: 3, unbekannt: 0 })`; `c.assets` `toEqual({ total: 3, genutzt: 1, neu: 2, unbekannt: 0 })`; `m._scanCountBadges(rows, null).all.unbekannt === rows.length`.

    **describe „renderScanTab (DOM, Sandbox)“** — Reihenfolge ist Vertrag (leerer Store zuerst).
    7. „ohne Snapshot: Hinweis nennt „Spiel scannen“ und einen Shortcut auf triggerGameScan, Liste leer, Zähler 0, kein Throw“: `{ ctx, captured } = await boot()`; `await ctx.renderScanTab()`; `captured.scanSnapshotList.innerHTML` enthält `'Spiel scannen'` und `"onclick=\"triggerGameScan()\""`; `captured.scanList.innerHTML === ''`; `captured.scanCount.textContent` enthält `'0'`; `evalIn(ctx, '_scanState.selectedId') === null`.
    8. „neuester Snapshot vorausgewählt; Snapshot-Liste: je Eintrag Datum UTC, BC-Version, Mods, KB, ⬇/🗑-Buttons mit escJsAttr-id; aktiver Eintrag markiert“: `older = rec(); newer = rec()` (newer.ts > older.ts); beide `ctx.idbSnapshotPut`; `await ctx.renderScanTab()`; `evalIn(ctx, '_scanState.selectedId') === newer.id`; `html = captured.scanSnapshotList.innerHTML`; enthält `"exportGameSnapshot('" + newer.id + "')"` und `"deleteGameSnapshot('" + newer.id + "')"` und dieselben für `older.id`; enthält `'R131'`, `'2 Mods'`, `' UTC'`, `'2 KB'`; `count(html, 'scan-snap-active') === 1` und das aktive Segment enthält `newer.id`; `count(html, 'idbSnapshotDelete') === 0`.
    9. „Fundliste mit echtem Manifest: Badges je Kategorie korrekt, Baseline-Info nennt die Bezeichner-Anzahl“: nach 8: `html = captured.scanList.innerHTML`; `badgeOf(rowFor(html, 'ChatRoomSendChat')) === 'genutzt'`; `badgeOf(rowFor(html, 'CharacterNickname')) === 'neu'`; `badgeOf(rowFor(html, 'Collar')) === 'genutzt'`; `badgeOf(rowFor(html, 'Weird')) === 'neu'`; `badgeOf(rowFor(html, 'ItemNeck')) === 'genutzt'`; `badgeOf(rowFor(html, 'ServerSend')) === 'genutzt'`; `badgeOf(rowFor(html, 'DrawImage')) === 'neu'`; `badgeOf(rowFor(html, 'BCX')) === 'genutzt'` (bcx ∈ modProbes); `badgeOf(rowFor(html, 'ChatRoomMessage')) === 'genutzt'`; `html` enthält `'bereits genutzt'`; `captured.scanBaselineInfo.textContent` (oder innerHTML) enthält `String(JSON.parse(src('baseline-manifest.json')).counts.identifiers)` und `'Gruppe'` (Hinweis auf gruppenweise Asset-Badges); `captured.scanCount.textContent` `toMatch(/\d+ von \d+ Einträgen/)` und enthält `'genutzt'`.
    10. „XSS: Mod-Name mit <img onerror> wird escaped (T-6-05)“: `html = captured.scanList.innerHTML`; `html` enthält `'&lt;img src=x onerror=alert(1)&gt;'`; `html` enthält NICHT `'<img src=x'`; `captured.scanSnapshotList.innerHTML` enthält kein `'<img'`.
    11. „Suche + Kategorie-Filter (filter): Suche „chatroom“ verengt; Kategorie „mods“ zeigt 2 Zeilen; Kombination; Zähler aktualisiert“: `captured.scanSearch.value = 'chatroom'; captured.scanCategory.value = 'all'; ctx._scanRender()`; `rowsOf(captured.scanList.innerHTML).length === 4` (nach der in Fall 5 festgeschriebenen Liste); `captured.scanSearch.value = ''; captured.scanCategory.value = 'mods'; ctx._scanRender()` → 2 Zeilen; `captured.scanSearch.value = 'bcx'; ctx._scanRender()` → 1 Zeile; `captured.scanCount.textContent` enthält `'1 von 1'`.
    12. „Paging (T-6-06): 1000 Getter → 300 Zeilen + „mehr laden (700 weitere)“; scanLoadMore → 600; nach 1000 kein Button“: `big = rec({ inventory: inv({ globals: { total: 1000, getters: Array.from({ length: 1000 }, (_, i) => 'PagerG' + i), functions: [], values: [] } }) })` — dadurch ist `big` der neueste; put; `captured.scanSearch.value = 'pagerg'; captured.scanCategory.value = 'globals'`; `await ctx.renderScanTab()`; `ctx._scanRender()`; `rowsOf(captured.scanList.innerHTML).length === 300`; `captured.scanList.innerHTML` enthält `'mehr laden (700 weitere)'` und `"onclick=\"scanLoadMore()\""`; `ctx.scanLoadMore()` → 600 Zeilen und `'(400 weitere)'`; zweimal weiter → 1000 Zeilen, kein `'mehr laden'`.
    13. „Debounce (T-6-06): zwei schnelle scanOnSearch-Aufrufe → clearTimeout, genau ein 150-ms-Timer, Render erst beim Timer-Lauf“: `timers = { list: [], setTimeout: vi.fn((fn, ms) => { timers.list.push({ fn, ms }); return timers.list.length; }), clearTimeout: vi.fn() }`; `{ ctx, captured } = await boot({ timers })`; `await ctx.renderScanTab()`; `timers.list.length = 0; timers.clearTimeout.mockClear()`; `captured.scanSearch.value = 'zzz-nichts'`; `before = captured.scanList.innerHTML`; `ctx.scanOnSearch(); ctx.scanOnSearch()`; `pending = timers.list.filter(t => t.ms === 150)`; `pending.length === 2` und `timers.clearTimeout` mindestens 1× aufgerufen (der erste Timer wurde verworfen); `captured.scanList.innerHTML === before` (noch nicht gerendert); `pending.at(-1).fn()` → `rowsOf(captured.scanList.innerHTML).length === 0` und `captured.scanCount.textContent` enthält `'0 von 0'`.
    14. „ohne BASELINE_MANIFEST: alle Badges „unbekannt“, Hinweis in scanBaselineInfo, kein Throw“: `{ ctx, captured } = await boot({ manifest: false })`; `await ctx.renderScanTab()`; `captured.scanBaselineInfo.textContent` (oder innerHTML) enthält `'baseline-manifest.js'`; `html = captured.scanList.innerHTML`; `count(html, 'scan-badge-genutzt') === 0`; `count(html, 'scan-badge-unbekannt') === rowsOf(html).length` (> 0).
    15. „scanSelectSnapshot wechselt den Datensatz ohne IDB-Neuladen und setzt die Seite zurück“: `{ ctx, captured } = await boot()`; `a = rec({ inventory: inv({ mods: [{ name: 'NurA', version: '1' }] }) }); b = rec({ inventory: inv({ mods: [{ name: 'NurB', version: '1' }] }) })`; put beide; `await ctx.renderScanTab()` (b vorausgewählt); `captured.scanCategory.value = 'mods'; ctx._scanRender()`; `captured.scanList.innerHTML` enthält `'NurB'` und nicht `'NurA'`; `ctx.scanSelectSnapshot(a.id)`; `captured.scanList.innerHTML` enthält `'NurA'` und nicht `'NurB'`; `evalIn(ctx, '_scanState.shown') === 300`; `count(captured.scanSnapshotList.innerHTML, 'scan-snap-active') === 1`.

    **describe „statisch: items.js-Verdrahtung, index.html, docs, scan-tab.js-Quelle“**
    16. „items.js: 'scan' in TAB_GROUPS.bots und in der Sichtbarkeits-Liste; genau eine geguardete Render-Zeile“: `items = src('items.js')`; `botsLine = items.split('\n').find(l => l.includes("bots:  ['bot','shop'"))`; `botsLine` enthält `"'scan'"`; `visLine = items.split('\n').find(l => l.includes("['items','outfit','curse','bot','log'"))`; `visLine` enthält `"'inventar','scan']"`; `count(items, "if (tab === 'scan')") === 1`; `count(items, "typeof renderScanTab === 'function'") === 1`; `count(items, 'renderScanTab') === 1`.
    17. „index.html: Button, Pane, ids, Handler, Optionen, Styles, Write-Zeilen, Kommentar; keine Direktaufrufe der Persistenz“: `html = src('index.html')`; `count(html, 'id="tab-scan-btn"') === 1`; `count(html, "onclick=\"switchTab('scan')\"") === 1`; `count(html, '🔎 Scan<') === 1`; `html.indexOf('id="tab-variablen-btn"') < html.indexOf('id="tab-scan-btn"')`; `count(html, 'id="tab-scan"') === 1`; `html.indexOf('id="tab-variablen"') < html.indexOf('id="tab-scan"')`; für jede id aus `IDS`: `count(html, 'id="' + id + '"') === 1`; `count(html, 'oninput="scanOnSearch()"') === 1`; `count(html, 'onchange="scanOnFilter()"') === 1`; `count(html, 'onclick="renderScanTab()"') === 1`; für `['all', 'globals', 'assets', 'groups', 'hooks', 'mods', 'patching', 'probes']`: `count(html, 'value="' + v + '"')` ≥ 1 innerhalb des `#tab-scan`-Slices; `.scan-row`, `.scan-badge-genutzt`, `.scan-badge-neu`, `.scan-badge-unbekannt`, `.scan-snap-active` je ≥ 1 im Slice; `count(html, 'baseline-manifest.js?_=') === 1`; `count(html, 'scan-tab.js?_=') === 1`; `html.indexOf('game-scan.js?_=') < html.indexOf('baseline-manifest.js?_=')` `< html.indexOf('scan-tab.js?_=')` `< html.indexOf('</script>', html.indexOf('var _cbv = Date.now();'))`; `html.slice(html.indexOf('LADEREIHENFOLGE'), html.indexOf('LADEREIHENFOLGE') + 320)` enthält `'baseline-manifest.js'` und `'scan-tab.js'`; `count(html, 'idbSnapshotDelete') === 0`; `count(html, 'build-baseline') === 0`; `count(html, 'analyze-snapshot') === 0`; `count(html, 'onclick="deleteGameSnapshot(') === 0` (Buttons entstehen nur im Rendering mit escJsAttr).
    18. „docs/LOAD-ORDER.md nennt beide Module, Positionen, Guard, CORE_SCRIPTS-Regel“: `doc = src('docs/LOAD-ORDER.md')`; `count(doc, 'scan-tab.js') >= 3`; `count(doc, 'baseline-manifest.js') >= 2`; `doc` enthält `'renderScanTab'`, `'idbSnapshotDelete'`, `'npm run baseline'`, `'| 9 | `scan-tab.js`'`, `'| 18 | `bc-autobackup.js`'`; `doc` enthält weiterhin `"'game-scan.js'"`, `'332fc2d'`, `'GAME_SCAN_DATA'`, `'loadOrderFatal'`, `'CORE_SCRIPTS'`, `'document.write'`.
    19. „scan-tab.js-Quelle: escHtml/escJsAttr-Nutzung, keine eigene Bridge-/IDB-/Schreiblogik, Debounce-Nutzung, Seitengröße“: `s = src('scan-tab.js')`; `count(s, 'escHtml(') >= 8`; `count(s, "deleteGameSnapshot('${escJsAttr(") === 1`; `count(s, "exportGameSnapshot('${escJsAttr(") === 1`; `count(s, "scanSelectSnapshot('${escJsAttr(") === 1`; `count(s, 'innerHTML +=') === 0`; `count(s, '_debounce(') === 1`; `count(s, 'const SCAN_PAGE_SIZE = 300;') === 1`; `count(s, 'idbSnapshotDelete(') === 1`; `count(s, 'idbSnapshotGetAll(') >= 1`; `count(s, 'typeof BASELINE_MANIFEST') >= 1`; `count(s, "addEventListener('message'") === 0`; `count(s, 'postMessage(') === 0`; `count(s, 'indexedDB.open(') === 0`; `count(s, '.delete(') === 0`; `count(s, '.clear(') === 0`; `count(s, 'idbSnapshotPut(') === 0`; `count(s, 'idbSet(') === 0`; `count(s, 'setInterval(') === 0`; `count(s, 'location.origin') === 0`; `count(s, 'toLocaleString') === 0`; `count(s, 'triggerGameScan') >= 1` und `count(s, 'typeof triggerGameScan') === 1`.

    **B) `tests/analyze-snapshot.test.js` (neu, ESM):** `createRequire`; `az = require('../tools/analyze-snapshot.js')`; `tab = require('../scan-tab.js')`; Fixture `inv()` und `SYN_MANIFEST` aus A) (per Copy oder gemeinsam in einem kleinen Helfer `tests/helpers/scanFixtures.js` — Helfer ist erlaubt, dann beide Testdateien importieren ihn; dann `files` von Task 1 um `tests/helpers/scanFixtures.js` ergänzen und im Commit stagen); `exported = { _meta: { version: 1 }, snapshot: { id: 'x', ts: 1000, gameVersion: 'R131', modCount: 2, mods: [], sizeBytes: 5, inventory: inv() } }`.
    20. „analyzeSnapshot: Zahlen identisch mit _scanCountBadges aus scan-tab.js; neu-Namen je Kategorie sortiert; meta aus dem Datensatz“: `r = az.analyzeSnapshot(exported, SYN_MANIFEST)`; `r.counts` `toEqual(tab._scanCountBadges(tab._scanFlatten(inv()), tab._scanBaselineSets(SYN_MANIFEST)))`; `r.neu.globals` `toEqual(['CharacterNickname', 'ChatRoomData', 'PlayerG1'])`; `r.neu.assets` `toEqual(['Ballgag', 'Weird'])`; `r.meta` `toEqual({ id: 'x', ts: 1000, gameVersion: 'R131', modCount: 2, sizeBytes: 5 })`; `r.baseline` `toEqual({ identifiers: 3, function: 2, assetGroup: 1, unknown: 0 })`.
    21. „akzeptiert die Rohform (Datensatz ohne _meta) und ein nacktes Inventar“: `az.analyzeSnapshot(exported.snapshot, SYN_MANIFEST).counts` `toEqual(r.counts)`; `az.analyzeSnapshot({ inventory: inv() }, SYN_MANIFEST).counts.all` `toEqual(r.counts.all)`; ungültige Eingabe (`null`, `{}`) wirft einen `Error` mit `'Snapshot'` im Text.
    22. „renderMarkdown: Tabellenzeile je Kategorie + all, Limit kappt Listen mit „… (+N weitere)“, keine Fenced-Code-Blöcke“: `md = az.renderMarkdown(r, { limit: 2 })`; für jede Kategorie und `all`: `md` enthält `'| ' + cat + ' |'`; `md` enthält `'| all | ' + r.counts.all.total`; `md` enthält `'(+1 weitere)'` (globals hat 3 neu, Limit 2); `count(md, '```') === 0`; `md` enthält `'R131'`.
    23. „CLI: node tools/analyze-snapshot.js <file> --limit 1 druckt Markdown, Exit 0; ohne Datei Exit ≠ 0 mit Usage“: `tmp = path.join(os.tmpdir(), 'az_' + Date.now() + '.json')`; `fs.writeFileSync(tmp, JSON.stringify(exported))`; `out = execFileSync(process.execPath, [path.join(REPO_ROOT, 'tools/analyze-snapshot.js'), tmp, '--limit', '1'], { encoding: 'utf8' })` (Manifest-Argument optional: Default `baseline-manifest.json` im Repo — dann enthält `out` die echte Baseline-Zahl); `out` enthält `'| globals |'` und `'| all |'`; `expect(() => execFileSync(process.execPath, [tool], { stdio: 'pipe' })).toThrow()` und die Fehlermeldung/`stderr` enthält `'Usage'`.
    24. „statisch: CJS, nur fs/path, require von ../scan-tab.js über __dirname, require.main-Guard, package.json-Script analyze, index.html ohne Bezug“: `t = src('tools/analyze-snapshot.js')`; `count(t, "require(path.join(__dirname, '..', 'scan-tab.js'))") === 1`; `count(t, 'require.main === module') === 1`; `count(t, 'module.exports') === 1`; `expect(t).not.toMatch(/^\s*(export|import)\s/m)`; `JSON.parse(src('package.json')).scripts.analyze === 'node tools/analyze-snapshot.js'`; `count(src('index.html'), 'analyze-snapshot') === 0`.

    **RED bestätigen:** `npx vitest run tests/scan-tab.test.js tests/analyze-snapshot.test.js` → beide rot (fehlende Exporte/Funktionen, `Cannot find module tools/analyze-snapshot.js`, statische Gates). Commit (nur diese Pfade, ggf. plus Helfer): `git add tests/scan-tab.test.js tests/analyze-snapshot.test.js` → `test(06-03): Scan-Tab (Rendering, Badges, Filter, Paging, Debounce, XSS, Verdrahtung) + analyze-snapshot — RED`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/scan-tab.test.js tests/analyze-snapshot.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "renderScanTab|_scanFlatten|analyze-snapshot|tab-scan" "$OUT" && grep -Eq "Test Files +[0-9]+ failed" "$OUT" && test ! -e tools/analyze-snapshot.js && git diff --quiet HEAD -- scan-tab.js items.js index.html docs/LOAD-ORDER.md package.json && FILES=$(git show --name-only --format= HEAD) && test "$(printf '%s\n' "$FILES" | grep -vc '^tests/')" = 0 && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Exit 0, keine `failed`-Zeile, die Ausgabe nennt keinen der Bezeichner, das Analyse-Skript existiert schon, eine Produktions-/Doku-Datei wurde bereits geändert, oder der Commit enthält Pfade außerhalb von tests/</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" tests/scan-tab.test.js` >= 19; `grep -c "it(" tests/analyze-snapshot.test.js` >= 5; `grep -c "onerror=alert(1)" tests/scan-tab.test.js` >= 2; `grep -c "PagerG" tests/scan-tab.test.js` >= 2; `grep -c "ms === 150" tests/scan-tab.test.js` >= 1; `grep -c "getElementById = " tests/scan-tab.test.js` >= 1
    - `grep -c "\.only(" tests/scan-tab.test.js tests/analyze-snapshot.test.js | awk -F: '{s+=$2} END {print s}'` = 0; `grep -c "animereviewer1-sketch" tests/scan-tab.test.js tests/analyze-snapshot.test.js | awk -F: '{s+=$2} END {print s}'` = 0
    - Rest der Suite unverändert grün: `npx vitest run --exclude tests/scan-tab.test.js --exclude tests/analyze-snapshot.test.js` → keine `failed`-Zeile
  </acceptance_criteria>
  <done>24 Fälle über sechs describe-Blöcke (19 + 5); Lauf nachweislich rot; nur Testdateien (ggf. plus Fixture-Helfer) committet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scan-tab.js Rendering + items.js (3 Zeilen) + index.html (Einfügungen) + docs/LOAD-ORDER.md — GREEN (SCAN-10)</name>
  <files>scan-tab.js, items.js, index.html, docs/LOAD-ORDER.md</files>
  <behavior>
    - Vorauswahl neuester Snapshot, Snapshot-Liste mit ⬇/🗑 je Eintrag, Hinweis + Shortcut ohne Snapshot (Tests 7, 8, 15)
    - Badges nach Baseline-Mengen inkl. Gruppen-Badge für Assets; ohne Manifest „unbekannt“ (Tests 4, 9, 14)
    - escHtml auf jedem Snapshot-String (Test 10); Suche/Filter (5, 11); Paging 300 + mehr laden (12); Debounce 150 ms (13)
    - Verdrahtung items.js/index.html/docs (16-18); Quell-Gates (19); 06-02-Audit bleibt grün
  </behavior>
  <read_first>tests/scan-tab.test.js (Task 1 — jede Erwartung ist Vertrag); scan-tab.js (06-02, Guard/Delete/Export/Export-Schwanz); items.js Zeilen 4223-4270 und 6504-6520; index.html Zeilen 2540-2557, 3104-3115, 3890-3965; docs/LOAD-ORDER.md (gesamt); persistence.js `_debounce` (Zeilen 358-364) und `idbSnapshotGetAll` (Sortierung); game-scan.js (`triggerGameScan`, Statuszeilen-Stil)</read_first>
  <action>
    **scan-tab.js (Rendering-Teil, unter den 06-02-Funktionen, vor dem Dual-Export-Schwanz):** Konstanten `const SCAN_PAGE_SIZE = 300;`, `const SCAN_CATEGORIES = ['globals', 'assets', 'groups', 'hooks', 'mods', 'patching', 'probes'];`, `const SCAN_BADGE_LABEL = { genutzt: 'bereits genutzt', neu: 'neu', unbekannt: 'unbekannt' };`; Zustand `let _scanState = { records: [], selectedId: null, rowsForId: null, rows: [], shown: SCAN_PAGE_SIZE };`; `let _scanRenderDebounced = null;` (lazy).
    Reine Funktionen (kein DOM, kein Global außer Argumenten — Node-tauglich): `_scanFlatten(inventory)` → `[]` für Nicht-Objekte; sonst Zeilen `{ category, kind, name, detail, group?, probe? }` in dieser Reihenfolge: globals: `getters[]` → `kind 'getter'`, `detail ''`; `functions[]` → `kind 'function'`, `name f.name`, `detail 'Arität ' + f.arity`; `values[]` → `kind 'value'`, `detail v.type`; assets: `assets.items[]` → `category 'assets'`, `kind 'item'`, `name it.Name`, `group String(it.Group ?? '')`, `detail 'Gruppe ' + group`; groups: `assets.groups[]` → `kind 'group'`, `name g.Name`, `detail (g.assetCount != null ? g.assetCount + ' Assets' : '')`; hooks: aus `chatHooks`: eine Zeile `kind 'api'`, `name 'ChatRoomRegisterMessageHandler'`, `detail` aus `exists`/`arity` (z. B. `'vorhanden · Arität 1'` bzw. `'fehlt'`) — nur wenn `chatHooks.ChatRoomRegisterMessageHandler` existiert; `registry.handlers[]` → `kind 'handler'`, `name h.Description || '(ohne Beschreibung)'`, `detail 'Priority ' + h.Priority`; `hookedChatFunctions[]` → `kind 'hooked'`, `name`, `detail 'gehookt von ' + hookedByMods.join(', ')`; mods: `mods[]` → `kind 'mod'`, `name m.name`, `detail (m.version || '') + (m.fullName ? ' · ' + m.fullName : '')`; patching: `modSdk.patching[]` → `kind 'patch'`, `name p.name`, `detail 'gehookt von ' + (p.hookedByMods || []).join(', ') + (p.patchedByMods?.length ? ' · gepatcht von ' + p.patchedByMods.join(', ') : '')`; probes: für jeden Key außer `sweep` in `probes` → `kind 'probe'`, `name key`, `probe key`, `detail present ? 'vorhanden' + (version ? ' · ' + version : '') : 'nicht vorhanden'`; anschließend je Probe die Namen aus `functions` / `api` / `screenFunctions.sample` / `sample` (was vorhanden ist) → `kind 'probe-api'`, `name key + '.' + fn`, `probe key`, `detail ''`; `sweep[]` → `kind 'sweep'`, `name`, `probe ''`, `detail 'Global mit Mod-Präfix'`. Alle Namen per `String(x ?? '')`; leere Namen überspringen. Die Fixture aus Task 1 ergibt damit 24 Zeilen — `EXPECTED_ROWS` im Test entsprechend.
    `_scanBaselineSets(manifest)` → `null` für falsy; sonst `{ identifiers: new Set(ids.map(e => e.name)), modProbes: new Set((manifest.modProbes || []).map(s => String(s).toLowerCase())), counts: { identifiers: ids.length, function: n, assetGroup: n, unknown: n } }` mit `ids = Array.isArray(manifest.identifiers) ? manifest.identifiers : []`. `_scanBadge(row, sets)` → `'unbekannt'` ohne `sets`; `assets` → `sets.identifiers.has(row.group)`; `mods` → `sets.modProbes.has(row.name.toLowerCase())`; `probes` → `row.probe && sets.modProbes.has(row.probe.toLowerCase())`; sonst `sets.identifiers.has(row.name)`; Ergebnis `'genutzt'` / `'neu'`. `_scanFilter(rows, query, category)` → `q = String(query || '').trim().toLowerCase()`; Kategorie `'all'` oder gleich; Treffer wenn `!q` oder `(row.name + ' ' + (row.detail || '')).toLowerCase().includes(q)`. `_scanCountBadges(rows, sets)` → Objekt mit Schlüsseln `all` + jeder Kategorie aus `SCAN_CATEGORIES`, Wert `{ total, genutzt, neu, unbekannt }`.
    DOM-Teil: `_scanEl(id)` = `document.getElementById(id)`; `_scanManifest()` = `typeof BASELINE_MANIFEST !== 'undefined' ? BASELINE_MANIFEST : null`; `async function renderScanTab()` → `records = await idbSnapshotGetAll()` (aufsteigend nach ts; neuester = letzter); `_scanState.records = records`; wenn `selectedId` nicht mehr vorhanden → `records.length ? records[records.length - 1].id : null`; `_scanApplySelection()`. `_scanApplySelection()` → `rec = records.find(r => r.id === selectedId)`; wenn `rec` und `rowsForId !== rec.id`: `rows = _scanFlatten(rec.inventory)`, `rowsForId = rec.id`; ohne `rec`: `rows = []`, `rowsForId = null`; `shown = SCAN_PAGE_SIZE`; `_scanRenderSnapshots(); _scanRenderBaselineInfo(); _scanRender();`. `_scanRenderSnapshots()` → `el = _scanEl('scanSnapshotList')`; ohne Datensätze: `innerHTML` = Hinweis-Div `Noch kein Snapshot – ⚙️ Tweaks → 🔎 Spiel scannen` plus, wenn `typeof triggerGameScan === 'function'`, ein Button `<button class="scan-btn" onclick="triggerGameScan()">🔎 Neuer Scan</button>` (der Tweaks-Trigger bleibt unverändert — RESEARCH Open Question 2); sonst je Datensatz (neuester zuerst) ein `<div class="scan-snap{ aktiv ? ' scan-snap-active' : '' }" onclick="scanSelectSnapshot('${escJsAttr(r.id)}')">` mit Text `📸 ${escHtml(_scanFormatTs(r.ts))} · BC ${escHtml(r.gameVersion || '?')} · ${r.modCount} Mods · ${_scanKb(r.sizeBytes)} KB` und zwei Buttons `<button class="scan-btn" onclick="event.stopPropagation(); exportGameSnapshot('${escJsAttr(r.id)}')">⬇ Exportieren</button>` und `<button class="scan-btn scan-btn-danger" onclick="event.stopPropagation(); deleteGameSnapshot('${escJsAttr(r.id)}')">🗑 Löschen</button>` — genau eine Vorlagestelle je Button (Gates zählen die `('${escJsAttr(`-Literale je 1×); ohne `innerHTML +=` (String zusammensetzen, einmal zuweisen). `_scanRenderBaselineInfo()` → `sets = _scanBaselineSets(_scanManifest())`; mit Manifest: Text `Baseline: ${counts.identifiers} Bezeichner (${counts.function} Funktionen, ${counts.assetGroup} Asset-Gruppen, ${counts.unknown} unklassifiziert) · „bereits genutzt“ = Name steht im Baseline-Manifest des Tools; Assets nach Gruppe (gruppenweise Präzision)`; ohne: `⚠️ baseline-manifest.js nicht geladen – Badges zeigen „unbekannt“` — per `textContent`. `_scanRender()` → `query = _scanEl('scanSearch')?.value ?? ''`, `category = _scanEl('scanCategory')?.value || 'all'`; `sets = _scanBaselineSets(_scanManifest())`; `filtered = _scanFilter(rows, query, category)`; `slice = filtered.slice(0, shown)`; Zeilen-HTML je Eintrag: `<div class="scan-row"><span class="scan-cat">${escHtml(r.category)}</span><span class="scan-kind">${escHtml(r.kind)}</span><span class="scan-name">${escHtml(r.name)}</span><span class="scan-detail">${escHtml(r.detail || '')}</span><span class="scan-badge scan-badge-${badge}">${SCAN_BADGE_LABEL[badge]}</span></div>`; danach, wenn `filtered.length > shown`: `<button class="scan-btn" onclick="scanLoadMore()">mehr laden (${filtered.length - shown} weitere)</button>`; einmalige Zuweisung an `scanList.innerHTML`; `c = _scanCountBadges(filtered, sets)`; `scanCount.textContent = `${slice.length} von ${filtered.length} Einträgen (${c.all.genutzt} genutzt · ${c.all.neu} neu)``. `scanSelectSnapshot(id)` → `selectedId = id; _scanApplySelection()` (kein IDB-Zugriff). `scanOnSearch()` → `shown = SCAN_PAGE_SIZE; if (!_scanRenderDebounced) _scanRenderDebounced = _debounce(_scanRender, 150); _scanRenderDebounced();`. `scanOnFilter()` → `shown = SCAN_PAGE_SIZE; _scanRender()`. `scanLoadMore()` → `shown += SCAN_PAGE_SIZE; _scanRender()`. Kein Init-Hook nötig (Render beim Tab-Wechsel); falls einer gesetzt wird, in `try/catch` und ohne IDB-Zugriff beim Laden. Dual-Export-Schwanz erweitern um `SCAN_PAGE_SIZE, SCAN_CATEGORIES, _scanFlatten, _scanBaselineSets, _scanBadge, _scanFilter, _scanCountBadges`. Weiterhin: keine Bridge-/IDB-Eigenlogik, kein Schreibpfad, `idbSnapshotDelete(` bleibt genau 1× (nur in `deleteGameSnapshot`), keine Gate-Literale in Kommentaren.
    **items.js — genau drei Änderungen (Probe-Befund 1):** `'scan'` an `TAB_GROUPS.bots` anhängen; `,'scan'` nach `'inventar'` in der Sichtbarkeits-Liste von `switchTab`; neue Zeile `  if (tab === 'scan')          { if (typeof renderScanTab === 'function') renderScanTab(); }` direkt nach der `inventar`-Zeile. Nichts sonst (Diff gegen `49e4365`: 3 `+`, 2 `-`).
    **index.html — nur Einfügungen + eine Kommentarzeile:** (a) nach Zeile `tab-variablen-btn`: `  <button class="tab-btn"        id="tab-scan-btn"          onclick="switchTab('scan')">🔎 Scan</button>`; (b) LADEREIHENFOLGE-Kommentar: `→ game-scan.js → baseline-manifest.js → scan-tab.js → money.js`; (c) nach der `game-scan.js`-Write-Zeile: `document.write('<scr'+'ipt src="baseline-manifest.js?_='+_cbv+'"><\/scr'+'ipt>');` und `document.write('<scr'+'ipt src="scan-tab.js?_='+_cbv+'"><\/scr'+'ipt>');`; (d) nach dem schließenden `</div>` von `#tab-variablen` (vor dem `<style>` „Lock / Timer Dashboard“): `<!-- Scan (SCAN-10) -->`, ein `<style>`-Block mit `.scan-row` (Grid `78px 70px minmax(0,1fr) minmax(0,1fr) 120px`, `gap:8px`, `padding:3px 6px`, `font-size:.75rem`, `border-bottom:1px solid rgba(255,255,255,.06)`), `.scan-name{font-family:monospace;overflow-wrap:anywhere}`, `.scan-cat,.scan-kind,.scan-detail{color:var(--text3)}`, `.scan-badge{border-radius:6px;padding:1px 6px;text-align:center;font-size:.6875rem}`, `.scan-badge-genutzt{background:rgba(74,222,128,.15);color:#4ade80}`, `.scan-badge-neu{background:rgba(251,191,36,.15);color:#fbbf24}`, `.scan-badge-unbekannt{color:var(--text3);border:1px solid rgba(255,255,255,.12)}`, `.scan-snap{display:flex;align-items:center;gap:10px;padding:6px 8px;border:1px solid rgba(255,255,255,.08);border-radius:6px;margin-bottom:4px;cursor:pointer;font-size:.75rem}`, `.scan-snap-active{border-color:var(--accent, #7c9cff);background:rgba(124,156,255,.08)}`, `.scan-btn{background:none;border:1px solid rgba(255,255,255,0.12);border-radius:6px;color:var(--text2);font-size:.6875rem;padding:2px 8px;cursor:pointer}`, `.scan-btn-danger{color:#f87171}`, `#scanSearch,#scanCategory{background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.12);border-radius:6px;color:var(--text1);font-size:.75rem;padding:4px 8px}`; dann `<div id="tab-scan" class="tab-pane">` im Stil von `#tab-inventar`: Kopfzeile mit `<span class="pane-title" …>🔎 Scan</span>`, `<span id="scanCount" style="font-size:.75rem;color:var(--text3)"></span>`, Aktualisieren-Button `onclick="renderScanTab()"` (`⟳ Aktualisieren`); Hinweistext (font-size:.6875rem, text3): „Fundliste des letzten Spiel-Scans (⚙️ Tweaks → 🔎 Spiel scannen). Badge „bereits genutzt“ = der Name steht im Baseline-Manifest des Tools (Assets: nach Gruppe). Snapshots werden nie automatisch gelöscht – 🗑 fragt immer nach.“; `<div id="scanSnapshotList"></div>`; `<div id="scanBaselineInfo" style="font-size:.6875rem;color:var(--text3);margin:6px 0"></div>`; Filterzeile `<div style="display:flex;gap:8px;margin:10px 0">` mit `<input id="scanSearch" type="search" placeholder="Suchen (Name, Detail)…" oninput="scanOnSearch()" style="flex:1">` und `<select id="scanCategory" onchange="scanOnFilter()">` mit den acht Optionen (`all` „Alle Kategorien“, `globals` „Globals“, `assets` „Assets“, `groups` „Asset-Gruppen“, `hooks` „Chat-Hooks“, `mods` „Mods“, `patching` „Patching“, `probes` „Probes“); `<div id="scanList"></div>`; `</div></div><!-- /tab-scan -->`. Kein Inline-Handler mit `deleteGameSnapshot(`/`exportGameSnapshot(` in index.html (die Buttons entstehen nur im Rendering mit `escJsAttr`), kein `idbSnapshotDelete`.
    **docs/LOAD-ORDER.md:** Tabelle: neue Zeile 8 `baseline-manifest.js` (Stellt bereit: `BASELINE_MANIFEST` — generiert per `npm run baseline`, nicht von Hand editieren; Braucht: —), neue Zeile 9 `scan-tab.js` (Stellt bereit: `renderScanTab`, `deleteGameSnapshot` (einzige Aufrufstelle von `idbSnapshotDelete`, hinter `confirm()`), `exportGameSnapshot`, `scanOnSearch`/`scanOnFilter`/`scanLoadMore`/`scanSelectSnapshot`; Braucht: persistence.js `idbSnapshotGetAll`/`idbSnapshotGet`/`idbSnapshotDelete`/`_debounce`, items.js `showStatus`/`escHtml`/`escJsAttr`/`_jsonParts`, optional game-scan.js `triggerGameScan` (typeof-geguardet), baseline-manifest.js (typeof-geguardet)); `money.js` → 10 … `bc-autobackup.js` → 18. Guard-Absatz: Satz ergänzen, dass `scan-tab.js` die Kette persistence.js → items.js (ohne bridge.js) prüft und in Node (ohne `window`) ohne Guard lädt (Dual-Export für `tools/analyze-snapshot.js`). „Neues Modul hinzufügen“ Schritt 4 präzisieren: `CORE_SCRIPTS` nur für Kern-Skripte (Laufzeit-Voraussetzung von items.js/bot-ui.js oder von jeder items.js-Sandbox benötigt); Blatt-Module wie `money.js`, `rank.js`, `scan-tab.js` werden in Tests explizit geladen (`loadScript(['items.js', 'baseline-manifest.js', 'scan-tab.js'])`). Abschnitt „Tests“: dieses Ladebeispiel ergänzen; `CORE_SCRIPTS`-Text unverändert lassen. Bestehende Gate-Strings (`'game-scan.js'`, `332fc2d`, `GAME_SCAN_DATA`, `GAME_INVENTORY_DATA`, `idbSnapshotPut`, `loadOrderFatal`, `CORE_SCRIPTS`, `document.write`, alle Modulnamen) bleiben erhalten.
    **GREEN:** `node --check scan-tab.js items.js`; `npx vitest run tests/scan-tab.test.js tests/snapshot-delete.test.js tests/scan-tab-export.test.js tests/load-order-guard.test.js tests/game-scan-bridge.test.js tests/delete-confirmation.test.js tests/screenshot-export.test.js` → grün (tests/analyze-snapshot.test.js bleibt bis Task 3 rot); Commit (nur diese vier Pfade): `git add scan-tab.js items.js index.html docs/LOAD-ORDER.md` → `feat(06-03): Scan-Tab — Snapshot-Liste, Badges aus Baseline, Suche/Filter/Paging, Verdrahtung items.js/index.html, LOAD-ORDER — GREEN`.
    Fürs SUMMARY: GREEN-Ausgabe, die Badge-Regel in einem Satz, die drei items.js-Zeilen, die Human-Checks (siehe `<human-check>`) als offene Liste.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && node --check scan-tab.js && node --check items.js && npx vitest run tests/scan-tab.test.js tests/snapshot-delete.test.js tests/scan-tab-export.test.js tests/load-order-guard.test.js tests/game-scan-bridge.test.js tests/delete-confirmation.test.js tests/screenshot-export.test.js tests/baseline-manifest.test.js && echo SCAN-TAB-GREEN</automated>
    <fails_when>`SCAN-TAB-GREEN` fehlt — Syntaxfehler, ein Fall von tests/scan-tab.test.js `failed` (z. B. Badge falsch, XSS unescaped, Paging über 300, Debounce fehlt, Vorauswahl falsch), der 06-02-Audit rot (zweite Aufrufstelle), oder ein Ladeordnungs-/Doku-Gate der bestehenden Dateien rot</fails_when>
  </verify>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && DI=$(git diff 49e4365 -- items.js) && test "$(printf '%s\n' "$DI" | grep '^+[^+]' | wc -l)" = 3 && test "$(printf '%s\n' "$DI" | grep '^-[^-]' | wc -l)" = 2 && DH=$(git diff 49e4365 -- index.html) && test "$(printf '%s\n' "$DH" | grep '^-[^-]' | grep -vc 'LADEREIHENFOLGE')" = 0 && git diff --quiet 49e4365 -- game-scan.js bridge.js loader.js persistence.js tests/helpers/loadScript.js && test "$(grep -c 'idbSnapshotDelete' index.html)" = 0 && test "$(grep -c 'build-baseline' index.html)" = 0 && test "$(grep -c 'analyze-snapshot' index.html)" = 0 && test "$(grep -c 'idbSnapshotDelete(' scan-tab.js)" = 1 && test "$(grep -c 'innerHTML +=' scan-tab.js)" = 0 && test "$(grep -Ec 'const SCAN_PAGE_SIZE += +300;' scan-tab.js)" = 1 && test "$(grep -c '_debounce(' scan-tab.js)" = 1 && test "$(grep -c "^const CORE_SCRIPTS = \['persistence.js', 'bridge.js', 'items.js', 'game-scan.js'\];" tests/helpers/loadScript.js)" = 0 -o "$(grep -c "CORE_SCRIPTS = \['persistence.js', 'bridge.js', 'items.js', 'game-scan.js'\]" tests/helpers/loadScript.js)" = 1 && FILES=$(git show --name-only --format= HEAD) && test "$(printf '%s\n' "$FILES" | sort | tr '\n' ' ')" = "docs/LOAD-ORDER.md index.html items.js scan-tab.js " && echo WIRING-OK</automated>
    <fails_when>`WIRING-OK` fehlt — items.js hat mehr/weniger als 3 hinzugefügte bzw. 2 gelöschte Zeilen gegenüber `49e4365`, index.html hat gelöschte Zeilen außer der Kommentarzeile, eine Nachbardatei wurde angefasst, index.html referenziert Persistenz-/Dev-Tooling direkt, scan-tab.js hat eine zweite Aufrufstelle oder `innerHTML +=`, `CORE_SCRIPTS` wurde erweitert, oder der Commit enthält andere Pfade als die vier genannten</fails_when>
    <human-check>End-of-Phase-Human-Checks (nicht blockierend, im SUMMARY listen; Orchestrator-Entscheidung 6): (1) Deploy/Serve der aktuellen Version, Tool hart neu laden (Strg+F5): kein FATAL-Banner; im Obertab 🤖 Bots erscheint „🔎 Scan“; Tab öffnen → ohne Snapshot: Hinweis auf ⚙️ Tweaks → 🔎 Spiel scannen plus „🔎 Neuer Scan“; mit vorhandenem Snapshot (Phase-5-Live-Check, sonst jetzt Bookmarklet neu ausführen, Verbinden, Spiel scannen): Snapshot-Liste mit Datum UTC, „BC R131“, „28 Mods“, KB; neuester markiert; Baseline-Zeile nennt die Bezeichner-Anzahl. (2) Fundliste: Kategorie „Alle“ zeigt 300 Zeilen + „mehr laden (N weitere)“; Suche „ChatRoom“ verengt sichtbar innerhalb ~150 ms ohne Ruckeln; Filter „Mods“ → 28 Zeilen; `ChatRoomSendChat` (globals) = „bereits genutzt“, `ItemNeck` (groups) = „bereits genutzt“, `BCX`/`LSCG`/`MBS`/`WCE` (mods) = „bereits genutzt“, ein beliebiger `Dialog*`-Name ohne Tool-Bezug = „neu“; kein Einfrieren beim Wechsel auf „Globals“. (3) 🗑 beim ältesten Snapshot: Dialog nennt Datum/BC-Version/Mods; Abbrechen → Eintrag bleibt (auch nach Reload); Bestätigen → Eintrag weg, andere bleiben, Status „✅ Snapshot gelöscht“. (4) ⬇ beim neuesten Snapshot: Datei `BC_Snapshot_<Datum>_R131_<id>.json` wird geladen; enthält `_meta` und `snapshot.inventory` (Größe im SUMMARY notieren) — diese Datei ist die Eingabe für Plan 06-04.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "async function renderScanTab()" scan-tab.js` = 1; `grep -c "function _scanFlatten(inventory)" scan-tab.js` = 1; `grep -c "function _scanBadge(row, sets)" scan-tab.js` = 1; `grep -c "function scanLoadMore()" scan-tab.js` = 1; `grep -c "typeof BASELINE_MANIFEST" scan-tab.js` >= 1; `grep -c "typeof triggerGameScan" scan-tab.js` = 1
    - `grep -c "escHtml(" scan-tab.js` >= 8; `grep -c "deleteGameSnapshot('\${escJsAttr(" scan-tab.js` = 1; `grep -c "exportGameSnapshot('\${escJsAttr(" scan-tab.js` = 1
    - `grep -c "if (tab === 'scan')          { if (typeof renderScanTab === 'function') renderScanTab(); }" items.js` = 1; `grep -c "'inventar','scan']" items.js` = 1; `grep -c "'variablen','scan']" items.js` = 1
    - `grep -c 'id="tab-scan-btn"' index.html` = 1; `grep -c 'id="tab-scan"' index.html` = 1; `grep -c 'baseline-manifest.js?_=' index.html` = 1; `grep -c 'scan-tab.js?_=' index.html` = 1; `grep -c 'onclick="renderScanTab()"' index.html` = 1
    - `grep -c '| 9 | `scan-tab.js`' docs/LOAD-ORDER.md` = 1; `grep -c '| 18 | `bc-autobackup.js`' docs/LOAD-ORDER.md` = 1; `grep -c 'npm run baseline' docs/LOAD-ORDER.md` >= 1
  </acceptance_criteria>
  <done>Tab sichtbar und funktional in der Sandbox (Vorauswahl, Badges, Suche, Filter, Paging, Debounce, XSS-sicher); drei items.js-Zeilen; index.html nur Einfügungen; Doku nachgezogen; alle genannten Testdateien grün; Human-Checks im SUMMARY gelistet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `tools/analyze-snapshot.js` + `npm run analyze` — GREEN (Grundlage für SCAN-12)</name>
  <files>tools/analyze-snapshot.js, package.json</files>
  <behavior>
    - Zahlen identisch mit `_scanCountBadges` aus scan-tab.js (Test 20); Export-, Roh- und Inventar-Form (21); Markdown mit Limit (22); CLI (23); statisch (24)
  </behavior>
  <read_first>tests/analyze-snapshot.test.js (Task 1 — Vertrag); scan-tab.js Dual-Export (Task 2); tools/build-baseline.js (06-01, CLI-/Modulstil); package.json</read_first>
  <action>
    **`tools/analyze-snapshot.js` (neu, CJS, nur `fs`/`path`):** Kopfkommentar (Deutsch): dev-only Analysewerkzeug für SCAN-12 — liest einen Snapshot-Export (⬇ aus dem Scan-Tab) und `baseline-manifest.json`, wendet exakt die Badge-Funktionen des Scan-Tabs an (eine Quelle der Wahrheit) und druckt Markdown; nie von index.html geladen. `const tab = require(path.join(__dirname, '..', 'scan-tab.js'));` (Guard entfällt ohne `window`). `loadJson(file)` (utf8 → `JSON.parse`). `unwrapSnapshot(exported)` → wenn `exported?.snapshot?.inventory` → `{ rec: exported.snapshot }`; sonst wenn `exported?.inventory` → `{ rec: exported }`; sonst `throw new Error('Kein Snapshot: erwartet {_meta, snapshot} oder einen Datensatz mit inventory')`. `analyzeSnapshot(exported, manifest)` → `rec = unwrapSnapshot(exported).rec`; `rows = tab._scanFlatten(rec.inventory)`; `sets = tab._scanBaselineSets(manifest)`; `counts = tab._scanCountBadges(rows, sets)`; `neu` = Objekt je Kategorie: sortierte, deduplizierte Namen der Zeilen mit Badge `'neu'` (Sortierung locale-frei); `genutzt` analog (für die Doku nützlich); `meta = { id: rec.id ?? null, ts: rec.ts ?? null, gameVersion: rec.gameVersion ?? null, modCount: rec.modCount ?? null, sizeBytes: rec.sizeBytes ?? null }`; `baseline = sets ? sets.counts : null`; Rückgabe `{ meta, baseline, counts, neu, genutzt }`. `renderMarkdown(result, { limit = 40 } = {})` → Überschrift `## Snapshot vs. Baseline` mit Meta-Zeile (`ts` als ISO UTC über `new Date(ts).toISOString()`, gameVersion, modCount, sizeBytes) und Baseline-Zeile; Tabelle `| Kategorie | gesamt | genutzt | neu | unbekannt |` mit einer Zeile je Kategorie aus `tab.SCAN_CATEGORIES` und zuletzt `| all | … |`; danach je Kategorie ein Absatz `### neu: <kategorie> (<n>)` mit den ersten `limit` Namen als kommagetrennte Inline-Code-Liste und, wenn gekappt, `… (+<rest> weitere)`; keine Fenced-Code-Blöcke (drei Backticks kommen im Output nicht vor). `module.exports = { loadJson, unwrapSnapshot, analyzeSnapshot, renderMarkdown }`. CLI-Guard `if (require.main === module)`: Argumente `<export.json> [manifest.json] [--limit N]`; ohne Export-Pfad → `console.error('Usage: node tools/analyze-snapshot.js <export.json> [manifest.json] [--limit N]')` und `process.exit(2)`; Manifest-Default `path.join(__dirname, '..', 'baseline-manifest.json')`; Ausgabe per `process.stdout.write(renderMarkdown(result, { limit }))`; Exit 0.
    **package.json:** per Edit nach `"baseline"` die Zeile `"analyze": "node tools/analyze-snapshot.js"` ergänzen — sonst nichts.
    **GREEN:** `node --check tools/analyze-snapshot.js`; `npx vitest run tests/analyze-snapshot.test.js tests/scan-tab.test.js` → grün; `npm test` → 28 Dateien grün. Commit (nur diese zwei Pfade): `git add tools/analyze-snapshot.js package.json` → `feat(06-03): tools/analyze-snapshot.js (npm run analyze) — Snapshot-vs-Baseline-Zahlen mit den Badge-Funktionen des Scan-Tabs — GREEN`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && node --check tools/analyze-snapshot.js && npx vitest run tests/analyze-snapshot.test.js tests/scan-tab.test.js && OUT=$(mktemp) && npm test > "$OUT" 2>&1; grep -Eq "Test Files +28 passed" "$OUT" && ! grep -Eq "Tests +[0-9]+ failed|Test Files +[0-9]+ failed" "$OUT" && test "$(grep -c '"analyze": "node tools/analyze-snapshot.js"' package.json)" = 1 && test "$(grep -c "require(path.join(__dirname, '..', 'scan-tab.js'))" tools/analyze-snapshot.js)" = 1 && test "$(grep -c 'analyze-snapshot' index.html)" = 0 && FILES=$(git show --name-only --format= HEAD) && test "$(printf '%s\n' "$FILES" | sort | tr '\n' ' ')" = "package.json tools/analyze-snapshot.js " && echo ANALYZE-OK</automated>
    <fails_when>`ANALYZE-OK` fehlt — Syntaxfehler, ein Fall der beiden Testdateien `failed`, die volle Suite zeigt weniger als 28 Dateien oder eine `failed`-Zeile, das npm-Script fehlt, das Skript bindet scan-tab.js nicht über `__dirname` ein, index.html referenziert das Skript, oder der Commit enthält andere Pfade als die zwei genannten</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'function analyzeSnapshot' tools/analyze-snapshot.js` = 1; `grep -c 'function renderMarkdown' tools/analyze-snapshot.js` = 1; `grep -c 'require.main === module' tools/analyze-snapshot.js` = 1; `grep -c 'Usage' tools/analyze-snapshot.js` >= 1
    - `D=$(git diff 49e4365 -- package.json) && test "$(printf '%s\n' "$D" | grep -c '^-[^-]')" = 0` (nur Einfügungen über beide Phase-6-Scripts)
  </acceptance_criteria>
  <done>Analyse-Skript liefert die Zahlen des Tabs als Markdown; CLI läuft; 28 Dateien grün; Commit exakt zwei Pfade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot-Inhalt (aus dem Spiel-Tab / Mods) → Tool-DOM | Mod-, Asset-, Hook-, Funktionsnamen sind fremdkontrollierte Strings; nur über `escHtml`/`escJsAttr` ins DOM |
| Datenvolumen (≈ 24k Zeilen) → Tool-Tab | Rendering muss gekappt und Sucheingabe entprellt sein, sonst friert das Tool-Fenster ein |
| Baseline-Manifest → Badge-Aussage | Fehlendes Manifest darf nie „genutzt“ vortäuschen |
| Ladeordnung (index.html, Tests) | Neue Module sind Blatt-Module; Guard fängt Fehlplatzierung sichtbar ab |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-05 | Tampering / Elevation of Privilege | Stored XSS über Mod-/Asset-Namen im Scan-Tab | high | mitigate | `escHtml` auf Name/Detail/Kategorie/Version, `escJsAttr` auf ids in Inline-Handlern; Test 10 mit `<img src=x onerror=alert(1)>`; statisch `escHtml(` ≥ 8, `innerHTML +=` 0×, index.html ohne Inline-Handler mit Snapshot-Daten |
| T-6-06 | Denial of Service | 18.7k Globals blockieren das Tool-Fenster | medium | mitigate | `SCAN_PAGE_SIZE = 300` + „mehr laden“ (Test 12), Suche über `_debounce(…, 150)` (Test 13), Flatten einmal je Snapshot-Auswahl (`rowsForId`-Cache) |
| T-6-12 | Tampering | Badge „genutzt“ ohne geladenes Manifest (falsche Sicherheit) | medium | mitigate | `_scanBadge(row, null) → 'unbekannt'` (Test 4/14), Hinweis in `#scanBaselineInfo` |
| T-6-13 | Denial of Service | Neue Module brechen die Ladekette / falsche Tab-Listen (Pitfall 1) | high | mitigate | Guard in scan-tab.js; Write-Zeilen direkt nach game-scan.js (Test 17); beide items.js-Listen + Render-Hook (Test 16); Diff-Gates 3/2 Zeilen; `CORE_SCRIPTS` unverändert |
| T-6-11 | Elevation of Privilege | Sammellöschung über die UI | high | mitigate | Nur Einzel-Buttons je Snapshot; kein „alle löschen“; index.html ohne `idbSnapshotDelete`; 06-02-Audit bleibt Pflichtlauf in Task 2 |
| T-6-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in diesem Plan (package.json bekommt nur ein Script) |
</threat_model>

<verification>
- `npx vitest run tests/scan-tab.test.js tests/analyze-snapshot.test.js` → 24 passed; `npm test` → 28 Dateien grün (2 expected fail unverändert).
- Verdrahtungs-Gates (zweiter `<automated>`-Block Task 2): items.js 3/2 Zeilen, index.html nur Einfügungen + Kommentarzeile, Nachbardateien unverändert, `CORE_SCRIPTS` unverändert.
- Drei Commits: test → feat (Tab) → feat (Analyse-Skript); Commit-Scope-Gates exakt.
- VALIDATION.md 6-03-01/6-03-02/6-03-03 ✅ nach GREEN; Human-Checks im 06-03-SUMMARY gelistet (nicht blockierend).
</verification>

<success_criteria>
- SCAN-10: Fundliste durchsuchbar, nach Kategorie filterbar, jede Zeile mit Badge „bereits genutzt“/„neu“ gegenüber dem Baseline-Manifest; Snapshot-Auswahl, Export- und Lösch-Buttons je Snapshot.
- Tab in Produktion verdrahtet (items.js drei Zeilen, index.html Einfügungen), dokumentiert (LOAD-ORDER), getestet (Sandbox), XSS-sicher, gepagt.
- `npm run analyze -- <export.json>` liefert die Zahlen des Tabs als Markdown (Grundlage für 06-04).
</success_criteria>

## Artifacts this phase produces

Siehe die phasenweite Liste in `06-01-PLAN.md`. Dieser Plan (06-03) liefert: `tests/scan-tab.test.js`, `tests/analyze-snapshot.test.js`, `tools/analyze-snapshot.js` (neu); `scan-tab.js` (Rendering-Teil), `items.js` (3 Zeilen), `index.html` (Tab-Button, Pane, Styles, Write-Zeilen, Kommentar), `docs/LOAD-ORDER.md`, `package.json` (Script `analyze`) (geändert).

<output>
Create `.planning/phases/06-scan-tab-analyse/06-03-SUMMARY.md` when done — mit RED-/GREEN-Ausgaben, der Badge-Regel, den drei items.js-Zeilen, der vollständigen Liste der End-of-Phase-Human-Checks (aus `<human-check>`) und der Commit-Liste.
</output>
