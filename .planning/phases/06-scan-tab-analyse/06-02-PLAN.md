---
phase: 06-scan-tab-analyse
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tests/snapshot-delete.test.js
  - tests/scan-tab-export.test.js
  - tests/persistence-module.test.js
  - persistence.js
  - scan-tab.js
autonomous: true
requirements: [SCAN-11]
tags: [snapshot-delete, confirm, static-audit, export, json-download, red-green]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SCAN-11: `idbSnapshotDelete(id)` in persistence.js ist die EINZIGE Lösch-Operation auf dem Store `snapshots` im gesamten Repo (Orchestrator-Entscheidung 2): ein Datensatz je Aufruf, `readwrite`-Transaktion, Rückgabe `true`/`false`, ungültige id (`undefined`/`null`/Objekt/leerer String) → `false` ohne IDB-Zugriff, Fehler → `console.warn('[IDB] snapshot delete:', err)` + `false`, nie ein Throw; keine Leer-/Sammellösch-Funktion; `_IDB_VERSION` bleibt 3, das Schema unverändert"
    - "UI-Pfad `deleteGameSnapshot(id)` (scan-tab.js) folgt der STAB-09-Guard-Reihenfolge (02-02): Existenz-Prüfung per `idbSnapshotGet(id)` (fehlt → `showStatus('⚠️ Snapshot nicht gefunden', 'info')`, kein confirm, `false`) → genau EIN `confirm()` mit Datum (ISO UTC), BC-Version und Mod-Anzahl im Text → Abbruch (`false` oder `confirm` nicht als Funktion verfügbar → fail-closed) löscht NICHTS und meldet nichts → `idbSnapshotDelete(id)` → Erfolg `showStatus('✅ Snapshot gelöscht', 'success')` + `renderScanTab()` (typeof-geguardet) → `true`; Fehlschlag `showStatus('❌ Snapshot konnte nicht gelöscht werden', 'error')` → `false`; kein „alle löschen“ (Aufrufstelle liegt nicht in einer Schleife)"
    - "Neuer statischer Audit (RESEARCH Pitfall 4, T-6-03): `tests/snapshot-delete.test.js` beweist, dass `objectStore(_IDB_SNAPSHOTS)` in persistence.js genau einmal mit der Lösch-Methode und nur innerhalb von `idbSnapshotDelete` aufgerufen wird, dass `idbSnapshotDelete(` in scan-tab.js genau einmal und nur innerhalb von `deleteGameSnapshot` hinter `confirm(` steht, dass keine andere Root-JS-Datei und index.html den Namen enthält, und dass scan-tab.js selbst keine IDB-Lösch-/Leer-/Open-Operation und keinen Snapshot-/KV-Schreibpfad enthält"
    - "Export (SCAN-12-Eingabe, Orchestrator-Entscheidung 3): `exportGameSnapshot(id)` lädt genau einen Datensatz per `idbSnapshotGet`, baut `{ _meta: { version: 1, tool: 'BC Konfigurator – Snapshot-Export', exportedAt, snapshotId, counts: { globals, assets, groups, patching, mods } }, snapshot: <Datensatz unverändert> }`, serialisiert über `_jsonParts(payload)` (items.js, größenfest) in einen `Blob` (`application/json`), lädt per Anker `BC_Snapshot_<YYYY-MM-DD>_<gameVersion>_<id>.json` herunter (Muster `exportScreenshotsOnly`), meldet `showStatus('✅ Snapshot exportiert – <kb> KB', 'success')`; unbekannte id → `showStatus('⚠️ Snapshot nicht gefunden', 'info')` ohne Download; Fehler → `showStatus('❌ Snapshot-Export fehlgeschlagen: <msg>', 'error')` ohne Throw; der Export mutiert nichts (kein add/put/delete auf `snapshots`)"
    - "scan-tab.js (erster Teil, Plan 06-03 ergänzt das Rendering) beginnt mit dem Ladereihenfolge-Guard (Muster game-scan.js: `idbSnapshotGetAll`/`idbSnapshotGet`/`idbSnapshotDelete` ← persistence.js, `showStatus`/`escHtml`/`escJsAttr`/`_jsonParts` ← items.js → `#loadOrderFatal` + Throw), ist Node-tauglich (Guard nur wenn `typeof window !== 'undefined'`, kein Top-Level-DOM-Zugriff) und endet mit dem Dual-Export-Schwanz (`deleteGameSnapshot`, `exportGameSnapshot`, `_scanFormatTs`, `_scanExportName`) — noch nicht in index.html geladen (Verdrahtung ist Plan 06-03)"
  artifacts:
    - path: "persistence.js"
      provides: "`async function idbSnapshotDelete(id)` neben idbSnapshotPut/GetAll/Get/Keys; Kopfkommentar der Snapshot-Sektion aktualisiert (Löschen nur über diese Funktion, nur hinter Bestätigung); Export-Schwanz um `idbSnapshotDelete` ergänzt"
      contains: "async function idbSnapshotDelete(id)"
    - path: "scan-tab.js"
      provides: "Guard, `_scanFormatTs(ts)`, `_scanExportName(rec)`, `deleteGameSnapshot(id)`, `exportGameSnapshot(id)`, Dual-Export-Schwanz"
      contains: "async function deleteGameSnapshot(id)"
    - path: "tests/snapshot-delete.test.js"
      provides: "Verhalten (confirm false/true/fehlt, unbekannte id, Fehlerpfad, idbSnapshotDelete direkt, Kernwert zweiter Datensatz) + describe „audit“ (statische Gates persistence.js / scan-tab.js / alle Root-JS / index.html)"
      contains: "deleteGameSnapshot"
    - path: "tests/scan-tab-export.test.js"
      provides: "Payload-Form, Dateiname, Anker/click, nicht gefunden, Fehlerpfad, Mutationsfreiheit (Op-Spy), statisch (_jsonParts/Blob, kein Schreibpfad)"
      contains: "exportGameSnapshot"
    - path: "tests/persistence-module.test.js"
      provides: "Nachzug: Export-Listen (2×) um `idbSnapshotDelete`; statisches Gate auf „genau eine Lösch-Operation, in idbSnapshotDelete“"
      contains: "'idbSnapshotDelete'"
  key_links:
    - from: "scan-tab.js `deleteGameSnapshot(id)`"
      to: "persistence.js `idbSnapshotDelete(id)`"
      via: "nur nach `confirm()` → true; typeof-Guard auf `confirm`"
      pattern: "idbSnapshotDelete\\(id\\)"
    - from: "scan-tab.js `exportGameSnapshot(id)`"
      to: "items.js `_jsonParts(payload)` → `Blob` → Anker-Download"
      via: "Muster `exportScreenshotsOnly` (Phase 4, SPLIT-07)"
      pattern: "_jsonParts\\(payload\\)"
    - from: "tests/snapshot-delete.test.js (audit)"
      to: "persistence.js / scan-tab.js / index.html / alle Root-*.js"
      via: "Zähl-Gates auf die Lösch-Literale und den Funktionsnamen"
      pattern: "readdirSync\\(REPO_ROOT\\)"
  prohibitions:
    - requirement_id: SCAN-11
      category: safety
      verification: automated
      statement: "Kein Löschen ohne Bestätigung: `deleteGameSnapshot` ruft `idbSnapshotDelete` nur nach `confirm() === true`; ohne verfügbare `confirm`-Funktion wird nicht gelöscht; kein „alle löschen“ (0× Schleife/`Promise.all` um die Aufrufstelle); Löschen ist nur manuell erreichbar (kein Init-/Timer-Pfad)"
    - requirement_id: SCAN-11
      category: safety
      verification: automated
      statement: "Genau eine Lösch-Operation auf dem Store `snapshots` im Repo (persistence.js, innerhalb `idbSnapshotDelete`); genau eine Aufrufstelle (scan-tab.js, innerhalb `deleteGameSnapshot`); 0× in index.html und jeder anderen Root-JS-Datei; keine `clear`-/`deleteDatabase`-Operation; scan-tab.js hat keinen Snapshot- oder KV-Schreibpfad"
    - requirement_id: SCAN-11
      category: process
      verification: automated
      statement: "items.js, game-scan.js, index.html, loader.js bleiben in diesem Plan unverändert (git diff gegen `49e4365` leer); Commit-Scope-Gates exakt"
---

<objective>
Löschen mit Bestätigung (SCAN-11) und der Snapshot-Export als Eingabe für die Analyse (SCAN-12): eine einzige neue Persistenzfunktion `idbSnapshotDelete(id)` (persistence.js, modelliert nach `idbScreenshotDelete`), der UI-Pfad `deleteGameSnapshot(id)` in der neuen Datei `scan-tab.js` (Existenz-Prüfung → `confirm()` → Löschen → Status, fail-closed ohne `confirm`), der Export `exportGameSnapshot(id)` (ein Datensatz als JSON-Download über den bestehenden `_jsonParts`/Blob/Anker-Pfad) und ein NEUER statischer Audit, der beweist, dass es im gesamten Repo genau eine Lösch-Operation und genau eine bestätigte Aufrufstelle gibt (die STAB-09-Auditdatei ist items.js-/Property-Delete-spezifisch und deckt das strukturell nicht ab — RESEARCH Pitfall 4). RED → GREEN; scan-tab.js wird erst in Plan 06-03 in index.html verdrahtet.

Purpose: Erfolgskriterium 3 („Snapshots lassen sich im Scan-Tab nur manuell und nach Bestätigungsdialog löschen; ohne Bestätigung bleibt alles erhalten“) — der Kernwert „nichts wird automatisch gelöscht“ für den ersten Löschpfad des Snapshot-Stores; plus der Export, den Plan 06-04 als echte Datengrundlage braucht.
Output: `scan-tab.js` (neu, Teil 1), `tests/snapshot-delete.test.js`, `tests/scan-tab-export.test.js` (neu); `persistence.js`, `tests/persistence-module.test.js` (geändert).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scan-tab-analyse/06-RESEARCH.md
@.planning/phases/06-scan-tab-analyse/06-VALIDATION.md
@.planning/phases/05-gamecode-inventar/05-03-SUMMARY.md
@.planning/phases/02-speicher-sicherheit/02-02-SUMMARY.md
@.planning/phases/04-entflechtung/04-03-SUMMARY.md
@persistence.js
@game-scan.js
@tests/screenshot-export.test.js
@tests/helpers/loadScript.js

Planer-Probe (2026-09-19, HEAD `49e4365`; per grep ankern) — verbindliche Befunde:
1. **Snapshot-Sektion persistence.js (Zeilen ~177-248):** `idbSnapshotPut(record)` (add-only, id-Validierung: Zahl oder nicht-leerer String, sonst `_idbSchreibfehler('Spiel-Snapshot', …)` + `false`), `idbSnapshotGetAll()` (sortiert nach `ts ?? id` aufsteigend — der NEUESTE ist der letzte), `idbSnapshotGet(id)` (`null` wenn fehlt), `idbSnapshotKeys()`. Kopfkommentar der Sektion sagt heute „Keine Lösch-/Leer-Funktion in dieser Phase (… Phase 6 bringt die Lösch-API mit Bestätigung, SCAN-11)“ — diesen Satz in Task 2 auf den neuen Stand bringen, OHNE das Lösch-Literal (`objectStore(...)` + Methodenname) in den Kommentar zu schreiben (Gate zählt Vorkommen). Export-Schwanz Zeilen 366-375: `idbSnapshotPut, idbSnapshotGetAll, idbSnapshotGet, idbSnapshotKeys` in einer Zeile — `idbSnapshotDelete` dort anhängen.
2. **Bestehende Gates, die dieser Plan nachziehen MUSS (sonst rot):** `tests/persistence-module.test.js` Zeilen 42-47 und 82-87 erwarten die exakte sortierte Export-Schlüsselliste (`'idbSet', 'idbSnapshotGet', 'idbSnapshotGetAll', 'idbSnapshotKeys', 'idbSnapshotPut'`) → `'idbSnapshotDelete'` zwischen `'idbSet'` und `'idbSnapshotGet'` einfügen (beide Stellen); Test „statisch: keine Lösch-API für Snapshots …“ (Zeilen 283-295) erwartet 0× Lösch-Operation auf `_IDB_SNAPSHOTS` und 0× `idbSnapshotDelete` → auf die neue Invariante umstellen (genau 1× Lösch-Operation, ≥ 2× Funktionsname = Definition + Export, weiterhin 0× `clear`/`deleteDatabase`, Titel und Kommentarblock anpassen). `tests/game-scan-bridge.test.js` Zeile ~358 erwartet weiterhin 0× `idbSnapshotDelete` in game-scan.js — game-scan.js bleibt unverändert. `tests/delete-confirmation.test.js` (STAB-09) scannt nur items.js — unberührt.
3. **Sandbox-Fakten:** `makeSandbox` definiert KEIN `confirm`, kein `Blob`, kein `URL`, kein `navigator` — per `extraGlobals` injizieren (`loadScript([...], { confirm: fn, Blob: FakeBlob, URL: urls })`). `document.createElement` liefert Stubs; tests/screenshot-export.test.js Zeilen 23-43 zeigen `FakeBlob`, `urls = { createObjectURL: vi.fn(() => 'blob:test'), revokeObjectURL: vi.fn() }`, `created`-Sammlung der Anker und `ctx.showStatus = vi.fn()`. Op-Spy-Muster: tests/game-scan-bridge.test.js Zeilen 58-74 (`IDBObjectStore.prototype.add` wrappen, `store: this.name` filtern, `afterEach` restaurieren) — hier zusätzlich für die Lösch-Methode des Prototyps. fake-indexeddb ist pro Testdatei prozessweit: ids je Test eindeutig wählen (`'sd_' + Date.now() + '_' + n`), Zählungen relativ.
4. **Funktionsdeklarationen sind Sandbox-Properties:** `ctx.deleteGameSnapshot` ist nach `loadScript` erreichbar; `ctx.renderScanTab = vi.fn()` nach dem Laden ersetzt die (in diesem Plan noch nicht existierende) globale Funktion — der typeof-geguardete Aufruf in `deleteGameSnapshot` trifft dann den Mock.
5. **Export-Muster items.js `exportScreenshotsOnly` (Zeilen 7222-7256):** `payload` → `_jsonParts(payload)` → `new Blob(parts, { type: 'application/json' })` → `a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = …; document.body.appendChild(a); a.click(); document.body.removeChild(a); setTimeout(() => URL.revokeObjectURL(a.href), 60000)`; `_meta` mit `exportedAt`/`version`/`tool`/`counts`. `_jsonParts` (Zeile 6891) ist global aus items.js.
6. **Datensatz-Form (05-03):** `{ id: '<ts>_<reqId>' | Zahl, ts, gameVersion, modCount, mods: [{name, version}], sizeBytes, inventory }`; `inventory.globals.total`, `inventory.assets.count`, `inventory.assets.groupCount`, `inventory.modSdk.patchingCount` liefern die `_meta.counts` (bei fehlenden Feldern `null`).
7. **Datumsformat ohne locale-API (Konvention 04-04/05-03):** `new Date(ts).toISOString().slice(0, 16).replace('T', ' ') + ' UTC'`; kein `toLocaleString`.
8. **Baseline nach Plan 06-01:** `npm test` → 24 Dateien grün.
</context>

<tasks>

<!-- Comment-Text-Discipline-Allowlist (#429): Gate-Literale der negativen Greps dieses Plans -->
<!-- planner-discipline-allow: .delete( -->
<!-- planner-discipline-allow: .clear( -->
<!-- planner-discipline-allow: deleteDatabase -->
<!-- planner-discipline-allow: indexedDB.open( -->
<!-- planner-discipline-allow: idbSnapshotPut( -->
<!-- planner-discipline-allow: idbSet( -->
<!-- planner-discipline-allow: idbSnapshotDelete -->
<!-- planner-discipline-allow: postMessage( -->
<!-- planner-discipline-allow: addEventListener('message' -->
<!-- planner-discipline-allow: Promise.all( -->
<!-- planner-discipline-allow: .forEach( -->
<!-- planner-discipline-allow: afterEach -->
<!-- planner-discipline-allow: objectStore(_IDB_SNAPSHOTS).clear( -->
<!-- planner-discipline-allow: setInterval( -->
<!-- planner-discipline-allow: toLocaleString -->

<task type="auto">
  <name>Task 1: Lösch-/Export-Tests schreiben, persistence-module-Gates nachziehen, RED bestätigen — `tests/snapshot-delete.test.js`, `tests/scan-tab-export.test.js`, `tests/persistence-module.test.js` (SCAN-11, T-6-02/T-6-03/T-6-04)</name>
  <files>tests/snapshot-delete.test.js, tests/scan-tab-export.test.js, tests/persistence-module.test.js</files>
  <read_first>tests/screenshot-export.test.js (gesamt — `FakeBlob`, `boot`, `anchor`, `payloadOf`, Fehlerpfad), tests/game-scan-bridge.test.js Zeilen 1-80 und 317-376 (Op-Spy, `count`/`src`, statische Gates), tests/persistence-module.test.js Zeilen 30-50, 76-90, 160-170, 283-295 (die nachzuziehenden Erwartungen), tests/delete-confirmation.test.js Zeilen 130-182 (Audit-Stil mit Enclosing-Function), 02-02-SUMMARY.md (Guard-Reihenfolge); Task 2 dieses Plans (Bezeichner/Texte sind Vertrag)</read_first>
  <action>
    **A) `tests/snapshot-delete.test.js` (neu, ESM):** Imports `describe, it, expect, vi, beforeEach, afterEach` (vitest); `fs`, `path`; `loadScript, makeSandbox, loadInto, makeElementStub, evalIn, settle, REPO_ROOT` aus `./helpers/loadScript.js`. Kopfkommentar (Deutsch): SCAN-11 — Verhalten des einzigen Snapshot-Löschpfads plus NEUER statischer Audit; die STAB-09-Auditdatei ist items.js-/Property-Delete-spezifisch und deckt eine IDB-Store-Löschung in persistence.js strukturell nicht ab (RESEARCH Pitfall 4). Helfer: `src(file)`, `count(hay, needle)`; `let seq = 0; function rec(extra)` → `{ id: 'sd_' + Date.now() + '_' + (++seq), ts: Date.now() - 86400000 * seq, gameVersion: 'R131', modCount: 2, mods: [{ name: 'BCX', version: '1' }, { name: 'WCE', version: '6' }], sizeBytes: 10, inventory: { schema: 1, marker: 'm' + seq }, ...extra }`; Op-Spy auf die Lösch-Methode: `origDelete = globalThis.IDBObjectStore.prototype.delete` (Wert vor dem Patch sichern), `deletes = []`, `installDeleteSpy(impl)` ersetzt die Prototyp-Methode durch eine Funktion, die `{ store: this.name, key: args[0] }` in `deletes` pusht und dann `origDelete.apply(this, args)` aufruft (oder `impl` wirft, wenn übergeben), `afterEach` restauriert; `beforeEach` leert `deletes`. `async function boot({ confirm } = {})` → `extra = { setTimeout: () => 0, clearTimeout: () => {} }`; wenn `confirm !== undefined` → `extra.confirm = confirm`; `ctx = loadScript(['items.js', 'scan-tab.js'], extra)`; `ctx.showStatus = vi.fn()`; `ctx.renderScanTab = vi.fn(async () => {})`; `await settle(40)`; `return ctx`.

    **describe „deleteGameSnapshot (SCAN-11): Bestätigung ist Pflicht“**
    1. „confirm → false: genau ein confirm-Aufruf mit Datum, BC-Version und Mod-Anzahl im Text; keine Lösch-Operation; Datensatz bleibt; kein Status; Rückgabe false“: `confirm = vi.fn(() => false)`; `ctx = await boot({ confirm })`; `r = rec()`; `expect(await ctx.idbSnapshotPut(r)).toBe(true)`; `installDeleteSpy()`; `expect(await ctx.deleteGameSnapshot(r.id)).toBe(false)`; `expect(confirm).toHaveBeenCalledTimes(1)`; `msg = confirm.mock.calls[0][0]`; `msg` enthält `'R131'`, `'2 Mods'`, `' UTC'` und `'löschen'`; `deletes.filter(d => d.store === 'snapshots').length === 0`; `expect(await ctx.idbSnapshotGet(r.id)).toEqual(r)`; `ctx.showStatus` nicht aufgerufen; `ctx.renderScanTab` nicht aufgerufen.
    2. „confirm → true: genau eine Lösch-Operation auf snapshots mit der id; Datensatz weg; zweiter Datensatz unverändert (Kernwert); Status success; renderScanTab aufgerufen; Rückgabe true“: `ctx = await boot({ confirm: () => true })`; `a = rec(); b = rec()`; beide `idbSnapshotPut`; `installDeleteSpy()`; `expect(await ctx.deleteGameSnapshot(a.id)).toBe(true)`; `snap = deletes.filter(d => d.store === 'snapshots')`; `snap.length === 1` und `snap[0].key === a.id`; `expect(await ctx.idbSnapshotGet(a.id)).toBeNull()`; `expect(await ctx.idbSnapshotGet(b.id)).toEqual(b)`; `ctx.showStatus` `toHaveBeenCalledWith('✅ Snapshot gelöscht', 'success')`; `ctx.renderScanTab` `toHaveBeenCalledTimes(1)`.
    3. „unbekannte id: confirm wird NICHT aufgerufen (Existenz-Prüfung zuerst), Status info, keine Lösch-Operation, Rückgabe false“: `confirm = vi.fn(() => true)`; `installDeleteSpy()`; `expect(await ctx.deleteGameSnapshot('sd_gibt_es_nicht')).toBe(false)`; `confirm` nicht aufgerufen; `ctx.showStatus` `toHaveBeenCalledWith('⚠️ Snapshot nicht gefunden', 'info')`; 0 Lösch-Operationen.
    4. „ohne confirm-Global (Sandbox-Default): nichts wird gelöscht, kein Throw, Rückgabe false (fail-closed)“: `ctx = await boot()` (kein confirm); `r = rec()`; put; `installDeleteSpy()`; `await expect(ctx.deleteGameSnapshot(r.id)).resolves.toBe(false)`; 0 Lösch-Operationen; Datensatz bleibt; `ctx.showStatus` nicht mit `'success'` aufgerufen.
    5. „Fehlerpfad: Lösch-Operation wirft → idbSnapshotDelete false, Status error, Datensatz bleibt, kein Throw, kein renderScanTab“: `ctx = await boot({ confirm: () => true })`; `r = rec()`; put; `installDeleteSpy(() => { throw new Error('kaputt'); })`; `expect(await ctx.deleteGameSnapshot(r.id)).toBe(false)`; `ctx.showStatus` `toHaveBeenCalledWith('❌ Snapshot konnte nicht gelöscht werden', 'error')`; Spy restaurieren (`globalThis.IDBObjectStore.prototype.delete = origDelete`), dann `expect(await ctx.idbSnapshotGet(r.id)).toEqual(r)`; `ctx.renderScanTab` nicht aufgerufen.
    6. „idbSnapshotDelete direkt: ungültige id → false ohne IDB-Zugriff; gültige unbekannte id → true (IDB-Semantik), Store sonst unverändert; fehlende id-Argumente löschen nie einen Datensatz“: `ctx = await boot()`; `r = rec()`; put; `installDeleteSpy()`; für `undefined`, `null`, `{}`, `''`: `expect(await ctx.idbSnapshotDelete(v)).toBe(false)` und 0 Lösch-Operationen; `expect(await ctx.idbSnapshotDelete('sd_unbekannt_' + Date.now())).toBe(true)`; `expect(await ctx.idbSnapshotGet(r.id)).toEqual(r)`; `warnSpy = vi.spyOn(console, 'warn').mockImplementation(() => {})` um den Fehlerpfad (Spy wirft) einmal direkt zu prüfen: `installDeleteSpy(() => { throw new Error('x'); })` → `expect(await ctx.idbSnapshotDelete(r.id)).toBe(false)` und `warnSpy` mit einem Argument, das `'[IDB] snapshot delete'` enthält; `warnSpy.mockRestore()`.

    **describe „audit: genau eine Lösch-Operation, genau eine bestätigte Aufrufstelle (T-6-03)“**
    7. „persistence.js: die Lösch-Methode auf objectStore(_IDB_SNAPSHOTS) genau einmal, innerhalb von idbSnapshotDelete; keine clear-/deleteDatabase-Operation; Export enthält idbSnapshotDelete; id-Validierung vorhanden; Version 3“: `p = src('persistence.js')`; `count(p, 'objectStore(_IDB_SNAPSHOTS).delete(') === 1`; `fnStart = p.indexOf('async function idbSnapshotDelete(id)')` > -1; `fnEnd` = Index des nächsten `'\nasync function '` bzw. `'\nfunction '` nach `fnStart` (das kleinere, > -1); `body = p.slice(fnStart, fnEnd)`; `count(body, 'objectStore(_IDB_SNAPSHOTS).delete(') === 1`; `count(body, "'readwrite'") === 1`; `count(body, "console.warn('[IDB] snapshot delete:'") === 1`; `body` enthält `typeof id === 'number'` und `typeof id === 'string'`; `count(p, 'objectStore(_IDB_SNAPSHOTS).clear(') === 0`; `count(p, 'deleteDatabase') === 0`; `count(p, 'const _IDB_VERSION = 3;') === 1`; der Export-Schwanz (ab `if (typeof module !== 'undefined' && module.exports)`) enthält `idbSnapshotDelete`.
    8. „scan-tab.js: idbSnapshotDelete( genau einmal, innerhalb deleteGameSnapshot, nach confirm(, typeof-Guard auf confirm, keine Schleife um die Aufrufstelle; Datei ohne eigene IDB-/Bridge-/Schreiblogik“: `s = src('scan-tab.js')`; `count(s, 'idbSnapshotDelete(') === 1`; `fnStart = s.indexOf('async function deleteGameSnapshot(id)')` > -1; `body` bis zum nächsten `'\nasync function '`/`'\nfunction '`; `count(body, 'idbSnapshotDelete(') === 1`; `count(body, 'confirm(') === 1`; `body.indexOf('confirm(') < body.indexOf('idbSnapshotDelete(')`; `body` enthält `typeof confirm`; `body.indexOf('idbSnapshotGet(') < body.indexOf('confirm(')` (Existenz vor Bestätigung); `count(body, 'for (') + count(body, 'while (') + count(body, '.forEach(') + count(body, '.map(') + count(body, 'Promise.all(') === 0`; Datei-weit: `count(s, '.delete(') === 0`, `count(s, '.clear(') === 0`, `count(s, 'indexedDB.open(') === 0`, `count(s, 'idbSnapshotPut(') === 0`, `count(s, 'idbSet(') === 0`, `count(s, "addEventListener('message'") === 0`, `count(s, 'postMessage(') === 0`, `count(s, 'setInterval(') === 0`, `count(s, 'loadOrderFatal') === 1`, `s` enthält `'wurde nicht vor scan-tab.js geladen'`.
    9. „kein anderer Aufrufer: alle Root-JS-Dateien außer persistence.js/scan-tab.js und index.html enthalten idbSnapshotDelete nicht; tools/ und tests/ ausgenommen“: `files = fs.readdirSync(REPO_ROOT).filter(f => f.endsWith('.js') && f !== 'persistence.js' && f !== 'scan-tab.js')`; `files.length >= 12`; für jede: `count(src(f), 'idbSnapshotDelete') === 0`; `count(src('index.html'), 'idbSnapshotDelete') === 0`.
    10. „Ladereihenfolge-Guard: ohne items.js FATAL-Box + Throw; in Node (ohne window) lädt scan-tab.js ohne Throw und exportiert deleteGameSnapshot/exportGameSnapshot“: `sb = makeSandbox(); created = []; sb.document.createElement = tag => { const el = makeElementStub(); el.tagName = tag; created.push(el); return el; }`; `loadInto(sb, 'persistence.js')`; `expect(() => loadInto(sb, 'scan-tab.js')).toThrow(/items\.js/)`; Box `id === 'loadOrderFatal'`, Text beginnt mit `FATAL: `, enthält `scan-tab.js`, endet mit `(siehe docs/LOAD-ORDER.md)`. Node: `const { createRequire } = await import('node:module'); const require = createRequire(import.meta.url); const m = require('../scan-tab.js')`; `typeof m.deleteGameSnapshot === 'function'`; `typeof m.exportGameSnapshot === 'function'`; `typeof m._scanFormatTs === 'function'`; `m._scanFormatTs(0) === '1970-01-01 00:00 UTC'`.

    **B) `tests/scan-tab-export.test.js` (neu, ESM):** Muster tests/screenshot-export.test.js. `FakeBlob` wie dort; `async function boot()` → `urls = { createObjectURL: vi.fn(() => 'blob:test'), revokeObjectURL: vi.fn() }`, `created = []`, `ctx = loadScript(['items.js', 'scan-tab.js'], { Blob: FakeBlob, URL: urls, setTimeout: () => 0, clearTimeout: () => {} })`, `ctx.document.createElement` sammelt Anker (`el.click = vi.fn()`), `ctx.showStatus = vi.fn()`, `await settle(40)`, `return { ctx, urls, created }`; `rec()` wie in A) mit Präfix `'ex_'` und `inventory: { schema: 1, gameVersion: 'R131', globals: { total: 5, getters: [], functions: [], values: [] }, assets: { count: 3, groupCount: 2, groups: [], items: [] }, modSdk: { available: true, patchingCount: 4, patching: [] }, mods: [], probes: {}, chatHooks: {}, errors: [] }`; `lastBlob(urls)`, `payloadOf(urls)`, `anchor(created)` wie im Muster. Op-Spy auf `add`, `put` und die Lösch-Methode von `IDBObjectStore.prototype` (`ops` mit `store`), `afterEach` restauriert.
    11. „exportiert genau den gespeicherten Datensatz unter snapshot; _meta mit version 1, tool nennt Snapshot, exportedAt ISO, snapshotId, counts aus dem Inventar“: put `r`; `await ctx.exportGameSnapshot(r.id)`; `urls.createObjectURL` 1×; `lastBlob(urls).type === 'application/json'`; `p = payloadOf(urls)`; `Object.keys(p).sort()` `toEqual(['_meta', 'snapshot'])`; `p.snapshot` `toEqual(r)`; `p._meta.version === 1`; `p._meta.tool` `toMatch(/Snapshot/)`; `Number.isNaN(Date.parse(p._meta.exportedAt)) === false`; `p._meta.snapshotId === r.id`; `p._meta.counts` `toEqual({ globals: 5, assets: 3, groups: 2, patching: 4, mods: 2 })`.
    12. „Download-Anker: Dateiname BC_Snapshot_<YYYY-MM-DD>_<gameVersion>_<id>.json, click 1×, href aus createObjectURL, Status success mit KB“: `a = anchor(created)`; `a.download` `toMatch(new RegExp('^BC_Snapshot_\\d{4}-\\d{2}-\\d{2}_R131_' + r.id + '\\.json$'))`; `a.href === 'blob:test'`; `a.click` 1×; letzter `showStatus`-Aufruf: `[1] === 'success'`, `[0]` enthält `'exportiert'` und `' KB'`.
    13. „unbekannte id: kein Download, showStatus info „nicht gefunden““: `await ctx.exportGameSnapshot('ex_nicht_da')`; `urls.createObjectURL` nicht aufgerufen; `anchor(created)` undefined; `ctx.showStatus` `toHaveBeenLastCalledWith('⚠️ Snapshot nicht gefunden', 'info')`.
    14. „Fehlerpfad: createObjectURL wirft → showStatus error mit Fehlertext, kein Throw“: `urls.createObjectURL = vi.fn(() => { throw new Error('boom'); })`; `await expect(ctx.exportGameSnapshot(r.id)).resolves.not.toThrow()` bzw. `await ctx.exportGameSnapshot(r.id)` in `expect(async () => …).not.toThrow()`-Form; letzter Status `'error'`, Text enthält `'Snapshot-Export fehlgeschlagen'` und `'boom'`.
    15. „mutiert nichts: Datensatz vor/nach identisch; keine add/put/delete-Operation auf snapshots während des Exports (Op-Spy)“: put; Spy installieren; export; `ops.filter(o => o.store === 'snapshots').length === 0`; `expect(await ctx.idbSnapshotGet(r.id)).toEqual(r)`.
    16. „Sonderzeichen in gameVersion werden im Dateinamen ersetzt; fehlende gameVersion → unbekannt“: put `rec({ gameVersion: 'R131 beta/1' })` → `a.download` enthält `'_R131_beta_1_'` und keinen Schrägstrich/Leerzeichen; put `rec({ gameVersion: null })` → `a.download` enthält `'_unbekannt_'`.
    17. „statisch: exportGameSnapshot nutzt _jsonParts + Blob + Anker wie exportScreenshotsOnly; kein Schreibpfad in scan-tab.js“: `s = src('scan-tab.js')`; `fnStart = s.indexOf('async function exportGameSnapshot(id)')` > -1; `body` bis zur nächsten Top-Level-Funktion; `body` enthält `'_jsonParts(payload)'`, `"new Blob("`, `"'application/json'"`, `'URL.createObjectURL('`, `'.download = '`, `'.click()'`, `'revokeObjectURL('`, `'idbSnapshotGet('`; `count(body, 'idbSnapshotDelete') === 0`; `count(s, 'idbSnapshotPut(') === 0`; `count(s, 'idbSet(') === 0`; `count(s, 'toLocaleString') === 0`.

    **C) `tests/persistence-module.test.js` (Nachzug):** an beiden Export-Listen (Zeilen ~42-47 und ~82-87) `'idbSnapshotDelete',` direkt vor `'idbSnapshotGet'` einfügen (Sortierung: `'idbSet' < 'idbSnapshotDelete' < 'idbSnapshotGet'`). Test „statisch: keine Lösch-API für Snapshots, Version 3, Store additiv, add statt put“ umbenennen in „statisch: Lösch-API nur idbSnapshotDelete (SCAN-11), Version 3, Store additiv, add statt put“ und die drei Erwartungen ändern: Lösch-Operation auf `_IDB_SNAPSHOTS` `toBe(1)` (statt 0); `count(persistenceSrc, 'idbSnapshotDelete')` `toBeGreaterThanOrEqual(2)` (statt 0); `count(persistenceSrc, 'async function idbSnapshotDelete(id)') === 1` neu; `clear`/`deleteDatabase` bleiben 0. Den Kommentarblock über dem describe (Zeilen ~160-165) anpassen: „Löschen ausschließlich über idbSnapshotDelete hinter Bestätigung (Phase 6, SCAN-11; Verhalten + Audit in tests/snapshot-delete.test.js)“. Nichts sonst ändern.

    **RED bestätigen:** `npx vitest run tests/snapshot-delete.test.js tests/scan-tab-export.test.js tests/persistence-module.test.js` → die zwei neuen Dateien rot (`ENOENT … scan-tab.js` bzw. `idbSnapshotDelete is not a function`), persistence-module rot an den drei geänderten Stellen. Commit (nur diese drei Pfade): `git add tests/snapshot-delete.test.js tests/scan-tab-export.test.js tests/persistence-module.test.js` → `test(06-02): Snapshot-Löschen mit Bestätigung, statischer Audit, Snapshot-Export; persistence-module-Gates nachgezogen — RED`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/snapshot-delete.test.js tests/scan-tab-export.test.js tests/persistence-module.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "scan-tab\.js|idbSnapshotDelete|deleteGameSnapshot|exportGameSnapshot" "$OUT" && grep -Eq "Test Files +[0-9]+ failed" "$OUT" && test ! -e scan-tab.js && git diff --quiet HEAD -- persistence.js items.js game-scan.js index.html && FILES=$(git show --name-only --format= HEAD) && test "$(printf '%s\n' "$FILES" | sort | tr '\n' ' ')" = "tests/persistence-module.test.js tests/scan-tab-export.test.js tests/snapshot-delete.test.js " && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Exit 0, keine `failed`-Zeile, die Ausgabe nennt keinen der Bezeichner, scan-tab.js existiert bereits, persistence.js/items.js/game-scan.js/index.html wurden schon geändert, oder der Commit enthält andere Pfade als die drei Testdateien</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" tests/snapshot-delete.test.js` >= 10; `grep -c "it(" tests/scan-tab-export.test.js` >= 7; `grep -c "readdirSync(REPO_ROOT)" tests/snapshot-delete.test.js` = 1; `grep -c "typeof confirm" tests/snapshot-delete.test.js` >= 1; `grep -c "Promise.all(" tests/snapshot-delete.test.js` >= 1 (Schleifen-Gate)
    - `grep -n "'idbSnapshotDelete'" tests/persistence-module.test.js` zeigt zwei Zeilen (je eine pro Export-Liste); `grep -c "async function idbSnapshotDelete(id)" tests/persistence-module.test.js` = 1; `grep -c "statisch: Lösch-API nur idbSnapshotDelete" tests/persistence-module.test.js` = 1
    - `grep -c "\.only(" tests/snapshot-delete.test.js tests/scan-tab-export.test.js | awk -F: '{s+=$2} END {print s}'` = 0; `afterEach` restauriert jede gepatchte Prototyp-Methode (`grep -c "afterEach" tests/snapshot-delete.test.js` >= 1, dito export)
    - Rest der Suite unverändert grün: `npx vitest run --exclude tests/snapshot-delete.test.js --exclude tests/scan-tab-export.test.js --exclude tests/persistence-module.test.js` → keine `failed`-Zeile
  </acceptance_criteria>
  <done>17 neue Fälle (10 + 7) plus drei nachgezogene Erwartungen; Lauf nachweislich rot; nur Testdateien committet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `idbSnapshotDelete` (persistence.js) + `scan-tab.js` Teil 1 (Guard, `deleteGameSnapshot`, `exportGameSnapshot`) — GREEN (SCAN-11)</name>
  <files>persistence.js, scan-tab.js</files>
  <behavior>
    - confirm false / fehlend → nichts gelöscht, kein Status; confirm true → genau eine Lösch-Operation, Nachbar-Datensatz bleibt (Tests 1-5)
    - `idbSnapshotDelete`: id-Validierung, Fehler → warn + false (Test 6); statische Audits (Tests 7-9); Guard + Node-Ladbarkeit (Test 10)
    - Export: Payload `{_meta, snapshot}`, Dateiname, Anker, Fehlerpfade, Mutationsfreiheit (Tests 11-17)
  </behavior>
  <read_first>tests/snapshot-delete.test.js und tests/scan-tab-export.test.js (Task 1 — jede Erwartung ist Vertrag); persistence.js Zeilen 115-148 (`idbScreenshotBatch`/`idbScreenshotDelete`), 177-248 (Snapshot-Sektion), 366-375 (Export-Schwanz); game-scan.js Zeilen 1-25 (Guard) und 40-48 (`_gameScanFormatTs`, `_gameScanKb`); items.js Zeilen 7222-7256 (`exportScreenshotsOnly`) und 6504-6520 (`escHtml`/`escJsAttr`)</read_first>
  <action>
    **persistence.js:** In der Snapshot-Sektion nach `idbSnapshotKeys` die Funktion `async function idbSnapshotDelete(id)` ergänzen: id-Validierung wie in `idbSnapshotPut` (Zahl oder nicht-leerer String, sonst sofort `false` — ohne `_idbOpen`); dann `db = await _idbOpen()`, eine `readwrite`-Transaktion auf `_IDB_SNAPSHOTS`, der Store-Zugriff `objectStore(_IDB_SNAPSHOTS)` mit der Lösch-Methode und `id` (genau diese eine Aufrufstelle im ganzen Repo — der Audit zählt das Literal aus den Acceptance-Criteria), Aufruf in `try/catch` innerhalb des Promise-Executors (bei Throw `tx.abort()` in try/catch und `reject`), `tx.oncomplete → resolve`, `tx.onerror → reject(e.target.error)`, `tx.onabort → reject(tx.error || e.target.error)`; außen `catch (err) { console.warn('[IDB] snapshot delete:', err); return false; }`; Erfolg `return true`. Den Sektions-Kopfkommentar aktualisieren: Löschen eines einzelnen Snapshots ausschließlich über `idbSnapshotDelete`, das nur aus dem bestätigten UI-Pfad `deleteGameSnapshot` (scan-tab.js) aufgerufen wird; keine Leer-/Sammellösch-Funktion (Kernwert) — im Kommentar den Funktionsnamen nennen ist erlaubt (Gate zählt ≥ 2), aber NICHT das Store-Lösch-Literal. Export-Schwanz: `idbSnapshotDelete` in die `idbSnapshot*`-Zeile aufnehmen. `_IDB_VERSION` bleibt 3, kein `onupgradeneeded`-Eingriff.

    **scan-tab.js (neu, Teil 1 — Plan 06-03 hängt das Rendering an):** Kopfkommentar (Deutsch, Sektionsstil wie game-scan.js): scan-tab.js — Scan-Tab (SCAN-10/11/12): Snapshot-Browser, Löschen nur mit Bestätigung, Export; lädt NACH persistence.js, items.js, game-scan.js und baseline-manifest.js (docs/LOAD-ORDER.md); nicht in `CORE_SCRIPTS` (Blatt-Modul, Tests laden es explizit); keine eigene Bridge-/IDB-Logik; Node-tauglich für tools/analyze-snapshot.js (Dual-Export). Ladereihenfolge-Guard als IIFE, erste Anweisung: `if (typeof window === 'undefined') return;` (Node/require — Guard entfällt, DOM wird nie zur Ladezeit berührt), dann `required = [['idbSnapshotGetAll', 'persistence.js'], ['idbSnapshotGet', 'persistence.js'], ['idbSnapshotDelete', 'persistence.js'], ['showStatus', 'items.js'], ['escHtml', 'items.js'], ['escJsAttr', 'items.js'], ['_jsonParts', 'items.js']]` und derselbe Box-/Throw-Pfad wie game-scan.js mit Text `FATAL: <Module> wurde nicht vor scan-tab.js geladen – Ladereihenfolge in index.html prüfen (siehe docs/LOAD-ORDER.md)` und `console.error('[BCK-Popup] ' + msg)`. Helfer: `_scanFormatTs(ts)` = `new Date(ts).toISOString().slice(0, 16).replace('T', ' ') + ' UTC'`; `_scanKb(bytes)` = `Math.round((bytes || 0) / 1024)`; `_scanSafeName(s)` = `String(s ?? '').replace(/[^\w.-]/g, '_')`; `_scanExportName(rec)` = `'BC_Snapshot_' + new Date(rec.ts).toISOString().slice(0, 10) + '_' + (rec.gameVersion ? _scanSafeName(rec.gameVersion) : 'unbekannt') + '_' + _scanSafeName(rec.id) + '.json'`.
    `async function deleteGameSnapshot(id)`: `rec = await idbSnapshotGet(id)`; fehlt → `showStatus('⚠️ Snapshot nicht gefunden', 'info'); return false`; `if (typeof confirm !== 'function') return false;` (fail-closed: ohne Dialog kein Löschen); `if (!confirm('Snapshot vom ' + _scanFormatTs(rec.ts) + ' (BC ' + (rec.gameVersion || '?') + ', ' + rec.modCount + ' Mods) wirklich löschen? Das kann nicht rückgängig gemacht werden.')) return false;`; `ok = await idbSnapshotDelete(id)`; `!ok` → `showStatus('❌ Snapshot konnte nicht gelöscht werden', 'error'); return false`; sonst `showStatus('✅ Snapshot gelöscht', 'success')`, `if (typeof renderScanTab === 'function') await renderScanTab();`, `return true`. Keine Schleife, kein `Promise.all`, genau eine `confirm(`- und eine `idbSnapshotDelete(`-Stelle.
    `async function exportGameSnapshot(id)`: `rec = await idbSnapshotGet(id)`; fehlt → `showStatus('⚠️ Snapshot nicht gefunden', 'info'); return false`; in `try`: `inv = rec.inventory || {}`; `payload = { _meta: { version: 1, tool: 'BC Konfigurator – Snapshot-Export', exportedAt: new Date().toISOString(), snapshotId: rec.id, counts: { globals: inv.globals?.total ?? null, assets: inv.assets?.count ?? null, groups: inv.assets?.groupCount ?? null, patching: inv.modSdk?.patchingCount ?? null, mods: rec.modCount ?? null } }, snapshot: rec }`; `parts = _jsonParts(payload)`; `blob = new Blob(parts, { type: 'application/json' })`; Anker wie `exportScreenshotsOnly` (`document.createElement('a')`, `href = URL.createObjectURL(blob)`, `download = _scanExportName(rec)`, `document.body.appendChild`, `click()`, `removeChild`, `setTimeout(() => URL.revokeObjectURL(a.href), 60000)`); `showStatus('✅ Snapshot exportiert – ' + _scanKb(blob.size ?? rec.sizeBytes) + ' KB', 'success')`; `return true`; `catch (e) { showStatus('❌ Snapshot-Export fehlgeschlagen: ' + (e?.message || e), 'error'); return false; }`. Nur Lesezugriffe.
    Dual-Export-Schwanz (Muster persistence.js): `if (typeof module !== 'undefined' && module.exports) { module.exports = { deleteGameSnapshot, exportGameSnapshot, _scanFormatTs, _scanKb, _scanSafeName, _scanExportName }; }` — Plan 06-03 erweitert diese Liste. Kein Init-Hook in diesem Plan; kein `setInterval`; kein `toLocaleString`; keine Kommentare mit den Gate-Literalen (Store-Lösch-Aufruf, clear, indexedDB.open, Schreibpfade).
    **GREEN:** `node --check persistence.js scan-tab.js`; `npx vitest run tests/snapshot-delete.test.js tests/scan-tab-export.test.js tests/persistence-module.test.js tests/game-scan-bridge.test.js tests/screenshot-store.test.js tests/delete-confirmation.test.js` → grün; `npm test` → 26 Dateien grün. Commit (nur diese zwei Pfade): `git add persistence.js scan-tab.js` → `feat(06-02): idbSnapshotDelete (einzige Lösch-Operation) + scan-tab.js: deleteGameSnapshot hinter confirm, exportGameSnapshot — GREEN`.
    Fürs SUMMARY: RED-/GREEN-Ausgaben, die Guard-Reihenfolge des Löschpfads, der Hinweis, dass scan-tab.js erst in 06-03 in index.html geladen wird, und der Export-Dateiname.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && node --check persistence.js && node --check scan-tab.js && npx vitest run tests/snapshot-delete.test.js tests/scan-tab-export.test.js tests/persistence-module.test.js tests/game-scan-bridge.test.js tests/screenshot-store.test.js tests/delete-confirmation.test.js tests/baseline-manifest.test.js && OUT=$(mktemp) && npm test > "$OUT" 2>&1; grep -Eq "Test Files +26 passed" "$OUT" && ! grep -Eq "Tests +[0-9]+ failed|Test Files +[0-9]+ failed" "$OUT" && echo GREEN-OK</automated>
    <fails_when>`GREEN-OK` fehlt — Syntaxfehler, ein Fall der sieben Dateien `failed` (z. B. Löschen ohne confirm, zweite Lösch-Operation, Export mutiert, Guard fehlt), oder die volle Suite zeigt weniger als 26 Dateien bzw. eine `failed`-Zeile</fails_when>
  </verify>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(grep -c 'objectStore(_IDB_SNAPSHOTS).delete(' persistence.js)" = 1 && test "$(grep -c 'objectStore(_IDB_SNAPSHOTS).clear(' persistence.js)" = 0 && test "$(grep -c 'deleteDatabase' persistence.js)" = 0 && test "$(grep -c 'async function idbSnapshotDelete(id)' persistence.js)" = 1 && test "$(grep -Ec 'const _IDB_VERSION += +3;' persistence.js)" = 1 && test "$(grep -c 'idbSnapshotDelete(' scan-tab.js)" = 1 && test "$(grep -c 'confirm(' scan-tab.js)" = 1 && test "$(grep -c 'typeof confirm' scan-tab.js)" = 1 && test "$(grep -c '\.delete(' scan-tab.js)" = 0 && test "$(grep -c '\.clear(' scan-tab.js)" = 0 && test "$(grep -c 'indexedDB.open(' scan-tab.js)" = 0 && test "$(grep -c 'idbSnapshotPut(' scan-tab.js)" = 0 && test "$(grep -c 'idbSet(' scan-tab.js)" = 0 && test "$(grep -c 'postMessage(' scan-tab.js)" = 0 && test "$(grep -c "addEventListener('message'" scan-tab.js)" = 0 && test "$(grep -c 'setInterval(' scan-tab.js)" = 0 && test "$(grep -c 'toLocaleString' scan-tab.js)" = 0 && test "$(grep -c 'idbSnapshotDelete' index.html items.js game-scan.js bridge.js bot-ui.js bot-engine.js | awk -F: '{s+=$2} END {print s}')" = 0 && git diff --quiet 49e4365 -- items.js game-scan.js index.html loader.js bridge.js && FILES=$(git show --name-only --format= HEAD) && test "$(printf '%s\n' "$FILES" | sort | tr '\n' ' ')" = "persistence.js scan-tab.js " && echo DELETE-AUDIT-OK</automated>
    <fails_when>`DELETE-AUDIT-OK` fehlt — mehr oder weniger als eine Lösch-Operation auf dem Snapshot-Store, eine clear-/deleteDatabase-Operation, mehr als eine Aufrufstelle oder confirm-Stelle in scan-tab.js, eine eigene IDB-/Bridge-/Schreib-Operation in scan-tab.js, ein Vorkommen des Funktionsnamens in index.html/items.js/game-scan.js/bridge.js/bot-*.js, eine Änderung an items.js/game-scan.js/index.html/loader.js/bridge.js gegenüber `49e4365`, oder der Commit enthält andere Pfade als persistence.js und scan-tab.js</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "if (typeof window === 'undefined') return;" scan-tab.js` = 1; `grep -c "wurde nicht vor scan-tab.js geladen" scan-tab.js` = 1; `grep -c 'loadOrderFatal' scan-tab.js` = 1
    - `grep -c 'async function deleteGameSnapshot(id)' scan-tab.js` = 1; `grep -c 'async function exportGameSnapshot(id)' scan-tab.js` = 1; `grep -c '_jsonParts(payload)' scan-tab.js` = 1; `grep -c "if (typeof module !== 'undefined' && module.exports)" scan-tab.js` = 1
    - `grep -c "console.warn('\[IDB\] snapshot delete:'" persistence.js` = 1; `grep -c 'idbSnapshotDelete' persistence.js` >= 2
    - `D=$(git diff 49e4365 -- persistence.js) && test "$(printf '%s\n' "$D" | grep -c '^-[^-]')" -le 4` (nur Kommentar-/Export-Zeilen ersetzt; die bestehenden Funktionen bleiben byte-identisch)
  </acceptance_criteria>
  <done>Genau eine Lösch-Operation im Repo, genau eine bestätigte Aufrufstelle; Löschen ohne Bestätigung unmöglich; Export liefert einen Datensatz als JSON; 26 Dateien grün; Commit exakt zwei Pfade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI (Klick 🗑) → `deleteGameSnapshot` → `idbSnapshotDelete` → IndexedDB `snapshots` | Einziger Pfad, auf dem gescannte Daten verschwinden dürfen — nur manuell, nur bestätigt |
| `exportGameSnapshot` → Blob-Download | Daten verlassen IndexedDB nur als vom Nutzer ausgelöste Datei; lokal, kein Netzwerk |
| Sandbox/Node-Laden von scan-tab.js | Modul darf ohne DOM laden (tools/analyze-snapshot.js), ohne dabei Lösch-/Schreibpfade zu berühren |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-6-02 | Tampering | Snapshot wird ohne Bestätigung gelöscht (Kernwert) | critical | mitigate | `deleteGameSnapshot`: Existenz → genau ein `confirm()` → Löschen; `confirm` false/fehlend → nichts (Tests 1, 4); statischer Nachweis `confirm(` vor `idbSnapshotDelete(` (Test 8); kein Init-/Timer-Pfad (0× `setInterval`, kein Init-Hook in diesem Plan) |
| T-6-03 | Tampering | Zweite Lösch-Stelle schleicht sich ein (Store-Operation oder API-Aufruf) | high | mitigate | Audit: Store-Lösch-Literal genau 1× in persistence.js innerhalb `idbSnapshotDelete`; `idbSnapshotDelete(` genau 1× in scan-tab.js innerhalb `deleteGameSnapshot`; 0× in index.html und jeder anderen Root-JS-Datei (readdir-Gate, Test 9); persistence-module-Gate nachgezogen |
| T-6-04 | Information Disclosure | Export gibt mehr preis als die eigenen Scan-Daten | low | accept | Payload = genau ein Datensatz (`toEqual`, Test 11) plus `_meta`-Zähler; Einzelnutzer-Tool, keine Fremddaten, kein Netzwerkpfad; Datei landet nur über den Browser-Download beim Nutzer |
| T-6-10 | Denial of Service | Lösch-/Exportfehler bleiben still (Nutzer glaubt, es sei gelöscht/exportiert) | medium | mitigate | `idbSnapshotDelete` → `false` + `console.warn`; `deleteGameSnapshot` → `showStatus(error)` (Test 5); Export-Fehler → `showStatus(error)` ohne Throw (Test 14); Erfolgs-Status nur auf dem Erfolgspfad |
| T-6-11 | Elevation of Privilege | Sammellöschung („alle löschen“) über eine Schleife um die eine Aufrufstelle | high | mitigate | Region-Gate: 0× `for (`/`while (`/`.forEach(`/`.map(`/`Promise.all(` im Funktionskörper von `deleteGameSnapshot` (Test 8); kein Delete-All-Button (Plan 06-03 hält index.html frei von `idbSnapshotDelete`) |
| T-6-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in diesem Plan |
</threat_model>

<verification>
- `npx vitest run tests/snapshot-delete.test.js tests/scan-tab-export.test.js tests/persistence-module.test.js` → grün; `npm test` → 26 Dateien grün (2 expected fail unverändert).
- Audit-Gates (zweiter `<automated>`-Block Task 2): eine Lösch-Operation, eine bestätigte Aufrufstelle, keine Fremdaufrufer, unveränderte Nachbardateien.
- Zwei Commits: test → feat; Commit-Scope-Gates exakt.
- VALIDATION.md 6-02-01/6-02-02 ✅ nach GREEN.
</verification>

<success_criteria>
- SCAN-11: Snapshots lassen sich nur manuell über `deleteGameSnapshot` und nur nach Bestätigung löschen; Abbruch lässt alles erhalten; kein „alle löschen“.
- Der Store `snapshots` hat genau eine Lösch-Operation im Repo, statisch bewiesen.
- Export eines einzelnen Snapshots als JSON funktioniert (Grundlage für Plan 06-04).
</success_criteria>

## Artifacts this phase produces

Siehe die phasenweite Liste in `06-01-PLAN.md` („Artifacts this phase produces“). Dieser Plan (06-02) liefert: `scan-tab.js` (Teil 1: Guard, `deleteGameSnapshot`, `exportGameSnapshot`, Helfer, Dual-Export), `tests/snapshot-delete.test.js`, `tests/scan-tab-export.test.js`; ändert `persistence.js` (`idbSnapshotDelete`, Kommentar, Export) und `tests/persistence-module.test.js` (Nachzug).

<output>
Create `.planning/phases/06-scan-tab-analyse/06-02-SUMMARY.md` when done — mit RED-/GREEN-Ausgaben, der Guard-Reihenfolge des Löschpfads, dem Export-Dateinamen-Schema, dem Hinweis auf die Verdrahtung in 06-03 und der Commit-Liste.
</output>
