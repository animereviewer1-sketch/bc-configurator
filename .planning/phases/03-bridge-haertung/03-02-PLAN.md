---
phase: 03-bridge-haertung
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/injected-code-origin.test.js
  - tests/loader-origin.test.js
  - tests/bot-engine-escaping.test.js
  - loader.js
  - items.js
  - bot-engine.js
  - bot-ui.js
autonomous: true
requirements: [STAB-05, STAB-06]
tags: [bridge, postmessage, origin, injected-code, loader, red-green]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "STAB-05: Jede der 36 injizierten `postMessage`-Stellen (17× items.js, 18× bot-engine.js, 1× bot-ui.js) sendet an den Tool-Origin statt an `\"*\"` — der Wert wird im Tool-Fenster aus `window.location.origin` berechnet und als String-Literal in die generierte Code-Zeichenkette eingebettet (Orchestrator-Entscheidung 2; NIE `new URL(popupRef.location)` — SecurityError, RESEARCH Pitfall 1). Behavioral bewiesen für `debugOsOutfit` (items.js), `bcKeys` (bot-ui.js) und `_buildBotCode` (bot-engine.js: `const _TOOL_ORIGIN=\"https://tool.test\";` genau 1× am Kopf des generierten Codes, `,_TOOL_ORIGIN)` 18×), statisch für alle 36"
    - "STAB-06 (eine Konstante): Auf der Tool-Seite existiert genau EINE Definition `const TOOL_ORIGIN = window.location.origin;` (items.js, neben `APP`); bot-engine.js und bot-ui.js konsumieren das Global; `location.origin` kommt in items.js/bot-engine.js/bot-ui.js zusammen genau 1× vor; der GitHub-Pages-Origin-String kommt in diesen drei Dateien 0× vor. In loader.js kommt er genau 1× vor (`POPUP_URL`), `ALLOWED_ORIGIN = new URL(POPUP_URL).origin` ist daraus abgeleitet (Orchestrator-Entscheidung 3)"
    - "STAB-06 (Loader-Hälfte): loader.js prüft weiterhin `ev.origin !== ALLOWED_ORIGIN` und zusätzlich die Quelle: `window.__BCK_popupRef` wird beim ersten Message bzw. bei jedem `PING` gepinnt; jede andere Nachricht mit `ev.source !== window.__BCK_popupRef` wird mit `BCK.warn` verworfen. Änderung minimal (+5/-1 Zeilen, kein Refactor) — der Loader läuft im Live-Spiel-Tab"
    - "Nach dem Deploy muss der Nutzer das Bookmarklet einmal neu ausführen (lädt loader.js neu) und das Tool-Fenster neu laden; alter Loader + neues Tool und neuer Loader + altes Tool bleiben kompatibel (gleicher `ALLOWED_ORIGIN`-Wert, gleiche Nachrichtentypen)"
    - "Annahme: Der Tool-Origin enthält nie Anführungszeichen oder Backslashes (Origin-Serialisierung ist `scheme://host[:port]`), daher ist die Einbettung in items.js/bot-ui.js per String-Konkatenation in Anführungszeichen sicher; in bot-engine.js wird trotzdem `JSON.stringify` verwendet (Escaping-Disziplin aus TEST-06). Lokal (`http://localhost:5500`) passt sich der Wert automatisch an — kein DEV-Flag nötig"
    - "Vor der Implementierung sind `tests/injected-code-origin.test.js` und `tests/loader-origin.test.js` nachweislich rot (36 Wildcards, `TOOL_ORIGIN` fehlt, Origin-String 2× in loader.js), danach grün; drei Commits: test RED → fix loader → fix Tool-Seite; die bestehende Escaping-Suite bleibt grün"
  artifacts:
    - path: "tests/injected-code-origin.test.js"
      provides: "STAB-05/06 Tool-Seite: 4 behaviorale Fälle (TOOL_ORIGIN-Wert, debugOsOutfit, bcKeys, _buildBotCode) + statischer Audit (Wildcard-Zählung pro Datei, einzige Definition, kein Origin-String)"
      contains: "_TOOL_ORIGIN"
    - path: "tests/loader-origin.test.js"
      provides: "STAB-06 Loader-Seite (statisch): Origin-String 1× (POPUP_URL), `ALLOWED_ORIGIN` abgeleitet, Source-Pinning-Guard vor dem Pin, 0 Wildcards"
      contains: "new URL(POPUP_URL).origin"
    - path: "tests/bot-engine-escaping.test.js"
      provides: "Minimal-Sandbox liefert `TOOL_ORIGIN: 'https://tool.test'` (bot-engine.js konsumiert das Global aus items.js)"
      contains: "TOOL_ORIGIN: 'https://tool.test'"
    - path: "items.js"
      provides: "`const TOOL_ORIGIN = window.location.origin;` + 17 Stellen `\"' + TOOL_ORIGIN + '\"`"
      contains: "const TOOL_ORIGIN = window.location.origin;"
    - path: "bot-engine.js"
      provides: "Kopfzeile `const _TOOL_ORIGIN=${JSON.stringify(TOOL_ORIGIN)};` im generierten IIFE + 18 Stellen `,_TOOL_ORIGIN)`"
      contains: "JSON.stringify(TOOL_ORIGIN)"
    - path: "bot-ui.js"
      provides: "1 Stelle in `bcKeys()` → `'\" + TOOL_ORIGIN + \"'`"
      contains: "TOOL_ORIGIN"
    - path: "loader.js"
      provides: "`const ALLOWED_ORIGIN = new URL(POPUP_URL).origin;` + Source-Pinning-Guard im `message`-Listener"
      contains: "src !== window.__BCK_popupRef"
  key_links:
    - from: "bot-engine.js `_buildBotCode` (Template-Literal ab `return \\`(function(){`)"
      to: "items.js `const TOOL_ORIGIN`"
      via: "Globales `const` eines klassischen Scripts (items.js wird per document.write vor bot-engine.js geladen); `${JSON.stringify(TOOL_ORIGIN)}` wird zur Generierungszeit im Tool-Fenster aufgelöst"
      pattern: "JSON\\.stringify\\(TOOL_ORIGIN\\)"
    - from: "generierter Bot-Code (läuft im Spiel-Tab)"
      to: "Tool-Fenster (`window.__BCK_popupRef.postMessage(..., _TOOL_ORIGIN)`)"
      via: "`_TOOL_ORIGIN` ist eine `const` am Kopf des generierten IIFE, lexikalisch in allen 18 verschachtelten Funktionen sichtbar"
      pattern: ",_TOOL_ORIGIN\\)"
    - from: "loader.js `message`-Listener"
      to: "loader.js `window.__BCK_popupRef`"
      via: "Pin beim ersten Message/PING, Vergleich `src !== window.__BCK_popupRef` für alle anderen Typen — dieselbe Referenz, die der injizierte Code zum Zurücksenden nutzt"
      pattern: "src !== window\\.__BCK_popupRef"
    - from: "tests/injected-code-origin.test.js"
      to: "items.js/bot-ui.js/bot-engine.js Generatoren"
      via: "`loadScript(['items.js','bot-data.js','bot-ui.js','bot-engine.js'], { opener })` (Planer-Probe: lädt fehlerfrei); gesendeter EXEC-Code wird aus `opener.postMessage.mock.calls` gelesen"
      pattern: "bot-engine.js"
  prohibitions:
    - requirement_id: STAB-05
      category: safety
      verification: automated
      statement: "Kein `\"*\"`/`'*'` mehr als postMessage-Ziel in items.js (außer den 2 Bootstrap-PINGs), bot-engine.js, bot-ui.js, loader.js; keine Laufzeit-Berechnung des Tool-Origins im Spiel-Tab (kein `popupRef.location`)"
    - requirement_id: STAB-06
      category: safety
      verification: automated
      statement: "Genau eine Tool-seitige Origin-Definition (`location.origin` 1× über items.js/bot-engine.js/bot-ui.js); der GitHub-Pages-Origin-String steht nur noch in loader.js `POPUP_URL`"
    - requirement_id: STAB-06
      category: safety
      verification: automated
      statement: "loader.js-Diff minimal: ≤ 8 Insertions, ≤ 2 Deletions; kein Refactor, keine Umbenennung, `ALLOWED_ORIGIN`-Verwendungen (Zeilenzahl 34) unverändert"
    - requirement_id: STAB-05
      category: safety
      verification: judgment
      statement: "Nur die 36 enumerierten Stellen und die Konstanten-Zeilen ändern — keine Umformatierung, keine Änderung an bcSend-Aufrufstellen, kein Build-Schritt; index.html unberührt"
    - requirement_id: STAB-05
      category: safety
      verification: judgment
      statement: "`git add` nur mit explizit genannten Pfaden; RED-Tests, loader.js-Fix und Tool-Seiten-Fix in drei getrennten Commits; keine Watch-Flags"
---

<objective>
Wildcard-Ziele im injizierten Code beseitigen und die Loader-Hälfte der Absender-Doppelprüfung schließen. Tool-Seite: genau eine Konstante `TOOL_ORIGIN = window.location.origin` in items.js; bot-engine.js bettet sie als `_TOOL_ORIGIN`-Kopfzeile in den generierten Bot-Code ein, items.js (17) und bot-ui.js (1) konkatenieren sie in ihre EXEC-Zeichenketten — 36 Stellen, mechanisch, mit behavioralem Nachweis an drei Generatoren und statischem Audit über alle. Loader-Seite: `ALLOWED_ORIGIN` wird aus `POPUP_URL` abgeleitet (eine Quelle), und `window.__BCK_popupRef` wird als gepinnte Quelle gegen `ev.source` geprüft (+5/-1 Zeilen).

Purpose: Erfolgskriterium 1 (kein `postMessage(..., "*")` im generierten Bot-Code; Spiel→Tool nutzt den statischen Tool-Origin) und Erfolgskriterium 2 (beide Seiten prüfen Origin UND Source gegen eine einzige Konstante). Ohne diesen Plan empfängt jedes Fenster mit einer Referenz auf das Tool-Fenster Spielzustand, Screenshots und Bot-Events (T-3-03), und ein zweites same-origin-Fenster könnte im Loader die Popup-Referenz kapern (T-3-02).
Output: `tests/injected-code-origin.test.js`, `tests/loader-origin.test.js` (neu), `tests/bot-engine-escaping.test.js` (Sandbox +1 Global), `loader.js`, `items.js`, `bot-engine.js`, `bot-ui.js`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bridge-haertung/03-RESEARCH.md
@.planning/phases/03-bridge-haertung/03-VALIDATION.md
@.planning/phases/03-bridge-haertung/03-01-SUMMARY.md
@.planning/phases/01-testfundament/01-03-SUMMARY.md

Planer-Probe (2026-09-13, HEAD `012c6d5`) — verbindliche Befunde:
1. Enumeration (Zeilen vor Plan 03-01; nach 03-01 um wenige Zeilen verschoben — immer per grep neu lokalisieren): items.js `"*"`-Stellen 2548, 2550, 2726, 2737, 2739, 5318, 5321, 7686, 7763, 7765, 7872, 7933, 7935, 8019, 8022, 10024, 10026 — alle innerhalb einfach-quotierter String-Stücke, alle enden mit `"*")` (2726 mit `"*");return;}`). bot-engine.js `'*'`-Stellen 216, 469, 1549, 1565, 1605, 1837, 1844, 2168, 2176, 2274, 2291, 2388, 2397, 2615, 2617, 3066, 3100, 3139 — alle innerhalb des EINEN Template-Literals von `_buildBotCode` (`return \`(function(){` Zeile 81 bis `})();\`;` Zeile 3242), alle als `,'*')`. Zeile 2936 (`raw.startsWith('*')`) ist KEINE postMessage-Stelle und darf nicht angefasst werden. bot-ui.js: Zeile 1023 in `bcKeys()`, doppelt-quotierte äußere Zeichenkette, `,'*')`.
2. Grep-Baseline `grep -cE ", *['\"]\*['\"]\)"`: items.js 19 (17 + 2 PING), bot-engine.js 18, bot-ui.js 1, loader.js 0. Ziel: 2 / 0 / 0 / 0.
3. `grep -c "animereviewer1-sketch.github.io"`: loader.js 2 (Zeile 30 `POPUP_URL`, Zeile 32 `ALLOWED_ORIGIN`), items.js/bot-engine.js/bot-ui.js/index.html 0. `grep -c "ALLOWED_ORIGIN" loader.js` = 34.
4. loader.js Listener (Zeilen 836-846): `if (!ev.data || ev.data.app !== APP) return;` → `if (ev.origin !== ALLOWED_ORIGIN) { BCK.warn(...); return; }` → `const src = ev.source;` → `window.__BCK_popupRef = src;`. Der Guard wird ZWISCHEN `const src = ev.source;` und der Pin-Zuweisung eingefügt.
5. `tests/bot-engine-escaping.test.js` hat eine EIGENE Minimal-Sandbox (`buildSandbox`, Zeilen 13-19: `{ console, btoa, atob, _money: undefined, _rankData: undefined, _shop: undefined }`) ohne items.js und ohne `location` — sobald `_buildBotCode` das Global `TOOL_ORIGIN` referenziert, braucht diese Sandbox den Eintrag `TOOL_ORIGIN: 'https://tool.test'`, sonst ReferenceError.
6. Gemeinsame Sandbox: `loadScript(['items.js','bot-data.js','bot-ui.js','bot-engine.js'], { opener })` lädt fehlerfrei; `ctx.debugOsOutfit('1', 0)` (nach `evalIn(ctx, "LSCG_DB['1']={versions:[{code:'abc'}]}")`), `ctx.bcKeys()` (nach `ctx._selBot = () => ({ id: 'b1', laufend: true })` und `evalIn(ctx, '_connected = true')`) und `ctx._buildBotCode({ id: 'b1', name: 'N', settings: { hearChat: true, modus: 'chat' }, triggers: [], events: [], szenen: [] })` funktionieren; die gesendeten EXEC-Codes stehen in `opener.postMessage.mock.calls[i][0].code`. Der Bootstrap-PING beim Laden ist `mock.calls[0]` — `mockClear()` nach dem Laden.
7. `location.origin` in der Sandbox ist seit Plan 03-01 `https://tool.test` (`SANDBOX_ORIGIN`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests für injizierten Code und Loader schreiben, Escaping-Sandbox um `TOOL_ORIGIN` ergänzen, RED bestätigen — `tests/injected-code-origin.test.js`, `tests/loader-origin.test.js` (STAB-05, STAB-06)</name>
  <precondition>Plan 03-01 abgeschlossen (`npm test` = 11 Dateien grün); `git status --short` außer `.planning/milestone.lock` leer</precondition>
  <files>tests/injected-code-origin.test.js, tests/loader-origin.test.js, tests/bot-engine-escaping.test.js</files>
  <read_first>tests/bot-engine-escaping.test.js Zeilen 1-45 (`buildSandbox`, `makeBot`, `parseOnly`); tests/bridge-protocol.test.js (Helfer `boot`/`mkOpener` aus 03-01 als Stilvorlage); tests/helpers/loadScript.js (`SANDBOX_ORIGIN`, `REPO_ROOT`, `evalIn`); .planning/phases/03-bridge-haertung/03-RESEARCH.md „Enumeration: Alle postMessage-Aufrufstellen“ und „Code Examples → Origin-Ableitung ohne zweite Definition“</read_first>
  <action>
    1. `tests/bot-engine-escaping.test.js`: im Objektliteral von `buildSandbox` den Eintrag `TOOL_ORIGIN: 'https://tool.test'` ergänzen (nach `_shop: undefined`), mit Kommentar „Global aus items.js, das `_buildBotCode` ab Phase 3 für das Origin-Literal braucht“. Sonst nichts ändern; die Datei bleibt grün.

    2. `tests/injected-code-origin.test.js` (neu, ESM) — Imports `describe, it, expect, vi, beforeAll` aus vitest, `fs`/`path` aus node, `loadScript, evalIn, REPO_ROOT, SANDBOX_ORIGIN` aus `./helpers/loadScript.js`. Helfer: `WILD = /,\s*['"]\*['"]\)/` (Wildcard als postMessage-Ziel), `src(file)` → `fs.readFileSync(path.join(REPO_ROOT, file), 'utf8')`, `lastCode(opener)` → `opener.postMessage.mock.calls.at(-1)[0].code`, `makeBot()` wie in der Escaping-Suite.

       `describe('Injizierter Code sendet an den Tool-Origin statt an "*" (STAB-05)')` mit `beforeAll`: `opener = { closed: false, postMessage: vi.fn() }; ctx = loadScript(['items.js','bot-data.js','bot-ui.js','bot-engine.js'], { opener, setTimeout: () => 0, clearTimeout: () => {} }); opener.postMessage.mockClear();` (Befund 6):
       - `it('TOOL_ORIGIN ist die Origin des Tool-Fensters')`: `evalIn(ctx, 'TOOL_ORIGIN') === SANDBOX_ORIGIN`.
       - `it('debugOsOutfit: Analyse-EXEC antwortet an den Tool-Origin')`: `evalIn(ctx, "LSCG_DB['1']={versions:[{code:'abc'}]}")`; `ctx.debugOsOutfit('1', 0)`; `code = lastCode(opener)`; `expect(code).toContain('"' + SANDBOX_ORIGIN + '"')`; `expect(code).not.toMatch(WILD)`; `expect(() => new Function(code)).not.toThrow()`.
       - `it('bcKeys: Key-Bericht-EXEC antwortet an den Tool-Origin')`: `ctx._selBot = () => ({ id: 'b1', laufend: true }); evalIn(ctx, '_connected = true'); ctx.bcKeys();` → `code` enthält `"'" + SANDBOX_ORIGIN + "'"`, kein `WILD`, parst.
       - `it('_buildBotCode: generierter Bot-Code deklariert _TOOL_ORIGIN einmal und sendet nur dorthin')`: `code = ctx._buildBotCode(makeBot())`; Anzahl Treffer von `/const _TOOL_ORIGIN="https:\/\/tool\.test";/g` = 1; Anzahl `/,_TOOL_ORIGIN\)/g` = 18; kein `WILD`; parst per `new Function`.

       `describe('Statischer Quell-Audit (STAB-05 / STAB-06 Tool-Seite)')`:
       - `it.each([['bot-engine.js', 0], ['bot-ui.js', 0], ['loader.js', 0]])('%s: %i Wildcard-Ziele')`: Zeilen, die `WILD` matchen, zählen.
       - `it('items.js: genau 2 Wildcard-Zeilen – beide Bootstrap-PING über window.opener')`: matchende Zeilen = 2, jede enthält `type: 'PING'` und `window.opener.postMessage(`.
       - `it('Tool-Origin ist genau einmal definiert')`: `src('items.js')` enthält `const TOOL_ORIGIN = window.location.origin;` genau 1×; über `items.js`+`bot-engine.js`+`bot-ui.js` kommt `location.origin` genau 1× vor; `const TOOL_ORIGIN` kommt in bot-engine.js/bot-ui.js 0× vor.
       - `it('kein GitHub-Pages-Origin-String in Tool-Dateien')`: `originHost` (Helfer: aus der `POPUP_URL`-Zeile von loader.js per Regex `/const POPUP_URL = '([^']+)'/` gelesen und mit `new URL(...).host` zum Host reduziert — kein hartcodierter Origin im Test) kommt in items.js, bot-engine.js, bot-ui.js je 0× vor.
       - `it('items.js: 17 Stellen konkatenieren TOOL_ORIGIN, bot-ui.js: 1')`: Vorkommen von `TOOL_ORIGIN + '")` in items.js = 17; Vorkommen von `TOOL_ORIGIN + "')` in bot-ui.js = 1; `JSON.stringify(TOOL_ORIGIN)` in bot-engine.js = 1.

    3. `tests/loader-origin.test.js` (neu, ESM, rein statisch — Loader-Sandbox ist per Entscheidung 4 Phase 5): Imports `describe, it, expect`, `fs`/`path`, `REPO_ROOT`; `L = fs.readFileSync(path.join(REPO_ROOT, 'loader.js'), 'utf8')`, `lines = L.split('\n')`.
       - `it('Der Tool-Origin-String steht genau einmal in loader.js – in POPUP_URL')`: `originHost` wie oben aus der `POPUP_URL`-Zeile ableiten; Zeilen, die `originHost` enthalten = 1, und diese Zeile enthält `const POPUP_URL`.
       - `it('ALLOWED_ORIGIN wird aus POPUP_URL abgeleitet, nicht zweitdefiniert')`: `L` enthält `const ALLOWED_ORIGIN = new URL(POPUP_URL).origin;` genau 1×; Zeilen, die mit `const ALLOWED_ORIGIN` beginnen (nach Trim) = 1.
       - `it('Listener pinnt die Quelle: Guard steht zwischen ev.source-Lesen und Pin-Zuweisung')`: `iSrc = lines.findIndex(l => l.includes('const src = ev.source;'))`, `iGuard = lines.findIndex(l => l.includes('src !== window.__BCK_popupRef'))`, `iPin = lines.findIndex(l => l.includes('window.__BCK_popupRef = src;'))` → alle ≥ 0 und `iSrc < iGuard && iGuard < iPin`; die Guard-Zeile enthält außerdem `ev.data.type !== 'PING'`.
       - `it('Origin-Check bleibt erhalten, keine Wildcards')`: `L` enthält `if (ev.origin !== ALLOWED_ORIGIN)` 1×; Zeilen mit `/,\s*['"]\*['"]\)/` = 0; Vorkommen von `ALLOWED_ORIGIN` (Zeilen) = 34.

    4. RED bestätigen: beide neuen Dateien rot — erwartet `TOOL_ORIGIN is not defined` bzw. Zählungen 19/18/1 statt 2/0/0, Origin-String 2 statt 1, Guard-Index −1. `npx vitest run tests/bot-engine-escaping.test.js` bleibt grün.

    5. Commit: `git add tests/injected-code-origin.test.js tests/loader-origin.test.js tests/bot-engine-escaping.test.js`; `git diff --cached --name-only | sort` = genau diese drei; Message `test(03-02): add failing injected-code and loader origin tests (STAB-05, STAB-06)`. `git status --short items.js bot-engine.js bot-ui.js loader.js` muss leer sein.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/injected-code-origin.test.js tests/loader-origin.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -q "TOOL_ORIGIN" "$OUT" && grep -q "POPUP_URL\|__BCK_popupRef\|animereviewer1" "$OUT" && grep -Eq "Tests +[0-9]+ failed" "$OUT" && npx vitest run tests/bot-engine-escaping.test.js > /dev/null 2>&1 && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — die neuen Tests sind unerwartet grün (Exit 0), die Ausgabe nennt weder `TOOL_ORIGIN` noch einen Loader-Marker, oder die Escaping-Suite ist durch den Sandbox-Eintrag rot geworden</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "TOOL_ORIGIN: 'https://tool.test'" tests/bot-engine-escaping.test.js` = 1
    - `grep -c "_TOOL_ORIGIN" tests/injected-code-origin.test.js` >= 2; `grep -c "debugOsOutfit" tests/injected-code-origin.test.js` >= 1; `grep -c "bcKeys" tests/injected-code-origin.test.js` >= 1; `grep -c "_buildBotCode" tests/injected-code-origin.test.js` >= 1; `grep -c "originHost" tests/injected-code-origin.test.js` >= 2
    - `grep -Fc "new URL(POPUP_URL).origin" tests/loader-origin.test.js` >= 1; `grep -Fc "src !== window.__BCK_popupRef" tests/loader-origin.test.js` >= 1; `grep -c "readFileSync" tests/loader-origin.test.js` >= 1
    - `test "$(cat tests/injected-code-origin.test.js tests/loader-origin.test.js | grep -v '^\s*//' | grep -c '\.only(')" = 0`
    - Nach dem Commit: `git show --name-only --format= HEAD | sort | tr '\n' ' '` = `tests/bot-engine-escaping.test.js tests/injected-code-origin.test.js tests/loader-origin.test.js `; `git status --short items.js bot-engine.js bot-ui.js loader.js` leer
  </acceptance_criteria>
  <done>Zwei neue Testdateien (behavioral + statisch) nachweislich rot, Escaping-Suite mit `TOOL_ORIGIN`-Global weiterhin grün; Commit enthält genau die drei Testpfade; Produktionsdateien unverändert.</done>
</task>

<task type="auto">
  <name>Task 2: loader.js — `ALLOWED_ORIGIN` aus `POPUP_URL` ableiten und Quelle pinnen (STAB-06 Loader-Hälfte), Loader-Test GREEN, committen</name>
  <precondition>`git status --short loader.js` gibt VOR dem Edit nichts aus — sonst STOPP und eskalieren</precondition>
  <reversibility rating="reversible">Sechs Zeilen im Loader; Revert per `git revert`. Betriebs-Hinweis (kein One-way-Door): loader.js wird über GitHub Pages ausgeliefert und per Bookmarklet in den laufenden Spiel-Tab geladen — nach Deploy oder Revert muss der Nutzer das Bookmarklet einmal neu ausführen, damit der neue Listener den alten ersetzt (`__BCK_LISTENER_FN__`-Austausch ist im Loader vorgesehen).</reversibility>
  <files>loader.js</files>
  <read_first>loader.js Zeilen 26-33 (`APP`, `POPUP_URL`, `ALLOWED_ORIGIN` mit `// FIX:`-Kommentar) und 828-848 (Listener-Kopf bis `BCK.info('← postMessage:' …)`); tests/loader-origin.test.js (Task 1)</read_first>
  <action>
    1. Die Zeile mit der zweiten, unabhängigen Origin-Definition (Zeile 32: `const ALLOWED_ORIGIN = '<GitHub-Pages-Origin>';`, direkt unter `POPUP_URL`) ersetzen durch `const ALLOWED_ORIGIN = new URL(POPUP_URL).origin;` — bestehenden `// FIX: Validate origin …`-Kommentar um „(aus POPUP_URL abgeleitet — eine Origin-Quelle, STAB-06)“ ergänzen. Der GitHub-Pages-Origin-String steht danach nur noch in der `POPUP_URL`-Zeile; in keinem Kommentar wiederholen.

    2. Im `message`-Listener direkt NACH `const src = ev.source;` und VOR `window.__BCK_popupRef = src;` einfügen (Befund 4): Kommentar „Source-Pinning (STAB-06): nach dem ersten Kontakt nur noch das gepinnte Tool-Fenster; PING darf immer neu pinnen (Tool-Fenster neu geladen / 🔄 Verbinden)“ und den Guard `if (window.__BCK_popupRef && src !== window.__BCK_popupRef && ev.data.type !== 'PING') { BCK.warn('postMessage von nicht gepinnter Quelle blockiert:', ev.data.type); return; }`. Die bestehende Pin-Zeile `window.__BCK_popupRef = src; // Bot kann damit Logs zurückschicken` bleibt unverändert darunter (pinnt beim ersten Kontakt und bei jedem PING neu). Keine weiteren Änderungen: kein Refactor, keine Umbenennung, `ALLOWED_ORIGIN`-Verwendungen bleiben (34 Zeilen).

    3. `node --check loader.js` → Exit 0. GREEN: `npx vitest run tests/loader-origin.test.js` → 4 passed. `tests/injected-code-origin.test.js` bleibt erwartungsgemäß rot (Tool-Seite folgt in Task 3).

    4. Commit: `git add loader.js`; Message `fix(03-02): derive ALLOWED_ORIGIN from POPUP_URL and pin message source in loader (STAB-06)`. `git show --numstat --format= HEAD` → Insertions ≤ 8, Deletions ≤ 2.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/loader-origin.test.js && node --check loader.js && test "$(grep -c 'animereviewer1-sketch.github.io' loader.js)" = 1 && test "$(grep -Fc 'const ALLOWED_ORIGIN = new URL(POPUP_URL).origin;' loader.js)" = 1 && test "$(grep -Fc 'src !== window.__BCK_popupRef' loader.js)" = 1 && test "$(grep -o 'ALLOWED_ORIGIN' loader.js | wc -l)" -eq 34 && test "$(grep -cE ",'\*'\)" loader.js)" = 0 && test "$(grep -cE ',"\*"\)' loader.js)" = 0 && test "$(git show --name-only --format= HEAD)" = "loader.js" && ADD=$(git show --numstat --format= HEAD | awk '{print $1}') && DEL=$(git show --numstat --format= HEAD | awk '{print $2}') && test "$ADD" -le 8 && test "$DEL" -le 2 && echo LOADER-OK</automated>
    <fails_when>`LOADER-OK` fehlt — Loader-Test rot, `node --check` SyntaxError, Origin-String noch 2× (oder 0×), Guard fehlt, `ALLOWED_ORIGIN`-Zeilenzahl verändert (Refactor), Commit enthält andere Pfade oder der Diff ist größer als +8/−2</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/injected-code-origin.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "Tests +[0-9]+ failed" "$OUT" && echo STILL-RED-AS-EXPECTED</automated>
    <fails_when>`STILL-RED-AS-EXPECTED` fehlt — der Tool-Seiten-Test ist grün, obwohl items.js/bot-engine.js/bot-ui.js noch nicht geändert wurden (Test prüft nicht, was er soll)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "animereviewer1-sketch.github.io" loader.js` = 1 und diese Zeile enthält `POPUP_URL`
    - `grep -Fc "const ALLOWED_ORIGIN = new URL(POPUP_URL).origin;" loader.js` = 1; `grep -o "ALLOWED_ORIGIN" loader.js | wc -l` → 34 (unverändert: 33 Verwendungen + 1 Definition)
    - `grep -Fc "src !== window.__BCK_popupRef" loader.js` = 1; `grep -Fc "ev.data.type !== 'PING'" loader.js` = 1; `grep -Fc "window.__BCK_popupRef = src;" loader.js` = 1
    - Keine Wildcard-Ziele: `grep -cE ",'\*'\)" loader.js` = 0 und `grep -cE ',"\*"\)' loader.js` = 0
    - `node --check loader.js` → Exit 0; `npx vitest run tests/loader-origin.test.js` → `4 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `loader.js`; Insertions ≤ 8, Deletions ≤ 2
  </acceptance_criteria>
  <done>loader.js kennt den Tool-Origin nur noch über `POPUP_URL`, prüft Origin UND gepinnte Quelle; Loader-Test grün; Commit minimal und nur loader.js; Tool-Seiten-Test weiterhin rot wie erwartet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `TOOL_ORIGIN` in items.js definieren und die 36 injizierten Stellen umstellen (items.js 17, bot-engine.js 18, bot-ui.js 1), GREEN bestätigen, committen (STAB-05, STAB-06 Tool-Seite)</name>
  <precondition>`git status --short items.js bot-engine.js bot-ui.js` gibt VOR dem Edit nichts aus — sonst STOPP und eskalieren</precondition>
  <reversibility rating="reversible">Mechanische Literal-Ersetzungen plus eine Konstante; Revert per `git revert`. Nach dem Deploy muss der Nutzer laufende Bots einmal neu deployen (der generierte Code wird beim Start neu erzeugt).</reversibility>
  <files>items.js, bot-engine.js, bot-ui.js</files>
  <read_first>items.js `const APP = 'BCKonfigurator';` (per grep; Einfügeort der Konstante direkt darunter) und die 17 Stellen per `grep -nE ', *"\*"\)' items.js`; bot-engine.js Zeile 81 (`return \`(function(){`) und die 18 Stellen per `grep -nE ",'\*'\)" bot-engine.js` (Zeile 2936 `startsWith('*')` ist NICHT dabei — nicht anfassen); bot-ui.js per `grep -nE ",'\*'\)" bot-ui.js` (1 Treffer in `bcKeys`); tests/injected-code-origin.test.js (Task 1); .planning/phases/03-bridge-haertung/03-RESEARCH.md „Pattern 1“ und „Pitfall 1“</read_first>
  <behavior>
    - `evalIn(ctx, 'TOOL_ORIGIN')` liefert die Sandbox-Origin; `debugOsOutfit`, `bcKeys`, `_buildBotCode` erzeugen Code ohne Wildcard-Ziel mit dem Origin-Literal; generierter Code parst weiterhin per `new Function`
    - Statisch: Wildcard-Zeilen items.js 2 (PING), bot-engine.js 0, bot-ui.js 0; `location.origin` 1× über die drei Dateien; Origin-String 0×; 17 / 18 / 1 Ersetzungen zählbar
    - Escaping-Suite (TEST-06) und volle Suite bleiben grün
  </behavior>
  <action>
    1. items.js — direkt unter `const APP = 'BCKonfigurator';` einfügen: Kommentar „Tool-eigene Origin für injizierten Code (STAB-05): im Tool-Fenster berechnet und als Literal in die EXEC-Zeichenketten eingebettet — im Spiel-Tab darf die Location des Popup-Fensters nicht gelesen werden (Cross-Origin, SecurityError). Einzige Tool-seitige Origin-Definition (STAB-06); bot-engine.js und bot-ui.js nutzen dieses Global.“ und die Zeile `const TOOL_ORIGIN = window.location.origin;`. Im Kommentar NICHT die Zeichenfolge `location.origin` wiederholen (statischer Audit zählt genau ein Vorkommen über die drei Tool-Dateien).

    2. items.js — an genau den 17 enumerierten Stellen (Befund 1) das doppelt-quotierte Wildcard-Ziel (der Stern in Anführungszeichen unmittelbar vor der schließenden Klammer des `postMessage`-Aufrufs) durch `"' + TOOL_ORIGIN + '"` ersetzen. Jede Stelle liegt in einem einfach-quotierten String-Stück; das Stück endet danach mit `},"' + TOOL_ORIGIN + '");'` (bei der Stelle mit `return;}` entsprechend `},"' + TOOL_ORIGIN + '");return;}'`). Sonst nichts an diesen Zeilen ändern. Zielzustand (Zählung siehe Acceptance Criteria): 17 Konkatenationen, als Wildcard-Ziele bleiben nur die zwei Bootstrap-PINGs aus 03-01.

    3. bot-engine.js — in `_buildBotCode` unmittelbar nach der Zeile `return \`(function(){` eine neue erste Zeile des generierten Codes einfügen: `const _TOOL_ORIGIN=${JSON.stringify(TOOL_ORIGIN)};` (davor optional ein JS-Kommentar im generierten Code, z. B. `// Tool-Origin für Rückmeldungen ans Popup (STAB-05)`). Dann an genau den 18 enumerierten Stellen das einfach-quotierte Wildcard-Ziel hinter dem Komma durch `_TOOL_ORIGIN` ersetzen, sodass jeder Aufruf mit `,_TOOL_ORIGIN)` endet. Die Emote-Erkennung mit `startsWith` (Zeile 2936) bleibt unverändert. `TOOL_ORIGIN` wird als Global aus items.js aufgelöst (Ladereihenfolge index.html: items.js vor bot-engine.js) — KEINE zweite Definition der Konstante in bot-engine.js. Zielzustand: 18× `,_TOOL_ORIGIN)`, 1× `JSON.stringify(TOOL_ORIGIN)`, 0 Wildcard-Ziele.

    4. bot-ui.js — in `bcKeys()` in der doppelt-quotierten Zeichenkette das einfach-quotierte Wildcard-Ziel hinter `bericht:_r}` durch `'" + TOOL_ORIGIN + "'` ersetzen, sodass das Stück `…bericht:_r},'" + TOOL_ORIGIN + "');}catch…` lautet. Zielzustand: 1 Konkatenation, 0 Wildcard-Ziele.

    5. `node --check items.js bot-engine.js bot-ui.js` → Exit 0. GREEN: `npx vitest run tests/injected-code-origin.test.js tests/bot-engine-escaping.test.js tests/loader-origin.test.js` → alle grün; `npm test` → 13 Dateien grün.

    6. Commit: `git add items.js bot-engine.js bot-ui.js`; `git diff --cached --name-only | sort` = genau diese drei; Message `fix(03-02): embed tool origin literal in all 36 injected postMessage sites (STAB-05, STAB-06)`. Nachweis im SUMMARY: `git show --stat --format= HEAD` (erwartet ≈ +38/−36 über drei Dateien), RED-Ausgabe aus Task 1, GREEN-Ausgabe.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/injected-code-origin.test.js tests/bot-engine-escaping.test.js tests/loader-origin.test.js && node --check items.js && node --check bot-engine.js && node --check bot-ui.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` in der `Tests`-Summary — etwa `TOOL_ORIGIN is not defined` (Escaping-Sandbox ohne Global oder Konstante nicht in items.js), eine übersehene Wildcard-Stelle (Zählung 17/18/1 verfehlt), oder ein SyntaxError durch falsch geschlossene String-Stücke</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(grep -cE ',"\*"\)' items.js)" = 0 && test "$(grep -oE ", *'\*'\)" items.js | wc -l)" -eq 2 && test "$(grep -cE ",'\*'\)" bot-engine.js)" = 0 && test "$(grep -cE ',"\*"\)' bot-engine.js)" = 0 && test "$(grep -cE ",'\*'\)" bot-ui.js)" = 0 && test "$(grep -cE ',"\*"\)' bot-ui.js)" = 0 && echo WILDCARDS-OK</automated>
    <fails_when>`WILDCARDS-OK` fehlt — in items.js ist noch ein doppelt-quotiertes Wildcard-Ziel übrig oder die einfach-quotierten sind nicht mehr genau die zwei Bootstrap-PINGs, oder bot-engine.js/bot-ui.js enthalten noch ein Wildcard-Ziel</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(grep -Fc 'const TOOL_ORIGIN = window.location.origin;' items.js)" = 1 && test "$(cat items.js bot-engine.js bot-ui.js | grep -c 'location\.origin')" = 1 && test "$(grep -oF '+ TOOL_ORIGIN +' items.js | wc -l)" -eq 17 && test "$(grep -oF ',_TOOL_ORIGIN)' bot-engine.js | wc -l)" -eq 18 && test "$(grep -Fc 'JSON.stringify(TOOL_ORIGIN)' bot-engine.js)" = 1 && test "$(grep -oF '+ TOOL_ORIGIN +' bot-ui.js | wc -l)" -eq 1 && test "$(grep -Fc "startsWith('*')" bot-engine.js)" = 1 && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "bot-engine.js bot-ui.js items.js " && echo SITES-OK</automated>
    <fails_when>`SITES-OK` fehlt — die Konstante fehlt oder ist doppelt definiert, Ersetzungszählungen ≠ 17/18/1, die Emote-Erkennung (`startsWith`) wurde angefasst, oder der Commit enthält andere Pfade als die drei Tool-Dateien</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(cat items.js bot-engine.js bot-ui.js | grep -c 'animereviewer1-sketch.github.io')" = 0 && echo NO-ORIGIN-LITERAL-OK</automated>
    <fails_when>`NO-ORIGIN-LITERAL-OK` fehlt — der GitHub-Pages-Origin-String wurde in items.js, bot-engine.js oder bot-ui.js hartcodiert statt aus `window.location.origin` abgeleitet</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "const TOOL_ORIGIN = window.location.origin;" items.js` = 1; `cat items.js bot-engine.js bot-ui.js | grep -c "location\.origin"` = 1 (einzige Definition; bot-engine.js/bot-ui.js definieren keine eigene Konstante)
    - Doppelt-quotierte Wildcard-Ziele: `grep -cE ',"\*"\)' items.js` = 0, bot-engine.js = 0, bot-ui.js = 0. Einfach-quotierte: `grep -oE ", *'\*'\)" items.js | wc -l` → 2 (beide Zeilen enthalten `type: 'PING'`); `grep -cE ",'\*'\)" bot-engine.js` = 0; bot-ui.js = 0; loader.js = 0
    - `grep -oF '+ TOOL_ORIGIN +' items.js | wc -l` → 17 (17 Konkatenationen); `grep -oF ",_TOOL_ORIGIN)" bot-engine.js | wc -l` → 18; `grep -Fc "JSON.stringify(TOOL_ORIGIN)" bot-engine.js` = 1; `grep -oF '+ TOOL_ORIGIN +' bot-ui.js | wc -l` → 1
    - `grep -Fc "startsWith('*')" bot-engine.js` = 1 (unverändert); `cat items.js bot-engine.js bot-ui.js | grep -c "animereviewer1-sketch.github.io"` = 0
    - `node --check items.js`, `node --check bot-engine.js`, `node --check bot-ui.js` → Exit 0; `npm test` → `Test Files  13 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` = `bot-engine.js`, `bot-ui.js`, `items.js`; `git log --format=%h 012c6d5..HEAD -- index.html` gibt nichts aus; `git status --short` zeigt nur `?? .planning/milestone.lock`
  </acceptance_criteria>
  <done>Genau eine Tool-Origin-Konstante; alle 36 injizierten Stellen senden an das eingebettete Origin-Literal; nur die zwei Bootstrap-PINGs tragen noch `'*'`; alle drei Testdateien und die volle Suite grün; Commit enthält genau die drei Tool-Dateien.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Injizierter Code (Spiel-Tab) → Tool-Fenster (`__BCK_popupRef.postMessage`) | Trägt Spielzustand, Screenshots, Bot-Events zurück; Ziel-Origin muss das Tool sein, nicht `'*'` |
| Tool-Fenster → Spiel-Tab (`loader.js` `message`-Listener, EXEC) | Einzige privilegierte Code-Ausführung im System (`new Function`) — Origin UND gepinnte Quelle |
| GitHub Pages → laufender Spiel-Tab (Bookmarklet lädt loader.js) | Deploy wirkt erst nach erneutem Bookmarklet-Lauf; alte/neue Loader-Version müssen mit alter/neuer Tool-Version koexistieren |
| Git-Arbeitsbaum → Repository | Drei Commits, je nur die genannten Pfade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-02 | Elevation of Privilege | loader.js Listener übernimmt `ev.source` jeder same-origin-Nachricht als `__BCK_popupRef` / führt EXEC aus | high | mitigate | Source-Pinning-Guard vor der Pin-Zuweisung (Task 2); Origin-Check bleibt; statischer Test fixiert Reihenfolge Lesen → Guard → Pin |
| T-3-03 | Information Disclosure | 36 injizierte `postMessage(..., '*')` liefern Spielzustand an jedes Fenster mit Referenz | high | mitigate | `TOOL_ORIGIN`-Literal an allen 36 Stellen (Task 3); behavioral an drei Generatoren + statischer Audit 2/0/0/0 |
| T-3-08 | Tampering | Zwei unabhängige Origin-Definitionen in loader.js driften auseinander | medium | mitigate | `ALLOWED_ORIGIN = new URL(POPUP_URL).origin` (Task 2); Grep-Gate Origin-String = 1 |
| T-3-09 | Denial of Service | Falsches Origin-Literal (z. B. SecurityError-Variante oder Tippfehler) → alle Rückmeldungen verloren, Tool „hängt“ | high | mitigate | Berechnung nur im Tool-Fenster (`window.location.origin`), nie im Spiel-Tab; Tests prüfen den konkreten Literalwert `https://tool.test`; Live-Smoke-Test (Plan 03-03 `<verification>`) prüft alle vier Flows |
| T-3-10 | Tampering | Versehentliche Änderung von `startsWith('*')` (Emote-Erkennung) durch Massenersetzung | medium | mitigate | Gate `grep -Fc "startsWith('*')" bot-engine.js` = 1; Ersetzungsmuster `,'*')` trifft die Emote-Zeile nicht |
| T-3-07 | Tampering | Git-Staging fremder Dateien | high | mitigate | Preconditions `git status --short` leer; explizite `git add`-Pfade; `git show --name-only` Gates; `git log 012c6d5..HEAD -- index.html` leer |
| T-3-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase |
</threat_model>

<verification>
- `npx vitest run tests/injected-code-origin.test.js tests/loader-origin.test.js tests/bot-engine-escaping.test.js` grün; `npm test` → 13 Testdateien grün.
- `node --check items.js bot-engine.js bot-ui.js loader.js` grün; drei Commits (test RED → fix loader → fix Tool-Seite); loader.js-Diff ≤ +8/−2.
- Statische Gates: Wildcard-Zeilen 2/0/0/0; Origin-String loader.js 1, Tool-Dateien 0; `location.origin` 1; Ersetzungen 17/18/1.
- Manual-Only (end-of-phase, gesammelt in Plan 03-03): Bookmarklet neu ausführen, Tool neu laden, Bot starten → Bot-Log im Tool sichtbar (bestätigt, dass `_TOOL_ORIGIN` im Live-Origin korrekt ist).
</verification>

<success_criteria>
- Erfolgskriterium 1: Kein `postMessage(..., "*")` mehr im generierten Bot-/Watcher-Code und im Loader; Spiel→Tool nutzt den statischen Tool-Origin (STAB-05).
- Erfolgskriterium 2 (beide Hälften): Loader prüft `event.origin` UND gepinnte `event.source`; Tool-Seite (03-01) prüft beides; genau eine Origin-Konstante je Seite, keine Zweitdefinition (STAB-06, Orchestrator-Entscheidung 3).
</success_criteria>

<output>
Create `.planning/phases/03-bridge-haertung/03-02-SUMMARY.md` when done — inkl. RED-Ausgabe aus Task 1, GREEN-Ausgaben aus Task 2/3, `git show --stat` der drei Commits, den Grep-Zählungen (2/0/0/0, 1, 17/18/1) und dem Betriebs-Hinweis „Bookmarklet neu ausführen + Tool neu laden + laufende Bots neu deployen“.
</output>
