---
phase: 03-bridge-haertung
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - tests/exec-log.test.js
  - items.js
  - index.html
autonomous: true
requirements: [STAB-08]
tags: [exec-log, bridge, tweaks-panel, indexeddb, ring-buffer, red-green]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "STAB-08: Jeder über `bcSend({ type: 'EXEC', … })` tatsächlich gesendete EXEC erzeugt genau einen Log-Eintrag `{ ts, desc, len }` — geloggt ausschließlich in `bcSend` (Orchestrator-Entscheidung 5, RESEARCH Pattern 3), null Änderungen an den ≥40 Aufrufstellen; Nicht-EXEC-Nachrichten und fehlgeschlagene Sends (kein Opener) erzeugen keinen Eintrag"
    - "`desc` = `msg.desc`, falls der Aufrufer ein nicht-leeres Label mitgibt, sonst die ersten 60 Zeichen des Codes (Whitespace zu einem Leerzeichen kollabiert); `len` = Zeichenlänge des Codes; `ts` = `Date.now()`"
    - "Ringpuffer: `EXEC_LOG_MAX = 200`; bei Überlauf fallen die ältesten Log-Einträge weg — und NUR Log-Einträge: `LSCG_DB`, `PROFILE_SCREENSHOTS`, Bot-Definitionen und alle anderen IDB-Schlüssel bleiben unberührt (Kernwert „Scan-Daten gehen nie verloren“; das EXEC-Log ist Betriebs-Telemetrie, RESEARCH Pitfall 5 — per Invarianz-Test bewiesen)"
    - "Persistenz unter dem neuen IDB-Schlüssel `BC_ExecLog_v1` (additiv, keine Migration) via bestehendem `idbSet`/`idbGet`, gedrosselt mit `_debounce` (1200 ms) wie `_debouncedSaveCurseDB`; `_loadExecLog()` beim Start MERGT gespeicherte Einträge vor die bereits im RAM liegenden und kappt auf 200 — nichts wird überschrieben, auch nicht bei Race zwischen IDB-Load und frühem EXEC"
    - "Log-Ansicht: Sektion `📜 EXEC-Log` im Tweaks-Panel (index.html, NUR Einfügungen nach `📊 Speicher`, Vorbild 02-03) mit `#execLogInfo` und Button `🔄 Aktualisieren` → `_renderExecLog()`; Anzeige neueste zuerst, je Zeile `HH:MM:SS · <desc> (<len> Zeichen)`, maximal `EXEC_LOG_SHOWN = 30` Zeilen plus Hinweis auf ältere Einträge; leer → `Noch kein EXEC gesendet`; die Anzeige aktualisiert sich bei jedem Append automatisch (Erfolgskriterium 4)"
    - "Vor der Implementierung ist `tests/exec-log.test.js` nachweislich rot (`_execLogAppend`/`_saveExecLog`/`_renderExecLog` fehlen), danach grün; RED und GREEN in getrennten Commits; GREEN-Commit enthält nur items.js und index.html"
  artifacts:
    - path: "tests/exec-log.test.js"
      provides: "STAB-08: Append-Vertrag (desc/len/ts, Label, Nicht-EXEC, kein Opener), Rotation 200 + Scan-Daten-Invarianz, Persistenz/Merge über zwei Sandboxen, Format und Anzeige (#execLogInfo), Auto-Render nach Append"
      contains: "BC_ExecLog_v1"
    - path: "items.js"
      provides: "`EXEC_LOG_KEY`, `EXEC_LOG_MAX`, `EXEC_LOG_SHOWN`, `_execLog`, `_execLogDesc(msg)`, `_execLogAppend(msg)`, `_saveExecLog()`, `_debouncedSaveExecLog`, `async _loadExecLog()`, `_execLogFormat(e)`, `_renderExecLog()`, Init-Hook; Hook-Zeile in `bcSend`"
      contains: "function _execLogAppend(msg)"
    - path: "index.html"
      provides: "Tweaks-Panel-Sektion `📜 EXEC-Log` mit `<div id=\"execLogInfo\">` und Button `onclick=\"_renderExecLog()\"`"
      contains: "id=\"execLogInfo\""
  key_links:
    - from: "items.js `bcSend(msg, silent)`"
      to: "items.js `_execLogAppend(msg)`"
      via: "Eine Zeile `if (msg.type === 'EXEC') _execLogAppend(msg);` nach den Guard-Klauseln und vor `window.opener.postMessage(...)` — einziger Sendepfad = einziger Hakenpunkt"
      pattern: "if \\(msg\\.type === 'EXEC'\\) _execLogAppend\\(msg\\);"
    - from: "items.js `_execLogAppend`"
      to: "IndexedDB `BC_ExecLog_v1` via `idbSet`"
      via: "`_debouncedSaveExecLog = _debounce(_saveExecLog, 1200)`; Tests rufen `_saveExecLog()` direkt (Sandbox-`setTimeout` in Bridge-Tests als No-op)"
      pattern: "_debounce\\(_saveExecLog, 1200\\)"
    - from: "index.html `#execLogInfo` / Button `_renderExecLog()`"
      to: "items.js `_renderExecLog`"
      via: "items.js wird per `document.write` (~Zeile 3082) NACH dem Panel-Markup (~2458) geladen; `function`-Deklaration ist global erreichbar für `onclick`"
      pattern: "_renderExecLog\\(\\)"
    - from: "tests/exec-log.test.js"
      to: "items.js `_execLog` (let) / `document.getElementById('execLogInfo')`"
      via: "`evalIn(ctx, '_execLog')` für die `let`-Variable; `ctx.document.getElementById`-Override für ein inspizierbares Element (Muster `captureInfo`, 02-03)"
      pattern: "execLogInfo"
  prohibitions:
    - requirement_id: STAB-08
      category: safety
      verification: automated
      statement: "Die Rotation berührt ausschließlich `_execLog`/`BC_ExecLog_v1` — kein anderer IDB-Schlüssel wird gelesen, geschrieben oder gelöscht (Invarianz-Test auf `LSCG_DB` nach 250 EXECs + Save); kein `confirm`-loses Löschen anderer Daten"
    - requirement_id: STAB-08
      category: safety
      verification: automated
      statement: "Keine Änderung an bcSend-Aufrufstellen: der GREEN-Commit enthält keine Diff-Zeile mit `type: 'EXEC'` (`git show HEAD -- items.js | grep -E \"^[-+].*type: *'EXEC'\"` leer); bot-engine.js/bot-ui.js/loader.js unberührt"
    - requirement_id: STAB-08
      category: safety
      verification: automated
      statement: "index.html nur mit Einfügungen (`git diff --numstat 012c6d5 HEAD -- index.html` Deletions = 0), keine Änderung an `<script>`-Tags oder `document.write`-Reihenfolge; kein Build-Schritt, keine neue Dependency"
    - requirement_id: STAB-08
      category: safety
      verification: judgment
      statement: "Keine Korrelations-IDs, kein EXEC_OK/EXEC_ERR-Matching, kein Loader-Umbau (RESEARCH Anti-Pattern) — reines Sende-Log mit Zeitstempel und Kurzbeschreibung"
    - requirement_id: STAB-08
      category: safety
      verification: judgment
      statement: "`git add` nur mit explizit genannten Pfaden; RED-Test und GREEN-Fix in getrennten Commits; keine Watch-Flags; `.planning/milestone.lock` nie stagen"
---

<objective>
EXEC-Protokoll (STAB-08): Ein RAM-Ringpuffer `_execLog` (Cap 200) mit Einträgen `{ ts, desc, len }` wird ausschließlich in `bcSend` befüllt — dem einzigen Sendepfad für alle ≥40 EXEC-Aufrufstellen —, gedrosselt unter `BC_ExecLog_v1` persistiert, beim Start gemergt geladen und im Tweaks-Panel als Sektion `📜 EXEC-Log` (nur Einfügungen in index.html, Vorbild `📊 Speicher` aus 02-03) neueste-zuerst angezeigt. RED → GREEN wie in den Plänen davor; Rotation wird per Invarianz-Test gegen Scan-Daten abgesichert. Der Plan endet mit der gesammelten Liste der Human-Checks der Phase (Live-Smoke-Test aller Bridge-Flows, sichtbarer Disconnect/Reconnect, EXEC-Log sichtbar) als nicht blockierende End-of-Phase-Verifikation (Orchestrator-Entscheidung 6).

Purpose: Erfolgskriterium 4 — jeder EXEC erscheint mit Zeitstempel und Kurzbeschreibung in einer Log-Ansicht im Tool; das macht die privilegierteste Operation des Systems (`new Function` im Spiel-Tab) nachträglich nachvollziehbar (T-3-04, Repudiation) und einen etwaigen Origin-Check-Bypass sichtbar.
Output: `tests/exec-log.test.js` (neu), `items.js` (EXEC-Log-Block + eine Hook-Zeile in `bcSend`), `index.html` (eine Sektion, nur Einfügungen).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bridge-haertung/03-RESEARCH.md
@.planning/phases/03-bridge-haertung/03-VALIDATION.md
@.planning/phases/03-bridge-haertung/03-01-SUMMARY.md
@.planning/phases/03-bridge-haertung/03-02-SUMMARY.md
@.planning/phases/02-speicher-sicherheit/02-03-SUMMARY.md

Planer-Probe (2026-09-13, HEAD `012c6d5`) — verbindliche Befunde:
1. `bcSend(msg, silent)` (items.js ~5916 vor 03-01, danach etwas verschoben — per `grep -n "^function bcSend" items.js`): Guard „kein opener“ → Log-Zeile → `window.opener.postMessage({ app: APP, ...msg }, _bcOrigin || '*')`. `msg` wird per Spread weitergereicht — ein optionales `desc`-Feld ginge mit an den Loader (harmlos, wird dort ignoriert).
2. `_debounce(fn, delay)` steht in items.js Zeile 90; Muster `const _debouncedSaveCurseDB = _debounce(function() { _saveCurseDB(); }, 1200);` Zeile 97. `idbGet`/`idbSet` Zeilen 23-65 (global, Quota-Pfad bereits sichtbar).
3. Init-Hook-Konvention (02-03, items.js ~584-587): `try { if (document.readyState !== 'loading') fn(); else document.addEventListener('DOMContentLoaded', () => …); } catch (e) {}` — in der Sandbox ist `readyState` `'complete'` → Direktaufruf beim Laden; im Browser läuft items.js während des Parsens (`'loading'`) → Listener.
4. fake-indexeddb ist pro Testdatei prozessweit: mehrere `loadScript`-Sandboxen in EINER Datei teilen dieselbe DB (Muster tests/idb-helpers.test.js). Daher: (a) `_loadExecLog()` läuft beim Laden asynchron — Tests warten nach `loadScript` mit `await settle()` aus dem Loader, bevor sie `_execLog` anfassen; (b) der Persistenztest nutzt eindeutige `desc`-Labels statt absoluter Zählungen; (c) Merge-Semantik in `_loadExecLog` (gespeichert + RAM) verhindert Clobbering.
5. `_execLog` ist `let` → nur per `evalIn(ctx, '_execLog')` lesbar (Referenz, mutierbar). `Date.now()` in der Sandbox funktioniert (eigene Realm-Intrinsics).
6. Markup-Vorbild index.html ~2457-2463 (`📊 Speicher`): `<div><div class="tweaks-section-title">📊 Speicher</div><div class="tweaks-btn-group"><button class="tweaks-btn" onclick="_speicherZeigeStatus()" …>🔄 Aktualisieren</button></div><div id="storageInfo" style="font-size:.6875rem;color:var(--text3);margin-top:5px;line-height:1.5">–</div></div>`; Einfügeort: unmittelbar nach dem schließenden `</div>` dieser Sektion, vor `<div class="tweaks-section-title">📦 Item-Katalog</div>`-Block. index.html enthält bisher 0× `execLog` und 0× `BC_ExecLog`.
7. Baseline `grep -c "bcSend({ *type: *'EXEC'" items.js bot-engine.js bot-ui.js` (Aufrufstellen) — vor und nach diesem Plan identisch; im SUMMARY festhalten.
</context>

<tasks>

<task type="auto">
  <name>Task 1: EXEC-Log-Test schreiben und RED bestätigen — `tests/exec-log.test.js` (STAB-08)</name>
  <precondition>Plan 03-02 abgeschlossen (`npm test` = 13 Dateien grün); `git status --short` außer `.planning/milestone.lock` leer</precondition>
  <files>tests/exec-log.test.js</files>
  <read_first>tests/bridge-protocol.test.js (Helfer `boot`/`mkOpener`, `setTimeout: () => 0` per `extraGlobals`); tests/idb-helpers.test.js (IDB-Round-Trip über die Sandbox, `settle`); tests/storage-estimate.test.js (`captureInfo`-Override); tests/helpers/loadScript.js (`settle`, `evalIn`, `makeElementStub`); .planning/phases/03-bridge-haertung/03-RESEARCH.md „Pattern 3“ und „Pitfall 5“</read_first>
  <action>
    `tests/exec-log.test.js` (neu, ESM): Imports `describe, it, expect, vi` aus vitest, `loadScript, evalIn, settle, makeElementStub` aus `./helpers/loadScript.js`.

    Helfer:
    - `boot()` (async) → `opener = { closed: false, postMessage: vi.fn() }`; `el = makeElementStub()`; `ctx = loadScript(['items.js'], { opener, setTimeout: () => 0, clearTimeout: () => {} })`; `ctx.document.getElementById = id => id === 'execLogInfo' ? el : makeElementStub()`; `await settle(50)` (Befund 4a); `opener.postMessage.mockClear()`; return `{ ctx, opener, el }`.
    - `log(ctx)` → `evalIn(ctx, '_execLog')`; `exec(ctx, code, extra = {})` → `ctx.bcSend({ type: 'EXEC', code, ...extra }, true)`.

    `describe('EXEC-Log: Append in bcSend (STAB-08)')`:
    1. `it('ein EXEC erzeugt genau einen Eintrag {ts, desc, len}')`: `boot()`; `n0 = log(ctx).length`; `exec(ctx, '(function(){ Foo(); })();')` → true; `log(ctx).length === n0 + 1`; letzter Eintrag: `typeof ts === 'number'` und `ts` innerhalb ±5 s von `Date.now()`; `desc === '(function(){ Foo(); })();'`; `len === 25`.
    2. `it('desc: Aufrufer-Label hat Vorrang, sonst die ersten 60 Zeichen (Whitespace kollabiert)')`: `exec(ctx, 'x', { desc: 'Outfit anwenden' })` → `desc === 'Outfit anwenden'`; `exec(ctx, 'a'.repeat(100))` → `desc.length === 60`, `len === 100`; `exec(ctx, '(function(){\n  var a = 1;\n})();')` → `desc === '(function(){ var a = 1; })();'`.
    3. `it('Nicht-EXEC-Nachrichten erzeugen keinen Eintrag')`: `ctx.bcSend({ type: 'GET_PLAYER' }, true)`; `ctx.bcSend({ type: 'PING' }, true)` → Länge unverändert.
    4. `it('ohne opener wird nicht gesendet und nicht geloggt')`: `loadScript(['items.js'], { setTimeout: () => 0 })`; `await settle(50)`; `bcSend({ type: 'EXEC', code: 'x' }, true) === false`; Länge unverändert.
    5. `it('EXEC_LOG_MAX ist 200; Überlauf verwirft die ältesten Log-Einträge')`: `evalIn(ctx, '_execLog.length = 0')`; für i in 1..201 `exec(ctx, 'c' + i, { desc: 'exec-' + i })`; Länge 200; `log(ctx)[0].desc === 'exec-2'`; `log(ctx)[199].desc === 'exec-201'`; `evalIn(ctx, 'EXEC_LOG_MAX') === 200`.

    `describe('EXEC-Log: Rotation berührt keine Scan-Daten (Kernwert)')`:
    6. `it('nach 250 EXECs und Save ist LSCG_DB in IDB byte-identisch')`: `boot()`; `seed = { '4711': { versions: [{ code: 'abc', ts: 1 }] } }`; `await ctx.idbSet('LSCG_DB', seed)`; 250× `exec(ctx, 'c' + i)`; `await ctx._saveExecLog()`; `expect(await ctx.idbGet('LSCG_DB')).toEqual(seed)`; zusätzlich `await ctx.idbSet('PROFILE_SCREENSHOTS', { p: 1 })` vor den EXECs und danach `toEqual({ p: 1 })`.

    `describe('EXEC-Log: Persistenz unter BC_ExecLog_v1')`:
    7. `it('_saveExecLog schreibt den Puffer; eine neue Sandbox lädt und mergt ihn')`: `{ ctx } = await boot()`; `tag = 'persist-' + Date.now()`; `exec(ctx, 'x', { desc: tag })`; `await ctx._saveExecLog()`; `stored = await ctx.idbGet('BC_ExecLog_v1')` → Array, letzter Eintrag `desc === tag`; `{ ctx: ctx2 } = await boot()` → `log(ctx2).some(e => e.desc === tag)`; `log(ctx2).length <= 200`.
    8. `it('_loadExecLog überschreibt keine bereits im RAM liegenden Einträge (Merge)')`: `ctx = loadScript(['items.js'], { opener, setTimeout: () => 0 })` OHNE settle; sofort `exec(ctx, 'early', { desc: 'early-' + Date.now() })`; `await settle(50)`; der Eintrag ist weiterhin enthalten und steht NACH allen gespeicherten Einträgen (Index des `early`-Eintrags = Länge − 1).

    `describe('EXEC-Log: Anzeige im Tweaks-Panel')`:
    9. `it('_execLogFormat: HH:MM:SS · desc (len Zeichen)')`: `ctx._execLogFormat({ ts: Date.now(), desc: 'Outfit anwenden', len: 42 })` matcht `/^\d{2}:\d{2}:\d{2} · Outfit anwenden \(42 Zeichen\)$/`.
    10. `it('_renderExecLog: leer → Hinweistext; sonst neueste zuerst, max EXEC_LOG_SHOWN Zeilen')`: `evalIn(ctx, '_execLog.length = 0')`; `ctx._renderExecLog()` → `el.textContent === 'Noch kein EXEC gesendet'`; `exec(ctx, 'a', { desc: 'erster' })`; `exec(ctx, 'b', { desc: 'zweiter' })`; `ctx._renderExecLog()` → `el.textContent.indexOf('zweiter') < el.textContent.indexOf('erster')`; 40 weitere EXECs → `el.textContent.split('\n').length` ≤ `evalIn(ctx, 'EXEC_LOG_SHOWN') + 1` (Zeilen + Hinweis auf ältere) und der Hinweis enthält `ältere`.
    11. `it('Append aktualisiert die Anzeige automatisch')`: nach `exec(ctx, 'z', { desc: 'auto-render' })` ohne expliziten Render-Aufruf enthält `el.textContent` `auto-render`.

    RED bestätigen: `npx vitest run tests/exec-log.test.js` MUSS rot sein — erwartet `TypeError: ctx._saveExecLog is not a function` / `_renderExecLog` / `_execLogFormat`, `evalIn` auf `_execLog` wirft ReferenceError, Längen-Assertions rot. Ist die Datei unerwartet grün: STOPP.

    Commit: `git add tests/exec-log.test.js`; Message `test(03-03): add failing EXEC log tests – ring buffer, persistence, scan-data invariance, panel view (STAB-08)`. `git status --short items.js index.html` muss leer sein.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/exec-log.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "_saveExecLog|_execLog|_renderExecLog" "$OUT" && grep -Eq "Tests +[0-9]+ failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Vitest endete mit Exit 0 (EXEC-Log existiert bereits oder die Tests rufen es nicht auf), oder weder `_saveExecLog`/`_execLog`/`_renderExecLog` noch `failed` erscheinen in der Ausgabe</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "BC_ExecLog_v1" tests/exec-log.test.js` >= 1; `grep -c "LSCG_DB" tests/exec-log.test.js` >= 2; `grep -c "PROFILE_SCREENSHOTS" tests/exec-log.test.js` >= 1
    - `grep -c "_saveExecLog" tests/exec-log.test.js` >= 2; `grep -c "_renderExecLog" tests/exec-log.test.js` >= 2; `grep -c "_execLogFormat" tests/exec-log.test.js` >= 1; `grep -c "EXEC_LOG_MAX" tests/exec-log.test.js` >= 1; `grep -c "EXEC_LOG_SHOWN" tests/exec-log.test.js` >= 1
    - `grep -Fc "'Noch kein EXEC gesendet'" tests/exec-log.test.js` >= 1; `grep -c "execLogInfo" tests/exec-log.test.js` >= 1; `grep -c "settle(" tests/exec-log.test.js` >= 2
    - `test "$(grep -v '^\s*//' tests/exec-log.test.js | grep -c '\.only(')" = 0`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/exec-log.test.js`; `git status --short items.js index.html` leer
  </acceptance_criteria>
  <done>Elf Fälle in vier describe-Blöcken; Lauf nachweislich rot, weil das EXEC-Log fehlt; roter Test committet; items.js und index.html unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EXEC-Log-Block + Hook in `bcSend` (items.js) und Sektion `📜 EXEC-Log` (index.html), GREEN bestätigen, committen (STAB-08)</name>
  <precondition>`git status --short items.js index.html` gibt VOR dem Edit nichts aus — sonst STOPP und eskalieren</precondition>
  <reversibility rating="reversible">Additiver IDB-Schlüssel `BC_ExecLog_v1` (Betriebs-Telemetrie, von nichts anderem gelesen) plus eine Hook-Zeile und eine Panel-Sektion; Revert per `git revert`, der Schlüssel bleibt verwaist und harmlos liegen.</reversibility>
  <files>items.js, index.html</files>
  <read_first>items.js `function bcSend(msg, silent)` (per grep) und die Zeilen davor (`_bridgeSenderOk`, `manualReconnect` — Einfügeort des Blocks: direkt VOR `function _bridgeSenderOk`); items.js Zeilen 90-98 (`_debounce`, `_debouncedSaveCurseDB`); items.js `_speicherZeigeStatus`-Block inkl. Init-Hook (per `grep -n "_speicherZeigeStatus" items.js`); index.html `📊 Speicher`-Sektion (per `grep -n 'id="storageInfo"' index.html`, 5 Zeilen davor bis 3 danach); tests/exec-log.test.js (Task 1)</read_first>
  <behavior>
    - Test 1-5: Append-Vertrag in `bcSend` (desc/len/ts, Label-Vorrang, 60-Zeichen-Kürzung mit Whitespace-Kollaps, keine Einträge für Nicht-EXEC/ohne Opener, Cap 200 mit Verwerfen der ältesten)
    - Test 6: nach 250 EXECs + Save sind `LSCG_DB` und `PROFILE_SCREENSHOTS` in IDB unverändert
    - Test 7-8: `_saveExecLog` persistiert unter `BC_ExecLog_v1`; neue Sandbox lädt und mergt; früher EXEC vor IDB-Load bleibt erhalten und steht hinten
    - Test 9-11: Format `HH:MM:SS · desc (len Zeichen)`, Anzeige neueste zuerst mit Kappung auf `EXEC_LOG_SHOWN`, Leer-Hinweis, Auto-Render nach Append; volle Suite grün
  </behavior>
  <action>
    1. items.js — direkt VOR `function _bridgeSenderOk(ev)` (Bridge-Abschnitt) einen Block mit Sektionskommentar `// ── EXEC-Log (STAB-08) ──…` einfügen. Kommentar davor: „Betriebs-Telemetrie, KEIN Scan-Datensatz: der Ringpuffer verwirft nur eigene Log-Einträge (älteste zuerst), nie Outfits/Screenshots/Bots. Geloggt wird ausschließlich in bcSend — dem einzigen Sendepfad aller EXEC-Aufrufstellen.“ Inhalt:
       - `const EXEC_LOG_KEY = 'BC_ExecLog_v1';`, `const EXEC_LOG_MAX = 200;`, `const EXEC_LOG_SHOWN = 30;`, `const EXEC_LOG_DESC_LEN = 60;`, `let _execLog = [];`.
       - `function _execLogDesc(msg)`: liefert `String(msg.desc).trim()`, wenn `msg.desc` ein nicht-leerer String ist; sonst `String(msg.code || '').replace(/\s+/g, ' ').trim().slice(0, EXEC_LOG_DESC_LEN)`; leerer Code → `'(leer)'`.
       - `function _execLogAppend(msg)`: pusht `{ ts: Date.now(), desc: _execLogDesc(msg), len: String(msg.code || '').length }`; bei `_execLog.length > EXEC_LOG_MAX` per `_execLog.splice(0, _execLog.length - EXEC_LOG_MAX)` die ältesten verwerfen; dann `_debouncedSaveExecLog()` und `_renderExecLog()`.
       - `function _saveExecLog() { return idbSet(EXEC_LOG_KEY, _execLog); }` und `const _debouncedSaveExecLog = _debounce(_saveExecLog, 1200);` (Muster Befund 2).
       - `async function _loadExecLog()`: `const stored = await idbGet(EXEC_LOG_KEY);` — ist `stored` ein Array, dann `_execLog = stored.concat(_execLog).slice(-EXEC_LOG_MAX)` (Merge: gespeichert vor RAM, nie überschreiben — Befund 4c), `try/catch` mit `console.warn('[ExecLog] IDB load error:', e)`; abschließend `_renderExecLog()`.
       - `function _execLogFormat(e)`: `d = new Date(e.ts)`; `hh:mm:ss` je `String(x).padStart(2, '0')` aus `getHours/getMinutes/getSeconds`; Rückgabe `hh + ':' + mm + ':' + ss + ' · ' + e.desc + ' (' + e.len + ' Zeichen)'`.
       - `function _renderExecLog()`: `el = document.getElementById('execLogInfo')`, ohne `el` Return; leer → `el.textContent = 'Noch kein EXEC gesendet'`; sonst `shown = _execLog.slice(-EXEC_LOG_SHOWN).reverse()`, `el.textContent = shown.map(_execLogFormat).join('\n')` plus, falls `_execLog.length > shown.length`, eine Zusatzzeile `'… ' + (_execLog.length - shown.length) + ' ältere Einträge'`.
       - Init-Hook nach Konvention (Befund 3): `try { if (document.readyState !== 'loading') _loadExecLog(); else document.addEventListener('DOMContentLoaded', () => _loadExecLog()); } catch (e) {}`.
       Alle Bezeichner Englisch, Kommentare/Anzeigetexte Deutsch. `_bcOrigin`, `_bridgeSenderOk`, `manualReconnect` unverändert.

    2. items.js — in `bcSend(msg, silent)` genau EINE Zeile einfügen, nach `if (!silent || msg.type !== 'PING') console.log(...)` und vor `window.opener.postMessage(...)`: `if (msg.type === 'EXEC') _execLogAppend(msg); // STAB-08: einziger Sendepfad = einziger Log-Hakenpunkt`. Keine Aufrufstelle anfassen (Prohibition).

    3. index.html — per Edit-Tool mit exaktem old_string unmittelbar nach dem schließenden `</div>` der Sektion `📊 Speicher` (Befund 6) eine NEUE Sektion einfügen (Einrückung wie die Nachbarn): `<div>` / `<div class="tweaks-section-title">📜 EXEC-Log</div>` / `<div style="font-size:.6875rem;color:var(--text3);margin-bottom:7px">Jeder an das Spiel gesendete EXEC mit Zeitstempel und Kurzbeschreibung (Ringpuffer, 200 Einträge).</div>` / `<div class="tweaks-btn-group">` / `<button class="tweaks-btn" onclick="_renderExecLog()" title="EXEC-Log neu aufbauen">🔄 Aktualisieren</button>` / `</div>` / `<div id="execLogInfo" style="font-size:.6875rem;color:var(--text3);margin-top:5px;line-height:1.5;white-space:pre-wrap;max-height:180px;overflow:auto;font-family:monospace">–</div>` / `</div>`. Nichts löschen, nichts umstellen: `git diff --numstat 012c6d5 HEAD -- index.html` → Deletions `0`.

    4. `node --check items.js` → Exit 0. GREEN: `npx vitest run tests/exec-log.test.js` → 11 passed; `npm test` → 14 Dateien grün (Init-Hook bricht keinen Ladepfad; `idbGet` gegen fake-indexeddb ist in allen Dateien verfügbar).

    5. Commit: `git add items.js index.html`; `git diff --cached --name-only | sort` = `index.html`, `items.js`; Message `feat(03-03): log every EXEC in bcSend to a capped ring buffer (BC_ExecLog_v1) with Tweaks-panel view (STAB-08)`.

    6. Nachweis für das SUMMARY: `git show --stat --format= HEAD`, RED-/GREEN-Ausgabe, `git diff --numstat 012c6d5 HEAD -- index.html`, Aufrufstellen-Zählung (Befund 7) vorher/nachher identisch, und die Liste der End-of-Phase-Human-Checks (siehe `<verification>`).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/exec-log.test.js && node --check items.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` in der `Tests`-Summary — etwa weil der Hook vor dem Guard sitzt (Eintrag ohne Opener), die Rotation `shift` statt `splice` nutzt und bei Merge >200 lässt, `_loadExecLog` den RAM-Puffer überschreibt (Test 8), oder eine andere Testdatei durch den Init-Hook rot wird</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(git diff --numstat 012c6d5 HEAD -- index.html | awk '{print $2}')" = "0" && test "$(grep -Fc 'id="execLogInfo"' index.html)" = 1 && test "$(grep -Fc 'onclick="_renderExecLog()"' index.html)" = 1 && test "$(grep -Fc "const EXEC_LOG_KEY = 'BC_ExecLog_v1';" items.js)" = 1 && test "$(grep -c 'const EXEC_LOG_MAX' items.js)" = 1 && test "$(grep -Fc "if (msg.type === 'EXEC') _execLogAppend(msg);" items.js)" = 1 && awk '/^function bcSend[(]msg, silent[)]/{f=1} f&&/_execLogAppend[(]msg[)]/{h=1} f&&/window\.opener\.postMessage/{if(h){print "OK"}; exit} f&&/^}/{exit}' items.js | grep -q OK && test "$(git show HEAD -- items.js | grep -cE "^[-+].*type: *'EXEC'")" = 0 && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "index.html items.js " && echo EXECLOG-OK</automated>
    <fails_when>`EXECLOG-OK` fehlt — index.html hat Löschungen, Markup fehlt/doppelt, Konstanten fehlen, der Hook steht nicht vor dem `postMessage` in `bcSend`, eine bcSend-Aufrufstelle wurde verändert, oder der Commit enthält andere Pfade als index.html und items.js</fails_when>
    <human-check>End-of-Phase (nicht blockierend): Bookmarklet im BC-Tab neu ausführen, Tool neu laden, einen Bot starten → im ⚙️-Panel unter `📜 EXEC-Log` erscheint ein Eintrag `HH:MM:SS · (function(){… (N Zeichen)`; `🔄 Aktualisieren` baut die Liste neu auf; nach Tool-Reload sind die Einträge noch da (IDB).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "const EXEC_LOG_KEY = 'BC_ExecLog_v1';" items.js` = 1; `grep -c "const EXEC_LOG_MAX" items.js` = 1; `grep -c "const EXEC_LOG_SHOWN" items.js` = 1 (Werte 200/30 werden in Test 5/10 per `evalIn` geprüft)
    - `grep -Fc "function _execLogAppend(msg)" items.js` = 1; `grep -Fc "function _saveExecLog()" items.js` = 1; `grep -Fc "async function _loadExecLog()" items.js` = 1; `grep -Fc "function _renderExecLog()" items.js` = 1; `grep -Fc "function _execLogFormat(e)" items.js` = 1; `grep -Fc "_debounce(_saveExecLog, 1200)" items.js` = 1
    - `grep -Fc "if (msg.type === 'EXEC') _execLogAppend(msg);" items.js` = 1 (einziger Hook); `awk`-Gate: Hook steht in `bcSend` vor `window.opener.postMessage`
    - `grep -Fc "Noch kein EXEC gesendet" items.js` = 1; `grep -c "_loadExecLog" items.js` >= 3 (Definition, Direktaufruf, Listener-Aufruf)
    - `grep -Fc 'id="execLogInfo"' index.html` = 1; `grep -Fc "📜 EXEC-Log" index.html` = 1; `git diff --numstat 012c6d5 HEAD -- index.html | awk '{print $2}'` = `0`
    - `git show HEAD -- items.js | grep -cE "^[-+].*type: *'EXEC'"` = 0; `git log --format=%h 012c6d5..HEAD -- loader.js` zeigt nur den Commit aus 03-02 Task 2; `git diff 012c6d5 HEAD --stat -- bot-engine.js bot-ui.js` zeigt nur die Änderungen aus 03-02
    - `node --check items.js` → Exit 0; `npx vitest run tests/exec-log.test.js` → `11 passed`; `npm test` → `Test Files  14 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` = `index.html`, `items.js`; `git status --short` zeigt nur `?? .planning/milestone.lock`
  </acceptance_criteria>
  <done>EXEC-Log-Block und eine Hook-Zeile in items.js, Sektion im Tweaks-Panel (nur Einfügungen); EXEC-Log-Test (11) und volle Suite (14 Dateien) grün; Commit enthält genau items.js und index.html; Human-Checks der Phase im SUMMARY gelistet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool-Fenster → Spiel-Tab (`bcSend` EXEC) | Privilegierteste Operation; das Log ist der Audit-Trail dieser Grenze |
| RAM-Ringpuffer → IndexedDB `BC_ExecLog_v1` | Additiver Schlüssel; Rotation darf nur diesen Schlüssel betreffen |
| items.js Init-Hook → vm-Sandbox der Tests | `_loadExecLog()` läuft beim Laden in JEDER items.js-Testdatei; darf nie werfen und nie clobbern |
| index.html Markup → `onclick="_renderExecLog()"` | Statisches Markup, keine Nutzereingabe; `textContent`-Rendering (kein innerHTML) |
| Git-Arbeitsbaum → Repository | Zwei Produktionsdateien in einem Commit, index.html nur Einfügungen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-04 | Repudiation | `bcSend` EXEC ohne Audit-Trail | medium | mitigate | `_execLogAppend` in `bcSend` (einziger Sendepfad, Awk-Gate „Hook vor postMessage“); Persistenz `BC_ExecLog_v1`; Anzeige im Panel; Test 1-3, 7 |
| T-3-11 | Tampering | Rotation/Persistenz des Logs löscht oder überschreibt Scan-Daten (Kernwert-Verstoß) | critical | mitigate | Rotation ausschließlich per `splice` auf `_execLog`; einziger IDB-Schlüssel `EXEC_LOG_KEY`; Invarianz-Test 6 (`LSCG_DB`, `PROFILE_SCREENSHOTS` nach 250 EXECs + Save unverändert); Kommentar „Betriebs-Telemetrie, kein Scan-Datensatz“ |
| T-3-12 | Denial of Service | Init-Hook `_loadExecLog()` wirft beim Laden oder überschreibt frühe Einträge | high | mitigate | `try/catch` + `console.warn`; Merge statt Replace (Test 8); `npm test` beweist keinen roten Ladepfad |
| T-3-13 | Information Disclosure | `desc` speichert 60 Zeichen generierten Codes in IDB | low | accept | Code ist tool-generiert (keine Secrets), Einzelnutzer, lokal; Rendering per `textContent` (kein XSS-Vektor) |
| T-3-14 | Tampering | index.html-Edit zerstört Nachbarsektionen / `<script>`-Reihenfolge | medium | mitigate | Edit-Tool mit exaktem Anker; `git diff --numstat 012c6d5 HEAD -- index.html` Deletions = 0; `#execLogInfo` genau 1× |
| T-3-07 | Tampering | Git-Staging fremder Dateien | high | mitigate | Precondition `git status --short items.js index.html` leer; `git add` nur dieser zwei Pfade; Diff-Gate ohne `type: 'EXEC'`-Zeilen |
| T-3-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase |
</threat_model>

<verification>
- `npx vitest run tests/exec-log.test.js` → 11 passed; `npm test` → 14 Testdateien grün (Phase 1/2/3 komplett).
- `node --check items.js loader.js bot-engine.js bot-ui.js` grün; zwei Commits dieses Plans (test RED → feat GREEN); index.html gegenüber `012c6d5` nur Insertions.
- Phase-Gate (VALIDATION.md): volle Suite grün; statische Gates aus 03-01/03-02 weiterhin erfüllt (Wildcard 2/0/0/0, Origin-String 1/0, `_bridgeSenderOk` 3×).

## End-of-Phase Human-Checks (Erfolgskriterium 5, STAB-07, STAB-08 — nicht blockierend, im 03-03-SUMMARY listen; Orchestrator-Entscheidung 6)

Voraussetzung: Deploy auf GitHub Pages, dann im BC-Tab das Bookmarklet NEU ausführen (lädt loader.js mit Source-Pinning), Tool-Fenster neu laden (liest `TOOL_ORIGIN`), laufende Bots neu deployen.

1. **Live-Smoke-Test aller Bridge-Flows (Kriterium 5):** `⚡ Laden` → Cache erscheint (`CACHE_DATA`); Bot starten → Bot-Log im Tool (`BOT_LOG` über `_TOOL_ORIGIN`); Screenshot einer Outfit-Version aufnehmen → Bild erscheint (`SCREENSHOT_DATA`); Raum-Scan → Spielerliste aktualisiert (`PLAYER_DATA`). In BEIDEN Konsolen keine Meldungen „von fremder Quelle/Origin ignoriert“, „nicht gepinnter Quelle blockiert“ oder `SecurityError`.
2. **Sichtbarer Disconnect + Reconnect (STAB-07):** BC-Tab neu laden → nach ≤ 20 s zeigt `#connStatus` rot `Verbindung verloren`; Bookmarklet erneut ausführen → automatischer Reconnect (`Verbunden`, grün). Danach `🔄 Verbinden` klicken → kurz `Nicht verbunden`, dann wieder `Verbunden`.
3. **EXEC-Log sichtbar (STAB-08):** ⚙️ → `📜 EXEC-Log` zeigt nach dem Bot-Start einen Eintrag mit Uhrzeit und Code-Präfix; `🔄 Aktualisieren` und Tool-Reload behalten die Einträge (IDB).

STATE.md-Blocker „[Phase 3]: Nach Origin-Pinning Live-Smoke-Test …“ wird durch Check 1 aufgelöst — Ergebnis im SUMMARY und bei `/gsd-verify-work` festhalten.
</verification>

<success_criteria>
- Erfolgskriterium 4: Jeder EXEC-Aufruf erscheint mit Zeitstempel und Kurzbeschreibung in der Log-Ansicht `📜 EXEC-Log` im Tool; Log ist persistent (`BC_ExecLog_v1`), auf 200 Einträge begrenzt und verwirft nie Scan-Daten (STAB-08, Orchestrator-Entscheidung 5).
- Erfolgskriterium 5 und STAB-07 visuell: als End-of-Phase-Human-Checks dokumentiert, nicht blockierend (Orchestrator-Entscheidung 6).
</success_criteria>

<output>
Create `.planning/phases/03-bridge-haertung/03-03-SUMMARY.md` when done — inkl. RED-Ausgabe aus Task 1, GREEN-Ausgabe aus Task 2, `git show --stat` beider Commits, `git diff --numstat 012c6d5 HEAD -- index.html`, Aufrufstellen-Zählung vorher/nachher und der vollständigen Liste der End-of-Phase-Human-Checks (Live-Smoke-Test Cache/EXEC/Screenshot/Raum-Scan; Disconnect/Reconnect; EXEC-Log sichtbar) mit dem Hinweis „Bookmarklet neu ausführen, Tool neu laden, Bots neu deployen“.
</output>
