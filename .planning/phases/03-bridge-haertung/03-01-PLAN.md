---
phase: 03-bridge-haertung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/loadScript.js
  - tests/load-script.test.js
  - tests/bridge-protocol.test.js
  - items.js
autonomous: true
requirements: [TEST-07, STAB-04, STAB-07]
tags: [bridge, postmessage, origin, vm-sandbox, heartbeat, red-green]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Die vm-Sandbox aus `tests/helpers/loadScript.js` sammelt `addEventListener`-Registrierungen pro Typ in `sandbox._listeners` (Map) und kann sie per `dispatch(sandbox, type, event)` / `dispatchMessage(sandbox, data, {origin, source})` mit einem synthetischen Event aufrufen; `removeEventListener` entfernt genau den übergebenen Handler; `location.origin` ist `https://tool.test` (Export `SANDBOX_ORIGIN`) und per `extraGlobals` überschreibbar. Alle zehn bestehenden Testdateien bleiben grün (TEST-07 Wave 0, RESEARCH Pitfall 3)"
    - "Tool-Seite, Trust-on-first-use (Orchestrator-Entscheidung 1): `_bcOrigin` wird beim ERSTEN gültigen Message (`ev.source === window.opener`, Origin nicht `'null'`) gelernt; jede spätere Nachricht muss `ev.source === window.opener` UND `ev.origin === _bcOrigin` erfüllen (`_bridgeSenderOk(ev)`), sonst wird sie mit `console.warn` verworfen — beobachtbar daran, dass `_lastMsgTs` unverändert bleibt und kein `case` läuft. Der temporäre Debug-Listener in `debugOsOutfit` prüft denselben Absender-Helfer"
    - "STAB-04: `bcSend` sendet an `_bcOrigin || '*'` — nach dem Handshake also gezielt an den gelernten Spiel-Origin; die zwei Bootstrap-PINGs (`startPingRetry`, Start-IIFE) bleiben als dokumentierte, getestete Ausnahme mit `'*'` (Orchestrator-Entscheidung 1, RESEARCH Pitfall 2) — statischer Audit: genau 2 Zeilen `window.opener.postMessage(…, '*')`, beide `type: 'PING'`; genau 1 Zeile `_bcOrigin || '*'`"
    - "STAB-07: `_heartbeatCheck()` (aus dem 5-s-Intervall extrahiert) markiert die Verbindung nach >15 s Stille als verloren (`_connected=false`, `#connStatus` → `Verbindung verloren` / `data-conn=off`, `startPingRetry()`); `manualReconnect()` setzt zusätzlich `_bcOrigin = null`, zeigt `Nicht verbunden`, sendet PING und lernt beim nächsten Handshake einen (ggf. neuen) Origin — der bestehende Button `🔄 Verbinden` (`#reconnectBtn`, index.html) bleibt der Reconnect-Pfad"
    - "Annahme (Edge, Orchestrator-Probe): Wechselt BC mitten in der Session den Origin (Mirror-Wechsel), werden die neuen Nachrichten verworfen, der Heartbeat läuft ab, die Statusanzeige wird rot; der automatische Ping-Retry KANN den neuen Origin NICHT übernehmen (bewusst: nur der Nutzer darf per `🔄 Verbinden` neu vertrauen) — erst `manualReconnect()` lernt neu. Nachricht vor dem Handshake mit unbekanntem Origin: wird angenommen und der Origin gelernt, sofern die Quelle der Opener ist (TOFU)"
    - "Vor der Implementierung ist `tests/bridge-protocol.test.js` nachweislich rot (`_bridgeSenderOk`/`_heartbeatCheck` fehlen, Fremd-Origin wird noch verarbeitet, `manualReconnect` setzt `_bcOrigin` nicht zurück), danach grün; RED und GREEN in getrennten Commits; GREEN-Commit enthält nur items.js"
  artifacts:
    - path: "tests/helpers/loadScript.js"
      provides: "Listener-Registry (`_listeners`), `dispatch`, `dispatchMessage`, `SANDBOX_ORIGIN`, `location`-Stub in `makeSandbox`"
      contains: "export function dispatchMessage("
    - path: "tests/load-script.test.js"
      provides: "Vier neue Fälle: Registry+dispatch, removeEventListener, items.js registriert `message`-Handler, `location.origin` Default/Override"
      contains: "_listeners"
    - path: "tests/bridge-protocol.test.js"
      provides: "TEST-07: Absender-Doppelprüfung (6 Fälle inkl. Debug-Listener), Handler-Dispatch nach Typ (5), bcSend-Sendepfad + statischer PING-Audit (5), Verbindungsverlust/Reconnect (5)"
      contains: "_heartbeatCheck"
    - path: "items.js"
      provides: "`_bridgeSenderOk(ev)`, TOFU-Lernen von `_bcOrigin`, `_heartbeatCheck()`, `_bcOrigin = null` in `manualReconnect()`, Kommentare zur Bootstrap-Ausnahme"
      contains: "function _bridgeSenderOk(ev)"
  key_links:
    - from: "tests/bridge-protocol.test.js"
      to: "items.js `window.addEventListener('message', …)` (Haupt-Handler, ~Zeile 5960)"
      via: "`dispatchMessage(ctx, data, { origin, source })` ruft die in `ctx._listeners.get('message')` gesammelten Handler mit `{data, origin, source}` auf — ohne die Registry aus Task 1 wäre der Handler beim Laden verworfen worden (falsches Grün)"
      pattern: "dispatchMessage"
    - from: "items.js Haupt-Handler und Debug-Listener (`debugOsOutfit`)"
      to: "items.js `_bridgeSenderOk(ev)`"
      via: "Beide Empfangspfade rufen denselben Helfer; `_bcOrigin` ist die einzige Tool-seitige Origin-Variable"
      pattern: "_bridgeSenderOk[(]ev[)]"
    - from: "items.js `_heartbeatCheck`"
      to: "items.js `setInterval(_heartbeatCheck, 5000)`"
      via: "Extraktion des Intervall-Callbacks in eine benannte `function`, damit der Test ihn ohne Timer direkt aufrufen kann (Sandbox-`setInterval` ist ein No-op)"
      pattern: "setInterval[(]_heartbeatCheck, 5000[)]"
    - from: "index.html `#reconnectBtn` (`onclick=\"manualReconnect()\"`, ~Zeile 2369) und `#connStatus` (~2365)"
      to: "items.js `manualReconnect()` / `_heartbeatCheck()`"
      via: "Bereits verdrahtet (RESEARCH State of the Art) — diese Phase ergänzt nur den `_bcOrigin`-Reset und testet den Statuswechsel"
      pattern: "manualReconnect"
  prohibitions:
    - requirement_id: STAB-04
      category: safety
      verification: automated
      statement: "Kein weiterer direkter `window.opener.postMessage(…, '*')` außer den zwei Bootstrap-PINGs; kein Wildcard-Ziel in `bcSend` außer dem Fallback `_bcOrigin || '*'` vor dem ersten Handshake"
    - requirement_id: TEST-07
      category: safety
      verification: automated
      statement: "Die Sandbox-Erweiterung darf keine der zehn bestehenden Testdateien rot machen (`npm test` nach Task 1 = 10 Dateien grün); keine Änderung an Produktionsdateien in Task 1"
    - requirement_id: STAB-07
      category: safety
      verification: judgment
      statement: "Kein automatischer `_bcOrigin`-Reset im Heartbeat-/Ping-Retry-Pfad — nur `manualReconnect()` darf neu vertrauen (sonst wäre die Origin-Erzwingung durch einen navigierten Opener umgehbar)"
    - requirement_id: STAB-04
      category: safety
      verification: judgment
      statement: "Kein Build-Schritt, keine neue Dependency, kein `bridge.js` (Extraktion ist Phase 4 / SPLIT-02); Änderungen an Ort und Stelle in items.js; loader.js, bot-engine.js, bot-ui.js, index.html bleiben in diesem Plan unberührt"
    - requirement_id: TEST-07
      category: safety
      verification: judgment
      statement: "`git add` nur mit explizit genannten Pfaden (nie `-A`, `.`, `commit -a`); `.planning/milestone.lock` und andere untracked Dateien nie stagen; RED-Test und GREEN-Fix in getrennten Commits; keine Watch-Flags (`vitest run`)"
---

<objective>
Tool-seitige Bridge-Härtung mit Testnetz: Zuerst wird die vm-Sandbox (`tests/helpers/loadScript.js`) so erweitert, dass in `items.js` registrierte `message`-Handler tatsächlich gesammelt und mit simulierten Nachrichten aufgerufen werden können (Wave 0 für TEST-07 — heute ist `addEventListener` ein No-op, RESEARCH Pitfall 3). Darauf entsteht `tests/bridge-protocol.test.js` (RED): Absender-Doppelprüfung Origin+Source mit Trust-on-first-use, Handler-Dispatch nach Nachrichtentyp, `bcSend`-Sendepfad mit gelerntem Origin und dokumentierter Bootstrap-Ausnahme, Heartbeat-Verlust und manueller Reconnect. Danach der GREEN-Fix in `items.js`: `_bridgeSenderOk(ev)` als gemeinsame Prüfung beider Empfangspfade, Origin nur einmal lernen und danach erzwingen, `_heartbeatCheck()` als benannte Funktion, `manualReconnect()` setzt `_bcOrigin` zurück.

Purpose: Erfolgskriterien 2 und 3 — die Tool-Seite prüft beide Absender-Merkmale (Orchestrator-Entscheidung 1 + 3, Tool-Hälfte), Verbindungsverlust ist sichtbar und Reconnect funktioniert auch nach Origin-Wechsel; alles mit simulierten Nachrichten getestet (TEST-07, Tool-Seite; Loader-Sandbox ist per Entscheidung 4 auf Phase 5 verschoben). STAB-04 ist laut RESEARCH bereits zu ~90 % umgesetzt (`bcSend` nutzt `_bcOrigin`) — dieser Plan verifiziert und dokumentiert die zwei Bootstrap-Ausnahmen per statischem Audit statt sie umzubauen.
Output: `tests/helpers/loadScript.js` (erweitert), `tests/load-script.test.js` (4 neue Fälle), `tests/bridge-protocol.test.js` (neu, ~21 Fälle), `items.js` (Helfer + drei chirurgische Änderungen).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bridge-haertung/03-RESEARCH.md
@.planning/phases/03-bridge-haertung/03-VALIDATION.md
@.planning/phases/01-testfundament/01-02-SUMMARY.md

Planer-Probe (2026-09-13, HEAD `012c6d5`) — verbindliche Befunde:
1. `loadScript(['items.js', 'bot-data.js', 'bot-ui.js', 'bot-engine.js'], { opener: { closed: false, postMessage: vi.fn() } })` lädt ohne Fehler. Die Start-IIFE in items.js (~Zeile 6774, `// ── Auto-load + initial PING`) sendet dabei SYNCHRON genau einen Bootstrap-PING: `opener.postMessage` wird einmal mit `[{ app: 'BCKonfigurator', type: 'PING' }, '*']` aufgerufen — Tests müssen das als STAB-04-Ausnahme assertieren und danach `mockClear()` aufrufen.
2. `window === sandbox` → `window.opener` ist `sandbox.opener` (per `extraGlobals`); `window.location` ist bis Task 1 `undefined`.
3. `let`-Variablen (`_bcOrigin`, `_connected`, `_lastMsgTs`, `_playerAbgelehnt`) sind keine Sandbox-Properties — lesen/schreiben nur per `evalIn(ctx, '_bcOrigin')` bzw. `evalIn(ctx, '_lastMsgTs = 0')`. `function`-Deklarationen (`bcSend`, `manualReconnect`, `startPingRetry`, `showStatus`) sind Sandbox-Properties und können per Zuweisung (`ctx.startPingRetry = vi.fn()`) überschrieben werden — Aufrufe im Produktionscode lösen den Namen zur Laufzeit über das Global auf, der Spy greift.
4. `logPush` (bot-ui.js) und `_handlePosData` (bot-ui.js) sowie `_moneyApply` (money.js) existieren nicht, wenn nur items.js geladen ist → für Dispatch-Tests per `extraGlobals` als `vi.fn()` injizieren.
5. `document.getElementById` liefert je Aufruf einen frischen Stub — für inspizierbare Elemente (`connStatus`, `statusMsg`) `ctx.document.getElementById` überschreiben (Muster `captureInfo` aus tests/storage-estimate.test.js).
6. `setInterval` ist in der Sandbox ein No-op (`() => 0`); `setTimeout` ist das echte Host-`setTimeout`. Der PONG-Pfad registriert 3-s- und 12-s-Folgetimer — in Bridge-Tests `setTimeout: () => 0, clearTimeout: () => {}` per `extraGlobals` mitgeben, damit nach Testende nichts mehr feuert (macht auch `_debounce` zum No-op — für diesen Plan unerheblich).
7. Zeilenangaben aus RESEARCH gelten für HEAD `012c6d5` (items.js 11 780 Zeilen): `APP` 5853, `_bcOrigin` 5861, `startPingRetry` 5863-5873 (PING `'*'` in 5871), Heartbeat-Intervall 5880-5892, `manualReconnect` 5904-5914, `bcSend` 5916-5933 (Send 5926), Haupt-Handler 5960-6299 (Source-Check 5967, Origin-Lernen 5974, `_lastMsgTs` 5975), Debug-Listener `debugOsOutfit` 7975-7998, Start-IIFE-PING 6776. Immer per grep neu lokalisieren.
8. Baseline: `npm test` = 10 Dateien, 97 passed + 2 expected fail. Grep-Baseline items.js: `grep -cE "window\.opener\.postMessage\(.*, '\*'\)"` = 2; `grep -Fc "_bcOrigin || '*'"` = 1; `grep -c "_bcOrigin = ev.origin"` = 1.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 3 gesamt; Plan-Zuordnung in Klammern)

| Pfad | Symbole / Inhalt | Req | Plan |
|------|------------------|-----|------|
| `tests/helpers/loadScript.js` (erweitert) | `_listeners`-Registry, `dispatch`, `dispatchMessage`, `SANDBOX_ORIGIN`, `location`-Stub | TEST-07 (W0) | 03-01 |
| `tests/load-script.test.js` (erweitert) | 4 Fälle: Registry, remove, items.js-Handler registriert, `location` Default/Override | TEST-07 (W0) | 03-01 |
| `tests/bridge-protocol.test.js` (neu) | Absender-Doppelprüfung, Dispatch nach Typ, bcSend-Origin + statischer PING-Audit, Heartbeat/Reconnect | TEST-07, STAB-04, STAB-07 | 03-01 |
| `items.js` `_bridgeSenderOk(ev)`, TOFU-Lernen, `_heartbeatCheck()`, `_bcOrigin = null` in `manualReconnect()` | Tool-Hälfte Origin+Source, sichtbarer Verlust, Reconnect | STAB-04, STAB-06 (Tool), STAB-07 | 03-01 |
| `tests/injected-code-origin.test.js` (neu) | Behavioral: `debugOsOutfit`, `bcKeys`, `_buildBotCode` senden an `https://tool.test`; statischer Audit der 36 Stellen + einzige `TOOL_ORIGIN`-Definition | STAB-05, STAB-06 | 03-02 |
| `tests/loader-origin.test.js` (neu) | Statisch: Origin-String 1× in loader.js, `ALLOWED_ORIGIN` abgeleitet, Source-Pinning-Guard vorhanden | STAB-06 | 03-02 |
| `tests/bot-engine-escaping.test.js` (Sandbox +`TOOL_ORIGIN`) | Minimal-Sandbox liefert das Global, das `_buildBotCode` ab Plan 02 braucht | STAB-05 | 03-02 |
| `items.js` `const TOOL_ORIGIN = window.location.origin;` + 17 Stellen; `bot-engine.js` `_TOOL_ORIGIN`-Kopfzeile im generierten Code + 18 Stellen; `bot-ui.js` 1 Stelle | Injizierter Code sendet an den Tool-Origin | STAB-05 | 03-02 |
| `loader.js` `ALLOWED_ORIGIN = new URL(POPUP_URL).origin` + Source-Pinning-Guard (+5/-1 Zeilen) | Loader-Hälfte Origin+Source, eine Origin-Quelle | STAB-06 | 03-02 |
| `tests/exec-log.test.js` (neu) | Ringpuffer `{ts, desc, len}`, Cap 200, Persistenz `BC_ExecLog_v1`, Scan-Daten-Invarianz, Anzeige | STAB-08 | 03-03 |
| `items.js` EXEC-Log-Block (`EXEC_LOG_KEY`, `_execLogAppend`, `_saveExecLog`, `_loadExecLog`, `_renderExecLog`) + Hook in `bcSend`; `index.html` Sektion `📜 EXEC-Log` (nur Einfügungen) | Jeder EXEC protokolliert und einsehbar | STAB-08 | 03-03 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Sandbox-Listener-Registry + `location`-Stub in `tests/helpers/loadScript.js`, Ladetest erweitern, alle bestehenden Tests grün halten (TEST-07 Wave 0)</name>
  <precondition>`git status --short` zeigt außer `?? .planning/milestone.lock` nichts; `npm test` = 10 Dateien grün (97 passed + 2 expected fail)</precondition>
  <files>tests/helpers/loadScript.js, tests/load-script.test.js</files>
  <read_first>tests/helpers/loadScript.js komplett (Header-Kommentar Zeilen 1-9 erwähnt „bare addEventListener“ — anpassen; `makeSandbox` Zeilen 85-135: `document.addEventListener` No-op bleibt, das Sandbox-`addEventListener` in Zeile ~121 wird ersetzt; `extraGlobals`-Spread zuletzt); tests/load-script.test.js komplett (7 Fälle, Stil, `beforeAll`-ctx); .planning/phases/03-bridge-haertung/03-RESEARCH.md Abschnitt „Pitfall 3“ und „Wave 0 Gaps“</read_first>
  <action>
    1. `tests/helpers/loadScript.js`:
       - Neuer Export `export const SANDBOX_ORIGIN = 'https://tool.test';`.
       - In `makeSandbox`: vor dem `sandbox`-Literal eine `const listeners = new Map();` anlegen (Typ → Array von Handlern). Im Literal das bisherige `addEventListener() {}` / `removeEventListener() {}` des Sandbox-Objekts (NICHT die des `document`-Stubs) ersetzen durch: `_listeners: listeners`; `addEventListener(type, fn)` — ignoriert Nicht-Funktionen, legt bei Bedarf das Array an, pusht `fn` (Options-Argument wird ignoriert); `removeEventListener(type, fn)` — entfernt genau die erste identische Referenz aus dem Array, sonst No-op.
       - Im Literal zusätzlich einen `location`-Stub VOR dem `...extraGlobals`-Spread (damit Tests ihn überschreiben können): `{ origin: SANDBOX_ORIGIN, href: SANDBOX_ORIGIN + '/bc-configurator/', protocol: 'https:', host: 'tool.test', hostname: 'tool.test', pathname: '/bc-configurator/', search: '', hash: '' }`. Begründung im Kommentar: Plan 03-02 führt `const TOOL_ORIGIN = window.location.origin;` in items.js ein — ohne Stub würde JEDE items.js-Testdatei beim Laden mit TypeError sterben.
       - Neue Exporte nach `evalIn`: `export function dispatch(sandbox, type, event)` — holt `sandbox._listeners.get(type)`, iteriert über eine KOPIE (`slice()`), weil Handler sich während des Dispatch selbst entfernen (Muster `debugOsOutfit`), ruft `fn.call(sandbox, event)` und gibt die Anzahl aufgerufener Handler zurück (0 wenn keine registriert). `export function dispatchMessage(sandbox, data, opts = {})` — Komfort-Wrapper: `dispatch(sandbox, 'message', { data, origin: opts.origin, source: opts.source })`.
       - Header-Kommentar: den Satz zu „bare addEventListener“ durch einen Satz zur Registry + `dispatch` ersetzen (Phase 3, TEST-07) und den `location`-Stub erwähnen. Datei bleibt ESM, LF.
    2. `tests/load-script.test.js` — Import um `dispatch`, `SANDBOX_ORIGIN` ergänzen (`vi` aus vitest importieren) und vier Fälle anhängen:
       - `it('addEventListener sammelt Handler pro Typ; dispatch ruft sie mit dem Event auf')`: `const s = loadScript([]); const fn = vi.fn(); s.addEventListener('message', fn); expect(dispatch(s, 'message', { data: 1 })).toBe(1); expect(fn).toHaveBeenCalledWith({ data: 1 }); expect(dispatch(s, 'keydown', {})).toBe(0);`
       - `it('removeEventListener entfernt genau den übergebenen Handler')`: zwei Handler registrieren, einen entfernen, dispatch → Rückgabe 1, nur der verbliebene wurde aufgerufen.
       - `it('items.js registriert seinen message-Handler in der Registry')`: mit dem `beforeAll`-`ctx`: `expect(ctx._listeners.get('message').length).toBeGreaterThanOrEqual(1)`; jeder Eintrag `typeof === 'function'`.
       - `it('location.origin ist gesetzt und per extraGlobals überschreibbar')`: `loadScript([]).location.origin === SANDBOX_ORIGIN`; `loadScript([], { location: { origin: 'https://x.test' } }).location.origin === 'https://x.test'`; `evalIn(loadScript([]), 'window.location.origin') === SANDBOX_ORIGIN`.
    3. `npx vitest run tests/load-script.test.js` → 11 passed; `npm test` → 10 Dateien grün (101 passed + 2 expected fail) — beweist, dass die Registry keinen bestehenden Test bricht (die registrierten Handler werden nur gesammelt, nie automatisch aufgerufen).
    4. Commit: `git add tests/helpers/loadScript.js tests/load-script.test.js`; `git diff --cached --name-only | sort` = genau diese zwei Pfade; Message `test(03-01): sandbox listener registry, dispatch helper and location stub for bridge tests (TEST-07)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/load-script.test.js && npm test</automated>
    <fails_when>Exit ≠ 0, `failed` in der `Tests`-Summary, weniger als `11 passed` für tests/load-script.test.js oder weniger als `10 passed` Testdateien in `npm test` — insbesondere wenn eine bestehende Datei durch die Registry (z. B. Handler, der beim Laden unerwartet aufgerufen wird) rot wird</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "export const SANDBOX_ORIGIN = 'https://tool.test';" tests/helpers/loadScript.js` = 1
    - `grep -Fc "export function dispatch(sandbox, type, event)" tests/helpers/loadScript.js` = 1; `grep -Fc "export function dispatchMessage(sandbox, data, opts = {})" tests/helpers/loadScript.js` = 1
    - `grep -c "_listeners" tests/helpers/loadScript.js` >= 2; `grep -Fc "location: {" tests/helpers/loadScript.js` = 1
    - `grep -o 'addEventListener() {}' tests/helpers/loadScript.js | wc -l` → 2 (nur noch die zwei Element-/document-Stubs; der Sandbox-No-op ist weg)
    - `grep -c "_listeners" tests/load-script.test.js` >= 1; `grep -c "dispatch(" tests/load-script.test.js` >= 3; `grep -c "SANDBOX_ORIGIN" tests/load-script.test.js` >= 2
    - `npx vitest run tests/load-script.test.js` → `11 passed`; `npm test` → `Test Files  10 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD | sort | tr '\n' ' '` = `tests/helpers/loadScript.js tests/load-script.test.js `; `git status --short items.js loader.js bot-engine.js bot-ui.js index.html` leer
  </acceptance_criteria>
  <done>Sandbox sammelt Listener und kann sie dispatchen, `location.origin` ist stubbar; vier neue Ladetests grün; volle Suite (10 Dateien) grün; Commit enthält genau die zwei Testpfade.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge-Protokoll-Test schreiben und RED bestätigen — `tests/bridge-protocol.test.js` (TEST-07, STAB-04, STAB-06 Tool-Hälfte, STAB-07)</name>
  <precondition>Task 1 committet; `git status --short` außer `.planning/milestone.lock` leer</precondition>
  <files>tests/bridge-protocol.test.js</files>
  <read_first>items.js Bridge-Abschnitt per `grep -n "POSTMESSAGE KOMMUNIKATION" items.js` bis `function loadCacheFromBC` (Ping-Retry, Heartbeat, `manualReconnect`, `bcSend`, Haupt-Handler mit `case 'PONG'`, `'BOT_LOG'`, `'POS_DATA'`, `'EXEC_OK'`, `'EXEC_ERR'`); items.js `window.debugOsOutfit` (per `grep -n "debugOsOutfit = function" items.js`, Handler-Guard und `reqId = 'dbg_' + Date.now()`); tests/storage-estimate.test.js (Muster `captureInfo`, `extraGlobals`); tests/delete-confirmation.test.js ab Zeile ~130 (statischer Quell-Audit per `fs.readFileSync`); tests/helpers/loadScript.js (neue Exporte aus Task 1)</read_first>
  <action>
    `tests/bridge-protocol.test.js` (neu, ESM): Imports `describe, it, expect, vi` aus vitest, `fs`/`path` aus node, `loadScript, evalIn, dispatchMessage, makeElementStub, REPO_ROOT` aus `./helpers/loadScript.js`.

    Helfer oben in der Datei:
    - `BC = 'https://bc.test'`, `BC2 = 'https://bc2.test'`, `EVIL = 'https://evil.example'`, `APP = 'BCKonfigurator'`.
    - `mkOpener()` → `{ closed: false, postMessage: vi.fn() }`.
    - `boot(extra = {})` → `const opener = mkOpener(); const els = { connStatus: makeElementStub(), statusMsg: makeElementStub() }; const ctx = loadScript(['items.js'], { opener, setTimeout: () => 0, clearTimeout: () => {}, logPush: vi.fn(), _handlePosData: vi.fn(), ...extra }); ctx.document.getElementById = id => els[id] || makeElementStub(); return { ctx, opener, els };` (Befunde 1-6). Der Bootstrap-PING liegt danach als erster Aufruf in `opener.postMessage.mock.calls`.
    - `msg(type, extra = {})` → `{ app: APP, type, ...extra }`; `send(ctx, data, origin, source)` → `dispatchMessage(ctx, data, { origin, source })`.
    - `handshake({ ctx, opener })` → `send(ctx, msg('PONG'), BC, opener); opener.postMessage.mockClear();`.
    - `lastTs(ctx)` → `evalIn(ctx, '_lastMsgTs')`; `resetTs(ctx)` → `evalIn(ctx, '_lastMsgTs = 0')`.

    `describe('Absender-Doppelprüfung: Origin + Source, Trust-on-first-use (STAB-06 Tool-Seite)')`:
    1. `it('lernt den Spiel-Origin beim ersten gültigen Handshake')`: `boot()`; `evalIn(ctx,'_bcOrigin')` ist `null`; `send(PONG, BC, opener)` gibt ≥1 zurück; danach `_bcOrigin === BC` und `_connected === true`.
    2. `it('verwirft Nachrichten, deren source nicht der Opener ist – auch mit korrektem Origin')`: nach `handshake`; `resetTs`; `send(msg('EXEC_OK'), BC, {})` → `lastTs === 0`, `els.statusMsg.textContent === ''`.
    3. `it('verwirft Nachrichten von fremdem Origin nach dem Handshake – auch mit korrekter source')`: nach `handshake`; `resetTs`; `send(msg('EXEC_OK'), EVIL, opener)` → `lastTs === 0`, `statusMsg` leer, `_bcOrigin === BC` (nicht umgelernt).
    4. `it('ohne opener wird nichts angenommen')`: `loadScript(['items.js'])` ohne `opener`; `resetTs`; `send(msg('PONG'), BC, {})` → `lastTs === 0`, `_connected === false`.
    5. `it('_bridgeSenderOk ist die gemeinsame Prüfung: vor dem Lernen zählt nur die Quelle, danach Quelle UND Origin')`: `boot()`; `ctx._bridgeSenderOk({ source: opener, origin: BC })` true; `({ source: {}, origin: BC })` false; nach `handshake`: `({ source: opener, origin: BC })` true, `({ source: opener, origin: EVIL })` false, `({ source: {}, origin: BC })` false.
    6. `it('auch der temporäre Debug-Listener von debugOsOutfit prüft den Absender')`: nach `handshake`; `evalIn(ctx, "LSCG_DB['1'] = { versions: [{ code: 'abc' }] }")`; `const before = ctx._listeners.get('message').length; ctx.debugOsOutfit('1', 0);` → Länge `before + 1`; `reqId` aus dem gesendeten EXEC-Code per Regex `/reqId:"(dbg_\d+)"/` lesen; `send(msg('OUTFIT_DEBUG_RESULT', { reqId, total: 0, missing: [], naked: [], missingNaked: [], assetFamily: 'F' }), EVIL, opener)` → Länge weiterhin `before + 1` (Handler blieb registriert, hat nichts angenommen); dieselbe Nachricht von `BC` → Länge `before` (Handler hat sich nach Annahme entfernt).

    `describe('Handler-Dispatch nach Nachrichtentyp (TEST-07)')`:
    1. `it('PONG: verbunden, Status grün, GET_PLAYER geht an den gelernten Origin')`: `boot()`; `opener.postMessage.mockClear()`; `send(PONG, BC, opener)`; `_connected === true`; `els.connStatus.textContent === 'Verbunden'`, `.dataset.conn === 'on'`; `opener.postMessage` wurde mit `[{ app: APP, type: 'GET_PLAYER' }, BC]` aufgerufen (`toHaveBeenCalledWith`).
    2. `it('BOT_LOG wird an logPush durchgereicht')`: nach `handshake`; `send(msg('BOT_LOG', { entry: { t: 1 } }), BC, opener)`; `ctx.logPush` mit `{ t: 1 }` aufgerufen.
    3. `it('POS_DATA wird an _handlePosData durchgereicht')`: `ctx._handlePosData` mit dem `data`-Objekt aufgerufen.
    4. `it('EXEC_OK / EXEC_ERR setzen die Statusmeldung')`: `EXEC_OK` → `els.statusMsg.textContent === '✅ Ausgeführt!'`; `EXEC_ERR` mit `msg: 'boom'` → `'❌ Fehler: boom'`.
    5. `it('unbekannter Typ wird angenommen, aber ohne Wirkung (kein Throw)')`: `resetTs`; `expect(() => send(msg('NOPE'), BC, opener)).not.toThrow()`; `lastTs > 0`; `statusMsg` leer. Zusätzlich: Nachricht ohne `app` (`{ type: 'PONG' }`) → `lastTs` bleibt 0.

    `describe('Sendepfad bcSend nutzt den gelernten Origin (STAB-04)')`:
    1. `it('Bootstrap-PING beim Laden geht an "*" – dokumentierte Ausnahme vor dem Handshake')`: `boot()`; `opener.postMessage.mock.calls[0]` deep-equals `[{ app: APP, type: 'PING' }, '*']`.
    2. `it('vor dem Handshake sendet bcSend an "*"')`: `ctx.bcSend({ type: 'GET_PLAYER' }, true)` → true; letzter Aufruf Ziel `'*'`.
    3. `it('nach dem Handshake sendet bcSend an den gelernten Spiel-Origin')`: Ziel `BC`; Payload `{ app: APP, type: 'GET_PLAYER' }`.
    4. `it('ohne opener liefert bcSend false und wirft nicht')`: `loadScript(['items.js'])`; `bcSend({ type: 'GET_PLAYER' }, true) === false`.
    5. `it('statischer Audit: genau zwei direkte Wildcard-Sends, beide Bootstrap-PING; genau ein Fallback in bcSend')`: `fs.readFileSync(path.join(REPO_ROOT, 'items.js'), 'utf8').split('\n')`; Zeilen, die `/window\.opener\.postMessage\(.*, '\*'\)/` matchen → Länge 2, jede enthält `type: 'PING'`; Zeilen mit `_bcOrigin || '*'` → Länge 1.

    `describe('Verbindungsverlust und Reconnect (STAB-07)')`:
    1. `it('_heartbeatCheck markiert die Verbindung nach 15 s Stille als verloren')`: nach `handshake`; `ctx.startPingRetry = vi.fn()`; `evalIn(ctx, '_lastMsgTs = Date.now() - 20000')`; `ctx._heartbeatCheck()`; `_connected === false`; `els.connStatus.textContent === 'Verbindung verloren'`, `.dataset.conn === 'off'`; `startPingRetry` einmal aufgerufen.
    2. `it('_heartbeatCheck lässt eine lebendige Verbindung in Ruhe')`: `_lastMsgTs = Date.now()`; nach dem Aufruf `_connected === true`, `startPingRetry` nicht aufgerufen.
    3. `it('manualReconnect setzt _bcOrigin zurück, zeigt "Nicht verbunden" und sendet PING an "*"')`: nach `handshake`; `ctx.manualReconnect()`; `_bcOrigin === null`; `_connected === false`; `els.connStatus.textContent === 'Nicht verbunden'`, `.dataset.conn === 'off'`; `opener.postMessage` mit `[{ app: APP, type: 'PING' }, '*']` aufgerufen.
    4. `it('nach manualReconnect wird ein neuer Origin gelernt (Mirror-Wechsel)')`: nach 3.: `send(PONG, BC2, opener)` → `_bcOrigin === BC2`, `_connected === true`.
    5. `it('Origin-Wechsel ohne Reconnect bleibt gesperrt – erst 🔄 Verbinden vertraut neu')`: nach `handshake` (BC); `resetTs`; `send(PONG, BC2, opener)` → `_bcOrigin === BC`, `lastTs === 0`.

    RED bestätigen: `npx vitest run tests/bridge-protocol.test.js` MUSS rot sein. Erwartet rot: Doppelprüfung 3 (Fremd-Origin wird heute umgelernt und verarbeitet), 5 und Debug-Listener 6 (`_bridgeSenderOk` fehlt → TypeError), Heartbeat 1-2 (`_heartbeatCheck` fehlt), Reconnect 3-5 (`_bcOrigin` wird nicht zurückgesetzt bzw. umgelernt). Erwartet bereits grün: Source-Check, Dispatch, bcSend-Fälle, statischer Audit (Regressionsschutz). Ist die Datei komplett grün: STOPP und eskalieren.

    Commit: `git add tests/bridge-protocol.test.js`; Message `test(03-01): add failing bridge protocol tests – origin+source check, dispatch, heartbeat, reconnect (TEST-07, STAB-04, STAB-07)`. `git status --short items.js` muss leer sein (im SUMMARY festhalten).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/bridge-protocol.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -q "_heartbeatCheck" "$OUT" && grep -q "_bridgeSenderOk" "$OUT" && grep -Eq "Tests +[0-9]+ failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Vitest endete mit Exit 0 (Helfer existieren schon oder die Tests rufen sie nicht auf), oder `_heartbeatCheck`/`_bridgeSenderOk`/`failed` erscheinen nicht in der Ausgabe</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "dispatchMessage" tests/bridge-protocol.test.js` >= 2; `grep -c "evalIn(" tests/bridge-protocol.test.js` >= 6
    - `grep -c "_bridgeSenderOk" tests/bridge-protocol.test.js` >= 3; `grep -c "_heartbeatCheck" tests/bridge-protocol.test.js` >= 2; `grep -c "manualReconnect" tests/bridge-protocol.test.js` >= 2; `grep -c "debugOsOutfit" tests/bridge-protocol.test.js` >= 1
    - `grep -Fc "'Verbindung verloren'" tests/bridge-protocol.test.js` >= 1; `grep -Fc "'Nicht verbunden'" tests/bridge-protocol.test.js` >= 1; `grep -Fc "'Verbunden'" tests/bridge-protocol.test.js` >= 1
    - `grep -Fc "type: 'PING' }, '*']" tests/bridge-protocol.test.js` >= 2 (Bootstrap-PING beim Laden + PING nach manualReconnect)
    - `grep -c "readFileSync" tests/bridge-protocol.test.js` >= 1 (statischer Audit)
    - `test "$(grep -v '^\s*//' tests/bridge-protocol.test.js | grep -c '\.only(')" = 0`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/bridge-protocol.test.js`; `git status --short items.js` leer
  </acceptance_criteria>
  <done>Rund 21 Fälle in vier describe-Blöcken; Lauf nachweislich rot mit `_heartbeatCheck`/`_bridgeSenderOk` in der Ausgabe; roter Test committet; items.js unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `_bridgeSenderOk`, Origin einmal lernen und erzwingen, `_heartbeatCheck`, `_bcOrigin`-Reset in `manualReconnect` — items.js GREEN, chirurgisch committen (STAB-04, STAB-07; Tool-Hälfte von STAB-06)</name>
  <precondition>`git status --short items.js` gibt VOR dem Edit nichts aus — sonst STOPP und eskalieren</precondition>
  <reversibility rating="reversible">Reine Guard-Logik in einem Helfer plus drei Einzeiler in items.js; kein Datenformat, kein Loader-Deploy — Revert per `git revert` ohne Nebenwirkung. Der Nutzer muss nach dem Deploy nur das Tool-Fenster neu laden.</reversibility>
  <files>items.js</files>
  <read_first>items.js Bridge-Abschnitt (per `grep -n "let _bcOrigin\|function startPingRetry\|Heartbeat-Watchdog\|function manualReconnect\|function bcSend\|window.addEventListener('message'" items.js`): Kommentarblock über `_bcOrigin`, `startPingRetry`, das Heartbeat-`setInterval(function() {…}, 5000)`, `manualReconnect`, Haupt-Handler-Kopf bis `_lastMsgTs = Date.now();`; die Handler-Guard-Zeile in `debugOsOutfit` (per `grep -n "ev.data.type !== 'OUTFIT_DEBUG_RESULT'" items.js`); Start-IIFE (per `grep -n "Auto-load + initial PING" items.js`); tests/bridge-protocol.test.js (Task 2)</read_first>
  <behavior>
    - Doppelprüfung: Nachrichten mit fremder source ODER (nach dem Lernen) fremdem Origin verändern `_lastMsgTs` nicht und lösen keinen `case` aus; `_bcOrigin` wird nur gelernt, solange es `null` ist; beide Listener nutzen `_bridgeSenderOk`
    - Heartbeat: `_heartbeatCheck()` setzt bei >15 s Stille `_connected=false`, `#connStatus` auf `Verbindung verloren`/`off`, stoppt den Raum-Scan, startet den Ping-Retry — und tut nichts, solange Nachrichten ankommen
    - Reconnect: `manualReconnect()` setzt `_bcOrigin = null`, danach lernt der nächste Handshake einen neuen Origin; ohne Reconnect bleibt ein Origin-Wechsel gesperrt
    - bcSend: unverändert `_bcOrigin || '*'`; Bootstrap-PINGs unverändert `'*'`; Test-Suite komplett grün (`npm test`)
  </behavior>
  <action>
    Alle Änderungen im Bridge-Abschnitt von items.js, an Ort und Stelle (kein bridge.js, kein Umzug):

    1. Helfer `_bridgeSenderOk(ev)` direkt VOR `function manualReconnect()` einfügen (nach dem `_playerAbgelehnt`-Kommentarblock), mit Kommentar „Absender-Doppelprüfung (STAB-06, Tool-Seite): Quelle muss der Opener sein; sobald beim ersten gültigen Handshake ein Spiel-Origin gelernt wurde, muss jede weitere Nachricht von genau diesem Origin kommen (Trust-on-first-use). Bei Mirror-Wechsel läuft der Heartbeat ab; nur `manualReconnect()` darf neu vertrauen.“ Körper: `return false`, wenn `!window.opener || ev.source !== window.opener`; `return false`, wenn `_bcOrigin && ev.origin !== _bcOrigin`; sonst `true`. Im Kommentar NICHT die Zeichenfolge `_bcOrigin = ev.origin` verwenden (Acceptance-Grep zählt genau eine Zuweisung).

    2. Haupt-Handler (`window.addEventListener('message', function(ev) {`): den bestehenden Source-Block (`if (!window.opener || ev.source !== window.opener) { console.warn(…); return; }`) durch `if (!_bridgeSenderOk(ev)) { console.warn('[BCK-Popup] message von fremder Quelle/Origin ignoriert:', ev.origin); return; }` ersetzen; den erklärenden Kommentar darüber um einen Satz zur Origin-Erzwingung kürzen/ergänzen. Die Lern-Zeile `if (ev.origin && ev.origin !== 'null') _bcOrigin = ev.origin;` ersetzen durch `if (!_bcOrigin && ev.origin && ev.origin !== 'null') _bcOrigin = ev.origin;` mit Kommentar „Spiel-Origin einmalig lernen (BC läuft auf mehreren Domains); danach erzwingt `_bridgeSenderOk` ihn“. Reihenfolge bleibt: app-Check → Absender-Check → `_playerAbgelehnt` → Lernen → `_lastMsgTs`.

    3. Debug-Listener in `debugOsOutfit`: den Guard `if (!ev.data || ev.data.app !== 'BCKonfigurator' || ev.data.type !== 'OUTFIT_DEBUG_RESULT' || ev.data.reqId !== reqId) return;` um `|| !_bridgeSenderOk(ev)` erweitern (einzige Änderung dort).

    4. Heartbeat: den anonymen Callback des 5-s-Intervalls (`setInterval(function() { if (!_connected) return; if (Date.now() - _lastMsgTs > 15000) { … } }, 5000);`) unverändert in eine benannte Deklaration `function _heartbeatCheck() { … }` überführen und das Intervall auf `setInterval(_heartbeatCheck, 5000);` setzen. Körper bleibt identisch (Warnung, `_connected=false`, `_playerChecked=false`, `stopRoomScan()`, `#connStatus` → `Verbindung verloren`/`off`, `startPingRetry()`); Kommentar: „benannt, damit der Test ihn ohne Timer aufrufen kann“. KEIN `_bcOrigin`-Reset hier (Prohibition).

    5. `manualReconnect()`: direkt nach `_playerAbgelehnt = false;` die Zeile `_bcOrigin = null;` mit Kommentar „Origin beim nächsten Handshake neu lernen (Mirror-Wechsel, STAB-07)“ — VOR `bcSend({ type: 'PING' })`, damit der PING über den `'*'`-Fallback rausgeht.

    6. Bootstrap-Ausnahme dokumentieren: über der PING-Zeile in `startPingRetry` und über der PING-Zeile der Start-IIFE je einen Kommentar „STAB-04-Ausnahme: Bootstrap-PING vor dem Handshake — Spiel-Origin noch unbekannt, Payload trägt keine Daten“ ergänzen. Die Sendezeilen selbst NICHT ändern; in Kommentaren nicht die Zeichenfolge `window.opener.postMessage(` gefolgt von `, '*')` schreiben (statischer Audit zählt genau 2 Codezeilen).

    7. `node --check items.js` → Exit 0. GREEN: `npx vitest run tests/bridge-protocol.test.js` → alle Fälle grün; `npm test` → 11 Dateien grün.

    8. Commit: `git add items.js` (nur dieser Pfad); Message `fix(03-01): enforce learned game origin + source on tool side, named heartbeat check, reconnect relearns origin (STAB-04, STAB-06, STAB-07)`. Nachweis im SUMMARY: `git show --stat --format= HEAD`, GREEN-Ausgabe, RED-Ausgabe aus Task 2.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/bridge-protocol.test.js && node --check items.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` in der `Tests`-Summary — etwa weil `_bridgeSenderOk` den Origin vor dem Lernen fälschlich erzwingt (Handshake-Test rot), `manualReconnect` den Reset hinter dem PING setzt, oder eine andere Testdatei durch den geänderten Handler-Kopf rot wird</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(grep -Fc 'function _bridgeSenderOk(ev)' items.js)" = 1 && test "$(grep -oF '_bridgeSenderOk(ev)' items.js | wc -l)" -eq 3 && test "$(grep -Fc 'function _heartbeatCheck()' items.js)" = 1 && test "$(grep -Fc 'setInterval(_heartbeatCheck, 5000)' items.js)" = 1 && test "$(grep -oE "window\.opener\.postMessage\(.*, '\*'\)" items.js | wc -l)" -eq 2 && test "$(grep -Fc "_bcOrigin || '*'" items.js)" = 1 && test "$(grep -c '_bcOrigin = ev.origin' items.js)" = 1 && awk '/^function manualReconnect[(][)]/{f=1} f&&/_bcOrigin = null;/{r=1} f&&/bcSend[(][{] type: .PING. [}][)]/{if(r){print "OK"}; exit} f&&/^}/{exit}' items.js | grep -q OK && test "$(git show --name-only --format= HEAD)" = "items.js" && echo SURGICAL-OK</automated>
    <fails_when>`SURGICAL-OK` fehlt — Helfer fehlt oder wird nicht an genau zwei Stellen aufgerufen, Heartbeat nicht extrahiert, ein dritter direkter Wildcard-Send ist entstanden, `_bcOrigin = null` steht nicht vor dem PING in `manualReconnect`, oder der HEAD-Commit enthält andere Pfade als items.js</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "function _bridgeSenderOk(ev)" items.js` = 1; `grep -oF "_bridgeSenderOk(ev)" items.js | wc -l` → 3 (Definition + Haupt-Handler + Debug-Listener)
    - `grep -Fc "if (!_bcOrigin && ev.origin && ev.origin !== 'null') _bcOrigin = ev.origin;" items.js` = 1; `grep -c "_bcOrigin = ev.origin" items.js` = 1
    - `grep -Fc "function _heartbeatCheck()" items.js` = 1; `grep -Fc "setInterval(_heartbeatCheck, 5000)" items.js` = 1; `grep -Fc "Heartbeat verloren" items.js` = 1
    - `awk '/^function _heartbeatCheck[(][)]/{f=1} f&&/_bcOrigin/{print "BAD"; exit} f&&/^}/{exit}' items.js` gibt nichts aus (kein Origin-Reset im Heartbeat)
    - `grep -oE "window\.opener\.postMessage\(.*, '\*'\)" items.js | wc -l` → 2; `grep -Fc "_bcOrigin || '*'" items.js` = 1; `grep -o "STAB-04-Ausnahme" items.js | wc -l` → 2
    - `grep -Fc 'id="connStatus"' index.html` = 1; `grep -Fc 'onclick="manualReconnect()"' index.html` = 1 (Reconnect-UI vorhanden, unverändert)
    - `node --check items.js` → Exit 0; `npx vitest run tests/bridge-protocol.test.js` → 0 failed; `npm test` → `Test Files  11 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `items.js`; `git log --format=%h 012c6d5..HEAD -- loader.js bot-engine.js bot-ui.js index.html` gibt nichts aus; `git status --short` zeigt nur `?? .planning/milestone.lock`
  </acceptance_criteria>
  <done>Beide Empfangspfade prüfen Origin+Source über `_bridgeSenderOk`; Origin wird einmal gelernt und danach erzwungen; `_heartbeatCheck` benannt; `manualReconnect` lernt neu; Bootstrap-Ausnahme dokumentiert; Bridge-Test und volle Suite grün; Commit enthält genau items.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spiel-Tab (Opener) → Tool-Fenster (`message`-Event) | Einzige Eingangstür für Spielzustand und Bot-Rückmeldungen; `ev.source`/`ev.origin` sind die einzigen verlässlichen Herkunftsmerkmale |
| Tool-Fenster → Spiel-Tab (`bcSend`) | Vor dem Handshake nur PING an `'*'` (kein Payload), danach gezielt an `_bcOrigin` |
| vm-Sandbox der Tests → Produktionscode | Registry darf nur sammeln, nie beim Laden dispatchen; sonst falsches Verhalten/Grün |
| Git-Arbeitsbaum → Repository | Ein Produktionspfad pro GREEN-Commit; untracked `.planning/milestone.lock` nie stagen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Spoofing | items.js `message`-Handler nimmt Nachricht von fremdem Origin an (Opener navigiert / Origin nur gelernt, nie erzwungen) | high | mitigate | `_bridgeSenderOk(ev)`: Source==Opener UND Origin==`_bcOrigin` nach TOFU; Lernen nur solange `null`; Tests „Fremd-Origin nach Handshake“, „Origin-Wechsel bleibt gesperrt“, Debug-Listener-Fall |
| T-3-03 | Information Disclosure | `bcSend` sendet Spielzustand/EXEC an `'*'` | high | mitigate | Bereits `_bcOrigin \|\| '*'`; statischer Audit fixiert genau 2 datenlose Bootstrap-PINGs + 1 Fallback; injizierter Code folgt in Plan 03-02 |
| T-3-04 | Repudiation | unprotokollierter EXEC | medium | transfer | Plan 03-03 (EXEC-Log in `bcSend`) |
| T-3-05 | Denial of Service | Nutzer nach echtem Mirror-Wechsel dauerhaft ausgesperrt | medium | mitigate | `manualReconnect()` setzt `_bcOrigin = null` (Test „nach manualReconnect neuer Origin“); Heartbeat macht den Verlust sichtbar; bewusst KEIN Auto-Reset (sonst T-3-01 umgehbar) |
| T-3-06 | Tampering | Sandbox-Registry ruft Handler beim Laden auf oder testet den falschen Handler (falsches Grün) | medium | mitigate | Registry sammelt nur; `dispatch` ist explizit; Ladetest prüft Registrierung; Bridge-Test beobachtet `_lastMsgTs`/DOM-Effekte statt nur „kein Throw“ |
| T-3-07 | Tampering | Git-Staging fremder Dateien (milestone.lock, index.html) | high | mitigate | Preconditions `git status --short` leer; `git add` nur genannter Pfade; `git show --name-only` Gates |
| T-3-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase (RESEARCH „Package Legitimacy Audit: nicht anwendbar“) |
</threat_model>

<verification>
- `npx vitest run tests/load-script.test.js` → 11 passed; `npx vitest run tests/bridge-protocol.test.js` → 0 failed; `npm test` → 11 Testdateien grün (alle Phase-1/2-Dateien unverändert grün).
- `node --check items.js` grün; genau drei Commits dieses Plans (test Wave 0 → test RED → fix GREEN); GREEN-Commit enthält nur items.js.
- Statische Gates: 2 direkte Wildcard-PINGs, 1 `_bcOrigin || '*'`-Fallback, 3× `_bridgeSenderOk(ev)`, kein `_bcOrigin`-Reset im Heartbeat.
- Manual-Only (end-of-phase, wird in Plan 03-03 `<verification>` gesammelt): sichtbarer Disconnect/Reconnect im echten Browser.
</verification>

<success_criteria>
- Erfolgskriterium 2 (Tool-Hälfte): Die Tool-Seite prüft `event.origin` und `event.source`; Nachrichten von fremdem Origin werden ignoriert; Typen, Origin-Prüfung und Dispatch sind mit simulierten Nachrichten getestet (TEST-07).
- Erfolgskriterium 3: Verbindungsverlust (kein Heartbeat) ist sichtbar (`Verbindung verloren`), Reconnect per `🔄 Verbinden` lernt den Origin neu (STAB-07).
- Erfolgskriterium 1 (Tool→Spiel): `bcSend` nutzt den gelernten Spiel-Origin; die zwei Bootstrap-PINGs sind die einzige, dokumentierte und per Test fixierte Ausnahme (STAB-04, Orchestrator-Entscheidung 1).
</success_criteria>

<output>
Create `.planning/phases/03-bridge-haertung/03-01-SUMMARY.md` when done — inkl. RED-Ausgabe aus Task 2, GREEN-Ausgabe aus Task 3, `git show --stat` der drei Commits, den statischen Grep-Zählungen und dem Hinweis, dass Plan 03-02 den `location`-Stub und Plan 03-03 `dispatchMessage` weiterverwenden.
</output>
