---
phase: 01-testfundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - vitest.config.js
  - vitest.no-idb.config.js
  - tests/setup/fake-indexeddb.js
  - tests/idb-canary.test.js
autonomous: false
requirements: [TEST-01, TEST-03]
tags: [vitest, tooling, fake-indexeddb, canary]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`npm test` führt `vitest run` aus und endet grün (Exit 0, Summary `Tests  1 passed`) — vor diesem Plan existierte kein package.json (TEST-01)"
    - "GitHub-Pages-Auslieferung bleibt buildfrei: kein Commit dieser Phase berührt index.html oder eine `<script>`-Zeile; package.json hat kein `dependencies`-Feld und kein `build`-Script (TEST-01)"
    - "Ohne den setupFiles-Eintrag (Meta-Config `vitest.no-idb.config.js`) schlägt genau ein Test — der Canary — mit der Meldung `fake-indexeddb/auto nicht geladen` fehl; mit setupFiles ist er grün (TEST-03)"
    - "Der Canary importiert fake-indexeddb nicht selbst; er prüft ausschließlich, was setupFiles bereitstellt — ein stilles Zurückfallen auf den localStorage-Pfad von idbGet/idbSet ist damit ausgeschlossen (TEST-03)"
    - "`node_modules/`, `coverage/` und `.vitest/` sind per .gitignore ausgeschlossen; `git status --short` zeigt nach `npm install` keine node_modules-Einträge"
    - "Annahme (TEST-01, Edge unclassified): `vitest run` ohne Testdateien endet mit Exit 1 (`No test files found`) — deshalb entstehen setupFiles-Wiring und Canary im selben Task/Commit, damit `npm test` nie in einem Zwischenzustand rot ist"
    - "Annahme (TEST-03, Edge unclassified): `genau ein Canary-Test` bezieht sich auf die Canary-Datei — sie enthält exakt eine `it()`; der Meta-Check führt nur diese Datei ohne setupFiles aus und erwartet `Tests  1 failed`"
  artifacts:
    - path: "package.json"
      provides: "npm-Manifest, nur devDependencies, Script `test` = `vitest run`"
      contains: "\"test\": \"vitest run\""
    - path: ".gitignore"
      provides: "Ausschluss von node_modules/, coverage/, .vitest/"
      contains: "node_modules/"
    - path: "vitest.config.js"
      provides: "Vitest-Konfiguration: environment node, include tests/**/*.test.js, setupFiles"
      contains: "tests/setup/fake-indexeddb.js"
    - path: "vitest.no-idb.config.js"
      provides: "Meta-Config für TEST-03: identisch, aber setupFiles leer"
      contains: "setupFiles: []"
    - path: "tests/setup/fake-indexeddb.js"
      provides: "Side-effect-Import von fake-indexeddb/auto (setzt globalThis.indexedDB im Worker)"
      contains: "fake-indexeddb/auto"
    - path: "tests/idb-canary.test.js"
      provides: "Genau ein Test: globalThis.indexedDB.open ist eine Funktion; klare Fehlermeldung"
      contains: "fake-indexeddb/auto nicht geladen"
    - path: "package-lock.json"
      provides: "Reproduzierbare Installation (exakte Pins)"
  key_links:
    - from: "package.json"
      to: "vitest.config.js"
      via: "Script `test` ruft `vitest run`, Vitest liest vitest.config.js aus dem Repo-Root"
      pattern: "vitest run"
    - from: "vitest.config.js"
      to: "tests/setup/fake-indexeddb.js"
      via: "test.setupFiles — läuft vor jeder Testdatei im Worker"
      pattern: "setupFiles"
    - from: "tests/setup/fake-indexeddb.js"
      to: "tests/idb-canary.test.js"
      via: "globalThis.indexedDB wird vom Setup gesetzt und vom Canary geprüft"
      pattern: "indexedDB"
    - from: "vitest.no-idb.config.js"
      to: "vitest.config.js"
      via: "importiert die Basis-Config und überschreibt nur test.setupFiles"
      pattern: "from './vitest.config.js'"
  prohibitions:
    - requirement_id: TEST-01
      category: safety
      verification: judgment
      statement: "Kein Bundler, kein Transpiler, kein `<script type=\"module\">` und kein `build`-Script — die Auslieferung bleibt reines `<script>`-Tag-Hosting"
    - requirement_id: TEST-01
      category: safety
      verification: judgment
      statement: "index.html wird in dieser Phase nicht verändert und nicht committet"
    - requirement_id: TEST-01
      category: safety
      verification: judgment
      statement: "Kein `git add -A` / `git add .` — jeder Commit staged ausschließlich die im Task genannten Pfade; die vorbestehenden unstaged Änderungen (bc-icons.js, bot-data.js, bot-ui.js, index.html, inventar.js, items.js, money.js, rank.js, shop.js) bleiben unstaged"
    - requirement_id: TEST-03
      category: safety
      verification: judgment
      statement: "Keine Testdatei importiert fake-indexeddb direkt — die Bereitstellung läuft ausschließlich über setupFiles, sonst wäre der Canary wirkungslos"
---

<objective>
Vitest-5-Harness als reine Dev-Toolchain anlegen — package.json (nur devDependencies, exakte Pins), .gitignore, vitest.config.js mit fake-indexeddb-Setup — und den IDB-Canary-Test (TEST-03) im selben Commit wie das setupFiles-Wiring erstellen. Vorher: Legitimitäts-Checkpoint für die per Heuristik als "too-new" markierten Pakete `vitest@5.0.0` und `@vitest/coverage-v8@5.0.0`.

Purpose: Erfüllt TEST-01 (`npm test` läuft lokal grün, Produktion unverändert und buildfrei) und TEST-03 (Canary schlägt ohne fake-indexeddb sichtbar fehl — kein stiller localStorage-Fallback). Alle weiteren Pläne der Phase setzen auf dieser Toolchain auf.
Output: `package.json`, `package-lock.json`, `.gitignore`, `vitest.config.js`, `vitest.no-idb.config.js`, `tests/setup/fake-indexeddb.js`, `tests/idb-canary.test.js`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testfundament/01-RESEARCH.md
@.planning/phases/01-testfundament/01-VALIDATION.md

Umgebung (verifiziert 2026-09-12): Node v24.12.0, npm 11.6.2, Windows 11, Git Bash als Shell für alle Kommandos. `core.autocrlf=true` ohne `.gitattributes` — die Warnung `LF will be replaced by CRLF` beim `git add` ist kosmetisch und wird bewusst hingenommen (kein `.gitattributes` in dieser Phase, um keine Renormalisierung des Repos auszulösen). Immer Forward-Slashes in Konfigurationspfaden.

Phase-Baseline-Commit für alle "Produktion unverändert"-Prüfungen: `d9ad6af` (docs: map existing codebase). Der Arbeitsbaum enthält ~900 Zeilen NICHT zugehöriger, unstaged Änderungen in `bc-icons.js bot-data.js bot-ui.js index.html inventar.js items.js money.js rank.js shop.js` — diese werden in dieser Phase nie gestaged.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 1 gesamt; Plan-Zuordnung in Klammern)

| Pfad | Symbole / Inhalt | Plan |
|------|------------------|------|
| `package.json` | Scripts `test` (`vitest run`), `test:watch` (`vitest`), `coverage` (`vitest run --coverage`); devDependencies `vitest@5.0.0`, `@vitest/coverage-v8@5.0.0`, `fake-indexeddb@6.2.5`, `lz-string@1.5.0` | 01-01 |
| `package-lock.json` | generiert durch `npm install` | 01-01 |
| `.gitignore` | `node_modules/`, `coverage/`, `.vitest/` | 01-01 |
| `vitest.config.js` | `defineConfig({ test: { environment: 'node', include: ['tests/**/*.test.js'], setupFiles: ['./tests/setup/fake-indexeddb.js'] } })` | 01-01 |
| `vitest.no-idb.config.js` | Basis-Config mit `test.setupFiles: []` — nur für den TEST-03-Meta-Check | 01-01 |
| `tests/setup/fake-indexeddb.js` | eine Zeile: Side-effect-Import `fake-indexeddb/auto` | 01-01 |
| `tests/idb-canary.test.js` | genau eine `it()`: `typeof globalThis.indexedDB?.open === 'function'` mit Meldung `CANARY: … fake-indexeddb/auto nicht geladen …` | 01-01 |
| `tests/helpers/loadScript.js` | `REPO_ROOT`, `makeElementStub()`, `makeSandbox(extraGlobals)`, `loadInto(sandbox, file)`, `loadScript(files, extraGlobals)`, `evalIn(sandbox, code)`, `settle(ms)` | 01-02 |
| `tests/load-script.test.js` | TEST-02: drei Produktionsdateien laden in Ladereihenfolge; Globals erreichbar; Edge-Cases leer/Reihenfolge/Doppelladen | 01-02 |
| `tests/idb-helpers.test.js` | `idbGet`/`idbSet` Round-Trip über die Sandbox mit fake-indexeddb | 01-02 |
| `tests/bot-data-validators.test.js` | TEST-05: `_normLogik`, `_migriereLogik` (Pflicht), `_botVarApply`, `_playerKeyApply` (Extras) | 01-03 |
| `tests/outfit-import-parser.test.js` | TEST-05: `_oiDetectType`, `_oiBuildExecCode` mit echtem lz-string; zwei `it.fails` für bekannte URI-Lücken | 01-03 |
| `tests/bot-engine-escaping.test.js` | TEST-06: `_buildBotCode` + `new Function()`-Parse-Check mit adversarialen Namen | 01-04 |
| `bot-engine.js` (Zeile 49, einzige Produktionsänderung der Phase) | `safeName` escaped zusätzlich `'`, `\r`, `\n` | 01-04 |
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: node_modules -->
<!-- Grund: `node_modules/` ist der Inhalt der zu schreibenden .gitignore (Action, Schritt 1) UND das Ziel des negativen git-status-Greps (Acceptance) — beides legitim. -->

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Paket-Legitimität bestätigen — vitest@5.0.0 und @vitest/coverage-v8@5.0.0 (Heuristik-Flag "too-new")</name>
  <read_first>.planning/phases/01-testfundament/01-RESEARCH.md (Abschnitt "Package Legitimacy Audit")</read_first>
  <what-built>Noch nichts installiert. Die Recherche hat `vitest` und `@vitest/coverage-v8` per Heuristik als `SUS` markiert — einziger Grund: die Major-Version 5.0.0 war zum Recherchezeitpunkt 9 Tage alt. Downloads (77 Mio./Woche bzw. 29 Mio./Woche) und Quell-Repo (github.com/vitest-dev/vitest) sprechen klar für Legitimität. `fake-indexeddb@6.2.5` ist `OK`; `lz-string@1.5.0` ist dieselbe Version wie der Produktions-CDN-Import. Per Legitimacy-Gate-Protokoll ist dieser Checkpoint nicht auto-approvable.</what-built>
  <how-to-verify>
    1. https://www.npmjs.com/package/vitest öffnen — prüfen: Version 5.0.0 vorhanden, Publisher/Maintainer = vitest-Team (vitest-dev), Repository-Link zeigt auf github.com/vitest-dev/vitest, wöchentliche Downloads im zweistelligen Millionenbereich.
    2. https://www.npmjs.com/package/@vitest/coverage-v8 öffnen — gleiche Prüfung; Version 5.0.0 muss zur vitest-Major passen.
    3. Optional: https://www.npmjs.com/package/fake-indexeddb (6.2.5, Repo dumbmatter/fakeIndexedDB) und https://www.npmjs.com/package/lz-string (1.5.0, pieroxy/lz-string).
    4. Keine der Seiten darf Deprecation-Hinweise oder ein abweichendes Repo zeigen.
  </how-to-verify>
  <acceptance_criteria>
    - Nutzer hat "approved" (oder gleichbedeutend) gesendet; bei jeder Abweichung STOPP — kein `npm install` ohne Freigabe
  </acceptance_criteria>
  <resume-signal>"approved" eingeben, um Task 2 (Installation) freizugeben — oder die Abweichung beschreiben</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Vitest-Toolchain anlegen (package.json, .gitignore, vitest.config.js, Setup, Meta-Config) und IDB-Canary im selben Commit</name>
  <reversibility rating="reversible">Reine Dev-Toolchain-Dateien; Entfernen von package.json/tests/ stellt den Vorzustand ohne Migration wieder her. `"type": "commonjs"` hält `node --check *.js` (bestehende Konvention) unverändert lauffähig.</reversibility>
  <files>package.json, package-lock.json, .gitignore, vitest.config.js, vitest.no-idb.config.js, tests/setup/fake-indexeddb.js, tests/idb-canary.test.js</files>
  <read_first>.planning/phases/01-testfundament/01-RESEARCH.md (Abschnitte "Standard Stack", "Pattern 2: IDB-Canary", "Pitfall 3"), items.js Zeilen 1-65 (nur diese Zeilen — die Datei hat 11.700 Zeilen; NIE komplett lesen), .planning/codebase/TESTING.md</read_first>
  <action>
    Reihenfolge ist wichtig: erst .gitignore, dann package.json, dann `npm install`, dann Config/Setup/Canary, dann Testlauf, dann Commit. Alle Kommandos in Git Bash im Repo-Root `C:/Users/andre/Documents/GitHub/bc-universal-configurator`.

    1. `.gitignore` (neu; existiert nicht — verifiziert) mit genau drei Zeilen: `node_modules/`, `coverage/`, `.vitest/`. Nichts anderes ignorieren — `package-lock.json` wird committet.

    2. `package.json` (neu; existiert nicht — verifiziert) mit exakt diesen Feldern: `name` = `bc-universal-configurator`, `version` = `0.0.0`, `private` = true, `type` = `commonjs` (per TEST-01/Konvention: die Produktionsdateien bleiben klassische Scripts; `node --check *.js` bleibt gültig — mit `module` würde `--check` sie als strict-mode-ESM parsen), `engines.node` = `>=22.12.0` (Vitest-5-Minimum), `scripts.test` = `vitest run` (NIE watch-mode; `vitest` allein ist watch), `scripts.test:watch` = `vitest`, `scripts.coverage` = `vitest run --coverage`, `devDependencies` mit EXAKTEN Pins ohne Caret: `vitest` = `5.0.0`, `@vitest/coverage-v8` = `5.0.0`, `fake-indexeddb` = `6.2.5`, `lz-string` = `1.5.0`. KEIN `dependencies`-Feld, KEIN `build`-Script (lz-string bleibt in Produktion ein CDN-`<script>`; die lokale Kopie dient nur den Outfit-Import-Tests in Plan 01-03).

    3. `npm install` ausführen (erzeugt `package-lock.json` und `node_modules/`). Danach prüfen: `npx vitest --version` enthält `5.0.0`; `git status --short` zeigt KEINE `node_modules`-Zeile (sonst .gitignore prüfen).

    4. `tests/setup/fake-indexeddb.js` (neu) mit genau einer Anweisung: ein bare Side-effect-Import des Pakets `fake-indexeddb` über dessen `auto`-Einstiegspunkt (Pfad `fake-indexeddb/auto`). Dieser setzt `globalThis.indexedDB`, `IDBKeyRange` usw. im Vitest-Worker.

    5. `vitest.config.js` (neu, ESM-Syntax; Vitest bündelt die Config selbst, `"type": "commonjs"` stört nicht): `import { defineConfig } from 'vitest/config'` und `export default defineConfig({ test: { environment: 'node', include: ['tests/**/*.test.js'], setupFiles: ['./tests/setup/fake-indexeddb.js'] } })`. Keine weiteren Optionen (kein `globals`, kein `pool`, keine Reporter-Änderung).

    6. `vitest.no-idb.config.js` (neu): Kopfkommentar "Meta-Check für TEST-03 — nur für: npx vitest run tests/idb-canary.test.js --config vitest.no-idb.config.js (erwartetes Ergebnis: 1 failed). Nicht für den normalen Testlauf." Inhalt: `import { defineConfig } from 'vitest/config'`, `import base from './vitest.config.js'`, `export default defineConfig({ ...base, test: { ...base.test, setupFiles: [] } })`. NICHT die Array-konkatenierende Merge-Hilfsfunktion aus `vitest/config` verwenden — sie würde den setupFiles-Eintrag der Basis-Config behalten; der Spread-Override ist der Punkt.

    7. `tests/idb-canary.test.js` (neu): `import { describe, it, expect } from 'vitest'`; genau EIN `describe('IDB canary (TEST-03)')` mit genau EINER `it('fake-indexeddb ist aktiv — kein stiller localStorage-Fallback')`, die eine einzige Assertion enthält: `expect(typeof globalThis.indexedDB?.open, MESSAGE).toBe('function')` mit MESSAGE = `CANARY: globalThis.indexedDB fehlt — fake-indexeddb/auto nicht geladen (test.setupFiles in vitest.config.js prüfen). idbGet/idbSet in items.js verschlucken jeden Fehler und liefern still null/false — ohne diesen Canary bliebe das unsichtbar.` Die Datei importiert fake-indexeddb NICHT selbst (sonst wäre der setupFiles-Check wirkungslos) und lädt keine Produktionsdatei.

    8. Testlauf: `npm test` → grün, `Tests  1 passed`. Meta-Check (TEST-03, Erfolgskriterium 3): `npx vitest run tests/idb-canary.test.js --config vitest.no-idb.config.js` → Exit ≠ 0, Ausgabe enthält `fake-indexeddb/auto nicht geladen` und `1 failed`. Danach erneut `npm test` → grün (die Meta-Config verändert nichts am Repo).

    9. Commit — NUR diese Pfade explizit stagen: `git add .gitignore package.json package-lock.json vitest.config.js vitest.no-idb.config.js tests/setup/fake-indexeddb.js tests/idb-canary.test.js`. Vorher/nachher `git status --short` prüfen: die vorbestehenden ` M`-Zeilen (bc-icons.js, bot-data.js, bot-ui.js, index.html, inventar.js, items.js, money.js, rank.js, shop.js) müssen in Spalte 1 ein Leerzeichen behalten (unstaged). `git diff --cached --name-only` muss exakt die sieben genannten Pfade listen. Commit-Message: `test(01-01): add vitest toolchain and fake-indexeddb canary (TEST-01, TEST-03)`. Niemals `git add -A`, `git add .` oder `git commit -a`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npm test</automated>
    <fails_when>non-zero exit, oder die Summary-Zeile `Tests` enthält nicht `1 passed`, oder die Ausgabe enthält `No test files found`</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/idb-canary.test.js --config vitest.no-idb.config.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -q "fake-indexeddb/auto nicht geladen" "$OUT" && grep -Eq "Tests +1 failed" "$OUT" && echo META-OK</automated>
    <fails_when>`META-OK` wird nicht ausgegeben — d. h. Vitest endete ohne setupFiles mit Exit 0, oder die Canary-Meldung bzw. `1 failed` fehlt in der Ausgabe</fails_when>
  </verify>
  <acceptance_criteria>
    - `test "$(node -e "const p=require('./package.json');console.log([p.devDependencies.vitest,p.devDependencies['@vitest/coverage-v8'],p.devDependencies['fake-indexeddb'],p.devDependencies['lz-string'],p.scripts.test,p.type,p.private,p.dependencies===undefined,p.scripts.build===undefined].join('|'))")" = "5.0.0|5.0.0|6.2.5|1.5.0|vitest run|commonjs|true|true|true"`
    - `npx vitest --version` gibt eine Zeile mit `5.0.0` aus
    - `grep -c "node_modules/" .gitignore` = 1, `grep -c "coverage/" .gitignore` = 1, `grep -c ".vitest/" .gitignore` = 1
    - `grep -c "tests/setup/fake-indexeddb.js" vitest.config.js` = 1; `grep -c "environment: 'node'" vitest.config.js` = 1; `grep -c "tests/\*\*/\*.test.js" vitest.config.js` = 1
    - `grep -c "fake-indexeddb/auto" tests/setup/fake-indexeddb.js` = 1 und die Datei hat genau 1 nicht-leere Zeile (`grep -cve '^\s*$' tests/setup/fake-indexeddb.js` = 1)
    - `grep -c "setupFiles: \[\]" vitest.no-idb.config.js` = 1
    - `grep -c "from './vitest.config.js'" vitest.no-idb.config.js` = 1
    - `grep -c "mergeConfig" vitest.no-idb.config.js` = 0 (Meta-Config setzt setupFiles per Spread-Override, nicht per Array-konkatenierendem Merge)
    - `grep -c "^\s*it(" tests/idb-canary.test.js` = 1 (genau eine it)
    - `grep -c "fake-indexeddb/auto nicht geladen" tests/idb-canary.test.js` = 1
    - `test "$(grep -Ec '^[[:space:]]*import .*fake-indexeddb' tests/idb-canary.test.js)" = 0` (Canary importiert das Paket nicht selbst)
    - `test "$(git status --short | grep -c "node_modules")" = 0` nach `npm install` (Installationsverzeichnis ist ignoriert)
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` listet exakt `.gitignore package-lock.json package.json tests/idb-canary.test.js tests/setup/fake-indexeddb.js vitest.config.js vitest.no-idb.config.js` (7 Pfade) und `git log --format=%h d9ad6af..HEAD -- index.html items.js bot-data.js outfit-import.js bot-engine.js` gibt nichts aus
    - `git status --short` zeigt weiterhin ` M index.html` (Leerzeichen in Spalte 1 = unstaged) — die unrelated Änderungen wurden nicht angefasst
  </acceptance_criteria>
  <done>`npm test` grün mit genau 1 Test; Meta-Check ohne setupFiles liefert `1 failed` mit der Canary-Meldung; Commit enthält exakt die sieben Toolchain-/Canary-Pfade; kein Produktionsfile im Commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm-Registry → Entwicklermaschine | Fremdcode (vitest, vite, esbuild, fake-indexeddb, lz-string) wird per `npm install` in `node_modules/` ausgeführt (Postinstall-Scripts, Test-Runner) |
| Test-Harness → Produktionscode | Tests behaupten Eigenschaften über den Produktionscode; ein stiller Fallback (kein IDB) würde grüne Tests ohne Aussagekraft erzeugen |
| Git-Arbeitsbaum → Repository | ~900 Zeilen unrelated Änderungen liegen unstaged neben den Phase-Artefakten; ein unpräzises Staging würde sie ungewollt committen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-02 | Repudiation | tests/idb-canary.test.js, vitest.config.js setupFiles | medium | mitigate | Canary prüft `globalThis.indexedDB.open` explizit und ohne eigenen Import; Meta-Check per `vitest.no-idb.config.js` beweist, dass er ohne setupFiles rot wird (Task 2, Verify 2) |
| T-1-SC | Tampering | npm installs (vitest, @vitest/coverage-v8, fake-indexeddb, lz-string) | high | mitigate | Exakte Version-Pins ohne Caret, `package-lock.json` committet, blockierender Legitimitäts-Checkpoint (Task 1) vor `npm install`; `[SUS]`-Flag ist reines Alters-Heuristik-Flag, per npmjs.com-Prüfung durch den Nutzer entkräftet |
| T-1-03 | Information Disclosure | .gitignore | low | mitigate | `node_modules/`, `coverage/`, `.vitest/` ausgeschlossen, bevor `npm install` läuft; Acceptance prüft `git status` auf node_modules-Einträge |
| T-1-06 | Tampering | Git-Staging der Phase-Commits | high | mitigate | Nur explizite Pfade stagen; `git diff --cached --name-only` muss exakt der Task-Liste entsprechen; `git log d9ad6af..HEAD -- index.html …` muss leer bleiben |
</threat_model>

<verification>
- `npm test` grün (Exit 0, `Tests  1 passed`).
- `npx vitest run tests/idb-canary.test.js --config vitest.no-idb.config.js` → Exit ≠ 0, Ausgabe enthält `fake-indexeddb/auto nicht geladen` und `Tests  1 failed` (TEST-03, Erfolgskriterium 3).
- `git log --format=%h d9ad6af..HEAD -- index.html items.js bot-data.js outfit-import.js bot-engine.js bot-ui.js loader.js` gibt nichts aus (Produktion unverändert, TEST-01).
- `git show --name-only --format= HEAD` listet nur die sieben Toolchain-/Canary-Pfade.
- Kein `build`-Script, kein `dependencies`-Feld in package.json; `index.html` ohne `type="module"` weiterhin unverändert (kein Commit berührt es).
</verification>

<success_criteria>
- TEST-01: `npm test` führt die Vitest-Suite lokal aus und endet grün; keine Änderung an index.html/`<script>`-Tags; kein Build-Schritt.
- TEST-03: Ohne setupFiles schlägt genau der eine Canary-Test mit klarer Meldung fehl; mit setupFiles grün.
- Commit-Hygiene: exakt sieben neue Pfade, unrelated Änderungen bleiben unstaged.
</success_criteria>

<output>
Create `.planning/phases/01-testfundament/01-01-SUMMARY.md` when done — inkl. der tatsächlichen `npx vitest --version`-Ausgabe, des Meta-Check-Ergebnisses und der Commit-Hashes.
</output>
