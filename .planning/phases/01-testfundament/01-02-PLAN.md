---
phase: 01-testfundament
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/helpers/loadScript.js
  - tests/load-script.test.js
  - tests/idb-helpers.test.js
  - tests/bot-data-validators.test.js
  - tests/outfit-import-parser.test.js
autonomous: true
requirements: [TEST-02, TEST-05]
tags: [vm-sandbox, fake-indexeddb, bot-data, outfit-import, lz-string]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`loadScript(['items.js','bot-data.js','outfit-import.js'])` lädt die drei unveränderten Produktionsdateien in EINE gemeinsame vm-Sandbox in Ladereihenfolge; danach sind `idbGet`, `idbSet`, `_normLogik`, `_migriereLogik`, `_botVarApply`, `_playerKeyApply`, `_oiDetectType`, `_oiBuildExecCode` als Funktionen auf dem Sandbox-Objekt erreichbar (TEST-02)"
    - "Top-Level-`let`-Globals der Produktionsdateien (`_bots`, `_botVars`, `_playerKeys`) sind KEINE Properties des Sandbox-Objekts — sie sind ausschließlich über `evalIn(sandbox, '_bots')` (vm.runInContext im selben Kontext) erreichbar; die Tests beweisen beides (TEST-02)"
    - "Kein Commit dieser Phase verändert items.js, bot-data.js oder outfit-import.js — `git log d9ad6af..HEAD -- items.js bot-data.js outfit-import.js` bleibt leer (TEST-02, Quelle unverändert)"
    - "`idbSet('TEST_rt', …)` → `idbGet('TEST_rt')` liefert den gespeicherten Wert über fake-indexeddb zurück; unbekannter Schlüssel liefert `null` — die Sandbox nutzt `globalThis.indexedDB` aus setupFiles, nicht den localStorage-Pfad (TEST-02, Bezug zum Canary aus 01-01)"
    - "`_normLogik` setzt `logik` des ersten Elements auf `und`, lässt Folgeelemente unverändert und reicht Nicht-Arrays/leere Arrays unverändert und ohne Exception durch; `_migriereLogik` bereinigt Legacy-Werte (`oder`, `und_oder`, `und_nicht`) an erster Stelle in triggers.bedingungen, triggers.ifBedingungen und events.bedingungen, liefert die Anzahl der Korrekturen, ist idempotent (zweiter Lauf → 0) und persistiert unter `BC_Bots_v2` (TEST-05, Nutzerentscheidung 2)"
    - "`_oiDetectType` klassifiziert echte `LZString.compressToBase64`-Ausgaben als `lzbase64`, rohen JS-Code, Leerstring und 19-Zeichen-Strings als `js`; `_oiBuildExecCode` erzeugt für gültige lzbase64-Codes den Apply-Pfad (`var _raw=<JSON>` + `AssetGet(`), für ungültige/rohe Eingaben nie den Apply-Pfad (Fallback-Wrapper ohne `var _raw=`); jede Ausgabe parst per `new Function()` ohne SyntaxError (TEST-05)"
    - "Ohne `LZString` in der Sandbox fällt `_oiBuildExecCode` auch für gültige lzbase64-Codes auf den Roh-Wrapper zurück (der `typeof LZString !== 'undefined'`-Guard ist getestet) (TEST-05)"
    - "`loadScript([])` liefert eine Basis-Sandbox mit Stubs und `window`-Selbstreferenz, aber ohne Produktions-Globals (`typeof sandbox.idbGet === 'undefined'`); `loadScript('items.js')` und `loadScript(null)` werfen `TypeError` (TEST-02, Edge `empty` — explizit)"
    - "`loadScript` lädt strikt in Array-Reihenfolge und sortiert nie um: `loadScript(['bot-data.js'])` ohne vorheriges items.js wirft synchron `ReferenceError: idbGet is not defined` (bot-data.js Zeile 90 ruft idbGet beim Laden) (TEST-02, Edge `ordering` — explizit)"
    - statement: "Zweimaliges Laden derselben Datei in dieselbe Sandbox (`loadScript(['items.js','items.js'])`) wirft `SyntaxError` (const-Redeklaration, z. B. `Identifier 'colorIsDefault' has already been declared`) — der Helfer dedupliziert nicht; Vertrag ist eine frische Sandbox pro Testdatei via `beforeAll` (TEST-02, Edge `adjacency`)"
      verification: backstop
    - "Annahme (TEST-05, Edge unclassified): `Bot-Validatoren` = `_normLogik`/`_migriereLogik` (Nutzerentscheidung 2; bot-data.js enthält keine `validate*`-Funktionen — RESEARCH Pitfall 6); `_botVarApply`/`_playerKeyApply` sind optionale Extras. `Ungültig abgelehnt` heißt: Nicht-Arrays werden unverändert und ohne Mutation durchgereicht; für den Parser: Nicht-LZ-Eingaben erreichen nie den `_raw=`-Apply-Pfad"
    - "Annahme (TEST-05, Planer-Befund): `_oiDetectType` kennt das LZString-URI-Alphabet (`+`, `-`, `$`) nicht und `_oiBuildExecCode` versucht den URI-Decoder nur, wenn der Base64-Decoder falsy liefert — echte `compressToEncodedURIComponent`-Codes mit `-` erreichen den Apply-Pfad daher nie. Beide Lücken werden per `it.fails` dokumentiert, NICHT gefixt (outfit-import.js bleibt unverändert); Fix-Kandidat für eine spätere Phase"
  artifacts:
    - path: "tests/helpers/loadScript.js"
      provides: "vm-Sandbox-Loader: REPO_ROOT, makeElementStub, makeSandbox, loadInto, loadScript, evalIn, settle"
      exports: ["REPO_ROOT", "makeElementStub", "makeSandbox", "loadInto", "loadScript", "evalIn", "settle"]
      min_lines: 60
    - path: "tests/load-script.test.js"
      provides: "TEST-02: Ladereihenfolge, Globals, let-Bindings, Edge-Cases leer/Nicht-Array/Reihenfolge/Doppelladen"
      contains: "idbGet is not defined"
    - path: "tests/idb-helpers.test.js"
      provides: "idbGet/idbSet Round-Trip über die Sandbox mit fake-indexeddb"
      contains: "TEST_rt"
    - path: "tests/bot-data-validators.test.js"
      provides: "TEST-05: _normLogik, _migriereLogik (Pflicht), _botVarApply, _playerKeyApply (Extras)"
      contains: "_migriereLogik"
    - path: "tests/outfit-import-parser.test.js"
      provides: "TEST-05: _oiDetectType, _oiBuildExecCode mit echtem lz-string; it.fails für URI-Lücken"
      contains: "compressToBase64"
  key_links:
    - from: "tests/helpers/loadScript.js"
      to: "items.js"
      via: "fs.readFileSync + vm.runInContext (Datei wird als Text gelesen, nie per require/import — keine Quelländerung nötig)"
      pattern: "runInContext"
    - from: "tests/helpers/loadScript.js"
      to: "tests/setup/fake-indexeddb.js"
      via: "Sandbox-Property `indexedDB: globalThis.indexedDB` — vom setupFiles-Import gesetzt"
      pattern: "globalThis\\.indexedDB"
    - from: "tests/bot-data-validators.test.js"
      to: "bot-data.js"
      via: "loadScript(['items.js','bot-data.js']) — items.js zuerst, weil bot-data.js:90 idbGet beim Laden aufruft"
      pattern: "'items.js', *'bot-data.js'"
    - from: "tests/outfit-import-parser.test.js"
      to: "outfit-import.js"
      via: "loadScript(['items.js','outfit-import.js'], { LZString }) — echtes lz-string 1.5.0 als Sandbox-Global"
      pattern: "LZString"
  prohibitions:
    - requirement_id: TEST-02
      category: safety
      verification: judgment
      statement: "items.js, bot-data.js und outfit-import.js werden weder editiert noch gestaged — Tests laden sie ausschließlich als Text per vm; kein Dual-Export, kein `module.exports`-Anhang in dieser Phase"
    - requirement_id: TEST-02
      category: safety
      verification: judgment
      statement: "Kein Test importiert eine Produktionsdatei per `import`/`require` (das würde `type: commonjs`/ESM-Parsing und Top-Level-Seiteneffekte im Host auslösen)"
    - requirement_id: TEST-05
      category: safety
      verification: judgment
      statement: "Von `_oiBuildExecCode` erzeugter Code wird nur per `new Function()` geparst, nie aufgerufen — er enthält `AssetGet`/`Player`-Zugriffe, die nur im BC-Spiel-Tab existieren"
    - requirement_id: TEST-05
      category: safety
      verification: judgment
      statement: "Keine handgeschriebenen Fake-LZ-Strings — Fixtures entstehen zur Laufzeit aus `lz-string@1.5.0` (derselben Version wie der Produktions-CDN-Import)"
    - requirement_id: TEST-02
      category: safety
      verification: judgment
      statement: "Kein `git add -A` / `git add .` — nur die fünf Testpfade dieses Plans werden gestaged; die unrelated ` M`-Änderungen bleiben unstaged"
---

<objective>
Den gemeinsamen vm-Sandbox-Loader `tests/helpers/loadScript.js` bauen (probe-verifizierte Stub-Liste), damit `items.js`, `bot-data.js` und `outfit-import.js` ohne Quelländerung in Ladereihenfolge in eine Sandbox geladen werden (TEST-02), und darauf die TEST-05-Tests schreiben: `_normLogik`/`_migriereLogik` aus bot-data.js sowie `_oiDetectType`/`_oiBuildExecCode` aus outfit-import.js mit gültigen und ungültigen Eingaben (echtes lz-string). Ein idbGet/idbSet-Round-Trip beweist, dass die Sandbox das fake-indexeddb aus setupFiles nutzt.

Purpose: TEST-02 ist das Fundament aller weiteren Tests der Phasen 1–4; TEST-05 legt das erste Sicherheitsnetz über die Bot-Datenstrukturen und den Outfit-Parser, bevor Phase 4 `items.js` entflechtet.
Output: `tests/helpers/loadScript.js`, `tests/load-script.test.js`, `tests/idb-helpers.test.js`, `tests/bot-data-validators.test.js`, `tests/outfit-import-parser.test.js`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testfundament/01-RESEARCH.md
@.planning/phases/01-testfundament/01-PATTERNS.md
@.planning/phases/01-testfundament/01-01-SUMMARY.md

Planer-Probe (2026-09-12, Node v24.12.0, mit installiertem fake-indexeddb@6.2.5 und lz-string@1.5.0 im Scratchpad) — diese Befunde KORRIGIEREN die Stub-Liste aus RESEARCH.md Pattern 1 und sind für den Loader verbindlich:
1. `setInterval` MUSS ein No-op sein (`() => 0`), `clearInterval` ebenso: items.js registriert beim Laden unbedingte Intervalle (Zeilen ~4325 und ~5838); ein echtes `setInterval` hält den Prozess am Leben (Probe hing > 120 s).
2. `document.getElementById`, `document.querySelector`, `document.createElement` MÜSSEN ein Element-Stub-Objekt liefern, nicht `null`: die Auto-Init-IIFE in items.js (Zeilen ~6711–6733) ruft beim Laden `renderGroups()`, `renderProfileList()`, `startPingRetry()` auf, und `renderGroups` dereferenziert `document.getElementById('favSection').classList` (Zeile ~692). Mit `null` bricht das Laden mit `TypeError: Cannot read properties of null (reading 'classList')` ab. `querySelectorAll` liefert `[]`.
3. `btoa`/`atob` müssen in der Sandbox liegen (Node-Globals, aber nicht Teil eines frischen vm-Kontexts) — `_buildBotCode` in bot-engine.js braucht `btoa`; für 01-03 relevant, im Basis-Loader deshalb enthalten.
4. Top-Level-`let`/`const` einer per `vm.runInContext` ausgeführten Datei werden KEINE Properties des Sandbox-Objekts (nur `function`-Deklarationen und `var`). `sandbox._bots` ist `undefined`; `vm.runInContext('_bots', sandbox)` liefert das Array (Referenz, mutierbar).
5. Mit dieser Stub-Liste laden items.js, bot-data.js, outfit-import.js und bot-engine.js fehlerfrei; die async Init-IIFE von bot-data.js läuft mit fake-indexeddb innerhalb von < 100 ms durch (ruft am Ende `_migriereLogik()` und den bare `addEventListener`-Aufruf, Zeile ~149).
6. Deterministische Fixtures (lz-string 1.5.0): `ARR = [{Group:'Cloth',Name:'Dress',Color:'#ff0000'},{Group:'Hat',Name:'Beret'}]` → `compressToBase64` = lzbase64, Apply-Pfad; `compressToEncodedURIComponent` enthält `+` und `-` → `_oiDetectType` liefert `js` (Lücke 1). `ARR_URI = [{Group:'Hat',Name:'Dress',Color:'#000000'}]` → `compressToEncodedURIComponent` = `NobwRA4gTg9grgBzALjACQIYBcwBowByGAtgKYpgAiUpAzrXmAMIwA2MUFAxAAx-9gAvgF0gA` → `lzuri` erkannt, aber `_oiBuildExecCode` fällt auf den Roh-Wrapper zurück (Base64-Decoder liefert truncated Garbage, URI-Decoder wird nie versucht — Lücke 2).
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 1 gesamt; Plan-Zuordnung in Klammern)

| Pfad | Symbole / Inhalt | Plan |
|------|------------------|------|
| `package.json`, `package-lock.json`, `.gitignore` | npm-Manifest (nur devDependencies, `test` = `vitest run`), Lockfile, Ignore-Liste | 01-01 |
| `vitest.config.js`, `vitest.no-idb.config.js` | Vitest-Config mit setupFiles; Meta-Config ohne setupFiles (TEST-03) | 01-01 |
| `tests/setup/fake-indexeddb.js`, `tests/idb-canary.test.js` | Side-effect-Import `fake-indexeddb/auto`; genau ein Canary-Test | 01-01 |
| `tests/helpers/loadScript.js` | `REPO_ROOT`, `makeElementStub()`, `makeSandbox(extraGlobals)`, `loadInto(sandbox, file)`, `loadScript(files, extraGlobals)`, `evalIn(sandbox, code)`, `settle(ms)` | 01-02 |
| `tests/load-script.test.js` | TEST-02: Laden in Reihenfolge, Globals, let-Bindings via evalIn, Edge-Cases | 01-02 |
| `tests/idb-helpers.test.js` | idbGet/idbSet Round-Trip, Missing-Key → null, put-Semantik | 01-02 |
| `tests/bot-data-validators.test.js` | TEST-05: `_normLogik`, `_migriereLogik`, Extras `_botVarApply`, `_playerKeyApply` | 01-02 |
| `tests/outfit-import-parser.test.js` | TEST-05: `_oiDetectType`, `_oiBuildExecCode`, zwei `it.fails` (URI-Lücken) | 01-02 |
| `tests/bot-engine-escaping.test.js` | TEST-06: `_buildBotCode` + `new Function()`-Parse-Check, eigene Minimal-Sandbox | 01-03 |
| `bot-engine.js` (Zeile 49 — einzige Produktionsänderung der Phase) | `safeName` escaped zusätzlich `'`, `\r`, `\n` | 01-03 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: vm-Sandbox-Loader `tests/helpers/loadScript.js` + Ladetest + idbGet/idbSet-Round-Trip (TEST-02)</name>
  <precondition>`node_modules/vitest` und `node_modules/fake-indexeddb` existieren (Plan 01-01 hat `npm install` ausgeführt; bei frischem Clone zuerst `npm install`)</precondition>
  <reversibility rating="reversible">Reiner Test-Helfer; API kann jederzeit umgebaut werden, solange die vier Testdateien mitziehen — keine Produktions- oder Datenformat-Abhängigkeit.</reversibility>
  <files>tests/helpers/loadScript.js, tests/load-script.test.js, tests/idb-helpers.test.js</files>
  <read_first>items.js Zeilen 1-65 (IDB-Helfer) und Zeilen 6705-6735 (Auto-Init-IIFE) — NUR diese Bereiche, die Datei hat 11.700 Zeilen und darf nie komplett gelesen werden; bot-data.js Zeilen 78-107 und 110-152; .planning/phases/01-testfundament/01-RESEARCH.md Abschnitte "Pattern 1" und "Common Pitfalls" 1-4; .planning/phases/01-testfundament/01-PATTERNS.md Abschnitt "Shared Patterns"; tests/idb-canary.test.js</read_first>
  <action>
    Alle Dateien in ESM-Syntax (Vitest transformiert Test- und Helferdateien unabhängig vom `"type": "commonjs"` in package.json). Produktionsdateien werden ausschließlich als Text gelesen — nie per `import`/`require`.

    A) `tests/helpers/loadScript.js` (neu) exportiert genau diese Symbole:
    - `REPO_ROOT`: `path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..')` (Repo-Root; Imports: `node:fs`, `node:vm`, `node:path`, `node:url`).
    - `makeElementStub()`: liefert ein neues Plain-Object mit `classList` (`add`, `remove` No-op; `toggle` und `contains` liefern `false`), `style: {}`, `dataset: {}`, `innerHTML: ''`, `textContent: ''`, `innerText: ''`, `value: ''`, `checked: false`, `disabled: false`, `hidden: false`, `className: ''`, `id: ''`, `title: ''`, `children: []`, `childNodes: []`, `options: []`, `appendChild(c)`/`removeChild(c)`/`insertBefore(c)` liefern `c`, `remove`/`replaceChildren`/`append`/`prepend`/`setAttribute`/`removeAttribute`/`addEventListener`/`removeEventListener`/`focus`/`blur`/`click`/`select`/`scrollIntoView` No-op, `getAttribute` → `null`, `hasAttribute`/`contains`/`matches` → `false`, `dispatchEvent` → `true`, `querySelector`/`closest` → `null`, `querySelectorAll`/`getElementsByClassName`/`getElementsByTagName` → `[]`, `getBoundingClientRect` → `{top:0,left:0,width:0,height:0,right:0,bottom:0}`, `parentElement`/`parentNode`/`firstChild`/`lastChild`/`nextSibling`/`previousSibling` → `null`, `offsetWidth`/`offsetHeight`/`scrollTop`/`scrollHeight`/`clientWidth`/`clientHeight` → `0`.
    - `makeSandbox(extraGlobals = {})`: baut das Sandbox-Objekt mit EXAKT dieser Stub-Liste (Reihenfolge: Stubs, dann `...extraGlobals`, damit Tests überschreiben können): `console` (Host), `setTimeout`/`clearTimeout` (Host — echte Timer, damit async Code fortschreitet), `setInterval: () => 0` und `clearInterval: () => {}` (No-op — siehe Kontext Befund 1), `btoa`/`atob` (Host-Globals), `localStorage` als Map-basiertes Objekt mit `getItem(k)` → Wert oder `null`, `setItem(k, v)` → `String(v)`, `removeItem(k)`, `clear()`, `key(i)`, Getter `length`; `document` mit `getElementById`/`querySelector`/`createElement` → jeweils `makeElementStub()`, `querySelectorAll` → `[]`, `createTextNode` → `{}`, `addEventListener`/`removeEventListener` No-op, `body`/`head`/`documentElement` → Element-Stub, `readyState: 'complete'`, `title: ''`, `hidden: false`, `activeElement: null`; bare `addEventListener`/`removeEventListener` No-op direkt auf dem Sandbox-Objekt (bot-data.js ~Zeile 149 ruft `addEventListener` ohne `window.`-Präfix); `indexedDB: globalThis.indexedDB` (aus setupFiles; NICHT im Helfer prüfen oder werfen — der Canary aus 01-01 ist die einzige Wache); `_money: undefined`, `_rankData: undefined`, `_shop: undefined` (bot-engine.js Zeile 52 greift per Optional Chaining auf undeklarierte Bezeichner zu — Property muss existieren). Danach `sandbox.window = sandbox` (Selbstreferenz; damit ist `window.addEventListener` automatisch der bare Stub) und `vm.createContext(sandbox)`; Rückgabe: das Sandbox-Objekt. KEIN `navigator`, `location`, `alert`, `confirm`, `prompt`, `MutationObserver`, `fetch` — Probe zeigt: nicht nötig zum Laden; Tests, die sie brauchen, geben sie über `extraGlobals` mit.
    - `loadInto(sandbox, file)`: liest `path.join(REPO_ROOT, file)` als UTF-8-Text und führt `vm.runInContext(code, sandbox, { filename: file })` aus; Rückgabe `sandbox`. Kein try/catch — Ladefehler müssen im Test sichtbar werfen.
    - `loadScript(files, extraGlobals = {})`: wirft `TypeError('loadScript: files must be an array of repo-relative paths')`, wenn `!Array.isArray(files)`; erzeugt `makeSandbox(extraGlobals)`; ruft `loadInto` für jede Datei strikt in Array-Reihenfolge; Rückgabe: Sandbox. Kein Deduplizieren, kein Umsortieren.
    - `evalIn(sandbox, code)`: `vm.runInContext(code, sandbox)` — Zugriff auf Top-Level-`let`/`const`-Bindings (`_bots`, `_botVars`, `_playerKeys`), die keine Sandbox-Properties sind (Kontext Befund 4).
    - `settle(ms = 100)`: `new Promise(r => setTimeout(r, ms))` — lässt die async Init-IIFEs (bot-data.js) nach dem Laden durchlaufen.
    Kopfkommentar (deutsch, 5-8 Zeilen): Zweck, warum `setInterval` No-op ist, warum `getElementById` einen Stub statt `null` liefert, warum `let`-Globals `evalIn` brauchen — mit Zeilenbezug auf items.js/bot-data.js.

    B) `tests/load-script.test.js` (neu): `import { describe, it, expect, beforeAll } from 'vitest'` und `import { loadScript, evalIn } from './helpers/loadScript.js'`. `describe('vm-Sandbox-Loader (TEST-02)')`:
    1. `beforeAll`: `ctx = loadScript(['items.js', 'bot-data.js', 'outfit-import.js'])`.
    2. it `lädt items.js, bot-data.js, outfit-import.js in Ladereihenfolge in eine gemeinsame Sandbox`: für jeden Namen aus `['idbGet','idbSet','_normLogik','_migriereLogik','_botVarApply','_playerKeyApply','_oiDetectType','_oiBuildExecCode']` `expect(typeof ctx[name]).toBe('function')`; `expect(ctx.window).toBe(ctx)`.
    3. it `let-Globals sind keine Sandbox-Properties, aber per evalIn erreichbar`: `expect(ctx._bots).toBeUndefined()`; `expect(Array.isArray(evalIn(ctx, '_bots'))).toBe(true)`; `expect(typeof evalIn(ctx, '_botVars')).toBe('object')`.
    4. it `leere Dateiliste liefert Basis-Sandbox ohne Produktions-Globals`: `const s = loadScript([])`; `expect(typeof s.idbGet).toBe('undefined')`; `expect(typeof s.localStorage.getItem).toBe('function')`; `expect(s.localStorage.getItem('x')).toBeNull()`; `expect(s.window).toBe(s)`; `expect(typeof s.document.getElementById('any').classList.toggle).toBe('function')`.
    5. it `Nicht-Array-Argument wird mit TypeError abgelehnt`: `expect(() => loadScript('items.js')).toThrow(TypeError)`; `expect(() => loadScript(null)).toThrow(TypeError)`.
    6. it `Reihenfolge ist Aufrufersache: bot-data.js ohne items.js wirft ReferenceError`: `expect(() => loadScript(['bot-data.js'])).toThrow(/idbGet is not defined/)`.
    7. it `Doppeltes Laden derselben Datei wirft SyntaxError (const-Redeklaration)`: `expect(() => loadScript(['items.js', 'items.js'])).toThrow(SyntaxError)`.
    8. it `Stubs neutralisieren Timer und DOM beim Laden`: `expect(ctx.setInterval(() => {}, 10)).toBe(0)`; `expect(ctx.document.querySelectorAll('.x')).toEqual([])`.

    C) `tests/idb-helpers.test.js` (neu): `import { describe, it, expect, beforeAll } from 'vitest'`, `import { loadScript } from './helpers/loadScript.js'`. `describe('idbGet/idbSet über die Sandbox (TEST-02, Canary-Bezug TEST-03)')`, `beforeAll`: `ctx = loadScript(['items.js'])`.
    1. it `Sandbox nutzt das fake-indexeddb aus setupFiles`: `expect(ctx.indexedDB).toBe(globalThis.indexedDB)`; `expect(typeof ctx.indexedDB.open).toBe('function')`.
    2. it `idbSet → idbGet Round-Trip`: `expect(await ctx.idbSet('TEST_rt', { a: 1, list: [1, 2] })).toBe(true)`; `expect(JSON.stringify(await ctx.idbGet('TEST_rt'))).toBe('{"a":1,"list":[1,2]}')` (String-Vergleich, weil das Ergebnis aus der Host-Realm des fake-indexeddb stammt — kein Realm-Vergleich per toStrictEqual).
    3. it `unbekannter Schlüssel liefert null`: `expect(await ctx.idbGet('TEST_missing')).toBeNull()`.
    4. it `put-Semantik: zweites idbSet überschreibt`: `idbSet('TEST_over', 1)` dann `idbSet('TEST_over', 2)`; `expect(await ctx.idbGet('TEST_over')).toBe(2)`.
    Schlüssel-Präfix `TEST_` verwenden, damit nichts mit Produktionsschlüsseln (`BC_Bots_v2` …) kollidiert.

    D) Laufen lassen: `npx vitest run tests/load-script.test.js tests/idb-helpers.test.js` → grün. Dann `npm test` → grün (Canary + diese beiden Dateien).

    E) Commit — nur diese drei Pfade: `git add tests/helpers/loadScript.js tests/load-script.test.js tests/idb-helpers.test.js`; `git diff --cached --name-only` muss exakt diese drei Pfade listen; die vorbestehenden ` M`-Einträge (items.js, bot-data.js, index.html …) bleiben unstaged. Message: `test(01-02): add vm sandbox loader and idb round-trip tests (TEST-02)`. Vor dem Editieren/Committen `git diff -- items.js bot-data.js outfit-import.js index.html | sha1sum` notieren und am Task-Ende vergleichen — muss identisch sein (Produktion nicht angefasst).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/load-script.test.js tests/idb-helpers.test.js</automated>
    <fails_when>non-zero exit, oder `failed` in der `Tests`-Summary-Zeile, oder `Test Files  2 passed` fehlt</fails_when>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./tests/helpers/loadScript.js').then(m=>console.log(['REPO_ROOT','makeElementStub','makeSandbox','loadInto','loadScript','evalIn','settle'].every(k=>k in m)))"` gibt `true` aus (ESM-Import im Host funktioniert, alle sieben Exporte vorhanden)
    - `grep -c "setInterval: () => 0" tests/helpers/loadScript.js` = 1; `grep -c "globalThis.indexedDB" tests/helpers/loadScript.js` = 1; `grep -c "_money: undefined" tests/helpers/loadScript.js` = 1; `grep -c "sandbox.window = sandbox" tests/helpers/loadScript.js` = 1; `grep -c "runInContext" tests/helpers/loadScript.js` >= 2
    - `grep -c "idbGet is not defined" tests/load-script.test.js` = 1; `grep -c "toThrow(SyntaxError)" tests/load-script.test.js` = 1; `grep -c "toThrow(TypeError)" tests/load-script.test.js` >= 1; `grep -c "^\s*it(" tests/load-script.test.js` >= 7
    - `grep -c "TEST_rt" tests/idb-helpers.test.js` >= 2; `grep -c "toBeNull()" tests/idb-helpers.test.js` >= 1
    - `test "$(grep -Ec "(import|require)\(?['\"]\.\./(items|bot-data|outfit-import|bot-engine)\.js" tests/load-script.test.js tests/idb-helpers.test.js tests/helpers/loadScript.js | awk -F: '{s+=$2} END {print s+0}')" = 0` (keine Produktionsdatei wird per import/require geladen)
    - `npm test` → Exit 0, `Test Files  3 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` = `tests/helpers/loadScript.js tests/idb-helpers.test.js tests/load-script.test.js`; `git log --format=%h d9ad6af..HEAD -- items.js bot-data.js outfit-import.js index.html` leer
  </acceptance_criteria>
  <done>Loader lädt die drei Produktionsdateien unverändert in Ladereihenfolge; alle acht Globals erreichbar; Edge-Cases (leer, Nicht-Array, Reihenfolge, Doppelladen) getestet; idbGet/idbSet Round-Trip grün über fake-indexeddb; Commit enthält exakt drei Testpfade.</done>
</task>

<task type="auto">
  <name>Task 2: Bot-Validatoren-Tests — `_normLogik` und `_migriereLogik` (Pflicht), `_botVarApply`/`_playerKeyApply` (Extras) (TEST-05)</name>
  <files>tests/bot-data-validators.test.js</files>
  <read_first>bot-data.js Zeilen 1-45 (`_normLogik`, `_migriereLogik`), 78-107 (`_saveBots`, `_botVarApply`, `_playerKeyApply`), 110-152 (async Init-IIFE — ruft `_migriereLogik()` nach dem IDB-Load); tests/helpers/loadScript.js (aus Task 1); .planning/phases/01-testfundament/01-RESEARCH.md "Pitfall 6"</read_first>
  <action>
    `tests/bot-data-validators.test.js` (neu): `import { describe, it, expect, beforeAll } from 'vitest'`, `import { loadScript, evalIn, settle } from './helpers/loadScript.js'`. `beforeAll`: `ctx = loadScript(['items.js', 'bot-data.js'])` und danach `await settle(150)` — zwingend, weil die Init-IIFE von bot-data.js (Zeile ~110-152) nach dem `idbGet`-Resolve selbst `_migriereLogik()` aufruft; ohne Wartezeit könnte sie unsere Fixtures vor der Assertion bereinigen (Race). Nutzerentscheidung 2: Validatoren = `_normLogik`/`_migriereLogik`; die Extras sind zusätzlich, nicht Voraussetzung.

    `describe('_normLogik (TEST-05)')`:
    1. it `setzt logik der ersten Bedingung auf und, lässt Folgeelemente unverändert und liefert dieselbe Referenz`: `const arr = [{ logik: 'oder', typ: 'wort' }, { logik: 'oder' }]`; `expect(ctx._normLogik(arr)).toBe(arr)`; `expect(arr[0].logik).toBe('und')`; `expect(arr[0].typ).toBe('wort')`; `expect(arr[1].logik).toBe('oder')`.
    2. it `normalisiert alle Legacy-Werte an erster Stelle`: für jeden Wert aus `['oder', 'und_oder', 'und_nicht']` → `ctx._normLogik([{ logik: v }])[0].logik === 'und'`; für `[{ typ: 'wort' }]` (ohne logik) → `[0].logik === 'und'`.
    3. it `ungültige Eingaben werden unverändert und ohne Exception durchgereicht`: `expect(ctx._normLogik(null)).toBeNull()`; `expect(ctx._normLogik(undefined)).toBeUndefined()`; `expect(ctx._normLogik('x')).toBe('x')`; `expect(ctx._normLogik(42)).toBe(42)`; `const o = { logik: 'oder' }; expect(ctx._normLogik(o)).toBe(o); expect(o.logik).toBe('oder')` (Objekt ist kein Array → nicht mutiert); `const e = []; expect(ctx._normLogik(e)).toBe(e); expect(e.length).toBe(0)`; `expect(ctx._normLogik([null])).toEqual([null])` (falsy erstes Element → keine Mutation, kein Throw).

    `describe('_migriereLogik (TEST-05)')` — Fixtures immer über `const bots = evalIn(ctx, '_bots'); bots.length = 0; bots.push(...)` setzen (Referenz mutieren; `ctx._bots` wäre undefined):
    4. it `bereinigt Legacy-Logik in triggers.bedingungen, triggers.ifBedingungen und events.bedingungen und liefert die Anzahl`: ein Bot `{ id: 'b1', name: 'B', triggers: [{ bedingungen: [{ logik: 'oder' }, { logik: 'oder' }], ifBedingungen: [{ logik: 'und_nicht' }] }], events: [{ bedingungen: [{ logik: 'und_oder' }] }] }`; `expect(ctx._migriereLogik()).toBe(3)`; danach `bedingungen[0].logik === 'und'`, `bedingungen[1].logik === 'oder'` (zweites Element unangetastet), `ifBedingungen[0].logik === 'und'`, `events[0].bedingungen[0].logik === 'und'`.
    5. it `ist idempotent`: direkt nach Test 4 (gleiches `_bots`) `expect(ctx._migriereLogik()).toBe(0)`.
    6. it `Bots ohne triggers/events, leere und bereits korrekte Bedingungen sind unkritisch`: `_bots` = `[{ id: 'b2' }, { id: 'b3', triggers: [{ bedingungen: [] }, {}, { bedingungen: [{ logik: 'und' }] }], events: [{}] }]`; `expect(() => ctx._migriereLogik()).not.toThrow()`; `expect(ctx._migriereLogik()).toBe(0)`.
    7. it `persistiert bereinigte Bots unter BC_Bots_v2`: `_bots` = `[{ id: 'b4', name: 'P', triggers: [{ bedingungen: [{ logik: 'oder', typ: 'wort' }] }] }]`; `expect(ctx._migriereLogik()).toBe(1)`; `await settle(100)` (`_saveBots` schreibt fire-and-forget); `const saved = await ctx.idbGet('BC_Bots_v2')`; `expect(JSON.stringify(saved)).toContain('"logik":"und"')`; `expect(JSON.stringify(saved)).toContain('"id":"b4"')`.

    `describe('Extras: _botVarApply / _playerKeyApply (Guard-Clauses)')` — Ermessen des Planers, nicht Teil der Pflicht:
    8. it `_botVarApply verwirft null-memberNum und leeren Namen, akzeptiert 0 als memberNum`: `ctx._botVarApply(null, 'x', 1)`; `ctx._botVarApply(5, '', 1)`; `ctx._botVarApply(5, 'gold', 3)`; `ctx._botVarApply(0, 'z', 'ok')`; `const vars = evalIn(ctx, '_botVars')`; `expect(vars['5']).toEqual({ gold: 3 })`; `expect(vars['0']).toEqual({ z: 'ok' })`; `expect(Object.keys(vars).sort()).toEqual(['0', '5'])`.
    9. it `_playerKeyApply verwirft null-memberNum und unbekannte Schlüssel, behält den Namen`: `ctx._playerKeyApply(null, 'A', 'gold', true)`; `ctx._playerKeyApply(7, 'Anna', 'gold', true)`; `ctx._playerKeyApply(7, '', 'platinum', true)`; `const keys = evalIn(ctx, '_playerKeys')`; `expect(keys['7']).toEqual({ name: 'Anna', bronze: false, silver: false, gold: true })`; `expect(keys['7'].platinum).toBeUndefined()`; `expect(Object.keys(keys)).toEqual(['7'])`.

    Laufen lassen: `npx vitest run tests/bot-data-validators.test.js` → grün; `npm test` → grün. Commit nur `tests/bot-data-validators.test.js`: `git add tests/bot-data-validators.test.js`, `git diff --cached --name-only` = genau dieser Pfad; Message `test(01-02): add bot-data validator tests for _normLogik/_migriereLogik (TEST-05)`. bot-data.js bleibt unangetastet (Diff-Hash-Vergleich wie in Task 1).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/bot-data-validators.test.js</automated>
    <fails_when>non-zero exit, oder `failed` in der `Tests`-Summary-Zeile, oder weniger als `9 passed` in der Summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "_normLogik" tests/bot-data-validators.test.js` >= 8; `grep -c "_migriereLogik" tests/bot-data-validators.test.js` >= 5; `grep -c "evalIn(ctx, '_bots')" tests/bot-data-validators.test.js` >= 3
    - `grep -c "toBe(3)" tests/bot-data-validators.test.js` >= 1 und `grep -c "toBe(0)" tests/bot-data-validators.test.js` >= 2 (Anzahl-Rückgabe und Idempotenz)
    - `grep -c "BC_Bots_v2" tests/bot-data-validators.test.js` = 1; `grep -c "settle(" tests/bot-data-validators.test.js` >= 2
    - `grep -c "toBeNull()" tests/bot-data-validators.test.js` >= 1 und `grep -c "'und_nicht'" tests/bot-data-validators.test.js` >= 1 und `grep -c "'und_oder'" tests/bot-data-validators.test.js` >= 1 (ungültige/Legacy-Eingaben)
    - `grep -c "loadScript(\['items.js', 'bot-data.js'\])" tests/bot-data-validators.test.js` = 1 (Ladereihenfolge items.js vor bot-data.js)
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/bot-data-validators.test.js`; `git log --format=%h d9ad6af..HEAD -- bot-data.js` leer
  </acceptance_criteria>
  <done>`_normLogik` und `_migriereLogik` sind mit gültigen, Legacy- und ungültigen Eingaben getestet (Normalisierung, Anzahl, Idempotenz, Persistenz unter BC_Bots_v2, kein Throw bei Nicht-Arrays); Extras für Guard-Clauses grün; bot-data.js unverändert.</done>
</task>

<task type="auto">
  <name>Task 3: Outfit-Import-Parser-Tests — `_oiDetectType` und `_oiBuildExecCode` mit echtem lz-string (TEST-05)</name>
  <files>tests/outfit-import-parser.test.js</files>
  <read_first>outfit-import.js Zeilen 344-472 (`_oiDetectType`, `_oiBuildExecCode` inkl. aller Rückgabezweige); tests/helpers/loadScript.js; .planning/phases/01-testfundament/01-RESEARCH.md "Code Examples" und "Don't Hand-Roll"</read_first>
  <action>
    `tests/outfit-import-parser.test.js` (neu): `import { describe, it, expect, beforeAll } from 'vitest'`, `import LZString from 'lz-string'` (UMD-Default-Import; Vitest-Interop liefert das Objekt mit `compressToBase64` usw.), `import { loadScript } from './helpers/loadScript.js'`. `beforeAll`: `ctx = loadScript(['items.js', 'outfit-import.js'], { LZString })` und zusätzlich `ctxNoLz = loadScript(['items.js', 'outfit-import.js'])` (ohne LZString-Global). Hilfsfunktion `parses(code)` = `expect(() => new Function(code)).not.toThrow()` — NUR parsen, NIE aufrufen (der Code enthält `AssetGet`/`Player`, die nur im BC-Tab existieren).

    Fixtures (deterministisch, probe-verifiziert mit lz-string 1.5.0):
    - `ARR = [{ Group: 'Cloth', Name: 'Dress', Color: '#ff0000' }, { Group: 'Hat', Name: 'Beret' }]`; `B64 = LZString.compressToBase64(JSON.stringify(ARR))`; `URI_MIXED = LZString.compressToEncodedURIComponent(JSON.stringify(ARR))` (enthält `+` und `-`).
    - `ARR_URI = [{ Group: 'Hat', Name: 'Dress', Color: '#000000' }]`; `URI_DASH = LZString.compressToEncodedURIComponent(JSON.stringify(ARR_URI))` (enthält `-`, kein `+`/`$` → wird als `lzuri` erkannt); zur Sicherheit im Test `expect(URI_DASH).toMatch(/-/)` und `expect(URI_DASH).not.toMatch(/[+$]/)`.
    - `RAW_JS = 'ServerPlayerInventoryLoad([])'`; `GARBAGE = 'A'.repeat(40)`; `STR_B64 = LZString.compressToBase64('ChatRoomSendLocal("hi")')` (komprimiert, aber kein JSON).

    `describe('_oiDetectType (TEST-05)')`:
    1. it `erkennt echte compressToBase64-Ausgabe als lzbase64`: `expect(ctx._oiDetectType(B64)).toBe('lzbase64')`; mit Whitespace-Rand `'  ' + B64 + '\n'` ebenfalls `lzbase64` (trim).
    2. it `Leerstring, Whitespace, 19-Zeichen-Strings und roher JS-Code sind js`: `''`, `'   '`, `'A'.repeat(19)`, `RAW_JS`, `'Player.Name'` → jeweils `'js'`.
    3. it `Grenze: 20 Base64-Zeichen sind lzbase64, Bindestrich-Alphabet ist lzuri, Dollar ist js`: `'A'.repeat(20)` → `'lzbase64'`; `'A'.repeat(19) + '-'` → `'lzuri'`; `URI_DASH` → `'lzuri'`; `'A'.repeat(19) + '$'` → `'js'`.
    4. `it.fails` `erkennt compressToEncodedURIComponent-Ausgaben mit + und - als LZ-Code — bekannte Lücke (URI-Alphabet ist + - $, nicht - _ . ~); Fix nicht in Phase 1`: `expect(ctx._oiDetectType(URI_MIXED)).not.toBe('js')`. (Aktuelles Verhalten: `'js'` — der Test ist absichtlich als erwartet-fehlschlagend markiert und wird rot, sobald jemand die Lücke schließt; dann `.fails` entfernen.)

    `describe('_oiBuildExecCode (TEST-05)')`:
    5. it `gültiger lzbase64-Code erzeugt den Apply-Pfad mit dekodiertem Array`: `const out = ctx._oiBuildExecCode(B64)`; `expect(out).toContain('var _raw=' + JSON.stringify(ARR))`; `expect(out).toContain('AssetGet(')`; `parses(out)`; Whitespace-gepolstert (`'  ' + B64 + '  '`) → identische Ausgabe.
    6. it `roher JS-Code wird nur in den try/catch-Wrapper gehüllt`: `const out = ctx._oiBuildExecCode(RAW_JS)`; `expect(out).toContain(RAW_JS)`; `expect(out).not.toContain('var _raw=')`; `expect(out).toMatch(/^\(function\(\)\{ try \{/)`; `parses(out)`.
    7. it `ungültige Base64-ähnliche Eingabe erreicht nie den Apply-Pfad`: `GARBAGE` → Ausgabe enthält `GARBAGE`, enthält nicht `var _raw=`, parst.
    8. it `komprimierter Nicht-JSON-Inhalt fällt auf den Roh-Wrapper zurück`: `STR_B64` → Ausgabe enthält nicht `var _raw=`, enthält `STR_B64` (nicht den dekomprimierten Text), parst.
    9. it `ohne LZString-Global bleibt auch gültiger lzbase64-Code roh (typeof-Guard)`: `const out = ctxNoLz._oiBuildExecCode(B64)`; `expect(out).not.toContain('var _raw=')`; `expect(out).toContain(B64)`; `parses(out)`.
    10. it `komprimierter JSON-String-Literal landet als Code im Wrapper`: `const s = LZString.compressToBase64('"ChatRoomSendLocal(1)"')`; `const out = ctx._oiBuildExecCode(s)`; `expect(out).toContain('"ChatRoomSendLocal(1)"')`; `expect(out).not.toContain('var _raw=')`; `parses(out)`.
    11. `it.fails` `lzuri-Code (nur Bindestrich) wird über den URI-Decoder entpackt — bekannte Lücke (Base64-Decoder liefert truncated Garbage, URI-Decoder wird nie versucht); Fix nicht in Phase 1`: `expect(ctx._oiBuildExecCode(URI_DASH)).toContain('var _raw=' + JSON.stringify(ARR_URI))`. (Aktuell: Roh-Wrapper mit `URI_DASH` — Test absichtlich als erwartet-fehlschlagend markiert.)

    Beide `it.fails`-Tests bekommen einen Kommentar mit Verweis: "Dokumentierter Befund Phase 1 (Planer-Probe 2026-09-12); outfit-import.js bleibt in Phase 1 unverändert; Kandidat für Phase 2 oder Quick-Task."

    Laufen lassen: `npx vitest run tests/outfit-import-parser.test.js` → grün (11 passed; die beiden `it.fails` zählen als passed). Dann `npm test` → grün. Commit nur `tests/outfit-import-parser.test.js`: `git add tests/outfit-import-parser.test.js`; `git diff --cached --name-only` = genau dieser Pfad; Message `test(01-02): add outfit-import parser tests with real lz-string fixtures (TEST-05)`. outfit-import.js unangetastet (Diff-Hash-Vergleich). Im SUMMARY.md die beiden dokumentierten Lücken unter "Deferred findings" aufführen.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/outfit-import-parser.test.js</automated>
    <fails_when>non-zero exit, oder `failed` in der `Tests`-Summary-Zeile, oder weniger als `11 passed` in der Summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "compressToBase64" tests/outfit-import-parser.test.js` >= 3; `grep -c "compressToEncodedURIComponent" tests/outfit-import-parser.test.js` >= 2; `grep -c "from 'lz-string'" tests/outfit-import-parser.test.js` = 1
    - `grep -n "it.fails(" tests/outfit-import-parser.test.js` listet zwei Zeilen (Test 4: `_oiDetectType`-URI-Alphabet, Test 11: `_oiBuildExecCode`-Decoder-Reihenfolge)
    - `grep -c "var _raw=" tests/outfit-import-parser.test.js` >= 5; `grep -c "AssetGet(" tests/outfit-import-parser.test.js` >= 1
    - `grep -c "new Function(" tests/outfit-import-parser.test.js` >= 1 (Parse-Helfer vorhanden)
    - `test "$(grep -Ec "_oiBuildExecCode\([^)]*\)\)\(\)" tests/outfit-import-parser.test.js)" = 0` (generierter Code wird nie aufgerufen)
    - `grep -c "{ LZString }" tests/outfit-import-parser.test.js` = 1 und `grep -c "ctxNoLz" tests/outfit-import-parser.test.js` >= 2 (Guard-Test ohne LZString)
    - `grep -c "'lzuri'" tests/outfit-import-parser.test.js` >= 2; `grep -c "'js'" tests/outfit-import-parser.test.js` >= 3
    - `npm test` → Exit 0, `Test Files  5 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/outfit-import-parser.test.js`; `git log --format=%h d9ad6af..HEAD -- outfit-import.js` leer
  </acceptance_criteria>
  <done>Parser mit gültigen (lzbase64 → Apply-Pfad) und ungültigen (roh, Garbage, Nicht-JSON, ohne LZString) Eingaben getestet; jede Ausgabe parst per `new Function()`; zwei bekannte URI-Lücken per `it.fails` dokumentiert; outfit-import.js unverändert; volle Suite grün mit 5 Testdateien.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Harness (Host-Realm) → vm-Sandbox (Produktionscode) | Produktionsdateien laufen mit Host-`setTimeout`, Host-`console` und dem fake-indexeddb des Workers; `vm` ist KEINE Sicherheitsgrenze |
| Test → generierter Code (`_oiBuildExecCode`) | Rückgabe ist Code für den BC-Spiel-Tab; wird im Test nur geparst |
| Git-Arbeitsbaum → Repository | unrelated Änderungen in items.js/bot-data.js/outfit-import.js liegen unstaged neben den neuen Testdateien |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-02 | Repudiation | tests/helpers/loadScript.js (`indexedDB: globalThis.indexedDB`) | medium | mitigate | Helfer prüft nicht selbst (bewusst), damit genau ein Canary-Test (01-01) die Wache bleibt; idb-helpers.test.js beweist zusätzlich `ctx.indexedDB === globalThis.indexedDB` und einen echten Round-Trip |
| T-1-04 | Tampering | vm-Sandbox mit Host-Timern | low | accept | Geladener Code ist der eigene Produktionscode des Repos; `setInterval` ist No-op, Sandbox hat kein `fs`/`process`/`require`; kein Fremdcode wird in die Sandbox geladen |
| T-1-05 | Elevation of Privilege | `new Function(code)` in outfit-import-parser.test.js | medium | mitigate | Nur Parse (`new Function(code)` ohne Aufruf); Acceptance verbietet das Muster `new Function(...)()`; Code enthält BC-Globals, die im Test ohnehin fehlen würden |
| T-1-07 | Tampering | lz-string devDependency | low | mitigate | Exakt 1.5.0 = Produktions-CDN-Version; nur als Sandbox-Global in Tests, nie in der Auslieferung; Pin in package.json (01-01) |
| T-1-06 | Tampering | Git-Staging | high | mitigate | Nur explizite Testpfade stagen; Diff-Hash der Produktionsdateien vor/nach jedem Task identisch; `git log d9ad6af..HEAD -- items.js bot-data.js outfit-import.js` bleibt leer |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine neuen Installationen in diesem Plan; Pakete aus 01-01 (package-lock.json, Legitimitäts-Checkpoint) |
</threat_model>

<verification>
- `npm test` grün, `Test Files  5 passed` (Canary, load-script, idb-helpers, bot-data-validators, outfit-import-parser).
- `git log --format=%h d9ad6af..HEAD -- items.js bot-data.js outfit-import.js index.html` gibt nichts aus.
- `git status --short` zeigt die neun vorbestehenden ` M`-Einträge weiterhin unstaged (Spalte 1 Leerzeichen).
- Die drei Commits dieses Plans enthalten zusammen exakt fünf Pfade unter `tests/`.
</verification>

<success_criteria>
- TEST-02: items.js, bot-data.js, outfit-import.js laden ohne Quelländerung per vm-Sandbox in Ladereihenfolge; Tests greifen auf deren Globals zu (Funktionen direkt, `let`-Bindings via `evalIn`); Edge-Cases leer/Reihenfolge/Doppelladen sind getestet.
- TEST-05: `_normLogik`/`_migriereLogik` und `_oiDetectType`/`_oiBuildExecCode` sind mit gültigen und ungültigen Eingaben getestet; ungültige Eingaben werden nachweislich nicht verarbeitet (keine Mutation bzw. kein Apply-Pfad).
- Zwei dokumentierte Parser-Lücken (URI-Alphabet, Decoder-Reihenfolge) sind als `it.fails` festgehalten und im SUMMARY als Deferred findings gelistet.
</success_criteria>

<output>
Create `.planning/phases/01-testfundament/01-02-SUMMARY.md` when done — inkl. finaler Stub-Liste des Loaders, Testanzahl pro Datei, Commit-Hashes und Abschnitt "Deferred findings" (URI-Alphabet in `_oiDetectType`, Decoder-Reihenfolge in `_oiBuildExecCode`).
</output>
