---
phase: 01-testfundament
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/bot-engine-escaping.test.js
  - bot-engine.js
autonomous: true
requirements: [TEST-06]
tags: [bot-engine, escaping, code-generation, security]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`_buildBotCode(bot)` erzeugt für jeden Namen aus der adversarialen Liste (Backtick, `${`, einfaches und doppeltes Anführungszeichen, Backslash, Backslash+Quote, `\\n`, `\\r\\n`, Umlaute/Unicode) Code, den `new Function(code)` ohne SyntaxError akzeptiert (TEST-06, Erfolgskriterium 5)"
    - "Der Bot-Name überlebt die Generierung unverfälscht: das `botName:'…'`-Literal im generierten Code evaluiert (isoliert, nur das Literal) exakt zum ursprünglichen `bot.name` — Escaping ist verlustfrei, nicht nur parsbar (TEST-06)"
    - "Vor dem Fix schlägt der Test für `O'Brien` (und die weiteren Quote-/Newline-Fälle) nachweislich fehl (RED committet), nach dem Fix ist er grün (GREEN committet) — der Test lockt den Bug (Nutzerentscheidung 1)"
    - "Die einzige Produktionsänderung der Phase ist Zeile 49 von bot-engine.js (`git diff --numstat d9ad6af HEAD -- bot-engine.js` = `1 1`); `node --check bot-engine.js` bleibt grün; index.html und alle anderen Produktionsdateien sind byte-identisch zu ihrem Vorzustand (Nutzerentscheidung 1)"
    - "Adversariale Strings in Trigger-Bedingungen, Aktionen, Events und Settings (Base64-Pfad) brechen den generierten Code ebenfalls nicht — der Test deckt den gesamten Nutzerdaten-Eingang ab, nicht nur `bot.name` (TEST-06)"
    - "Annahme (TEST-06, Edge unclassified): Testgrenze ist `new Function(code)` = Parsen durch den nativen JS-Parser; der generierte Code wird nie ausgeführt (er referenziert `bcModSdk`, `ChatRoom`, `Player`, die nur im Spiel-Tab existieren). Der Fix escaped neben `'` (verifizierter Bug, Nutzerentscheidung 1) auch `\\r`/`\\n` — Planer-Ermessen: dieselbe Zeile, dieselbe Fehlerklasse (probe-verifiziert: `Multi\\nLine` → `Invalid or unexpected token`), Konvention wie `escJsAttr` in items.js. Soll strikt nur `'` gefixt werden: die beiden letzten `.replace`-Aufrufe und die zwei Newline-Fixtures entfernen"
  artifacts:
    - path: "tests/bot-engine-escaping.test.js"
      provides: "TEST-06: it.each über adversariale Namen + Voll-Payload-Test + Name-Roundtrip; eigene Minimal-vm-Sandbox (window, console, btoa, atob, _money/_rankData/_shop)"
      contains: "O'Brien"
    - path: "bot-engine.js"
      provides: "safeName escaped Backslash, Backtick, einfaches Anführungszeichen, CR, LF"
      contains: "replace(/'/g"
  key_links:
    - from: "tests/bot-engine-escaping.test.js"
      to: "bot-engine.js"
      via: "fs.readFileSync + vm.runInContext in eine Minimal-Sandbox; `_buildBotCode` wird als Sandbox-Property aufgerufen"
      pattern: "runInContext"
    - from: "bot-engine.js (Zeile 49, safeName)"
      to: "bot-engine.js (Zeilen 802, 1826, 2923, 3213, 3241, 3244)"
      via: "`${safeName}` wird in einfach gequotete Strings des generierten Codes interpoliert — deshalb ist `'` das kritische Zeichen, nicht der Backtick"
      pattern: "\\$\\{safeName\\}"
  prohibitions:
    - requirement_id: TEST-06
      category: safety
      verification: judgment
      statement: "Generierter Bot-Code wird im Test ausschließlich geparst (`new Function(code)`), niemals aufgerufen — kein `eval`, kein `(...)()` auf dem Ergebnis"
    - requirement_id: TEST-06
      category: safety
      verification: judgment
      statement: "Keine andere Änderung an bot-engine.js als Zeile 49; keine Änderung an index.html, items.js, bot-data.js, bot-ui.js, outfit-import.js oder loader.js"
    - requirement_id: TEST-06
      category: safety
      verification: judgment
      statement: "`git add bot-engine.js` nur, nachdem `git status --short bot-engine.js` VOR dem Edit leer war — andernfalls STOPP (sonst würden fremde Hunks mitcommittet); nie `git add -A`, `git add .` oder `git commit -a`"
    - requirement_id: TEST-06
      category: safety
      verification: judgment
      statement: "Kein Import des Loaders aus Plan 01-02 (`tests/helpers/loadScript.js`) — dieser Plan läuft parallel zu 01-02 und darf nicht davon abhängen"
---

<objective>
Den verifizierten Escaping-Bug in `bot-engine.js` `_buildBotCode` (Zeile 49, `safeName` escaped nur Backslash und Backtick, landet aber in einfach gequoteten Strings) mit einem Test locken und minimal fixen: erst der rote Test (adversariale Bot-Namen inkl. `O'Brien` → `new Function()` wirft SyntaxError), dann die eine geänderte Zeile, die `'`, `\r` und `\n` zusätzlich escaped. Einzige Produktionsänderung der Phase; Commit mit chirurgischer Präzision (bot-engine.js ist im Arbeitsbaum unverändert — verifiziert 2026-09-12).

Purpose: TEST-06 fordert syntaktisch gültigen Code für Nutzerdaten mit Backticks, `${` und Sonderzeichen; Erfolgskriterium 5 verlangt "ohne SyntaxError". Ohne Fix ist das Kriterium für Namen mit Anführungszeichen unerfüllbar (RESEARCH Pitfall 5, Assumption A1; Nutzerentscheidung 1: fixen).
Output: `tests/bot-engine-escaping.test.js`, `bot-engine.js` (Zeile 49).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testfundament/01-RESEARCH.md
@.planning/phases/01-testfundament/01-01-SUMMARY.md

Planer-Probe (2026-09-12, Node v24.12.0) an einer Scratch-Kopie von bot-engine.js — verbindliche Befunde:
1. bot-engine.js lädt in einer Minimal-Sandbox aus `console`, `window` (Selbstreferenz), `btoa`, `atob`, `_money`, `_rankData`, `_shop` (alle drei als `undefined`-Property). Ohne `btoa` wirft `_buildBotCode` `ReferenceError: btoa is not defined` (Zeile 53, `btoa(unescape(encodeURIComponent(_cfgRaw)))`); ohne `_money`-Property `ReferenceError: _money is not defined` (Zeile 52). Kein DOM, kein IDB, kein items.js nötig — deshalb eigene Inline-Sandbox statt Loader aus 01-02 (Parallelität).
2. Ergebnis ORIGINAL (Zeile 49 unverändert) mit `new Function(code)`: `NormalName`, `Back\`tick`, `Dollar${brace}`, `Quote"Double`, `Trail\\`, `Umlaut Ärger ✓` → OK; `O'Brien`, `Combo \`${x}\` and 'quote'`, `Back\\'slash` → `missing ) after argument list`; `Multi\nLine`, `CR\rLF\r\n` → `Invalid or unexpected token`.
3. Ergebnis FIXED (Zeile 49 mit `.replace(/'/g,"\\'").replace(/\r/g,'\\r').replace(/\n/g,'\\n')` ergänzt): alle elf Fixtures parsen; das `botName:'…'`-Literal evaluiert zu `O'Brien\nX` für den Namen `O'Brien\nX` (verlustfrei).
4. `git status --short bot-engine.js` ist leer (Datei im Arbeitsbaum unverändert; die ~900 unrelated Zeilen liegen in bc-icons.js, bot-data.js, bot-ui.js, index.html, inventar.js, items.js, money.js, rank.js, shop.js). Ein einfaches `git add bot-engine.js` staged daher exakt den Fix-Hunk.
5. `safeName` wird an sechs Stellen interpoliert (Zeilen 802, 1826, 2923, 3213, 3241, 3244), immer in einfach gequoteten Strings des generierten Codes (z. B. `botName:'${safeName}'`). Alle anderen Nutzerdaten (Trigger, Events, Settings, Szenen) gehen per JSON → Base64 (`cfgJson`, `trigsJson`, `eventsJson`, `scenesJson`); `bot.id` wird per `\W`→`_` bereinigt bzw. per `JSON.stringify` eingebettet.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 1 gesamt; Plan-Zuordnung in Klammern)

| Pfad | Symbole / Inhalt | Plan |
|------|------------------|------|
| `package.json`, `package-lock.json`, `.gitignore` | npm-Manifest (nur devDependencies, `test` = `vitest run`), Lockfile, Ignore-Liste | 01-01 |
| `vitest.config.js`, `vitest.no-idb.config.js` | Vitest-Config mit setupFiles; Meta-Config ohne setupFiles (TEST-03) | 01-01 |
| `tests/setup/fake-indexeddb.js`, `tests/idb-canary.test.js` | Side-effect-Import `fake-indexeddb/auto`; genau ein Canary-Test | 01-01 |
| `tests/helpers/loadScript.js` | `REPO_ROOT`, `makeElementStub()`, `makeSandbox(extraGlobals)`, `loadInto(sandbox, file)`, `loadScript(files, extraGlobals)`, `evalIn(sandbox, code)`, `settle(ms)` | 01-02 |
| `tests/load-script.test.js`, `tests/idb-helpers.test.js` | TEST-02: Laden in Reihenfolge, Globals, Edge-Cases; idbGet/idbSet Round-Trip | 01-02 |
| `tests/bot-data-validators.test.js`, `tests/outfit-import-parser.test.js` | TEST-05: `_normLogik`/`_migriereLogik`; `_oiDetectType`/`_oiBuildExecCode` | 01-02 |
| `tests/bot-engine-escaping.test.js` | TEST-06: `ADVERSARIAL_NAMES`, `makeBot(name, extra)`, `buildSandbox()`, `it.each`-Parse-Check, Voll-Payload-Test, Name-Roundtrip | 01-03 |
| `bot-engine.js` (Zeile 49 — einzige Produktionsänderung der Phase) | `safeName` escaped zusätzlich `'`, `\r`, `\n` | 01-03 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Escaping-Test schreiben und RED bestätigen — `tests/bot-engine-escaping.test.js` (TEST-06)</name>
  <precondition>`node_modules/vitest` existiert (Plan 01-01 hat `npm install` ausgeführt; bei frischem Clone zuerst `npm install`)</precondition>
  <files>tests/bot-engine-escaping.test.js</files>
  <read_first>bot-engine.js Zeilen 1-2 und 28-80 (`_buildBotCode`-Kopf, `safeId`/`safeName`, `_cfgRaw`, `cfgJson`, `eventsJson`) sowie die sechs Zeilen 802, 1826, 2923, 3213, 3241, 3244 (Interpolationsstellen) — NICHT die ganze Datei (3.356 Zeilen); .planning/phases/01-testfundament/01-RESEARCH.md "Pattern 3" und "Pitfall 4/5"; tests/idb-canary.test.js (Stil)</read_first>
  <action>
    `tests/bot-engine-escaping.test.js` (neu, ESM): `import { describe, it, expect, beforeAll } from 'vitest'`, `import fs from 'node:fs'`, `import vm from 'node:vm'`, `import path from 'node:path'`, `import { fileURLToPath } from 'node:url'`. KEIN Import des gemeinsamen Sandbox-Loaders aus Plan 01-02 (Verzeichnis tests/helpers — dieser Plan läuft parallel dazu) — Kommentar im Kopf: "Eigene Minimal-Sandbox: bot-engine.js braucht weder DOM noch IDB noch items.js; hält 01-03 unabhängig von 01-02."

    Hilfsfunktionen:
    - `REPO_ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..')`.
    - `buildSandbox()`: Objekt `{ console, btoa, atob, _money: undefined, _rankData: undefined, _shop: undefined }`, dann `sandbox.window = sandbox`, `vm.createContext(sandbox)`, `vm.runInContext(fs.readFileSync(path.join(REPO_ROOT, 'bot-engine.js'), 'utf8'), sandbox, { filename: 'bot-engine.js' })`, Rückgabe sandbox. bot-engine.js wird als Text gelesen, nie importiert.
    - `makeBot(name, extra = {})`: `{ id: 'b1', name, settings: { hearChat: true, modus: 'chat' }, triggers: [], events: [], szenen: [], ...extra }` (leere triggers/events, damit `_mapAkt` nicht `CURSE_DB`/`PROFILES` anfasst).
    - `parseOnly(code)`: `expect(() => new Function(code)).not.toThrow()` — der Code wird NUR geparst, NIE aufgerufen (er referenziert `bcModSdk`, `ChatRoom`, `Player`).
    - `ADVERSARIAL_NAMES` (JS-Literale exakt so): `'NormalName'`, `'Back\`tick'`, `'Dollar${brace}'`, `"O'Brien"`, `'Quote"Double'`, `"Combo \`${x}\` and 'quote'"`, `'Trail\\'` (ein Backslash am Ende), `"Back\\'slash"` (Backslash gefolgt von Quote), `'Multi\nLine'`, `'CR\rLF\r\n'`, `'Umlaut Ärger ✓'` — elf Einträge.

    `describe('_buildBotCode Escaping (TEST-06)')`, `beforeAll`: `ctx = buildSandbox()`; `expect(typeof ctx._buildBotCode).toBe('function')`.
    1. `it.each(ADVERSARIAL_NAMES)('bot.name=%j erzeugt Code, den new Function() ohne SyntaxError akzeptiert', (name) => parseOnly(ctx._buildBotCode(makeBot(name))))`.
    2. it `bot.name überlebt die Generierung verlustfrei (botName-Literal)`: für jeden Namen aus `ADVERSARIAL_NAMES`: `const code = ctx._buildBotCode(makeBot(name))`; `const m = code.match(/botName:'((?:[^'\\]|\\.)*)'/)`; `expect(m, 'botName-Literal nicht gefunden für ' + JSON.stringify(name)).not.toBeNull()`; `expect(new Function("return '" + m[1] + "';")()).toBe(name)` — hier wird ausschließlich das isolierte String-Literal evaluiert, nicht der Bot-Code.
    3. it `adversariale Nutzerdaten in id, settings, triggers, events und szenen brechen den Code nicht (Base64-Pfad)`: `const evil = "x'\"\`${y}\\\n\r</script>"`; `const bot = makeBot(evil, { id: "b1'\`${x}", settings: { hearChat: true, modus: 'chat', figurName: evil, figurRede: evil, figurErzaehler: evil }, triggers: [{ id: 't1', name: evil, aktiv: true, bedingungen: [{ typ: 'wort', wert: evil, logik: 'und' }], aktionen: [{ typ: 'chat', text: evil }] }], events: [{ id: 'e1', name: evil, aktiv: true, bedingungen: [], aktionen: [{ typ: 'chat', text: evil }] }], szenen: [{ id: 's1', name: evil }] })`; `const code = ctx._buildBotCode(bot)`; `parseOnly(code)`; `expect(code).toContain('_BID=')`; `expect(code).toContain(JSON.stringify(bot.id))`.
    4. it `Sandbox-Vertrag: ohne vordeklarierte _money/_rankData/_shop wirft _buildBotCode ReferenceError`: eigene Sandbox `{ console, btoa, atob }` + window-Selbstreferenz + bot-engine.js laden; `expect(() => sb._buildBotCode(makeBot('X'))).toThrow(ReferenceError)` (dokumentiert Pitfall 4 — die Produktionsdatei ist hier absichtlich unverändert).

    RED bestätigen: `npx vitest run tests/bot-engine-escaping.test.js` MUSS jetzt rot sein — erwartet: Test 1 fehlschlagend für `O'Brien`, `Combo …`, `Back\\'slash`, `Multi\nLine`, `CR\rLF\r\n` (5 Fälle) und Test 2 fehlschlagend (Literal für Quote-Namen kaputt); Tests 3 und 4 sowie die sechs harmlosen Namen grün. Ist die Datei unerwartet GRÜN, ist entweder der Fix schon drin oder die Fixtures sind nicht adversarial — dann STOPP und prüfen, nicht weitermachen.

    Commit des roten Tests (TDD-RED, wie in diesem Projekt üblich): `git add tests/bot-engine-escaping.test.js`; `git diff --cached --name-only` = exakt dieser Pfad; Message `test(01-03): add failing escaping test for _buildBotCode — O'Brien breaks new Function() (TEST-06)`. Vorher `git status --short bot-engine.js` prüfen und im SUMMARY festhalten (muss leer sein — Voraussetzung für Task 2).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/bot-engine-escaping.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -q "O'Brien" "$OUT" && grep -Eq "Tests +[0-9]+ failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` wird nicht ausgegeben — Vitest endete mit Exit 0 (Suite unerwartet grün: Fix bereits vorhanden oder Fixtures nicht adversarial), oder `O'Brien`/`failed` fehlen in der Ausgabe</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "O'Brien" tests/bot-engine-escaping.test.js` >= 1; `grep -c "it.each(ADVERSARIAL_NAMES)" tests/bot-engine-escaping.test.js` = 1; `node -e "const s=require('fs').readFileSync('tests/bot-engine-escaping.test.js','utf8');const m=s.match(/ADVERSARIAL_NAMES\s*=\s*\[([\s\S]*?)\];/);console.log(m?eval('['+m[1]+']').length:0)"` gibt `11` aus
    - `grep -c "_money: undefined" tests/bot-engine-escaping.test.js` >= 1; `grep -c "btoa" tests/bot-engine-escaping.test.js` >= 2; `grep -c "runInContext" tests/bot-engine-escaping.test.js` >= 2
    - `test "$(grep -c "helpers/loadScript" tests/bot-engine-escaping.test.js)" = 0` (kein Import des 01-02-Loaders)
    - `grep -c "new Function(" tests/bot-engine-escaping.test.js` >= 2 (Parse-Helfer und Literal-Roundtrip vorhanden)
    - `test "$(grep -Ec "_buildBotCode\([^)]*\)\)\(\)" tests/bot-engine-escaping.test.js)" = 0` (Bot-Code wird nie aufgerufen)
    - `grep -c "botName:'" tests/bot-engine-escaping.test.js` >= 1 (Name-Roundtrip-Test vorhanden)
    - `git status --short bot-engine.js` gibt nichts aus (Voraussetzung für Task 2 dokumentiert)
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/bot-engine-escaping.test.js`
  </acceptance_criteria>
  <done>Test existiert mit elf adversarialen Namen, Voll-Payload-Test, Name-Roundtrip und Sandbox-Vertrag; Lauf ist nachweislich rot (`O'Brien` u. a.); roter Test committet; bot-engine.js noch unverändert und im Arbeitsbaum sauber.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Minimalen Escaping-Fix in bot-engine.js Zeile 49 anwenden, GREEN bestätigen, chirurgisch committen (TEST-06)</name>
  <precondition>`git status --short bot-engine.js` gibt VOR dem Edit nichts aus (Datei im Arbeitsbaum unverändert) — sonst STOPP: ein `git add bot-engine.js` würde fremde Hunks mitstagen; dann `git diff bot-engine.js` inspizieren und den Fall an den Nutzer eskalieren statt zu committen</precondition>
  <reversibility rating="reversible">Eine Zeile; Revert per `git revert` ohne Datenmigration; generierter Code für Namen ohne `'`/`\r`/`\n` ist byte-identisch zum Vorzustand.</reversibility>
  <files>bot-engine.js</files>
  <read_first>bot-engine.js Zeile 49 (per `sed -n '49p' bot-engine.js` — exakt vergleichen, bevor editiert wird) und Zeilen 802, 1826, 2923, 3213, 3241, 3244; items.js Zeilen mit `function escJsAttr` (per `grep -n "function escJsAttr" items.js`, dann 6 Zeilen lesen — Konventionsvorbild); tests/bot-engine-escaping.test.js (aus Task 1)</read_first>
  <behavior>
    - Test 1 (it.each): für alle elf `ADVERSARIAL_NAMES` liefert `_buildBotCode(makeBot(name))` Code, den `new Function()` ohne SyntaxError parst
    - Test 2: das `botName:'…'`-Literal evaluiert für jeden der elf Namen exakt zum Original (verlustfrei, inkl. `O'Brien`, `Multi\nLine`, `CR\rLF\r\n`, `Back\\'slash`)
    - Test 3/4 bleiben grün (Base64-Pfad unverändert; Sandbox-Vertrag unverändert)
    - Für Namen ohne `'`, `\r`, `\n` ist der generierte Code identisch zum Vorzustand (Regression: `NormalName` → gleiche Länge/Inhalt wie vor dem Fix)
  </behavior>
  <action>
    Nutzerentscheidung 1: minimaler, klar begrenzter Fix in bot-engine.js, sonst keine Produktionsänderung.

    1. `git status --short bot-engine.js` → muss leer sein (Precondition). `sed -n '49p' bot-engine.js` → muss exakt lauten: `  const safeName = bot.name.replace(/\\/g,'\\\\').replace(/`/g,'\\`');` (zwei führende Leerzeichen). Weicht die Zeile ab, STOPP.

    2. Genau diese Zeile per Edit-Tool (exakter old_string = die Zeile aus Schritt 1) ersetzen durch: `  const safeName = bot.name.replace(/\\/g,'\\\\').replace(/`/g,'\\`').replace(/'/g,"\\'").replace(/\r/g,'\\r').replace(/\n/g,'\\n');` — d. h. drei zusätzliche `.replace`-Aufrufe am Ende derselben Zeile: einfaches Anführungszeichen → `\'` (der verifizierte Bug: `${safeName}` landet in einfach gequoteten Strings), CR → `\r`, LF → `\n` (Planer-Ermessen, gleiche Zeile, gleiche Fehlerklasse, Vorbild `escJsAttr` in items.js). Reihenfolge ist wichtig: Backslash-Escaping muss zuerst bleiben, sonst würden die neu eingefügten Backslashes doppelt escaped. KEINE weitere Änderung — kein Kommentar-Umbau, keine Umformatierung, kein Zeilenende-Wechsel (Datei bleibt LF).

    3. Prüfen: `git diff --numstat bot-engine.js` → `1	1	bot-engine.js` (genau eine Zeile raus, eine rein); `git diff bot-engine.js | grep -c '^[-+][^-+]'` → `2`; `node --check bot-engine.js` → Exit 0.

    4. GREEN: `npx vitest run tests/bot-engine-escaping.test.js` → alle Tests grün (elf it.each-Fälle + drei weitere = 14 passed). Dann `npm test` → gesamte Suite grün.

    5. Commit — ausschließlich `git add bot-engine.js` (kein weiterer Pfad; der Test ist bereits in Task 1 committet). `git diff --cached --numstat` → genau `1	1	bot-engine.js`. `git status --short` → die neun vorbestehenden ` M`-Einträge (bc-icons.js, bot-data.js, bot-ui.js, index.html, inventar.js, items.js, money.js, rank.js, shop.js) bleiben unstaged. Message: `fix(01-03): escape single quotes and line breaks in _buildBotCode safeName (TEST-06)`. Die Warnung `LF will be replaced by CRLF` ist kosmetisch (core.autocrlf) und kein Fehler.

    6. Nachweis für das SUMMARY: `git show --stat --format= HEAD` (nur bot-engine.js, 1 insertion, 1 deletion) und `git log --format=%h d9ad6af..HEAD -- index.html items.js bot-data.js bot-ui.js outfit-import.js loader.js` (leer).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/bot-engine-escaping.test.js && node --check bot-engine.js</automated>
    <fails_when>non-zero exit (Vitest rot oder `node --check` SyntaxError), oder `failed` in der `Tests`-Summary-Zeile, oder weniger als `14 passed`</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(git diff --numstat d9ad6af HEAD -- bot-engine.js | awk '{print $1"/"$2}')" = "1/1" && test -z "$(git log --format=%h d9ad6af..HEAD -- index.html items.js bot-data.js bot-ui.js outfit-import.js loader.js)" && echo SURGICAL-OK</automated>
    <fails_when>`SURGICAL-OK` wird nicht ausgegeben — bot-engine.js hat gegenüber der Phase-Baseline mehr als 1/1 Zeilen geändert, oder ein Commit seit d9ad6af berührt eine andere Produktionsdatei</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n '49p' bot-engine.js | grep -Fc "replace(/'/g"` = 1; `sed -n '49p' bot-engine.js | grep -Fc 'replace(/\r/g'` = 1; `sed -n '49p' bot-engine.js | grep -Fc 'replace(/\n/g'` = 1; `sed -n '49p' bot-engine.js | grep -Fc "const safeName = bot.name.replace(/\\\\/g,'\\\\\\\\')"` = 1 (Backslash-Escaping bleibt an erster Stelle)
    - `grep -n "safeName" bot-engine.js` listet weiterhin genau die Zeilen 49, 802, 1826, 2923, 3213, 3241, 3244 (eine Definition + sechs Interpolationen — keine neue oder entfernte Verwendung)
    - `node --check bot-engine.js` → Exit 0
    - `npx vitest run tests/bot-engine-escaping.test.js` → `14 passed`; `npm test` → Exit 0
    - Nach dem Commit: `git show --name-only --format= HEAD` = `bot-engine.js`; `git show --numstat --format= HEAD | awk '{print $1"/"$2"/"$3}'` = `1/1/bot-engine.js`; `test "$(git status --short | grep -c '^M')" = 0` (nichts Fremdes gestaged; die vorbestehenden ` M`-Einträge bleiben in Spalte 1 mit Leerzeichen)
    - `git log --format=%h d9ad6af..HEAD -- index.html items.js bot-data.js bot-ui.js outfit-import.js loader.js` gibt nichts aus
  </acceptance_criteria>
  <done>Zeile 49 escaped zusätzlich `'`, `\r`, `\n`; Escaping-Test grün (14 passed) und volle Suite grün; Commit enthält ausschließlich bot-engine.js mit 1/1 geänderten Zeilen; keine andere Produktionsdatei in der Phase committet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nutzerdaten (Bot-Name, Trigger, Events) → generierter Spielcode | `_buildBotCode` interpoliert `bot.name` direkt in einfach gequotete String-Literale des Codes, der später im BC-Tab per EXEC/eval läuft; alle übrigen Nutzerdaten gehen per Base64 |
| Test → generierter Code | Rückgabe von `_buildBotCode` wird im Test nur geparst |
| Git-Arbeitsbaum → Repository | einzige Produktionsänderung der Phase muss isoliert committet werden, während neun andere Dateien unrelated Änderungen tragen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering | bot-engine.js `_buildBotCode` `safeName` (Zeile 49; Interpolation Zeilen 802, 1826, 2923, 3213, 3241, 3244) | high | mitigate | ASVS V5 Output-Encoding: `'`, `\r`, `\n` zusätzlich escapen (Task 2); Test lockt Parsbarkeit UND Verlustfreiheit für elf adversariale Namen (Task 1); Base64-Pfad für alle übrigen Nutzerdaten per Voll-Payload-Test abgedeckt |
| T-1-05 | Elevation of Privilege | `new Function(code)` in tests/bot-engine-escaping.test.js | medium | mitigate | Nur Parse; einzige Evaluation ist das isolierte `botName`-String-Literal (`return '…';`), nie der Bot-Code; Acceptance verbietet das Aufrufmuster |
| T-1-06 | Tampering | Git-Staging von bot-engine.js | high | mitigate | Precondition `git status --short bot-engine.js` leer; `git diff --numstat` = 1/1; nur `git add bot-engine.js`; `git log d9ad6af..HEAD -- <andere Produktionsdateien>` leer (Verify 2) |
| T-1-04 | Tampering | Minimal-vm-Sandbox mit Host-`btoa`/`console` | low | accept | Geladen wird nur der eigene bot-engine.js; kein `fs`/`process` in der Sandbox; `vm` ist keine Sicherheitsgrenze, wird auch nicht als solche behandelt |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine neuen Installationen in diesem Plan; Pakete aus 01-01 (package-lock.json, Legitimitäts-Checkpoint) |
</threat_model>

<verification>
- `npx vitest run tests/bot-engine-escaping.test.js` → 14 passed; `npm test` → gesamte Suite grün.
- `git diff --numstat d9ad6af HEAD -- bot-engine.js` = `1	1	bot-engine.js`; `node --check bot-engine.js` grün.
- `git log --format=%h d9ad6af..HEAD -- index.html items.js bot-data.js bot-ui.js outfit-import.js loader.js` leer.
- RED-Commit (Test) liegt vor dem GREEN-Commit (Fix) in `git log`.
</verification>

<success_criteria>
- TEST-06 / Erfolgskriterium 5: `_buildBotCode` erzeugt für Namen mit Backticks, `${`, Anführungszeichen, Backslashes, Zeilenumbrüchen und Unicode Code, den `new Function()` ohne SyntaxError akzeptiert; der Name bleibt verlustfrei erhalten.
- Nutzerentscheidung 1: genau eine geänderte Zeile in bot-engine.js, chirurgisch committet; alle anderen Produktionsdateien und index.html unverändert.
</success_criteria>

<output>
Create `.planning/phases/01-testfundament/01-03-SUMMARY.md` when done — inkl. RED-Ausgabe (welche Fixtures vor dem Fix fehlschlugen), GREEN-Ausgabe, `git show --numstat HEAD` und Hinweis auf das Planer-Ermessen (`\r`/`\n` mitgefixt — ein Satz, damit der Nutzer es bei Bedarf zurücknehmen kann).
</output>
