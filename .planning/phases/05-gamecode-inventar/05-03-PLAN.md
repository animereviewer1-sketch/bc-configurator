---
phase: 05-gamecode-inventar
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - tests/game-scan-bridge.test.js
  - game-scan.js
  - index.html
  - tests/helpers/loadScript.js
  - tests/persistence-module.test.js
  - docs/LOAD-ORDER.md
autonomous: true
requirements: [SCAN-01, SCAN-08]
tags: [game-scan, bridge-handler, snapshot, tweaks-panel, load-order, red-green, human-check]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SCAN-01 (Tool-Hälfte): Ein Klick auf „🔎 Spiel scannen“ im Tweaks-Panel ruft `triggerGameScan()` (game-scan.js): `reqId = 'gi_' + Date.now() + '_' + laufende Nummer`, `bcSend({ type: 'GET_GAME_INVENTORY', reqId })` über den bestehenden, handshake-geprüften Sendepfad; ohne Handshake weist `bcSend` ab (`false`), die Statuszeile `#gameScanInfo` nennt „Verbinden“, nichts wird pending; mit Handshake ist die reqId in `_giPending` (Map) registriert und die Statuszeile zeigt „Scan läuft…“; `GAME_INVENTORY_PROGRESS` mit pendender reqId aktualisiert die Statuszeile auf „Scan läuft… Schritt <step>/<total> – <label>“; Nachrichten mit unbekannter reqId werden ignoriert (T-5-04) — alles registriert über `onBridgeMessage`, ohne Änderung an items.js oder bridge.js (SPLIT-02-Muster)"
    - "SCAN-08: `GAME_INVENTORY_DATA {reqId, snapshot}` mit pendender reqId → `_saveGameInventorySnapshot(snapshot)` baut den Datensatz `{ id: ts, ts, gameVersion, modCount, mods: [{name, version}], sizeBytes: JSON.stringify(inventory).length, inventory }` (`ts = Date.now()` der Tool-Uhr), loggt `console.info('[GameScan] Snapshot-Größe (JSON-Zeichen):', sizeBytes, …)` (RESEARCH Open Question 2) und schreibt per `idbSnapshotPut` (add-only) in den Store `snapshots`; Erfolg → `showStatus('✅ Spiel-Scan gespeichert – <n> Mods, <kb> KB', 'success')` + Statuszeile „Letzter Scan <ISO> UTC · BC <gameVersion> · <n> Mods · <kb> KB · <k> Snapshot(s) gespeichert“; Fehler (Quota/Constraint) → sichtbar über `_idbSchreibfehler` („Speicher voll“) UND Statuszeile „❌ Snapshot NICHT gespeichert – siehe Statusmeldung“, nie still; `{reqId, err}` → `showStatus('❌ Spiel-Scan fehlgeschlagen: <err>', 'error')`, nichts gespeichert; zwei gleichzeitige Scans werden beide gespeichert (jede pendende reqId), ein zweiter Scan lässt den ersten Datensatz unverändert (zwei Datensätze, keine Löschung, keine Überschreibung); beim Laden zeigt die Statuszeile die vorhandene Snapshot-Anzahl aus `idbSnapshotKeys()`"
    - "Ladereihenfolge: `game-scan.js` wird in index.html im ersten `_cbv`-Block direkt nach `items.js` per `document.write` geladen (nur Einfügungen; Kommentar `LADEREIHENFOLGE` um `→ game-scan.js` ergänzt); game-scan.js beginnt mit dem Ladereihenfolge-Guard (`idbSnapshotPut` ← persistence.js, `bcSend`/`onBridgeMessage` ← bridge.js, `showStatus` ← items.js → `#loadOrderFatal` + Throw); `docs/LOAD-ORDER.md` hat die Zeile für game-scan.js (Position 7, Folgezeilen renummeriert), nennt es als lebendes Beispiel für neue Nachrichtentypen (Beispiel `GAME_SCAN_DATA` bleibt) und korrigiert die veraltete Aussage zur Handler-Isolation (bridge.js isoliert seit Commit 332fc2d jeden Handler per try/catch, Doppelregistrierung ist No-op); `CORE_SCRIPTS` = `['persistence.js', 'bridge.js', 'items.js', 'game-scan.js']`, `expandLoadOrder` fügt Kern-Skripte VOR items.js vor dem ersten `items.js` und Kern-Skripte NACH items.js direkt danach ein (keine Duplikate) — alle 45 bestehenden `loadScript([...])`-Aufrufstellen bleiben unverändert"
    - "Tweaks-Panel (index.html, nur Einfügung zwischen `🖼️ Screenshot-Speicher` und `📦 Item-Katalog`): Sektion `🔎 Spiel-Scan` mit Erklärtext (read-only, Snapshot, nie automatisch gelöscht), Button `<button class=\"tweaks-btn\" id=\"gameScanBtn\" onclick=\"triggerGameScan()\" …>🔎 Spiel scannen</button>` und `<div id=\"gameScanInfo\" …>–</div>` im Stil von `#screenshotStoreInfo`"
    - "End-of-Phase-Human-Checks (nicht blockierend, `<human-check>` in Task 2): Live-Scan in der echten 28-Mod-Session nach erneutem Bookmarklet-Lauf, Spiel-Tab bleibt bedienbar, Snapshot in DevTools sichtbar, zweiter Scan behält den ersten, Payload-Größe aus der Konsole ins SUMMARY"
  artifacts:
    - path: "game-scan.js"
      provides: "Ladereihenfolge-Guard, `_giSeq`, `_giPending`, `_gameScanInfo(text)`, `_gameScanFormatTs(ts)`, `triggerGameScan()`, Handler `GAME_INVENTORY_PROGRESS`/`GAME_INVENTORY_DATA`, `_saveGameInventorySnapshot(inventory, meta)`, `_renderGameScanInfo(last)`, Init-Hook"
      contains: "onBridgeMessage('GAME_INVENTORY_DATA'"
    - path: "tests/game-scan-bridge.test.js"
      provides: "17 Fälle: Guard, Trigger/reqId/Handshake, PROGRESS, DATA → Datensatz/Log/Status, zweiter Scan behält ersten, gleichzeitige Scans, fremde reqId, err, Quota, Init-Anzeige, fehlendes Element, statische Gates für index.html/docs/CORE_SCRIPTS/game-scan.js"
      contains: "GAME_INVENTORY_DATA"
    - path: "index.html"
      provides: "Write-Zeile `game-scan.js?_=` nach items.js, Kommentar-Ergänzung, Tweaks-Sektion `🔎 Spiel-Scan`"
      contains: "id=\"gameScanInfo\""
    - path: "tests/helpers/loadScript.js"
      provides: "`CORE_SCRIPTS` mit `game-scan.js`, `expandLoadOrder` mit Vor-/Nach-items-Regel"
      contains: "'game-scan.js'"
    - path: "docs/LOAD-ORDER.md"
      provides: "Tabellenzeile 7 game-scan.js, Guard-Absatz, lebendes Beispiel `GAME_INVENTORY_DATA`, korrigierte Isolations-Aussage, `CORE_SCRIPTS`-Text"
      contains: "game-scan.js"
  key_links:
    - from: "index.html Button `onclick=\"triggerGameScan()\"`"
      to: "game-scan.js `triggerGameScan()` → bridge.js `bcSend({ type: 'GET_GAME_INVENTORY', reqId })`"
      via: "Handshake-Prüfung in `bcSend` (STAB-04); `_giPending.set(reqId, { startedAt })` nur bei `true`"
      pattern: "bcSend\\(\\{ type: 'GET_GAME_INVENTORY', reqId \\}\\)"
    - from: "bridge.js `_bridgeDispatch` (nach Origin-/Source-Prüfung)"
      to: "game-scan.js `onBridgeMessage('GAME_INVENTORY_PROGRESS' | 'GAME_INVENTORY_DATA', …)`"
      via: "reqId-Filter über `_giPending`"
      pattern: "onBridgeMessage\\('GAME_INVENTORY_PROGRESS'"
    - from: "game-scan.js `_saveGameInventorySnapshot`"
      to: "persistence.js `idbSnapshotPut(record)` (add-only) / `idbSnapshotKeys()`"
      via: "Datensatz `{ id, ts, gameVersion, modCount, mods, sizeBytes, inventory }`; Rückgabe `false` → Statuszeile „NICHT gespeichert“"
      pattern: "idbSnapshotPut\\("
    - from: "tests/helpers/loadScript.js `expandLoadOrder`"
      to: "Reihenfolge persistence.js → bridge.js → items.js → game-scan.js"
      via: "`CORE_SCRIPTS`-Split an `items.js`"
      pattern: "CORE_SCRIPTS = \\['persistence.js', 'bridge.js', 'items.js', 'game-scan.js'\\]"
  prohibitions:
    - requirement_id: SCAN-08
      category: safety
      verification: automated
      statement: "game-scan.js enthält keine Lösch-/Leer-Operation und ruft keine Lösch-API auf (statisch 0× `.delete(`, 0× `.clear(`, 0× `idbSnapshotDelete`); zweiter Scan lässt den ersten Datensatz unverändert (Test); kein `put`-Überschreiben (persistence add-only)"
    - requirement_id: SCAN-01
      category: safety
      verification: automated
      statement: "game-scan.js besitzt keine eigene Bridge-/IDB-Logik (0× `addEventListener('message'`, 0× `postMessage(`, 0× `indexedDB.open(`), kein Wildcard-Ziel, kein `location.origin`, kein Tool-Host-String; items.js und bridge.js werden nicht verändert (git-Diff gegen `f3973f6` leer)"
    - requirement_id: SCAN-01
      category: safety
      verification: automated
      statement: "index.html: nur Einfügungen plus die eine geänderte Kommentarzeile `LADEREIHENFOLGE` (Diff gegen `f3973f6` hat außer dieser Zeile 0 gelöschte Zeilen); `tests/loader-origin.test.js`, `loader.js` unverändert gegenüber Plan 05-02"
---

<objective>
Die Tool-Hälfte des Spiel-Scans: neues Modul `game-scan.js` (Trigger-Button, reqId-Korrelation über eine Pending-Map, Fortschrittszeile, Snapshot-Datensatz mit Größenlog und add-only-Speicherung, sichtbare Fehlerpfade), eingehängt über `onBridgeMessage` ohne Änderung an items.js/bridge.js, geladen als viertes Kern-Skript direkt nach items.js (index.html nur Einfügungen), dokumentiert in `docs/LOAD-ORDER.md` und in der Test-Ladeordnung (`CORE_SCRIPTS`/`expandLoadOrder`). RED → GREEN gegen die bestehende Tool-Sandbox (`loadScript`) mit simulierten Bridge-Nachrichten; End-of-Phase-Human-Checks (Live-Scan, Bedienbarkeit, DevTools, zweiter Scan) als `<human-check>`.

Purpose: Erfolgskriterien 2 (Klick → Inventar) und 5 (jeder Scan als Snapshot mit BC-Version, Zeitstempel, Mod-Liste; frühere Snapshots bleiben) — der Kernwert „nichts geht verloren, jede Speicherung erfolgreich oder sichtbar fehlgeschlagen“ auf der Tool-Seite.
Output: `tests/game-scan-bridge.test.js`, `game-scan.js` (neu); `index.html`, `tests/helpers/loadScript.js`, `tests/persistence-module.test.js`, `docs/LOAD-ORDER.md` (geändert).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gamecode-inventar/05-RESEARCH.md
@.planning/phases/05-gamecode-inventar/05-VALIDATION.md
@.planning/phases/05-gamecode-inventar/05-01-SUMMARY.md
@.planning/phases/05-gamecode-inventar/05-02-SUMMARY.md
@.planning/phases/04-entflechtung/04-02-SUMMARY.md
@docs/LOAD-ORDER.md
@tests/helpers/loadScript.js
@bridge.js

Planer-Probe (2026-09-15, HEAD `f3973f6`; per grep ankern) — verbindliche Befunde:
1. **Registry-Muster:** `onBridgeMessage(type, handler)` (bridge.js) — Doppelregistrierung No-op, `_bridgeDispatch` isoliert jeden Handler per `try/catch` (`console.error('[Bridge] Handler-Fehler bei', …)`, Commit 332fc2d). Die Shell prüft `app`, `_bridgeSenderOk` (Opener + gelernter Origin), `_playerAbgelehnt`, lernt `_bcOrigin`, setzt `_lastMsgTs`, dann Dispatch. `bcSend(msg, silent)`: ohne Opener → `false` + Status „BC-Fenster nicht verfügbar“; ohne `_bcOrigin` und Typ ≠ PING → `false` + Status „Noch nicht mit BC verbunden – erst 🔄 Verbinden“; sonst `window.opener.postMessage({ app: APP, ...msg }, _bcOrigin)` → `true`.
2. **Tool-Sandbox-Muster für Bridge-Tests** (tests/exec-log.test.js, tests/bridge-registry.test.js): `opener = { closed: false, postMessage: vi.fn() }`; `ctx = loadScript([...], { opener, setTimeout: () => 0, clearTimeout: () => {} })`; Handshake simulieren mit `evalIn(ctx, "_bcOrigin = 'https://game.test'")`; Nachricht empfangen mit `dispatchMessage(ctx, { app: 'BCKonfigurator', ...data }, { origin: 'https://game.test', source: opener })`; `ctx.showStatus = vi.fn()` nach dem Laden ist gängige Praxis (tests/screenshot-store.test.js:62); `ctx.document.getElementById = id => id === 'gameScanInfo' ? el : makeElementStub()` (Muster exec-log.test.js). fake-indexeddb ist pro Testdatei prozessweit — mehrere Sandboxen einer Datei teilen die DB: Zählungen relativ (vorher/nachher) statt absolut.
3. **Ladeordnung (Test):** `CORE_SCRIPTS = ['persistence.js', 'bridge.js', 'items.js']`; `expandLoadOrder(files)` fügt fehlende Kern-Skripte VOR dem ersten `items.js` ein; `tests/persistence-module.test.js` Zeilen 98-108 prüfen `CORE_SCRIPTS.at(-1) === 'items.js'` und `expandLoadOrder(['persistence.js', 'items.js'])` `toEqual(['persistence.js', 'bridge.js', 'items.js'])` — beide Erwartungen müssen mit der neuen Regel nachgezogen werden (Task 2); `expandLoadOrder(['items.js', 'bot-data.js'])` `toEqual([...CORE_SCRIPTS, 'bot-data.js'])` bleibt mit „direkt nach items.js einfügen“ automatisch wahr. `tests/load-script.test.js` „Doppeltes Laden … SyntaxError“ bleibt wahr (zweites `items.js` wirft weiterhin). 45 `loadScript([...])`-Aufrufstellen in 20 Dateien laden nach der Änderung zusätzlich game-scan.js — das Modul darf beim Laden deshalb nur den Guard, zwei Registrierungen und einen fehlertoleranten, stillen Init-Hook ausführen (keine Konsolenausgabe im Erfolgspfad — Teardown-Race, STATE.md).
4. **index.html:** erster `_cbv`-Block Zeilen 3099-3104 (`<script>` … `persistence.js` → `bridge.js` → `items.js` … `</script>`), Kommentar Zeile 3097 `<!-- LADEREIHENFOLGE (nicht umstellen): … → items.js → money.js → … -->`; tests/load-order-guard.test.js prüft `LADEREIHENFOLGE`-Slice (200 Zeichen) enthält `bridge.js` und dass `persistence.js?_=` genau einmal vorkommt. Tweaks-Panel: Sektion `🖼️ Screenshot-Speicher` (Zeilen ~2473-2480) endet mit `<div id="screenshotStoreInfo" …>–</div></div>`, direkt danach `<div>` mit `📦 Item-Katalog`; tests/screenshot-export.test.js prüft `indexOf('id="screenshotStoreInfo"') < indexOf('📦 Item-Katalog')` — eine Einfügung dazwischen hält das.
5. **docs/LOAD-ORDER.md:** Tabelle 1-15 (items.js = 6, money.js = 7 … bc-autobackup.js = 15); Abschnitt „Neuen Nachrichtentyp registrieren“ enthält das Beispiel `GAME_SCAN_DATA` (tests/load-order-guard.test.js erwartet `/GAME_SCAN_DATA|MEIN_TYP/`) und den veralteten Satz „Es gibt **keine try/catch-Isolation** zwischen Handlern …“ (RESEARCH State of the Art: seit 332fc2d falsch); Abschnitt „Tests“ nennt `CORE_SCRIPTS (aktuell ['persistence.js', 'bridge.js', 'items.js'])`; tests/load-order-guard.test.js erwartet weiterhin die Strings `CORE_SCRIPTS`, `document.write`, `loadOrderFatal`, alle Modulnamen.
6. **Guard-Muster** (bot-ui.js Zeilen 1-15): IIFE, `required = [['idbGet', 'persistence.js'], ['bcSend', 'bridge.js'], ['onBridgeMessage', 'bridge.js'], ['showStatus', 'items.js']]`, `typeof window[e[0]] !== 'function'`, Box `#loadOrderFatal` mit `FATAL: <Module> wurde nicht vor bot-ui.js geladen – Ladereihenfolge in index.html prüfen (siehe docs/LOAD-ORDER.md)`, `console.error('[BCK-Popup] ' + msg)`, `throw new Error(msg)`. tests/load-order-guard.test.js prüft Box-Text-Form `^FATAL: ` und Endung `(siehe docs/LOAD-ORDER.md)`.
7. **Statuszeilen-Konvention** (items.js `_renderScreenshotStoreInfo`, 04-04): Datum ohne locale-API: `new Date(ts).toISOString().slice(0, 16).replace('T', ' ') + ' UTC'`; Init-Hook-Konvention (02-03): `try { if (document.readyState !== 'loading') fn(); else document.addEventListener('DOMContentLoaded', () => fn()); } catch (e) {}` — in der Sandbox `readyState === 'complete'`.
8. **Snapshot-Vertrag aus Plan 05-02:** `inventory = { schema: 1, gameVersion, ts, durationMs, globals, assets, modSdk, mods: [{name, fullName, version, repository}], probes, chatHooks, errors }`; `GAME_INVENTORY_DATA` trägt `snapshot` (Erfolg) oder `err` (String). Der Tool-Datensatz verpackt das Inventar (`inventory`) und zieht `gameVersion`/`mods` (auf `{name, version}` reduziert) heraus.
9. **Statischer Audit-Kontext:** tests/injected-code-origin.test.js prüft `location.origin` (genau 1× in items.js) und den Tool-Host-String nur für die dort gelisteten Dateien — game-scan.js muss beides trotzdem meiden (eigener statischer Fall in Task 1, Host aus loader.js `POPUP_URL` abgeleitet). `WILD = /,\s*['"]\*['"]\)/`.
10. **Baseline nach Plan 05-02:** `npm test` = 22 Dateien grün.
</context>

<tasks>

<!-- Comment-Text-Discipline-Allowlist (#429): Gate-Literale; im Plan nur hier, in <verify> und in Testbeschreibungen -->
<!-- planner-discipline-allow: .delete( -->
<!-- planner-discipline-allow: .clear( -->
<!-- planner-discipline-allow: idbSnapshotDelete -->
<!-- planner-discipline-allow: indexedDB.open( -->
<!-- planner-discipline-allow: location.origin -->
<!-- planner-discipline-allow: postMessage( -->
<!-- planner-discipline-allow: addEventListener('message' -->
<!-- planner-discipline-allow: toLocaleString -->

<task type="auto">
  <name>Task 1: Tool-seitige Scan-Tests schreiben und RED bestätigen — `tests/game-scan-bridge.test.js` (SCAN-01 Tool-Hälfte, SCAN-08)</name>
  <precondition>Plan 05-01 und 05-02 gelandet: `grep -c 'async function idbSnapshotPut(record)' persistence.js` = 1 und `grep -c 'window.__BCK_buildGameInventory' loader.js` = 1; `npm test` grün (22 Dateien)</precondition>
  <files>tests/game-scan-bridge.test.js</files>
  <read_first>tests/exec-log.test.js (boot-Muster mit opener/Handshake/getElementById-Override), tests/bridge-registry.test.js (`send`/`msg`-Helfer, `dispatchMessage`), tests/screenshot-store.test.js (Op-Spy auf `IDBObjectStore.prototype`, `afterEach`-Restauration), tests/load-order-guard.test.js (Guard-Test mit `makeSandbox`/`loadInto`, docs-/index.html-Gates), tests/injected-code-origin.test.js (`WILD`, Host-Ableitung), tests/helpers/loadScript.js; Task 2 dieses Plans (Bezeichner/Texte sind Vertrag)</read_first>
  <action>
    Neue Datei (ESM; Imports `describe, it, expect, vi, beforeEach, afterEach` aus vitest; `fs`, `path`; `loadScript, loadInto, makeSandbox, makeElementStub, evalIn, dispatchMessage, settle, expandLoadOrder, CORE_SCRIPTS, REPO_ROOT` aus `./helpers/loadScript.js`). Kopfkommentar (Deutsch): SCAN-01 Tool-Hälfte + SCAN-08; Bridge-Nachrichten werden simuliert (Loader läuft hier nicht); fake-indexeddb pro Datei geteilt → relative Zählungen.
    Helfer: `BC = 'https://game.test'`; `async function boot({ handshake = true } = {})` → `opener = { closed: false, postMessage: vi.fn() }`; `info = makeElementStub()`; `ctx = loadScript(['items.js', 'game-scan.js'], { opener, setTimeout: () => 0, clearTimeout: () => {} })`; `if (handshake) evalIn(ctx, "_bcOrigin = '" + BC + "'")`; `ctx.document.getElementById = id => id === 'gameScanInfo' ? info : makeElementStub()`; `ctx.showStatus = vi.fn()`; `evalIn(ctx, '_idbFehlerGemeldet = 0')`; `await settle(60)`; `opener.postMessage.mockClear()`; `return { ctx, opener, info }`. `recv(ctx, opener, data)` → `dispatchMessage(ctx, { app: 'BCKonfigurator', ...data }, { origin: BC, source: opener })`. `inventory(extra = {})` → `{ schema: 1, gameVersion: 'R131', ts: 1, durationMs: 5, globals: { total: 3, getters: [], functions: [], values: [], byPrefix: {}, inventory: { groups: [], other: { count: 0, names: [] } } }, assets: { count: 0, groupCount: 0, groups: [], items: [] }, modSdk: { available: true, version: '1.2.0', modCount: 2, patchingCount: 0, patching: [] }, mods: [{ name: 'BCX', fullName: 'Bondage Club Extended', version: '1.1.19', repository: 'r' }, { name: 'WCE', fullName: 'Wholesome Club Extensions', version: '6.3.19', repository: 'r' }], probes: {}, chatHooks: {}, errors: [], ...extra }`. Op-Spy: `origAdd = globalThis.IDBObjectStore.prototype.add`, `adds = []`, `installAddSpy()` (sammelt `{ store: this.name, arg }` und ruft das Original), `afterEach` restauriert; `count(hay, needle)`; `src(file)`.
    **describe „Ladereihenfolge-Guard (game-scan.js)“**
    1. „ohne items.js: FATAL-Box nennt items.js und game-scan.js, Throw“: `sb = makeSandbox(); created = []; sb.document.createElement = tag => { const el = makeElementStub(); el.tagName = tag; created.push(el); return el; }; loadInto(sb, 'persistence.js'); loadInto(sb, 'bridge.js'); expect(() => loadInto(sb, 'game-scan.js')).toThrow(/items\.js/)`; Box `id === 'loadOrderFatal'` existiert, `textContent` beginnt mit `FATAL: `, enthält `game-scan.js` und endet mit `(siehe docs/LOAD-ORDER.md)`; `typeof sb.triggerGameScan === 'undefined'`.
    2. „mit allen Vorläufern: kein Throw, triggerGameScan ist Funktion, beide Handler registriert“: `{ ctx } = await boot()`; `typeof ctx.triggerGameScan === 'function'`; `evalIn(ctx, "(_bridgeHandlers.get('GAME_INVENTORY_PROGRESS') || []).length") === 1`; dito `GAME_INVENTORY_DATA`.
    **describe „triggerGameScan (SCAN-01)“**
    3. „sendet GET_GAME_INVENTORY mit reqId gi_<ts>_<n> an den gelernten Origin; Statuszeile „Scan läuft…““: `reqId = ctx.triggerGameScan()`; `expect(reqId).toMatch(/^gi_\d+_\d+$/)`; `opener.postMessage` `toHaveBeenCalledWith({ app: 'BCKonfigurator', type: 'GET_GAME_INVENTORY', reqId }, BC)`; `info.textContent` `toContain('Scan läuft')`.
    4. „zwei Klicks → zwei verschiedene reqIds“: `a = ctx.triggerGameScan(); b = ctx.triggerGameScan(); expect(a).not.toBe(b)`.
    5. „ohne Handshake: null, kein GET_GAME_INVENTORY, Statuszeile nennt Verbinden“: `boot({ handshake: false })` → `ctx.triggerGameScan() === null`; keine `postMessage`-Call mit `type: 'GET_GAME_INVENTORY'`; `info.textContent` `toContain('Verbinden')`.
    **describe „GAME_INVENTORY_PROGRESS“**
    6. „pendende reqId: Statuszeile „Schritt 2/6 – Assets 3/10“; fremde reqId: unverändert“: `reqId = ctx.triggerGameScan()`; `recv({ type: 'GAME_INVENTORY_PROGRESS', reqId, step: 2, total: 6, label: 'Assets 3/10' })` → `info.textContent` enthält `'2/6'` und `'Assets 3/10'`; `before = info.textContent`; `recv({ … reqId: 'gi_fremd_9', step: 5, total: 6, label: 'X' })` → `info.textContent === before`.
    7. „fehlendes Statuselement wirft nicht“: `ctx.document.getElementById = () => null`; `expect(() => recv(PROGRESS …)).not.toThrow()`; `ctx.triggerGameScan()` wirft nicht.
    **describe „GAME_INVENTORY_DATA → Snapshot (SCAN-08)“**
    8. „pendende reqId: genau ein add in snapshots, Datensatz-Form, Größenlog, Erfolgs-Status, Statuszeile“: `installAddSpy(); infoSpy = vi.spyOn(console, 'info').mockImplementation(() => {})`; `reqId = ctx.triggerGameScan()`; `inv = inventory()`; `recv({ type: 'GAME_INVENTORY_DATA', reqId, snapshot: inv })`; `await settle(80)`; `snapAdds = adds.filter(a => a.store === 'snapshots')`; `snapAdds.length === 1`; `rec = snapAdds[0].arg`; `Object.keys(rec).sort()` `toEqual(['gameVersion', 'id', 'inventory', 'modCount', 'mods', 'sizeBytes', 'ts'])`; `rec.id === rec.ts`; `typeof rec.ts === 'number'` und `Math.abs(rec.ts - Date.now()) < 5000`; `rec.gameVersion === 'R131'`; `rec.modCount === 2`; `rec.mods` `toEqual([{ name: 'BCX', version: '1.1.19' }, { name: 'WCE', version: '6.3.19' }])`; `rec.sizeBytes === JSON.stringify(inv).length`; `rec.inventory` `toEqual(inv)`; `infoSpy` `toHaveBeenCalledWith(expect.stringContaining('[GameScan]'), rec.sizeBytes, expect.anything(), expect.anything(), expect.anything(), expect.anything())` — oder robuster: ein `info`-Aufruf, dessen Argumente `'[GameScan]'` und `rec.sizeBytes` enthalten; `ctx.showStatus` `toHaveBeenCalledWith(expect.stringContaining('gespeichert'), 'success')`; `info.textContent` enthält `'R131'`, `'2 Mods'`, `'Snapshot'`; `(await ctx.idbSnapshotGet(rec.id))` `toEqual(rec)`; `infoSpy.mockRestore()`.
    9. „zweiter Scan behält den ersten: zwei Datensätze, erster unverändert (Kernwert)“: `n0 = (await ctx.idbSnapshotKeys()).length`; erster Scan mit `inventory()`, zweiter Scan (neue reqId) mit `inventory({ gameVersion: 'R132' })` (zwischen den Scans `await settle(5)`, damit `Date.now()` sich unterscheidet — bei Gleichstand wäre add-only sichtbar rot statt still); `keys = await ctx.idbSnapshotKeys()`; `keys.length === n0 + 2`; die zwei neuen Datensätze (per `idbSnapshotGet`) haben `gameVersion` `'R131'` bzw. `'R132'`; `ctx.showStatus` nie mit `'error'` aufgerufen.
    10. „gleichzeitige Scans: DATA für beide pendenden reqIds wird gespeichert; danach ist die reqId verbraucht“: `a = trigger(); b = trigger()`; `recv(DATA a)`; `await settle(5)`; `recv(DATA b)`; `await settle(80)`; zwei neue Keys; `installAddSpy()`; `recv(DATA a)` erneut → `await settle(50)` → `adds.filter(snapshots).length === 0`.
    11. „unbekannte reqId: kein add, kein Status (T-5-04)“: `installAddSpy(); recv({ type: 'GAME_INVENTORY_DATA', reqId: 'gi_fremd_1', snapshot: inventory() })`; `await settle(50)`; 0 adds; `ctx.showStatus` nicht aufgerufen.
    12. „err: Fehler-Status mit Fehlertext, nichts gespeichert, Statuszeile „fehlgeschlagen““: `reqId = trigger()`; `installAddSpy()`; `recv({ type: 'GAME_INVENTORY_DATA', reqId, err: 'DataCloneError: could not be cloned' })`; `await settle(50)`; 0 adds; `ctx.showStatus` `toHaveBeenCalledWith(expect.stringContaining('DataCloneError'), 'error')` und der Text enthält `'fehlgeschlagen'`; `info.textContent` `toContain('fehlgeschlagen')`.
    13. „DATA ohne snapshot-Objekt: wie err behandelt“: `recv({ type: 'GAME_INVENTORY_DATA', reqId: trigger() })` → Fehler-Status, 0 adds.
    14. „Quota: add wirft QuotaExceededError → „Speicher voll“ sichtbar, Statuszeile „NICHT gespeichert“, kein Erfolgs-Status“: `globalThis.IDBObjectStore.prototype.add = function () { const e = new Error('quota'); e.name = 'QuotaExceededError'; throw e; }`; `reqId = trigger()`; `recv(DATA inventory())`; `await settle(80)`; `ctx.showStatus` `toHaveBeenCalledWith(expect.stringContaining('Speicher voll'), 'error')`; `info.textContent` `toContain('NICHT gespeichert')`; kein Aufruf mit `'success'`.
    15. „Init: Statuszeile zeigt vorhandene Snapshot-Anzahl beim Laden“: nach mindestens einem gespeicherten Snapshot in dieser Datei `{ info: info2 } = await boot()` → `info2.textContent` `toMatch(/\d+ Snapshot/)`.
    **describe „statisch: Ladeordnung, index.html, docs, game-scan.js“**
    16. „index.html: Write-Zeile direkt nach items.js im ersten _cbv-Block, Kommentar nennt game-scan.js, Sektion 🔎 Spiel-Scan genau einmal zwischen Screenshot-Speicher und Item-Katalog“: `html = src('index.html')`; `count(html, 'game-scan.js?_=') === 1`; `html.indexOf('items.js?_=') < html.indexOf('game-scan.js?_=')`; `html.indexOf('game-scan.js?_=') < html.indexOf('</script>', html.indexOf('var _cbv = Date.now();'))`; `html.slice(html.indexOf('LADEREIHENFOLGE'), html.indexOf('LADEREIHENFOLGE') + 260)` enthält `'game-scan.js'`; `count(html, 'onclick="triggerGameScan()"') === 1`; `count(html, 'id="gameScanInfo"') === 1`; `count(html, '🔎 Spiel scannen') === 1`; `count(html, '🔎 Spiel-Scan') === 1`; `html.indexOf('id="screenshotStoreInfo"') < html.indexOf('id="gameScanInfo"')`; `html.indexOf('id="gameScanInfo"') < html.indexOf('📦 Item-Katalog')`.
    17. „docs/LOAD-ORDER.md + CORE_SCRIPTS + game-scan.js-Quelle“: `doc = src('docs/LOAD-ORDER.md')`; `count(doc, 'game-scan.js') >= 3`; `doc` enthält `'GAME_INVENTORY_DATA'`, `'GAME_SCAN_DATA'` (Guard-Test-Regex), `'idbSnapshotPut'`, `"'game-scan.js'"` (CORE_SCRIPTS-Text) und `'332fc2d'`; `doc` enthält NICHT die Phrase „keine try/catch-Isolation“ (`expect(doc).not.toContain('keine try/catch-Isolation')`); `CORE_SCRIPTS` `toEqual(['persistence.js', 'bridge.js', 'items.js', 'game-scan.js'])`; `expandLoadOrder(['items.js'])` `toEqual([...CORE_SCRIPTS])`; `expandLoadOrder(['items.js', 'bot-data.js'])` `toEqual([...CORE_SCRIPTS, 'bot-data.js'])`; `expandLoadOrder(['items.js', 'game-scan.js'])` `toEqual([...CORE_SCRIPTS])`; `expandLoadOrder(['persistence.js', 'items.js'])` `toEqual([...CORE_SCRIPTS])`; `expandLoadOrder(['bot-data.js'])` `toEqual(['bot-data.js'])`. `gs = src('game-scan.js')`: `count(gs, "onBridgeMessage('GAME_INVENTORY_PROGRESS'") === 1`; `count(gs, "onBridgeMessage('GAME_INVENTORY_DATA'") === 1`; `count(gs, "type: 'GET_GAME_INVENTORY'") === 1`; `count(gs, 'idbSnapshotPut(') >= 1`; `count(gs, 'idbSnapshotKeys(') >= 1`; `count(gs, 'loadOrderFatal') === 1` und `gs` enthält `'wurde nicht vor game-scan.js geladen'`; 0× `addEventListener('message'`, 0× `postMessage(`, 0× `indexedDB.open(`, 0× `.delete(`, 0× `.clear(`, 0× `idbSnapshotDelete`, 0× `location.origin`, 0× `toLocaleString`; Tool-Host (aus loader.js `POPUP_URL` abgeleitet, Muster tests/injected-code-origin.test.js) 0×; keine Zeile matcht `WILD`.
    Commit (nur dieser Pfad): `git add tests/game-scan-bridge.test.js` → `test(05-03): Tool-seitige Spiel-Scan-Tests (Trigger, PROGRESS, DATA→Snapshot, Fehlerpfade, Ladeordnung) — RED`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/game-scan-bridge.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "game-scan\.js|triggerGameScan|GAME_INVENTORY_DATA|CORE_SCRIPTS" "$OUT" && grep -Eq "Tests +[0-9]+ failed|Test Files +[0-9]+ failed" "$OUT" && test ! -e game-scan.js && git diff --quiet HEAD -- index.html docs/LOAD-ORDER.md tests/helpers/loadScript.js tests/persistence-module.test.js && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "tests/game-scan-bridge.test.js " && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Exit 0, die Ausgabe nennt keinen der Bezeichner und keine `failed`-Zeile, game-scan.js existiert bereits, eine der Produktions-/Doku-/Helper-Dateien wurde schon geändert, oder der Commit enthält andere Pfade</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" tests/game-scan-bridge.test.js` >= 17; `grep -c 'QuotaExceededError' tests/game-scan-bridge.test.js` >= 1; `grep -c 'gi_fremd' tests/game-scan-bridge.test.js` >= 2; `grep -c 'expandLoadOrder' tests/game-scan-bridge.test.js` >= 5; `grep -c "GAME_SCAN_DATA" tests/game-scan-bridge.test.js` >= 1
    - `grep -c 'animereviewer1-sketch' tests/game-scan-bridge.test.js` = 0; `afterEach` restauriert `IDBObjectStore.prototype.add`; kein `.only(`
    - `npm test` außer der neuen Datei unverändert grün (`npx vitest run --exclude tests/game-scan-bridge.test.js` → 22 Dateien)
  </acceptance_criteria>
  <done>17 Fälle über sechs describe-Blöcke; Lauf nachweislich rot (ENOENT game-scan.js); roter Test committet; keine andere Datei verändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `game-scan.js` + index.html-Einfügungen + `CORE_SCRIPTS`/`expandLoadOrder` + docs/LOAD-ORDER.md — GREEN; End-of-Phase-Human-Checks (SCAN-01 Tool-Hälfte, SCAN-08)</name>
  <precondition>`grep -c 'window.__BCK_buildGameInventory' loader.js` = 1 (Plan 05-02) und `grep -c 'async function idbSnapshotKeys()' persistence.js` = 1 (Plan 05-01)</precondition>
  <files>game-scan.js, index.html, tests/helpers/loadScript.js, tests/persistence-module.test.js, docs/LOAD-ORDER.md</files>
  <read_first>tests/game-scan-bridge.test.js (Task 1 — jede Erwartung ist Vertrag); bot-ui.js Zeilen 1-15 (Guard); items.js `_renderScreenshotStoreInfo` (Statuszeilen-Stil, ISO-Datum) und `showStatus`; bridge.js `bcSend`/`onBridgeMessage`; persistence.js `idbSnapshotPut`/`idbSnapshotKeys`/`_idbSchreibfehler`; index.html Zeilen 2473-2486 (Screenshot-Speicher-Sektion, Item-Katalog) und 3097-3104 (`LADEREIHENFOLGE`, erster `_cbv`-Block); docs/LOAD-ORDER.md (gesamt); tests/helpers/loadScript.js (`CORE_SCRIPTS`, `expandLoadOrder`); tests/persistence-module.test.js Zeilen 96-110</read_first>
  <behavior>
    - Klick → `GET_GAME_INVENTORY` mit eindeutiger reqId (nur nach Handshake); PROGRESS aktualisiert die Statuszeile; DATA für pendende reqId → Datensatz add-only gespeichert, Größe geloggt, Erfolg/Fehler sichtbar; fremde reqId ignoriert; beide gleichzeitigen Scans gespeichert; zweiter Scan behält den ersten
    - game-scan.js lädt als viertes Kern-Skript; Guard bei falscher Reihenfolge; Doku und Test-Ladeordnung konsistent
  </behavior>
  <action>
    **A. `game-scan.js`** (neu, klassisches Skript, deutsche Kommentare, englische Bezeichner):
    - Kopfkommentar: `// ── game-scan.js — Spiel-Scan (SCAN-01/SCAN-08): Trigger, Fortschritt, Snapshot-Speicherung ──` + Erläuterung: lädt NACH persistence.js, bridge.js, items.js (docs/LOAD-ORDER.md); registriert seine zwei Nachrichtentypen über `onBridgeMessage` (keine Änderung an items.js/bridge.js, SPLIT-02); Snapshots werden nie überschrieben oder automatisch gelöscht (Kernwert; Löschen mit Bestätigung folgt in Phase 6).
    - Ladereihenfolge-Guard als ERSTE Anweisung (Muster bot-ui.js, wörtlich übernommen mit angepasster Tabelle): `required = [['idbSnapshotPut', 'persistence.js'], ['bcSend', 'bridge.js'], ['onBridgeMessage', 'bridge.js'], ['showStatus', 'items.js']]`, Meldung `'FATAL: ' + missing.join(', ') + ' wurde nicht vor game-scan.js geladen – Ladereihenfolge in index.html prüfen (siehe docs/LOAD-ORDER.md)'`, Box `#loadOrderFatal`, `console.error('[BCK-Popup] ' + msg)`, `throw`.
    - Zustand: `let _giSeq = 0;` `const _giPending = new Map(); // reqId → { startedAt }`.
    - `function _gameScanInfo(text)` → `const el = document.getElementById('gameScanInfo'); if (el) el.textContent = text;`.
    - `function _gameScanFormatTs(ts)` → `new Date(ts).toISOString().slice(0, 16).replace('T', ' ') + ' UTC'` (keine locale-API).
    - `function _gameScanKb(bytes)` → `Math.round((bytes || 0) / 1024)`.
    - `function triggerGameScan()` → `const reqId = 'gi_' + Date.now() + '_' + (++_giSeq); if (!bcSend({ type: 'GET_GAME_INVENTORY', reqId })) { _gameScanInfo('❌ Nicht verbunden – erst 🔄 Verbinden, dann erneut scannen'); return null; } _giPending.set(reqId, { startedAt: Date.now() }); _gameScanInfo('Scan läuft… (angefordert ' + _gameScanFormatTs(Date.now()) + ')'); return reqId;`.
    - `onBridgeMessage('GAME_INVENTORY_PROGRESS', function(ev) { const d = ev.data || {}; if (!_giPending.has(d.reqId)) return; _gameScanInfo('Scan läuft… Schritt ' + d.step + '/' + d.total + ' – ' + (d.label || '')); });`
    - `onBridgeMessage('GAME_INVENTORY_DATA', function(ev) { const d = ev.data || {}; if (!_giPending.has(d.reqId)) return; const meta = _giPending.get(d.reqId); _giPending.delete(d.reqId); if (d.err || !d.snapshot || typeof d.snapshot !== 'object') { const why = d.err ? String(d.err) : 'keine Daten empfangen'; showStatus('❌ Spiel-Scan fehlgeschlagen: ' + why, 'error'); _gameScanInfo('❌ Scan fehlgeschlagen: ' + why); return; } _saveGameInventorySnapshot(d.snapshot, meta); });` — Hinweis: `_giPending.delete` ist das Entfernen eines Map-Eintrags im RAM, keine IDB-Operation; die statischen Gates zählen `.delete(` auf der Quelle — deshalb den Eintrag stattdessen über `_giPending.set(d.reqId, null)` + `has`-Prüfung auf `!!_giPending.get(reqId)` als „verbraucht“ markieren, oder die Pending-Verwaltung als Plain-Object `_giPending = Object.create(null)` mit `_giPending[reqId] = null` führen (statisch sauber, Test 10 „danach verbraucht“ bleibt erfüllt). Gewählt: Plain-Object, `_giPendingHas(reqId)` = `Object.prototype.hasOwnProperty.call(_giPending, reqId) && !!_giPending[reqId]`, verbraucht = `_giPending[reqId] = null`.
    - `async function _saveGameInventorySnapshot(inventory, meta)` → `const ts = Date.now(); const modsRaw = Array.isArray(inventory.mods) ? inventory.mods : []; const mods = modsRaw.map(m => ({ name: String((m && m.name) ?? ''), version: String((m && m.version) ?? '') })); let sizeBytes = null; try { sizeBytes = JSON.stringify(inventory).length; } catch (e) { sizeBytes = null; } const record = { id: ts, ts, gameVersion: typeof inventory.gameVersion === 'string' ? inventory.gameVersion : null, modCount: mods.length, mods, sizeBytes, inventory }; console.info('[GameScan] Snapshot-Größe (JSON-Zeichen):', sizeBytes, '· Mods:', mods.length, '· BC', record.gameVersion, '· Dauer ms:', meta && meta.startedAt ? ts - meta.startedAt : null); const ok = await idbSnapshotPut(record); if (!ok) { _gameScanInfo('❌ Snapshot NICHT gespeichert – siehe Statusmeldung (Speicher voll?)'); return false; } showStatus('✅ Spiel-Scan gespeichert – ' + mods.length + ' Mods, ' + _gameScanKb(sizeBytes) + ' KB', 'success'); await _renderGameScanInfo(record); return true;`.
    - `async function _renderGameScanInfo(last)` → `try { const keys = await idbSnapshotKeys(); const n = keys.length; let text; if (last) text = 'Letzter Scan ' + _gameScanFormatTs(last.ts) + ' · BC ' + (last.gameVersion || '?') + ' · ' + last.modCount + ' Mods · ' + _gameScanKb(last.sizeBytes) + ' KB · ' + n + ' Snapshot' + (n === 1 ? '' : 's') + ' gespeichert'; else if (n > 0) text = n + ' Snapshot' + (n === 1 ? '' : 's') + ' gespeichert · letzter ' + _gameScanFormatTs(Math.max.apply(null, keys.filter(k => typeof k === 'number'))); else text = 'Noch kein Scan gespeichert'; _gameScanInfo(text); } catch (e) { /* still: Statuszeile ist Komfort, kein Datenpfad */ }` — keine Konsolenausgabe im Erfolgspfad (Befund 3).
    - Init-Hook (Konvention 02-03): `try { if (document.readyState !== 'loading') _renderGameScanInfo(); else document.addEventListener('DOMContentLoaded', function() { _renderGameScanInfo(); }); } catch (e) {}`.
    - Keine `postMessage`-, `addEventListener('message'`-, `indexedDB`-, Lösch- oder `location.origin`-Nutzung; kein Tool-Host-String; keine `toLocaleString`.
    **B. `index.html`** (nur Einfügungen + eine Kommentarzeile): (1) Zeile 3097: im `LADEREIHENFOLGE`-Kommentar `→ items.js → money.js` zu `→ items.js → game-scan.js → money.js` ergänzen. (2) Nach der Zeile `document.write('<scr'+'ipt src="items.js?_='+_cbv+'"><\/scr'+'ipt>');` die Zeile `document.write('<scr'+'ipt src="game-scan.js?_='+_cbv+'"><\/scr'+'ipt>');` einfügen (gleicher Block). (3) Tweaks-Panel: direkt nach dem schließenden `</div>` der Sektion `🖼️ Screenshot-Speicher` (also vor `<div>` mit `📦 Item-Katalog`) einfügen: `<div>` / `<div class="tweaks-section-title">🔎 Spiel-Scan</div>` / `<div style="font-size:.6875rem;color:var(--text3);margin-bottom:7px">Read-only-Inventar des laufenden Spiels (Globals, Assets, Mods, Hooks). Jeder Scan wird als Snapshot in IndexedDB gespeichert und nie automatisch gelöscht – der Spiel-Tab bleibt währenddessen bedienbar.</div>` / `<div class="tweaks-btn-group">` / `<button class="tweaks-btn" id="gameScanBtn" onclick="triggerGameScan()" title="Inventar des laufenden Spiels read-only erheben und als Snapshot speichern">🔎 Spiel scannen</button>` / `</div>` / `<div id="gameScanInfo" style="font-size:.6875rem;color:var(--text3);margin-top:5px;line-height:1.5">–</div>` / `</div>`.
    **C. `tests/helpers/loadScript.js`:** `CORE_SCRIPTS = ['persistence.js', 'bridge.js', 'items.js', 'game-scan.js']`; `expandLoadOrder(files)`: `idx = files.indexOf('items.js')`; `-1` → Kopie; sonst `itemsPos = CORE_SCRIPTS.indexOf('items.js')`, `before = CORE_SCRIPTS.slice(0, itemsPos).filter(f => !files.includes(f))`, `after = CORE_SCRIPTS.slice(itemsPos + 1).filter(f => !files.includes(f))`, Rückgabe `[...files.slice(0, idx), ...before, 'items.js', ...after, ...files.slice(idx + 1)]`; Kopfkommentar um einen Absatz „Phase 5: game-scan.js als Kern-Skript NACH items.js“ ergänzen.
    **D. `tests/persistence-module.test.js`** (nur die zwei Erwartungen, Befund 3): Testname/Erwartung „endet mit items.js“ → `expect(CORE_SCRIPTS.at(-1)).toBe('game-scan.js')` plus `expect(CORE_SCRIPTS.indexOf('items.js')).toBeLessThan(CORE_SCRIPTS.indexOf('game-scan.js'))`; `expandLoadOrder(['persistence.js', 'items.js'])` → `toEqual(['persistence.js', 'bridge.js', 'items.js', 'game-scan.js'])`.
    **E. `docs/LOAD-ORDER.md`:** (1) Tabelle: neue Zeile `| 7 | game-scan.js | Spiel-Scan: triggerGameScan, Handler GAME_INVENTORY_PROGRESS/GAME_INVENTORY_DATA, Snapshot-Speicherung (idbSnapshotPut), Statuszeile #gameScanInfo | persistence.js (idbSnapshotPut/idbSnapshotKeys), bridge.js (bcSend/onBridgeMessage), items.js (showStatus) |` (Bezeichner in Backticks); Zeilen money.js … bc-autobackup.js auf 8 … 16 renummerieren. (2) Guard-Absatz: Satz ergänzen, dass game-scan.js dieselbe Kette plus `idbSnapshotPut` prüft. (3) Abschnitt „Neuen Nachrichtentyp registrieren“: Beispiel `GAME_SCAN_DATA` beibehalten; Satz ergänzen „Lebendes Beispiel seit Phase 5: `game-scan.js` registriert `GAME_INVENTORY_PROGRESS` und `GAME_INVENTORY_DATA` genau so und speichert über `idbSnapshotPut`.“; den veralteten Satz über fehlende Isolation ERSETZEN durch: „Seit Commit `332fc2d` isoliert `_bridgeDispatch` jeden Handler per try/catch (`console.error('[Bridge] Handler-Fehler bei …')`) — ein werfender Handler blockiert seine Geschwister nicht; die Registrierung derselben Funktion ein zweites Mal ist ein No-op.“ (4) „Neues Modul hinzufügen“ Schritt 4: „… an der richtigen Position relativ zu persistence.js/bridge.js/items.js — Skripte, die NACH items.js laden, stehen in `CORE_SCRIPTS` hinter `'items.js'` und werden von `expandLoadOrder` direkt nach dem ersten `items.js` eingefügt.“ (5) Abschnitt „Tests“: `CORE_SCRIPTS` (aktuell `['persistence.js', 'bridge.js', 'items.js', 'game-scan.js']`) und die Vor-/Nach-items-Regel beschreiben.
    **F. GREEN, Gates, Commit:** `node --check game-scan.js`; `npx vitest run tests/game-scan-bridge.test.js tests/load-order-guard.test.js tests/persistence-module.test.js tests/load-script.test.js tests/bridge-registry.test.js tests/screenshot-export.test.js`; `npm test`. Commit (genau diese fünf Pfade): `git add docs/LOAD-ORDER.md game-scan.js index.html tests/helpers/loadScript.js tests/persistence-module.test.js` → `feat(05-03): game-scan.js — Spiel-Scan-Trigger, Fortschritt, Snapshot-Speicherung; Ladeordnung + Doku (SCAN-01, SCAN-08)`.
    Fürs SUMMARY: RED-/GREEN-Ausgaben, Datensatz-Form, die Human-Checks (siehe `<human-check>`) als offene Liste, Hinweis „Bookmarklet nach Deploy neu ausführen (Loader-Update)“, Payload-Größe nach dem ersten echten Scan nachtragen (RESEARCH Open Question 2, Schwelle 20 MB → erst dann Mehrteiligkeit erwägen).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && node --check game-scan.js && npx vitest run tests/game-scan-bridge.test.js tests/load-order-guard.test.js tests/persistence-module.test.js tests/load-script.test.js tests/bridge-registry.test.js tests/screenshot-export.test.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` — etwa weil der Guard fehlt/falsch benannt ist (Test 1), `bcSend` ohne Handshake nicht abgewiesen wird (Test 5), die reqId-Filterung fehlt (Test 6/11), der Datensatz andere Keys hat (Test 8), gleichzeitige Scans nur den letzten speichern (Test 10), der Quota-Pfad still bleibt (Test 14), `expandLoadOrder` game-scan.js vor items.js einfügt (Guard-Throw in jeder items.js-Sandbox), oder eine bestehende Datei durch das zusätzliche Kern-Skript rot wird</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(grep -c 'game-scan.js?_=' index.html)" = 1 && test "$(grep -c 'onclick=\"triggerGameScan()\"' index.html)" = 1 && test "$(grep -c 'id=\"gameScanInfo\"' index.html)" = 1 && test "$(git diff f3973f6 HEAD -- index.html | grep '^-[^-]' | grep -vc 'LADEREIHENFOLGE')" = 0 && test "$(grep -Fc "'game-scan.js'" tests/helpers/loadScript.js)" -ge 1 && test "$(grep -c 'game-scan.js' docs/LOAD-ORDER.md)" -ge 3 && test "$(grep -c '332fc2d' docs/LOAD-ORDER.md)" -ge 1 && test "$(grep -c 'GAME_SCAN_DATA' docs/LOAD-ORDER.md)" -ge 1 && test "$(grep -c 'loadOrderFatal' game-scan.js)" = 1 && test "$(grep -Fc "onBridgeMessage('GAME_INVENTORY_DATA'" game-scan.js)" = 1 && test "$(grep -Fc "onBridgeMessage('GAME_INVENTORY_PROGRESS'" game-scan.js)" = 1 && test "$(grep -c 'idbSnapshotPut(' game-scan.js)" -ge 1 && test "$(grep -Fc '.delete(' game-scan.js)" = 0 && test "$(grep -Fc '.clear(' game-scan.js)" = 0 && test "$(grep -c 'idbSnapshotDelete' game-scan.js)" = 0 && test "$(grep -Fc 'indexedDB.open(' game-scan.js)" = 0 && test "$(grep -Fc 'postMessage(' game-scan.js)" = 0 && test "$(grep -Fc "addEventListener('message'" game-scan.js)" = 0 && test "$(grep -Fc 'location.origin' game-scan.js)" = 0 && test "$(grep -c 'toLocaleString' game-scan.js)" = 0 && test -z "$(git diff --name-only f3973f6 HEAD -- items.js bridge.js tests/loader-origin.test.js)" && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "docs/LOAD-ORDER.md game-scan.js index.html tests/helpers/loadScript.js tests/persistence-module.test.js " && echo GAME-SCAN-OK</automated>
    <fails_when>`GAME-SCAN-OK` fehlt — index.html hat gelöschte Zeilen außer der Kommentarzeile, Button/Statuszeile/Write-Zeile fehlen oder doppeln, `CORE_SCRIPTS`/Doku nicht nachgezogen, game-scan.js enthält eigene Bridge-/IDB-/Lösch-Logik oder Origin-Bezüge, items.js/bridge.js/loader-origin-Test wurden angefasst, oder der Commit enthält andere Pfade als die fünf genannten</fails_when>
    <human-check>End-of-Phase-Human-Checks der Phase 5 (nicht blockierend, im SUMMARY listen; Orchestrator-Entscheidung 6): (1) Deploy auf GitHub Pages; im Spiel-Tab das Bookmarklet NEU ausführen (der Loader trägt jetzt den Enumerator — ohne erneuten Lauf antwortet der alte Loader nicht auf `GET_GAME_INVENTORY`); Tool hart neu laden (Strg+F5): kein FATAL-Banner; ⚙️ → Sektion `🔎 Spiel-Scan` zeigt „Noch kein Scan gespeichert“. (2) Verbinden, dann „🔎 Spiel scannen“: die Statuszeile zählt „Schritt 1/6 – Globals x/38 … 6/6 – Chat-Hooks“ hoch; währenddessen im Spiel-Tab chatten, scrollen, Menü öffnen — der Tab bleibt bedienbar (kein Einfrieren); nach wenigen Sekunden „✅ Spiel-Scan gespeichert – 28 Mods, N KB“ und die Statuszeile „Letzter Scan … · BC R131 · 28 Mods · N KB · 1 Snapshot gespeichert“. (3) Tool-Konsole: Zeile `[GameScan] Snapshot-Größe (JSON-Zeichen): <Zahl> …` — Zahl ins SUMMARY übernehmen (RESEARCH Open Question 2; erst ab > 20 MB Mehrteiligkeit erwägen); Spiel-Tab-Konsole: `[BCK-BC] [OK] [GameScan] Inventar gesendet: ~18700 Globals, 4764 Assets, 28 Mods, <ms> ms`. (4) DevTools → Application → IndexedDB → `BCKonfigurator` (Version 3) → Store `snapshots`: ein Datensatz mit `gameVersion: "R131"`, `modCount: 28`, `mods` (BCT, FUSAM, WCE, LSCG, MBS, BCX, Themed …), `inventory.globals.total` ≈ 18700, `inventory.globals.getters.length` ≈ 189, `inventory.assets.count` = 4764, `inventory.assets.groupCount` = 120, `inventory.modSdk.patchingCount` ≈ 546, `inventory.probes.bcx.version` gesetzt, `inventory.probes.bcx.api.length` > 0, `inventory.chatHooks.registry.introspectable` (Wert notieren), `inventory.errors` leer. (5) Zweiter Scan: zwei Datensätze, der erste unverändert (id/ts identisch, Inhalt gleich); Statuszeile „2 Snapshots gespeichert“; nach Tool-Reload zeigt die Statuszeile weiterhin „2 Snapshots gespeichert“. (6) Bestehende Flows unverändert: Cache laden, Raum-Scan, Outfit-Screenshot, Bot-Deploy (EXEC-Log-Eintrag), 🔄 Verbinden nach BC-Reload.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/game-scan-bridge.test.js` → 17 passed; `npm test` → 23 Testdateien grün, 2 expected fail unverändert
    - `grep -c "showStatus(" game-scan.js` >= 2 (Erfolg + Fehler); `grep -c "'\[GameScan\] Snapshot-Größe" game-scan.js` >= 1; `grep -c '_renderGameScanInfo' game-scan.js` >= 3; `grep -c 'DOMContentLoaded' game-scan.js` >= 1
    - `grep -c '| 16 |' docs/LOAD-ORDER.md` >= 1 (bc-autobackup.js renummeriert) und `grep -c '| 7 | \`game-scan.js\`' docs/LOAD-ORDER.md` >= 1
    - Zwei Commits dieses Plans: `test(05-03)` → `feat(05-03)`; `git status --short` leer (außer `.planning/`)
  </acceptance_criteria>
  <done>Klick → `GET_GAME_INVENTORY`; PROGRESS/DATA korreliert über pendende reqIds; jeder Scan als add-only-Datensatz `{id, ts, gameVersion, modCount, mods, sizeBytes, inventory}` mit Größenlog; Fehler sichtbar; game-scan.js als viertes Kern-Skript geladen, dokumentiert und in der Test-Ladeordnung; 17 Tests grün, volle Suite grün; Human-Checks im SUMMARY gelistet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spiel-Tab → Tool-Fenster (`GAME_INVENTORY_*`) | bridge.js prüft Opener + gelernten Origin; game-scan.js filtert zusätzlich über pendende reqIds |
| Tool-Fenster → Spiel-Tab (`GET_GAME_INVENTORY`) | Nur über `bcSend` (Handshake-Pflicht, kein Wildcard) |
| game-scan.js → IndexedDB `snapshots` | add-only über persistence.js; Fehler sichtbar |
| Ladereihenfolge (index.html, Tests) | Guard + `CORE_SCRIPTS` halten Reihenfolge; ein falsch platziertes Modul fällt sichtbar aus |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-04 | Spoofing | Gefälschte/veraltete `GAME_INVENTORY_DATA` erzeugt Snapshot | medium | mitigate | Bridge-Shell (Opener + Origin) unverändert; zusätzlich reqId-Korrelation über `_giPending` (Test 11: unbekannte reqId → kein add; Test 10: verbrauchte reqId → kein add) |
| T-5-05 | Tampering | Snapshot wird überschrieben/gelöscht (Kernwert) | critical | mitigate | add-only-Speicherung (`idbSnapshotPut`, Test 9 zwei Datensätze, erster unverändert); statisch 0× Lösch-/Leer-Operation in game-scan.js; keine Lösch-API in dieser Phase (Phase 6 mit Bestätigung) |
| T-5-06 | Denial of Service | Quota-/Constraint-Fehler beim Speichern bleibt still | high | mitigate | `_idbSchreibfehler` (Quota-Text) + Statuszeile „NICHT gespeichert“ + kein Erfolgs-Status (Test 14); `err`-Nachricht → Fehler-Status (Test 12/13) |
| T-5-11 | Denial of Service | Modul bricht die Ladekette (falsche Reihenfolge, Parse-Fehler) | high | mitigate | Guard mit `#loadOrderFatal` (Test 1), `node --check`, Write-Zeile direkt nach items.js (Test 16), `CORE_SCRIPTS`-Regel (Test 17), volle Suite mit game-scan.js in jeder items.js-Sandbox |
| T-5-12 | Information Disclosure | Snapshot-Inhalt (Mod-Liste, Asset-Katalog) verlässt das Tool | low | accept | Daten bleiben clientseitig in IndexedDB; kein Netzwerkpfad; nur der Nutzer selbst nutzt das Tool (PROJECT.md) |
| T-5-13 | Repudiation | Unklar, wann/was gescannt wurde | low | mitigate | Datensatz mit `ts`, `gameVersion`, `modCount`, `sizeBytes`; Statuszeile mit ISO-Zeit; Konsolen-Log der Größe |
| T-5-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase |
</threat_model>

<verification>
- `npx vitest run tests/game-scan-bridge.test.js` → 17 passed; `npm test` → 23 Dateien grün (2 expected fail unverändert).
- Statische Gates: index.html nur Einfügungen (+ Kommentarzeile), game-scan.js ohne eigene Bridge-/IDB-/Lösch-Logik, Doku und `CORE_SCRIPTS` nachgezogen, items.js/bridge.js/loader-origin-Test unverändert.
- Zwei Commits: test → feat.
- Phase-Gate (VALIDATION.md 5-03-01/5-03-02): ✅; Human-Checks (Manual-Only-Verifications) im 05-03-SUMMARY gelistet — nicht blockierend (Orchestrator-Entscheidung 6).
</verification>

<success_criteria>
- SCAN-01 (Tool): Klick → `GET_GAME_INVENTORY` → PROGRESS/DATA korreliert; ohne Handshake sichtbar abgewiesen.
- SCAN-08: jeder Scan als Datensatz mit BC-Version, Zeitstempel, Mod-Liste; add-only; Fehler sichtbar; frühere Snapshots bleiben.
- Ladeordnung: game-scan.js als Kern-Skript in Produktion, Doku und Tests; alle bestehenden Tests grün.
</success_criteria>

## Artifacts this phase produces

Phasenweite Liste (Besitzer-Plan in Klammern; dieser Plan: 05-03):

**Neue Dateien**
- `game-scan.js` (05-03) — Guard, `triggerGameScan`, Handler `GAME_INVENTORY_PROGRESS`/`GAME_INVENTORY_DATA`, `_saveGameInventorySnapshot`, `_renderGameScanInfo`, Init-Hook
- `tests/game-scan-bridge.test.js` (05-03); `tests/helpers/loaderSandbox.js`, `tests/loader-sandbox.test.js` (05-01); `tests/game-inventory-enumerator.test.js` (05-02)

**Neue Symbole / Strukturen**
- Snapshot-Datensatz `{ id: ts, ts, gameVersion, modCount, mods: [{name, version}], sizeBytes, inventory }` im Store `snapshots` (IDB v3, 05-01) — Vertrag für Phase 6 (Scan-Tab, Baseline-Diff, Löschen mit Bestätigung)
- Tweaks-Sektion `🔎 Spiel-Scan` (`#gameScanBtn`, `#gameScanInfo`) (05-03)
- `CORE_SCRIPTS = ['persistence.js', 'bridge.js', 'items.js', 'game-scan.js']`, `expandLoadOrder` mit Vor-/Nach-items-Regel (05-03)
- loader.js `buildGameInventory`/`GET_GAME_INVENTORY` (05-02); persistence.js `idbSnapshot*` (05-01)

**Geänderte Dateien**: index.html (05-03, Einfügungen + Kommentarzeile), docs/LOAD-ORDER.md (05-03), tests/helpers/loadScript.js (05-03), tests/persistence-module.test.js (05-01, 05-03), loader.js (05-02), persistence.js (05-01), tests/screenshot-migration.test.js (05-01)

<output>
Create `.planning/phases/05-gamecode-inventar/05-03-SUMMARY.md` when done — mit RED-/GREEN-Ausgaben, der Datensatz-Form, der vollständigen Liste der End-of-Phase-Human-Checks (aus `<human-check>`, inkl. Feld für die gemessene Payload-Größe), dem Hinweis „Bookmarklet nach Deploy neu ausführen“ und der Commit-Liste.
</output>
