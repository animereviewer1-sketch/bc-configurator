---
phase: 05-gamecode-inventar
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/game-inventory-enumerator.test.js
  - loader.js
autonomous: true
requirements: [SCAN-02, SCAN-03, SCAN-04, SCAN-05, SCAN-06, SCAN-07]
tags: [loader, enumerator, descriptor-walk, chunking, structured-clone, bcmodsdk, mod-probes, red-green]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SCAN-01 (Loader-Hälfte): `GET_GAME_INVENTORY {reqId}` über den bestehenden, origin-/source-geprüften Listener löst `buildGameInventory(reqId, post)` aus; der Case kehrt sofort zurück (einzige asynchrone Antwort im `switch`, bewusste Abweichung, im Code kommentiert); der Loader postet n× `GAME_INVENTORY_PROGRESS {reqId, step, total: 6, label}` und genau 1× `GAME_INVENTORY_DATA {reqId, snapshot}` (bzw. `{reqId, err}`), jeweils mit `app: APP` an das gepinnte Tool-Fenster mit Ziel-Origin `ev.origin` (an dieser Stelle nachweislich gleich der Tool-Origin, Befund 3) — `tests/loader-origin.test.js` bleibt unverändert grün (Zähler 34)"
    - "SCAN-02: `snapshot.globals` = `{ total, getters: [Namen], functions: [{name, arity}], values: [{name, type}], byPrefix: {16 Präfixe → Anzahl}, inventory: { groups: [{prefix, group, count, sample≤5}], other: {count, names≤50} } }` — Klassifizierung ausschließlich über `Object.getOwnPropertyNames(window)` + `Object.getOwnPropertyDescriptor`; Accessor-Properties werden als Getter-NAMEN erfasst und nie gelesen; Funktionen tragen `arity` aus der `length`-Daten-Property; `InventoryItem<Gruppe>…`-Funktionen werden nach dem längsten passenden `AssetGroup`-Namen gebündelt, `Inventory`-Kernfunktionen (z. B. `InventoryGet`) bleiben Einzeleinträge"
    - "SCAN-03: `snapshot.assets` = `{ count, groupCount, groups: [{…GI_GROUP_KEYS, assetCount}], items: [{ Name, Group: <Group.Name|null>, ParentItem: <Name|string|null>, Layer: {count, names}, …88 Allowlist-Keys }] }` — Allowlist `GI_ASSET_KEYS` (die 100 SCAN-13-Keys ohne `Group`, `ParentItem`, `Layer` und ohne `Dynamic*`), Werte durch `giBounded` (Tiefe ≤ 2, Arrays ≤ 200, Strings ≤ 500, Funktionen fallen weg); `Asset[]` fehlt → `assets: { error }`, Scan läuft trotzdem zu Ende; `JSON.stringify(snapshot)` wirft nicht (kein Zirkel)"
    - "SCAN-04: `snapshot.chatHooks` = `{ ChatRoomRegisterMessageHandler: {exists, arity, kind}, registry: {introspectable:false, checked:[…]} | {introspectable:true, source, count, handlers:[{Description, Priority}]}, hookedChatFunctions: [{name, hookedByMods}] }` — Registry wird nur gelesen (Kandidat `ChatRoomMessageHandlers`), nie wird ein Handler registriert, gewrappt oder gepatcht; Callbacks werden verworfen"
    - "SCAN-05: `snapshot.mods` = `[{name, fullName, version, repository}]` aus `bcModSdk.getModsInfo()`; `snapshot.modSdk` = `{ available, version, modCount, patchingCount, patching: [{name, originalHash, hookedByMods, patchedByMods}] }` aus `getPatchingInfo()` (Map) — die Funktionsfelder `original`/`sdkEntrypoint`/`currentEntrypoint` werden nie übernommen und nie aufgerufen; ohne `bcModSdk`: `mods: []`, `modSdk.available === false`, Scan vollständig"
    - "SCAN-06: `snapshot.probes` = `{ wce: {present, version, functions}, bcx: {present, loaded, version, api}, mbs: {present, version, apiVersion:{major,minor}, api}, lscg: {present, loaded, api, screenFunctions}, themed: {present, loaded, screenFunctionCount, sample}, sweep: [Namen] }` — exakt die fünf Probes der Konsolen-Befunde; `bcx` über `Object.getOwnPropertyNames` (API nicht-enumerierbar), `api`-Listen als `[{name, kind}]` per Deskriptor ohne Lesen von Gettern; abwesende Mods → `present: false`"
    - "SCAN-07: read-only — die einzigen Funktionsaufrufe des Enumerators auf entdeckte Werte sind `bcModSdk.getModsInfo()` und `bcModSdk.getPatchingInfo()` (SDK-Lese-API laut SCAN-05); alle Zähler der Sandbox-Fixtures (`hits.getter`, `hits.fn`, `hits.dynamic`, `hits.patching`) bleiben nach einem vollständigen Scan 0; die Arbeit läuft in Slices von `GI_BATCH = 500` Namen/Assets über `requestIdleCallback` (`{ timeout: 1500 }`), Fallback `setTimeout(fn, 0)`; der Case gibt vor dem ersten Slice zurück; jedes gepostete Objekt übersteht `structuredClone` (Test) — ein Post-Fehler (DataCloneError) wird abgefangen und als `GAME_INVENTORY_DATA {reqId, err}` gemeldet"
  artifacts:
    - path: "loader.js"
      provides: "Sektion `── Gamecode-Inventar (SCAN-01..07)` mit `GI_SCHEMA`, `GI_BATCH`, `GI_STEPS`, `GI_PREFIXES`, `GI_ASSET_KEYS`, `GI_GROUP_KEYS`, `giReadData`, `giBounded`, `giNext`, `giChunked`, `giDescribeApi`, `buildGameInventory(reqId, post)`, `window.__BCK_buildGameInventory`; Case `GET_GAME_INVENTORY` im bestehenden `switch`"
      contains: "function buildGameInventory(reqId, post)"
    - path: "tests/game-inventory-enumerator.test.js"
      provides: "describe-Blöcke „GET_GAME_INVENTORY“, „Global-Klassifizierung“, „Asset-Serialisierung“, „bcModSdk“, „Fallback-Probes“, „Chat-Hook“, „SCAN-07“ gegen `makeLoaderSandbox`"
      contains: "structuredClone"
  key_links:
    - from: "loader.js `switch (ev.data.type)` Case `GET_GAME_INVENTORY`"
      to: "`buildGameInventory(ev.data.reqId, post)`"
      via: "`post = msg => src.postMessage({ app: APP, ...msg }, _giTarget)` mit `_giTarget = ev.origin` (bereits gegen die Tool-Origin geprüft)"
      pattern: "case 'GET_GAME_INVENTORY':"
    - from: "`buildGameInventory` Schritte 1-6"
      to: "`post({ type: 'GAME_INVENTORY_PROGRESS', reqId, step, total, label })` / `post({ type: 'GAME_INVENTORY_DATA', reqId, snapshot })`"
      via: "`giChunked` (Slices à 500) → `giNext` (requestIdleCallback | setTimeout 0)"
      pattern: "GAME_INVENTORY_DATA"
    - from: "Jeder Wertzugriff des Enumerators"
      to: "`giReadData(obj, name)` → `{ kind: 'missing'|'getter'|'data', value }`"
      via: "`Object.getOwnPropertyDescriptor`; `kind === 'getter'` wird nie ausgewertet"
      pattern: "function giReadData\\(obj, name\\)"
    - from: "tests/game-inventory-enumerator.test.js"
      to: "tests/helpers/loaderSandbox.js `makeLoaderSandbox` (Plan 05-01)"
      via: "`send({ type: 'GET_GAME_INVENTORY', reqId })`, `waitFor`/`runUntil`, `posts`, `hits`"
      pattern: "makeLoaderSandbox"
  prohibitions:
    - requirement_id: SCAN-07
      category: safety
      verification: automated
      statement: "Im Enumerator-Bereich von loader.js (zwischen den Sektionsmarkern `── Gamecode-Inventar` und `── PostMessage Listener`): keine Quelltext-Konvertierung von Funktionen (Pitfall 3), kein Index-Zugriff auf das Fensterobjekt, kein dynamisches Auswerten von Code; Fixture-Zähler `hits.*` bleiben 0 nach dem Scan; ein werfender Getter und eine werfende `Dynamic*`-Funktion bringen den Scan nicht zu Fall"
    - requirement_id: SCAN-01
      category: safety
      verification: automated
      statement: "Loader-Änderung minimal: genau ein neuer Case, ein neuer Sektionsblock, eine Exposure-Zeile; Anzahl der `ALLOWED_ORIGIN`-Vorkommen bleibt 34 (statischer Test unverändert); kein Wildcard-Ziel; `node --check loader.js` grün; kein anderer Case wird verändert (Diff außerhalb der Sektion und des neuen Case ist leer)"
    - requirement_id: SCAN-05
      category: safety
      verification: automated
      statement: "`getPatchingInfo()`-Einträge werden ausschließlich auf `name`/`originalHash`/`hookedByMods`/`patchedByMods` reduziert (Test: keine weiteren Keys, `hits.patching === 0`); Mod-Objekte werden nie gespreadet oder per JSON-Roundtrip kopiert"
---

<objective>
Der Enumerator: ein einziger, in sich geschlossener Funktionsblock `buildGameInventory(reqId, post)` in `loader.js`, der das laufende Spiel read-only, deskriptorbasiert und gechunkt inventarisiert (Globals mit Typ/Arität, `Inventory*`-Bündelung, Asset-Katalog per Allowlist ohne Zirkel, Asset-Gruppen, `bcModSdk`-Mods und Hook-Registry ohne Funktionswerte, fünf Mod-Probes, Chat-Hook-Probe) und über `GAME_INVENTORY_PROGRESS`/`GAME_INVENTORY_DATA` an das Tool-Fenster meldet — plus der eine neue Case `GET_GAME_INVENTORY` im bestehenden Listener. RED → GREEN gegen die Loader-Sandbox aus Plan 05-01.

Purpose: Erfolgskriterien 2, 3 und 4 — vollständiges Inventar inklusive Mods, ohne dass der Spiel-Tab einfriert, ohne jeden Funktionsaufruf auf entdeckte Werte, ohne `DataCloneError` (RESEARCH Pitfall 1-6, SCAN-13-Befunde als Ground Truth).
Output: `tests/game-inventory-enumerator.test.js` (neu), `loader.js` (Sektion + Case + Exposure).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gamecode-inventar/05-RESEARCH.md
@.planning/phases/05-gamecode-inventar/05-CONSOLE-FINDINGS.md
@.planning/phases/05-gamecode-inventar/05-CONSOLE-RESULT.json
@.planning/phases/05-gamecode-inventar/05-VALIDATION.md
@.planning/phases/05-gamecode-inventar/05-01-SUMMARY.md
@tests/helpers/loaderSandbox.js
@tests/loader-origin.test.js

Planer-Probe (2026-09-15, HEAD `f3973f6`; per grep ankern) — verbindliche Befunde:
1. **Listener-Seam:** `window.__BCK_LISTENER_FN__ = function (ev) { … }` (loader.js ~836); Reihenfolge im Kopf: `app`-Check → `if (ev.origin !== ALLOWED_ORIGIN) return` → `const src = ev.source` → Source-Pinning (`window.__BCK_popupRef`) → `switch (ev.data.type)`. Jeder bestehende Case antwortet synchron vor `break` (z. B. `GET_POS` echo mit `reqId: ev.data.reqId`, loader.js ~904-912) — der neue Case ist der einzige asynchrone (RESEARCH Pattern 5, im Code kommentieren). Letzter Case `LOAD_CRAFT_CACHE` endet ~1361, danach `}` des switch und `};` der Funktion.
2. **IIFE-Scope:** loader.js ist `(function () { 'use strict'; … })();` — Funktionsdeklarationen darin sind KEINE Globals. Test-Seam per Exposure `window.__BCK_buildGameInventory = buildGameInventory;` (Muster `window._BCU_serializeChar = function (C) {…}`, loader.js:799). Sektion direkt VOR `// ── PostMessage Listener ───` (loader.js ~828) einfügen; Funktionsdeklarationen sind gehoisted, der Case darf `buildGameInventory` also unabhängig von der Position referenzieren.
3. **Statischer Zähler 34:** `tests/loader-origin.test.js` zählt `(L.match(/ALLOWED_ORIGIN/g) || []).length === 34` und bleibt unangetastet (Orchestrator-Entscheidung 4). Der neue Case antwortet deshalb an `ev.origin`: die Zeile `if (ev.origin !== ALLOWED_ORIGIN) { … return; }` steht wenige Zeilen davor im selben Handler, `ev.origin` ist an der Case-Stelle also beweisbar identisch mit der Tool-Origin. Der Kommentar im Case darf den Bezeichner der Konstante NICHT nennen (er würde mitgezählt). Der Wildcard-Audit (`/,\s*['"]\*['"]\)/`, tests/injected-code-origin.test.js) ist davon unberührt. Der Sandbox-Test beweist das Ziel dynamisch: `posts[i].origin === LOADER_TOOL_ORIGIN`.
4. **Chunking-Muster im Repo:** `_BCU_leerlauf(fn)` (loader.js:1441-1444): `requestIdleCallback(fn, { timeout: 1500 })` falls Funktion, sonst `setTimeout(fn, 16)`. Der Enumerator kopiert das Muster als eigenen Helfer `giNext` (Fallback laut Orchestrator-Entscheidung 2 mit `setTimeout(fn, 0)`), damit die Sektion in sich geschlossen bleibt und `_BCU_leerlauf` unverändert bleibt.
5. **Sandbox-Realm:** In der Sandbox ist `window` das Host-Sandbox-Objekt; `Object.getOwnPropertyNames(window)` liefert dessen eigene Properties inkl. `console`, `setTimeout`, `document`, `_listeners`, `window` (Selbstreferenz — nur Namen/Typen werden erfasst, nie rekursiv). Getter-Deskriptoren sind sichtbar; Host-`structuredClone` wirft bei Funktionswerten `DataCloneError`, klont aber Zirkel klaglos — deshalb zusätzlich `JSON.stringify(snapshot)` als Zirkel-Beweis.
6. **Sandbox-Timer:** `makeLoaderSandbox()` ohne Optionen nutzt den echten `setTimeout` und KEIN `requestIdleCallback` → Fallback-Pfad; ein Scan mit ~2.000 synthetischen Properties + 13 Assets läuft in wenigen Ticks (< 200 ms) — `waitFor` genügt. `manualTimers: true` liefert eine deterministische Queue (`timerQueue`, `counts.timeout`/`counts.ric`, `runUntil`) für die Chunking-Beweise; ohne `bcModSdk` hängt der `installBCXFilter`-Retry in der Queue (re-enqueued sich) — `runUntil` mit Prädikat verwenden.
7. **SCAN-13-Ground-Truth** (05-CONSOLE-RESULT.json): Präfixe `GI_PREFIXES = ['Character','Lock','Common','Player','Asset','Reputation','Skill','Pose','Dialog','Inventory','Item','Server','Assets','Online','Wardrobe','Chat']` (Überlappung `Asset`/`Assets` bewusst wie im Konsolen-Snippet); 100 Asset-Keys → `GI_ASSET_KEYS` (88, siehe Task 2); `getPatchingInfo()` liefert eine `Map` mit `valueKeys ["name","original","originalHash","sdkEntrypoint","currentEntrypoint","hookedByMods","patchedByMods"]`; `bcModSdk.version` `'1.2.0'`; Mod-Globals wie in `makeModGlobals` (Plan 05-01) modelliert.
8. **Reversibilität:** Nachrichtenvertrag und Snapshot-Form liegen vollständig in diesem Repo (Loader + Tool); `inventory.schema = 1` ist die Versionierungs-Naht für spätere Formänderungen; der Nutzer muss nach dem Deploy das Bookmarklet neu ausführen (kein One-way-Door, im SUMMARY und im Human-Check von Plan 05-03 vermerken). Rating `reversible`.
9. **Baseline nach Plan 05-01:** `npm test` = 21 Dateien grün.
</context>

<tasks>

<!-- Comment-Text-Discipline-Allowlist (#429): Gate-Literale der Region-Prüfung; im Plan nur hier und in <verify> -->
<!-- planner-discipline-allow: toString( -->
<!-- planner-discipline-allow: window[ -->
<!-- planner-discipline-allow: eval( -->
<!-- planner-discipline-allow: new Function -->
<!-- planner-discipline-allow: JSON.parse( -->

<task type="auto">
  <name>Task 1: Enumerator-Tests schreiben und RED bestätigen — `tests/game-inventory-enumerator.test.js` (SCAN-02..07, SCAN-01 Loader-Hälfte)</name>
  <precondition>Plan 05-01 gelandet: `tests/helpers/loaderSandbox.js` exportiert `makeLoaderSandbox` und `npx vitest run tests/loader-sandbox.test.js` ist grün</precondition>
  <files>tests/game-inventory-enumerator.test.js</files>
  <read_first>tests/helpers/loaderSandbox.js (API: `makeLoaderSandbox`, `send`, `posts`, `hits`, `timerQueue`, `counts`, `runUntil`, `waitFor`, `assets`, `LOADER_TOOL_ORIGIN`); tests/loader-sandbox.test.js (Aufrufmuster); .planning/phases/05-gamecode-inventar/05-RESEARCH.md „Pattern 1-5“, „Common Pitfalls 1-6“, „Validation Architecture“; 05-CONSOLE-RESULT.json (`assetKeys`, `byPrefix`, `patchingSample`); Task 2 dieses Plans (Snapshot-Vertrag — Feldnamen exakt übernehmen)</read_first>
  <action>
    Neue Datei (ESM, Imports `describe, it, expect, beforeAll` aus vitest; `makeLoaderSandbox, LOADER_TOOL_ORIGIN` aus `./helpers/loaderSandbox.js`; `fs`/`path`/`REPO_ROOT` für den statischen Block). Kopfkommentar (Deutsch): SCAN-02..07 gegen die Loader-Sandbox; Snapshot-Vertrag laut Plan 05-02; „Zähler `hits` müssen 0 bleiben“ als Kernbeweis für SCAN-07. Helfer: `async function scan(opts = {}, reqId = 'gi_test_1')` → `const sb = makeLoaderSandbox(opts); sb.send({ type: 'GET_GAME_INVENTORY', reqId }); await sb.waitFor(() => sb.posts.some(p => p.msg.type === 'GAME_INVENTORY_DATA' && p.msg.reqId === reqId)); const data = sb.posts.find(…).msg; return { ...sb, data, snapshot: data.snapshot };` und `progress(sb, reqId)` → alle `GAME_INVENTORY_PROGRESS`-Posts mit dieser reqId.
    **describe „GET_GAME_INVENTORY (SCAN-01 Loader-Hälfte)“** (eine gemeinsame Sandbox in `beforeAll`: `scan({ syntheticGlobals: 1200, inventoryFunctions: 600 })`):
    1. „Case kehrt sofort zurück, DATA kommt später mit derselben reqId, snapshot ist Objekt“: nach `send` (vor `waitFor`) existiert noch keine DATA-Post → in einem eigenen Fall mit frischer Sandbox prüfen: `sb.send(...)`; `expect(sb.posts.some(p => p.msg.type === 'GAME_INVENTORY_DATA')).toBe(false)`; dann `await sb.waitFor(...)`; `data.reqId === 'gi_test_1'`; `typeof data.snapshot === 'object'`; `data.err` undefined.
    2. „jede Post trägt app, reqId und geht an die Tool-Origin“: alle Posts mit Typ `GAME_INVENTORY_*` haben `msg.app === 'BCKonfigurator'`, `msg.reqId === reqId`, `origin === LOADER_TOOL_ORIGIN`.
    3. „PROGRESS: mindestens 6 Meldungen, total 6, step monoton nicht fallend von 1 bis 6, label nicht leer; DATA ist die letzte Post dieser reqId“.
    4. „zwei gleichzeitige Scans mit eigenen reqIds laufen beide zu Ende“: frische Sandbox; `send(reqId 'a')`, `send(reqId 'b')`; `waitFor` beide DATA; beide `snapshot.globals.total` gleich und > 0.
    5. „fremde Origin/Quelle: kein Scan“: frische Sandbox; `send({ type: 'GET_GAME_INVENTORY', reqId: 'x' }, { origin: 'https://evil.test' })`; kurz warten (`await new Promise(r => setTimeout(r, 50))`); keine Post.
    **describe „Global-Klassifizierung (SCAN-02)“** (Sandbox aus `beforeAll` oben wiederverwenden oder eigene mit `syntheticGlobals: 1200, inventoryFunctions: 600`):
    6. `snapshot.globals.total === Object.getOwnPropertyNames(ctx).length`; `globals.getters` enthält `'giThrowingGetter'` und `'giCountingGetter'`; `globals.functions` enthält `{ name: 'giFn3', arity: 3 }` (per `toContainEqual`) und `{ name: 'InventoryGet', arity: 2 }`; `globals.values` enthält `{ name: 'giData1', type: 'number' }`, `{ name: 'giStr2', type: 'string' }`, `{ name: 'GameVersion', type: 'string' }`; kein Eintrag in `functions`/`values` hat einen Namen aus `getters`.
    7. „Inventory-Bündelung“: `globals.inventory.groups` enthält Eintrag mit `prefix: 'InventoryItemItemTorso2'` und `group: 'ItemTorso2'` und `count > 0` und `sample.length <= 5`; Eintrag `InventoryItemItemTorso` zählt NUR Namen, die nicht zu `ItemTorso2` gehören (Längster-Präfix: jeder `sample`-Name beginnt mit `InventoryItemItemTorso` und nicht mit `InventoryItemItemTorso2`); `globals.inventory.other.count >= 1` (`InventoryItemHasEffect`) und `other.names` enthält `'InventoryItemHasEffect'`; Summe `groups[].count + other.count` = Anzahl eigener Namen, die mit `InventoryItem` beginnen; kein `functions`-Eintrag beginnt mit `InventoryItem`.
    8. „byPrefix“: `globals.byPrefix.Inventory` = Anzahl eigener Namen, die mit `Inventory` beginnen; Objekt hat genau die 16 Schlüssel `Character, Lock, Common, Player, Asset, Reputation, Skill, Pose, Dialog, Inventory, Item, Server, Assets, Online, Wardrobe, Chat`.
    9. „Funktionswerte tauchen nirgends auf“: `expect(() => structuredClone(snapshot.globals)).not.toThrow()`.
    **describe „Asset-Serialisierung (SCAN-03)“**:
    10. `snapshot.assets.count === 13`, `groupCount === 4`; `items.length === 13`; das erste Item hat `Group === 'ItemArms'` (String), `ParentItem === null`, `Layer` `toEqual({ count: 2, names: ['L1', 'L2'] })`, `Effect` `toEqual(['Block'])`, `DefaultColor` `toEqual(['Default', '#123456'])`, `ExpressionTrigger[0]` `toEqual({ Group: 'Eyes', Name: 'Closed', Timer: 5 })`, `OverrideHeight` `toEqual({ Height: 1, Priority: 2 })`; zweites Item `ParentItem === 'ItemArmsAsset0'`; Waise (`Name === 'OrphanAsset'`) hat `Group === null` und `Layer` `{ count: 0, names: [] }`.
    11. „keine Dynamic*-Keys, kein Gruppenobjekt, kein Zirkel“: kein Item hat einen Key, der mit `Dynamic` beginnt; kein Item hat `typeof item.Group === 'object'`; `expect(() => JSON.stringify(snapshot.assets)).not.toThrow()`; `expect(() => structuredClone(snapshot.assets)).not.toThrow()`; `hits.dynamic === 0`.
    12. „Gruppen“: `assets.groups` enthält `{ Name: 'ItemTorso2', assetCount: 3 }` als Teilobjekt (`expect.objectContaining`); keine Gruppe hat Key `Asset`.
    13. „Asset[] fehlt → assets.error, Scan vollständig“: `scan({ withAssets: false })` → `typeof snapshot.assets.error === 'string'`; `snapshot.globals.total > 0`; `snapshot.mods.length === 2`; DATA vorhanden.
    **describe „bcModSdk (SCAN-05)“**:
    14. `snapshot.mods` `toEqual` der zwei Mock-Mods (vier Felder); `snapshot.modSdk` `toMatchObject({ available: true, version: '1.2.0', modCount: 2, patchingCount: 3 })`; `modSdk.patching` enthält `{ name: 'CommonDrawAppearanceBuild', originalHash: 'B02DDFE3', hookedByMods: ['LSCG', 'BCT'], patchedByMods: [] }`; JEDER Patching-Eintrag hat exakt die Keys `['hookedByMods', 'name', 'originalHash', 'patchedByMods']` (sortiert); `hits.patching === 0`.
    15. „ohne bcModSdk“: `scan({ withBcModSdk: false, manualTimers: true })` — hier statt `waitFor`: `sb.send(...)`, dann `sb.runUntil(() => sb.posts.some(DATA))`; `snapshot.mods` `toEqual([])`; `snapshot.modSdk.available === false`; `snapshot.probes.bcx.present === true` (Probes laufen unabhängig weiter).
    **describe „Fallback-Probes (SCAN-06)“**:
    16. `probes.wce` `toMatchObject({ present: true, version: '6.3.19' })` und `functions` enthält `'fbcChatNotify'`, `'wceServerAppearance'` (nur Namen).
    17. `probes.bcx` `toMatchObject({ present: true, loaded: true, version: '1.1.19-test' })`; `api` enthält `{ name: 'getRuleState', kind: 'function' }` und `{ name: 'secret', kind: 'getter' }`; `hits.getter === 0` (Accessor nie gelesen).
    18. `probes.mbs` `toMatchObject({ present: true, version: '1.10.25', apiVersion: { major: 1, minor: 5 } })`; `api` enthält `{ name: 'getDebug', kind: 'function' }`.
    19. `probes.lscg` `toMatchObject({ present: true, loaded: true })`; `api` enthält `{ name: 'getModule', kind: 'function' }`; `screenFunctions.count >= 2` und `sample` enthält `'LSCG_SuggestionMiniGameRun'`.
    20. `probes.themed` `toMatchObject({ present: true, loaded: true })`; `screenFunctionCount >= 2`; `sample` enthält `'Themed_mainmenuLoad'`.
    21. „ohne Mod-Globals: alle fünf present false, sweep leer“: `scan({ withModGlobals: false })` → für jede der fünf Probes `present === false`; `probes.sweep` `toEqual([])`.
    22. `hits.fn === 0` nach dem Standard-Scan (keine Mod-Funktion wurde aufgerufen).
    **describe „Chat-Hook (SCAN-04)“**:
    23. `chatHooks.ChatRoomRegisterMessageHandler` `toEqual({ exists: true, arity: 1, kind: 'data' })`; `chatHooks.registry` `toEqual({ introspectable: false, checked: ['ChatRoomMessageHandlers'] })`; `chatHooks.hookedChatFunctions` enthält `{ name: 'ChatRoomMessage', hookedByMods: ['BCX'] }`.
    24. „Registry vorhanden → nur Description/Priority, Callback nie aufgerufen“: `scan({ extraGlobals: { ChatRoomMessageHandlers: [{ Description: 'Test', Priority: 100, Callback() { throw new Error('Callback aufgerufen'); } }] } })` → `registry` `toEqual({ introspectable: true, source: 'ChatRoomMessageHandlers', count: 1, handlers: [{ Description: 'Test', Priority: 100 }] })`.
    **describe „SCAN-07: read-only, gechunkt, klonbar“**:
    25. „werfender Getter und werfende Dynamic*-Funktion bringen den Scan nicht zu Fall; alle Zähler 0“: Standard-Scan → `hits` `toEqual({ getter: 0, fn: 0, dynamic: 0, patching: 0 })`; `snapshot.errors` `toEqual([])`.
    26. „structuredClone und JSON.stringify des gesamten Snapshots werfen nicht; alle Posts klonbar“: `expect(() => structuredClone(snapshot)).not.toThrow()`; `expect(() => JSON.stringify(snapshot)).not.toThrow()`; für jede Post `expect(() => structuredClone(p.msg)).not.toThrow()`.
    27. „Chunking mit setTimeout-Fallback: Case kehrt zurück, bevor die Arbeit erledigt ist; ≥ ceil(N/500) Ticks“: `sb = makeLoaderSandbox({ syntheticGlobals: 20000, manualTimers: true })`; `sb.send(...)`; `n0 = Object.getOwnPropertyNames(sb.ctx).length` (NACH `send` messen — der Listener pinnt synchron `__BCK_popupRef` als neue Property, der erste Slice läuft erst im nächsten Tick); `expect(sb.posts.some(DATA)).toBe(false)`; `expect(sb.timerQueue.length).toBeGreaterThanOrEqual(1)`; `ticks = sb.runUntil(() => sb.posts.some(DATA))`; `expect(ticks).toBeGreaterThanOrEqual(Math.ceil(n0 / 500))`; `sb.counts.timeout >= Math.ceil(n0 / 500)`; `sb.counts.ric === 0`; DATA vorhanden, `snapshot.globals.total === n0`.
    28. „requestIdleCallback wird bevorzugt, wenn vorhanden“: `makeLoaderSandbox({ syntheticGlobals: 3000, manualTimers: true, requestIdleCallback: true })`; `send`; `runUntil(DATA)`; `counts.ric >= Math.ceil(3000 / 500)`; `counts.timeout === 0`.
    29. „Post-Fehler (DataCloneError) wird als err gemeldet, nicht verschluckt“: frische Sandbox; `sb.ctx.__BCK_buildGameInventory('gi_err', (msg) => { if (msg.type === 'GAME_INVENTORY_DATA' && msg.snapshot) { const e = new Error('could not be cloned'); e.name = 'DataCloneError'; throw e; } sb.posts.push({ msg, origin: 'direct' }); })`; `await sb.waitFor(() => sb.posts.some(p => p.msg.type === 'GAME_INVENTORY_DATA' && p.msg.err))`; `err` enthält `'DataCloneError'`.
    30. „gameVersion und Metadaten“: `snapshot.schema === 1`; `snapshot.gameVersion === 'R131'`; `typeof snapshot.ts === 'number'`; `typeof snapshot.durationMs === 'number'`.
    **describe „statisch: Loader-Änderung minimal und read-only“** (liest loader.js als Text; `region = zwischen den Zeilen mit '── Gamecode-Inventar' und '── PostMessage Listener'`):
    31. `count(L, "case 'GET_GAME_INVENTORY':") === 1`; `count(L, 'function buildGameInventory(reqId, post)') === 1`; `count(L, 'window.__BCK_buildGameInventory = buildGameInventory;') === 1`; `(L.match(/ALLOWED_ORIGIN/g) || []).length === 34`; Region enthält 0× `toString(`, 0× `window[`, 0× `eval(`, 0× `new Function`, 0× `JSON.parse(`, mindestens 1× `Object.getOwnPropertyDescriptor(` und 1× `Object.getOwnPropertyNames(`; Region enthält `requestIdleCallback` und `setTimeout(`; der Case-Block (von `case 'GET_GAME_INVENTORY':` bis zum nächsten `break;`) enthält `ev.origin` und `buildGameInventory(`.
    Datei committen (nur dieser Pfad): `git add tests/game-inventory-enumerator.test.js` → `test(05-02): Enumerator-Tests SCAN-02..07 gegen Loader-Sandbox (RED)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/game-inventory-enumerator.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "GAME_INVENTORY_DATA|__BCK_buildGameInventory|waitFor|buildGameInventory" "$OUT" && grep -Eq "Tests +[0-9]+ failed|Test Files +[0-9]+ failed" "$OUT" && git diff --quiet HEAD -- loader.js && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "tests/game-inventory-enumerator.test.js " && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Exit 0 (Loader antwortet bereits oder Tests prüfen nichts), die Ausgabe nennt keinen der Bezeichner und keine `failed`-Zeile, loader.js wurde bereits verändert, oder der Commit enthält andere Pfade</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "it(" tests/game-inventory-enumerator.test.js` >= 30; `grep -c 'structuredClone' tests/game-inventory-enumerator.test.js` >= 4; `grep -c 'hits' tests/game-inventory-enumerator.test.js` >= 6; `grep -c 'runUntil' tests/game-inventory-enumerator.test.js` >= 3; `grep -c 'LOADER_TOOL_ORIGIN' tests/game-inventory-enumerator.test.js` >= 1
    - `grep -c 'animereviewer1-sketch' tests/game-inventory-enumerator.test.js` = 0 (Origin nur über die Ableitung aus loaderSandbox.js); kein `.only(`
    - `npx vitest run tests/loader-sandbox.test.js tests/loader-origin.test.js` weiterhin grün
  </acceptance_criteria>
  <done>31 Fälle über sieben describe-Blöcke; Lauf nachweislich rot; roter Test committet; loader.js unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `loader.js` — Sektion `buildGameInventory` (deskriptorbasiert, gechunkt, Allowlists, Probes) + Case `GET_GAME_INVENTORY` — GREEN (SCAN-02..07, SCAN-01 Loader-Hälfte)</name>
  <files>loader.js</files>
  <read_first>loader.js Zeilen 1-32 (`BCK`-Logger, `APP`), 795-826 (`_BCU_serializeChar`-Exposure), 828-856 (Listener-Kopf und `switch`), 1343-1364 (letzter Case, Ende des switch/der Funktion), 1437-1444 (`_BCU_leerlauf`); tests/game-inventory-enumerator.test.js (Task 1 — jede Erwartung ist Vertrag); .planning/phases/05-gamecode-inventar/05-RESEARCH.md „Pattern 1-5“, „Code Examples“; 05-CONSOLE-RESULT.json (`assetKeys`, `byPrefix`)</read_first>
  <behavior>
    - `GET_GAME_INVENTORY` → sofortige Rückkehr; später n× PROGRESS + 1× DATA mit reqId an `ev.origin`
    - Getter nie gelesen, Funktionen nie aufgerufen (außer den zwei SDK-Lesefunktionen), `hits.*` = 0
    - Allowlists/`giBounded` → kein Funktionswert, kein Zirkel; `structuredClone`/`JSON.stringify` des Snapshots werfen nicht
    - Slices à 500 über `requestIdleCallback` (bevorzugt) / `setTimeout(fn, 0)`; ≥ ceil(N/500) Ticks
    - Fehlende Quellen (`Asset`, `bcModSdk`, Mod-Globals, Registry) degradieren je Abschnitt, Scan endet immer mit DATA
  </behavior>
  <action>
    **A. Neue Sektion** direkt VOR der Zeile `// ── PostMessage Listener ───…` einfügen, Kopf: `// ── Gamecode-Inventar (SCAN-01..07): read-only, deskriptorbasiert, gechunkt ──` plus 6-10 Zeilen deutscher Erläuterung (Zweck; Regel „Werte nur über giReadData, Accessor = Name ohne Lesen; einzige Funktionsaufrufe: bcModSdk.getModsInfo()/getPatchingInfo()“; Chunking; Snapshot-Vertrag `schema 1`). Inhalt — ausschließlich Funktionsdeklarationen und `const`-Konstanten im IIFE-Scope, keine Änderung an bestehenden Funktionen:
    - `const GI_SCHEMA = 1; const GI_BATCH = 500; const GI_SAMPLE = 5; const GI_OTHER_MAX = 50; const GI_LIST_MAX = 200; const GI_STR_MAX = 500; const GI_DEPTH_MAX = 2;`
    - `const GI_STEPS = ['Globals', 'Assets', 'Asset-Gruppen', 'ModSDK', 'Mod-Probes', 'Chat-Hooks'];` (total = 6)
    - `const GI_PREFIXES = ['Character','Lock','Common','Player','Asset','Reputation','Skill','Pose','Dialog','Inventory','Item','Server','Assets','Online','Wardrobe','Chat'];`
    - `const GI_ASSET_KEYS = ['Name','Description','Enable','Visible','DrawOffset','NotVisibleOnScreen','Wear','Activity','ActivityAudio','AllowActivity','AllowActivityOn','ActivityExpression','BuyGroup','InventoryID','Effect','Bonus','Block','Expose','Hide','HideItem','HideItemExclude','HideItemAttribute','Require','SetPose','AllowActivePose','Value','NeverSell','Difficulty','SelfBondage','SelfUnlock','ExclusiveUnlock','Random','RemoveAtLogin','WearTime','RemoveTime','RemoveTimer','MaxTimer','HeightModifier','ZoomModifier','Prerequisite','Extended','AlwaysExtend','AlwaysInteract','AllowLock','LayerVisibility','IsLock','PickDifficulty','OwnerOnly','LoverOnly','FamilyOnly','ExpressionTrigger','RemoveItemOnRemove','AllowEffect','AllowBlock','AllowTighten','AllowHide','AllowHideItem','DefaultColor','EditOpacity','Audio','Category','Fetish','ArousalZone','IsRestraint','BodyCosplay','OverrideBlinking','DialogSortOverride','AllowRemoveExclusive','InheritColor','CreateLayerTypes','AllowLockType','AvailableLocations','OverrideHeight','DrawLocks','AllowExpression','MirrorExpression','FixedPosition','ColorableLayerCount','CustomBlindBackground','Attribute','PreviewIcons','Tint','AllowTint','DefaultTint','Gender','CraftGroup','ExpressionPrerequisite','AllowColorize'];` (88 = die 100 SCAN-13-Keys ohne `Group`, `ParentItem`, `Layer`, `Dynamic*`; Kommentar mit Herkunft).
    - `const GI_GROUP_KEYS = ['Name','Description','Category','Family','IsDefault','IsRestraint','AllowNone','AllowColorize','AllowCustomize','Random','Color','ParentGroup','Clothing','Underwear','BodyCosplay','Hide','Block','Zone','SetPose','AllowPose','AllowExpression','Effect','MirrorGroup','RemoveItemOnRemove','DrawingPriority','DrawingLeft','DrawingTop','DrawingFullAlpha','DrawingBlink','InheritColor','FreezeActivePose','PreviewZone','MirrorActivitiesFrom','HasPreviewImages','IsAppearance','IsItem','IsScript'];` — Kommentar: [ASSUMED] Feldnamen aus BC-Quellcode-Kenntnis, nicht per SCAN-13 verifiziert; unbekannte Keys werden übersprungen, Funktionswerte fallen durch `giBounded` weg (kein Fehlerpfad).
    - `function giReadData(obj, name)` → `{ kind: 'missing' }` wenn `obj` kein Objekt/keine Funktion oder Deskriptor fehlt; `{ kind: 'getter' }` wenn Deskriptor `get` oder `set` hat; sonst `{ kind: 'data', value: d.value }`. Deskriptor-Aufruf in `try/catch` (Proxy-Traps) → `{ kind: 'missing' }`.
    - `function giScalar(v)` → `typeof v === 'string' ? v.slice(0, GI_STR_MAX) : (v == null ? null : (typeof v === 'number' || typeof v === 'boolean') ? v : String(v))` — für Metafelder (Name/Version/Hash).
    - `function giBounded(v, depth)` → Primitive: string (gekürzt), number, boolean, null → wie sind; `undefined`/function/symbol → `undefined` (wird vom Aufrufer übersprungen); bigint → String; `depth > GI_DEPTH_MAX` → `'[…]'`; Array → `v.slice(0, GI_LIST_MAX).map(x => giBounded(x, depth + 1))` mit `undefined`-Einträgen zu `null`; Objekt → für `Object.keys(v)` (max GI_LIST_MAX) `r = giReadData(v, k)`; nur `kind === 'data'` und Ergebnis `!== undefined` übernehmen.
    - `function giNext(fn)` → `if (typeof requestIdleCallback === 'function') requestIdleCallback(fn, { timeout: 1500 }); else setTimeout(fn, 0);` (Muster `_BCU_leerlauf`, Fallback 0 ms per Entscheidung 2).
    - `function giChunked(items, onItem, onSlice, onDone)` → Index `i`; `tick()` verarbeitet bis `min(i + GI_BATCH, items.length)` (jedes `onItem(item, index)` in `try/catch`, Fehler zählt der Aufrufer), ruft `onSlice(sliceIndex, sliceCount)`, dann `if (i < items.length) giNext(tick); else onDone();`. WICHTIG: Der ERSTE Slice läuft ebenfalls über `giNext(tick)` (nicht synchron), damit der Case vor jeder Arbeit zurückkehrt (Test 27: keine DATA/keine Arbeit vor dem ersten Tick; bei 0 Items sofort `onDone` über `giNext`).
    - `function giDescribeApi(obj)` → `Object.getOwnPropertyNames(obj)` (nicht `Object.keys` — Pitfall 5), max GI_LIST_MAX, je Name `{ name, kind }` mit `kind` = `'getter'` (Accessor) | `'function'` | `typeof value` (`null` → `'null'`) — nie lesen bei Accessor.
    - `function giMods(sdk)`/`giPatching(sdk)`: `getModsInfo` bzw. `getPatchingInfo` per `giReadData(sdk, …)` holen; nur wenn `kind === 'data'` und Funktion → AUFRUFEN (dokumentierte Ausnahme, SCAN-05); Ergebnis Array → je Eintrag `{ name, fullName, version, repository }` via `giScalar(giReadData(m, k).value)`; Map (oder Objekt mit `forEach`/`entries`) → je Eintrag `{ name: giScalar(name ?? key), originalHash: giScalar(...), hookedByMods: Array → giScalar je Element, sonst [], patchedByMods: analog }`; nichts anderes übernehmen.
    - `function buildGameInventory(reqId, post)`: `const t0 = Date.now(); const errors = []; const snapshot = { schema: GI_SCHEMA, gameVersion: null, ts: t0, durationMs: 0, globals: null, assets: null, modSdk: null, mods: [], probes: null, chatHooks: null, errors };` `function fail(step, err) { errors.push({ step, message: String(err && err.message || err) }); }` `function progress(step, sliceIndex, sliceCount) { safePost({ type: 'GAME_INVENTORY_PROGRESS', reqId, step, total: GI_STEPS.length, label: GI_STEPS[step - 1] + (sliceCount > 1 ? ' ' + sliceIndex + '/' + sliceCount : '') }); }` `let aborted = false; function safePost(msg) { if (aborted) return; try { post(msg); } catch (err) { aborted = true; try { post({ type: 'GAME_INVENTORY_DATA', reqId, err: String((err && err.name) || 'Error') + ': ' + String((err && err.message) || err) }); } catch (e2) {} } }` — nach `aborted` keine weiteren Posts.
      Schritt 1 (Globals): `gameVersion` = `giReadData(window, 'GameVersion')` → string oder null. `names = Object.getOwnPropertyNames(window)`; Gruppennamen aus `giReadData(window, 'AssetGroup')` (Array → `giReadData(g, 'Name')` strings), nach Länge absteigend sortiert; Akkumulatoren `getters`, `functions`, `values`, `byPrefix` (16 Schlüssel, 0), `buckets` (Map Gruppe → `{ prefix, group, count, sample }`), `other = { count: 0, names: [] }`. `giChunked(names, name => { for (p of GI_PREFIXES) if (name.startsWith(p)) byPrefix[p]++; const r = giReadData(window, name); if (r.kind === 'missing') return; if (r.kind === 'getter') { getters.push(name); return; } const v = r.value, t = typeof v; if (t === 'function') { const ar = giReadData(v, 'length'); const arity = ar.kind === 'data' && typeof ar.value === 'number' ? ar.value : null; if (name.startsWith('InventoryItem')) { const g = groupNames.find(gn => name.startsWith('InventoryItem' + gn)); if (g) { bucket(g).count++; if (sample.length < GI_SAMPLE) sample.push(name); } else { other.count++; if (other.names.length < GI_OTHER_MAX) other.names.push(name); } } else functions.push({ name, arity }); } else values.push({ name, type: v === null ? 'null' : t }); }, (si, sc) => progress(1, si, sc), () => { snapshot.globals = { total: names.length, getters, functions, values, byPrefix, inventory: { groups: Array.from(buckets.values()), other } }; step2(); })`. Fehler beim Vorbereiten (z. B. `getOwnPropertyNames` wirft) → `fail(1, err)`, `snapshot.globals = { error }`, weiter mit step2.
      Schritt 2 (Assets): `A = giReadData(window, 'Asset')`; nicht Array → `snapshot.assets = { error: 'Asset[] nicht verfügbar', count: 0, groupCount: 0, groups: [], items: [] }`, `progress(2, 1, 1)`, step3. Sonst `items = []`; `giChunked(A, a => { if (!a || typeof a !== 'object') return; const it = {}; const g = giReadData(a, 'Group'); it.Group = g.kind === 'data' && g.value && typeof g.value === 'object' ? giScalar(giReadData(g.value, 'Name').value) : (g.kind === 'data' && typeof g.value === 'string' ? g.value : null); const p = giReadData(a, 'ParentItem'); it.ParentItem = p.kind !== 'data' || p.value == null ? null : (typeof p.value === 'object' ? giScalar(giReadData(p.value, 'Name').value) : giScalar(p.value)); const L = giReadData(a, 'Layer'); it.Layer = L.kind === 'data' && Array.isArray(L.value) ? { count: L.value.length, names: L.value.slice(0, GI_OTHER_MAX).map(l => l && typeof l === 'object' ? giScalar(giReadData(l, 'Name').value) : null) } : { count: 0, names: [] }; for (k of GI_ASSET_KEYS) { const r = giReadData(a, k); if (r.kind !== 'data' || r.value === undefined) continue; const b = giBounded(r.value, 0); if (b !== undefined) it[k] = b; } items.push(it); }, (si, sc) => progress(2, si, sc), () => { snapshot.assets = { count: A.length, groupCount: 0, groups: [], items }; step3(); })`. Reihenfolge im Objekt: `Group`, `ParentItem`, `Layer` zuerst, dann Allowlist — `Name` kommt über die Allowlist.
      Schritt 3 (Asset-Gruppen): `G = giReadData(window, 'AssetGroup')`; Array → `groups = G.slice(0, 1000).map(g => { out = {}; for (k of GI_GROUP_KEYS) {…giBounded…}; const as = giReadData(g, 'Asset'); out.assetCount = as.kind === 'data' && Array.isArray(as.value) ? as.value.length : 0; return out; })`; `snapshot.assets.groups = groups; snapshot.assets.groupCount = groups.length;` (bei `assets.error` trotzdem setzen); sonst `groupsError`. `progress(3, 1, 1)`; step4 über `giNext`.
      Schritt 4 (ModSDK): `S = giReadData(window, 'bcModSdk')`; Objekt → `mods = giMods(S.value)`, `patching = giPatching(S.value)`, `snapshot.modSdk = { available: true, version: giScalar(giReadData(S.value, 'version').value), modCount: mods.length, patchingCount: patching.length, patching }`, `snapshot.mods = mods`; jeder Teil in `try/catch` → `fail(4, err)`, Teilresultat behalten; sonst `snapshot.modSdk = { available: false, version: null, modCount: 0, patchingCount: 0, patching: [] }`, `snapshot.mods = []`. `progress(4, 1, 1)`; step5 über `giNext`.
      Schritt 5 (Mod-Probes) — aus `snapshot.globals` (Namen) + Deskriptoren: `allNames = getters ∪ functions.map(name) ∪ values.map(name)`; `fnNames = functions.map(name)` (Bündel-Namen zählen hier nicht — die `fbc*`/`LSCG_*`/`Themed_*`-Funktionen beginnen nicht mit `InventoryItem`). `wce`: `v = giReadData(window, 'FBC_VERSION')`; `present = v.kind === 'data' && typeof v.value === 'string'`; `{ present, version: present ? v.value : null, functions: fnNames.filter(n => /^(fbc|wce)/i.test(n)).slice(0, GI_LIST_MAX) }`. `bcx`: `loaded = bool(giReadData(window, 'BCX_Loaded'))` (Wert nur wenn boolean, sonst null); `o = giReadData(window, 'bcx')`; `present = o.kind === 'data' && !!o.value && typeof o.value === 'object'`; `version` = `giReadData(o.value, 'version')` → data string → Wert | getter → `'getter'` | sonst null; `api = present ? giDescribeApi(o.value) : []`; `{ present, loaded, version, api }`. `mbs`: `o = giReadData(window, 'mbs')`; `version = giScalar(giReadData(o.value, 'MBS_VERSION').value)`; `av = giReadData(o.value, 'API_VERSION')` → Objekt → `{ major: giScalar(giReadData(av.value, 'major').value), minor: … }` sonst null; `{ present, version, apiVersion, api: giDescribeApi(o.value) }`. `lscg`: `loaded = LSCG_Loaded` (bool|null); `o = giReadData(window, 'LSCG')`; `screen = allNames.filter(n => /^LSCG_/.test(n))`; `{ present, loaded, api: present ? giDescribeApi(o.value) : [], screenFunctions: { count: screen.length, sample: screen.slice(0, GI_OTHER_MAX) } }`. `themed`: `loaded = ThemedLoaded` (bool|null); `th = allNames.filter(n => /^Themed_/.test(n))`; `{ present: loaded === true || th.length > 0, loaded, screenFunctionCount: th.length, sample: th.slice(0, GI_OTHER_MAX) }`. `sweep`: `allNames.filter(n => /^(WCE|FBC|LSCG|MBS|BCX|Themed)/i.test(n)).slice(0, GI_LIST_MAX)`. `snapshot.probes = { wce, bcx, mbs, lscg, themed, sweep }`; Fehler → `fail(5, err)`. `progress(5, 1, 1)`; step6 über `giNext`.
      Schritt 6 (Chat-Hooks): `h = giReadData(window, 'ChatRoomRegisterMessageHandler')`; `exists = h.kind === 'data' && typeof h.value === 'function'`; `arity` wie oben; `{ exists, arity, kind: h.kind }`. `registry`: `checked = ['ChatRoomMessageHandlers']`; für jeden Kandidaten `c = giReadData(window, name)`; erstes Array → `{ introspectable: true, source: name, count: arr.length, handlers: arr.slice(0, GI_LIST_MAX).map(x => ({ Description: giScalar(giReadData(x, 'Description').value), Priority: (giReadData(x, 'Priority').value ?? null) })) }` (Priority nur number, sonst null); keiner → `{ introspectable: false, checked }`. `hookedChatFunctions = (snapshot.modSdk.patching || []).filter(p => /^ChatRoom/.test(p.name)).map(p => ({ name: p.name, hookedByMods: p.hookedByMods }))`. `snapshot.chatHooks = { ChatRoomRegisterMessageHandler: …, registry, hookedChatFunctions }`. `progress(6, 1, 1)`; `snapshot.durationMs = Date.now() - t0`; `safePost({ type: 'GAME_INVENTORY_DATA', reqId, snapshot })`; `BCK.ok('[GameScan] Inventar gesendet: ' + names.length + ' Globals, ' + (snapshot.assets.count || 0) + ' Assets, ' + snapshot.mods.length + ' Mods, ' + snapshot.durationMs + ' ms')`.
      Jeder Schritt als eigene innere Funktion (`step2`…`step6`, `finish`), jeweils komplett in `try/catch` mit `fail(n, err)` und Weiterlauf; ein Fehler in einem Schritt darf die Kette nie abbrechen. Kein Aufruf von `giNext` ist synchron rekursiv.
    - Exposure als letzte Zeile der Sektion: `window.__BCK_buildGameInventory = buildGameInventory; // Test-Seam (Muster _BCU_serializeChar); im Spiel ungenutzt`.
    **B. Neuer Case** im `switch`, direkt VOR `case 'GET_CRAFT_CACHE':` (oder nach `LOAD_CRAFT_CACHE` — konsistent mit Kommentar), exakt in dieser Form:
    `case 'GET_GAME_INVENTORY': {` / Kommentarzeilen: „Einziger Case, der NICHT synchron antwortet: die Enumeration läuft gechunkt in Leerlaufpausen (SCAN-07) und postet PROGRESS/DATA später über denselben Absender. Ziel-Origin = ev.origin — oben in diesem Handler bereits gegen die Tool-Origin geprüft (identischer Wert); bewusst nicht erneut über die Konstante, damit der statische Zähler in tests/loader-origin.test.js unverändert bleibt.“ / `const _giTarget = ev.origin;` / `const _giReqId = String(ev.data.reqId ?? '');` / `BCK.info('GET_GAME_INVENTORY \u2192 Scan gestartet | reqId:', _giReqId);` / `buildGameInventory(_giReqId, function (msg) { src.postMessage({ app: APP, ...msg }, _giTarget); });` / `break;` / `}`. Keine weitere Zeile im Listener ändern.
    **C. Prüfen und committen:** `node --check loader.js`; `npx vitest run tests/game-inventory-enumerator.test.js tests/loader-sandbox.test.js tests/loader-origin.test.js tests/injected-code-origin.test.js`; `npm test`. Bei rotem Zähler-Test (35 statt 34): ein neuer Bezeichner-Treffer im Kommentar oder Code — entfernen, NICHT den Test anpassen. Commit (nur loader.js): `feat(05-02): Gamecode-Enumerator buildGameInventory + Case GET_GAME_INVENTORY (read-only, gechunkt) — SCAN-02..07`.
    Fürs SUMMARY: RED-/GREEN-Ausgaben, Snapshot-Vertrag (Feldliste), die zwei bewussten Ausnahmen (SDK-Aufrufe), Hinweis „Bookmarklet nach Deploy neu ausführen“, gemessene Tick-Zahlen aus Test 27/28, Region-Gates.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && node --check loader.js && npx vitest run tests/game-inventory-enumerator.test.js tests/loader-sandbox.test.js tests/loader-origin.test.js tests/injected-code-origin.test.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` — etwa weil der erste Slice synchron läuft (Test 27 sieht Arbeit vor dem ersten Tick), ein Getter gelesen wird (`hits.getter` > 0), `Dynamic*`/Patching-Funktionen aufgerufen oder übernommen werden (Test 11/14/25), `Group` als Objekt durchrutscht (JSON.stringify wirft), ein Post-Fehler verschluckt wird (Test 29), `bcx` per `Object.keys` beschrieben wird (Test 17 findet `getRuleState` nicht), oder der Zähler 34 durch einen zusätzlichen Konstantentreffer kippt</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && REGION=$(awk '/── Gamecode-Inventar/{f=1} /── PostMessage Listener/{f=0} f' loader.js) && test -n "$REGION" && test "$(printf '%s' "$REGION" | grep -c 'toString(')" = 0 && test "$(printf '%s' "$REGION" | grep -Fc 'window[')" = 0 && test "$(printf '%s' "$REGION" | grep -c 'eval(')" = 0 && test "$(printf '%s' "$REGION" | grep -c 'new Function')" = 0 && test "$(printf '%s' "$REGION" | grep -Fc 'JSON.parse(')" = 0 && test "$(printf '%s' "$REGION" | grep -c 'Object.getOwnPropertyDescriptor(')" -ge 1 && test "$(printf '%s' "$REGION" | grep -c 'Object.getOwnPropertyNames(')" -ge 2 && test "$(printf '%s' "$REGION" | grep -c 'requestIdleCallback')" -ge 2 && test "$(grep -c "case 'GET_GAME_INVENTORY':" loader.js)" = 1 && test "$(grep -c 'function buildGameInventory(reqId, post)' loader.js)" = 1 && test "$(grep -Fc 'window.__BCK_buildGameInventory = buildGameInventory;' loader.js)" = 1 && test "$(grep -o 'ALLOWED_ORIGIN' loader.js | wc -l | tr -d ' ')" = 34 && test -z "$(git diff --name-only f3973f6 HEAD -- tests/loader-origin.test.js)" && test "$(git diff f3973f6 HEAD -- loader.js | grep -c '^-[^-]')" = 0 && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "loader.js " && echo ENUMERATOR-OK</automated>
    <fails_when>`ENUMERATOR-OK` fehlt — Region fehlt oder enthält einen verbotenen Aufruf, Case/Funktion/Exposure fehlen oder doppeln, der Konstanten-Zähler ist nicht 34, `tests/loader-origin.test.js` wurde angefasst, das Diff zu `f3973f6` enthält GELÖSCHTE Zeilen in loader.js (Änderung war nicht rein additiv), oder der Commit enthält andere Pfade als loader.js</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/game-inventory-enumerator.test.js` → 31 passed; `npm test` → 22 Testdateien grün, 2 expected fail unverändert
    - `grep -c 'GI_ASSET_KEYS' loader.js` >= 2; `grep -c "'AllowColorize'" loader.js` >= 1; `grep -c 'GI_STEPS' loader.js` >= 2; `grep -c 'GAME_INVENTORY_PROGRESS' loader.js` >= 1; `grep -c 'GAME_INVENTORY_DATA' loader.js` >= 2 (Erfolg + Fehlerpfad)
    - Diff zu `f3973f6` in loader.js: nur Einfügungen (0 gelöschte Zeilen), zwei zusammenhängende Blöcke (Sektion + Case)
    - Zwei Commits dieses Plans: `test(05-02)` → `feat(05-02)`
  </acceptance_criteria>
  <done>`GET_GAME_INVENTORY` liefert über PROGRESS/DATA ein klonbares, zirkelfreies Inventar (Globals, Assets, Gruppen, ModSDK, Probes, Chat-Hooks) ohne Getter-Lesen und ohne Funktionsaufrufe, gechunkt mit rIC/setTimeout-Fallback; 31 Tests grün; statische Origin-Tests unverändert grün; Loader-Diff rein additiv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool-Fenster → Loader (`GET_GAME_INVENTORY`) | Bestehende Origin-/Source-Prüfung; `reqId` ist reiner Korrelations-String, wird nie interpretiert |
| Enumerator ↔ Spielzustand (window, Asset, bcModSdk, Mod-Globals) | Lesen ohne Nebenwirkung: Deskriptoren statt Werte, keine Aufrufe außer SDK-Lesefunktionen |
| Loader → Tool-Fenster (`GAME_INVENTORY_*`) | Structured Clone: kein Funktionswert, begrenzte Tiefe/Umfang; größte Payload, die je über die Bridge lief |
| Haupt-Thread des Spiel-Tabs | Chunking à 500 in Leerlaufpausen; Case kehrt sofort zurück |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-01 | Tampering | Enumerator ruft entdeckte Funktion/Getter auf (Spielzustand) | high | mitigate | `giReadData` (Deskriptor, Accessor nie ausgewertet) als einziger Wertzugriff; Fixture-Zähler `hits.*` = 0 (Test 17/22/25); werfender Getter + werfende `Dynamic*` im Fixture; Region-Gate (keine Quelltext-Konvertierung, kein Index-Zugriff, kein dynamisches Auswerten); dokumentierte Ausnahme nur `getModsInfo()`/`getPatchingInfo()` |
| T-5-02 | Denial of Service | `DataCloneError` verwirft die ganze Antwort | high | mitigate | Allowlists + `giBounded` (Funktionen fallen weg, Tiefe ≤ 2); `structuredClone`-Test auf Snapshot und jede Post (Test 9/11/26); `safePost` meldet einen Post-Fehler als `err` (Test 29) |
| T-5-03 | Denial of Service | Spiel-Tab friert ein (18.700 Namen, 4.764 Assets synchron) | high | mitigate | `giChunked` à 500 über `giNext` (rIC bevorzugt, `setTimeout 0`), erster Slice asynchron; Ticks ≥ ceil(N/500) (Test 27/28); Case kehrt sofort zurück (Test 1) |
| T-5-04 | Spoofing | Gefälschtes `GET_GAME_INVENTORY` / falsches Antwortziel | medium | mitigate | Bestehende Origin-/Source-Shell des Listeners (Test 5); Antwort an `ev.origin`, das nachweislich der Tool-Origin entspricht (Test 2 prüft `origin === LOADER_TOOL_ORIGIN`); `reqId`-Echo in jeder Post |
| T-5-09 | Tampering | Loader-Änderung verändert bestehende Cases/Sicherheitszeilen | high | mitigate | Rein additives Diff (0 gelöschte Zeilen), Zähler 34, `tests/loader-origin.test.js` + `tests/injected-code-origin.test.js` unverändert grün |
| T-5-10 | Information Disclosure | Snapshot enthält Spielerdaten (Appearance, Chat) | low | accept | Erfasst werden nur Namen/Typen/Aritäten, Asset-Definitionen und Mod-Metadaten; keine `Player`-Werte, keine Chat-Inhalte (Handler-Callbacks verworfen); Daten bleiben lokal in IDB |
| T-5-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase |
</threat_model>

<verification>
- `npx vitest run tests/game-inventory-enumerator.test.js` → 31 passed; `npm test` → 22 Dateien grün (2 expected fail unverändert).
- Region-Gates (loader.js): 0× verbotene Aufrufe, ≥ 1× Deskriptor-Zugriff, rIC + setTimeout; Zähler 34; Diff rein additiv.
- Zwei Commits: test → feat.
- VALIDATION.md 5-02-01/5-02-02: ✅ nach diesem Plan; Live-Check (echter 28-Mod-Tab) folgt als End-of-Phase-Human-Check in Plan 05-03.
</verification>

<success_criteria>
- SCAN-02..06: Snapshot-Abschnitte `globals`, `assets`, `modSdk`/`mods`, `probes`, `chatHooks` vollständig laut Vertrag; Degradation je Abschnitt ohne Abbruch.
- SCAN-07: Zähler 0, klonbar, zirkelfrei, gechunkt, Case kehrt sofort zurück.
- SCAN-01 (Loader): Case + PROGRESS/DATA mit reqId an die Tool-Origin; statische Origin-Tests unverändert.
</success_criteria>

## Artifacts this phase produces

Phasenweite Liste (Besitzer-Plan in Klammern; dieser Plan: 05-02):

**Neue Dateien**
- `tests/helpers/loaderSandbox.js`, `tests/loader-sandbox.test.js` (05-01); `tests/game-inventory-enumerator.test.js` (05-02); `tests/game-scan-bridge.test.js`, `game-scan.js` (05-03)

**Neue Symbole / Strukturen**
- loader.js (05-02): Sektion `── Gamecode-Inventar` mit `GI_*`-Konstanten, `giReadData`, `giBounded`, `giScalar`, `giNext`, `giChunked`, `giDescribeApi`, `giMods`, `giPatching`, `buildGameInventory(reqId, post)`, `window.__BCK_buildGameInventory`; Case `GET_GAME_INVENTORY`; Nachrichten `GAME_INVENTORY_PROGRESS {reqId, step, total, label}` und `GAME_INVENTORY_DATA {reqId, snapshot | err}`; Snapshot-Vertrag `schema 1`: `{ schema, gameVersion, ts, durationMs, globals, assets, modSdk, mods, probes, chatHooks, errors }`
- IndexedDB v3 Store `snapshots` + `idbSnapshot*` (05-01); Tweaks-Sektion `🔎 Spiel-Scan`, `triggerGameScan`, Snapshot-Datensatz (05-03)

**Geänderte Dateien**: loader.js (05-02, rein additiv), persistence.js (05-01), index.html/docs/LOAD-ORDER.md/tests/helpers/loadScript.js (05-03)

<output>
Create `.planning/phases/05-gamecode-inventar/05-02-SUMMARY.md` when done — mit RED-/GREEN-Ausgaben, dem Snapshot-Vertrag als Feldliste, den zwei dokumentierten SDK-Aufruf-Ausnahmen, den gemessenen Tick-Zahlen, dem Hinweis „Bookmarklet nach Deploy neu ausführen“ und der Commit-Liste.
</output>
