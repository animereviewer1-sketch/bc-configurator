---
phase: 05-gamecode-inventar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/loaderSandbox.js
  - tests/loader-sandbox.test.js
  - tests/persistence-module.test.js
  - tests/screenshot-migration.test.js
  - persistence.js
autonomous: true
requirements: [SCAN-13]
tags: [loader-sandbox, vm, indexeddb, snapshots, object-store, red-green, wave-0]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SCAN-13 ist erfüllt durch die committeten Artefakte `05-CONSOLE-FINDINGS.md` + `05-CONSOLE-RESULT.json` (R131, bcModSdk 1.2.0, 28 Mods, 18.700 Fenster-Properties / 189 Getter, 4.764 Assets / 120 Gruppen / 100 Asset-Keys, `Asset.Group.Asset`-Zirkel, Mod-Globals) — kein Task, Orchestrator-Entscheidung 1; jede Zahl und jeder Bezeichner der Pläne 05-02/05-03 ist daraus abgeleitet"
    - "Wave 0 (Orchestrator-Entscheidung 4): `tests/helpers/loaderSandbox.js` führt `loader.js` erstmals in-process aus — `makeLoaderSandbox()` liefert `{ ctx, posts, src, send, hits, timerQueue, runUntil, waitFor, opened }`, mit Stubs `URL`, `alert`, `open` (Popup-Fake), `screen`, `MutationObserver`, `ServerSocket.on/off`, `Player`/`ChatRoomCharacter`/`ChatRoomData`, `GameVersion`, `ChatRoomRegisterMessageHandler`, `Asset[]`/`AssetGroup[]` (synthetisch, mit `Group.Asset`-Zirkel und `Dynamic*`-Funktionen), `bcModSdk`-Mock (`getPatchingInfo()` = `Map` mit Funktionswerten), Mod-Globals (`FBC_VERSION`, `BCX_Loaded`/`bcx` nicht-enumerierbar, `mbs`, `LSCG_Loaded`/`LSCG`, `ThemedLoaded`/`Themed_*`), synthetischem Fenster (werfender Getter, zählender Getter, Fallen-Funktion) und `requestIdleCallback` standardmäßig ABWESEND; nach dem Laden sind 0 Timer offen, genau 1 `message`-Listener registriert, `__BCK_LISTENER_FN__` erreichbar; die bestehende statische Datei `tests/loader-origin.test.js` bleibt byte-identisch"
    - "SCAN-08 (Speicherhälfte; Requirement-Owner Plan 05-03): `persistence.js` öffnet IndexedDB `BCKonfigurator` Version 3; `onupgradeneeded` legt additiv den Object-Store `snapshots` (`keyPath: 'id'`) an — kein Migrationsschritt, keine bestehenden Daten werden bewegt; `idbSnapshotPut(record)` schreibt add-only (ein vorhandener `id` wird NIE überschrieben: `add` statt `put`, Fehler sichtbar), `idbSnapshotGetAll()` liefert alle Datensätze nach `ts` aufsteigend, `idbSnapshotGet(id)` einen oder `null`, `idbSnapshotKeys()` die ids; ein Quota-/Schreibfehler läuft über `_idbSchreibfehler('Spiel-Snapshot', err)` (Statuszeile, nie still); es gibt in dieser Phase KEINE Lösch-API für Snapshots (Phase 6, SCAN-11)"
    - "Bestehende Tests mit v2-Annahmen (`tests/screenshot-migration.test.js`: Version, `openRaw(2)`→`openRaw(3)`, `openRaw(3)`→`openRaw(4)`, statischer Versions-String) und die Export-Liste in `tests/persistence-module.test.js` sind nachgezogen; `npm test` bleibt grün (20 Dateien + 1 neue, 255+2 → mehr)"
  artifacts:
    - path: "tests/helpers/loaderSandbox.js"
      provides: "`LOADER_TOOL_ORIGIN` (aus `POPUP_URL` in loader.js abgeleitet), `GAME_ORIGIN`, `makeSyntheticAssets`, `makeBcModSdkMock`, `makeModGlobals`, `makeLoaderSandbox(opts)`"
      contains: "export function makeLoaderSandbox"
    - path: "tests/loader-sandbox.test.js"
      provides: "Smoke-/Fixture-Tests: loader.js lädt, PING→PONG an die Tool-Origin, fremde Origin/App ignoriert, 0 Timer, synthetisches Fenster mit Getter-Deskriptoren, Asset-Zirkel, bcModSdk-Map mit Funktionswerten, nicht-enumerierbare bcx-API"
      contains: "__BCK_LISTENER_FN__"
    - path: "persistence.js"
      provides: "`_IDB_VERSION = 3`, `_IDB_SNAPSHOTS = 'snapshots'`, Store-Anlage in `onupgradeneeded`, `idbSnapshotPut`, `idbSnapshotGetAll`, `idbSnapshotGet`, `idbSnapshotKeys`; erweiterter Export-Schwanz"
      contains: "const _IDB_SNAPSHOTS = 'snapshots';"
    - path: "tests/persistence-module.test.js"
      provides: "describe „Snapshot-Store (SCAN-08, IDB v3)“: Version/Stores/keyPath, Round-Trip put/get/getAll/keys, add-only, Quota sichtbar, ungültiger Datensatz, Export-Liste, statisches Gate (keine Lösch-API)"
      contains: "idbSnapshotPut"
    - path: "tests/screenshot-migration.test.js"
      provides: "v3-Nachzug: `_IDB_VERSION` 3, Raw-Öffnungen auf 3, onversionchange-Hebung auf 4"
      contains: "openRaw(4)"
  key_links:
    - from: "tests/helpers/loaderSandbox.js `makeLoaderSandbox`"
      to: "loader.js (IIFE) via `loadInto(ctx, 'loader.js')` aus tests/helpers/loadScript.js"
      via: "`makeSandbox(stubs)` + Stubs, danach `ctx.__BCK_LISTENER_FN__({ data, origin, source })` als Handler-Seam"
      pattern: "loadInto\\(ctx, 'loader.js'\\)"
    - from: "persistence.js `_idbOpen` `req.onupgradeneeded`"
      to: "Object-Store `snapshots`"
      via: "`if (!db.objectStoreNames.contains(_IDB_SNAPSHOTS)) db.createObjectStore(_IDB_SNAPSHOTS, { keyPath: 'id' });` — additiv, neben kv und screenshots"
      pattern: "createObjectStore\\(_IDB_SNAPSHOTS"
    - from: "persistence.js `idbSnapshotPut`"
      to: "`_idbSchreibfehler('Spiel-Snapshot', err)` → items.js `showStatus` (typeof-geguardet)"
      via: "catch-Pfad wie `idbScreenshotBatch`; Quota-Text „Speicher voll“, sonst „Speichern fehlgeschlagen (Spiel-Snapshot)“"
      pattern: "_idbSchreibfehler\\('Spiel-Snapshot'"
  prohibitions:
    - requirement_id: SCAN-08
      category: safety
      verification: automated
      statement: "Keine Lösch-/Leer-Operation auf dem Snapshot-Store in persistence.js (statisch 0× `objectStore(_IDB_SNAPSHOTS).delete(` und 0× `objectStore(_IDB_SNAPSHOTS).clear(`), kein `deleteDatabase`, keine Versionssenkung; `idbSnapshotPut` überschreibt nie (add-only-Test: zweiter Schreibversuch mit gleicher id → `false`, erster Datensatz unverändert)"
    - requirement_id: SCAN-13
      category: safety
      verification: automated
      statement: "`tests/loader-origin.test.js` wird nicht verändert (git-Diff gegen `f3973f6` leer); die Loader-Sandbox ändert nichts an `loader.js` (dieser Plan fasst loader.js nicht an)"
    - requirement_id: SCAN-08
      category: safety
      verification: judgment
      statement: "Kein Build, keine neuen Pakete; `git add` nur genannte Pfade; RED und GREEN getrennt committet; `vitest run` ohne Watch"
---

<objective>
Wave 0 der Phase 5: (1) eine dedizierte `loader.js`-Vitest-Sandbox, damit der Enumerator aus Plan 05-02 überhaupt automatisiert testbar ist — `loader.js` wurde bisher nie in-process ausgeführt (RESEARCH „Wave 0 Gaps“, Phase-3-Pitfall 4); (2) die Speicherhälfte von SCAN-08: IndexedDB Version 3 mit dem additiven Object-Store `snapshots` und den add-only-Primitiven `idbSnapshotPut`/`idbSnapshotGetAll`/`idbSnapshotGet`/`idbSnapshotKeys` in `persistence.js` (RED → GREEN), inklusive Nachzug der v2-Annahmen in bestehenden Tests. SCAN-13 gilt als erfüllt durch die committeten Konsolen-Artefakte (Orchestrator-Entscheidung 1) — dieser Plan verankert das in den must_haves, ohne Task.

Purpose: Ohne Sandbox kein Nachweis für SCAN-02..07 (Getter nie lesen, kein Funktionsaufruf, `structuredClone`-Beweis, Chunking); ohne Store kein versionierter Snapshot (Erfolgskriterium 5, Kernwert „nie automatisch entfernt“).
Output: `tests/helpers/loaderSandbox.js`, `tests/loader-sandbox.test.js` (neu), `persistence.js` (v3 + Snapshot-Primitiven), `tests/persistence-module.test.js` (erweitert), `tests/screenshot-migration.test.js` (v3-Nachzug).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gamecode-inventar/05-RESEARCH.md
@.planning/phases/05-gamecode-inventar/05-CONSOLE-FINDINGS.md
@.planning/phases/05-gamecode-inventar/05-CONSOLE-RESULT.json
@.planning/phases/05-gamecode-inventar/05-VALIDATION.md
@.planning/phases/04-entflechtung/04-04-SUMMARY.md
@docs/LOAD-ORDER.md
@tests/helpers/loadScript.js
@persistence.js

Planer-Probe (2026-09-15, HEAD `f3973f6`; per grep/Spike ankern) — verbindliche Befunde:
1. **loader.js läuft in der vm-Sandbox** (Spike ausgeführt): Nötige Stubs über `makeSandbox` hinaus: `URL` (für `new URL(POPUP_URL).origin`), `alert`, `open` als Sandbox-Property (loader ruft `window.open(...)`, und `window === sandbox`), `screen.width/height`, `MutationObserver` (Klasse mit `observe`/`disconnect`), `ServerSocket.on/off`, `bcModSdk.registerMod` → `{ hookFunction() {} }` (sonst 500-ms-Retry-Timer in `installBCXFilter`), `document.getElementById` truthy (sonst 2-s-Retry in `_installChatObserver`; `makeSandbox` liefert bereits `makeElementStub()`), `Player`/`ChatRoomCharacter`/`ChatRoomData`. Ergebnis mit allen Stubs: 0 offene Timer, genau 1 `message`-Listener, `window.__BCK_LISTENER_FN__` ist eine Funktion, `window.open` genau 1× aufgerufen; `__BCK_LISTENER_FN__({ data: { app: 'BCKonfigurator', type: 'PING' }, origin: <Tool-Origin>, source: src })` postet `{ app, type: 'PONG' }` an die aus `POPUP_URL` abgeleitete Tool-Origin.
2. **Deskriptoren cross-realm:** `Object.getOwnPropertyDescriptor(window, name)` im Kontext sieht Accessor-Deskriptoren, die per `Object.defineProperty(sandbox, name, { get() {…} })` auf dem Host-Sandbox-Objekt liegen (`typeof d.get === 'function'`); `Object.getOwnPropertyNames(window)` liefert die eigenen Properties des Sandbox-Objekts (103 im Spike). Host-`structuredClone` auf Kontext-Objekten: Plain-Objects/Arrays/Map klonbar, ein Funktionswert wirft `DataCloneError` — der billigste Pitfall-6-Nachweis (RESEARCH Code Example 3).
3. **Statischer Zähler:** `tests/loader-origin.test.js` erwartet exakt 34 Vorkommen des Bezeichners `ALLOWED_ORIGIN` in loader.js (1 Definition, 1 Prüfung, 32 Sendestellen) und bleibt unangetastet (Orchestrator-Entscheidung 4) — relevant für Plan 05-02, in diesem Plan wird loader.js nicht verändert.
4. **v2-Annahmen in bestehenden Tests:** `tests/screenshot-migration.test.js` Zeile 7 (Kommentar „hebt die DB auf Version 3“), 138 (`expect(evalIn(ctx, '_IDB_VERSION')).toBe(2)`), 153/167/193 (`openRaw(2)` — bei DB-Version 3 würde das mit `VersionError` scheitern), 238 (`openRaw(3)` zur Auslösung von `onversionchange` — muss auf 4), 255 (`'const _IDB_VERSION = 2;'`), Testname Zeile 234 („hebt die DB auf v3“). `tests/persistence-module.test.js` Zeile ~80: sortierte Export-Liste (`Object.keys(module.exports).sort()`), Zeile 117 nur `>= 1`. Sonst hängt kein Test an der Versionsnummer (grep `_IDB_VERSION|openRaw|indexedDB.open` in tests/).
5. **Quota-Pfad:** `_idbSchreibfehler(label, err)` (persistence.js): Quota-Erkennung `err.name === 'QuotaExceededError' || /quota/i.test(err.name || '')`, Text `'❌ Speicher voll – "' + label + '" wurde NICHT gesichert. Backup anlegen und aufräumen!'`, sonst `'❌ Speichern fehlgeschlagen (' + label + '): ' + (err?.message || err)`, Drossel 10 s über `_idbFehlerGemeldet` — Tests setzen `evalIn(ctx, '_idbFehlerGemeldet = 0')` vor jedem Fehlerfall. Muster für synchron werfende Store-Operation in einem Callback: `try { st.add(...) } catch (err) { try { tx.abort(); } catch (e) {} reject(err); return; }` (wie `idbScreenshotBatch`).
6. **fake-indexeddb 6.2.5:** pro Testdatei eine Instanz; alle Sandboxen einer Datei teilen `BCKonfigurator`. Store-Operationen sind auf `globalThis.IDBObjectStore.prototype` patchbar (`put`/`add`/`delete`), Restauration in `afterEach` (Muster tests/screenshot-store.test.js).
7. **Baseline:** `npm test` = 20 Dateien, 255 passed + 2 expected fail (2026-09-15, 1,97 s). Bekannte, nicht blockierende Teardown-Race in tests/load-order-guard.test.js (STATE.md) — Sandbox-`console` in der Loader-Sandbox deshalb standardmäßig stumm.
8. **SCAN-13-Zahlen** (05-CONSOLE-RESULT.json, Grundlage der Fixtures): 18.700 Fenster-Properties / 189 Getter (alle Standard-DOM); 12.557 Präfix-Treffer, davon `Inventory` 11.000; 4.764 Assets, 120 Gruppen, 100 Asset-Keys, `Asset.Group.Asset` zirkulär, Tiefe 5; bcModSdk 1.2.0: `getModsInfo()` → Array `{name, fullName, version, repository}` (28), `getPatchingInfo()` → `Map` (546) mit Funktionsfeldern `original`/`sdkEntrypoint`/`currentEntrypoint`; Mod-Globals: `FBC_VERSION` (string) + `fbc*`/`wce*`-Funktionen, `BCX_Loaded` + `bcx` (API nicht-enumerierbar, `Object.keys(bcx)` = `[]`), `mbs.MBS_VERSION`/`mbs.API_VERSION.{major,minor}`, `LSCG_Loaded` + `LSCG` (Funktions-Keys), `ThemedLoaded` + `Themed_*` (`Themed` selbst `undefined`).
9. **Reversibilität (Orchestrator):** IDB-Versionsbump 2→3 ist `costly` (IDB kennt kein Downgrade), abgesichert: rein additiv, kein Datensatz wird bewegt; Rollback = alten Code fahren, Store bleibt ungenutzt liegen. Kein blockierender Checkpoint (REVERSIBILITY_GATES: Flag genügt bei `costly`).
</context>

<tasks>

<!-- Comment-Text-Discipline-Allowlist (#429): die folgenden Literale beschreiben Gates/Testinhalte und müssen im Plan wörtlich stehen -->
<!-- planner-discipline-allow: objectStore(_IDB_SNAPSHOTS).delete( -->
<!-- planner-discipline-allow: objectStore(_IDB_SNAPSHOTS).clear( -->
<!-- planner-discipline-allow: deleteDatabase -->
<!-- planner-discipline-allow: idbSnapshotDelete -->
<!-- planner-discipline-allow: openRaw(2 -->
<!-- planner-discipline-allow: openRaw(3 -->
<!-- planner-discipline-allow: openRaw(4 -->
<!-- planner-discipline-allow: const _IDB_VERSION = 3; -->
<!-- planner-discipline-allow: toBe(2) -->
<!-- planner-discipline-allow: objectStore(_IDB_SNAPSHOTS).put( -->

<task type="auto">
  <name>Task 1: Loader-Sandbox `tests/helpers/loaderSandbox.js` + Smoke-Test `tests/loader-sandbox.test.js` — loader.js läuft erstmals in-process (SCAN-13-Folge, Wave 0)</name>
  <precondition>`npm test` grün (20 Dateien); `git status --short` leer (außer `.planning/`)</precondition>
  <files>tests/helpers/loaderSandbox.js, tests/loader-sandbox.test.js</files>
  <read_first>tests/helpers/loadScript.js (`makeSandbox`, `makeElementStub`, `loadInto`, `_listeners`); loader.js Zeilen 1-35 (Logger, `POPUP_URL`, `ALLOWED_ORIGIN`), 828-855 (Listener-Kopf: `app`-Check, Origin-Check, Source-Pinning, `switch`), 1365-1412 (`installBCXFilter`, `bcModSdk.registerMod`), 1441-1444 (`_BCU_leerlauf`), 1493-1508 (`_installOutfitScan`/`ServerSocket`), 1580-1604 (`MutationObserver`), 1615-1637 (`screen`, `window.open`, `alert`); .planning/phases/05-gamecode-inventar/05-CONSOLE-RESULT.json (Fixture-Formen); tests/loader-origin.test.js (bleibt unverändert; Ableitung der Origin aus `POPUP_URL` übernehmen)</read_first>
  <action>
    **A. `tests/helpers/loaderSandbox.js`** (neu, ESM wie loadScript.js; deutscher Kopfkommentar: Zweck „loader.js in-process“, Liste der Stubs und WARUM jeder nötig ist — Befund 1). Importiert `fs`, `path`, `REPO_ROOT`, `makeSandbox`, `makeElementStub`, `loadInto` aus `./loadScript.js`.
    Exports:
    - `LOADER_TOOL_ORIGIN`: aus loader.js abgeleitet — Quelltext lesen, `/const POPUP_URL = '([^']+)'/` matchen, `new URL(m[1]).origin` (keine Host-Zeichenkette in der Datei hart codieren).
    - `GAME_ORIGIN = 'https://game.test'`, `APP = 'BCKonfigurator'`.
    - `makeSyntheticAssets({ groups = ['ItemArms', 'ItemTorso', 'ItemTorso2', 'Cloth'], perGroup = 3, hits } = {})` → `{ Asset, AssetGroup }`. Je Gruppe ein Objekt `{ Name, Description: 'Gruppe ' + Name, Category: Name.startsWith('Item') ? 'Item' : 'Appearance', Family: 'Female3DCG', IsRestraint: Name.startsWith('Item'), Clothing: !Name.startsWith('Item'), Zone: [[0, 0, 1, 1]], Asset: [] }`. Je Asset `i` in Gruppe: `{ Name: Name + 'Asset' + i, Description: 'Asset ' + i, Group: <Gruppenobjekt> (Zirkel: Gruppe.Asset.push(asset)), ParentItem: i === 0 ? null : Name + 'Asset0', Enable: true, Visible: true, Value: i * 10, Difficulty: i, Effect: ['Block'], Block: [], Prerequisite: 'Prereq' + i, Extended: i % 2 === 0, IsLock: false, IsRestraint: <wie Gruppe>, AllowLock: true, AllowLockType: ['MetalPadlock'], DefaultColor: ['Default', '#123456'], Layer: [{ Name: 'L1', Priority: 1 }, { Name: 'L2', Priority: 2 }], ColorableLayerCount: 2, Tint: [{ Color: 0 }], ExpressionTrigger: [{ Group: 'Eyes', Name: 'Closed', Timer: 5 }], OverrideHeight: { Height: 1, Priority: 2 }, Category: ['Bondage'], Attribute: ['Attr'], Gender: null, CraftGroup: Name + 'Craft', DynamicName() { hits.dynamic++; return 'x'; }, DynamicDescription() { throw new Error('Dynamic* wurde aufgerufen'); }, DynamicBeforeDraw() { hits.dynamic++; }, DynamicAfterDraw() { hits.dynamic++; } }`. Zusätzlich GENAU EIN Waisen-Asset ohne `Group` (`{ Name: 'OrphanAsset', Description: 'ohne Gruppe', Enable: true, Layer: [] }`) am Ende von `Asset`. `hits` ist ein Zählerobjekt (Default `{ getter: 0, fn: 0, dynamic: 0, patching: 0 }`), das der Aufrufer hereingibt.
    - `makeBcModSdkMock({ mods, patching, hits } = {})` → Objekt `{ version: '1.2.0', apiVersion: 1, errorReporterHooks: {}, registerMod(info) { registrations.push(info); return { hookFunction() {}, patchFunction() {}, removeHook() {} }; }, getModsInfo() { return mods; }, getPatchingInfo() { return patching; }, registrations }`. Defaults: `mods = [{ name: 'BCX', fullName: 'Bondage Club Extended', version: '1.1.19-test', repository: 'https://example.test/bcx' }, { name: 'WCE', fullName: 'Wholesome Club Extensions', version: '6.3.19', repository: 'https://example.test/wce' }]`; `patching = new Map([['CommonDrawAppearanceBuild', { name: 'CommonDrawAppearanceBuild', original() { hits.patching++; }, originalHash: 'B02DDFE3', sdkEntrypoint() { hits.patching++; }, currentEntrypoint() { hits.patching++; }, hookedByMods: ['LSCG', 'BCT'], patchedByMods: [] }], ['ChatRoomMessage', { … hookedByMods: ['BCX'], patchedByMods: ['WCE'] }], ['ServerSend', { … hookedByMods: ['BCK_BCXFilter'], patchedByMods: [] }]])` — jede Funktion erhöht `hits.patching`.
    - `makeModGlobals(hits)` → Plain-Object mit: `FBC_VERSION: '6.3.19'`, `fbcChatNotify() { hits.fn++; }`, `fbcDisplayText() { hits.fn++; }`, `wceServerAppearance() { hits.fn++; }`, `BCX_Loaded: true`, `bcx`: Objekt, dessen Properties per `Object.defineProperty` NICHT-enumerierbar sind — `version: '1.1.19-test'` (Daten-Property), `getRuleState() { hits.fn++; }` (Funktion), sowie ein Accessor `secret` mit `get() { hits.getter++; return 1; }` — sodass `Object.keys(bcx)` `[]` liefert; `mbs: { MBS_VERSION: '1.10.25', API_VERSION: { major: 1, minor: 5 }, getDebug() { hits.fn++; }, runTests() { hits.fn++; }, wheelEvents: {}, css: 'x' }`, `LSCG_Loaded: true`, `LSCG: { getModule() { hits.fn++; }, Outfits: {}, sendLSCGBeep() { hits.fn++; }, HypnoTriggers: ['x'] }`, `LSCG_SuggestionMiniGameRun() { hits.fn++; }`, `LSCG_SleepyMiniGameLoad() { hits.fn++; }`, `ThemedLoaded: true`, `Themed_mainmenuLoad() { hits.fn++; }`, `Themed_mainmenuRun() { hits.fn++; }`, `Themed_colorsBackground: 'Sheet'`.
    - `makeLoaderSandbox(opts = {})` mit `opts`: `syntheticGlobals` (Zahl, Default 0: erzeugt `giData<i>` (Zahl), `giStr<i>` (String), `giObj<i>` (Objekt) und `giFn<i>` (Funktion mit Arität `i % 4`, Körper erhöht `hits.fn`) reihum), `inventoryFunctions` (Zahl, Default 0: erzeugt `InventoryItem<Gruppe><Name>Load/Draw/Click`-Funktionen reihum über die Gruppen aus `makeSyntheticAssets`, plus 3 Kern-Funktionen `InventoryGet(C, G)`, `InventoryWear(C, A, G)`, `InventoryItemHasEffect(I, E)` — Arität per Parameterliste; alle erhöhen `hits.fn`), `throwingGetter` (Default true: `Object.defineProperty(ctx, 'giThrowingGetter', { get() { hits.getter++; throw new Error('Getter mit Nebenwirkung gelesen'); }, enumerable: true, configurable: true })`), `countingGetter` (Default true: `giCountingGetter`, `get() { hits.getter++; return 42; }`), `withBcModSdk` (Default true), `withAssets` (Default true), `withModGlobals` (Default true), `manualTimers` (Default false: `setTimeout`-Stub `(fn) => { timerQueue.push(fn); counts.timeout++; return timerQueue.length; }`, `clearTimeout` No-op), `requestIdleCallback` (Default `null` = abwesend; `true` installiert `(fn) => { timerQueue.push(fn); counts.ric++; return timerQueue.length; }`), `extraGlobals` (Objekt, zuletzt gespreadet), `console` (Default stumm `{ log() {}, info() {}, warn() {}, error() {}, debug() {} }`), `mods`/`patching` (durchgereicht an `makeBcModSdkMock`).
      Ablauf: `hits = { getter: 0, fn: 0, dynamic: 0, patching: 0 }`; `opened = []`; `stubs = { console, URL, alert() {}, open(url, name, features) { const w = { closed: false, url, name, features, focus() {}, postMessage() {} }; opened.push(w); return w; }, screen: { width: 1920, height: 1080 }, location: { origin: GAME_ORIGIN, href: GAME_ORIGIN + '/R131/BondageClub/', pathname: '/R131/BondageClub/', protocol: 'https:', host: 'game.test', hostname: 'game.test', search: '', hash: '' }, MutationObserver: class { observe() {} disconnect() {} takeRecords() { return []; } }, ServerSocket: { on() {}, off() {} }, Player: { MemberNumber: 1, Name: 'Testspielerin', AssetFamily: 'Female3DCG', Appearance: [] }, ChatRoomCharacter: [], ChatRoomData: { Name: 'Testraum' }, GameVersion: 'R131', ChatRoomRegisterMessageHandler: function (handler) { hits.fn++; } }` (+ `Asset`/`AssetGroup`, `bcModSdk`, Mod-Globals je nach Flags; + Timer-Stubs) → `ctx = makeSandbox({ ...stubs, ...opts.extraGlobals })`; synthetische Globals und Getter per `Object.defineProperty`/Zuweisung auf `ctx` (VOR dem Laden); `loadInto(ctx, 'loader.js')`; `posts = []`; `src = { postMessage(msg, origin) { posts.push({ msg, origin }); }, closed: false }`; `send(data, { origin = LOADER_TOOL_ORIGIN, source = src } = {})` → `ctx.__BCK_LISTENER_FN__({ data: { app: APP, ...data }, origin, source })`; `runUntil(pred, max = 200000)` → nimmt `timerQueue.shift()()` bis `pred()` wahr oder Queue leer oder `max` erreicht, liefert Anzahl gelaufener Ticks; `waitFor(pred, timeoutMs = 8000)` → pollt mit dem HOST-`setTimeout` (nicht dem Sandbox-Stub) alle 5 ms, wirft `Error('waitFor: Timeout')` bei Ablauf; Rückgabe `{ ctx, posts, src, send, hits, timerQueue, counts, runUntil, waitFor, opened, assets: { Asset, AssetGroup } }`.
    **B. `tests/loader-sandbox.test.js`** (neu; `describe('Loader-Sandbox: loader.js läuft in-process (Wave 0, Phase 5)')`):
    1. „loader.js lädt ohne Throw; genau ein message-Listener; `__BCK_LISTENER_FN__` ist Funktion; window.open genau einmal mit POPUP_URL“: `opened.length === 1`, `opened[0].url === LOADER_TOOL_ORIGIN + '/bc-configurator/'` (aus `POPUP_URL` — Pfad ebenfalls per Regex ableiten statt hart codieren, oder nur `startsWith(LOADER_TOOL_ORIGIN)` prüfen), `ctx._listeners.get('message').length === 1`, `typeof ctx.__BCK_LISTENER_FN__ === 'function'`, `ctx.__BCK_LISTENER__ === true`.
    2. „PING → PONG an die Tool-Origin“: `send({ type: 'PING' })` → `posts` = `[{ msg: { app: APP, type: 'PONG' }, origin: LOADER_TOOL_ORIGIN }]`.
    3. „fremde Origin und fremde App werden ignoriert“: `send({ type: 'PING' }, { origin: 'https://evil.test' })` → keine neue Post; `ctx.__BCK_LISTENER_FN__({ data: { app: 'X', type: 'PING' }, origin: LOADER_TOOL_ORIGIN, source: src })` → keine neue Post.
    4. „Source-Pinning: nach dem ersten Kontakt blockt eine andere Quelle alles außer PING“ (STAB-06 jetzt dynamisch): `send({ type: 'GET_POS', reqId: 'p1' })` postet `POS_DATA` mit `reqId 'p1'`; danach `send({ type: 'GET_POS', reqId: 'p2' }, { source: { postMessage() {} } })` → keine Post mit `reqId 'p2'`.
    5. „mit allen Stubs bleiben 0 Timer offen“: `makeLoaderSandbox({ manualTimers: true })` → `timerQueue.length === 0` nach dem Laden.
    6. „synthetisches Fenster: Getter als Accessor-Deskriptor sichtbar, nie gelesen“: `makeLoaderSandbox({ syntheticGlobals: 1000 })` → `Object.getOwnPropertyNames(ctx).length >= 1000`; `evalIn`-frei: `const d = Object.getOwnPropertyDescriptor(ctx, 'giThrowingGetter'); typeof d.get === 'function'`; `hits.getter === 0`; `expect(() => ctx.giThrowingGetter).toThrow(/Nebenwirkung/)`; danach `hits.getter === 1` (die Fixture zählt wirklich); `ctx.giCountingGetter === 42` → `hits.getter === 2`; `typeof ctx.giFn3 === 'function'` und `ctx.giFn3.length === 3`.
    7. „Inventory-Fixture: Kern- und Item-Funktionen“: `makeLoaderSandbox({ inventoryFunctions: 20 })` → `typeof ctx.InventoryGet === 'function'`, `ctx.InventoryGet.length === 2`; Anzahl eigener Property-Namen, die mit `InventoryItem` beginnen, `>= 20`; mindestens ein Name beginnt mit `InventoryItemItemTorso2` (Längster-Präfix-Fall).
    8. „Asset-Fixture: Zirkel `Asset.Group.Asset`, Dynamic*-Funktionen, Waise“: `assets.Asset[0].Group.Asset.includes(assets.Asset[0])`; `expect(() => JSON.stringify(assets.Asset[0])).toThrow(/circular/i)` (Pitfall 2 dokumentiert); `typeof assets.Asset[0].DynamicName === 'function'`; `assets.Asset.at(-1).Name === 'OrphanAsset'` und `assets.Asset.at(-1).Group === undefined`; `assets.AssetGroup.length === 4`; `assets.Asset.length === 13`.
    9. „bcModSdk-Mock: getModsInfo Array, getPatchingInfo Map mit Funktionswerten, registerMod liefert hookFunction“: `Array.isArray(ctx.bcModSdk.getModsInfo())`, `ctx.bcModSdk.getPatchingInfo() instanceof Map` — ACHTUNG Realm: die Map wird im Host erzeugt und ist Host-`Map` → `instanceof Map` im Host-Test korrekt; `typeof ctx.bcModSdk.getPatchingInfo().get('CommonDrawAppearanceBuild').original === 'function'`; `ctx.bcModSdk.registrations.some(r => r.name === 'BCK_BCXFilter')` (loader hat sich beim Laden registriert).
    10. „Mod-Globals: bcx-API nicht-enumerierbar, aber per getOwnPropertyNames sichtbar“: `Object.keys(ctx.bcx).length === 0`; `Object.getOwnPropertyNames(ctx.bcx)` enthält `'version'`, `'getRuleState'`, `'secret'`; `ctx.bcx.version === '1.1.19-test'`; `ctx.FBC_VERSION === '6.3.19'`; `ctx.mbs.API_VERSION.minor === 5`; `ctx.LSCG_Loaded === true`; `ctx.ThemedLoaded === true`; `hits.fn === 0` (nichts wurde aufgerufen).
    11. „ohne bcModSdk: Retry-Timer landet in der manuellen Queue (Fixture-Verhalten dokumentiert)“: `makeLoaderSandbox({ withBcModSdk: false, manualTimers: true })` → `timerQueue.length >= 1` (der `installBCXFilter`-Retry) — Hinweis im Test, dass `runUntil` deshalb mit Prädikat arbeitet.
    Kopfkommentar der Testdatei: erklärt, dass `tests/loader-origin.test.js` (statisch) bewusst unverändert bleibt und diese Datei die dynamische Ergänzung ist.
    **C. Commit** (nur diese zwei Pfade): `git add tests/helpers/loaderSandbox.js tests/loader-sandbox.test.js` → `test(05-01): loader.js-Sandbox mit Stubs + Smoke-Test (Wave 0)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/loader-sandbox.test.js tests/loader-origin.test.js && test "$(grep -c 'export function makeLoaderSandbox' tests/helpers/loaderSandbox.js)" = 1 && test "$(grep -c 'export function makeSyntheticAssets' tests/helpers/loaderSandbox.js)" = 1 && test "$(grep -c 'export function makeBcModSdkMock' tests/helpers/loaderSandbox.js)" = 1 && test "$(grep -c 'export function makeModGlobals' tests/helpers/loaderSandbox.js)" = 1 && test "$(grep -c "loadInto(ctx, 'loader.js')" tests/helpers/loaderSandbox.js)" = 1 && test "$(grep -c 'animereviewer1-sketch' tests/helpers/loaderSandbox.js tests/loader-sandbox.test.js | awk -F: '{s+=$2} END {print s}')" = 0 && test -z "$(git diff --name-only f3973f6 HEAD -- tests/loader-origin.test.js loader.js)" && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "tests/helpers/loaderSandbox.js tests/loader-sandbox.test.js " && echo SANDBOX-OK</automated>
    <fails_when>`SANDBOX-OK` fehlt — ein Smoke-Fall rot (z. B. Retry-Timer offen, weil ein Stub fehlt; Getter-Deskriptor nicht sichtbar; PONG an falsche Origin), Exporte fehlen/heißen anders, der Tool-Host steht hart codiert in Helper/Test, `loader.js` oder `tests/loader-origin.test.js` wurden angefasst, oder der Commit enthält andere Pfade</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/loader-sandbox.test.js` → 11 Fälle grün; `npx vitest run tests/loader-origin.test.js` weiterhin grün (Zähler 34)
    - `grep -c 'Object.defineProperty' tests/helpers/loaderSandbox.js` >= 3 (werfender Getter, zählender Getter, nicht-enumerierbare bcx-API)
    - `grep -c 'requestIdleCallback' tests/helpers/loaderSandbox.js` >= 1 und Default ist abwesend (`makeLoaderSandbox()` ohne Option → `typeof ctx.requestIdleCallback === 'undefined'`; Fall in Test 5 oder 6 mitprüfen)
    - `grep -c "hits" tests/helpers/loaderSandbox.js` >= 10; kein `.only(` in der Testdatei
    - `git status --short loader.js persistence.js` leer
  </acceptance_criteria>
  <done>loader.js läuft in einer Vitest-Sandbox mit dokumentierten Stubs; Handler-Seam `send()`, Post-Sammler, Zähler `hits`, manuelle Timer-Queue und Fixtures (Assets mit Zirkel, bcModSdk-Map, Mod-Globals) stehen für Plan 05-02 bereit; 11 Smoke-Fälle grün; statische Origin-Tests unverändert.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot-Store-Tests schreiben (RED) + v3-Nachzug in `tests/screenshot-migration.test.js` — `tests/persistence-module.test.js` (SCAN-08 Speicherhälfte, Wave 0)</name>
  <files>tests/persistence-module.test.js, tests/screenshot-migration.test.js</files>
  <read_first>tests/persistence-module.test.js (bestehende describe-Blöcke, Export-Listen-Test, `loadScript(['persistence.js'])`-Muster); tests/screenshot-migration.test.js Zeilen 1-70 (Helfer `openRaw`, Spy-Muster), 130-140, 150-155, 165-170, 190-195, 232-260; tests/idb-helpers.test.js Zeilen 55-120 (Quota-Fall, `_idbFehlerGemeldet`, Monkeypatch mit Restauration); persistence.js (`_idbSchreibfehler`, `idbScreenshotBatch`, Export-Schwanz)</read_first>
  <action>
    **A. `tests/screenshot-migration.test.js` — v3-Nachzug (nur diese Stellen, Befund 4):** Kopfkommentar Zeile 7 „hebt die DB auf Version 3“ → „Version 4“; Zeile 138 `toBe(2)` → `toBe(3)`; Zeilen 153, 167, 193 `openRaw(2)` → `openRaw(3)`; Zeile 238 `openRaw(3)` → `openRaw(4)` (Variable `db3` darf bleiben oder `db4` heißen — konsistent halten); Zeile 255 `'const _IDB_VERSION = 2;'` → `'const _IDB_VERSION = 3;'`; Testname Zeile 234 „hebt die DB auf v3“ → „v4“. Nichts anderes in dieser Datei ändern.
    **B. `tests/persistence-module.test.js` — Export-Liste:** im Test „mit module-Global füllt der Export-Schwanz module.exports“ die sortierte Erwartung um `'idbSnapshotGet', 'idbSnapshotGetAll', 'idbSnapshotKeys', 'idbSnapshotPut'` ergänzen (alphabetisch korrekt zwischen `idbSet` … einsortieren: `idbScreenshot*` < `idbSet` < `idbSnapshot*`).
    **C. `tests/persistence-module.test.js` — neuer Block `describe('Snapshot-Store (SCAN-08, IDB v3)')`** (Imports `vi`, `afterEach` ergänzen, falls fehlend; Helfer `openRaw(version)` wie in screenshot-migration.test.js lokal definieren; `boot()` → `showStatus = vi.fn(); ctx = loadScript(['persistence.js'], { showStatus }); evalIn(ctx, '_idbFehlerGemeldet = 0'); return { ctx, showStatus }`; `origAdd = globalThis.IDBObjectStore.prototype.add`, `afterEach` restauriert; Beispieldatensatz `rec(id, extra)` → `{ id, ts: id, gameVersion: 'R131', modCount: 1, mods: [{ name: 'BCX', version: '1.1.19' }], sizeBytes: 12, inventory: { schema: 1, marker: 'a' }, ...extra }`):
    1. „_IDB_VERSION ist 3; Stores kv, screenshots, snapshots; snapshots hat keyPath id“: `boot()`; `await ctx.idbGet('warm')`; `evalIn(ctx, '_IDB_VERSION') === 3`; `db = await openRaw(3)`; `Array.from(db.objectStoreNames).sort()` `toEqual(['kv', 'screenshots', 'snapshots'])`; `db.transaction('snapshots', 'readonly').objectStore('snapshots').keyPath === 'id'`; `db.close()`.
    2. „Round-Trip: put → get/getAll/keys, Reihenfolge nach ts“: `await ctx.idbSnapshotPut(rec(2000)) === true`; `await ctx.idbSnapshotPut(rec(1000)) === true`; `await ctx.idbSnapshotGet(1000)` `toEqual(rec(1000))`; `await ctx.idbSnapshotGet(999) === null`; `(await ctx.idbSnapshotGetAll()).map(r => r.id)` `toEqual([1000, 2000])`; `(await ctx.idbSnapshotKeys()).sort()` `toEqual([1000, 2000])`; `showStatus` nicht aufgerufen.
    3. „add-only: gleiche id ein zweites Mal → false, Statusmeldung, erster Datensatz unverändert (Kernwert)“: `await ctx.idbSnapshotPut(rec(1000, { inventory: { schema: 1, marker: 'b' } })) === false`; `showStatus` `toHaveBeenCalledWith(expect.stringContaining('Speichern fehlgeschlagen (Spiel-Snapshot)'), 'error')`; `(await ctx.idbSnapshotGet(1000)).inventory.marker === 'a'`; `(await ctx.idbSnapshotKeys()).length === 2`.
    4. „Quota: add wirft QuotaExceededError → false, „Speicher voll“ sichtbar, nichts geschrieben“: `evalIn(ctx, '_idbFehlerGemeldet = 0')`; `globalThis.IDBObjectStore.prototype.add = function () { const e = new Error('quota'); e.name = 'QuotaExceededError'; throw e; }`; `await ctx.idbSnapshotPut(rec(3000)) === false`; `showStatus` `toHaveBeenCalledWith(expect.stringContaining('Speicher voll'), 'error')` und der Aufruf enthält `'Spiel-Snapshot'`; Restauration; `(await ctx.idbSnapshotGet(3000)) === null`.
    5. „ungültiger Datensatz (ohne numerische id) → false ohne IDB-Zugriff“: `evalIn(ctx, '_idbFehlerGemeldet = 0')`; Spy auf `add` (zählt Aufrufe, ruft Original); `await ctx.idbSnapshotPut({ ts: 1 }) === false`; `await ctx.idbSnapshotPut(null) === false`; Spy-Zähler 0; `showStatus` mit `'Spiel-Snapshot'` und `'error'` aufgerufen.
    6. „Store-Fehler in getAll/keys/get werden geloggt und degradieren zu []/[]/null“: `warn = vi.spyOn(console, 'warn').mockImplementation(() => {})`; `origGetAll = globalThis.IDBObjectStore.prototype.getAll`; Patch wirft `new Error('kaputt')`; `await ctx.idbSnapshotGetAll()` `toEqual([])`; Restauration; `warn` mit `expect.stringContaining('[IDB]')` aufgerufen; `warn.mockRestore()`.
    7. „statisch: keine Lösch-API für Snapshots, Version 3, Store additiv, add statt put“: `src = fs.readFileSync(persistence.js)`; `count(src, 'const _IDB_VERSION = 3;') === 1`; `count(src, "const _IDB_SNAPSHOTS = 'snapshots';") === 1`; `count(src, "createObjectStore(_IDB_SNAPSHOTS, { keyPath: 'id' })") === 1`; `count(src, 'objectStore(_IDB_SNAPSHOTS).add(') === 1`; `count(src, 'objectStore(_IDB_SNAPSHOTS).put(') === 0`; `count(src, 'objectStore(_IDB_SNAPSHOTS).delete(') === 0`; `count(src, 'objectStore(_IDB_SNAPSHOTS).clear(') === 0`; `count(src, 'idbSnapshotDelete') === 0`; `count(src, 'deleteDatabase') === 0`; `count(src, "_idbSchreibfehler('Spiel-Snapshot'") >= 1`.
    **D. RED bestätigen und committen** (nur die zwei Testdateien): `git add tests/persistence-module.test.js tests/screenshot-migration.test.js` → `test(05-01): Snapshot-Store-Tests (RED) + v3-Nachzug Migrationstest`. persistence.js bleibt in diesem Task unverändert.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/persistence-module.test.js tests/screenshot-migration.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "idbSnapshotPut|_IDB_VERSION|snapshots" "$OUT" && grep -Eq "Tests +[0-9]+ failed|Test Files +[0-9]+ failed" "$OUT" && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "tests/persistence-module.test.js tests/screenshot-migration.test.js " && git diff --quiet HEAD -- persistence.js && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` fehlt — Exit 0 (Funktionen existieren bereits oder Tests prüfen nichts), die Ausgabe nennt keinen der Bezeichner und keine `failed`-Zeile, persistence.js wurde bereits geändert, oder der Commit enthält andere Pfade als die zwei Testdateien</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'idbSnapshotPut' tests/persistence-module.test.js` >= 8; `grep -c 'QuotaExceededError' tests/persistence-module.test.js` >= 1; `grep -c "keyPath" tests/persistence-module.test.js` >= 2; `grep -c 'deleteDatabase' tests/persistence-module.test.js` >= 1
    - `grep -c 'openRaw(3' tests/screenshot-migration.test.js` >= 3; `grep -c 'openRaw(4' tests/screenshot-migration.test.js` >= 1; `grep -c 'openRaw(2' tests/screenshot-migration.test.js` = 0; `grep -Ec 'const _IDB_VERSION += +3;' tests/screenshot-migration.test.js` >= 1; `grep -c "toBe(2)" tests/screenshot-migration.test.js` = 0
    - `afterEach` restauriert `IDBObjectStore.prototype.add` (grep `afterEach` >= 1 in persistence-module.test.js); kein `.only(`
  </acceptance_criteria>
  <done>7 neue Snapshot-Store-Fälle + angepasste Export-Liste nachweislich rot; Migrationstest auf v3/v4 nachgezogen; roter Test committet; persistence.js unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `persistence.js` — IDB Version 3, Store `snapshots`, add-only-Primitiven, Export — GREEN (SCAN-08 Speicherhälfte)</name>
  <reversibility rating="costly">IDB kennt kein Downgrade; Store `snapshots` mit `keyPath 'id'` wird ab Phase 6 gelesen — Rollback nur durch alten Code (Store bleibt ungenutzt liegen), nie durch Versionssenkung; abgesichert durch rein additive Anlage ohne Datenbewegung (Orchestrator akzeptiert, kein Checkpoint).</reversibility>
  <files>persistence.js</files>
  <read_first>persistence.js (gesamte Datei: Kopf, `_idbOpen`, `_idbSchreibfehler`, `idbScreenshotBatch`/`idbScreenshotGetAll`/`idbScreenshotKeys`, Export-Schwanz); tests/persistence-module.test.js (Task 2, Erwartungen an Texte und Zählungen); .planning/phases/05-gamecode-inventar/05-RESEARCH.md „Pattern 6“</read_first>
  <behavior>
    - `_IDB_VERSION` 2 → 3 mit Kommentar „v3: Object-Store 'snapshots' (SCAN-08). Nie senken.“; `onupgradeneeded` legt `snapshots` (`keyPath: 'id'`) zusätzlich an — bestehende Stores unberührt
    - `idbSnapshotPut(record)`: `record` muss Objekt mit `typeof record.id === 'number'` sein, sonst `_idbSchreibfehler('Spiel-Snapshot', new Error('ungültiger Snapshot-Datensatz'))` und `false` ohne IDB-Zugriff; sonst readwrite-Transaktion auf `_IDB_SNAPSHOTS`, `objectStore(_IDB_SNAPSHOTS).add(record)` in `try/catch` (synchroner Wurf → `tx.abort()` + reject), `tx.oncomplete` → `true`, `tx.onerror`/`tx.onabort` → reject; catch → `_idbSchreibfehler('Spiel-Snapshot', err)`, `false`. Doppelte id → ConstraintError → sichtbar, nie überschrieben
    - `idbSnapshotGetAll()`: readonly `getAll()`, Ergebnis nach `ts` aufsteigend sortiert (`(a.ts ?? a.id) - (b.ts ?? b.id)`); Fehler → `console.warn('[IDB] snapshots getAll:', err)` und `[]`
    - `idbSnapshotGet(id)`: readonly `get(id)` → Datensatz oder `null`; Fehler → warn + `null`
    - `idbSnapshotKeys()`: readonly `getAllKeys()` → Array; Fehler → warn + `[]`
    - Export-Schwanz um die vier Funktionen erweitert
  </behavior>
  <action>
    In persistence.js (deutsche Kommentare, Stil der Screenshot-Sektion): (1) Versionszeile: `const _IDB_VERSION = 3; // v3: Object-Store 'snapshots' (SCAN-08). Nie senken – IDB kennt kein Downgrade.` (2) Nach `SCREENSHOT_MIGRATION_KEY`: `// Snapshot-Store (SCAN-08): ein Datensatz je Spiel-Scan, id = Zeitstempel (Tool-Uhr). Add-only – nie überschreiben, nie automatisch löschen (Kernwert); Löschen mit Bestätigung folgt in Phase 6 (SCAN-11).` + `const _IDB_SNAPSHOTS = 'snapshots';`. (3) In `req.onupgradeneeded` als dritte Zeile: `if (!db.objectStoreNames.contains(_IDB_SNAPSHOTS)) db.createObjectStore(_IDB_SNAPSHOTS, { keyPath: 'id' });`. (4) Neue Sektion `// ── Snapshot-Store: ein Datensatz je Spiel-Scan (SCAN-08) ──` direkt nach `idbScreenshotKeys` und VOR `_migrateScreenshotsToStore` mit den vier Funktionen aus `<behavior>`; keine Lösch- oder Leer-Funktion anlegen. Bezeichner exakt: `async function idbSnapshotPut(record)`, `async function idbSnapshotGetAll()`, `async function idbSnapshotGet(id)`, `async function idbSnapshotKeys()`. (5) Export-Schwanz: `idbSnapshotPut, idbSnapshotGetAll, idbSnapshotGet, idbSnapshotKeys,` als eigene Zeile nach der Screenshot-Zeile. (6) Kopfkommentar der Datei um einen Satz zu v3 ergänzen (Snapshot-Store, SCAN-08). Keine weitere Änderung — insbesondere `_migrateScreenshotsToStore`, `idbSet`, die Migrations-IIFE und `_debounce` bleiben byte-identisch.
    GREEN: `npx vitest run tests/persistence-module.test.js tests/screenshot-migration.test.js` grün; `node --check persistence.js`; volle Suite `npm test` grün (jetzt 21 Dateien). Commit (nur persistence.js): `feat(05-01): IDB v3 mit Snapshot-Store snapshots (add-only, keine Lösch-API) — SCAN-08 Speicherhälfte`.
    Fürs SUMMARY: RED-/GREEN-Ausgaben, die geänderten Zeilen des Migrationstests, Hinweis „IDB-Bump ist costly: kein Downgrade, additiv“ und dass die Lösch-API bewusst fehlt (Phase 6).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/persistence-module.test.js tests/screenshot-migration.test.js tests/screenshot-store.test.js tests/idb-helpers.test.js tests/screenshot-export.test.js && node --check persistence.js && npm test</automated>
    <fails_when>Exit ≠ 0 oder `failed` — etwa weil `put` statt `add` genutzt wird (add-only-Test 3 kippt), der Quota-Wurf nicht abgefangen wird (Test 4 hängt/wirft), `getAll` nicht nach ts sortiert (Test 2), die Export-Liste nicht stimmt, der Migrationstest noch v2-Werte erwartet, oder eine bestehende Datei durch den Versionsbump rot wird</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(grep -Ec 'const _IDB_VERSION += +3;' persistence.js)" = 1 && test "$(grep -Fc "const _IDB_SNAPSHOTS = 'snapshots';" persistence.js)" = 1 && test "$(grep -Fc "createObjectStore(_IDB_SNAPSHOTS, { keyPath: 'id' })" persistence.js)" = 1 && test "$(grep -Fc 'objectStore(_IDB_SNAPSHOTS).add(' persistence.js)" = 1 && test "$(grep -Fc 'objectStore(_IDB_SNAPSHOTS).put(' persistence.js)" = 0 && test "$(grep -Fc 'objectStore(_IDB_SNAPSHOTS).delete(' persistence.js)" = 0 && test "$(grep -Fc 'objectStore(_IDB_SNAPSHOTS).clear(' persistence.js)" = 0 && test "$(grep -c 'idbSnapshotDelete' persistence.js)" = 0 && test "$(grep -c 'deleteDatabase' persistence.js)" = 0 && test "$(grep -c 'async function idbSnapshotPut(record)' persistence.js)" = 1 && test "$(grep -c 'async function idbSnapshotGetAll()' persistence.js)" = 1 && test "$(grep -c 'async function idbSnapshotGet(id)' persistence.js)" = 1 && test "$(grep -c 'async function idbSnapshotKeys()' persistence.js)" = 1 && test "$(grep -Fc "_idbSchreibfehler('Spiel-Snapshot'" persistence.js)" -ge 1 && diff <(git show f3973f6:persistence.js | awk '/^async function _migrateScreenshotsToStore/{f=1} f') <(awk '/^async function _migrateScreenshotsToStore/{f=1} f' persistence.js | sed '/idbSnapshotPut, idbSnapshotGetAll, idbSnapshotGet, idbSnapshotKeys,/d') && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "persistence.js " && echo SNAPSHOT-STORE-OK</automated>
    <fails_when>`SNAPSHOT-STORE-OK` fehlt — Version/Store/Bezeichner weichen ab, eine Lösch-/Leer-/put-Operation auf dem Snapshot-Store wurde eingebaut, der Datei-Teil ab `_migrateScreenshotsToStore` ist nicht mehr byte-identisch (außer der neuen Export-Zeile), oder der Commit enthält andere Pfade als persistence.js</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm test` → 21 Testdateien grün, 2 expected fail unverändert; `npx vitest run tests/loader-origin.test.js` grün
    - Zwei Commits dieses Tasks-Paars: `test(05-01)` (Task 2) → `feat(05-01)` (Task 3); Task 1 separat davor
    - `git status --short` leer (außer `.planning/`)
  </acceptance_criteria>
  <done>persistence.js öffnet v3 mit additivem Store `snapshots`; `idbSnapshotPut` ist add-only mit sichtbarem Fehlerpfad; `idbSnapshotGetAll/Get/Keys` vorhanden; keine Lösch-API; alle Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest-Prozess ↔ loader.js in vm-Sandbox | Erstmalige Ausführung von Spielcode-Adapter im Testprozess; Stubs müssen Timer/Netz/DOM neutralisieren |
| Tool-Fenster → IndexedDB `snapshots` | Neuer Store, additive Versionierung; Schreibpfad darf nie überschreiben, Fehler nie verschlucken |
| Zweiter Tool-Tab ↔ IDB-Versionswechsel v2→v3 | `onblocked`/`onversionchange` (Phase 4) greifen unverändert |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-01 | Tampering | Sandbox-Fixtures (Getter/Funktionen) messen nicht, was Plan 05-02 beweisen muss | high | mitigate | Zähler `hits` in jedem Getter/jeder Funktion/`Dynamic*`/Patching-Funktion; Smoke-Test 6/10 beweist, dass die Zähler bei echtem Zugriff hochgehen (Fixture-Selbsttest) |
| T-5-05 | Tampering | Snapshot-Store verliert/überschreibt Snapshots (Kernwert) | critical | mitigate | add-only (`add`, nie `put`), Test 3 (gleiche id → false, Datensatz unverändert), statisch 0× delete/clear/deleteDatabase auf dem Store, keine Lösch-API in dieser Phase |
| T-5-06 | Denial of Service | Quota beim Snapshot-Write bleibt still | high | mitigate | `_idbSchreibfehler('Spiel-Snapshot', err)` (Quota-Text „Speicher voll“), Test 4 mit gepatchtem `add`; `false` als Rückgabe für den Aufrufer (Plan 05-03 zeigt „NICHT gespeichert“) |
| T-5-07 | Denial of Service | Versionsbump v2→v3 bricht bestehende Tabs/Tests | medium | mitigate | Rein additiv; `onblocked`/`onversionchange` aus Phase 4 unverändert (Migrationstest auf v3/v4 nachgezogen, byte-identischer Datei-Teil ab `_migrateScreenshotsToStore`) |
| T-5-08 | Denial of Service | Loader-Sandbox hält den Vitest-Worker mit Retry-Timern am Leben | low | mitigate | Alle Retry-Auslöser gestubbt (bcModSdk, ServerSocket, Chat-Log-Element); Smoke-Test 5 beweist 0 offene Timer; `manualTimers` für Fälle ohne Stubs |
| T-5-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine Installationen in dieser Phase (RESEARCH: Package Legitimacy Audit nicht anwendbar) |
</threat_model>

<verification>
- `npx vitest run tests/loader-sandbox.test.js` → 11 passed; `npx vitest run tests/persistence-module.test.js tests/screenshot-migration.test.js` → grün; `npm test` → 21 Dateien grün (2 expected fail unverändert).
- Statische Gates: persistence.js Version 3, Store additiv, add-only, 0× Lösch-/Leer-Operation auf `snapshots`; `tests/loader-origin.test.js` und `loader.js` byte-identisch zu `f3973f6`.
- Drei Commits: test (Sandbox) → test (RED Snapshot-Store) → feat (persistence v3).
- VALIDATION.md 5-01-01..03: ✅ nach diesem Plan.
</verification>

<success_criteria>
- SCAN-13: als erfüllt dokumentiert (Artefakte), Zahlen als Fixture-Grundlage genutzt.
- Wave 0: loader.js läuft in-process; Seams `send`/`posts`/`hits`/`runUntil`/`waitFor` vorhanden.
- SCAN-08 (Speicher): v3-Store `snapshots`, add-only, sichtbare Fehler, keine Lösch-API; bestehende Suite grün.
</success_criteria>

## Artifacts this phase produces

Phasenweite Liste (Besitzer-Plan in Klammern; dieser Plan: 05-01):

**Neue Dateien**
- `tests/helpers/loaderSandbox.js` (05-01) — `makeLoaderSandbox`, `makeSyntheticAssets`, `makeBcModSdkMock`, `makeModGlobals`, `LOADER_TOOL_ORIGIN`, `GAME_ORIGIN`
- `tests/loader-sandbox.test.js` (05-01), `tests/game-inventory-enumerator.test.js` (05-02), `tests/game-scan-bridge.test.js` (05-03)
- `game-scan.js` (05-03) — Scan-Trigger, `GAME_INVENTORY_PROGRESS`/`GAME_INVENTORY_DATA`-Handler, Snapshot-Speicherung, Statuszeile

**Neue Symbole / Strukturen**
- IndexedDB `BCKonfigurator` Version 3, Object-Store `snapshots` (`keyPath 'id'`), `idbSnapshotPut`/`idbSnapshotGetAll`/`idbSnapshotGet`/`idbSnapshotKeys` (05-01)
- loader.js: `buildGameInventory(reqId, post)`, `window.__BCK_buildGameInventory`, Case `GET_GAME_INVENTORY`, Nachrichten `GAME_INVENTORY_PROGRESS {reqId, step, total, label}` / `GAME_INVENTORY_DATA {reqId, snapshot | err}` (05-02)
- Tweaks-Sektion `🔎 Spiel-Scan` mit Button „🔎 Spiel scannen“ und `#gameScanInfo`; Snapshot-Datensatz `{ id, ts, gameVersion, modCount, mods: [{name, version}], sizeBytes, inventory }` (05-03)
- `CORE_SCRIPTS` um `game-scan.js` (nach items.js) + `expandLoadOrder`-Regel für Nach-items-Skripte (05-03)

**Geänderte Dateien**: persistence.js (05-01), tests/persistence-module.test.js (05-01, 05-03), tests/screenshot-migration.test.js (05-01), loader.js (05-02), index.html (05-03, nur Einfügungen), docs/LOAD-ORDER.md (05-03), tests/helpers/loadScript.js (05-03)

<output>
Create `.planning/phases/05-gamecode-inventar/05-01-SUMMARY.md` when done — mit RED-/GREEN-Ausgaben, der Stub-Liste der Loader-Sandbox (und warum jeder Stub nötig ist), den geänderten Zeilen des Migrationstests, dem Reversibilitäts-Hinweis (IDB v3 costly, additiv) und der Commit-Liste.
</output>
