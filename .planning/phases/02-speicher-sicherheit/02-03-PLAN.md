---
phase: 02-speicher-sicherheit
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - tests/storage-estimate.test.js
  - items.js
  - index.html
autonomous: true
requirements: [STAB-03]
tags: [storage-estimate, tweaks-panel, ui, vm-sandbox, red-green]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Im Einstellungen-Panel (⚙️, `#tweaksPanel`) zeigt eine Sektion `📊 Speicher` den belegten und verfügbaren Browser-Speicher aus `navigator.storage.estimate()` als lesbaren Text, z. B. `12,0 MB von 1,00 GB belegt (1 %)`; ein Button `🔄 Aktualisieren` fragt neu ab (STAB-03, Erfolgskriterium 3)"
    - "`_speicherFormat(usage, quota)` ist eine reine Funktion: Bytes → `x,y MB` (unter 1 GiB, eine Nachkommastelle) bzw. `x,yz GB` (ab 1 GiB, zwei Nachkommastellen), Prozent gerundet; ab 90 % wird ` – Speicher fast voll!` angehängt; ohne Kontingent (`quota` 0/undefined) lautet der Text `<usage> belegt (Kontingent unbekannt)`"
    - "`_speicherZeigeStatus()` degradiert sichtbar statt zu werfen: fehlt `navigator`, `navigator.storage` oder `estimate` → `Speicher-API nicht verfügbar`; wirft `estimate()` → `Speicherabfrage fehlgeschlagen` (+ `console.warn('[Speicher] …')`); fehlt `#storageInfo` → stiller Return (RESEARCH: Feature-Detection ist Pflicht, Assumption A1)"
    - "Das Laden von items.js in der vm-Sandbox (ohne `navigator`) wirft weiterhin nicht — alle bestehenden Tests bleiben grün; der Init-Hook folgt der items.js-Konvention (`document.readyState !== 'loading'` → direkt, sonst `DOMContentLoaded`)"
    - "index.html erhält ausschließlich Einfügungen (eine Sektion nach `🔁 Automatisches Backup`, `git diff --numstat` Deletions = 0); keine `<script>`-Tag-Änderung, kein Build-Schritt (Orchestrator-Entscheidung 6: Markup nur, weil das Tweaks-Panel per Konvention statisch in index.html liegt — Vorbild `#bcBackupInfo`)"
    - "Vor der Implementierung ist `tests/storage-estimate.test.js` nachweislich rot (Funktionen fehlen, RED committet), danach grün (GREEN committet)"
    - "Annahme (STAB-03, Edge): `navigator.storage.estimate()` liefert `usage`/`quota` in Bytes (Web Storage API); im `file://`-Kontext oder in privaten Fenstern älterer Browser kann die API fehlen — dann zeigt das Panel die Fallback-Meldung, kein Absturz. Im echten Browser nur manuell prüfbar (end-of-phase, VALIDATION.md „Manual-Only“)"
  artifacts:
    - path: "tests/storage-estimate.test.js"
      provides: "STAB-03: it.each für `_speicherFormatBytes` (4 Fälle), `_speicherFormat` (3 Fälle), `_speicherZeigeStatus` (5 Fälle: ok, kein navigator, navigator ohne storage, estimate wirft, Laden ohne navigator) mit `navigator`-Stub per `extraGlobals` und `document.getElementById`-Override für `#storageInfo`"
      contains: "_speicherZeigeStatus"
    - path: "items.js"
      provides: "`_speicherFormatBytes(n)`, `_speicherFormat(usage, quota)`, `async function _speicherZeigeStatus()`, Init-Hook nach items.js-Konvention"
      contains: "async function _speicherZeigeStatus()"
    - path: "index.html"
      provides: "Tweaks-Panel-Sektion `📊 Speicher` mit `<div id=\"storageInfo\">` und Button `onclick=\"_speicherZeigeStatus()\"`"
      contains: "id=\"storageInfo\""
  key_links:
    - from: "index.html `#storageInfo` / Button `_speicherZeigeStatus()`"
      to: "items.js `_speicherZeigeStatus`"
      via: "items.js wird per `document.write` (index.html ~3074) nach dem Panel-Markup (~2456) geladen — Element existiert, Funktion ist globales `function`-Declaration (auf `window` erreichbar für `onclick`)"
      pattern: "_speicherZeigeStatus[(]([(])"
    - from: "tests/storage-estimate.test.js"
      to: "items.js `_speicherZeigeStatus` → `navigator.storage.estimate()`"
      via: "`loadScript(['items.js'], { navigator: { storage: { estimate: async () => ({ usage, quota }) } } })` — `navigator` ist kein Sandbox-Default, wird per `extraGlobals` gemischt (RESEARCH Test-Implikation, Planer-Probe: `typeof navigator` in der Sandbox = `undefined`)"
      pattern: "navigator:"
    - from: "tests/storage-estimate.test.js"
      to: "items.js `document.getElementById('storageInfo')`"
      via: "`ctx.document.getElementById = id => id === 'storageInfo' ? el : makeElementStub()` — der Sandbox-`document` ist ein mutierbares Objekt; ohne Override liefert jeder Aufruf einen frischen Stub, dessen `textContent` nicht inspizierbar wäre (01-REVIEW WR-02)"
      pattern: "getElementById"
  prohibitions:
    - requirement_id: STAB-03
      category: safety
      verification: judgment
      statement: "Kein Build-Schritt, keine neue Dependency; index.html nur mit Einfügungen im Tweaks-Panel-Body, keine Änderung an `<script>`-Tags, `document.write`-Reihenfolge oder bestehenden Sektionen"
    - requirement_id: STAB-03
      category: safety
      verification: judgment
      statement: "Keine eigene Speicherberechnung über `JSON.stringify(...).length` aller IDB-Schlüssel (RESEARCH „Don't Hand-Roll“) — ausschließlich `navigator.storage.estimate()` mit Feature-Detection"
    - requirement_id: STAB-03
      category: safety
      verification: judgment
      statement: "`git add items.js index.html` nur, nachdem `git status --short items.js index.html` VOR dem Edit leer war; nie `git add -A`, `git add .` oder `git commit -a`; Test (RED) und Implementierung (GREEN) in getrennten Commits"
    - requirement_id: STAB-03
      category: safety
      verification: judgment
      statement: "Keine Änderung an tests/helpers/loadScript.js — `navigator` kommt per `extraGlobals`, der Element-Override per Zuweisung auf `ctx.document`"
---

<objective>
Speicherstand ablesbar machen (STAB-03): eine reine Formatierungsfunktion `_speicherFormat(usage, quota)`, ein Anzeige-Helfer `_speicherZeigeStatus()` mit Feature-Detection und Fehler-Fallbacks in items.js, plus eine minimale Sektion `📊 Speicher` im bestehenden Tweaks-Panel nach dem Vorbild von `🔁 Automatisches Backup` (`#bcBackupInfo`). RED → GREEN wie in den Plänen davor: erst der rote Test (Funktionen existieren nicht), dann Implementierung und Markup in einem Commit.

Purpose: Erfolgskriterium 3 verlangt, dass der Nutzer belegten und verfügbaren Speicher im Tool sieht — heute gibt es keinen einzigen `navigator.storage`-Aufruf im Repo. Zusammen mit der Quota-Meldung aus `idbSet` (Plan 02-01) kann der Nutzer so rechtzeitig aufräumen, bevor Speicherungen fehlschlagen.
Output: `tests/storage-estimate.test.js` (neu), `items.js` (drei Funktionen + Init-Hook), `index.html` (eine Sektion, nur Einfügungen).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-speicher-sicherheit/02-RESEARCH.md
@.planning/phases/02-speicher-sicherheit/02-VALIDATION.md
@.planning/phases/02-speicher-sicherheit/02-02-SUMMARY.md

Planer-Probe (2026-09-13) — verbindliche Befunde:
1. `navigator` ist in der vm-Sandbox `undefined` (`typeof ctx.navigator` und `evalIn(ctx, 'typeof navigator')` beide `undefined`). `extraGlobals` wird in `makeSandbox` zuletzt gespreadet → `{ navigator: … }` kollidiert mit keinem Default.
2. `ctx.document` ist das in `makeSandbox` erzeugte Objekt; `getElementById` liefert bei JEDEM Aufruf ein frisches `makeElementStub()` (01-REVIEW WR-02) — für einen inspizierbaren `textContent` muss der Test `ctx.document.getElementById` überschreiben und für `'storageInfo'` ein festes Stub-Objekt zurückgeben.
3. `document.readyState` ist in der Sandbox `'complete'` → ein Init-Hook nach items.js-Konvention (~4253: `if (document.readyState!=='loading') … else document.addEventListener('DOMContentLoaded', …)`) ruft `_speicherZeigeStatus()` beim Laden sofort auf. Die Funktion MUSS daher ohne `navigator` gefahrlos sein (`typeof navigator === 'undefined'`-Guard), sonst bricht JEDER Test, der items.js lädt.
4. Vorbild-Markup index.html ~2449-2455 (`🔁 Automatisches Backup`): `<div><div class="tweaks-section-title">…</div><div class="tweaks-btn-group"><button class="tweaks-btn" onclick="…">…</button></div><div id="bcBackupInfo" style="font-size:.6875rem;color:var(--text3);margin-top:5px;line-height:1.5">–</div></div>`. Vorbild-JS index.html ~4168-4181 (`_bcBackupZeigeStatus`, inline `<script>` — für STAB-03 NICHT dort, sondern in items.js, damit die vm-Sandbox die Funktion laden kann).
5. items.js wird in index.html ~3074 per `document.write` geladen, das Tweaks-Panel-Markup steht davor (~2379-2500) → `#storageInfo` existiert, wenn items.js läuft.
6. Zeilennummern sind Stand vor Phase 2; nach 02-01/02-02 verschoben — immer per grep lokalisieren. `git status --short` muss vor den Edits leer sein.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 2 gesamt; Plan-Zuordnung in Klammern)

| Pfad | Symbole / Inhalt | Plan |
|------|------------------|------|
| `tests/idb-helpers.test.js` (erweitert) | describe `idbSet/idbGet Fehlerpfade (TEST-04, STAB-02)`: Quota, Nicht-Quota, Throttle, Throttle-Reset, idbGet-Fehler, fehlendes showStatus; `afterEach`-Restore | 02-01 |
| `tests/screenshot-sync.test.js` (neu) | STAB-01: `_syncLscgScreenshotToProfiles(mk, fp)` liest `mk\|fp`, kopiert zu allen Slots, überschreibt nie, Legacy-Pfad, Persistenz | 02-01 |
| `items.js` `_syncLscgScreenshotToProfiles(mk, fp)` | Signatur + `const key = fp ? (mk + '\|' + fp) : mk;` | 02-01 |
| `tests/delete-confirmation.test.js` (neu) | STAB-09: `DELETE_PATHS` (10 Pfade) × {abgebrochen, bestätigt}; statischer Quell-Audit | 02-02 |
| `tests/delete-consistency.test.js` (neu) | STAB-10: fünf Löschpfade ohne verwaiste Kopien; manuelle Bilder bleiben; Helfer-Verträge; IDB-Persistenz | 02-02 |
| `items.js` `confirm()` in fünf Pfaden; `_LSCG_PROFIL_KOPIEN_HINWEIS`; `_removeLscgScreenshotFromProfiles(fp, img)`, `_removeLscgScreenshotKeyFromProfiles(key)`, `_removeAllLscgScreenshotsFromProfiles()` + Aufrufe | STAB-09, STAB-10 | 02-02 |
| `tests/storage-estimate.test.js` (neu) | STAB-03: `_speicherFormatBytes` (4), `_speicherFormat` (3), `_speicherZeigeStatus` (5) mit `navigator`-Stub per `extraGlobals` und `getElementById`-Override | 02-03 |
| `items.js` `_speicherFormatBytes(n)`, `_speicherFormat(usage, quota)`, `_speicherZeigeStatus()` + Init-Hook | STAB-03 | 02-03 |
| `index.html` Tweaks-Panel-Sektion `📊 Speicher` (`#storageInfo`, Button `🔄 Aktualisieren`) | STAB-03 — einzige index.html-Änderung der Phase, nur Einfügungen | 02-03 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Speicheranzeige-Test schreiben und RED bestätigen — `tests/storage-estimate.test.js` (STAB-03)</name>
  <precondition>Plan 02-02 ist abgeschlossen und committet; `npm test` grün; `git status --short` leer</precondition>
  <files>tests/storage-estimate.test.js</files>
  <read_first>tests/helpers/loadScript.js Zeilen 18-25 (`makeElementStub` Export) und 90-140 (`makeSandbox`: `document`-Objekt, `extraGlobals`-Spread); .planning/phases/02-speicher-sicherheit/02-RESEARCH.md Abschnitt „STAB-03: Speicherplatz-Anzeige — Ort und Vorbild" (Codevorschlag + Test-Implikation); tests/idb-helpers.test.js (Stil, `ctx.showStatus`-Override als Muster für Property-Overrides)</read_first>
  <action>
    `tests/storage-estimate.test.js` (neu, ESM): `import { describe, it, expect } from 'vitest'`, `import { loadScript, makeElementStub } from './helpers/loadScript.js'`.

    Helfer:
    - `NAV_OK = { storage: { estimate: async () => ({ usage: 12582912, quota: 1073741824 }) } }` (12 MiB von 1 GiB → 1 %).
    - `NAV_FAIL = { storage: { estimate: async () => { throw new Error('estimate kaputt'); } } }`.
    - `captureInfo(ctx)`: `const el = makeElementStub(); ctx.document.getElementById = (id) => id === 'storageInfo' ? el : makeElementStub(); return el;` (Befund 2 — Override auf dem Sandbox-`document`, kein Loader-Change).

    `describe('Speicheranzeige (STAB-03)')`:
    1. `it.each([[0, '0,0 MB'], [1048576, '1,0 MB'], [12582912, '12,0 MB'], [1073741824, '1,00 GB']])('_speicherFormatBytes(%i) → %s', (n, erwartet) => { const ctx = loadScript(['items.js']); expect(ctx._speicherFormatBytes(n)).toBe(erwartet); })` — Komma als Dezimaltrenner (deutsche UI), MB mit einer, GB mit zwei Nachkommastellen.
    2. `it('_speicherFormat: belegt/verfügbar/Prozent')`: `ctx._speicherFormat(12582912, 1073741824)` → `'12,0 MB von 1,00 GB belegt (1 %)'`.
    3. `it('_speicherFormat: ab 90 % Warnhinweis')`: `ctx._speicherFormat(966367642, 1073741824)` → `'921,6 MB von 1,00 GB belegt (90 %) – Speicher fast voll!'`.
    4. `it('_speicherFormat: ohne Kontingent')`: `ctx._speicherFormat(5242880, 0)` → `'5,0 MB belegt (Kontingent unbekannt)'`; `ctx._speicherFormat(5242880, undefined)` → gleicher Text.
    5. `it('_speicherZeigeStatus schreibt das Ergebnis von navigator.storage.estimate() nach #storageInfo')`: `const ctx = loadScript(['items.js'], { navigator: NAV_OK }); const el = captureInfo(ctx); await ctx._speicherZeigeStatus(); expect(el.textContent).toBe('12,0 MB von 1,00 GB belegt (1 %)');`.
    6. `it('ohne navigator: Fallback-Text, keine Exception')`: `loadScript(['items.js'])` (kein navigator); `captureInfo`; `await expect(ctx._speicherZeigeStatus()).resolves.toBeUndefined()`; `el.textContent === 'Speicher-API nicht verfügbar'`.
    7. `it('navigator ohne storage.estimate: Fallback-Text')`: `loadScript(['items.js'], { navigator: {} })`; gleicher Text wie 6.
    8. `it('estimate() wirft: sichtbare Fehlermeldung statt Exception')`: `loadScript(['items.js'], { navigator: NAV_FAIL })`; `await expect(ctx._speicherZeigeStatus()).resolves.toBeUndefined()`; `el.textContent === 'Speicherabfrage fehlgeschlagen'` (das `console.warn` der Funktion erscheint in der Testausgabe — erwartet, kein Fehler).
    9. `it('Laden von items.js ohne navigator wirft nicht (Init-Hook ist guarded)')`: `expect(() => loadScript(['items.js'])).not.toThrow()`; `expect(typeof ctx._speicherZeigeStatus).toBe('function')`.

    RED bestätigen: `npx vitest run tests/storage-estimate.test.js` MUSS rot sein — erwartet: alle Fälle bis auf den `not.toThrow()`-Teil von 9 rot mit `TypeError: ctx._speicher… is not a function` (Fall 9 rot wegen `typeof … === 'function'`). Ist die Datei unerwartet grün: STOPP (Funktionen existieren bereits?).

    Commit des roten Tests: `git add tests/storage-estimate.test.js`; `git diff --cached --name-only` = exakt dieser Pfad; Message `test(02-03): add failing storage estimate panel test (STAB-03)`. Vorher `git status --short items.js index.html` prüfen und im SUMMARY festhalten (muss leer sein).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/storage-estimate.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -q "_speicherZeigeStatus" "$OUT" && grep -Eq "Tests +[0-9]+ failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` wird nicht ausgegeben — Vitest endete mit Exit 0 (Funktionen existieren bereits oder der Test ruft sie nicht auf), oder `_speicherZeigeStatus`/`failed` fehlen in der Ausgabe</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "navigator:" tests/storage-estimate.test.js` >= 3 (NAV_OK, NAV_FAIL, leeres Objekt); `grep -c "makeElementStub" tests/storage-estimate.test.js` >= 3 (Import + captureInfo)
    - `grep -c "_speicherFormatBytes" tests/storage-estimate.test.js` >= 2; `grep -c "_speicherFormat(" tests/storage-estimate.test.js` >= 4; `grep -c "_speicherZeigeStatus" tests/storage-estimate.test.js` >= 5
    - `grep -Fc "12,0 MB von 1,00 GB belegt (1 %)" tests/storage-estimate.test.js` >= 2; `grep -Fc "Speicher fast voll!" tests/storage-estimate.test.js` >= 1; `grep -Fc "Kontingent unbekannt" tests/storage-estimate.test.js` >= 1; `grep -Fc "Speicher-API nicht verfügbar" tests/storage-estimate.test.js` >= 2; `grep -Fc "Speicherabfrage fehlgeschlagen" tests/storage-estimate.test.js` >= 1
    - `grep -c "storageInfo" tests/storage-estimate.test.js` >= 1
    - `test "$(grep -v '^\s*//' tests/storage-estimate.test.js | grep -c '\.only(')" = 0`
    - `git status --short items.js index.html` gibt nichts aus
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/storage-estimate.test.js`
  </acceptance_criteria>
  <done>Zwölf Testfälle (4 + 3 + 5) vorhanden; Lauf nachweislich rot, weil die drei Funktionen fehlen; roter Test committet; items.js und index.html unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `_speicherFormatBytes`/`_speicherFormat`/`_speicherZeigeStatus` in items.js + Sektion `📊 Speicher` in index.html, GREEN bestätigen, committen (STAB-03)</name>
  <precondition>`git status --short items.js index.html` gibt VOR dem Edit nichts aus — sonst STOPP und eskalieren</precondition>
  <reversibility rating="reversible">Rein additive Anzeige (drei Funktionen, eine Markup-Sektion); liest nur `navigator.storage.estimate()`, schreibt keine Daten; Revert per `git revert` ohne Nebenwirkung.</reversibility>
  <files>items.js, index.html</files>
  <read_first>items.js Zeile mit `function bcSpeichernJetzt()` (per grep; Einfügeort direkt danach, im Persistenz-Block) und die Init-Zeile `try { if (document.readyState!=='loading') _applyGroupUI('items'); else …` (per `grep -n "readyState!=='loading'" items.js` — Konventions-Vorbild für den Hook); index.html Block `🔁 Automatisches Backup` (per `grep -n 'id="bcBackupInfo"' index.html`, 8 Zeilen davor bis 2 danach lesen — Einfügeort und Markup-Vorbild); tests/storage-estimate.test.js (aus Task 1)</read_first>
  <behavior>
    - Test 1-4: Byte-Formatierung MB/GB mit Komma, Prozent gerundet, Warnhinweis ab 90 %, Text ohne Kontingent
    - Test 5: mit `navigator`-Stub landet der formatierte Text in `#storageInfo.textContent`
    - Test 6-8: ohne `navigator`, ohne `storage.estimate`, bei werfendem `estimate()` → jeweils der Fallback-Text, nie eine Exception (Promise resolved)
    - Test 9: Laden von items.js ohne `navigator` wirft nicht; alle bestehenden Testdateien bleiben grün (`npm test`)
  </behavior>
  <action>
    1. items.js — direkt nach `function bcSpeichernJetzt() { … }` einen Block mit Sektionskommentar `// ── Speicherplatz-Anzeige (STAB-03) ──…` einfügen:
       - `function _speicherFormatBytes(n)`: `n` per `Number(n) || 0` normalisieren; ab `1073741824` (1 GiB) → `(n / 1073741824).toFixed(2)` + `' GB'`, sonst `(n / 1048576).toFixed(1)` + `' MB'`; Dezimalpunkt per `.replace('.', ',')` durch Komma ersetzen (deutsche UI; kein `toLocaleString`, damit die Ausgabe umgebungsunabhängig deterministisch bleibt).
       - `function _speicherFormat(usage, quota)`: beide per `Number(x) || 0`; ohne `quota` → `_speicherFormatBytes(usage) + ' belegt (Kontingent unbekannt)'`; sonst `pct = Math.round(usage / quota * 100)` und `_speicherFormatBytes(usage) + ' von ' + _speicherFormatBytes(quota) + ' belegt (' + pct + ' %)'`, bei `pct >= 90` zusätzlich `' – Speicher fast voll!'` (Gedankenstrich U+2013 wie in den bestehenden Statusmeldungen).
       - `async function _speicherZeigeStatus()`: `el = document.getElementById('storageInfo')`, ohne `el` → Return; Feature-Detection `typeof navigator === 'undefined' || !navigator.storage || typeof navigator.storage.estimate !== 'function'` → `el.textContent = 'Speicher-API nicht verfügbar'`, Return; sonst `try { const { usage, quota } = await navigator.storage.estimate(); el.textContent = _speicherFormat(usage, quota); } catch (e) { console.warn('[Speicher] estimate:', e); el.textContent = 'Speicherabfrage fehlgeschlagen'; }`. Kommentar: Guard ist Pflicht, weil die vm-Sandbox der Tests kein `navigator` hat und der Hook beim Laden feuert (Befund 3).
       - Init-Hook nach items.js-Konvention (Vorbild `_applyGroupUI`-Zeile): `try { if (document.readyState !== 'loading') _speicherZeigeStatus(); else document.addEventListener('DOMContentLoaded', () => setTimeout(_speicherZeigeStatus, 1500)); } catch (e) {}` — in der Sandbox (`readyState` `'complete'`) läuft der Direktaufruf und trifft den Guard; im Browser (items.js läuft während des Parsens, `readyState` `'loading'`) feuert der Listener wie beim Backup-Panel mit 1,5 s Verzögerung.
       Keine Umformatierung anderer Stellen, Datei bleibt LF. `node --check items.js` → Exit 0.

    2. index.html — per Edit-Tool mit exaktem old_string die zwei Zeilen `<div id="bcBackupInfo" …>–</div>` + `  </div>` (Ende des Backup-Blocks) um eine NEUE Sektion direkt danach ergänzen (Markup-Vorbild Befund 4, 7 Zeilen + Leerzeile):
       `<div>` / `<div class="tweaks-section-title">📊 Speicher</div>` / `<div class="tweaks-btn-group">` / `<button class="tweaks-btn" onclick="_speicherZeigeStatus()" title="Belegten und verfügbaren Browser-Speicher neu abfragen">🔄 Aktualisieren</button>` / `</div>` / `<div id="storageInfo" style="font-size:.6875rem;color:var(--text3);margin-top:5px;line-height:1.5">–</div>` / `</div>` — Einrückung wie die Nachbarsektionen (zwei bzw. vier Leerzeichen). Nichts löschen, nichts umstellen: `git diff --numstat index.html` → Deletions `0`, Insertions 7-9.

    3. GREEN: `npx vitest run tests/storage-estimate.test.js` → 12 passed. Dann `npm test` → gesamte Suite grün (beweist Befund 3: der Hook wirft in keiner Testdatei).

    4. Commit — `git add items.js index.html` (genau diese zwei Pfade; der Test ist bereits in Task 1 committet); `git diff --cached --name-only | sort` = `index.html`, `items.js`; Message `feat(02-03): show storage usage via navigator.storage.estimate() in settings panel (STAB-03)`.

    5. Nachweis für das SUMMARY: `git show --stat --format= HEAD`, GREEN-Ausgabe, `git diff --numstat 0d9ddbe HEAD -- index.html` (Deletions 0) und der Hinweis, dass die Live-Anzeige im Browser nur manuell prüfbar ist (end-of-phase Human-Check unten).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/storage-estimate.test.js && node --check items.js && npm test</automated>
    <fails_when>non-zero exit (Vitest rot oder `node --check` SyntaxError), `failed` in der `Tests`-Summary-Zeile, oder weniger als `12 passed` für tests/storage-estimate.test.js — insbesondere wenn eine andere Testdatei durch einen ungeguardeten Init-Hook (`ReferenceError: navigator is not defined`) rot wird</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(git diff --numstat 0d9ddbe HEAD -- index.html | awk '{print $2}')" = "0" && test "$(grep -Fc 'id="storageInfo"' index.html)" = 1 && test "$(grep -Fc 'onclick="_speicherZeigeStatus()"' index.html)" = 1 && test "$(git show --name-only --format= HEAD | sort | tr '\n' ' ')" = "index.html items.js " && echo MARKUP-OK</automated>
    <fails_when>`MARKUP-OK` wird nicht ausgegeben — index.html hat gegenüber der Phase-Baseline Löschungen, `#storageInfo` oder der Button fehlen bzw. sind doppelt, oder der HEAD-Commit enthält andere Pfade als index.html und items.js</fails_when>
    <human-check>Tool im Browser öffnen (GitHub Pages oder lokal per HTTP), ⚙️ Einstellungen öffnen: Sektion `📊 Speicher` zeigt nach ~1,5 s einen Text wie `123,4 MB von 60,00 GB belegt (0 %)`; Klick auf `🔄 Aktualisieren` frischt den Text auf. Vergleich mit DevTools → Application → Storage (Größenordnung muss übereinstimmen). Manual-Only laut VALIDATION.md, end-of-phase.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "function _speicherFormatBytes(n)" items.js` = 1; `grep -Fc "function _speicherFormat(usage, quota)" items.js` = 1; `grep -Fc "async function _speicherZeigeStatus()" items.js` = 1
    - `grep -Fc "navigator.storage.estimate()" items.js` = 1; `grep -Fc "Speicher-API nicht verfügbar" items.js` = 1; `grep -Fc "Speicherabfrage fehlgeschlagen" items.js` = 1; `grep -Fc "Kontingent unbekannt" items.js` = 1; `grep -Fc "Speicher fast voll!" items.js` = 1
    - `awk '/^async function _speicherZeigeStatus[(]/{f=1} f&&/typeof navigator === .undefined./{print "OK"; exit} f&&/^}/{exit}' items.js` gibt `OK` aus (Guard im Funktionskörper)
    - `grep -Fc "_speicherZeigeStatus" items.js` >= 3 (Definition, Direktaufruf, Listener-Aufruf) und `grep -c "readyState !== 'loading'" items.js` >= 1
    - `grep -Fc 'id="storageInfo"' index.html` = 1; `grep -Fc "📊 Speicher" index.html` = 1; `git diff --numstat 0d9ddbe HEAD -- index.html | awk '{print $2}'` = `0`
    - `node --check items.js` → Exit 0; `npx vitest run tests/storage-estimate.test.js` → `12 passed`; `npm test` → Exit 0
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` = `index.html` und `items.js`; `git status --short` ist leer; `git log --format=%h 0d9ddbe..HEAD -- loader.js bot-data.js bot-ui.js bot-engine.js outfit-import.js` gibt nichts aus
  </acceptance_criteria>
  <done>Drei Funktionen und Init-Hook in items.js, Sektion im Tweaks-Panel; Speicheranzeige-Test grün (11 passed), volle Suite grün, `node --check` grün; Commit enthält genau items.js und index.html (index.html nur Einfügungen); Human-Check für die Live-Anzeige ist für die End-of-Phase-Verifikation dokumentiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-API `navigator.storage.estimate()` → Tool | Read-only Abfrage; kann fehlen (`file://`, private Fenster) oder rejecten — darf nie den Ladepfad von items.js brechen |
| items.js Init-Hook → vm-Sandbox der Tests | Der Hook läuft beim Laden in JEDER Testdatei, die items.js lädt; ohne Guard fällt die gesamte Suite |
| index.html Markup → `onclick="_speicherZeigeStatus()"` | Statisches Markup, keine Nutzereingabe interpoliert |
| Git-Arbeitsbaum → Repository | Zwei Produktionsdateien in einem Commit, index.html nur Einfügungen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-09 | Denial of Service | items.js Init-Hook `_speicherZeigeStatus()` beim Laden | high | mitigate | `typeof navigator === 'undefined'`-Guard + `try/catch` um den Hook (Acceptance-awk-Gate); Test 9 und `npm test` beweisen, dass kein Ladepfad bricht |
| T-2-10 | Information Disclosure | `#storageInfo` zeigt Speichermengen | low | accept | Nur Byte-Zahlen des eigenen Browsers, Einzelnutzer-Tool, keine Weitergabe |
| T-2-11 | Tampering | index.html Tweaks-Panel-Edit | medium | mitigate | Edit-Tool mit exaktem Anker; `git diff --numstat` Deletions = 0 gegen Phase-Baseline `0d9ddbe`; `#storageInfo` genau 1× |
| T-2-06 | Tampering | Git-Staging von items.js / index.html | high | mitigate | Precondition `git status --short items.js index.html` leer; `git add` nur dieser zwei Pfade; `git log 0d9ddbe..HEAD -- <andere Produktionsdateien>` leer |
| T-2-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine neuen Installationen in dieser Phase; `navigator.storage` ist native Browser-API |
</threat_model>

<verification>
- `npx vitest run tests/storage-estimate.test.js` → 12 passed; `npm test` → gesamte Suite grün (alle Phase-1- und Phase-2-Dateien).
- `node --check items.js` grün; `git diff --numstat 0d9ddbe HEAD -- index.html` → Deletions 0; genau zwei Commits dieses Plans (test RED → feat GREEN).
- Phase-Gate (VALIDATION.md): `npm test` grün, `node --check items.js` grün; Manual-Only-Checks (Speicherpanel im Browser, Confirm-Dialoge) als end-of-phase Human-Checks im SUMMARY aufgelistet.
</verification>

<success_criteria>
- Erfolgskriterium 3: Der Nutzer kann belegten und verfügbaren Speicher (`navigator.storage.estimate()`) im Einstellungen-Panel ablesen und per Button aktualisieren; Fallbacks bei fehlender API sind sichtbar statt still.
- Orchestrator-Entscheidung 6: Panel nach dem Backup-Status-Vorbild; Logik in items.js (testbar), index.html nur um eine Sektion ergänzt (keine Löschungen); Produktion bleibt reine `<script>`-Auslieferung ohne Build.
</success_criteria>

<output>
Create `.planning/phases/02-speicher-sicherheit/02-03-SUMMARY.md` when done — inkl. RED-Ausgabe aus Task 1, GREEN-Ausgabe, `git show --stat` des feat-Commits, `git diff --numstat 0d9ddbe HEAD -- index.html` und der Liste der end-of-phase Human-Checks (Speicherpanel im Browser; Confirm-Dialoge aus Plan 02-02).
</output>
