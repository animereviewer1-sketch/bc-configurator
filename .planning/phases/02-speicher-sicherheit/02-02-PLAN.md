---
phase: 02-speicher-sicherheit
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/delete-confirmation.test.js
  - tests/delete-consistency.test.js
  - items.js
autonomous: true
requirements: [STAB-09, STAB-10]
tags: [delete-confirmation, screenshots, lscg, profile-screenshots, mbs-wheel, vm-sandbox, red-green]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Jeder UI-erreichbare Lösch-Pfad für Bilder, einzelne Outfits und Outfit-Versionen ruft `confirm()` genau einmal auf, bevor er etwas entfernt: `removeProfileScreenshot`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `mbsWheelDeleteShot`, der Wheel-Zweig von `deleteOsScreenshotFromLb` (neu) sowie `deleteLscgVersion`, `clearAllProfileScreenshots`, `clearAllLscgScreenshots`, `clearAllLscgOutfits`, `mbsWheelClearAllShots`, `mbsWheelClearAll`, `deleteProfile` (bereits vorhanden). Liefert `confirm()` `false`, sind `LSCG_DB`, `LSCG_SCREENSHOTS`, `PROFILE_SCREENSHOTS` und `_mbsWheelShots` danach byte-identisch zum Vorzustand (STAB-09, Erfolgskriterium 4, Orchestrator-Entscheidung 2)"
    - "Ein statischer Quell-Audit im Test beweist: jede Zeile in items.js, die `delete LSCG_SCREENSHOTS[…]`, `delete PROFILE_SCREENSHOTS[…]`, `delete _mbsWheelShots[…]`, `delete LSCG_DB[…]` ausführt oder einen dieser vier Stores auf `{}` zurücksetzt, liegt in einer Funktion mit `confirm(` oder auf der dokumentierten Ausnahmeliste (Umbenennungen `markAllProfilesOld`/`profileRename`, Konsolen-Reparatur `repairOsOutfitCode`, Aufräum-Helfer `_removeLscgScreenshotFromProfiles`) — kein anderer Code-Pfad entfernt gespeicherte Scan-Daten (STAB-09)"
    - "Nach einem bestätigten Löschen eines LSCG-Bildes (`deleteLscgVersion`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `clearAllLscgScreenshots`, `clearAllLscgOutfits`) existiert keine byte-identische Kopie dieses Bildes mehr unter einem Profil-Slot, den `_lscgFpMap` einem Fingerprint des betroffenen Members zuordnet — weder im RAM noch nach `bcSpeichernJetzt()` unter `BC_PROFILE_SCREENSHOTS_v1` / `BC_LSCG_SCREENSHOTS_v1` in IDB (STAB-10, Erfolgskriterium 5)"
    - "Manuell hochgeladene Profil-Bilder (`uploadProfileScreenshot`) und Profil-Bilder, die nicht zum gelöschten LSCG-Bild identisch sind, bleiben bei jeder LSCG-Löschung erhalten — der Helfer entfernt ausschließlich Einträge mit `PROFILE_SCREENSHOTS[k] === img` (Kernwert: nichts geht als Nebeneffekt verloren)"
    - "Die `confirm()`-Texte der fünf LSCG-Bild-Löschpfade nennen die Konsequenz `Synchronisierte Kopien in Profil-Bildern werden mit entfernt.` (RESEARCH Pitfall 5)"
    - "Vor den Fixes sind `tests/delete-confirmation.test.js` (fünf confirm-lose Pfade + statischer Audit) und `tests/delete-consistency.test.js` (verwaiste Kopien) nachweislich rot (RED committet); nach Task 2 ist die Confirmation-Datei grün, nach Task 3 beide (GREEN committet)"
    - "Annahme (STAB-09, Edge): Ein `confirm()` pro Vorgang deckt die gesamte Batch-Operation der `clearAll*`-Funktionen ab; `false` verhindert den gesamten Vorgang, `true` führt ihn vollständig aus — ein Abbruch „mitten im Batch“ ist strukturell ausgeschlossen, weil alle Änderungen synchron im RAM erfolgen und erst danach gebündelt gespeichert werden"
    - "Annahme (STAB-10, Edge): Die Identitätsprüfung `=== img` ist das Unterscheidungsmerkmal zwischen synchronisierter Kopie und manuellem Upload. Ein manuell hochgeladenes Bild, das byte-identisch zum LSCG-Bild ist, würde mit entfernt — praktisch unmöglich (unterschiedliche Quelle, Skalierung, JPEG-Kodierung) und in jedem Fall dieselbe Bildinformation; im SUMMARY dokumentieren"
    - "Annahme (STAB-09, Scope): `LSCG_MAX_VERSIONS = 1500` (items.js ~8162, Trim per `versions.slice(-1500)` beim Scan-Import ~8506 und ~10098) ist eine bewusste Obergrenze pro Member und bleibt unverändert; `enrichProfileNamesWithIDs()` (Umbenennung ohne Screenshot-Mitzug) bleibt gemäß Orchestrator-Entscheidung 4 unverändert — beide im SUMMARY als bekannte, nicht geänderte Pfade festhalten"
  artifacts:
    - path: "tests/delete-confirmation.test.js"
      provides: "STAB-09: `DELETE_PATHS`-Tabelle (10 Pfade) × {confirm→false: nichts gelöscht, confirm 1×; confirm→true: Ziel entfernt, confirm 1×}; statischer Quell-Audit mit `CONFIRM_REQUIRED`/`ALLOWED_WITHOUT_CONFIRM`"
      contains: "deleteOsScreenshotFromLb"
    - path: "tests/delete-consistency.test.js"
      provides: "STAB-10: fünf Löschpfade ohne verwaiste Kopien, manuelle Bilder bleiben, Cross-Version-Kopie (`Anna_v2c`), Helfer-Verträge, IDB-Persistenz nach bcSpeichernJetzt()"
      contains: "_removeLscgScreenshotKeyFromProfiles"
    - path: "items.js"
      provides: "`confirm()` in fünf bisher confirm-losen Pfaden; `_LSCG_PROFIL_KOPIEN_HINWEIS`; `_removeLscgScreenshotFromProfiles(fp, img)`, `_removeLscgScreenshotKeyFromProfiles(key)`, `_removeAllLscgScreenshotsFromProfiles()` + Aufrufe in fünf LSCG-Löschpfaden"
      contains: "function _removeLscgScreenshotFromProfiles(fp, img)"
  key_links:
    - from: "tests/delete-confirmation.test.js / tests/delete-consistency.test.js"
      to: "items.js `confirm(…)`-Aufrufe"
      via: "`loadScript(['items.js'], { confirm: spy })` — `confirm` ist kein vordefiniertes Sandbox-Global, wird per `extraGlobals` eingespeist und zur Aufrufzeit als freier Bezeichner über das Sandbox-Global aufgelöst (Planer-Probe: funktioniert, ohne Stub `ReferenceError`)"
      pattern: "confirm:"
    - from: "items.js `deleteLscgVersion` / `deleteOsScreenshot` / `deleteOsScreenshotKey` / `clearAllLscgScreenshots` / `clearAllLscgOutfits`"
      to: "items.js `_removeLscgScreenshotKeyFromProfiles(key)` → `_removeLscgScreenshotFromProfiles(fp, img)` → `PROFILE_SCREENSHOTS` + `_saveProfileScreenshots()`"
      via: "Aufruf VOR `delete LSCG_SCREENSHOTS[key]` bzw. VOR `LSCG_SCREENSHOTS = {}` / `_lscgFpMap = {}` — der Helfer braucht das Bild (Identitätsvergleich) und die Fingerprint-Map"
      pattern: "_removeLscgScreenshotKeyFromProfiles[(](key[(]);"
    - from: "items.js `_removeLscgScreenshotKeyFromProfiles`"
      to: "items.js `_syncLscgScreenshotToProfiles` (Plan 02-01)"
      via: "Spiegelbild: dieselbe Menge Fingerprints (Schlüssel-`fp` + alle `v.fingerprint` von `LSCG_DB[mk].versions`), dieselben Slots aus `_lscgFpMap[fp]`; Sync kopiert, Remove entfernt identische Kopien"
      pattern: "_lscgFpMap[(][fp[(]]"
  prohibitions:
    - requirement_id: STAB-09
      category: safety
      verification: judgment
      statement: "Kein Lösch-Pfad wird entfernt oder deaktiviert — der Nutzer soll weiterhin löschen können, nur nach Bestätigung (PROJECT.md Key Decision „Löschen bleibt möglich, aber nur manuell mit Bestätigung“)"
    - requirement_id: STAB-10
      category: safety
      verification: judgment
      statement: "Der Aufräum-Helfer entfernt nie einen `PROFILE_SCREENSHOTS`-Eintrag, der nicht byte-identisch (`===`) zum gelöschten LSCG-Bild ist; keine Löschung nach Slot-Name allein"
    - requirement_id: STAB-09
      category: safety
      verification: judgment
      statement: "Einzige Produktionsdatei dieses Plans ist items.js; keine Änderung an index.html (die Buttons `pmodRemoveBtn` / `os-card-del` / Lightbox rufen weiterhin dieselben Funktionsnamen auf), loader.js, bot-*.js, outfit-import.js"
    - requirement_id: STAB-09
      category: safety
      verification: judgment
      statement: "`git add items.js` nur, nachdem `git status --short items.js` VOR dem Edit leer war; nie `git add -A`, `git add .` oder `git commit -a`; Tests (RED) und Fixes (GREEN) in getrennten Commits"
    - requirement_id: STAB-10
      category: safety
      verification: judgment
      statement: "Keine Änderung an `_syncLscgScreenshotToProfiles` (Plan 02-01), an `LSCG_MAX_VERSIONS`, an `enrichProfileNamesWithIDs` oder an `window.repairOsOutfitCode`; kein Build-Schritt, keine neue Dependency, keine Änderung an tests/helpers/loadScript.js"
---

<objective>
Löschen von Scan-Daten ist nur noch nach Bestätigung möglich und räumt konsistent auf: (1) `confirm()` in den fünf verifizierten confirm-losen Pfaden — `removeProfileScreenshot` (Profil-Modal „✕ Entfernen"), `deleteOsScreenshot`/`deleteOsScreenshotKey` (🗑 auf jeder Versions-Karte und Lightbox), `mbsWheelDeleteShot` (Orchestrator-Entscheidung 2) und dem in der Planer-Probe zusätzlich gefundenen Wheel-Zweig von `deleteOsScreenshotFromLb`; (2) ein zentraler Aufräum-Helfer `_removeLscgScreenshotFromProfiles(fp, img)` (Orchestrator-Entscheidung 5, Spiegelbild von `_syncLscgScreenshotToProfiles`), der von jedem LSCG-Löschpfad über `_removeLscgScreenshotKeyFromProfiles(key)` / `_removeAllLscgScreenshotsFromProfiles()` aufgerufen wird und ausschließlich byte-identische Kopien aus `PROFILE_SCREENSHOTS` entfernt. Beides RED → GREEN: beide Testdateien zuerst rot committen, dann STAB-09-Fix (Confirmation-Datei grün), dann STAB-10-Fix (beide grün).

Purpose: Erfolgskriterium 4 („nur über eine explizite Nutzeraktion mit Bestätigungsdialog") ist heute an den am häufigsten geklickten Lösch-Buttons verletzt; Erfolgskriterium 5 („keine verwaisten Einträge") wird von `deleteLscgVersion` und beiden `clearAllLscg*` verletzt, weil kein Gegenstück zum Sync existiert (RESEARCH Root Cause). Der Kernwert verlangt beides im Code und im Test.
Output: `tests/delete-confirmation.test.js`, `tests/delete-consistency.test.js` (beide neu), `items.js` (fünf `confirm()`-Einbauten, eine Konstante, drei Helfer, fünf Aufrufstellen).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-speicher-sicherheit/02-RESEARCH.md
@.planning/phases/02-speicher-sicherheit/02-VALIDATION.md
@.planning/phases/02-speicher-sicherheit/02-01-SUMMARY.md

Planer-Probe (2026-09-13, Node v24.12.0, `loadScript(['items.js'], { confirm: spy })`, `ctx.showStatus = spy`) — verbindliche Befunde:
1. Alle Lösch-Funktionen laufen in der Sandbox ohne Exception, wenn `confirm` per `extraGlobals` gestellt ist: `deleteOsScreenshotKey('123|FP1')` löschte OHNE confirm-Aufruf; `removeProfileScreenshot('Anna_v2')` löschte OHNE confirm; `deleteLscgVersion('123', 1)` rief confirm 1× auf; `clearAllLscgOutfits()` rief confirm 1× auf; `deleteOsScreenshotFromLb()` mit `_osLightboxWheelFp = 'WFP'` löschte `_mbsWheelShots['WFP']` OHNE confirm (in der RESEARCH-Tabelle nicht als eigener Verstoß gelistet — der Wheel-Zweig löscht inline, nicht über `mbsWheelDeleteShot`).
2. `mbsWheelDeleteShot(5, 0)` funktioniert nur mit vollständigem Fixture `_mbsWheelData = [{ memberNumber: 5, name: 'Bob', outfits: [{ name: 'Outfit A', items: [{ group: 'G', asset: 'A' }] }] }]` — ohne `outfits[].name` wirft `_buildWheelMemberHtml` (`o.name[0]`) beim Re-Render. Fingerprint dazu: `_mbsOutfitFp(o)` = `'G:A'`. Mit Fixture: löscht `_mbsWheelShots['G:A']` OHNE confirm.
3. Alle betroffenen Zustände sind Top-Level-`let` → nur per `evalIn` setzen/lesen: `LSCG_DB` (8163), `LSCG_SCREENSHOTS` (7499), `_lscgFpMap` (8169), `PROFILE_SCREENSHOTS` (560), `_mbsWheelShots` (9359), `_mbsWheelData` (9183), `_osLightboxWheelFp`/`_osLightboxKey`/`_osLightboxMk` (10349-10351). `_lscgFpMap[fp]` enthält Profil-Slot-Namen (Schlüssel von `PROFILE_SCREENSHOTS`), rebuilt aus `_lscgSlots` (`_rebuildFpMapFromSlots`, 8449).
4. Enclosing-Funktion jeder Lösch-Zeile (per awk über `^(async )?function NAME` / `^window.NAME = function`): 619 `markAllProfilesOld` (Rename), 2079 `deleteProfile`, 2893 `removeProfileScreenshot`, 3112 `profileRename` (Rename), 7979 `repairOsOutfitCode` (Konsole), 9450 `mbsWheelClearAll`, 9640 `mbsWheelDeleteShot`, 9746 `mbsWheelClearAllShots`, 10397 `deleteOsScreenshotFromLb`, 10411 `deleteOsScreenshot`, 10420 `deleteOsScreenshotKey`, 10461/10473 `deleteLscgVersion`, 10484 `clearAllProfileScreenshots`, 10494 `clearAllLscgScreenshots`, 10502/10503 `clearAllLscgOutfits`. Die `let X = {}`-Deklarationen matchen das Reset-Muster `^\s*X\s*=\s*\{\}` nicht (Zeile beginnt mit `let`).
5. `_syncLscgScreenshotToProfiles` kopiert das Bild eines Members in die Slots ALLER Fingerprints dieses Members (Schleife über `entry.versions`), nicht nur des aufrufenden `fp` — deshalb muss der Remove-Helfer für einen Schlüssel `mk|fp` ebenfalls alle `v.fingerprint` von `LSCG_DB[mk].versions` durchgehen (Cross-Version-Kopie `Anna_v2c` im Fixture).
6. `bcSpeichernJetzt()` führt geplante Saves sofort aus (ohne die `idbSet`-Promise abzuwarten) → `await settle(100)` vor `idbGet`. Schlüssel: `BC_PROFILE_SCREENSHOTS_v1`, `BC_LSCG_SCREENSHOTS_v1` (`LSCG_SCREENSHOTS_KEY`), `BC_MBS_WHEEL_SS_v1` (`_MBS_WHEEL_SS_KEY`).
7. Zeilennummern oben sind Stand VOR Plan 02-01 (dessen Fix verschiebt alles nach ~7551 um +1). Immer per `grep -n "function NAME" items.js` lokalisieren.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 2 gesamt; Plan-Zuordnung in Klammern)

| Pfad | Symbole / Inhalt | Plan |
|------|------------------|------|
| `tests/idb-helpers.test.js` (erweitert) | describe `idbSet/idbGet Fehlerpfade (TEST-04, STAB-02)`: Quota, Nicht-Quota, Throttle, Throttle-Reset, idbGet-Fehler, fehlendes showStatus; `afterEach`-Restore | 02-01 |
| `tests/screenshot-sync.test.js` (neu) | STAB-01: `_syncLscgScreenshotToProfiles(mk, fp)` liest `mk\|fp`, kopiert zu allen Slots, überschreibt nie, Legacy-Pfad, Persistenz | 02-01 |
| `items.js` `_syncLscgScreenshotToProfiles(mk, fp)` | Signatur + `const key = fp ? (mk + '\|' + fp) : mk;` | 02-01 |
| `tests/delete-confirmation.test.js` (neu) | STAB-09: `DELETE_PATHS` (10 Pfade) × {abgebrochen, bestätigt}; statischer Quell-Audit (`DELETE_RE`, `FN_RE`, `CONFIRM_REQUIRED`, `ALLOWED_WITHOUT_CONFIRM`) | 02-02 |
| `tests/delete-consistency.test.js` (neu) | STAB-10: fünf Löschpfade ohne verwaiste Kopien; manuelle Bilder bleiben; Cross-Version-Kopie; Helfer-Verträge; IDB-Persistenz | 02-02 |
| `items.js` `confirm()` in `removeProfileScreenshot`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `mbsWheelDeleteShot`, `deleteOsScreenshotFromLb` (Wheel-Zweig) | STAB-09 | 02-02 |
| `items.js` `_LSCG_PROFIL_KOPIEN_HINWEIS`, `_removeLscgScreenshotFromProfiles(fp, img)`, `_removeLscgScreenshotKeyFromProfiles(key)`, `_removeAllLscgScreenshotsFromProfiles()` + Aufrufe in `deleteLscgVersion`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `clearAllLscgScreenshots`, `clearAllLscgOutfits` | STAB-10 | 02-02 |
| `tests/storage-estimate.test.js` (neu) | STAB-03: `_speicherFormatBytes`, `_speicherFormat`, `_speicherZeigeStatus` mit `navigator`-Stub, Fallbacks | 02-03 |
| `items.js` `_speicherFormatBytes(n)`, `_speicherFormat(usage, quota)`, `_speicherZeigeStatus()` + Init-Hook | STAB-03 | 02-03 |
| `index.html` Tweaks-Panel-Sektion `📊 Speicher` (`#storageInfo`, Button `🔄 Aktualisieren`) | STAB-03 — einzige index.html-Änderung der Phase | 02-03 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Lösch-Bestätigungs- und Konsistenz-Tests schreiben, RED bestätigen — `tests/delete-confirmation.test.js`, `tests/delete-consistency.test.js` (STAB-09, STAB-10)</name>
  <precondition>Plan 02-01 ist abgeschlossen und committet (`grep -Fc "function _syncLscgScreenshotToProfiles(mk, fp)" items.js` = 1); `npm test` grün; `git status --short` leer</precondition>
  <files>tests/delete-confirmation.test.js, tests/delete-consistency.test.js</files>
  <read_first>items.js: die Funktionen `removeProfileScreenshot`, `deleteOsScreenshotFromLb`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `deleteLscgVersion`, `clearAllProfileScreenshots`, `clearAllLscgScreenshots`, `clearAllLscgOutfits`, `mbsWheelDeleteShot`, `mbsWheelClearAllShots`, `mbsWheelClearAll`, `deleteProfile` — jeweils per `grep -n "^function NAME" items.js` lokalisieren und nur den Funktionskörper lesen (RESEARCH-Tabelle: ~2890, ~10395-10424, ~10451-10510, ~9635, ~9743, ~9445, ~2075); `_syncLscgScreenshotToProfiles` und `_getLscgScreenshot` (~7515-7570, Slot-Semantik von `_lscgFpMap`); .planning/phases/02-speicher-sicherheit/02-RESEARCH.md Abschnitt „STAB-09/STAB-10: Vollständige Lösch-Pfad-Tabelle" und Pitfalls 3-5; tests/screenshot-sync.test.js (aus 02-01: `setState`-Muster per evalIn); tests/helpers/loadScript.js Zeilen 120-160</read_first>
  <action>
    Beide Dateien (ESM) importieren `{ describe, it, expect, beforeAll, beforeEach }` aus vitest und `{ loadScript, evalIn, settle, REPO_ROOT }` aus `./helpers/loadScript.js`; die Confirmation-Datei zusätzlich `fs` und `path` (node:). Gemeinsames Muster: modulweit `let confirmResult = true; const confirmCalls = [];`, `ctx = loadScript(['items.js'], { confirm: (msg) => { confirmCalls.push(String(msg)); return confirmResult; } })` in `beforeAll`, danach `ctx.showStatus = () => {}` (verhindert echte 4-s-Timer). Helfer `setState(ctx)` setzt in EINEM `evalIn`-Aufruf per Zuweisung (alle Ziele sind Top-Level-`let`, Befund 3): `LSCG_DB`, `LSCG_SCREENSHOTS`, `_lscgFpMap`, `PROFILE_SCREENSHOTS`, `_mbsWheelData`, `_mbsWheelShots`, `_osLightboxWheelFp = 'W2'`, `_osLightboxKey = null`, `_osLightboxMk = null`. Helfer `snap(ctx)` = `evalIn(ctx, 'JSON.stringify({ db: LSCG_DB, shots: LSCG_SCREENSHOTS, prof: PROFILE_SCREENSHOTS, wheel: _mbsWheelShots })')`; `profShots(ctx)` = `evalIn(ctx, 'JSON.parse(JSON.stringify(PROFILE_SCREENSHOTS))')`.

    Gemeinsames Fixture (JSON per `JSON.stringify` in den evalIn-String einbetten):
    - `LSCG_DB = { '123': { name: 'Anna', versions: [{ code: 'c1', fingerprint: 'FP1', ts: 1 }, { code: 'c2', fingerprint: 'FP2', ts: 2 }] }, '456': { name: 'Bob', versions: [{ code: 'c4', fingerprint: 'FP4', ts: 4 }] } }`
    - `LSCG_SCREENSHOTS = { '123|FP1': 'data:img1', '123|FP2': 'data:img2', '456': 'data:legacy' }`
    - `_lscgFpMap = { FP1: ['Anna_v1'], FP2: ['Anna_v2', 'Anna_v2b', 'Anna_v2c'], FP4: ['Bob_v1'] }`
    - `PROFILE_SCREENSHOTS = { Anna_v1: 'data:img1', Anna_v2: 'data:img2', Anna_v2b: 'data:manual', Anna_v2c: 'data:img1', Bob_v1: 'data:legacy', Fremd: 'data:other' }` — `Anna_v2b` = manueller Upload (nicht identisch), `Anna_v2c` = Cross-Version-Kopie (Befund 5), `Fremd` = Profil ohne LSCG-Bezug
    - `_mbsWheelData = [{ memberNumber: 5, name: 'Bob', outfits: [{ name: 'Outfit A', items: [{ group: 'G', asset: 'A' }] }] }]`, `_mbsWheelShots = { 'G:A': 'data:wheel', W2: 'data:wheel2' }` (Befund 2)

    **`tests/delete-confirmation.test.js`** — `describe('Löschen nur mit Bestätigung (STAB-09)')`, `beforeEach`: `confirmCalls.length = 0; confirmResult = true; setState(ctx)`.
    Tabelle `DELETE_PATHS` (Array von `{ name, call, gone }`, `gone` liefert per `evalIn` ein Boolean):
    1. `removeProfileScreenshot('Anna_v1')` — gone: `PROFILE_SCREENSHOTS['Anna_v1'] === undefined`
    2. `deleteOsScreenshot('456')` (Legacy-Schlüssel) — gone: `LSCG_SCREENSHOTS['456'] === undefined`
    3. `deleteOsScreenshotKey('123|FP1')` — gone: `LSCG_SCREENSHOTS['123|FP1'] === undefined`
    4. `mbsWheelDeleteShot(5, 0)` — gone: `_mbsWheelShots['G:A'] === undefined`
    5. `deleteOsScreenshotFromLb()` (Wheel-Zweig, `_osLightboxWheelFp = 'W2'`) — gone: `_mbsWheelShots['W2'] === undefined`
    6. `deleteLscgVersion('123', 0)` — gone: `LSCG_DB['123'].versions.length === 1 && LSCG_SCREENSHOTS['123|FP1'] === undefined`
    7. `clearAllProfileScreenshots()` — gone: `Object.keys(PROFILE_SCREENSHOTS).length === 0`
    8. `clearAllLscgScreenshots()` — gone: `Object.keys(LSCG_SCREENSHOTS).length === 0`
    9. `clearAllLscgOutfits()` — gone: `Object.keys(LSCG_DB).length === 0 && Object.keys(LSCG_SCREENSHOTS).length === 0`
    10. `mbsWheelClearAllShots()` — gone: `Object.keys(_mbsWheelShots).length === 0`
    Zwei `it.each(DELETE_PATHS)`-Blöcke (Objekt-Zeilen → Vitest-Interpolation `$name`, nicht `%s`): (a) `'$name: confirm → false löscht nichts und fragt genau einmal'`: `confirmResult = false; const before = snap(ctx); call(ctx); expect(confirmCalls).toHaveLength(1); expect(snap(ctx)).toBe(before);` (b) `'$name: confirm → true entfernt das Ziel und fragt genau einmal'`: `call(ctx); expect(confirmCalls).toHaveLength(1); expect(gone(ctx)).toBe(true);`. Zusätzlich `it('confirm-Text ist ein nicht-leerer String')` für Pfad 3 (`deleteOsScreenshotKey('123|FP1')` mit `confirmResult = true`; `typeof confirmCalls[0] === 'string'` und Länge > 0).
    Statischer Quell-Audit `it('jede Lösch-Zeile der vier Stores liegt in einer Funktion mit confirm() oder auf der Ausnahmeliste')`: `src = fs.readFileSync(path.join(REPO_ROOT, 'items.js'), 'utf8').split('\n')`; `DELETE_RE = /delete (LSCG_SCREENSHOTS|PROFILE_SCREENSHOTS|_mbsWheelShots|LSCG_DB)\[|^\s*(LSCG_SCREENSHOTS|PROFILE_SCREENSHOTS|_mbsWheelShots|LSCG_DB)\s*=\s*\{\}/`; `FN_RE = /^(?:async )?function ([A-Za-z_$][\w$]*)|^window\.([A-Za-z_$][\w$]*)\s*=\s*(?:async )?function/`; Zeilen sequentiell durchlaufen, aktuelle Enclosing-Funktion aus `FN_RE` merken, Treffer von `DELETE_RE` als `{ line, fn }` sammeln. `CONFIRM_REQUIRED = ['deleteProfile','removeProfileScreenshot','deleteOsScreenshot','deleteOsScreenshotKey','mbsWheelDeleteShot','deleteOsScreenshotFromLb','deleteLscgVersion','clearAllProfileScreenshots','clearAllLscgScreenshots','clearAllLscgOutfits','mbsWheelClearAllShots','mbsWheelClearAll']`; `ALLOWED_WITHOUT_CONFIRM = ['markAllProfilesOld','profileRename','repairOsOutfitCode','_removeLscgScreenshotFromProfiles']` (Kommentar je Eintrag: Umbenennung mit Mitzug / Umbenennung / Konsolen-Reparatur, nicht UI-erreichbar / Aufräum-Helfer, nur hinter bestätigten Aufrufern). Assertions: (i) `hits.filter(h => !CONFIRM_REQUIRED.includes(h.fn) && !ALLOWED_WITHOUT_CONFIRM.includes(h.fn))` ist `[]` (Fehlermeldung mit Zeilennummern); (ii) `hits.length >= 15` (Vollständigkeits-Plausibilität, Befund 4); (iii) für jeden Namen in `CONFIRM_REQUIRED`: Funktionskörper (von der `FN_RE`-Zeile bis zur nächsten Zeile, die mit `}` beginnt) existiert und `toMatch(/confirm[(]/)`. Kommentar im Kopf: `LSCG_MAX_VERSIONS`-Trim (`versions.slice(-…)`) und `enrichProfileNamesWithIDs` sind bewusst nicht Teil des Audits (Obergrenze bzw. Umbenennung, siehe Plan 02-02 must_haves).

    **`tests/delete-consistency.test.js`** — `describe('Bestätigtes Löschen hinterlässt keine verwaisten Kopien (STAB-10)')`, `beforeEach`: `confirmResult = true; setState(ctx)`. Fälle:
    1. `'deleteLscgVersion entfernt das Versionsbild und alle identischen Profil-Kopien des Members (inkl. Cross-Version-Kopie Anna_v2c)'`: `deleteLscgVersion('123', 0)`; `expect(evalIn(ctx, "LSCG_SCREENSHOTS['123|FP1']")).toBeUndefined()`; `expect(profShots(ctx)).toEqual({ Anna_v2: 'data:img2', Anna_v2b: 'data:manual', Bob_v1: 'data:legacy', Fremd: 'data:other' })` (Anna_v1 und Cross-Version-Kopie Anna_v2c weg; Rest bleibt); `LSCG_DB['123'].versions.length === 1`.
    2. `'deleteOsScreenshotKey entfernt nur identische Kopien — manuelle Profil-Bilder bleiben'`: `deleteOsScreenshotKey('123|FP2')`; `profShots` = `{ Anna_v1: 'data:img1', Anna_v2b: 'data:manual', Anna_v2c: 'data:img1', Bob_v1: 'data:legacy', Fremd: 'data:other' }`.
    3. `'deleteOsScreenshot (Legacy-Schlüssel mk) räumt die Profil-Kopie über die Versions-Fingerprints'`: `deleteOsScreenshot('456')`; `PROFILE_SCREENSHOTS['Bob_v1'] === undefined`; `Anna_*` und `Fremd` unverändert.
    4. `'clearAllLscgScreenshots leert LSCG_SCREENSHOTS und alle identischen Profil-Kopien, LSCG_DB bleibt'`: `profShots` = `{ Anna_v2b: 'data:manual', Fremd: 'data:other' }`; `Object.keys(LSCG_DB).length === 2`.
    5. `'clearAllLscgOutfits leert LSCG_DB, LSCG_SCREENSHOTS, _lscgFpMap und alle identischen Profil-Kopien'`: `profShots` = `{ Anna_v2b: 'data:manual', Fremd: 'data:other' }`; `Object.keys(LSCG_DB).length === 0`; `Object.keys(_lscgFpMap).length === 0`.
    6. `'Helfer-Vertrag: _removeLscgScreenshotFromProfiles(fp, img) entfernt nur identische Slots und liefert die Anzahl'`: `expect(ctx._removeLscgScreenshotFromProfiles('FP2', 'data:img1')).toBe(1)` (nur Anna_v2c); `expect(ctx._removeLscgScreenshotFromProfiles(null, 'x')).toBe(0)`; dann `setState(ctx)` erneut aufrufen (frisches Fixture innerhalb desselben `it`), dann `expect(ctx._removeLscgScreenshotKeyFromProfiles('123|FP1')).toBe(2)` (Anna_v1 über FP1 + Anna_v2c über FP2 desselben Members); `expect(ctx._removeLscgScreenshotKeyFromProfiles('gibt-es-nicht')).toBe(0)`.
    7. `'nach bcSpeichernJetzt() sind die Kopien auch in IDB weg'`: `deleteLscgVersion('123', 0)`; `ctx.bcSpeichernJetzt()`; `await settle(100)`; `const p = await ctx.idbGet('BC_PROFILE_SCREENSHOTS_v1')`; `expect(p?.Anna_v1).toBeUndefined()`; `expect(p?.Anna_v2c).toBeUndefined()`; `expect(p?.Anna_v2b).toBe('data:manual')`; `const s = await ctx.idbGet('BC_LSCG_SCREENSHOTS_v1')`; `expect(s?.['123|FP1']).toBeUndefined()`; `expect(s?.['123|FP2']).toBe('data:img2')`.

    RED bestätigen: `npx vitest run tests/delete-confirmation.test.js tests/delete-consistency.test.js` MUSS rot sein — erwartet in der Confirmation-Datei: Pfade 1-5 in beiden it.each-Blöcken rot (confirm nie aufgerufen) + Audit rot (fünf Funktionen ohne `confirm(`), Pfade 6-10 grün; in der Consistency-Datei: Fälle 1-7 rot (verwaiste Kopien bzw. Helfer fehlen — `TypeError: … is not a function`). Ist eine der Dateien unerwartet grün: STOPP und prüfen (Fixture nicht per `evalIn` gesetzt? `confirm` nicht per `extraGlobals`?).

    Commit der roten Tests: `git add tests/delete-confirmation.test.js tests/delete-consistency.test.js`; `git diff --cached --name-only` = genau diese zwei Pfade; Message `test(02-02): add failing delete confirmation and consistency tests (STAB-09, STAB-10)`. Vorher `git status --short items.js` prüfen und im SUMMARY festhalten (muss leer sein).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/delete-confirmation.test.js tests/delete-consistency.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -q "removeProfileScreenshot" "$OUT" && grep -q "Anna_v2c" "$OUT" && grep -Eq "Tests +[0-9]+ failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` wird nicht ausgegeben — Vitest endete mit Exit 0 (mindestens eine Datei unerwartet grün), oder die Namen `removeProfileScreenshot` / `Anna_v2c` fehlen in der Fehlerausgabe (Fixture oder Tabelle unvollständig)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "confirm:" tests/delete-confirmation.test.js` >= 1 und `grep -c "confirm:" tests/delete-consistency.test.js` >= 1 (Stub per extraGlobals)
    - `grep -c "deleteOsScreenshotFromLb" tests/delete-confirmation.test.js` >= 2 (Tabelle + CONFIRM_REQUIRED); `grep -c "mbsWheelDeleteShot" tests/delete-confirmation.test.js` >= 2
    - `grep -c "it.each(DELETE_PATHS)" tests/delete-confirmation.test.js` >= 2 (beide Blöcke); `grep -c "CONFIRM_REQUIRED" tests/delete-confirmation.test.js` >= 3; `grep -c "ALLOWED_WITHOUT_CONFIRM" tests/delete-confirmation.test.js` >= 2; `grep -c "repairOsOutfitCode" tests/delete-confirmation.test.js` >= 1
    - `grep -c "Anna_v2c" tests/delete-consistency.test.js` >= 3; `grep -c "data:manual" tests/delete-consistency.test.js` >= 4; `grep -c "_removeLscgScreenshotKeyFromProfiles" tests/delete-consistency.test.js` >= 2; `grep -c "_removeLscgScreenshotFromProfiles" tests/delete-consistency.test.js` >= 2
    - `grep -c "BC_PROFILE_SCREENSHOTS_v1" tests/delete-consistency.test.js` >= 1; `grep -c "BC_LSCG_SCREENSHOTS_v1" tests/delete-consistency.test.js` >= 1; `grep -c "bcSpeichernJetzt" tests/delete-consistency.test.js` >= 1
    - `grep -c "Outfit A" tests/delete-confirmation.test.js` >= 1 (vollständiges Wheel-Fixture, Befund 2)
    - `test "$(grep -v '^\s*//' tests/delete-confirmation.test.js tests/delete-consistency.test.js | grep -c '\.only(')" = 0`
    - `git status --short items.js` gibt nichts aus
    - Nach dem Commit: `git show --name-only --format= HEAD | sort` = `tests/delete-confirmation.test.js` und `tests/delete-consistency.test.js` (genau zwei Pfade)
  </acceptance_criteria>
  <done>Beide Testdateien existieren mit der 10-Pfad-Tabelle, dem statischen Audit und den sieben Konsistenzfällen; Lauf nachweislich rot in genau den erwarteten Fällen; rote Tests committet; items.js unverändert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `confirm()` in die fünf confirm-losen Lösch-Pfade einbauen, Confirmation-Test GREEN, committen (STAB-09)</name>
  <precondition>`git status --short items.js` gibt VOR dem Edit nichts aus — sonst STOPP und eskalieren (fremde Hunks würden mitgestaged)</precondition>
  <reversibility rating="reversible">Nur zusätzliche Guard-Zeilen (`if (!confirm(…)) return;`) in bestehenden Funktionen; Revert per `git revert`, keine Datenmigration, keine Schlüsseländerung.</reversibility>
  <files>items.js</files>
  <read_first>items.js `removeProfileScreenshot`, `deleteOsScreenshotFromLb`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `mbsWheelDeleteShot` (jeweils per `grep -n "^function NAME" items.js`, dann 10-15 Zeilen ab Treffer); als Text-Vorbild `deleteLscgVersion` (`confirm('Version v' + … + ' löschen?\n\nDiese Aktion kann nicht rückgängig gemacht werden.')`) und `mbsWheelClearAllShots`; tests/delete-confirmation.test.js (aus Task 1)</read_first>
  <behavior>
    - Für jeden der zehn `DELETE_PATHS`: `confirm → false` → `confirm` genau 1× aufgerufen, `snap(ctx)` unverändert; `confirm → true` → `confirm` genau 1×, Ziel entfernt
    - Statischer Audit: alle zwölf `CONFIRM_REQUIRED`-Funktionen enthalten `confirm(`; keine Lösch-Zeile außerhalb der bekannten Funktionen
    - Bestehende Löschpfade (6-10) verhalten sich unverändert (weiterhin genau ein confirm)
  </behavior>
  <action>
    Fünf punktuelle Einbauten in items.js — je ein Guard direkt nach den bestehenden Existenz-Prüfungen, VOR der ersten Mutation; deutsche Texte im Stil der bestehenden Dialoge. Keine Umformatierung, keine Umbenennung, Datei bleibt LF.

    a. `removeProfileScreenshot(pname)`: nach `if (!name) return;` zwei Zeilen einfügen: `if (!PROFILE_SCREENSHOTS[name]) return;` (kein Dialog für ein nicht vorhandenes Bild) und `if (!confirm('Profil-Bild von "' + name + '" entfernen?')) return;`.
    b. `deleteOsScreenshot(mk)`: nach `if (!LSCG_SCREENSHOTS[mk]) return;` einfügen: `if (!confirm('Bild für #' + mk + ' löschen?')) return;`.
    c. `deleteOsScreenshotKey(key)`: nach `if (!LSCG_SCREENSHOTS[key]) return;` einfügen: `if (!confirm('Dieses Bild löschen?')) return;`.
    d. `mbsWheelDeleteShot(mn, oi)`: nach `if (!o) return;` die Zeile `delete _mbsWheelShots[_mbsOutfitFp(o)];` ersetzen durch vier Zeilen: `const fp = _mbsOutfitFp(o);`, `if (!_mbsWheelShots[fp]) return;`, `if (!confirm('Wheel-Bild von "' + (o.name || '?') + '" löschen?')) return;`, `delete _mbsWheelShots[fp];`.
    e. `deleteOsScreenshotFromLb()`: als erste Anweisung im Zweig `if (_osLightboxWheelFp !== null) {` einfügen: `if (!confirm('Wheel-Bild löschen?')) return;` (Abbruch lässt die Lightbox offen — die anderen beiden Zweige fragen bereits über `deleteOsScreenshotKey`/`deleteOsScreenshot`, daher dort KEIN zusätzliches confirm — sonst Doppeldialog).

    Prüfen: `node --check items.js` → Exit 0; `git diff --numstat items.js` → Insertions zwischen 9 und 12, Deletions genau 1 (Zeile aus d).

    GREEN: `npx vitest run tests/delete-confirmation.test.js` → alle grün (20 it.each-Fälle + confirm-Text + Audit = 22 passed). `tests/delete-consistency.test.js` bleibt erwartungsgemäß rot (Helfer kommen in Task 3) — das ist hier KEIN Fehler, im SUMMARY vermerken.

    Commit — ausschließlich `git add items.js`; `git diff --cached --name-only` = `items.js`; Message `fix(02-02): require confirm() on every screenshot delete path (STAB-09)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/delete-confirmation.test.js && node --check items.js</automated>
    <fails_when>non-zero exit, `failed` in der `Tests`-Summary-Zeile, oder weniger als `22 passed` — insbesondere wenn der statische Audit noch eine Funktion ohne `confirm(` meldet oder ein it.each-Fall `confirm` 0× oder 2× zählt</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/delete-consistency.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -Eq "Tests +[0-9]+ failed" "$OUT" && echo STILL-RED-AS-EXPECTED</automated>
    <fails_when>`STILL-RED-AS-EXPECTED` wird nicht ausgegeben — die Konsistenz-Datei ist bereits grün, was bedeuten würde, dass Task 2 über seinen Scope hinaus STAB-10-Logik eingebaut hat (dann Diff prüfen und den STAB-10-Teil nach Task 3 verschieben)</fails_when>
  </verify>
  <acceptance_criteria>
    - Für jeden Namen in `removeProfileScreenshot deleteOsScreenshot deleteOsScreenshotKey mbsWheelDeleteShot deleteOsScreenshotFromLb`: `awk '/^function NAME[(]/{f=1} f&&/confirm[(]/{print "OK"; exit} f&&/^}/{exit}' items.js` gibt `OK` aus (Guard liegt im Funktionskörper)
    - `grep -Fc "Profil-Bild von" items.js` = 1; `grep -Fc "Wheel-Bild von" items.js` = 1; `grep -Fc "Wheel-Bild löschen?" items.js` = 1; `grep -Fc "Dieses Bild löschen?" items.js` = 1; `grep -Fc "Bild für #' + mk + ' löschen?" items.js` = 1
    - `grep -c "confirm(" items.js` ist um genau 5 höher als vor dem Task (Vorher-Wert im SUMMARY notieren)
    - `node --check items.js` → Exit 0; `npx vitest run tests/delete-confirmation.test.js` → `22 passed`
    - Nach dem Commit: `git show --name-only --format= HEAD` = `items.js`; `git status --short` ist leer
  </acceptance_criteria>
  <done>Alle fünf Pfade fragen genau einmal; Abbruch lässt alle vier Stores unverändert; Confirmation-Test grün (22 passed); Konsistenz-Test noch rot (erwartet); Commit enthält nur items.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Aufräum-Helfer `_removeLscgScreenshotFromProfiles` + Aufrufe in allen LSCG-Löschpfaden, Hinweistext in den Dialogen, beide Tests GREEN, committen (STAB-10)</name>
  <precondition>Task 2 committet; `git status --short items.js` gibt VOR dem Edit nichts aus — sonst STOPP</precondition>
  <reversibility rating="reversible">Zusätzliche Helfer und Aufrufe; entfernt zur Laufzeit ausschließlich byte-identische Kopien eines ohnehin vom Nutzer bestätigt gelöschten Bildes. Revert per `git revert`; bereits entfernte Kopien wären ohne Revert des Nutzer-Löschvorgangs ohnehin verwaist gewesen.</reversibility>
  <files>items.js</files>
  <read_first>items.js `_syncLscgScreenshotToProfiles` (nach 02-01: Signatur `(mk, fp)`; per grep lokalisieren, 22 Zeilen lesen — der Helfer ist sein Spiegelbild), `_getLscgScreenshot` (Fallback 3 nutzt `_lscgFpMap[fp]` → `PROFILE_SCREENSHOTS[k]`), `deleteLscgVersion` (Reihenfolge: Screenshot-Block VOR Fingerprint-Map-Bereinigung VOR `versions.splice`), `clearAllLscgScreenshots`, `clearAllLscgOutfits` (setzt `_lscgFpMap = {}` — Helfer muss davor laufen), `deleteOsScreenshot`, `deleteOsScreenshotKey`, `_saveProfileScreenshots` (~563), `LSCG_SCREENSHOTS_KEY` (~7498); .planning/phases/02-speicher-sicherheit/02-RESEARCH.md „Empfohlener Fix-Ansatz" und Pitfall 4/5; tests/delete-consistency.test.js (aus Task 1)</read_first>
  <behavior>
    - Consistency Fall 1-5: nach jedem bestätigten LSCG-Löschen keine identische Kopie mehr in `PROFILE_SCREENSHOTS`; `data:manual` und `Fremd` bleiben in allen Fällen
    - Fall 6: `_removeLscgScreenshotFromProfiles('FP2', 'data:img1')` → 1; `(null, 'x')` → 0; `_removeLscgScreenshotKeyFromProfiles('123|FP1')` → 2; unbekannter Schlüssel → 0
    - Fall 7: nach `bcSpeichernJetzt()` fehlen die Kopien auch unter `BC_PROFILE_SCREENSHOTS_v1` / `BC_LSCG_SCREENSHOTS_v1`
    - Confirmation-Datei bleibt grün (22 passed); der Audit akzeptiert den neuen Helfer über `ALLOWED_WITHOUT_CONFIRM`
  </behavior>
  <action>
    Orchestrator-Entscheidung 5: ein zentraler Helfer statt viermal duplizierter Aufräumlogik. Signatur um `img` erweitert (`_removeLscgScreenshotFromProfiles(fp, img)`), weil die Identitätsprüfung `=== img` die einzige Möglichkeit ist, synchronisierte Kopien von manuellen Uploads (`uploadProfileScreenshot`) zu unterscheiden — Kernwert, siehe must_haves-Annahme; im SUMMARY als Planer-Ermessen benennen.

    1. Konstante direkt unter `let LSCG_SCREENSHOTS = {};` (per `grep -n "^let   LSCG_SCREENSHOTS" items.js`): `const _LSCG_PROFIL_KOPIEN_HINWEIS = 'Synchronisierte Kopien in Profil-Bildern werden mit entfernt.';` mit Kommentar „Hinweistext für confirm()-Dialoge, die LSCG-Bilder löschen (STAB-09/STAB-10)".

    2. Drei Helfer direkt NACH dem Ende von `_syncLscgScreenshotToProfiles` (vor dem Kommentar `// ── Einzelnen Screenshot aufnehmen`), Kommentar „Spiegelbild von _syncLscgScreenshotToProfiles: entfernt statt zu kopieren; nur byte-identische Kopien, manuelle Profil-Bilder bleiben":
       - `function _removeLscgScreenshotFromProfiles(fp, img)`: bei falsy `fp` oder `img` → `return 0`; über `_lscgFpMap[fp] ?? []` iterieren; für jedes `k` mit `PROFILE_SCREENSHOTS[k] === img` den Eintrag per `delete` entfernen und zählen; bei Zähler > 0 `_saveProfileScreenshots()`; Zähler zurückgeben.
       - `function _removeLscgScreenshotKeyFromProfiles(key)`: `img = LSCG_SCREENSHOTS[key]`, bei fehlendem Bild `return 0`; `mk` = Teil vor dem ersten `|` (per `indexOf('|')`; ohne `|` ist `key` selbst der `mk`); Fingerprint-Menge (`Set`) aus dem Schlüssel-Teil nach `|` (falls vorhanden) plus allen `v.fingerprint` aus `LSCG_DB[mk]?.versions ?? []` (truthy) — exakt die Menge, die der Sync beim Kopieren nutzt (Befund 5); Summe der `_removeLscgScreenshotFromProfiles(fp, img)`-Rückgaben zurückgeben. Kommentar: „MUSS vor dem Löschen aus LSCG_SCREENSHOTS laufen (braucht das Bild)".
       - `function _removeAllLscgScreenshotsFromProfiles()`: Summe von `_removeLscgScreenshotKeyFromProfiles(key)` über `Object.keys(LSCG_SCREENSHOTS)`.

    3. Aufrufstellen (jeweils VOR der Mutation des LSCG-Stores):
       - `deleteLscgVersion`: im Block `if (key && LSCG_SCREENSHOTS[key]) {` als erste Anweisung `_removeLscgScreenshotKeyFromProfiles(key);` (vor `delete LSCG_SCREENSHOTS[key];` — und damit vor der `_lscgFpMap`-Bereinigung und dem `splice`). Confirm-Text erweitern: `'…rückgängig gemacht werden.\n' + _LSCG_PROFIL_KOPIEN_HINWEIS`.
       - `deleteOsScreenshot(mk)`: nach dem confirm-Guard aus Task 2 `const n = _removeLscgScreenshotKeyFromProfiles(mk);`, dann das bestehende `delete`; Status: `'🗑️ Bild für #' + mk + ' gelöscht' + (n ? ' (+' + n + ' Profil-Kopien)' : '')`. Confirm-Text: `'Bild für #' + mk + ' löschen?\n\n' + _LSCG_PROFIL_KOPIEN_HINWEIS`.
       - `deleteOsScreenshotKey(key)`: analog mit `key`; Status `'🗑️ Bild gelöscht' + (n ? …)`; Confirm-Text `'Dieses Bild löschen?\n\n' + _LSCG_PROFIL_KOPIEN_HINWEIS`.
       - `clearAllLscgScreenshots()`: nach dem confirm-Guard `const n = _removeAllLscgScreenshotsFromProfiles();` VOR `LSCG_SCREENSHOTS = {};`; Status `'🗑️ Alle Bilder gelöscht' + (n ? ' (+' + n + ' Profil-Kopien)' : '')`; Confirm-Text `'…bleiben erhalten.\n' + _LSCG_PROFIL_KOPIEN_HINWEIS`.
       - `clearAllLscgOutfits()`: nach dem confirm-Guard `_removeAllLscgScreenshotsFromProfiles();` als erste Anweisung VOR `LSCG_DB = {};` (der Helfer braucht `LSCG_DB`, `LSCG_SCREENSHOTS` und `_lscgFpMap` noch intakt); Confirm-Text `'…Codes und Bilder.\n' + _LSCG_PROFIL_KOPIEN_HINWEIS`.
       Keine weiteren Aufrufe — insbesondere NICHT in `clearAllProfileScreenshots` (leert ohnehin alles), `removeProfileScreenshot` (Profil-Seite, kein LSCG-Bild), `deleteProfile`, den Wheel-Pfaden (eigener Store ohne Sync) oder `window.repairOsOutfitCode` (nimmt das Bild neu auf, Kopien bleiben gültig).

    4. Prüfen: `node --check items.js` → Exit 0. GREEN: `npx vitest run tests/delete-consistency.test.js tests/delete-confirmation.test.js` → beide grün (7 + 22 = 29 passed). Dann `npm test` → gesamte Suite grün (inkl. tests/screenshot-sync.test.js aus 02-01 — der Sync ist unverändert).

    5. Commit — ausschließlich `git add items.js`; Message `fix(02-02): remove synced profile copies on LSCG screenshot delete (STAB-10)`. Nachweis für das SUMMARY: `git show --stat --format= HEAD`, RED-Ausgabe aus Task 1, GREEN-Ausgabe, und die drei bewusst nicht geänderten Pfade (`LSCG_MAX_VERSIONS`-Trim, `enrichProfileNamesWithIDs`, `repairOsOutfitCode`) als „Bekannt, nicht geändert" mit Begründung.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/delete-consistency.test.js tests/delete-confirmation.test.js && node --check items.js && npm test</automated>
    <fails_when>non-zero exit (Vitest rot oder `node --check` SyntaxError), `failed` in der `Tests`-Summary-Zeile, oder weniger als `29 passed` für die beiden Dateien — insbesondere wenn `data:manual` oder `Fremd` in einem Fall fehlt (Helfer löscht zu viel) oder `Anna_v2c` übrig bleibt (Cross-Version-Fingerprints nicht durchlaufen)</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && ok=1; for f in deleteLscgVersion deleteOsScreenshot deleteOsScreenshotKey; do awk -v n="$f" '$0 ~ ("^function " n "[(]") {f=1} f && /_removeLscgScreenshotKeyFromProfiles[(]/ {print "OK"; exit} f && /^}/ {exit}' items.js | grep -q OK || ok=0; done; for f in clearAllLscgScreenshots clearAllLscgOutfits; do awk -v n="$f" '$0 ~ ("^function " n "[(]") {f=1} f && /_removeAllLscgScreenshotsFromProfiles[(]/ {print "OK"; exit} f && /^}/ {exit}' items.js | grep -q OK || ok=0; done; for f in deleteLscgVersion deleteOsScreenshot deleteOsScreenshotKey clearAllLscgScreenshots clearAllLscgOutfits; do awk -v n="$f" '$0 ~ ("^function " n "[(]") {f=1} f && /_LSCG_PROFIL_KOPIEN_HINWEIS/ {print "OK"; exit} f && /^}/ {exit}' items.js | grep -q OK || ok=0; done; HEADFILES=$(git show --name-only --format= HEAD); test "$ok" = 1 && test "$HEADFILES" = "items.js" && echo WIRING-OK</automated>
    <fails_when>`WIRING-OK` wird nicht ausgegeben — in mindestens einer der fünf LSCG-Löschfunktionen fehlt der Helfer-Aufruf (`_removeLscgScreenshotKeyFromProfiles` in den drei Einzel-Pfaden, `_removeAllLscgScreenshotsFromProfiles` in den zwei Bulk-Pfaden) oder der Hinweistext `_LSCG_PROFIL_KOPIEN_HINWEIS` im Dialog, oder der HEAD-Commit enthält mehr als items.js</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "function _removeLscgScreenshotFromProfiles(fp, img)" items.js` = 1; `grep -Fc "function _removeLscgScreenshotKeyFromProfiles(key)" items.js` = 1; `grep -Fc "function _removeAllLscgScreenshotsFromProfiles()" items.js` = 1
    - `awk '/^function _removeLscgScreenshotFromProfiles[(]/{f=1} f&&/=== img/{print "OK"; exit} f&&/^}/{exit}' items.js` gibt `OK` aus (Identitätsprüfung im Helfer)
    - Für jeden Namen in `deleteLscgVersion deleteOsScreenshot deleteOsScreenshotKey`: `awk '/^function NAME[(]/{f=1} f&&/_removeLscgScreenshotKeyFromProfiles[(]/{print "OK"; exit} f&&/^}/{exit}' items.js` gibt `OK` aus; für `clearAllLscgScreenshots clearAllLscgOutfits` analog mit `_removeAllLscgScreenshotsFromProfiles[(]`
    - `awk '/^function clearAllLscgOutfits[(]/{f=1} f&&/_removeAllLscgScreenshotsFromProfiles[(]/{h=NR} f&&/LSCG_DB = \{\}/{d=NR} f&&/^}/{exit} END{print (h&&d&&h<d)?"ORDER-OK":"ORDER-BAD"}' items.js` gibt `ORDER-OK` aus (Helfer läuft vor dem Reset)
    - `grep -Fn "_syncLscgScreenshotToProfiles(mk, fp)" items.js` listet weiterhin genau zwei Zeilen: die Definition und den Aufruf im Aufnahme-Callback (Sync gegenüber Plan 02-01 unverändert)
    - `node --check items.js` → Exit 0; `npx vitest run tests/delete-consistency.test.js` → `7 passed`; `npx vitest run tests/delete-confirmation.test.js` → `22 passed`; `npm test` → Exit 0
    - Nach dem Commit: `git show --name-only --format= HEAD` = `items.js`; `git status --short` ist leer; `git log --format=%h 0d9ddbe..HEAD -- index.html loader.js bot-data.js bot-ui.js bot-engine.js outfit-import.js` gibt nichts aus
  </acceptance_criteria>
  <done>Drei Helfer vorhanden und an fünf LSCG-Löschpfaden vor der Mutation verdrahtet; Dialoge nennen die Konsequenz; Konsistenz- und Bestätigungs-Tests grün (29 passed), volle Suite grün; Commit enthält nur items.js; RED-Commit liegt vor beiden GREEN-Commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-Buttons (`pmodRemoveBtn`, `os-card-del`, Lightbox 🗑, Wheel-Karten) → Lösch-Funktionen | Ein Klick ist eine explizite Nutzeraktion, aber ohne `confirm()` kein bewusster Entschluss — Fehlklick = Datenverlust |
| LSCG-Löschpfade → `PROFILE_SCREENSHOTS` | Kopien, die der Sync angelegt hat, gehören logisch zum LSCG-Bild; manuelle Uploads gehören dem Profil — die Grenze verläuft zwischen identischen und nicht-identischen Bildern |
| Test → items.js (statischer Quell-Audit) | Der Audit liest die Produktionsdatei und verlangt für jede Lösch-Zeile eine bekannte Enclosing-Funktion |
| Git-Arbeitsbaum → Repository | Drei Commits (RED, STAB-09, STAB-10), Produktionsänderung nur items.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-02 | Tampering | items.js `removeProfileScreenshot`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `mbsWheelDeleteShot`, `deleteOsScreenshotFromLb` — unbestätigte destruktive Aktion | high | mitigate | Task 2: `confirm()`-Guard vor jeder Mutation; Task 1 beweist per it.each, dass `false` alle vier Stores unverändert lässt, und per statischem Audit, dass keine Lösch-Zeile außerhalb bekannter, bestätigter Funktionen existiert |
| T-2-03 | Tampering | items.js `deleteLscgVersion`, `clearAllLscgScreenshots`, `clearAllLscgOutfits`, `deleteOsScreenshot*` — verwaiste Kopien in `PROFILE_SCREENSHOTS` (Integrität: UI zeigt Bilder, deren Quelle gelöscht ist) | medium | mitigate | Task 3: zentraler Helfer, an jedem LSCG-Löschpfad vor der Mutation aufgerufen; Konsistenz-Test inkl. IDB-Persistenz und Cross-Version-Kopie |
| T-2-07 | Denial of Service (Datenverlust) | items.js `_removeLscgScreenshotFromProfiles` — könnte manuelle Profil-Bilder mitlöschen | high | mitigate | Identitätsprüfung `PROFILE_SCREENSHOTS[k] === img` (Acceptance-awk-Gate); Testfälle `data:manual` und `Fremd` bleiben in allen fünf Pfaden; Helfer-Vertrag Fall 6 |
| T-2-06 | Tampering | Git-Staging von items.js | high | mitigate | Precondition `git status --short items.js` leer vor jedem Edit; nur `git add items.js`; `git log 0d9ddbe..HEAD -- <andere Produktionsdateien>` leer |
| T-2-08 | Repudiation | `confirm()`-Texte nennen die Konsequenz nicht | low | mitigate | `_LSCG_PROFIL_KOPIEN_HINWEIS` in allen fünf LSCG-Dialogen (awk-Gate je Funktion + `grep -Fn` listet Definition und fünf Dialoge) |
| T-2-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine neuen Installationen in dieser Phase; nur bestehende devDependencies aus Phase 1 |
</threat_model>

<verification>
- `npx vitest run tests/delete-confirmation.test.js` → 22 passed; `npx vitest run tests/delete-consistency.test.js` → 7 passed; `npm test` → gesamte Suite grün.
- `node --check items.js` grün; drei Commits in der Reihenfolge test (RED) → fix (STAB-09) → fix (STAB-10), jeder Fix-Commit enthält nur items.js.
- Manuell (end-of-phase, VALIDATION.md „Manual-Only"): Im Browser 🗑 auf einer Versions-Karte und „✕ Entfernen" im Profil-Modal klicken → Dialog erscheint; Abbrechen → Bild bleibt; Bestätigen → Bild ist in Outfit-Scan UND Profil weg.
- Bekannte, bewusst nicht geänderte Pfade (im SUMMARY festhalten): `LSCG_MAX_VERSIONS = 1500`-Trim beim Scan-Import (Obergrenze pro Member, nicht UI-erreichbar), `enrichProfileNamesWithIDs()` (Umbenennung ohne Screenshot-Mitzug, Orchestrator-Entscheidung 4), `window.repairOsOutfitCode` (Konsolen-Reparatur, nimmt Bild neu auf).
</verification>

<success_criteria>
- Erfolgskriterium 4: Bilder, einzelne Outfits und Outfit-Versionen lassen sich nur mit `confirm()` löschen; `false` lässt alle Stores unverändert; kein anderer Code-Pfad entfernt Scan-Daten (statischer Audit grün).
- Erfolgskriterium 5: Nach bestätigtem Löschen existiert das LSCG-Bild weder in `LSCG_SCREENSHOTS` noch als identische Kopie in `PROFILE_SCREENSHOTS` — im RAM und in IDB; manuelle Profil-Bilder bleiben.
</success_criteria>

<output>
Create `.planning/phases/02-speicher-sicherheit/02-02-SUMMARY.md` when done — inkl. RED-Ausgabe aus Task 1 (welche Pfade/Fälle rot waren), `confirm(`-Zählung vor/nach Task 2, GREEN-Ausgaben, `git show --stat` je Fix-Commit, Planer-Ermessen (`img`-Parameter im Helfer, Wheel-Zweig der Lightbox als sechster Pfad) und der Liste bewusst nicht geänderter Pfade.
</output>
