---
phase: 02-speicher-sicherheit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/idb-helpers.test.js
  - tests/screenshot-sync.test.js
  - items.js
autonomous: true
requirements: [TEST-04, STAB-02, STAB-01]
tags: [indexeddb, quota, screenshot-sync, vm-sandbox, red-green]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Wirft `IDBObjectStore.prototype.put` einen `DOMException` mit `name === 'QuotaExceededError'`, liefert `idbSet(key, value)` `false`, ruft `showStatus(msg, 'error')` genau einmal mit einer Meldung auf, die `Speicher voll` und den Schlüssel enthält, und `idbGet(key)` liefert danach `null` — kein Teilschreibvorgang (STAB-02, TEST-04, Erfolgskriterium 2)"
    - "Ein zweiter Schreibfehler innerhalb von 10 s liefert weiterhin `false`, löst aber keine zweite `showStatus`-Meldung aus; nach Rücksetzen von `_idbFehlerGemeldet` wird wieder gemeldet — der Throttle ist als Ist-Zustand getestet, nicht geändert (Orchestrator-Entscheidung 3)"
    - "Ein Nicht-Quota-Fehler (z. B. `UnknownError`) liefert `false` und meldet `Speichern fehlgeschlagen`; der `idbGet`-Fehlerpfad liefert `null` ohne Exception und ohne UI-Meldung (dokumentierter Ist-Zustand, TEST-04)"
    - "`_syncLscgScreenshotToProfiles(mk, fp)` liest den Screenshot unter demselben Schlüssel `mk + '|' + fp`, unter dem die Aufnahme ihn gespeichert hat (`storeKey`, items.js ~2735), und kopiert ihn in `PROFILE_SCREENSHOTS[k]` für jedes `k` aus `_lscgFpMap[fp]`, das noch kein Bild hat; Legacy-Schlüssel ohne `fp` (`storeKey === mk`) funktionieren unverändert (STAB-01, Erfolgskriterium 1)"
    - "Ein bereits vorhandenes Profil-Bild (manuell hochgeladen) wird vom Sync nie überschrieben — die `if (!PROFILE_SCREENSHOTS[k])`-Bedingung bleibt erhalten (Kernwert: nichts wird überschrieben)"
    - "Vor dem Fix ist `tests/screenshot-sync.test.js` nachweislich rot (RED committet), nach dem Fix grün (GREEN committet) — der Test lockt den Bug wie in Phase 1 (01-03)"
    - "Annahme (STAB-01, Edge): Der Fix ändert nur die Leseseite (Signatur + Schlüsselbildung, `git diff --numstat` = `3 2` für items.js). Die Schleife über `entry.versions` bleibt unverändert, weil sie absichtlich ALLE Profile mit passendem Fingerprint synchronisiert (RESEARCH 'Fix-Ansatz'); das schleifenlokale `const fp = v.fingerprint` schattiert den neuen Parameter legal in einem eigenen Blockscope und wird chirurgisch nicht angefasst"
  artifacts:
    - path: "tests/idb-helpers.test.js"
      provides: "TEST-04: describe 'idbSet/idbGet Fehlerpfade' — Quota, Nicht-Quota, Throttle, Throttle-Reset, idbGet-Fehler, showStatus fehlt; Monkeypatch von IDBObjectStore.prototype.put/get mit afterEach-Restore"
      contains: "QuotaExceededError"
    - path: "tests/screenshot-sync.test.js"
      provides: "STAB-01: Sync liest mk|fp, kopiert zu allen Slots aus _lscgFpMap[fp], überschreibt nie, Legacy-mk-Pfad, unbekannter Schlüssel, Persistenz unter BC_PROFILE_SCREENSHOTS_v1 nach bcSpeichernJetzt()"
      contains: "_syncLscgScreenshotToProfiles"
    - path: "items.js"
      provides: "`function _syncLscgScreenshotToProfiles(mk, fp)` mit `const key = fp ? (mk + '|' + fp) : mk;` als Leseschlüssel"
      contains: "function _syncLscgScreenshotToProfiles(mk, fp)"
  key_links:
    - from: "tests/idb-helpers.test.js"
      to: "items.js idbSet (Zeilen 40-63)"
      via: "`globalThis.IDBObjectStore.prototype.put` wird ersetzt — dieselbe Prototyp-Instanz, die die Sandbox über `indexedDB: globalThis.indexedDB` nutzt (RESEARCH 'Verifizierter Quota-Test-Pattern', live geprüft); `ctx.showStatus = spy` überschreibt die Top-Level-function-Deklaration als Sandbox-Property"
      pattern: "IDBObjectStore.prototype.put"
    - from: "items.js Aufnahme-Callback (~2735-2738, `storeKey` + `_syncLscgScreenshotToProfiles(mk, fp)`)"
      to: "items.js `_syncLscgScreenshotToProfiles(mk, fp)` (~7551)"
      via: "identische Schlüsselbildung `fp ? (mk + '|' + fp) : mk` auf Schreib- und Leseseite"
      pattern: "mk \\+ '\\|' \\+ fp"
    - from: "tests/screenshot-sync.test.js"
      to: "items.js LSCG_DB / LSCG_SCREENSHOTS / _lscgFpMap / PROFILE_SCREENSHOTS"
      via: "alle vier sind Top-Level-`let` → keine Sandbox-Properties → ausschließlich per `evalIn(ctx, …)` setzen und lesen (Phase-1-Pattern)"
      pattern: "evalIn"
  prohibitions:
    - requirement_id: STAB-02
      category: safety
      verification: judgment
      statement: "Kein Produktionsfix für STAB-02 in diesem Plan — `idbSet` bleibt byte-identisch, es sei denn Task 1 wird wider Erwarten rot; dann STOPP, Befund im SUMMARY dokumentieren und den Orchestrator entscheiden lassen (Orchestrator-Entscheidung 1)"
    - requirement_id: STAB-01
      category: safety
      verification: judgment
      statement: "Einzige Produktionsänderung dieses Plans ist `_syncLscgScreenshotToProfiles` in items.js (Signatur + Schlüsselzeile); keine Änderung an index.html, loader.js, bot-*.js, outfit-import.js oder an der Schreibseite (~2735-2738)"
    - requirement_id: STAB-01
      category: safety
      verification: judgment
      statement: "`git add items.js` nur, nachdem `git status --short items.js` VOR dem Edit leer war; nie `git add -A`, `git add .` oder `git commit -a`; Testdateien und Produktionsdatei in getrennten Commits (RED vor GREEN)"
    - requirement_id: TEST-04
      category: safety
      verification: judgment
      statement: "Jeder Monkeypatch an `IDBObjectStore.prototype` wird in `afterEach` restauriert — sonst leckt er in andere Testdateien desselben Vitest-Workers (RESEARCH Pitfall 2)"
    - requirement_id: TEST-04
      category: safety
      verification: judgment
      statement: "Kein Build-Schritt, keine neue Dependency, keine Änderung an vitest.config.js oder tests/helpers/loadScript.js (die Sandbox reicht per `extraGlobals`/Property-Überschreibung aus)"
---

<objective>
Den Kernwert „jede Speicherung ist entweder erfolgreich oder sichtbar fehlgeschlagen" per Regressionstest verankern (TEST-04 für den bereits implementierten STAB-02-Quota-Pfad in `idbSet`) und den verifizierten Screenshot-Sync-Bug (STAB-01: `_syncLscgScreenshotToProfiles` liest `LSCG_SCREENSHOTS[mk]` statt `LSCG_SCREENSHOTS[mk + '|' + fp]`) nach dem Phase-1-Muster RED → GREEN fixen — erst der rote Test, dann die minimale Änderung an der Leseseite.

Purpose: Erfolgskriterium 1 (Screenshot erscheint nach Profil-Sync im Profil) ist heute für jedes Outfit mit Fingerprint unerfüllbar, weil der Sync sofort mit `if (!img) return;` abbricht. Erfolgskriterium 2 verlangt Testabdeckung des Quota-Pfads — `01-REVIEW.md` WR-03 hat genau diese Lücke benannt.
Output: `tests/idb-helpers.test.js` (erweitert), `tests/screenshot-sync.test.js` (neu), `items.js` (`_syncLscgScreenshotToProfiles`, 3 Zeilen rein / 2 raus).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-speicher-sicherheit/02-RESEARCH.md
@.planning/phases/02-speicher-sicherheit/02-VALIDATION.md
@.planning/phases/01-testfundament/01-02-SUMMARY.md

Planer-Probe (2026-09-13, Node v24.12.0, `tests/helpers/loadScript.js` unverändert) — verbindliche Befunde:
1. `loadScript(['items.js'])` + `await ctx.idbGet('warmup')`, dann `globalThis.IDBObjectStore.prototype.put = function () { throw new DOMException('simuliert', 'QuotaExceededError'); }` → `await ctx.idbSet('Q_KEY', {x:1})` liefert `false`; `ctx.showStatus`-Spy erhält genau `{ msg: '❌ Speicher voll – "Q_KEY" wurde NICHT gesichert. Backup anlegen und aufräumen!', type: 'error' }`; nach Restore liefert `ctx.idbGet('Q_KEY')` `null`.
2. `globalThis.IDBObjectStore.prototype.get = function () { throw new DOMException('boom', 'UnknownError'); }` → `ctx.idbGet('Q_KEY')` liefert `null`, wirft nicht (Pattern analog, jetzt ebenfalls live verifiziert).
3. `ctx.showStatus = spy` wirkt sofort auf den nächsten `idbSet`-Aufruf (Top-Level-`function`-Deklaration ist mutierbare Sandbox-Property). `_idbFehlerGemeldet` ist Top-Level-`let` → nur per `evalIn(ctx, '_idbFehlerGemeldet = 0')` rücksetzbar.
4. `LSCG_DB` (8163), `LSCG_SCREENSHOTS` (7499), `_lscgFpMap` (8169), `PROFILE_SCREENSHOTS` (560), `PROFILES` (487) sind alle Top-Level-`let` → im Test ausschließlich per `evalIn` setzen/lesen. `showStatus` ruft in der Sandbox ein echtes `setTimeout(…, 4000)` — in Tests, die Lösch-/Sync-Funktionen aufrufen, `ctx.showStatus` durch einen Spy ersetzen.
5. `bcSpeichernJetzt()` (items.js 546) führt alle per `_sammelSpeicher.plane` geplanten Schreibvorgänge sofort aus (u. a. `_saveProfileScreenshotsJetzt` → `idbSet('BC_PROFILE_SCREENSHOTS_v1', PROFILE_SCREENSHOTS)`); die `idbSet`-Promise wird dabei nicht abgewartet → im Test danach `await settle(100)` vor `idbGet`.
6. `git status --short` ist leer (Arbeitsbaum sauber, HEAD `0d9ddbe`). Ein einfaches `git add items.js` staged daher exakt den Fix-Hunk — Precondition trotzdem vor jedem Produktions-Edit erneut prüfen.
7. `navigator` ist in der Sandbox `undefined` (nur für Plan 03 relevant; hier nicht gebraucht).
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 2 gesamt; Plan-Zuordnung in Klammern)

| Pfad | Symbole / Inhalt | Plan |
|------|------------------|------|
| `tests/idb-helpers.test.js` (erweitert) | describe `idbSet/idbGet Fehlerpfade (TEST-04, STAB-02)`: Quota, Nicht-Quota, Throttle, Throttle-Reset, idbGet-Fehler, fehlendes showStatus; `afterEach`-Restore von `IDBObjectStore.prototype.put/get` | 02-01 |
| `tests/screenshot-sync.test.js` (neu) | STAB-01: `_syncLscgScreenshotToProfiles(mk, fp)` liest `mk\|fp`, kopiert zu allen Slots, überschreibt nie, Legacy-Pfad, unbekannter Schlüssel, Persistenz | 02-01 |
| `items.js` `_syncLscgScreenshotToProfiles(mk, fp)` | Signatur + `const key = fp ? (mk + '\|' + fp) : mk;` — Leseseite gleich Schreibseite | 02-01 |
| `tests/delete-confirmation.test.js` (neu) | STAB-09: `it.each` über alle Lösch-Pfade mit `confirm → false` (nichts gelöscht, `confirm` genau 1×) und `confirm → true` (Ziel entfernt); statischer Quell-Audit: jede `delete …[`/`… = {}`-Zeile der vier Stores liegt in einer Funktion mit `confirm(` oder auf der dokumentierten Ausnahmeliste | 02-02 |
| `tests/delete-consistency.test.js` (neu) | STAB-10: nach bestätigtem Löschen keine identische Kopie mehr in `PROFILE_SCREENSHOTS`; manuelle Profil-Bilder bleiben; Persistenz in IDB nach `bcSpeichernJetzt()` | 02-02 |
| `items.js` `confirm()` in `removeProfileScreenshot`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `mbsWheelDeleteShot`, `deleteOsScreenshotFromLb` (Wheel-Zweig) | STAB-09 | 02-02 |
| `items.js` `_removeLscgScreenshotFromProfiles(fp, img)`, `_removeLscgScreenshotKeyFromProfiles(key)`, `_removeAllLscgScreenshotsFromProfiles()`, `_LSCG_PROFIL_KOPIEN_HINWEIS` + Aufrufe in `deleteLscgVersion`, `deleteOsScreenshot`, `deleteOsScreenshotKey`, `clearAllLscgScreenshots`, `clearAllLscgOutfits` | STAB-10 | 02-02 |
| `tests/storage-estimate.test.js` (neu) | STAB-03: `_speicherFormatBytes`, `_speicherFormat`, `_speicherZeigeStatus` mit `navigator`-Stub per `extraGlobals`, Fallbacks | 02-03 |
| `items.js` `_speicherFormatBytes(n)`, `_speicherFormat(usage, quota)`, `_speicherZeigeStatus()` + Init-Hook | STAB-03 | 02-03 |
| `index.html` Tweaks-Panel-Sektion `📊 Speicher` (`#storageInfo`, Button `🔄 Aktualisieren`) | STAB-03 — einzige index.html-Änderung der Phase, nur Einfügungen | 02-03 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Quota- und Fehlerpfade von idbSet/idbGet testen — `tests/idb-helpers.test.js` erweitern (TEST-04, STAB-02)</name>
  <precondition>`node_modules/vitest` existiert (Phase 1 hat `npm install` ausgeführt; bei frischem Clone zuerst `npm install`); `npm test` ist vor Beginn grün (Phase-1-Stand: 44 passed + 2 expected fail)</precondition>
  <files>tests/idb-helpers.test.js</files>
  <read_first>items.js Zeilen 5-63 (`_idbOpen`, `idbGet`, `idbSet`, `_idbFehlerGemeldet`, Throttle-Kommentar) — nicht mehr; tests/idb-helpers.test.js (gesamt, 30 Zeilen — bestehendes describe bleibt unverändert); tests/helpers/loadScript.js Zeilen 120-160 (`makeSandbox`, `loadScript`, `evalIn`); .planning/phases/02-speicher-sicherheit/02-RESEARCH.md Abschnitte „Verifizierter Ist-Zustand: idbGet/idbSet" und „Verifizierter Quota-Test-Pattern" sowie Pitfall 1 und 2</read_first>
  <action>
    Orchestrator-Entscheidung 1: STAB-02 ist in `idbSet` bereits implementiert — dieser Task liefert ausschließlich den Test. Er MUSS auf Anhieb grün sein. Wird er rot, ist das ein echter Befund: STOPP, Ausgabe ins SUMMARY, nicht eigenmächtig `idbSet` ändern.

    `tests/idb-helpers.test.js`: Import-Zeile auf `import { describe, it, expect, beforeAll, beforeEach, afterEach } from 'vitest';` und `import { loadScript, evalIn } from './helpers/loadScript.js';` erweitern. Das bestehende describe `idbGet/idbSet über die Sandbox (TEST-02, …)` bleibt unverändert. Darunter ein zweites describe `'idbSet/idbGet Fehlerpfade (TEST-04, STAB-02)'`:

    - Modulweit: `const origPut = globalThis.IDBObjectStore.prototype.put;` und `const origGet = globalThis.IDBObjectStore.prototype.get;` (VOR jedem Patch gesichert). Helfer `failPut(name)` und `failGet(name)`: setzen die jeweilige Prototyp-Methode auf `function () { throw new DOMException('simuliert', name); }` — synchroner Throw im Promise-Executor rejected die Promise, das äußere `try/catch` in `idbSet`/`idbGet` fängt ihn (RESEARCH, live geprüft).
    - `beforeAll`: `ctx = loadScript(['items.js'])`, dann `await ctx.idbGet('warmup')` (öffnet die DB einmal, damit `IDBObjectStore` instanziiert ist).
    - `beforeEach`: `calls = []`; `ctx.showStatus = (msg, type) => { calls.push({ msg, type }); }`; `evalIn(ctx, '_idbFehlerGemeldet = 0')` (Pitfall 1 — sonst unterdrückt der 10-s-Throttle jede Meldung nach dem ersten Fall, Testreihenfolge-Unabhängigkeit).
    - `afterEach`: beide Prototyp-Methoden auf `origPut`/`origGet` zurücksetzen (Pitfall 2 — Leck in andere Testdateien desselben Workers).

    Testfälle (Namen genau so):
    1. `'QuotaExceededError: idbSet liefert false, meldet "Speicher voll" per showStatus(…, "error"), kein Teilschreibvorgang'`: `failPut('QuotaExceededError')`; `expect(await ctx.idbSet('TEST_quota', { big: 'x' })).toBe(false)`; `expect(calls).toHaveLength(1)`; `expect(calls[0].type).toBe('error')`; `expect(calls[0].msg).toContain('Speicher voll')`; `expect(calls[0].msg).toContain('TEST_quota')`; Restore per `globalThis.IDBObjectStore.prototype.put = origPut`; `expect(await ctx.idbGet('TEST_quota')).toBeNull()`.
    2. `'Nicht-Quota-Fehler (UnknownError): idbSet liefert false und meldet "Speichern fehlgeschlagen"'`: `failPut('UnknownError')`; Rückgabe `false`; `calls[0].type === 'error'`; `calls[0].msg` enthält `'Speichern fehlgeschlagen'` und `'TEST_unknown'`, enthält NICHT `'Speicher voll'`.
    3. `'Throttle: zweiter Fehler binnen 10 s liefert false, aber keine zweite Meldung (Ist-Zustand, Entscheidung 3)'`: `failPut('QuotaExceededError')`; zwei `idbSet`-Aufrufe hintereinander (`TEST_t1`, `TEST_t2`) → beide `false`; `calls` hat Länge 1 und `calls[0].msg` enthält `'TEST_t1'`.
    4. `'nach Rücksetzen von _idbFehlerGemeldet wird wieder gemeldet'`: `failPut('QuotaExceededError')`; `idbSet('TEST_r1', 1)`; `evalIn(ctx, '_idbFehlerGemeldet = 0')`; `idbSet('TEST_r2', 1)`; `calls` hat Länge 2, `calls[1].msg` enthält `'TEST_r2'`.
    5. `'idbGet-Fehlerpfad: liefert null, wirft nicht, keine UI-Meldung (dokumentierter Ist-Zustand)'`: zuerst regulär `await ctx.idbSet('TEST_get', 42)` (put unpatched), dann `failGet('UnknownError')`; `expect(await ctx.idbGet('TEST_get')).toBeNull()`; `expect(calls).toHaveLength(0)`; nach Restore liefert `idbGet('TEST_get')` wieder `42`.
    6. `'idbSet ohne showStatus-Funktion wirft nicht und liefert false'`: `ctx.showStatus = undefined`; `failPut('QuotaExceededError')`; `expect(await ctx.idbSet('TEST_nostatus', 1)).toBe(false)` (der `typeof showStatus === 'function'`-Guard greift).

    Lauf: `npx vitest run tests/idb-helpers.test.js` → alle Fälle grün (4 bestehende + 6 neue = 10 passed). Dann `npm test` → gesamte Suite grün, keine Folgeschäden durch den Monkeypatch (Beweis für den afterEach-Restore).

    Commit: `git add tests/idb-helpers.test.js`; `git diff --cached --name-only` = exakt dieser Pfad; Message `test(02-01): cover idbSet quota and failure paths (TEST-04, STAB-02)`.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/idb-helpers.test.js && npm test</automated>
    <fails_when>non-zero exit, `failed` in der `Tests`-Summary-Zeile, oder weniger als `10 passed` für tests/idb-helpers.test.js — insbesondere wenn der Quota-Fall rot ist (dann echter STAB-02-Befund → STOPP) oder `npm test` durch einen nicht restaurierten Monkeypatch in anderen Dateien rot wird</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "QuotaExceededError" tests/idb-helpers.test.js` >= 3; `grep -c "UnknownError" tests/idb-helpers.test.js` >= 2
    - `grep -c "IDBObjectStore.prototype.put" tests/idb-helpers.test.js` >= 2 und `grep -c "IDBObjectStore.prototype.get" tests/idb-helpers.test.js` >= 2 (Patch + Restore je Methode)
    - `grep -c "afterEach" tests/idb-helpers.test.js` >= 2 (Import + Hook)
    - `grep -c "_idbFehlerGemeldet" tests/idb-helpers.test.js` >= 2 (Reset in beforeEach + Throttle-Reset-Fall)
    - `grep -c "Speicher voll" tests/idb-helpers.test.js` >= 2; `grep -c "Speichern fehlgeschlagen" tests/idb-helpers.test.js` >= 1
    - `grep -c "toBeNull()" tests/idb-helpers.test.js` >= 3 (bestehender Fall + Kein-Teilschreibvorgang + idbGet-Fehler)
    - `test "$(grep -v '^\s*//' tests/idb-helpers.test.js | grep -c '\.only(')" = 0`
    - `npx vitest run tests/idb-helpers.test.js` → `10 passed`; `npm test` → Exit 0
    - `git status --short items.js` gibt nichts aus (keine Produktionsänderung in diesem Task)
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/idb-helpers.test.js`
  </acceptance_criteria>
  <done>Sechs neue Fehlerpfad-Tests grün auf Anhieb (STAB-02 bestätigt implementiert), Monkeypatches restauriert (volle Suite grün), Commit enthält nur die Testdatei; items.js unverändert.</done>
</task>

<task type="auto">
  <name>Task 2: Screenshot-Sync-Test schreiben und RED bestätigen — `tests/screenshot-sync.test.js` (STAB-01)</name>
  <files>tests/screenshot-sync.test.js</files>
  <read_first>items.js Zeilen 2731-2739 (Schreibseite: `storeKey`, `_syncLscgScreenshotToProfiles(mk, fp)`) und 7550-7569 (`_syncLscgScreenshotToProfiles(mk)` — Leseseite, der Bug) — per `grep -n "function _syncLscgScreenshotToProfiles" items.js` lokalisieren, nicht auf Zeilennummern verlassen; items.js Zeilen 560-565 (`PROFILE_SCREENSHOTS`, `_saveProfileScreenshots`, Schlüssel `BC_PROFILE_SCREENSHOTS_v1`) und 546 (`bcSpeichernJetzt`); .planning/phases/02-speicher-sicherheit/02-RESEARCH.md Abschnitt „STAB-01: Screenshot-Sync-Bug"; tests/bot-data-validators.test.js (Stil für evalIn-Fixtures)</read_first>
  <action>
    `tests/screenshot-sync.test.js` (neu, ESM): `import { describe, it, expect, beforeAll, beforeEach } from 'vitest'`, `import { loadScript, evalIn, settle } from './helpers/loadScript.js'`.

    `describe('_syncLscgScreenshotToProfiles liest denselben Schlüssel wie die Aufnahme (STAB-01)')`:
    - `beforeAll`: `ctx = loadScript(['items.js'])`; `ctx.showStatus = () => {}`; `expect(typeof ctx._syncLscgScreenshotToProfiles).toBe('function')`.
    - Helfer `setState(ctx, { db, shots, fpMap, profShots })`: ein `evalIn(ctx, …)`-Aufruf, der die vier Top-Level-`let`-Globals per Zuweisung neu setzt: `LSCG_DB = <JSON>; LSCG_SCREENSHOTS = <JSON>; _lscgFpMap = <JSON>; PROFILE_SCREENSHOTS = <JSON>;` (JSON per `JSON.stringify` einbetten — alle vier sind `let`, daher keine Sandbox-Properties, Befund 4). Helfer `profShots(ctx)` = `evalIn(ctx, 'JSON.parse(JSON.stringify(PROFILE_SCREENSHOTS))')`.
    - `beforeEach`: Standard-Fixture: `LSCG_DB = { '123': { name: 'Anna', versions: [{ code: 'c1', fingerprint: 'FP1', ts: 1 }] } }`, `LSCG_SCREENSHOTS = { '123|FP1': 'data:img1' }`, `_lscgFpMap = { FP1: ['Anna_v1', 'Anna_v1b'] }`, `PROFILE_SCREENSHOTS = {}`.

    Testfälle:
    1. `'kopiert das unter mk|fp gespeicherte Bild in alle Profil-Slots des Fingerprints'`: `ctx._syncLscgScreenshotToProfiles('123', 'FP1')`; `expect(profShots(ctx)).toEqual({ Anna_v1: 'data:img1', Anna_v1b: 'data:img1' })`. — VOR dem Fix rot (Funktion liest `LSCG_SCREENSHOTS['123']` → undefined → Return, Ergebnis `{}`).
    2. `'überschreibt ein vorhandenes Profil-Bild nie (manuelle Uploads bleiben)'`: Fixture-Variante mit `PROFILE_SCREENSHOTS = { Anna_v1: 'data:manual' }`; nach Sync: `Anna_v1 === 'data:manual'`, `Anna_v1b === 'data:img1'`. — VOR dem Fix rot (Anna_v1b bleibt leer).
    3. `'Legacy-Schlüssel ohne Fingerprint (storeKey === mk) funktioniert weiterhin'`: `LSCG_SCREENSHOTS = { '123': 'data:legacy' }`; `ctx._syncLscgScreenshotToProfiles('123', null)`; beide Slots = `'data:legacy'`. — vor und nach dem Fix grün (Regressionsschutz).
    4. `'unbekannter Schlüssel: keine Änderung, keine Exception'`: `expect(() => ctx._syncLscgScreenshotToProfiles('999', 'FPX')).not.toThrow()`; `profShots(ctx)` = `{}`.
    5. `'Sync persistiert nach bcSpeichernJetzt() unter BC_PROFILE_SCREENSHOTS_v1'`: Sync wie Fall 1; `ctx.bcSpeichernJetzt()`; `await settle(100)`; `const saved = await ctx.idbGet('BC_PROFILE_SCREENSHOTS_v1')`; `expect(saved?.Anna_v1).toBe('data:img1')`. — VOR dem Fix rot (kein `changed`, kein Save).
    6. `'Schreibseite und Leseseite bilden den Schlüssel identisch (statischer Quell-Check)'`: `fs.readFileSync('items.js')` (Pfad über `REPO_ROOT` aus dem Loader; `import { REPO_ROOT }` ergänzen); Funktionskörper von `_syncLscgScreenshotToProfiles` extrahieren (von `function _syncLscgScreenshotToProfiles(` bis zur nächsten Zeile, die mit `}` beginnt); `expect(body).toMatch(/mk \+ '\|' \+ fp/)`; `expect(body).toMatch(/function _syncLscgScreenshotToProfiles\(mk, fp\)/)`. — VOR dem Fix rot.

    RED bestätigen: `npx vitest run tests/screenshot-sync.test.js` MUSS rot sein — erwartet: Fälle 1, 2, 5, 6 fehlschlagend, Fälle 3 und 4 grün. Ist die Datei unerwartet GRÜN, ist der Fix schon drin oder das Fixture setzt die Globals nicht (typischer Fehler: direkte Property-Zuweisung statt `evalIn`) — dann STOPP und prüfen.

    Commit des roten Tests: `git add tests/screenshot-sync.test.js`; `git diff --cached --name-only` = exakt dieser Pfad; Message `test(02-01): add failing test for screenshot sync key mk|fp (STAB-01)`. Vorher `git status --short items.js` prüfen und im SUMMARY festhalten (muss leer sein — Voraussetzung für Task 3).
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && OUT=$(mktemp) && npx vitest run tests/screenshot-sync.test.js > "$OUT" 2>&1; RC=$?; test "$RC" -ne 0 && grep -q "Anna_v1" "$OUT" && grep -Eq "Tests +[0-9]+ failed" "$OUT" && echo RED-CONFIRMED</automated>
    <fails_when>`RED-CONFIRMED` wird nicht ausgegeben — Vitest endete mit Exit 0 (Suite unerwartet grün: Fix bereits vorhanden oder Fixture wirkungslos), oder `Anna_v1`/`failed` fehlen in der Ausgabe</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "_syncLscgScreenshotToProfiles" tests/screenshot-sync.test.js` >= 5
    - `grep -c "evalIn" tests/screenshot-sync.test.js` >= 3 (Import, setState, profShots); `grep -c "'123|FP1'" tests/screenshot-sync.test.js` >= 1
    - `grep -c "data:manual" tests/screenshot-sync.test.js` >= 2; `grep -c "data:legacy" tests/screenshot-sync.test.js` >= 2
    - `grep -c "BC_PROFILE_SCREENSHOTS_v1" tests/screenshot-sync.test.js` >= 1; `grep -c "bcSpeichernJetzt" tests/screenshot-sync.test.js` >= 1
    - `grep -c "REPO_ROOT" tests/screenshot-sync.test.js` >= 2 (Import + Nutzung im statischen Check)
    - `test "$(grep -v '^\s*//' tests/screenshot-sync.test.js | grep -c '\.only(')" = 0`
    - `git status --short items.js` gibt nichts aus (Voraussetzung für Task 3 dokumentiert)
    - Nach dem Commit: `git show --name-only --format= HEAD` = `tests/screenshot-sync.test.js`
  </acceptance_criteria>
  <done>Sechs Testfälle vorhanden; Lauf nachweislich rot in genau den vier Bug-Fällen; roter Test committet; items.js noch unverändert und im Arbeitsbaum sauber.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Leseseite von `_syncLscgScreenshotToProfiles` auf `mk|fp` umstellen, GREEN bestätigen, chirurgisch committen (STAB-01)</name>
  <precondition>`git status --short items.js` gibt VOR dem Edit nichts aus — sonst STOPP: `git diff items.js` inspizieren und an den Nutzer eskalieren statt zu committen (fremde Hunks würden sonst mitgestaged)</precondition>
  <reversibility rating="reversible">Drei Zeilen in einer Funktion; Revert per `git revert` ohne Datenmigration; Schreibseite und gespeicherte Schlüssel bleiben unverändert — der Fix liest nur zusätzlich den richtigen Schlüssel.</reversibility>
  <files>items.js</files>
  <read_first>items.js `_syncLscgScreenshotToProfiles` (per `grep -n "function _syncLscgScreenshotToProfiles" items.js`, dann 20 Zeilen ab Treffer lesen — exakt vergleichen, bevor editiert wird); items.js Zeile mit `const storeKey = fp ? (mk + '|' + fp) : mk;` (per `grep -n "const storeKey = fp" items.js`) als Vorbild der Schlüsselbildung; tests/screenshot-sync.test.js (aus Task 2)</read_first>
  <behavior>
    - Test 1: `_syncLscgScreenshotToProfiles('123', 'FP1')` kopiert `LSCG_SCREENSHOTS['123|FP1']` nach `PROFILE_SCREENSHOTS['Anna_v1']` und `['Anna_v1b']`
    - Test 2: vorhandenes `PROFILE_SCREENSHOTS['Anna_v1'] = 'data:manual'` bleibt, `Anna_v1b` wird gefüllt
    - Test 3: Legacy `LSCG_SCREENSHOTS['123']` mit `fp = null` synchronisiert wie bisher
    - Test 4: unbekannter Schlüssel → keine Änderung, keine Exception
    - Test 5: nach `bcSpeichernJetzt()` liegt der Slot unter `BC_PROFILE_SCREENSHOTS_v1` in IDB
    - Test 6: Funktionskörper enthält `mk + '|' + fp` und die Signatur `(mk, fp)`
  </behavior>
  <action>
    1. `git status --short items.js` → leer (Precondition). Funktionskopf lokalisieren und exakt vergleichen — die ersten drei Zeilen der Funktion müssen lauten:
       `function _syncLscgScreenshotToProfiles(mk) {`
       `  const img = LSCG_SCREENSHOTS[mk];`
       `  if (!img) return;`
       Weicht das ab, STOPP.

    2. Per Edit-Tool (exakter old_string = die ersten zwei Zeilen aus Schritt 1) ersetzen durch:
       `function _syncLscgScreenshotToProfiles(mk, fp) {`
       `  const key = fp ? (mk + '|' + fp) : mk;`
       `  const img = LSCG_SCREENSHOTS[key];`
       — Signatur nimmt das zweite Argument an, das der Aufrufer (`_syncLscgScreenshotToProfiles(mk, fp)` im Aufnahme-Callback) bereits übergibt; Schlüsselbildung identisch zur Schreibseite (`storeKey`). Der Rest der Funktion (Schleife über `entry.versions`, `_lscgFpMap[fp]`, `if (!PROFILE_SCREENSHOTS[k])`, `_saveProfileScreenshots()`) bleibt byte-identisch — die Schleife synchronisiert absichtlich alle Fingerprints des Members (RESEARCH „Fix-Ansatz"), und das schleifenlokale `const fp = v.fingerprint` schattiert den Parameter legal in seinem Blockscope; NICHT umbenennen (chirurgischer Fix, keine Umformatierung, Datei bleibt LF).

    3. Prüfen: `git diff --numstat items.js` → `3	2	items.js`; `node --check items.js` → Exit 0.

    4. GREEN: `npx vitest run tests/screenshot-sync.test.js` → 6 passed. Dann `npm test` → gesamte Suite grün (Phase-1-Tests + Task 1 + Task 2).

    5. Commit — ausschließlich `git add items.js` (der Test ist bereits in Task 2 committet). `git diff --cached --numstat` → genau `3	2	items.js`. Message: `fix(02-01): read mk|fp key in _syncLscgScreenshotToProfiles (STAB-01)`. Die Warnung `LF will be replaced by CRLF` ist kosmetisch (core.autocrlf), kein Fehler.

    6. Nachweis für das SUMMARY: `git show --stat --format= HEAD` (nur items.js, 3 insertions, 2 deletions); RED-Ausgabe aus Task 2 (welche vier Fälle vor dem Fix rot waren) und GREEN-Ausgabe zitieren.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && npx vitest run tests/screenshot-sync.test.js && node --check items.js && npm test</automated>
    <fails_when>non-zero exit (Vitest rot oder `node --check` SyntaxError), `failed` in der `Tests`-Summary-Zeile, oder weniger als `6 passed` für tests/screenshot-sync.test.js</fails_when>
    <automated>cd "C:/Users/andre/Documents/GitHub/bc-universal-configurator" && test "$(git show --numstat --format= HEAD | awk '{print $1"/"$2"/"$3}')" = "3/2/items.js" && test "$(grep -Fc "function _syncLscgScreenshotToProfiles(mk, fp)" items.js)" = 1 && echo SURGICAL-OK</automated>
    <fails_when>`SURGICAL-OK` wird nicht ausgegeben — der HEAD-Commit enthält mehr als items.js oder mehr als 3/2 Zeilen, oder die neue Signatur fehlt bzw. existiert mehrfach</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -Fc "function _syncLscgScreenshotToProfiles(mk, fp)" items.js` = 1 und `grep -Fc "function _syncLscgScreenshotToProfiles(" items.js` = 1 (genau eine Definition, und zwar die neue Signatur)
    - `grep -F -A2 "function _syncLscgScreenshotToProfiles(mk, fp)" items.js | grep -Fc "const key = fp ? (mk + '|' + fp) : mk;"` = 1; `grep -F -A3 "function _syncLscgScreenshotToProfiles(mk, fp)" items.js | grep -Fc "LSCG_SCREENSHOTS[key]"` = 1
    - `grep -c "_syncLscgScreenshotToProfiles(mk, fp);" items.js` = 1 (Schreibseiten-Aufruf unverändert)
    - `node --check items.js` → Exit 0; `npx vitest run tests/screenshot-sync.test.js` → `6 passed`; `npm test` → Exit 0
    - Nach dem Commit: `git show --name-only --format= HEAD` = `items.js`; `git show --numstat --format= HEAD | awk '{print $1"/"$2"/"$3}'` = `3/2/items.js`; `git status --short` ist leer
    - `git log --format=%h 0d9ddbe..HEAD -- index.html loader.js bot-data.js bot-ui.js bot-engine.js outfit-import.js` gibt nichts aus
  </acceptance_criteria>
  <done>Leseseite bildet den Schlüssel wie die Schreibseite; Sync-Test grün (6 passed), volle Suite grün, `node --check` grün; Commit enthält ausschließlich items.js mit 3/2 geänderten Zeilen; RED-Commit liegt vor dem GREEN-Commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool → IndexedDB (`idbSet`) | Schreibvorgang kann durch Quota/Transaktionsfehler scheitern; der Nutzer muss das sehen, sonst läuft das Tool weiter, als wäre gesichert (Kernwert) |
| Aufnahme-Callback → `_syncLscgScreenshotToProfiles` → `PROFILE_SCREENSHOTS` | RAM-zu-RAM-Kopie zwischen zwei Stores; falscher Schlüssel bedeutet stilles Nichtstun, überschreibender Sync würde manuelle Uploads zerstören |
| Test-Monkeypatch → `IDBObjectStore.prototype` (Host-Realm) | Patch ist global für den Vitest-Worker; ohne Restore beeinflusst er fremde Testdateien |
| Git-Arbeitsbaum → Repository | Produktionsänderung muss isoliert committet werden (nur items.js, nur der Fix-Hunk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Repudiation | items.js `idbSet` (Zeilen 40-63) — stiller Schreibfehler | high | mitigate | Task 1: Quota- und Nicht-Quota-Fehler liefern `false` und lösen `showStatus(…, 'error')` aus; kein Teilschreibvorgang (`idbGet` → `null`); Throttle-Verhalten explizit als Ist-Zustand getestet; Wird der Test rot → STOPP statt eigenmächtigem Fix |
| T-2-04 | Tampering | items.js `_syncLscgScreenshotToProfiles` → `PROFILE_SCREENSHOTS` | medium | mitigate | Task 2/3: Sync liest den korrekten Schlüssel; `if (!PROFILE_SCREENSHOTS[k])`-Guard bleibt und ist per Test 2 (`data:manual` bleibt) abgesichert — kein Überschreiben manueller Uploads |
| T-2-05 | Denial of Service | tests/idb-helpers.test.js Monkeypatch `IDBObjectStore.prototype.put/get` | medium | mitigate | `afterEach`-Restore auf gesicherte Originale; `npm test` nach Task 1 beweist, dass andere Dateien nicht betroffen sind |
| T-2-06 | Tampering | Git-Staging von items.js | high | mitigate | Precondition `git status --short items.js` leer; `git diff --numstat` = 3/2; nur `git add items.js`; `git log 0d9ddbe..HEAD -- <andere Produktionsdateien>` leer |
| T-2-SC | Tampering | npm/pip/cargo installs | high | mitigate | Keine neuen Installationen in dieser Phase (RESEARCH „Package Legitimacy Audit": keine Pakete); nur bestehende devDependencies aus Phase 1 (package-lock.json) |
</threat_model>

<verification>
- `npx vitest run tests/idb-helpers.test.js` → 10 passed; `npx vitest run tests/screenshot-sync.test.js` → 6 passed; `npm test` → gesamte Suite grün.
- `node --check items.js` grün; `git show --numstat --format= HEAD` (GREEN-Commit) = `3 2 items.js`.
- `git log --oneline` zeigt drei Commits dieses Plans in der Reihenfolge test (TEST-04) → test (STAB-01 RED) → fix (STAB-01 GREEN).
- Bekannter, bewusst nicht geänderter Nachbar-Befund (Orchestrator-Entscheidung 4): `enrichProfileNamesWithIDs()` (items.js ~6743-6813) baut `PROFILES` neu, migriert aber `PROFILE_SCREENSHOTS`-Schlüssel nicht mit — kein Datenverlust (Bild bleibt unter altem Schlüssel), UX-Lücke; im SUMMARY als „Deferred" festhalten, nicht fixen.
</verification>

<success_criteria>
- Erfolgskriterium 2 (Testteil): Quota-Pfad von `idbSet` und die `idbGet`/`idbSet`-Helfer sind per Test abgedeckt; STAB-02 ist als bereits implementiert bestätigt (Test auf Anhieb grün).
- Erfolgskriterium 1: `_syncLscgScreenshotToProfiles(mk, fp)` liest denselben Schlüssel `mk|fp` wie die Aufnahme; RED→GREEN dokumentiert; einzige Produktionsänderung sind 3/2 Zeilen in items.js.
</success_criteria>

<output>
Create `.planning/phases/02-speicher-sicherheit/02-01-SUMMARY.md` when done — inkl. Task-1-Ausgabe (Beweis, dass der Quota-Test auf Anhieb grün war), RED-Ausgabe aus Task 2 (vier rote Fälle), GREEN-Ausgabe aus Task 3, `git show --numstat HEAD` und dem Deferred-Hinweis zu `enrichProfileNamesWithIDs()`.
</output>
